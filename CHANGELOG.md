## v21.97 新成就「鸿运当头」（掉落线第二档里程碑）——累计 5 次之外的幸运纪念（新内容·单成就，承 v21.91 长明不熄 / v21.93 驱雾百战 / v21.95 彻夜长明 多档先例；选题轮换：v21.91 新成就、v21.92 体验打磨、v21.93 新成就、v21.94 体验打磨、v21.95 新成就、v21.96 体验打磨后本轮回新成就——成就版图逐线核对后各线都有印记，但多档线并不齐：等级线 5/10/12 三档、宝箱线 6/12 两档、讨伐线 10/100 两档、时长线 60/120 两档，唯独战斗掉落线（lucky 幸运眷顾=累计 5 次额外掉落）只有第一档——练到 Lv12/刷满图鉴的玩家（全程战斗 30-100 场，rollDrop 约 38% 触发额外掉落，累计掉落普遍 10-40 次）在「天降横财」处毫无第二枚回应；现补第二档（LUCKY2_GOAL=30 次），承 等级三档/宝箱两档/讨伐两档/时长两档 多档先例；开工摸底：基线 git 干净树（最新提交 3b7c1fe，v21.96）、基线 npm test 九十二件套全绿、并发检查双测 mtime 无变化、纯新增成就零战斗/数值接触无需生存模拟——故本轮选新内容而非平衡）

- 【新成就】`js/data.js`：掉落线第二档阈值补 `LUCKY2_GOAL = 30`（与 LUCKY_GOAL 同一「成就阈值数据化」家族——判定/描述/进度三处同读一份源，改门槛只改 data.js 一处自动跟随，后续加第三档只增常量不再动条目）+ ACH_LIST 末尾追加 `{id:'lucky2', name:'鸿运当头', d:`累计获得 ${LUCKY2_GOAL} 次额外掉落`, ok:g=>(g.drops||0)>=LUCKY2_GOAL, prog:g=>`${g.drops||0}/${LUCKY2_GOAL}`}`（35→36 项；无 r 字段纯里程碑，承 memoir/lvl5/hardtrue 惯例——运气本身就是奖励）。关键设计：判定读**既有** `hero.drops`（rules.rollDrop 五档唯一写入点、随存档快照持久化、图鉴页脚「额外掉落：N」同读一份源）——`(g.drops||0)` 防御式读取，旧档无此字段=0 不误解锁、零迁移（承 v19.41 seen 同款）；不设新计数/新状态/新判定点：drops 是持续累积量，applyAchievements 既有通路（胜利/开箱/酿造/进图/买甲任意下一次判定点）当场解锁，反馈不迟到，承 lvl12 同款。
- 【零回归面】未动既有 35 个成就任何字段（lucky2 追加在末尾、displayOrder 不变）；未动 hero.drops 写入（rules.js rollDrop 五档 `(hero.drops||0)+1` 逐字未动）/胜利结算/掉落档位/图鉴页脚；未动存档格式（drops 随既有存档快照自动落盘/读回、旧档防御式读取）。只改 1 个游戏文件（`js/data.js`：版本号 v21.96→v21.97 + 版本注释 + LUCKY2_GOAL 常量 + 成就条目 + 导出）+ `CHANGELOG.md`（本条）+ `package.json`（test 串第 93 份）+ `README.md`（tests 树收录 smoke_v2197_lucky2 + 冒烟九十三件套（九十二件套清除）+ v21.97 守护描述 + C 键行「36 项」/图鉴&成就行「36 项成就」双处）+ 新增 `tests/smoke_v2197_lucky2.mjs`（仓库常驻，承 v21.10-v21.96 冒烟入库先例）+ 顺带把 v21.97 随新现实更新的姊妹 pin 一并落位：smoke_v2196..v2176 的 README 件套 pin（九十二→九十三件套，共 21 件）、smoke_v2196..v2179 的 GAME_VERSION 字面量 pin（v21.96→v21.97，共 17 件）、smoke_v2196..v2192 的 GAME_VERSION 恒等 pin（===v21.96→===v21.97，共 5 件）、smoke_v2195..v2159 的 README 成就 pin（35→36 双处，共 12 件）、smoke_v2195 的 ACH_LIST 精确计数断言去硬化（===35→>=35 存活性口径，承 v21.95 对 v2193 同款先例）。
- 【验证】`node --check js/data.js tests/smoke_v2197_lucky2.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 九十三件套全绿（既有 92 件逐项零回归——含 v2196..v2176 更新后件套 pin、v2196..v2179 更新后 GAME_VERSION pin、v2196..v2192 更新后恒等 pin、v2195..v2159 更新后成就 pin 与 v2195 去硬化断言—— + 新增 v21.97 全过——GAME_VERSION 格式合法且越过 v21.96、data.js 含 v21.97 注释与版本字面量并保留 v21.96 历史注释、LUCKY2_GOAL===30 且导出、ACH_LIST 36 项精确 + lucky2 唯一/无 r/描述由常量派生/ok 与 prog 同读 (g.drops||0)、既有 35 成就 id 零回归 + lucky2 末尾序位、ok/prog 谓词逐值（0·29 false / 30·60 true / 缺字段 false·0/30 防御 / prog 15/30·30/30·60/30 不钳制）、rules.js 源级落位（五档写入点 `hero.drops = (hero.drops||0)+1` 恰 5 处 + core.js newGame `drops:0` 起始）、运行期五档（drops=30 真实解锁落 hero.ach / 29 不误报 / 新档 0 不误报 / 重复去重 / 缺 drops 旧档不抛错不解锁）、drawAch 36 项+末页滚动渲染不抛错、README/package/CHANGELOG 同步、姊妹件套 pin 复查（v2196..v2176 九十三件套 + v2196..v2179 GAME_VERSION v21.97 + v2196..v2192 恒等 v21.97 + 旧代 v21.96 字面量 pin 全库零残留 + 旧代 35 项 README 正向 pin 全库零残留））。

## v21.96 阵亡画面补收集进度三件套——倒下的这一刻，成就/图鉴/宝箱收到哪了一览无余（体验打磨·信息透明·纯显示，承 v21.79 标题预览 / v21.82 胜利画面 / v21.87 尾声战绩页 / v21.94 状态页 同一「收集进度三件套」主线：四屏齐备后逐屏核对 run 战果口径屏，阵亡画面（drawDead）是唯一仍缺 成就/图鉴/宝箱 的屏——它有 等级/金币/累计讨伐/时长 战绩行（v19.x）与 余粮行（v19.88），但玩家倒在强敌面前要判断「这趟值不值得 B 重整旗鼓 / R 重开新档」时，收集三件套一眼看不到；选题轮换：v21.91 新成就、v21.92 体验打磨、v21.93 新成就、v21.94 体验打磨、v21.95 新成就后本轮回体验打磨——收集三件套主线在标题预览/胜利画面/尾声/状态页逐屏收官后的最后一块拼图；开工摸底：基线 git 干净树（最新提交 5a8d583，v21.95）、基线 npm test 九十一件套全绿、并发检查双测 mtime 无变化、纯显示零结算零数值零存档故无需生存模拟——故本轮选体验打磨而非平衡/新内容）

- 【显示补齐】`js/view/menus.js` drawDead：建议行（y=292）之后、R 提示（y=332）之前新增一行 `🏆 成就 N/M · 📕 图鉴 X/M · 📦 宝箱 Y/M`（CV.width/2,312,'bold 13px','#7dd47f'——与 drawWin/drawEnding 收集行同式同色，行间 20px ≥16 不触、其余行零位移）+ 两行派生（`BESTIARY_TARGET.filter((n) => ((hero.bestiary || {})[n] | 0) >= 1).length` 与 `chestCount(hero)`，与 slotPreview/drawWin/drawEnding 逐字同式）+ 注释；分母同读 ACH_LIST.length / BESTIARY_TARGET.length / chestTotal() 一份单一数据源（chestCount 三形态防御式、|0 归一旧布尔 bestiary 零迁移），纯显示零结算零存档变化。
- 【零回归面】未动 drawDead 既有任何行（战绩行 236 / 余粮行 264 / 建议行 292 / R·T 提示 332·362 / B 提示 392 逐字未动、零位移）；未动 hero.bestiary/ach/chests 任何写入（battle.startBattle 记 seen、winBattle 记 bestiary、applyAchievements 落 ach、world 开箱记 chests 逐字未动）；未动 drawWin/drawEnding/drawStatus/slotPreview 四处三件套；未动存档格式（bestiary/ach/chests 随既有存档快照自动落盘/读回、防御式读取）。只改 2 个游戏文件（`js/data.js`：GAME_VERSION v21.95→v21.96 + 版本注释；`js/view/menus.js`：drawDead 两行派生 + 一行绘制 + 注释）+ `CHANGELOG.md`（本条）+ `package.json`（test 串第 92 份）+ `README.md`（tests 树收录 smoke_v2196_deadrecap + 冒烟九十二件套（九十一件套清除）+ v21.96 守护描述 + 系统清单「战败复盘」段补收集三件套口径）+ 新增 `tests/smoke_v2196_deadrecap.mjs`（仓库常驻，承 v21.10-v21.95 冒烟入库先例）+ 顺带把 v21.96 随新现实更新的姊妹 pin 一并落位：smoke_v2195..v2176 的 README 件套 pin（九十一→九十二件套，共 20 件）、smoke_v2195..v2179 的 GAME_VERSION 字面量 pin（v21.95→v21.96，共 16 件）、smoke_v2194..v2192 的 GAME_VERSION 恒等 pin（===v21.95→===v21.96，共 4 件）。
- 【验证】`node --check js/data.js js/view/menus.js tests/smoke_v2196_deadrecap.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 九十二件套全绿（既有 91 件逐项零回归——含 v2195..v2176 更新后件套 pin、v2195..v2179 更新后 GAME_VERSION pin、v2194..v2192 更新后恒等 pin—— + 新增 v21.96 全过——GAME_VERSION 格式合法且越过 v21.95、data.js 含 v21.96 注释与版本字面量并保留 v21.95 历史注释、menus.js 源级落位（drawDead 派生与 slotPreview/drawWin/drawEnding 同式/收集行落位 y=312 且既有行逐字零位移/旧行 y 坐标零残留）、运行期三档（推进档 成就 1/35·图鉴 2/13·宝箱 2/12 逐值 / 缺字段防御档 0/35·0/13·0/12 不抛错 / Boss 战败档 _bossRetry 分支零回归 + 三件套行共存）、行宽 estW ≤640 与行间 20px 预算、README/package/CHANGELOG 同步、姊妹件套 pin 复查（v2195..v2176 九十二件套 + v2195..v2179 GAME_VERSION v21.96 + v2194..v2192 恒等 v21.96 + 旧代 v21.95 字面量 pin 全库零残留））。

## v21.95 新成就「彻夜长明」（游玩时长第二档里程碑）——时长线 60 分钟档之后的第二枚印记（新内容·单成就，承 v21.91 长明不熄第一档 / v21.84 灯燃长夜 / v21.86 灵药初成 / v21.93 驱雾百战 单成就先例；选题轮换：v21.91 新成就、v21.92 体验打磨、v21.93 新成就、v21.94 体验打磨后本轮回新成就——v21.91 成就版图逐线核对后各线都有印记，但多档线并不齐：等级线 5/10/12 三档、宝箱线 6/12 两档、讨伐线 10/100 两档，唯独时长线（ptime 长明不熄=60 分钟）只有第一档——通关全流程/补图鉴/练 Lv12 的玩家（累计游玩普遍 1-2 小时）在「从一而终」处毫无第二枚回应；现补第二档（PLAY_TIME2_GOAL=7200 秒=120 分钟），承 等级三档/宝箱两档/讨伐两档 多档先例；开工摸底：基线 git 干净树（最新提交 85afc18，v21.94）、基线 npm test 九十件套全绿、并发检查双测 mtime 无变化、纯新增成就零战斗/数值接触无需生存模拟——故本轮选新内容而非平衡）

- 【新成就】`js/data.js`：时长线第二档阈值补 `PLAY_TIME2_GOAL = 7200`（120 分钟；与 PLAY_TIME_GOAL / ELIXIR_GOAL / HUNT2_GOAL 同一「成就阈值数据化」家族——判定/描述/进度三处同读一份源，改门槛只改 data.js 一处自动跟随，零裸字面量）+ ACH_LIST 末尾追加 `{id:'ptime2', name:'彻夜长明', d:`累计游玩 ${PLAY_TIME2_GOAL / 60} 分钟`, ok:g=>(g.time||0)>=PLAY_TIME2_GOAL, prog:g=>`${Math.floor((g.time||0)/60)}/${PLAY_TIME2_GOAL/60}`}`（34→35 项；无 r 字段纯里程碑，承 memoir/lvl5/hardtrue 惯例——守着灯的时间本身就是奖励；prog 分钟数与 ptime 同族「X/N 不钳制」口径）。关键设计：判定读**既有** `hero.time`（main.js tick 逐秒累计、随存档快照持久化、状态 I/尾声/战绩页/标题预览 ⏱ 同读一份源）——`(g.time||0)` 防御式读取，旧档无此字段=0 不误解锁、零迁移（承 v19.41 seen 同款）；不设新计数/新状态/新判定点：time 是持续累积量，applyAchievements 既有通路（胜利/开箱/酿造/进图/买甲任意下一次判定点）当场解锁，反馈不迟到，承 lvl12 同款。
- 【零回归面】未动既有 34 个成就任何字段（ptime2 追加在末尾、displayOrder 不变）；未动 hero.time 写入/格式化（fmtTime）/HUD/状态页/战绩页/标题预览任何显示；未动存档格式（time 随既有存档快照自动落盘/读回、旧档防御式读取）。只改 1 个游戏文件（`js/data.js`：版本号 v21.94→v21.95 + 版本注释 + PLAY_TIME2_GOAL 常量 + 成就条目 + 导出）+ `CHANGELOG.md`（本条）+ `package.json`（test 串第 91 份）+ `README.md`（tests 树收录 smoke_v2195_ptime2 + 冒烟九十一件套（九十件套清除）+ v21.95 守护描述 + C 键行「35 项」/图鉴&成就行「35 项成就」双处）+ 新增 `tests/smoke_v2195_ptime2.mjs`（仓库常驻，承 v21.10-v21.94 冒烟入库先例）+ 顺带把 v21.95 随新现实更新的姊妹 pin 一并落位：smoke_v2194..v2176 的 README 件套 pin（九十→九十一件套，共 19 件）、smoke_v2194..v2179 的 GAME_VERSION 字面量 pin（v21.94→v21.95，共 15 件）、smoke_v2193..v2159 的 README 成就 pin（34→35 双处，共 11 件，含嵌套复查链）、smoke_v2192/v2193 的 README tests 树尾部 pin（尾条目随新现实更新）、smoke_v2193 的 ACH_LIST 精确计数断言去硬化（===34→>=34 存活性口径，承 v21.93 对 v2191 同款先例）。
- 【验证】`node --check js/data.js tests/smoke_v2195_ptime2.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 九十一件套全绿（既有 90 件逐项零回归——含 v2194..v2176 更新后件套 pin、v2194..v2179 更新后 GAME_VERSION pin、v2193..v2159 更新后成就 pin、v2192/v2193 更新后树尾 pin 与 v2193 去硬化断言—— + 新增 v21.95 全过——GAME_VERSION 格式合法且越过 v21.94、data.js 含 v21.95 注释与版本字面量并保留 v21.94 历史注释、PLAY_TIME2_GOAL===7200 且导出、ACH_LIST 35 项精确 + ptime2 唯一/无 r/描述由常量派生/ok 与 prog 同读 (g.time||0)、既有 34 成就 id 零回归 + ptime2 末尾序位、ok/prog 谓词逐值（0·7199 false / 7200·14400 true / 缺字段 false·0/120 防御 / prog 60/120·120/120·240/120 不钳制）、main.js 源级落位（tick 累计行）、运行期四档（time=7200 真实解锁落 hero.ach / 7199 不误报 / 新档 0 不误报 / 重复去重 / 缺 time 旧档不抛错不解锁）、drawAch 35 项+末页滚动渲染不抛错、README/package/CHANGELOG 同步、姊妹件套 pin 复查（v2194..v2176 九十一件套 + v2194..v2179 GAME_VERSION v21.95 + v2193..v2159 成就 pin 35 项双处 + v2193 去硬化确认 + 旧代 v21.94 字面量 pin 全库零残留））。

## v21.94 状态页资源行补图鉴进度——「收集三件套」在 I 状态页最后一块拼图（体验打磨·信息透明·纯显示，承 v21.79 标题预览 slotPreview / v21.82 胜利画面 drawWin / v21.87 尾声战绩页 drawEnding 同一「收集进度三件套」主线：成就·图鉴·宝箱三件套在 标题预览/胜利画面/尾声 三处 run 总结屏齐备，唯独 I 状态页（drawStatus 资源行）有 📦 宝箱（v19.72 起）与 ⏱ 时长，却无图鉴——玩家按 I 看「这趟收集到哪了」，图鉴 N/13 还得再开 B 图鉴页才看得到；选题轮换：v21.83 新技能、v21.84 新成就、v21.85 体验打磨、v21.86 新成就、v21.87 体验打磨、v21.88 新成就、v21.89 存档兼容、v21.90 基建打磨、v21.91 新成就、v21.92 体验打磨、v21.93 新成就后本轮回体验打磨——承接 v21.79/82/87 三件套主线的收官（三处总结屏之外，玩家最常看的常规屏就是 I 状态页）；开工摸底：基线 git 干净树（最新提交 9bb52df，v21.93）、基线 npm test 八十九件套全绿、并发检查双测 mtime 无变化、纯显示零结算零数值零存档故无需生存模拟——故本轮选体验打磨而非平衡/新内容）

- 【显示补齐】`js/view/menus.js` drawStatus：资源行补派生 `const codexN = BESTIARY_TARGET.filter((n) => ((hero.bestiary || {})[n] | 0) >= 1).length;`（与 slotPreview/drawWin/drawEnding 三处同式——同一份 BESTIARY_TARGET 单一数据源、|0 归一防御式，旧布尔 bestiary/缺字段旧档零迁移）+ 资源行 14px 串并入 `📕:${codexN}/${BESTIARY_TARGET.length}`（行位/基线 264 零位移，金币/🍖/🧪/🍄/📦 双口径/⏱ 既有段逐字保留，纯显示零结算零存档变化）。
- 【宽预算】行宽实测（/tmp/jrpg_viz @napi-rs/canvas 14px 最宽组合「金币99999·📕13/13·📦12/12·⏱999:59:59」）≈402px、右缘 ≈512 ≤ 面板右缘 570（留 58px 余量）；estW 官方估算口径 ≈490——smoke_v2122 的「≤460」预算断言随新现实更新为「≤520」（注释注明 @napi-rs 实测口径仍 ≤570）。
- 【零回归面】未动 drawStatus 既有任何行（姓名/地图/经验条/HP/MP/攻防/武器防具/技能/下一技能/冒险进度/主线支线逐字未动）；未动 hero.bestiary 任何写入（battle.startBattle 记 seen / winBattle 记 bestiary 逐字未动）；未动 slotPreview/drawWin/drawEnding 三处三件套；未动存档格式（bestiary 随既有存档快照自动落盘/读回、防御式读取）。只改 2 个游戏文件（`js/data.js`：GAME_VERSION v21.93→v21.94 + 版本注释；`js/view/menus.js`：drawStatus 一行派生 + 资源行并入）+ `tests/smoke_v2122_chesttotal.mjs`（宽预算断言随新现实 460→520 + worstLine 并入 📕）+ `CHANGELOG.md`（本条）+ `package.json`（test 串第 90 份）+ `README.md`（tests 树收录 smoke_v2194_statuscodex + 冒烟九十件套（八十九件套清除）+ v21.94 守护描述 + 系统清单「状态页宝箱计数双口径」段补图鉴口径）+ 新增 `tests/smoke_v2194_statuscodex.mjs`（仓库常驻，承 v21.10-v21.93 冒烟入库先例）+ 顺带把 v21.94 随新现实更新的姊妹 pin 一并落位：smoke_v2193..v2176 的 README 件套 pin（八十九→九十件套，共 18 件）、smoke_v2193..v2179 的 GAME_VERSION 字面量 pin（v21.93→v21.94，共 14 件）、smoke_v2122 的宽预算断言（≤460→≤520，worstLine 并入 📕13/13）。
- 【验证】`node --check js/data.js js/view/menus.js tests/smoke_v2194_statuscodex.mjs tests/smoke_v2122_chesttotal.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 九十件套全绿（既有 89 件逐项零回归——含 v2193..v2176 更新后件套 pin、v2193..v2179 更新后 GAME_VERSION pin 与 v2122 更新后宽预算—— + 新增 v21.94 全过——GAME_VERSION 格式合法且越过 v21.93、data.js 含 v21.94 注释与版本字面量并保留 v21.93 历史注释、menus.js 源级落位（drawStatus codexN 派生与 slotPreview/drawWin/drawEnding 同式/资源行 📕 并入且既有段逐字保留/旧行零残留/行位 264 零位移）、运行期四档（新档 0/13 · 推进档 2/13 · 旧布尔 bestiary true 归一 1/13 · 缺字段防御档 0/13 不抛错）+ 行内三件套共存档（📕+📦+成就+⏱ 同段）、行宽 estW ≤520 且右缘 ≤570、README/package/CHANGELOG 同步、姊妹件套 pin 复查（v2193..v2176 九十件套 + v2193..v2179 GAME_VERSION v21.94 + 旧代 v21.93 字面量 pin 全库零残留））。

## v21.93 新成就「驱雾百战」（讨伐线第二档里程碑）——成就版图逐线核对后的第二档补全（新内容·单成就，承 v21.84 灯燃长夜 / v21.86 灵药初成 / v21.88 走遍四方 / v21.91 长明不熄 单成就先例；选题轮换：v21.83 新技能、v21.84 新成就、v21.85 体验打磨、v21.86 新成就、v21.87 体验打磨、v21.88 新成就、v21.89 存档兼容、v21.90 基建打磨、v21.91 新成就、v21.92 体验打磨后本轮回新成就——成就版图逐线核对（v21.91）：战斗（firstblood/hunt10/lucky）、等级（lvl5·lvl10·lvl12）、金币（rich）、图鉴（scholar·perfection）、支线（八条单支线 + allquests）、Boss（boss/cave/trueboss/rush）、装备（legend/aegis）、碎片（memoir）、技能（skills）、难度（hardtrue）、宝箱（chests·allchests）、精英（elites）、酿造（brew）、探索（wander）、时长（ptime）各线都有印记，但多档线并不齐：等级线已有 5/10/12 三档、宝箱线已有 6/12 两档，唯独伴随全场战斗的「累计讨伐」（hero.totalWins，battle.winBattle 唯一写入点、随存档快照持久化）至今只有第一档（hunt10 驱雾十战=10 只）——练到 Lv12 的玩家（按 v21.34 链式经验曲线累计讨伐已 100+）在「再来一场」处毫无纪念；现补第二档（HUNT2_GOAL=100 只，约 Lv12 练级途程内可达），承 等级三档/宝箱两档 多档先例；开工摸底：基线 git 干净树（最新提交 4ea5734，v21.92）、基线 npm test 八十八件套全绿、并发检查双测 mtime 无变化、纯新增成就零战斗/数值接触无需生存模拟——故本轮选新内容而非平衡）

- 【新成就】`js/data.js`：讨伐线第二档阈值补 `HUNT2_GOAL = 100`（与 HUNT_GOAL 同一「成就阈值数据化」家族——判定/描述/进度三处同读一份源，改门槛只改 data.js 一处自动跟随，零裸字面量；后续加第三档只增常量不再动条目）+ ACH_LIST 末尾追加 `{id:'hunt100', name:'驱雾百战', d:`累计讨伐 ${HUNT2_GOAL} 只魔物`, ok:g=>g.totalWins>=HUNT2_GOAL, prog:g=>`${g.totalWins||0}/${HUNT2_GOAL}`}`（33→34 项；无 r 字段纯里程碑，承 memoir/skills/hardtrue 惯例——百战本身就是奖励）。关键设计：判定读**既有** `hero.totalWins`（battle.winBattle 唯一写入点、随存档快照持久化）——`(g.totalWins||0)` 防御式读取，旧档无此字段=0 不误解锁、零迁移（承 v19.41 seen 同款）；不设新计数/新状态/新判定点：totalWins 是持续累积量，applyAchievements 既有通路（胜利/开箱/酿造/进图/买甲任意下一次判定点）当场解锁，反馈不迟到，承 lvl12 同款。
- 【零回归面】未动既有 33 个成就任何字段（hunt100 追加在末尾、displayOrder 不变）；未动 hero.totalWins 写入（battle.js winBattle `hero.totalWins++` 逐字未动）/胜利结算/图鉴计数；未动存档格式（totalWins 随既有存档快照自动落盘/读回、旧档防御式读取）。只改 2 个游戏文件（`js/data.js`：版本号 v21.92→v21.93 + 注释 + HUNT2_GOAL 常量 + 成就条目 + 导出）+ `CHANGELOG.md`（本条）+ `package.json`（test 串第 89 份）+ `README.md`（tests 树收录 smoke_v2193_hunt100 + 冒烟八十九件套（八十八件套清除）+ v21.93 守护描述 + C 键行「34 项」/图鉴&成就行「34 项成就」双处）+ 新增 `tests/smoke_v2193_hunt100.mjs`（仓库常驻，承 v21.10-v21.92 冒烟入库先例）+ 顺带把 v21.93 随新现实更新的姊妹 pin 一并落位：smoke_v2192..v2176 的 README 件套 pin（八十八→八十九件套，共 17 件）、smoke_v2192..v2179 的 GAME_VERSION 字面量 pin（v21.92→v21.93，共 13 件）、smoke_v2191..v2159 的 README 成就 pin（33→34 双处，共 10 件）、smoke_v2191 的 ACH_LIST 精确计数断言去硬化（===33→>=33 存活性口径，承 v21.91 对 v2188 同款先例）。
- 【验证】`node --check js/data.js tests/smoke_v2193_hunt100.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 八十九件套全绿（既有 88 件逐项零回归——含 v2192..v2176 更新后件套 pin、v2192..v2179 更新后 GAME_VERSION pin、v2191..v2159 更新后成就 pin 与 v2191 去硬化断言—— + 新增 v21.93 全过——GAME_VERSION 格式合法且越过 v21.92、data.js 含 v21.93 注释与版本字面量并保留 v21.92 历史注释、HUNT2_GOAL===100 且导出、ACH_LIST 34 项精确 + hunt100 唯一/无 r/描述由常量派生/ok 与 prog 同读 (g.totalWins||0)、既有 33 成就 id 零回归 + hunt100 末尾序位、ok/prog 谓词逐值（0·99 false / 100·150 true / 缺字段 false·0/100 防御 / prog 50/100·100/100·150/100 不钳制）、battle.js 源级落位（winBattle 唯一写入点 hero.totalWins++）、运行期五档（totalWins=100 真实解锁落 hero.ach / 99 不误报 / 新档 0 不误报 / 重复去重 / 缺 totalWins 旧档不抛错不解锁）、drawAch 34 项+末页滚动渲染不抛错、README/package/CHANGELOG 同步、姊妹件套 pin 复查（v2192..v2176 八十九件套 + v2192..v2179 GAME_VERSION v21.93 + v2191..v2159 成就 pin 34 项双处 + v2191 去硬化确认 + 旧代 v21.92 字面量 pin 全库零残留））。

## v21.92 快速旅行目的地等级达标预警——「推荐 Lv.X 起」从陈述标准变成「你够不够格」的红字判断（体验打磨·信息透明·纯显示，承 v19.56 进图预警 / v19.89 状态页推荐等级 / v21.46 试炼推荐等级同一「难度透明」主线：快速旅行列表的「推荐 Lv.X 起」提示（TRAVEL_LIST 派生）与进图预警（v19.56，传送落地后才提示）都只陈述标准，玩家在旅行菜单上仍要自己心算「我 Lv.几、够不够」——进图预警是「事后」提醒，而「该不该去」的决策发生在旅行菜单；开工摸底：基线 git 干净树（最新提交 20f077a，v21.91）、基线 npm test 八十七件套全绿、并发检查双测 mtime 无变化（上次落盘 05:10 CHANGELOG，本次开工 ~06:14）、平衡模拟复测（普通带药 100%/普通裸刷 99.5%/困难带药 99.2%/困难裸刷 15.6% 生还——开局数值健康、无需调整）、纯显示零结算零数值零存档故无需生存模拟——故本轮选体验打磨而非平衡/新内容）

- 【预警落位】`js/view/menus.js` drawTravel：尾部页脚之后按选中行实时比对 `MAPS[k].recLv` 与 `hero.level`（防御式 `(MAPS[selK]||{}).recLv`），未达标且非当前所在地（`selK !== curMap()`——已在该图时语义由进图预警/状态页承载，避免「目的地」失真）补红色预警行 `⚠️ 目的地推荐 Lv.N · 你当前 Lv.M · 先补给再战！`（320, travelFootY(TRAVEL_LIST.length)+24，12px #ff5b5b 居中——页脚基线 320 让位 +24=344、字底 ≈356 < 面板底 380 不越界；与 v19.56 进图预警同式同源同口径）；未到访目的地（？？？无法解锁行）同样预警——首访决策点正是最需要数字的时刻；village 安全区（recLv=1）恒不触发、达标零噪音。单一数据源：与 TRAVEL_LIST 提示派生 / world.transition 进图预警 / 状态页推荐等级同读 `MAPS.recLv`，调门槛只改 MAPS 一处四端同步，零裸字面量。
- 【零回归面】未动 drawTravel 既有行（TRAVEL_LIST.forEach 行/desc/提示右列/页脚逐字未动）；未动 doTravel/visited/进图预警/状态页/TRAVEL_LIST 数据任何逻辑；未动任何结算/数值/存档/遇敌/成就。只改 2 个游戏文件（`js/data.js`：GAME_VERSION v21.91→v21.92 + 版本注释；`js/view/menus.js`：drawTravel 预警行 + 注释）+ `CHANGELOG.md`（本条）+ `package.json`（test 串第 88 份）+ `README.md`（tests 树补录 smoke_v2191_ptime——v21.91 漏树行，本版一并补记 + 收录 smoke_v2192_travelwarn + 冒烟八十八件套（八十七件套清除）+ v21.92 守护描述 + 系统清单快速旅行句补目的地等级达标预警口径）+ 新增 `tests/smoke_v2192_travelwarn.mjs`（仓库常驻，承 v21.10-v21.91 冒烟入库先例）+ 顺带把 v21.92 随新现实更新的姊妹 pin 一并落位：smoke_v2191..v2176 的 README 件套 pin（八十七→八十八件套，共 16 件）、smoke_v2191..v2179 的 GAME_VERSION 字面量 pin（v21.91→v21.92，共 12 件）、smoke_v2191 的负向 pin（八十六件套清除→八十七件套清除）。
- 【验证】`node --check js/data.js js/view/menus.js tests/smoke_v2192_travelwarn.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 八十八件套全绿（既有 87 件逐项零回归——含 v2191..v2176 更新后件套 pin、v2191..v2179 更新后 GAME_VERSION pin—— + 新增 v21.92 全过——GAME_VERSION 格式合法且越过 v21.91、data.js 含 v21.92 注释与版本字面量并保留 v21.91 历史注释、MAPS.recLv 契约 1/3/6/10 与 TRAVEL_LIST 四图一一对应、menus.js 源级落位（selK/selRec 防御式派生/预警行文案与条件/页脚 +24 让位/既有行逐字零回归）、运行期六档（Lv1 选雾语林命中「推荐 Lv.3 · 你当前 Lv.1」/Lv1 选星井矿脉数值派生/Lv3 恰达标零预警/Lv1 选当前所在地 📍 零预警（k!==curMap 守卫）/安全区 village 恒不触发/Lv10 选无字回廊恰达标零预警 + 各档渲染不抛错）、README/package/CHANGELOG 同步（tests 树含 v2191 补录与 v2192/八十八件套/v21.92 守护描述/系统清单预警口径/第 88 份）、姊妹件套 pin 复查（v2191..v2176 八十八件套 + v2191..v2179 GAME_VERSION v21.92 + 旧代 v21.91 字面量 pin 全库零残留））。



## v21.91 新成就「长明不熄」（游玩时长里程碑）——成就版图逐线核对的最后一条空白（新内容·单成就，承 v21.84 灯燃长夜 / v21.86 灵药初成 / v21.88 走遍四方 单成就先例；选题轮换：v21.83 新技能、v21.84 新成就、v21.85 体验打磨、v21.86 新成就、v21.87 体验打磨、v21.88 新成就、v21.89 存档兼容、v21.90 基建打磨后本轮继续新内容——成就版图逐线核对：战斗（firstblood/hunt10/lucky）、等级（lvl5·lvl10·lvl12）、金币（rich）、图鉴（scholar·perfection）、支线（八条单支线 + allquests）、Boss（boss/cave/trueboss/rush）、装备（legend/aegis）、碎片（memoir）、技能（skills）、难度（hardtrue）、宝箱（chests·allchests）、精英（elites）、酿造（brew）、探索（wander）各线都有印记，唯独「冒险时长」这条陪伴全程、随存档持久化的计时线（main.js render 驱动 hero.time、状态 I/尾声/战绩页 ⏱ 同读一份源、slotPreview 随档显示）没有任何纪念：玩家在这颗灯下待满一小时毫无回响；开工摸底：基线 git 干净树（最新提交 67ab200，v21.90）、基线 npm test 八十七件套全绿、并发检查双测 mtime 无变化、纯新增成就零战斗/数值接触故无需生存模拟——故本轮选新内容而非平衡）

- 【新成就】`js/data.js`：游玩时长阈值补 `PLAY_TIME_GOAL = 3600`（60 分钟；与 FIRSTBLOOD_GOAL / LVL12_GOAL / ELIXIR_GOAL 同一「成就阈值数据化」家族——判定/描述/进度三处同读一份源，改门槛只改 data.js 一处自动跟随，零裸字面量）+ ACH_LIST 末尾追加 `{id:'ptime', name:'长明不熄', d:`累计游玩 ${PLAY_TIME_GOAL / 60} 分钟`, ok:g=>(g.time||0)>=PLAY_TIME_GOAL, prog:g=>`${Math.floor((g.time||0)/60)}/${PLAY_TIME_GOAL/60}`}`（32→33 项；无 r 字段纯里程碑，承 memoir/lvl5/hardtrue 惯例——守着灯的时间本身就是奖励；prog 分钟数与 lvl5 同族「X/N 不钳制」口径）。关键设计：判定读**既有** `hero.time`（main.js render 逐帧累计、随存档快照持久化、状态 I/尾声/战绩页/标题预览 ⏱ 同读一份源）——`(g.time||0)` 防御式读取，旧档无此字段=0 不误解锁、零迁移（承 v19.41 seen 同款）；不设新计数/新状态/新判定点：time 是持续累积量，applyAchievements 既有通路（胜利/开箱/酿造/进图/买甲任意下一次判定点）当场解锁，反馈不迟到，承 lvl12 同款。
- 【零回归面】未动既有 32 个成就任何字段（ptime 追加在末尾、displayOrder 不变）；未动 hero.time 写入/格式化（fmtTime）/HUD/状态页/战绩页/标题预览任何显示；未动存档格式（time 随既有存档快照自动落盘/读回、旧档防御式读取）。只改 2 个游戏文件（`js/data.js`：版本号 v21.90→v21.91 + 注释 + PLAY_TIME_GOAL 常量 + 成就条目 + 导出）+ `CHANGELOG.md`（本条）+ `package.json`（test 串第 87 份）+ `README.md`（tests 树收录 smoke_v2191_ptime + 冒烟八十七件套（八十六件套清除）+ v21.91 守护描述 + C 键行「33 项」/图鉴&成就行「33 项成就」双处）+ 新增 `tests/smoke_v2191_ptime.mjs`（仓库常驻，承 v21.10-v21.90 冒烟入库先例）+ 顺带把 v21.91 随新现实更新的姊妹 pin 一并落位：smoke_v2190..v2176 的 README 件套 pin（八十六→八十七件套，共 15 件）、smoke_v2190..v2179 的 GAME_VERSION 字面量 pin（v21.90→v21.91，共 11 件）、smoke_v2188/v2186/v2184/v2180/v2177/v2176/v2173/v2168/v2159 的 README 成就 pin（32→33 双处）、smoke_v2188 的 ACH_LIST 精确计数断言去硬化（===32→>=32 存活性口径，承 v21.88 对 v2186 同款先例）。
- 【验证】`node --check js/data.js tests/smoke_v2191_ptime.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 八十七件套全绿（既有 86 件逐项零回归——含 v2190..v2176 更新后件套 pin、v2190..v2179 更新后 GAME_VERSION pin、v2188..v2159 更新后成就 pin 与 v2188 去硬化断言—— + 新增 v21.91 全过——GAME_VERSION 格式合法且越过 v21.90、data.js 含 v21.91 注释与版本字面量、PLAY_TIME_GOAL===3600 且导出、ACH_LIST 33 项精确 + ptime 唯一/无 r/描述由常量派生/ok 与 prog 同读 (g.time||0)、既有 32 成就 id 零回归 + ptime 末尾序位、ok/prog 谓词逐值（0·3599 false / 3600·7200 true / 缺字段 false·0/60 防御 / prog 30/60·60/60·120/60 不钳制）、main.js 源级落位（render 累计行）、运行期四档（time=3600 真实解锁落 hero.ach / 3599 不误报 / 新档 0 不误报 / 重复去重 / 缺 time 旧档不抛错不解锁）、drawAch 33 项+末页滚动渲染不抛错、README/package/CHANGELOG 同步、姊妹件套 pin 复查（v2190..v2176 八十七件套 + v2190..v2179 GAME_VERSION v21.91 + v2188..v2159 成就 pin 33 项双处 + v2188 去硬化确认））。

## v21.90 启动脚本口径统一与默认仅本机绑定——start.sh/start.command 不再用旧名「勇者传说」打招呼（体验打磨·口径一致·安全加固，承 v21.29-32/v21.38-43/v21.74「同一功能所有入口口径一致」主线：v2.0 起游戏更名「潮灯记」（title/README/package 早已同步），但两个一键启动脚本的注释与启动回执仍写着「勇者传说」——玩家在 Finder 双击 start.command 或跑 ./start.sh 时，终端回执「🎮 勇者传说 · 已启动」是旧名在游戏外唯一的残留入口，与本作标题不符；同时 `python3 -m http.server "$PORT"` 默认绑定 0.0.0.0，同一局域网的任何人都能访问这台机器上的游戏服务器（共享网络/宿舍场景实际暴露）。选题轮换：v21.83 新技能、v21.84 新成就、v21.85 体验打磨、v21.86 新成就、v21.87 体验打磨、v21.88 新成就、v21.89 存档兼容后本轮选「启动体验/基建打磨」——游戏本体透明面已极齐备，本轮扫游戏外唯一残留旧名与默认暴露面；开工摸底：基线 git 干净树（最新提交 bcd7532，v21.89）、基线 npm test 八十五件套全绿、并发检查双测 mtime 无变化（03:39 前无写入）、中期生存模拟复核 村100%/林99.64%/矿100%/廊100%（含 Lv3 精英逃跑对照 95.05%）数值健康无需再调——故本轮改启动脚本/文档而非平衡/新内容）

- 【旧名清除】`start.sh` / `start.command`：注释头「勇者传说 · 一键启动（…）」→「潮灯记 · 一键启动（…）」；启动回执 `🎮 勇者传说 · 已启动：$URL` → `🕯️ 潮灯记 · 已启动：$URL`（🕯️ 灯主题与游戏标题「潮灯记」同脉，回执行其余逐字未动、URL/端口逻辑零接触）；`improve-plan.md` 留档标题「勇者传说 · 改进计划」→「潮灯记 · 改进计划」（同一旧名残留家族，一并清零）。
- 【默认仅本机】两脚本 `python3 -m http.server "$PORT"` → `python3 -m http.server --bind 127.0.0.1 "$PORT"`——python3 http.server 自 Python 3.x 起默认绑定 0.0.0.0（局域网可访问），显式绑定后默认只在本机可访问（本机 `http://localhost:$PORT` 与浏览器打开路径逐字不变，纯静态服务零功能影响；start.command 头注释补一行「v21.90 起仅绑定 127.0.0.1」说明）；`README.md` 手动方式命令同口径补 `--bind 127.0.0.1` 并注明「如需局域网联机试玩去掉即可」。
- 【零回归面】未动任何游戏逻辑/结算/存档/数值/绘制（js/ 下仅 data.js 版本号+注释）；未动脚本的端口顺延/`trap` 清理/`open` 打开浏览器/`wait` 逻辑（逐字保留）；未动 README 其余章节（仅手动命令行与 tests 树/件套/守护描述三处同步）；BACKUP_singlefile_v1.23.html 是 v1.23 历史回滚备份、其中若含旧名属历史事实、刻意不动。只改 2 个脚本（start.sh/start.command）+ `improve-plan.md`（标题一行）+ `README.md`（手动命令/tests 树/件套口径/守护描述）+ `js/data.js`（GAME_VERSION v21.89→v21.90 + 版本注释）+ `CHANGELOG.md`（本条）+ `package.json`（test 串第 86 份）+ 新增 `tests/smoke_v2190_launch.mjs`（仓库常驻，承 v21.10-v21.89 冒烟入库先例）+ 顺带把 v21.90 随新现实更新的姊妹 pin 一并落位：smoke_v2189..v2176 的 README 件套 pin（八十五→八十六件套，含嵌套复查链，共 14 件）、smoke_v2189..v2179 的 GAME_VERSION 字面量 pin（v21.89→v21.90，共 10 件）。
- 【验证】`node --check js/data.js tests/smoke_v2190_launch.mjs`、`bash -n start.sh start.command` 与 `npm run check`（25 模块）全部通过；真实启动验证：`PORT=8123 python3 -m http.server --bind 127.0.0.1 8123` 起服后 `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:8123/` 返回 200、`lsof -iTCP:8123 -sTCP:LISTEN` 确认仅监听 127.0.0.1（无 0.0.0.0/:: 暴露面）；`npm test` 八十六件套全绿（既有 85 件逐项零回归——含 v2189..v2176 更新后件套 pin、v2189..v2179 更新后 GAME_VERSION pin—— + 新增 v21.90 全过——版本锚点、data.js 注释与字面量、start.sh/start.command/improve-plan.md 源级落位（潮灯记 ×3 处/--bind ×2 处/旧名零残留）、README/package/CHANGELOG 同步、姊妹件套 pin 复查（v2189..v2176 八十六件套 + v2189..v2179 GAME_VERSION v21.90））。

## v21.89 旧档 visited 兜底与旅行端防御式读取——快速旅行在旧档上不再炸（存档兼容·韧性，承 v19.41 seen 兜底 / v21.88 wander (g.visited||[]) 同一「旧档零迁移、防御式读取」家族：v21.88 成就端已防御，但 快速旅行 两端（menus.drawTravel 渲染 / core.doTravel 判定）仍直接 hero.visited.includes——旧档（含单文件时代读档）缺 visited 字段时按 T 开旅行页即抛 TypeError、现有旅行防误触/未探索拦截语义整页失效；选题轮换：v21.83 新技能、v21.84 新成就、v21.85 体验打磨、v21.86 新成就、v21.87 体验打磨、v21.88 新成就后本轮选「存档兼容/韧性」——三件套/透明面已相当齐备，本轮扫旧档防御家族的最后一块拼图（v19.41 seen / v21.48 skills / v21.76 chests 三形态 / v21.88 visited 成就端之后，visited 旅行端是唯一仍裸读的字段消费点）；开工摸底：基线 git 干净树（最新提交 74d8814，v21.88）、基线 npm test 八十四件套全绿、并发检查双测 mtime 无变化、零结算零数值零存档格式变化故无需生存模拟）

- 【旧档兜底】`js/quests.js` migrateQuests：visited 兜底 `if (!Array.isArray(hero.visited)) hero.visited = ['village'];`——与上方 seen/fragments/skills 兜底同族（读档一次性补字段、新增字段随 snapshotHero 全量快照自动持久化，零存档结构变更）；值为 `['village']` 与 core.newGame 起始值逐字一致（任何存档必然到访过起始村——即便存档其他字段再老，起点也是潮灯镇），且不误解锁「走遍四方」（其余三图仍须真实到访、Object.keys(MAPS).every 判定不因兜底变化）；既有含 visited 的存档逐字不动、兜底只补缺失。
- 【防御式读取】`js/view/menus.js` drawTravel `unlocked=hero.visited.includes(k)` → `unlocked=(hero.visited||[]).includes(k)`；`js/core.js` doTravel `if (!S.G.visited.includes(key))` → `if (!(S.G.visited||[]).includes(key))`——沿 v21.88 成就端 `(g.visited||[])` 同式；双保险：即使绕过 load（retryBoss/内存路径/异常存档数据）缺字段也不抛错，渲染「？？？ · 未探索」、判定走「尚未探索此地」拦截，零行为变化（新档/正常档两端判定逐字等价）。
- 【零回归面】未动 world.transition 既有 visited 守卫（`S.G.visited && !S.G.visited.includes(name)` 逐字未动——v21.26 起同一行）；未动 visited 写入点/写入逻辑/TRAVEL_LIST/旅行列表/快速旅行防误触（v21.6）/未探索拦截报文（逐字未动）；未动 ACH_LIST.wander 判定（防御式读取逐字未动、兜底值不影响）；未动存档格式（visited 随既有存档快照自动落盘/读回、兜底只在读档内存路径生效）。只改 3 个游戏文件（`js/data.js`：GAME_VERSION v21.88→v21.89 + 版本注释；`js/quests.js`：migrateQuests visited 兜底 + 注释；`js/view/menus.js` + `js/core.js`：旅行两端防御式读取各一行）+ `CHANGELOG.md`（本条）+ `package.json`（test 串第 85 份）+ `README.md`（tests 树收录 smoke_v2189_visitedlegacy + 冒烟八十五件套（八十四件套清除）+ v21.89 守护描述）+ 新增 `tests/smoke_v2189_visitedlegacy.mjs`（仓库常驻，承 v21.10-v21.88 冒烟入库先例）+ 顺带把 v21.89 随新现实更新的姊妹 pin 一并落位：smoke_v2188..v2176 的 README 件套 pin（八十四→八十五件套，含嵌套复查链，共 66 处）、smoke_v2188..v2179 的 GAME_VERSION 字面量 pin（v21.88→v21.89，共 17 处）。
- 【验证】`node --check js/data.js js/quests.js js/view/menus.js js/core.js tests/smoke_v2189_visitedlegacy.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 八十五件套全绿（既有 84 件逐项零回归——含 v2188..v2176 更新后件套 pin、v2188..v2179 更新后 GAME_VERSION pin—— + 新增 v21.89 全过——GAME_VERSION 格式合法且越过 v21.88、data.js 含 v21.89 注释与版本字面量、源级落位（migrateQuests 兜底 `['village']` / drawTravel·doTravel 防御式读取且旧裸读零残留 / world.transition 既有守卫逐字未动 / 兜底注释落位）、运行期五档（newGame 起始 visited 恰 ['village'] 零回归 / 旧档真实 load→migrateQuests 兜底落位且不误解锁 wander / 旧档 drawTravel 不抛错且「？？？ · 未探索」×3 如实渲染 / 旧档 doTravel 不抛错且拦截不跳转（scene 仍 world）/ 非 load 路径缺字段防御档双端不抛错 / 既有 visited 存档经 migrateQuests 逐字保留零覆盖）、README/package 同步（tests 树收录/八十五件套/v21.89 守护描述/第 85 份）、姊妹件套 pin 复查（v2188..v2176 八十五件套 + v2188..v2179 GAME_VERSION v21.89））。

- 【新成就】`js/data.js`：ACH_LIST 末尾追加 `{id:'wander', name:'走遍四方', d:`踏遍全部 ${Object.keys(MAPS).length} 张地图`, ok:g=>Object.keys(MAPS).every(m=>(g.visited||[]).includes(m)), prog:g=>`${Object.keys(MAPS).filter(m=>(g.visited||[]).includes(m)).length}/${Object.keys(MAPS).length}`}`（31→32 项）。判定/进度同读 Object.keys(MAPS) 单一数据源（与 TRAVEL_LIST 四图一一对应——加/删地图只改 data.js 一处、本成就自动跟随，绝无第二套口径，承 allquests 对 QUESTS / allchests 对 chestTotal 同「全收集分母数据化」家族）+ 既有 hero.visited 到访记录（world.transition 进图时 push，全游戏唯一写入点），`(g.visited||[])` 防御式读取——旧档无 visited 字段=0 不误解锁、零迁移（承 v19.41 seen 同款）；无 r 字段纯里程碑（与 memoir/skills/aegis/hardtrue 同款——到访本身就是奖励），prog 同式 X/N 不钳制。判读「到访」而非「讨伐/开箱」：四图到访是每档冒险必走的主线通路（镇→林→矿→廊），恰好是「从没被纪念过却人人都会完成」的一档——与第一场胜利（firstblood）同为开荒期即达成的「仪式性」里程碑。
- 【反馈不迟到】`js/world.js` transition：visited 落账行（`if (S.G.visited && !S.G.visited.includes(name)) S.G.visited.push(name);` 逐字未动）之后补 `applyAchievements();`——transition 是全游戏唯一的 visited 写入点（传送门/出口/快速旅行/水晶开门四条入口全走本函数），进图落账当场判定，最晚一张图（通常是双徽记开门的无字回廊）落账即解锁（承 v19.51 开箱当场判定 / v21.73 buyArmor / v21.86 brewNow 同一「反馈不迟到」惯例）；applyAchievements 幂等（已解锁不重报、不重复横幅），重复进图零噪音。
- 【零回归面】未动既有 31 个成就任何字段（wander 追加在末尾、displayOrder 不变）；未动 visited 写入逻辑/快速旅行判定/doTravel/旅行列表（仅在其后追加一行判定）；未动存档格式（visited 随既有存档快照自动落盘、旧档防御式读取）；未动 hero.js/成就页渲染（ACH_LIST.length 动态读取，32 项仍 PAGE=10、滚动四页自动扩展）。只改 2 个游戏文件（`js/data.js`：版本号 v21.87→v21.88+注释+成就条目；`js/world.js`：transition 一行判定+注释）+ `CHANGELOG.md`（本条）+ `package.json`（test 串第 84 份）+ `README.md`（tests 树收录 smoke_v2188_wander + 冒烟八十四件套（八十三件套清除）+ v21.88 守护描述 + C 键行「32 项」/图鉴&成就行「32 项成就」双处）+ 新增 `tests/smoke_v2188_wander.mjs`（仓库常驻，承 v21.10-v21.87 冒烟入库先例）+ 顺带把 v21.88 随新现实更新的姊妹 pin 一并落位：smoke_v2187..v2176 的 README 件套 pin（八十三→八十四件套，含嵌套复查链）、smoke_v2187..v2179 的 GAME_VERSION 字面量 pin（v21.87→v21.88）、smoke_v2186 的 ACH_LIST 精确计数断言去硬化（===31→>=31 存活性口径，承 v21.77 对 v2176 同款先例）、smoke_v2186/v2184/v2180/v2177/v2176/v2173/v2168/v2159 的 README 成就 pin（31→32 双处，含嵌套复查链）。
- 【验证】`node --check js/data.js js/world.js js/main.js` 与 `npm run check`（25 模块）全部通过；`npm test` 八十四件套全绿（既有 83 件逐项零回归 + smoke_v2188_wander 全过——版本锚点、ACH_LIST 32 项精确、wander 唯一/无 r/描述与 ok/prog 由 Object.keys(MAPS) 派生、既有 31 成就 id 零回归 + grain 29/brew 30/wander 31 序位、ok/prog 谓词逐值（全到访 true·4/4 / 部分 false·1/4 / 缺字段 false·0/4 防御 / 重复键不影响 every）、MAPS 恰四图且与 TRAVEL_LIST 一一对应、world.js 源级落位（push 逐字保留 + 其后 applyAchievements + 既有 import）、运行期四档（transition(gallery) 落账四图齐且当场解锁落 hero.ach / 3/4 不误解锁 / 已解锁重复去重 / 缺 visited 旧档不抛错不解锁）、drawAch 32 项两档滚动渲染不抛错、README/package 同步、姊妹件套 pin 复查）。

## v21.87 尾声战绩页补收集进度两件——「按 Enter 观看尾声」的 run 总结屏补齐 v21.79 标题预览/v21.82 胜利画面确立的「收集进度三件套」（成就·图鉴·宝箱）最后一块拼图：drawEnding（尾声战绩页）此前的战绩行（v19.49 累计讨伐/成就/记忆/金币/时长）唯独缺 图鉴与宝箱——玩家站在这趟 run 的最终总结屏上想「继续收集还是回标题」，一眼看不到图鉴/宝箱收到哪了（成就/记忆已有）（体验打磨·信息透明·口径一致，承 v19.49 战绩补全 / v21.79 标题预览三件套 / v21.82 胜利画面两件同一主线；选题轮换：v21.83 新技能、v21.84 新成就、v21.85 体验打磨、v21.86 新成就后本轮回体验打磨——收集三件套三处 run 总结屏（标题预览/胜利画面/尾声）逐屏核对，尾声是最后一块缺 图鉴/宝箱 的屏；开工摸底：基线 git 干净树（最新提交 d4d834e，v21.86）、基线 npm test 八十二件套全绿、并发检查双测 mtime 无变化、纯显示零结算零数值零存档故无需生存模拟——故本轮选体验打磨而非平衡/新内容）

- 【显示补齐】`js/view/menus.js` drawEnding：补 `const codexN = BESTIARY_TARGET.filter((n) => ((hero.bestiary || {})[n] | 0) >= 1).length;` 与 `const chestN = chestCount(hero);` 两行派生 + 独立收集行 `📕 图鉴 N/M · 📦 宝箱 N/M`（320,366 bold 13px #7dd47f），页脚「按 Enter 返回标题」376→396 让位（文案逐字未动）；与 slotPreview（v21.79）/drawWin（v21.82）同读 BESTIARY_TARGET / chestCount·chestTotal 一份单一数据源（chestCount 三形态防御式、旧档零迁移），绝无第二套口径；战绩行/故事行（含真结局八行档末行 321）全部零位移，纯显示零结算零存档变化。
- 【顺带补齐】README tests 树补回 `+ smoke_v2186_brew`（v21.86 只更新了守护描述行、树漏入，本版随 v2187 一并补上）。
- 【零回归面】未动 drawEnding 既有战绩行/页脚文案（逐字保留，仅 y 376→396 让位）；未动 drawWin/slotPreview/成就页/图鉴页/状态页任何字段；未动存档格式（bestiary/chests/ach/fragments 均为既有字段防御式读取）。只改 2 个游戏文件（`js/data.js`：版本号+注释；`js/view/menus.js`：drawEnding 两行派生+一行绘制+页脚让位+注释）+ `CHANGELOG.md`（本条）+ `package.json`（test 串第 83 份）+ `README.md`（tests 树补 v2186/v2187 + 冒烟八十三件套（八十二件套清除）+ v21.87 守护描述 + 系统清单「尾声战绩页收集进度」新段）+ 新增 `tests/smoke_v2187_endingrecap.mjs`（仓库常驻，承 v21.10-v21.86 冒烟入库先例）+ 顺带把 v21.87 随新现实更新的姊妹 pin 一并落位：smoke_v2186..v2176 的 README 件套 pin（八十二→八十三件套，含嵌套复查链）、smoke_v2186..v2179 的 GAME_VERSION 字面量 pin（v21.86→v21.87）。
- 【验证】`npm run check` 25 模块全绿；`npm test` 八十三件套全绿（既有 82 件零回归 + smoke_v2187_endingrecap 全过）。

## v21.86 新成就「灵药初成」（酿造线里程碑）——采蘑菇→酿造锅→高级灵药这条自选系统链此前在 30 项成就里查无一条（图鉴 perfection/支线 allquests/碎片 memoir/技能 skills/宝箱 allchests/等级 lvl5·lvl10·lvl12/装备 legend·aegis/难度 hardtrue/精英 elites 各线都有里程碑，唯独「酿造」没有——玩家第一次酿出灵药毫无回响）（新内容·单成就，承 v21.76 一箱不漏 / v21.77 精英猎手 / v21.84 灯燃长夜 单成就先例；选题轮换：v21.83 新技能、v21.84 新成就、v21.85 体验打磨后本轮继续新内容——成就版图逐线核对，酿造/灵药线是最后一块无印记的空白；开工摸底：基线 git 干净树（最新提交 385e876，v21.85）、基线 npm test 八十一件套全绿、并发检查双测 mtime 无变化（上次落盘 22:07 CHANGELOG，本次开工 ~22:45）、纯新增成就零战斗/数值接触故无需生存模拟——故本轮选新内容而非平衡）

- 【新成就】`js/data.js`：酿造瓶数阈值补 `ELIXIR_GOAL = 1`（与 FIRSTBLOOD_GOAL / LVL5_GOAL / TREASURE_GOAL 同一「成就阈值数据化」家族——判定/进度两处同读一份源，改门槛只改 data.js 一处自动跟随，零裸字面量）+ ACH_LIST 末尾追加 `{id:'brew', name:'灵药初成', d:'酿造出第一瓶高级灵药', ok:g=>(g.brews||0)>=ELIXIR_GOAL, prog:g=>`${g.brews||0}/${ELIXIR_GOAL}`}`（30→31 项；无 r 字段纯里程碑，承 lvl5/memoir 惯例——奖励在酿造产物本身；prog 与 lvl5 同族「X/N 不钳制」口径）。关键设计：判定读**新增** `hero.brews` 防御式计数而非既有 potion2 库存——potion2 含任务奖励/掉落来源（四条支线各奖 1 瓶、rollDrop 6% 掉落），按它判定会把「任务送的灵药」误记为「酿造」；brews 只由 brewNow 写入，旧档无此字段=0 不误解锁、零迁移（承 v19.41 seen 同款，防御式 `(g.brews||0)` 读取）。
- 【反馈不迟到】`js/core.js` brewNow：酿造成功结算（mushrooms -= BREW_MUSHROOMS / gold -= BREW_GOLD / potion2++）之后补 `hero.brews = (hero.brews || 0) + 1;` + `applyAchievements();` 当场判定（承 v21.73 buyArmor 当场判定先例——不判定则要等下一场战斗胜利的 applyAchievements 通路才解锁，违背「反馈不迟到」惯例）；applyAchievements 幂等（已解锁不重报、不重复横幅）；成就横幅「🔓 成就解锁：【灵药初成】 酿造出第一瓶高级灵药」与「🧪 酿造成功！」经 v21.10 消息队列先后完整可见。
- 【零回归面】未动既有 30 个成就任何字段（brew 追加在末尾、displayOrder 不变）；未动 potion2 库存结算/酿造配方（BREW_MUSHROOMS/BREW_GOLD）/材料不足与任务蘑菇保护分支（仅在两处结算行之间插入计数+判定+注释）；未动 hero.js/unlockedAchievements/成就页渲染（ACH_LIST.length 动态读取，31 项仍 PAGE=10、滚动四页自动扩展）；未动存档格式（brews 随既有新档快照自动落盘、旧档防御式读取）。只改 2 个游戏文件（`js/data.js`：版本号+注释+常量+成就条目+导出；`js/core.js`：brewNow 计数+当场判定+注释）+ `CHANGELOG.md`（本条）+ `package.json`（test 串第 82 份）+ `README.md`（tests 树守护描述含 smoke_v2186_brew + 冒烟八十二件套（八十一件套清除）+ v21.86 守护描述 + C 键行「31 项」/图鉴&成就行「31 项成就」双处）+ 新增 `tests/smoke_v2186_brew.mjs`（仓库常驻，承 v21.10-v21.85 冒烟入库先例）+ 顺带把 v21.86 随新现实更新的姊妹 pin 一并落位：smoke_v2185/v2184/v2183/v2182/v2181/v2180/v2179/v2178/v2177/v2176 的 README 件套 pin（八十一→八十二件套，含嵌套复查链）、smoke_v2185/v2184/v2183/v2182/v2181/v2179 的 GAME_VERSION 字面量 pin（v21.85→v21.86）、smoke_v2184 的 ACH_LIST 精确计数断言去硬化（===30→>=30 存活性口径，承 v21.77 对 v2176 同款先例）、smoke_v2184/v2180/v2177/v2176/v2173/v2168/v2159 的 README 成就 pin（30→31 双处，含嵌套复查链）。
- 验证：`node --check js/data.js js/core.js tests/smoke_v2186_brew.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 八十二件套全绿（既有 81 件逐项零回归——含 v2185..v2176 更新后件套 pin、v2185..v2179 更新后 GAME_VERSION pin、v2184..v2159 更新后成就 pin 与 v2184 去硬化断言—— + 新增 v21.86 **全过**——GAME_VERSION 格式合法且越过 v21.85、data.js 含 v21.86 注释与版本字面量、ELIXIR_GOAL===1 导出、ACH_LIST 31 项精确 + brew 唯一/无 r/描述逐字/判定与常量同读/prog 0/1·1/1·3/1 不钳制、既有 30 成就 id 零回归（grain 29 / brew 30 序位）、ok 谓词逐值（缺字段 false/0 false/1 true/3 true）、core.js 源级落位（brews 计数 + 当场 applyAchievements + potion2++/配方常量逐字保留）、unlockedAchievements 集成三档、brewNow 运行期全链路四档（真实酿造 brews1/potion2 1/扣料扣金一致 + 横幅「🔓 成就解锁：【灵药初成】」即时可见 + 重复酿造去重/材料不足拦截/任务蘑菇保护拦截零计数）、applyAchievements 运行期三档（1 解锁/0 不误报/重复去重）、drawAch 31 项+第四页滚动渲染不抛错、README/package 同步（tests 树/八十二件套/v21.86 守护描述/31 项双处/第 82 份）、姊妹件套 pin 复查（v2185..v2176 八十二件套 + v2185..v2179 GAME_VERSION v21.86 + v2184..v2159 成就 pin 31 项双处 + v2184 去硬化确认））。

## v21.85 试炼碑通关后状态如实分档——「已通关」不再挂「新挑战」档（体验打磨·信息透明·状态如实，承 v19.52 阵容预览 / v19.60 奖励预告 / v21.46 推荐等级 / 强敌祭坛「击败自动熄灭」同主线；选题轮换：v21.80 新内容、v21.81-82 体验打磨、v21.83 新技能、v21.84 新成就后本轮回体验打磨——开工摸底：基线 git 干净树（最新提交 86a6522，v21.84）、基线 npm test 八十件套全绿、并发检查双测 mtime 无变化（上次落盘 20:51 CHANGELOG，本次开工 ~21:10）、存档/数值零接触故无需生存模拟——纯显示改动，故本轮选体验打磨而非平衡/新内容；选题排查：试炼碑状态标签是「凡有状态、如实显示」主线的最后一块拼图——祭坛 ⚠Lv 击败熄灭、宝箱已开等同图状态均有如实反馈，唯独试炼碑已通关（hero.rushDone，「百炼成钢」落袋）后仍挂「⚔️ 试炼三连战 + 通关奖」默认新挑战档，「还能不能再打/还能不能再拿奖」无从判断）

- 【现实核查：再战确实仍发全额通关奖】`battle.winBattle` 试炼通关分支（`stage >= RUSH_BOSSES.length`）无 `hero.rushDone` 守卫——三连战通关后仍可重复踏碑开战、每次仍按 `rushReward(level)` 发全额通关奖（Lv12 = 390 金）。这是既有设计行为（试炼碑踏碑判定 `onTrialStele` 同样无 rushDone 门），本版**不改结算**——只让展示如实反映它（与 v21.62「只改文字口径不绑键」同族：发现的是一处展示与事实脱节，不是结算 bug）。
- 【碑上标签分档】`js/view/drawWorld.js` 试炼碑显示块（cave TRIAL glyph）`lab` 改三档：未解锁 `试炼·未解锁` 逐字不变；已解锁未通关 `⚔️ 试炼三连战 {roster} · 建议Lv.{RUSH_REC_LV}` 逐字不变（roster/建议等级仍读 `RUSH_BOSSES`→`SPECIES[].lv` / `RUSH_REC_LV` 单一数据源）；已通关 → `✅ 试炼三连战 · 已通关（可再战）`（阵容/建议等级已通关玩家不需要，同祭坛熄灭「不再报挑战信息」口径）。
- 【奖励行分档】`if (ready)` 内 `rewLab` 分档：未通关 `💰 通关奖 {rushReward} 金（随等级）` 逐字不变；已通关 → `💰 再战通关奖 {rushReward} 金（随等级）`——与 winBattle 无守卫行为如实对应，不藏不误导（若未来给再战加一次性守卫，本行随冒烟即红）。
- 【面向提示分档】`faceHint` 试炼碑分支 `lab = S.G.rushDone ? '踩上再战' : '踩上挑战'`——与碑上标签同一份 `S.G.rushDone` 源，脚下方框不再与头顶标签口径打架；其余分支（喷泉/宝箱/祭坛/水晶/传送门/门锁/NPC/商店/旅馆/酿造/读碑）逐字零回归。
- 【零回归面】未动 `battle.winBattle` 任何结算（rushDone 置位/`rushReward`/`hero.gold += reward`/`applyAchievements`/换关回血逐字未动）；未动 `world.onTrialStele`（`bossDefeated && caveBoss → startRush` 逐字未动，再战可重复是既有行为）；未动 `RUSH_BOSSES/RUSH_REC_LV/rushReward/SPECIES` 任何数值；未动守碑人台词/成就「百炼成钢」/试炼徽记/帮助页四项；未动 HUD/存档/读档。只改 2 个游戏文件（`js/view/drawWorld.js`：碑标签/奖励行/faceHint 三处分档 + v21.85 注释；`js/data.js`：GAME_VERSION v21.84→v21.85 + 版本注释）+ `CHANGELOG.md`（本条）+ `package.json`（test 串第 81 份）+ `README.md`（tests 树收录 smoke_v2185_steleclear + 冒烟八十一件套（八十件套清除）+ v21.85 守护描述 + 系统清单试炼碑行补已通关分档口径）+ 新增 `tests/smoke_v2185_steleclear.mjs`（仓库常驻，承 v21.10-v21.84 冒烟入库先例）+ 顺带把 v21.85 随新现实更新的姊妹 pin 一并落位：smoke_v2184/v2183/v2182/v2181/v2180/v2179/v2178/v2177/v2176 的 README 件套 pin（八十→八十一件套，含嵌套复查链）、smoke_v2184/v2183/v2182/v2181/v2179 的 GAME_VERSION 字面量 pin（v21.84→v21.85）。
- 验证：`node --check js/view/drawWorld.js js/data.js tests/smoke_v2185_steleclear.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 八十一件套全绿（既有 80 件逐项零回归——含 v2184..v2176 更新后件套 pin、v2184/v2183/v2182/v2181/v2179 更新后 GAME_VERSION pin—— + 新增 v21.85 **42/42 全过**——GAME_VERSION 格式合法且越过 v21.84、data.js 含 v21.85 注释与版本字面量、drawWorld.js 源级落位（✅ 已通关（可再战）/💰 再战通关奖/踩上再战 + S.G.rushDone 三处同源 + 既有档 试炼·未解锁/⚔️ 试炼三连战/💰 通关奖/踩上挑战 逐字零残留）、rushReward(12)===390 与 RUSH_REC_LV===12/RUSH_BOSSES 3 关互证、运行期三档渲染捕获实证（已通关档 标签+再战奖励+踩上再战 逐字落位且新挑战档零渲染 / 未通关档 逐字零回归且已通关档零渲染 / 未解锁档 试炼·未解锁 且挑战/奖励/面向提示全不渲染）、宽度预算（已通关标签 ≈156px / 再战奖励 ≈164px ≤480）、README/package 同步（tests 树收录/八十一件套/v21.85 守护描述/系统清单已通关分档/第 81 份）、姊妹件套 pin 复查（v2184..v2176 八十一件套 + v2184/v2183/v2182/v2181/v2179 GAME_VERSION v21.85））。



- 【新成就】`js/data.js`：等级阈值常量补 `LVL12_GOAL = 12`（与 LVL5_GOAL/LVL10_GOAL 同族，已导出——判定/描述/进度三处同读一份源，改门槛只改 data.js 一处自动跟随，零裸字面量）+ ACH_LIST 在 lvl10 之后追加 `{id:'lvl12', name:'灯燃长夜', d:\`等级达到 ${LVL12_GOAL} 级\`, ok:g=>g.level>=LVL12_GOAL, prog:g=>\`${g.level||1}/${LVL12_GOAL}\`}`（29→30 项；无 r 字段纯里程碑，承 lvl5/lvl10 惯例；判定读既有 hero.level、零新计数/零新状态/零迁移——13+ 级同达标，等级无上限）；名取「灯燃长夜」：练到终局等级，手里的灯足够亮、足以燃过无字回廊的长夜；GAME_VERSION v21.83→v21.84 + 版本注释（设计意图/选题轮换/终局等级语义）。
- 【零回归面】未动既有 29 个成就任何字段（lvl12 追加在 lvl10 之后、displayOrder 不变）；未动 lvl5/lvl10 判定/描述/进度（d 同式 `/级`、prog 同式 `X/N` 逐字不变）；未动 hero.grantXp/level 递增/存档任何逻辑；未动 RUSH_REC_LV/终焉之神 SPECIES 任何数值（仅作设计目标等级的互证被冒烟引用）；未动 drawAch 渲染（ACH_LIST.length 动态读取，30 项仍 PAGE=10 三页滚动）。只改 2 个游戏文件（`js/data.js`：版本号+注释+常量+成就条目+导出）+ `CHANGELOG.md`（本条）+ `package.json`（test 串第 80 份）+ `README.md`（C 键行「30 项」/图鉴&成就行「30 项成就」/tests 树收录 smoke_v2184_lvl12 + 冒烟八十件套（七十九件套清除）+ v21.84 守护描述）+ 新增 `tests/smoke_v2184_lvl12.mjs`（仓库常驻，承 v21.10-v21.83 冒烟入库先例）+ 顺带把 v21.84 随新现实更新的姊妹 pin 一并落位：smoke_v2183/v2182/v2181/v2180/v2179/v2178/v2177/v2176 的 README 件套 pin（七十九→八十件套，含嵌套复查链）、smoke_v2183/v2182/v2181/v2179 的 GAME_VERSION 字面量 pin（v21.83→v21.84）、smoke_v2180/v2177/v2176/v2173/v2168/v2159 的 README 成就 pin（29→30 双处，含嵌套复查链）、smoke_v2180 的 ACH_LIST 精确计数断言去硬化（===29→>=29 存活性口径，承 v21.77 对 v2176 同款先例）。
- 验证：`node --check js/data.js tests/smoke_v2184_lvl12.mjs tests/smoke_v2183_mpsip.mjs tests/smoke_v2176_allchests.mjs tests/smoke_v2177_elites.mjs tests/smoke_v2178_codexseen.mjs tests/smoke_v2179_titlerecap.mjs tests/smoke_v2180_grain.mjs tests/smoke_v2181_helpquickcast.mjs tests/smoke_v2182_winrecap.mjs tests/smoke_v2159_skillach.mjs tests/smoke_v2168_hardtrue.mjs tests/smoke_v2173_aegis.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 八十件套全绿（既有 79 件逐项零回归——含 v2183..v2176 更新后件套 pin、v2183/v2182/v2181/v2179 更新后 GAME_VERSION pin、v2180/v2177/v2176/v2173/v2168/v2159 更新后成就 pin 与 v2180 去硬化断言—— + 新增 v21.84 **44/44 全过**——GAME_VERSION 格式合法且越过 v21.83、data.js 含 v21.84 注释与版本字面量、LVL12_GOAL===12 导出、ACH_LIST 30 项精确 + lvl12 唯一/无 r/描述由常量派生、ok/prog 谓词逐值（11 级 false / 12·15 级 true / 无 level false；prog 1/12·11/12·12/12·缺失防御式 1/12）、既有 29 成就 id 零回归 + lvl5/lvl10 判定零回归、RUSH_REC_LV===12 与 LVL12_GOAL 互证、unlockedAchievements 集成三档、applyAchievements 运行期三档（12 级真实解锁落 hero.ach / 11 级不误报 / 重复调用去重）、drawAch 30 项首屏+第三页滚动渲染不抛错、README/package 同步（tests 树收录/八十件套/30 项双处/v21.84 守护描述/第 80 份）、姊妹件套 pin 复查（v2183..v2176 八十件套 + v2183/v2182/v2181/v2179 GAME_VERSION v21.84 + v2180 去硬化确认））。

## v21.83 Lv11 新技能「星砂回响」——全游戏首个「汲蓝」招（新内容·战斗机制，承 v21.48 汲光击先例）：技能领悟表自 v21.48（Lv9 汲光击）后 Lv10-12（终焉之神/试炼的高压收尾段）再无新招——Lv11 补全游戏首个 MP 汲回招：伤害 ×2.4 并把伤害的一半汲回为 MP（单次上限 25% 最大MP，DRAIN_MP_PCT/DRAIN_MP_CAP 单一数据源派生），与汲光击（汲回 HP）同族错位——边打边回的「打蓝循环」，对口陨石术 14MP 的持久战；封印口径：祸乱形态「封印治愈」封的是治疗——汲蓝招不含治疗，刻意不并入 heal 封印（与 drain 招不同，不钻「回血」空子），为终焉之神战保留一条被封印治愈后仍可运转的 MP 引擎（skillForbidden/技能菜单封印条件逐字零改动即「未封」契约）；光元素恒 ×1 中性（无魔物弱/抗 'light'）（选题轮换：v21.80 新内容、v21.81-82 体验打磨两连后本轮继续新内容——技能轮换回归；开工摸底：基线 git 干净树（最新提交 72341d2，v21.82）、基线 npm test 七十八件套全绿、并发检查双测 mtime 无变化（上次落盘 18:13 CHANGELOG，本次开工 ~19:05）、中期生存模拟复核（村100%/林99.72%/矿100%/廊100%，数值健康无需再调——故本轮选新战斗机制而非平衡；承 v21.48「技能轮换自 v17.x 停在 Lv7」后 Lv9 补招先例，Lv10-12 是最后一段无招空白）

- 【新技能】`js/data.js`：LEARN_AT 补 `11: '星砂回响'`（与 1/3/4/5/7/9 同表，MAX_LEARN_LV 自动派生 9→11——skillXpHint/成就诸技通明/读档补学三端自动跟随）+ SKILL_DATA['星砂回响'] `{mp:12, mult:2.4, kind:'atk', sfx:'ice', txt:'✨', element:'light', drainMp:DRAIN_MP_PCT, drainMpCap:DRAIN_MP_CAP, hint:'汲回伤害50%为MP·上限25%MP', colors:[星辉蓝三色]}`——drainMp/drainMpCap 由新常量 DRAIN_MP_PCT(0.5)/DRAIN_MP_CAP(0.25) 派生（与 DRAIN_PCT/DRAIN_HP_CAP 同族独立旋钮，已导出）；hint 与结算/战报三处同读一份源；光元素沿用 ELEM_NAME '光'（v21.48 已补，恒 ×1 中性，不扰动既有克制表）；H 页「技能克制」行 r[1] 补「 / 星砂回蓝」（14px estW ≈374.3 ≤470 面板预算，r[2] 倍率规则逐字零回归）。注释写明封印口径设计意图与旧档兼容（Lv≥11 老存档 migrateQuests 自动补领，承 v21.48 同族）。
- 【汲蓝结算】`js/battle.js` doSkill：drain（汲回 HP）块之后补 drainMp 块——`mcap = round(mpMax × drainMpCap)`、`dm = min(mpMax−mp, min(mcap, round(dmg×drainMp)))`、`hero.mp += dm`、战报 `（汲蓝 N MP）`（与汲光击「汲回 0 HP」同族：满蓝付费后按可回量钳制、如实标注；零新状态/零存档变化）。skillForbidden 封印条件逐字零回归——drainMp 招不由 `(kind==='heal' || skill.drain)` 命中 → 祸乱形态不封（未封契约）。
- 【七招排版容量】`js/view/drawBattle.js` 技能菜单行距条件式：`ROW_SP = skills.length > 6 ? 29 : 34` + `HINT_DY = >6 ? 14 : 16`（≤6 招逐字保持 v21.48 布局零回归；7 招时末行提示基线 348 不触页脚「当前 MP」368）；`js/view/menus.js` 状态页技能行条件式：`SKILL_ROW_SP = >6 ? 12 : 14` + `SKILL_ROW_Y0 = >6 ? 302 : 308`（≤6 招零回归；7 招时末行基线 380 不触「📖 下一技能」396）。数字键 1-7 快捷直发（main.js `'1'..'7'` + 页脚/H 页「数字键1-7」口径）与 v21.48 逐字兼容——第 7 招恰好坐满 1-7 号键位，零改动。
- 【零回归面】未动既有 6 招任何字段（SKILL_DATA 逐字未动、LEARN_AT 1/3/4/5/7/9 逐字未动）；未动 elemMult/克制表/灼烧/冻结/碎甲/汲回任何结算；未动 skillForbidden/drawBattle 封印标注条件（未封契约即「刻意不改」）；未动 hero.checkSkills/skillXpHint 逻辑（LEARN_AT/MAX_LEARN_LV 驱动自动接入）；未动 ACH_LIST（诸技通明判定/描述由 LEARN_AT.length 派生自动 6→7）；未动 ELEM_NAME/SPECIES（光元素仍无弱/抗引用）。只改 3 个游戏文件（`js/data.js` 版本号+注释+常量+LEARN_AT/H 页行+导出；`js/battle.js` doSkill 汲蓝块；`js/view/drawBattle.js`+`js/view/menus.js` 七招排版条件式）+ `CHANGELOG.md`（本条）+ `package.json`（test 串第 79 份）+ `README.md`（战斗段汲蓝描述/系统清单技能链/数值速查技能领悟行 + tests 树收录 smoke_v2183_mpsip + 冒烟七十九件套（七十八件套清除）+ v21.83 守护描述）+ 新增 `tests/smoke_v2183_mpsip.mjs`（仓库常驻，承 v21.10-v21.82 冒烟入库先例）+ 顺带把 v21.83 随新现实更新的姊妹 pin 一并落位：smoke_v2182/v2181/v2180/v2179/v2178/v2177/v2176 的 README 件套 pin（七十八→七十九件套，含嵌套复查链）、smoke_v2182/v2181/v2179 的 GAME_VERSION 字面量 pin（v21.82→v21.83）、smoke_v2148 的 MAX_LEARN_LV 精确 pin（9→11）与排版 pin（ROW_SP/状态页行距条件式随新现实）、smoke_v2159 的技能全表 pin（6→7 招列表/谓词档位/标签）与 smoke_v2161 的技能契约 pin（6→7 招）。
- 验证：`node --check js/data.js js/battle.js js/view/drawBattle.js js/view/menus.js tests/smoke_v2183_mpsip.mjs tests/smoke_v2148_drain.mjs tests/smoke_v2159_skillach.mjs tests/smoke_v2161_skillhint.mjs tests/smoke_v2182_winrecap.mjs tests/smoke_v2181_helpquickcast.mjs tests/smoke_v2179_titlerecap.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 七十九件套全绿（既有 78 件逐项零回归——含 v2182/v2181/v2180/v2179/v2178/v2177/v2176 更新后 件套 pin、v2182/v2181/v2179 更新后 GAME_VERSION pin、v2148/v2159/v2161 更新后技能 pin—— + 新增 v21.83 **48/48 全过**——GAME_VERSION 格式合法且越过 v21.82、data.js 含 v21.83 注释与版本字面量、LEARN_AT 11 级条目与既有等级零回归、MAX_LEARN_LV 自动派生 11、DRAIN_MP_PCT/DRAIN_MP_CAP 导出 0.5/0.25、SKILL_DATA 字段落位（mp12/×2.4/atk/光/drainMp·drainMpCap 由常量派生）与 hint 同源、七招全配 SKILL_DATA 且 mp 正整数/hint 非空、光元素恒 ×1 中性契约、H 页技能克制行 r[1] 补星砂回蓝且 r[2] 逐字、battle.js 汲蓝结算源级落位与 skillForbidden 封印条件逐字零回归（未封契约）、drawBattle/menus 七招排版条件式源级落位、运行期六档实证（上限钳制 15、可回量钳制 12、封印不封且汲光击仍封对照、未学拦截、Lv11 旧档补学七招、Lv10 不越级）、checkSkills 领悟报文逐字与已会不重报、skillXpHint Lv10 扫到星砂回响/Lv11 全招 null、skillEstimate 预览正值、七招菜单/七招状态页/六招状态页渲染不抛错、README/package 同步（tree 收录/七十九件套/Lv11 星砂回响 数值速查行/第 79 份）、姊妹件套 pin 复查（v2182..v2176 七十九件套 + v2182/v2181/v2179 GAME_VERSION v21.83））。

## v21.82 胜利画面补收集进度两件——「灯芯回来了」的 run 总结屏补齐 v21.79 确立的收集三件套口径：drawWin 是击败幽冥魔王后的结算屏（v19.49 战绩行已有 累计讨伐/成就 N/M/时长），但 v21.79 标题预览确立的「收集进度三件套」（成就·图鉴·宝箱）在这一刻仍缺 图鉴 与 宝箱——玩家站在这趟 run 的总结屏上想「继续收集还是 R 重开/Enter 尾声」，一眼看不到图鉴/宝箱收到哪了（成就页/图鉴页/状态页/标题预览都得再翻）（体验打磨·信息透明·口径一致，承 v21.79 标题预览三件套 / v19.49 战绩补全同一主线；选题轮换：v21.80 新内容、v21.81 体验打磨后本轮回体验打磨——开工摸底：整目录基线备份 `/tmp/my-jrpg_backup_20260909_180400`、基线七十七件套全绿、并发检查双测 mtime 无变化、生存模拟数值健康无需再调（v21.68 起历轮口径），故本轮选体验打磨而非平衡/新内容）

- 【漏网：战绩行只有成就一件】`view/menus.js` drawWin 在 v19.49 战绩行（y=362）之后补一行 `📕 图鉴 ${codexN}/${BESTIARY_TARGET.length} · 📦 宝箱 ${chestN}/${chestTotal()}`（y=378，bold 14px #7dd47f 与标题预览同色系）——`codexN = BESTIARY_TARGET.filter((n) => ((S.G.bestiary || {})[n] | 0) >= 1).length`、`chestN = chestCount(S.G)`，与 slotPreview（v21.79）/成就页/图鉴页/状态页同读 ACH_LIST / BESTIARY_TARGET / chestCount·chestTotal 一份单一数据源（chestCount 三形态防御式、旧档零迁移、|0 归一与 v21.79 同式），绝无第二套口径；v19.49 战绩行与主标题/等级行逐字未动；页脚 `按 Enter 观看尾声 · 按 R 重开新档(连按两次)` 文案逐字未动、y 380→396 让位（画布 640×480 垂直富余，主标题/怪物零位移）。纯显示零结算零数值零存档变化。
- 【零回归面】未动 battle.js/core.js/world.js/drawDead/drawEnding/标题预览任何逻辑（仅 drawWin 一体内 2 行派生 + 1 行绘制 + 页脚 y）；未动 ACH_LIST/BESTIARY_TARGET/chestCount/chestTotal 任何数值；未动成就判定/图鉴计数/宝箱计数任何结算。只改 2 个游戏文件（`js/view/menus.js`：drawWin 补两件 + 页脚让位 + 注释；`js/data.js`：GAME_VERSION v21.81→v21.82 + 版本注释）+ `CHANGELOG.md`（本条）+ `package.json`（test 串第 78 份）+ `README.md`（tests 树收录 smoke_v2182_winrecap + 冒烟七十八件套（七十七件套清除）+ v21.82 守护描述 + 系统清单「胜利画面收集进度」条目）+ 新增 `tests/smoke_v2182_winrecap.mjs`（仓库常驻，承 v21.10-v21.81 冒烟入库先例）+ 顺带把 v21.82 随新现实更新的姊妹 pin 一并落位：smoke_v2181/v2180/v2179/v2178/v2177/v2176 的 README 件套 pin（七十七→七十八件套，含嵌套复查链）、smoke_v2181 与 smoke_v2179 的 GAME_VERSION 字面量 pin（v21.81→v21.82）。
- 验证：`node --check js/view/menus.js js/data.js tests/smoke_v2182_winrecap.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 七十八件套全绿（既有 77 件逐项零回归——含 v2181..v2176 更新后 pin 断言—— + 新增 v21.82 **36/36 全过**——GAME_VERSION 格式合法且越过 v21.81、data.js 含 v21.82 版本注释与版本字面量、drawWin 源级落位（图鉴/宝箱派生与 slotPreview 同式互证、v19.49 战绩行与页脚文案逐字保留、旧页脚 y=380 零残留、收集行 y=378 与页脚 y=396）、行宽预算（最坏收集行 14px ≈300px ≤640 画布）与垂直间距（362/378/396 互不重叠）、运行期实证（DOM/音频/存储桩 + main.js 真实导入：推进档渲染捕获「📕 图鉴 2/13 · 📦 宝箱 2/12」与「累计讨伐 3 只 · 成就 1/29 · ⏱01:02:03」且不抛错、缺失字段防御档捕获「图鉴 0/M · 宝箱 0/N」）、README/package 同步（tree 收录/七十八件套/v21.82 守护描述/系统清单条目/第 78 份）、六件旧冒烟 pin 复查（v2181..v2176 七十八件套 + v2181/v2179 GAME_VERSION v21.82））。

## v21.81 帮助页「战斗」行补技能菜单数字键快捷直发口径——H 页作战前知识中枢的入口补齐：数字键 1-7 快捷直发技能（v19.48 起）在战斗内技能菜单页脚（drawBattle「[↑↓]选择 [Enter]施放 [数字键]快捷 [Esc]取消」）与 README 战斗段（「或按数字键 1-7 快捷直发——两种方式同效」）都有标注，唯独 H 页「战斗」行只写「2技能(↑↓/Enter 选招)」——H 页作为「战前知识中枢」的操作总清单（v21.14 确立）漏了这个同效入口（体验打磨·可发现性，承 v21.14「功能存在就必须能看到入口」/v21.29-32/v21.38-43 同一主线；选题轮换：v21.78-79 体验打磨、v21.80 新内容后本轮继续体验打磨）

- 【新入口】`js/data.js` HELP_PAGES 操作说明页「战斗」行 r[2] 次行追加 ` · 技能菜单数字键1-7快捷直发`——r[1] 主行（`1攻击 2技能(↑↓/Enter 选招) 3药水 4逃跑(约60%) 5防御 6蓄力`，FLEE_SUCCESS 派生）逐字未动；r[2] 既有三段（蓄力×CHARGE_MULT 同源派生 / Boss无法逃跑 / ↑↓ 回看战斗记录）逐字未动、新段追加在末；12px estW ≈453.6 ≤470 面板预算（x=100 起、面板右缘 570），行数不变仍 14（>10 行 sp=25 档不触页脚 452）；纯文字零逻辑零结算零数值变化。与战斗内技能菜单页脚「[数字键]快捷」、main.js 数字键 1-7 分派（`'1'..'7'` indexOf + idx<list.length）、README「数字键 1-7 快捷直发」三方同口径互证。
- 【零回归面】未动 HELP_PAGES 其余 13 行与其余三页任何字段；未动 drawHelp 渲染（r[2] 机制 v19.59 起既有）；未动 main.js/battle.js/drawBattle.js 任何逻辑（仅作为功能真实性互证被冒烟引用）；未动 README 快速上手表/战斗段（早已同口径，本次仅补 H 页一端）。只改 2 个游戏文件（`js/data.js`：GAME_VERSION v21.80→v21.81 + 版本注释 + 战斗行 r[2] + 行注释）+ `CHANGELOG.md`（本条）+ `package.json`（test 串第 77 份）+ `README.md`（tests 树收录 smoke_v2181_helpquickcast + 七十七件套（七十六件套清除）+ v21.81 守护描述）+ 新增 `tests/smoke_v2181_helpquickcast.mjs`（仓库常驻，承 v21.10-v21.80 冒烟入库先例）+ 顺带把 v21.81 随新现实更新的姊妹 pin 一并落位：smoke_v2180/v2179/v2178/v2177/v2176 的 README 件套 pin（七十六→七十七件套，含嵌套复查链）、smoke_v2179 的 GAME_VERSION 字面量 pin（v21.80→v21.81）。
- 验证：`node --check js/data.js tests/smoke_v2181_helpquickcast.mjs tests/smoke_v2176_allchests.mjs tests/smoke_v2177_elites.mjs tests/smoke_v2178_codexseen.mjs tests/smoke_v2179_titlerecap.mjs tests/smoke_v2180_grain.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 七十七件套全绿（既有 76 件逐项零回归——含 v2180/v2179/v2178/v2177/v2176 更新后 pin 断言—— + 新增 v21.81 **27/27 全过**——GAME_VERSION 格式合法且越过 v21.80、data.js 含 v21.81 版本注释与版本字面量、HELP_PAGES 战斗行契约（唯一存在/r[1] 与 FLEE_SUCCESS 拼接逐字恒等零回归/r[2] 含新增段与既有三段逐字/r[2] 新段序位）、行宽预算（r[2] 12px ≈453.6 ≤470、r[1] 14px ≈375.2 ≤470 巡检）、页长预算（操作说明页仍 14 行 sp=25 档）、功能真实三方互证（drawBattle「[数字键]快捷」/main.js '1'..'7' 分派/README「数字键 1-7 快捷直发」）、README/package 同步（tree 收录/七十七件套/v21.81 守护描述/第 77 份）、六件旧冒烟 pin 复查（v2180..v2176 七十七件套 + v2179 GAME_VERSION v21.81））。

## v21.80 潮灯镇新 NPC「粮铺掌柜」+ 讨伐支线「护粮的委托」（side_grain）+ 单支线成就「护粮安民」——开荒期第一条镇内全流程讨伐支线：潮灯镇此前只有灯长「采集型」（side_mushroom）与守书记「雾语林讨伐型」（side_stone）两条支线，唯独镇子自家田里晃的「四基础怪」没有任何任务挂钩——哥布林是潮灯镇遇敌池限定怪（village.pool 之一、Lv.1 即可撞见），接取→讨伐→交付全流程在镇南粮田一圈内完成（新内容，承 v21.52 拾骨人新支线先例；开工摸底：既有提交 262643b（v21.79）即基线、基线 npm test 七十五件套全绿、并发检查双测 mtime 无变化、中期生存模拟历来 村100%/林99.6+/矿100%/廊100% 数值无需再调——故本轮选新内容而非平衡；选题轮换：v21.76-77 新成就两连、v21.78-79 体验打磨两连后本轮继续新内容）

- 【新 NPC + 新支线】数据层四件套：`js/data.js` village.extras 落 `{ x: 15, y: 12, ty: 'NPC' }`（镇南粮田西缘——全图 extras 扫描 (15,12) 仅此一处、全局坐标键未被任何他图占用、可行走格非路径要塞）+ NPC_SPOTS `'15,12':'grainman'` + NPCS.grainman（name 粮铺掌柜 / mark basket / 静态 lines 兜底）+ `js/view/sprites.js` NPC_SHEET 映射 grainman→mwCartman（杂货商贩造型，星砂车夫同款）。QUESTS.side_grain 四态任务页（offer/active/turnin/done）与 GRAIN_GOAL=3 单一数据源（cond/condProg/obj/offer 同读，与 MIST/STONE/EMBER/BONE_GOAL 同一「支线目标单一数据源」家族）；无 unlockOn → 从开局即 offer、保留「接取→讨伐→交付」完整流程（承 side_ember 同款）；奖励 50 金 + 1 生命药水（开荒期最低配，介于 side_mist 60 金之下、与镇内 Lv.1 档次匹配）；active 页按 hero 实时报进度（承 side_stone/side_bone 函数页同款）；哥布林弱冰提示与 SPECIES 哥布林 weak:'ice' 单一数据源口径一致。
- 【新成就】`js/data.js` ACH_LIST 末尾追加 `{id:'grain', name:'护粮安民', d:'完成粮铺掌柜的委托（护粮的委托）', ok:g=>!!(g.quests&&g.quests.side_grain==='done')}`（28→29）——与 quest/cartman/names/mist/stone/ember/bone 同一「单支线成就」模式（读既有 g.quests.side_grain==='done'，交付侧 status 由 quests.setSideQuest 写定），零新计数/零新状态/零新依赖；无 r 字段（奖励在任务结算侧，同 cartman/mist/stone 惯例）。
- 【灯火同心自动跟随】支线分母由 QUESTS 派生 7→8（allquests 本体零改动，d「完成全部 8 个支线任务」/prog「0/8」自动跟随；成就页/日志/状态页/任务顶标/胜利战报「📜 支线讨伐进度」全部走既有 QUESTS 驱动通路自动接入）。
- 【零回归面】未动既有 28 个成就任何字段（grain 追加在末尾、displayOrder 不变）；未动任何地图 rows/传送门/遇敌池/掉落/数值；未动 quests.js/battle.js/world.js/core.js 任何逻辑（新支线全走既有通路）；未动 NPC_SPOTS 既有 17 键；未动掌灯阿婆/拾骨人等任何 NPC。只改 3 个游戏文件（`js/data.js` 版本号+注释+常量+NPC/支线/成就+导出；`js/view/sprites.js` NPC_SHEET 一行）+ `README.md`（潮灯镇条目 + C 键行「29 项」+ 图鉴&成就行「29 项成就」+ tests 树收录 smoke_v2180_grain + 七十六件套（七十五件套清除）+ v21.80 守护描述）+ `CHANGELOG.md`（本条）+ `package.json`（test 串第 76 份）+ 新增 `tests/smoke_v2180_grain.mjs`（仓库常驻，承 v21.10-v21.79 冒烟入库先例）+ 顺带把 v21.80 随新现实更新的姊妹 pin 一并落位：smoke_v2179 的 GAME_VERSION 字面量 pin（v21.79→v21.80）与 README 件套 pin（七十五→七十六件套）、smoke_v2178/v2177/v2176 的 README 件套 pin（七十五→七十六件套）、smoke_v2177 的 ACH_LIST 精确计数断言（===28→>=28 存活性口径，承 v21.76 先例）与 QUESTS side 支线条数断言（7→8）、smoke_v2177/v2176/v2173/v2168/v2159 的 README 成就 pin（28→29 双处，含嵌套复查链）、smoke_v2152/v2159/v2168/v2173/v2176/v2177 六件的 allquests 分母断言（0/7→0/8）、smoke_v2160 的五条讨伐支线名单与 condProg 计数断言（5→6）、smoke_v2145 日志全量落画名单补「未归的矿灯/护粮的委托」。
- 验证：`node --check js/data.js js/view/sprites.js tests/smoke_v2180_grain.mjs tests/smoke_v2152_bonequest.mjs tests/smoke_v2159_skillach.mjs tests/smoke_v2160_questprog.mjs tests/smoke_v2145_journalscroll.mjs tests/smoke_v2168_hardtrue.mjs tests/smoke_v2173_aegis.mjs tests/smoke_v2176_allchests.mjs tests/smoke_v2177_elites.mjs tests/smoke_v2178_codexseen.mjs tests/smoke_v2179_titlerecap.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 七十六件套全绿（既有 75 件逐项零回归——含 v2179/v2178/v2177/v2176/v2160/v2145 更新后 pin 断言—— + 新增 v21.80 **56/56 全过**——GAME_VERSION 格式合法且越过 v21.79、data.js 含 v21.80 注释与版本字面量、GRAIN_GOAL===3 导出、NPC_SPOTS '15,12'===grainman 唯一映射 + 既有 17 键未动 + 全图 extras 扫描 (15,12) 仅 village 一处、NPCS.grainman 契约、QUESTS.side_grain 契约（cond 2 假 3 真/prog 2/3/奖励 50 金+1 药水/四态页 [Enter] 收尾/offer 由 GRAIN_GOAL 派生）、ACH_LIST 29 项精确 + grain ok 谓词两档 + allquests 0/8 + 既有成就 id 零回归、运行期全链路（offer→accept→active（进度 2/3 只）→turnin→reward（+50 金 +1 药水）→done→成就 true→顶标归零→重复不重发）、npcQuestPages 函数页进度/offer 叙事/done「（支线任务·已完成）」、旧档零迁移落 offer、loadMap(village) 落位 + Enter/E 真实交互开对话、SPECIES 哥布林 weak:ice 契约、sprites 映射源级、README/package 同步、六件旧冒烟 0/8 pin 复查）。

## v21.79 标题页存档预览补「收集进度」三件套——选槽前一眼看清「哪个档更完整」：slotPreview 此前报 姓名/等级/金币/地图/进度/难度/存档时间/时长（v19.45/v19.64/v19.65），唯独缺收集进度——多存档槽玩家想挑更完整的档继续，只能进游戏按 I/C/B 逐页翻（体验打磨·信息透明，承 v19.45-65「选槽前一眼看清」同一主线；选题轮换：v21.76-78 新成就/图鉴汇总连续三版后本轮回体验打磨）

- 【新汇总】`js/core.js` slotPreview：预览行末追加 ` · 成就N/M·图鉴N/M·宝箱N/M`——N 分别与成就页「已解锁 X/28」、图鉴页「记忆收录 X/13」、状态页「📦 已开 X/全图 N」同读 ACH_LIST / BESTIARY_TARGET / chestCount·chestTotal 一份单一数据源（分母亦同源派生，绝无第二套口径）；防御式 `(hero.ach||[]).length` / `(hero.bestiary||{})[n]|0`（旧布尔 true 归一为 1）/ `chestCount(hero)`（Set/数组/缺失三形态兼容），旧档零迁移零误报；纯显示零结算零存档变化；既有段（姓名/Lv/金币/地图进度/难度/存档时间/时长）逐字未动，仍单行 13px 居中（最坏估算宽 ≈577 ≤620，640 画布两侧余量），标题页布局零位移（y=360 行未动、无换行）。
- 【零回归面】未动 saveGame/load/snapshotHero/restoreChests 任何逻辑；未动 drawTitle（pv 行渲染逐字未动）；未动成就/图鉴/宝箱任何计数与显示；未动 ACH_LIST/BESTIARY_TARGET/chestTotal 任何数据。只改 2 个游戏文件（`js/core.js`：data.js 导入补四常量 + slotPreview 派生段 + 注释；`js/data.js`：GAME_VERSION v21.78→v21.79 + 版本注释）+ `README.md`（系统清单标题页预览口径 + tests 树收录 + 七十五件套（七十四件套清除）+ v21.79 守护描述）+ `CHANGELOG.md`（本条）+ `package.json`（test 串第 75 份）+ 新增 `tests/smoke_v2179_titlerecap.mjs`（仓库常驻，承 v21.10-v21.78 冒烟入库先例）+ 顺带把 `tests/smoke_v2178_codexseen.mjs` 的 README 件套 pin（七十四件套→七十五件套，含其对 smoke_v2177/v2176 的复查断言）与 `tests/smoke_v2177_elites.mjs` 的 README 件套 pin（七十四件套→七十五件套，含其对 smoke_v2176 的复查断言）与 `tests/smoke_v2176_allchests.mjs` 的 README 件套 pin（七十四件套→七十五件套）随新现实更新。
- 验证：`node --check js/core.js js/data.js tests/smoke_v2179_titlerecap.mjs tests/smoke_v2176_allchests.mjs tests/smoke_v2177_elites.mjs tests/smoke_v2178_codexseen.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 七十五件套全绿（既有 74 件逐项零回归——含 smoke_v2176/v2177/v2178 更新后断言与 smoke_v1965 slotPreview 子串断言（追加段不破坏）—— + 新增 v21.79 **30/30 全过**——GAME_VERSION 格式合法且越过 v21.78、data.js 含 v21.79 注释与版本字面量、core.js 源级落位（四常量导入 + achN/codexN/chestN 派生 + 三件套段与分母同源 + 既有段逐字保留 + 单行无换行）、运行期五档实证（新档 0/0/0 → 推进档 2/2/2 逐值 → 旧布尔 bestiary 归一为 1 → 旧字段缺失防御档不抛错 → 困难+真结局档（无字回廊·灯已归还·困难）→ 空槽 null）、行宽预算 ≤620（最坏 + 四姓名抽查）、README/package.json 同步、smoke_v2178/v2177/v2176 件套 pin 七十五件套确认）。
- 【另附：开工摸底】整目录基线备份 `/tmp/my-jrpg_backup_20260909_141432`（铁律：整目录 cp -R）；基线 git 干净树（最新提交 70a9ca2，v21.78）；并发检查双测 mtime 无变化（上次落盘 13:11 data.js / 13:26 CHANGELOG，本次开工 ~14:15）；中期生存模拟复核（balance_sim 真实 randomEncounter 蒙特卡洛：普通·3药·智能 100% / 保守 100% / 无药 99.53% · 困难·3药·智能 99.13% / 无药裸刷 15.35%（玩家自选保留）；凶手分布均匀——野狼/哥布林/毒蛇，无「角色倒挂」，数值健康无需再调，故本轮选体验打磨）；选题排查：「选槽前一眼看清」主线（v19.45 难度/v19.64 时间戳/v19.65 时长）三件已齐，唯收集进度是最后一块空白——成就页/图鉴页/状态页三端计数早已存在，却从未在标题预览汇过总。

## v21.78 图鉴页脚补「已遭遇 X/13」汇总——图鉴统计口径补齐「见过 vs 打过」第三档：页脚此前只有「记忆收录 X/13（已讨伐种类数）/累计讨伐（只数）」，但 v21.37 起每行已遭遇未讨伐者标了真实「已遭遇 ✕N」——「至少撞见过几种」这个总数却始终缺席：玩家想知道「还剩几种从没碰到过（❓）」，只能从 13 行里逐行数 ❓ 或数「⚠️ 尚未讨伐」（体验打磨·信息透明，承 v21.37 已遭遇计数化同一主线；选题轮换：v21.76-77 新成就两连后本轮回体验打磨）

- 【新汇总】`js/view/menus.js`：drawCodex 页脚「累计讨伐」行由 `累计讨伐：N   ·   额外掉落：M` 补为 `累计讨伐：N   ·   已遭遇：K/13   ·   额外掉落：M`——K = BESTIARY_TARGET.filter(n => ((hero.seen||{})[n]|0) > 0).length，与每行 seenCt 同读 hero.seen 计数（battle.startBattle 每次进战 +1、真身经 canonicalName 归一，与讨伐/记忆收录同口径），BESTIARY_TARGET 同源派生——调图鉴目标种数只改 data.js 一处、本汇总自动跟随，绝无第二套口径；防御式 `(hero.seen||{})[n]|0` 读取（旧档布尔 true 归一为 1、缺失为 0），零新计数/零新状态/零存档变化、纯显示；与上方「记忆收录 X/13」并读即「见过 K 种 vs 打过 X 种」双口径，一眼看出还差几种没撞见（❓ 种数 = 13 − K）。行宽预算：14px estW ≈297.5 ≤480（面板 80..560 全长，页脚居中 320 两侧余量充足），未触及「按 B / Esc 关闭」页脚行与图鉴滚动/簇行布局（404/426/448 三行基线逐字未动）。
- 【零回归面】未动 hero.seen 任何计数逻辑（battle.startBattle 逐字未动）；未动图鉴每行「已遭遇 ✕N / 讨伐 ✕N / 未讨伐」渲染（seenCt/got 分支逐字未动）；未动「记忆收录：X/13」行（404 基线逐字未动）；未动 BESTIARY_TARGET/FRAGMENTS/成就任何数据；未动 README 成就口径（仍 28 项）。只改 2 个游戏文件（`js/view/menus.js`：met 派生 + 页脚行 + 注释；`js/data.js`：GAME_VERSION v21.77→v21.78 + 版本注释）+ `README.md`（图鉴&成就行补「页脚汇总 已遭遇 X/13」+ tests 树收录 + 七十四件套（七十三件套清除）+ v21.78 守护描述）+ `CHANGELOG.md`（本条）+ `package.json`（test 串第 74 份）+ 新增 `tests/smoke_v2178_codexseen.mjs`（仓库常驻，承 v21.10-v21.77 冒烟入库先例）+ 顺带把 `tests/smoke_v2177_elites.mjs` 的 README 件套 pin（七十三件套→七十四件套，含其对 smoke_v2176 的复查断言）与 `tests/smoke_v2176_allchests.mjs` 的 README 件套 pin（七十三件套→七十四件套）随新现实更新。
- 验证：`node --check js/view/menus.js js/data.js tests/smoke_v2178_codexseen.mjs tests/smoke_v2177_elites.mjs tests/smoke_v2176_allchests.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 七十四件套全绿（既有 73 件逐项零回归——含 smoke_v2177/v2176 更新后断言—— + 新增 v21.78 **26/26 全过**——GAME_VERSION 格式合法且越过 v21.77、data.js 含 v21.78 注释、menus.js 源级落位（met 派生 + 页脚并入已遭遇 + 旧页脚文案零残留 + seenCt 每行口径零回归）、行宽预算 ≤480、三档运行期实证（全未知档「已遭遇：0/13」且「记忆收录：0/13」→ 真实 startBattle 链部分撞见档（史莱姆 ✕2 + 幽冥魔王真身归一 ✕1）「已遭遇：2/13」且行级 ✕2/✕1 零回归 → 全撞见档「已遭遇：13/13」且「记忆收录：1/13」独立）、README/package.json 同步、smoke_v2177/v2176 件套 pin 七十四件套确认）。

## v21.77 新成就「精英猎手」（双精英讨伐里程碑）——精英线里程碑补齐：全游戏仅有的两只 isElite 精英——雾语林随机精英「石心魔像」（约 7% 撞见、掉记忆碎片、必掉蘑菇、石甲机制）与无字回廊中段祭坛「残焰魔像」（守名者支线「残焰的安息」目标）——此前成就版图里只有侧面纪念（碎片/支线），唯独没有「猎手」这一档：图鉴（perfection）/支线（allquests）/碎片（memoir）/技能（skills）/宝箱（allchests）各线里程碑齐备，双精英本身反而无印记（新内容·单成就，承 v21.68 逆风行灯 / v21.73 龙鳞加身 / v21.76 一箱不漏 单成就先例；选题轮换：v21.74-75 体验打磨、v21.76 新成就后本轮继续新内容——成就版图逐线核对（等级/金币/讨伐/图鉴/支线/碎片/技能/装备/难度/试炼/宝箱全齐），精英线是最后一块空白）

- 【新成就】`js/data.js`：ACH_LIST 末尾追加 `{id:'elites', name:'精英猎手', d:`讨伐精英「${ELITE_GOLEM.name}」与「${EMBER_GOLEM.name}」`, ok:g=>((g.bestiary||{})[ELITE_GOLEM.name]||0)>=1 && ((g.bestiary||{})[EMBER_GOLEM.name]||0)>=1, prog:g=>`${(((g.bestiary||{})[ELITE_GOLEM.name]||0)>0?1:0)+(((g.bestiary||{})[EMBER_GOLEM.name]||0)>0?1:0)}/2`}`（27→28）——判定/进度/描述三处同读 ELITE_GOLEM.name / EMBER_GOLEM.name 一份源（与 withSpecies/图鉴/帮助页同一份精英数据源，改精英名只改 data.js 一处、本成就自动跟随，绝无第二套口径；两者定义在 ACH_LIST 之前，无 TDZ 困扰）；判定读既有 hero.bestiary 讨伐计数（与 perfection/condProg 同一份源），防御式 `(g.bestiary||{})[name]||0` 读取、旧档零迁移；无 r 字段（与 memoir/skills/aegis/hardtrue/allchests 同款纯里程碑——击杀本身即奖励：碎片/蘑菇/支线进度）。解锁时机：winBattle 既有 applyAchievements 通路（battle.js 六处调用之一），第二只精英落袋当场解锁、反馈不迟到（bestiary 结算落账在前、判定在后）。
- 【可达性守护】SPECIES 中 isElite 恰两只（石心魔像/残焰魔像）——与 ELITE_GOLEM/EMBER_GOLEM.name 逐字一致、与 BESTIARY_TARGET 13 种（含两者）同一表族；双精英都是可击杀遭遇（石心魔像随机精英 ~7%、残焰魔像回廊固定祭坛 tile 435），非隐藏/不可达，本版冒烟把「isElite 恰两只 + 双名同源 + 13 种含双精英」钉死为契约，未来新增/改名精英即红。
- 【零回归面】未动 ELITE_GOLEM/EMBER_GOLEM 任何数值（hp/atk/def/xp/gold/acts/phase2 逐字未动）；未动既有 27 个成就任何字段（elites 追加在末尾、displayOrder 不变）；未动 winBattle/bestiary 计数/condProg/applyAchievements 任何逻辑（新成就走既有判定通路自动接入）；未动 drawAch 渲染（ACH_LIST.length 动态读取，28 项仍 PAGE=10 三页滚动）；残焰已熄（ember 支线成就，quests 判定）与灯火同心（allquests，支线分母仍 0/7）零回归。只改 2 个游戏文件（`js/data.js`：GAME_VERSION v21.76→v21.77 + 版本注释 + elites 条目；`README.md`：成就口径「28 项」双处 + tests 树收录 + 七十三件套（七十二件套清除）+ v21.77 守护描述）+ `CHANGELOG.md`（本条）+ `package.json`（test 串第 73 份）+ 新增 `tests/smoke_v2177_elites.mjs`（仓库常驻，承 v21.10-v21.76 冒烟入库先例）+ 顺带把 `tests/smoke_v2176_allchests.mjs` 的 ACH_LIST 总数断言（===27→>=27 存活性口径，承 v21.73/v21.76 对旧版同款先例）与 README 件套 pin（七十二件套→七十三件套）及 README 成就 pin（27→28）随新现实更新 + 顺带把 `tests/smoke_v2168_hardtrue.mjs` 与 `tests/smoke_v2159_skillach.mjs` 的 README 成就 pin（27→28，含 smoke_v2168 对 smoke_v2159 的复查断言）随新现实更新 + 顺带把 `tests/smoke_v2173_aegis.mjs` 的 README 成就 pin（27→28，含其对 smoke_v2168/smoke_v2159 的复查断言）随新现实更新 + 顺带把 `tests/smoke_v2164_achname.mjs` 四档运行期用例的预置 ach 补 `elites`（fullBestiary 含双精英会连带新成就横幅，预置剔除后聚焦原版 perfection 横幅断言）。
- 验证：`node --check js/data.js tests/smoke_v2176_allchests.mjs tests/smoke_v2173_aegis.mjs tests/smoke_v2168_hardtrue.mjs tests/smoke_v2159_skillach.mjs tests/smoke_v2164_achname.mjs tests/smoke_v2177_elites.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 七十三件套全绿（既有 72 件逐项零回归——含 smoke_v2176/v2173/v2168/v2159 更新后断言与 smoke_v2164 连带项预置更新（fullBestiary 含双精英会连带 elites，四档预置 ach 剔除后聚焦原版横幅断言）+ 新增 v21.77 **44/44 全过**——GAME_VERSION 格式合法且越过 v21.76、data.js 含 v21.77 注释、SPECIES isElite 恰两只契约（与 ELITE_GOLEM/EMBER_GOLEM.name 一致）、ELITE_GOLEM/EMBER_GOLEM 定义行源级落位、BESTIARY_TARGET 仍 13 种含双精英（讨伐可及）、ACH_LIST 契约（elites 唯一/总数精确 28/无 r 有 prog/d 由双精英名派生/源级落位）、ok 谓词逐值七档（空对象/空 bestiary/仅石心魔像/仅残焰魔像/双 1 只/多次讨伐 ≥1/计 0 无讨伐）、prog 四档（0/2、石心 1/2、焰 1/2、2/2）、既有 27 成就 id 零回归、残焰已熄（ember）零回归、allquests 分母 0/7 不受影响、QUESTS side 仍 7 条、unlockedAchievements 集成三档（双档含 elites/单档空档不含/已解锁不重复报）、运行期全链路（newGame 空 bestiary 不解锁 → 双精英落账真实解锁落 hero.ach 且横幅「🔓 成就解锁：【精英猎手】讨伐精英「石心魔像」与「残焰魔像」」齐备 → 重复调用去重）、battle.js applyAchievements 调用 ≥6 处源级落位（winBattle 既有通路）、drawAch 两态+第三页滚动渲染不抛错、README/package.json 同步、smoke_v2176 总数断言去硬化确认（===27 零残留）、smoke_v2176/v2168/v2159 README 成就 pin 28 项双处落位确认）。
- 【另附：开工摸底】本轮以 git 干净树（最新提交 a3ca4ef，v21.76）为基线（铁律二选一：既有提交即基线，无需再整目录副本）；基线 `npm test` 七十二件套全绿后开工（后台串跑 EXIT=0 确认）；并发检查双测 mtime 无变化（上次落盘 10:10 data.js / 10:25 CHANGELOG，本次开工 ~11:15）；中期生存模拟 `/tmp/jrpg_mid_sim.mjs` 复核（村100%/林99.66%/矿100%/廊100%，数值无需再调——故本轮选新内容而非平衡）；选题排查：成就版图（等级/金币/讨伐/图鉴/支线/碎片/技能/装备/难度/试炼/宝箱）逐线核对，精英线（双 isElite）是唯一无印记的线——石心魔像只有记忆碎片侧面纪念、残焰魔像只有守名者支线侧面纪念。

## v21.76 新成就「一箱不漏」（全图宝箱全收集里程碑）——宝箱线全收集档补齐：收集向「全」档版图的最后一块空白——图鉴全收集（perfection）/支线全清（allquests）/记忆碎片集齐（memoir）/技能全领悟（skills）都有满档里程碑，唯独宝箱只有 6 只半程档「开箱寻宝」（TREASURE_GOAL），开遍全图 12 只（镇2+林3+矿2+星砂宝藏4+廊1，chestTotal() 单一数据源，v21.22 起状态页「已开 X/全图 N」同源公示）的玩家毫无纪念（新内容·单成就，承 v21.68 逆风行灯 / v21.73 龙鳞加身 单成就先例；选题轮换：v21.74-75 体验打磨两连后本轮回新内容）

- 【新成就】`js/data.js`：ACH_LIST 末尾追加 `{id:'allchests', name:'一箱不漏', d:\`开启全图全部 ${chestTotal()} 个宝箱\`, ok:g=>chestCount(g)>=chestTotal(), prog:g=>\`${chestCount(g)}/${chestTotal()}\`}`（26→27）——判定/进度/描述三处同读 chestCount/chestTotal 一份源（v21.22 数据化收口，加/删宝箱只改 MAPS/CAVE_TREASURE 一处、成就自动跟随，绝无第二套口径）；判定读既有 hero.chests 存档字段（world.onChestStep 开箱 add、读档 restoreChests 归一为 Set、chestCount 防御式兼容 Set/数组/缺失三形态），零新计数/零新状态/零迁移（旧档无 chests 字段 → chestCount=0 < 12 不误解锁）；无 r 字段（与 memoir/skills/aegis/hardtrue 同款纯里程碑——宝箱内容本身就是奖励）。解锁时机：onChestStep 开箱当场 applyAchievements（v19.51 既有「反馈不迟到」通路），第 12 只落袋即解锁，winBattle 各处判定通路兜底。
- 【可达性守护】hero.chests 以裸「x,y」局部坐标为键——若两张图在同一局部坐标各放一只宝箱，Set 去重会让 chestCount 永远到不了 chestTotal（成就死锁）；当前全图 12 只逐图扫描无冲突（镇 22,1/22,16、林 22,4/12,11/1,16、矿 16,2/1,12+星砂 17,8/18,8/17,9/18,9、廊 16,1），本版冒烟把「坐标键跨图唯一」钉死为契约，未来加箱撞键即红。
- 【零回归面】未动 TREASURE_GOAL=6 与「开箱寻宝」任何判定/描述/进度（6 只半程档原样）；未动其余 25 个成就任何字段（allchests 追加在末尾、displayOrder 不变）；未动 onChestStep/rollDrop/chestCount/chestTotal 任何逻辑（新成就走既有判定通路自动接入）；未动 drawAch 渲染（ACH_LIST.length 动态读取，27 项仍 PAGE=10 三页滚动）；灯火同心支线分母仍 0/7（allchests 不是支线）。只改 2 个游戏文件（`js/data.js`：GAME_VERSION v21.75→v21.76 + 版本注释 + allchests 条目；`README.md`：成就口径「27 项」双处 + tests 树收录 + 七十二件套（七十一件套清除）+ v21.76 守护描述）+ `CHANGELOG.md`（本条）+ `package.json`（test 串第 72 份）+ 新增 `tests/smoke_v2176_allchests.mjs`（仓库常驻，承 v21.10-v21.75 冒烟入库先例）+ 顺带把 `tests/smoke_v2175_battletag.mjs` 的 README 件套断言按 v21.7 惯例去硬化（存活性口径，实件数由本版冒烟守护）+ 顺带把 `tests/smoke_v2173_aegis.mjs` 的 ACH_LIST 总数断言（===26→>=26 存活性口径，承 v21.73 对 smoke_v2168「===25→>=25」同款先例）与 README 成就 pin（26→27）随新现实更新 + 顺带把 `tests/smoke_v2168_hardtrue.mjs` 与 `tests/smoke_v2159_skillach.mjs` 的 README 成就 pin（26→27）随新现实更新。
- 验证：`node --check js/data.js tests/smoke_v2173_aegis.mjs tests/smoke_v2168_hardtrue.mjs tests/smoke_v2159_skillach.mjs tests/smoke_v2175_battletag.mjs tests/smoke_v2176_allchests.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 七十二件套全绿（既有 71 件逐项零回归——含 smoke_v2175 去硬化后断言与 smoke_v2173/smoke_v2168/smoke_v2159 更新后断言 + 新增 v21.76 **38/38 全过**——GAME_VERSION 格式合法且越过 v21.75、data.js 含 v21.76 注释、chestTotal===12 与 chestCount 三形态契约、TREASURE_GOAL=6 未动、全图宝箱坐标键跨图唯一（可达性钉死）、ACH_LIST 契约（allchests 唯一/总数精确 27/无 r 有 prog/d 由 chestTotal 派生/源级落位）、ok 谓词逐值七档（空档/0 只/6 只/11 只/12 只 Set/12 只数组/13 只超出）、prog 三档（0/12、6/12、12/12）、既有 26 成就 id 零回归、开箱寻宝半程档零回归（5 只 false/6 只 true/prog 6/6）、allquests 分母 0/7 不受影响、unlockedAchievements 集成三档、运行期全链路（newGame 空 Set 不解锁 → 12 只真实解锁落 hero.ach 且横幅「🔓 成就解锁：【一箱不漏】开启全图全部 12 个宝箱」齐备 → 重复调用去重）、运行期 E2E 四档（move(0,-1) 真实踩上村庄宝箱 (22,1) 且 chests 计入达 12 / 第 12 只落袋当场解锁 / 解锁横幅与开箱报文双段齐备 / 已开箱格反复踩踏不重复计解锁）、drawAch 两态+第三页滚动渲染不抛错、README/package.json 同步、smoke_v2175 件套断言去硬化确认、smoke_v2173 总数断言随新现实更新确认）。
- 【另附：开工摸底】整目录基线备份 `/tmp/my-jrpg_backup_20260909_093551`（铁律：整目录 cp -R）；基线 `npm test` 七十一件套全绿后方开工；并发检查双测 mtime 无变化（上次落盘 08:50，本次开工 ~09:36）；选题轮换：v21.73 新成就、v21.74-75 体验打磨两连，本轮回新内容——收集向「全」档版图逐线核对（图鉴/支线/碎片/技能齐备），宝箱线是唯一缺口（状态页 v21.22 起就公示「已开 X/全图 N」，成就页却查无此档）。

## v21.75 战斗画面普通魔物补「敌方特性」角标——毒蛇/雾灵的招牌机制战斗内首次可见：drawBattle 的「敌方招数一览」（右上角 620,162 槽位）只覆盖持 acts 的 Boss/精英，普通魔物该位置刻意留白——但带 SPECIES tag 的普通怪（毒蛇「☠️ 会施毒 · 扣血3回合」/雾灵「❄️ 雾凝成冰」）其特性此前只在图鉴（要已讨伐才揭示）与 H 页可见，战斗内全程无一字：第一次撞见毒蛇的玩家只有中招掉血后才知道它会施毒（体验打磨·信息透明，承 v6.x 招数一览 / v21.2 精英出没预告 / v21.47 威胁预警重击线同一「威胁提前可见」主线；选题轮换：v21.73-74 内容/按键口径后本轮回战斗信息透明打磨）

- 【修正：招数一览块补 else 分支——无 acts 时同槽位直读 SPECIES tag 显示「敌方特性：…」】`js/view/drawBattle.js`：`if (enemy.acts && enemy.acts.length) {…招数一览…}` 补 else 分支：`const spTag = (SPECIES[canonicalName(enemy.name)] || {}).tag; if (spTag) text('敌方特性：…', 620, 162, 'bold 11px', '#8fa8b8', 'right')`——与招数一览同槽位同式同色（620,162 右对齐灰蓝），两角标由 if/else 互斥绝不并存；tag 直读 SPECIES[canonicalName].tag（普通怪实例 scaleEnemy 本就不带 tag 字段——单一数据源走 SPECIES，与图鉴 codexTag / 帮助页「石心魔像·出没」行同一份源，调特性文案只改 data.js 一处、三端自动跟随，零裸字面量）；无 tag 的六种白板普通怪（史莱姆/野狼/骷髅兵/哥布林/树精/石魔像）保持留白零噪音；石甲/灼烧/冻结角标在 112-144 行，特性行 162 不与其重叠。纯显示零结算零存档变化。
- 【零回归面】未动 SPECIES 任何条目（tag 文案/acts/phase2 逐字未动）；未动 encounter.scaleEnemy（普通怪实例字段集原样）；未动招数一览 if 分支逐字（Boss/精英行招表原样）；未动 codexTag/帮助页/威胁预警任何逻辑；未动战利品预览行的 弱点/抗性/bonus 显示。只改 2 个游戏文件（`js/view/drawBattle.js`：else 分支 + 注释；`js/data.js`：GAME_VERSION v21.74→v21.75 + 版本注释）+ `README.md`（战斗段补「敌方特性」角标口径 + tests 树收录 + 七十一件套（七十件套清除）+ v21.75 守护描述）+ `CHANGELOG.md`（本条）+ `package.json`（test 串第 71 份）+ 新增 `tests/smoke_v2175_battletag.mjs`（仓库常驻，承 v21.10-v21.74 冒烟入库先例）+ 顺带把 `tests/smoke_v2174_storye.mjs` 的 README 件套断言按 v21.7 惯例去硬化（存活性口径，实件数由本版冒烟守护）。
- 验证：`node --check js/view/drawBattle.js js/data.js tests/smoke_v2175_battletag.mjs tests/smoke_v2174_storye.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 七十一件套全绿（既有 70 件逐项零回归——含 smoke_v2174 去硬化后断言—— + 新增 v21.75 **25/25 全过**——GAME_VERSION 格式合法且越过 v21.74、data.js 含 v21.75 注释、drawBattle.js 源级 else 分支与 SPECIES/canonicalName 同源读取落位、SPECIES tag 契约（毒蛇含「会施毒」且回合数 POISON_TURNS 派生 / 雾灵「雾凝成冰」逐字 / 六种白板普通怪无 tag 零噪音 / 五种 acts 强敌不进 else 分支）、scaleEnemy 实例不带 tag 单一数据源契约、运行期实证六档（毒蛇战亮「敌方特性：☠️ 会施毒 · 扣血3回合」逐字且落位 620,162 右对齐同槽位且无招数一览 / 雾灵战亮「❄️ 雾凝成冰」/ 史莱姆战特性与招数一览双无零噪音 / 石心魔像战招数一览含石甲零回归且无特性行 / 幽冥魔王战招数一览含重击回血零回归且无特性行 / 全档 drawBattle 渲染不抛错）、README/package.json 同步、smoke_v2174 件套断言去硬化确认）。
- 【另附：开工摸底】整目录基线备份 `/tmp/my-jrpg_backup_20260909_0832`（铁律：整目录 cp -R）；基线 `npm test` 七十件套全绿后方开工；并发检查双测 mtime 无变化（上次落盘 07:58，本次开工 ~08:32）；中期生存模拟 `/tmp/jrpg_mid_sim.mjs` 复核（村100%/林99.68%/矿100%/廊100%，数值无需再调——故本轮选体验打磨而非平衡）；选题排查：战斗画面信息透明版图（招数一览/攻击预判/变身线/战利品预览/角标列）逐块核对，普通魔物特性是唯一「图鉴/H 页都有、战斗内无一字」的漏网（毒蛇施毒是开荒期最需要预警的普通怪机制）。

## v21.74 开场叙事页翻页补 E 键别名——「Enter/E 同效」口径的最后一个漏网场景收口：README 快速上手表与 H 页「对话 / 确认」行自 v21.29 起就承诺「Enter / E」、对话进行中（talk.onKey）v21.43 已同效，唯独开场叙事页（STORY 五页）同为「按确认键翻页」语境却仍只认 Enter——新玩家看完文档按 E 翻开场毫无反应（体验打磨·口径一致，承 v21.29 大地图 E 交互 / v21.43 对话翻页 E 别名同一主线；v21.16/v21.33/v21.43 同族「文档写的键按了没反应」；选题轮换：v21.73 新成就内容后本轮回体验打磨）

- 【修正：story.onKey 补 e/E 与 Enter 同路径，页脚与 STORY 末行同步如实标注双键】`js/main.js`：story.onKey 的早退条件由 `if (e.key !== 'Enter') return;` 改为 `if (e.key !== 'Enter' && e.key !== 'e' && e.key !== 'E') return;`——E 与 Enter 完全同路径：翻页 / 末页 goto('world') / 首次教程提示（💡 教程，v21.30 Enter/E对话口径）三状态零行为差；KEY 无 'e' 映射（不与移动键冲突）。`js/view/menus.js`：drawStory 逐帧提示由 `按 Enter 继续` 改为 `按 Enter / E 继续`（瞬时消息 ms=0 机制逐字未动，承 v21.18/v21.70「按键提示必须如实反映可用键」主线）。`js/data.js`：STORY 末行 `[ Enter ] 去把灯芯讨回来` 改为 `[ Enter / E ] 去把灯芯讨回来`（15px estW ≈181.6 ≤520 叙事面板预算内）+ GAME_VERSION v21.73→v21.74 + 版本注释；`js/view/hud.js` 消息队列注释里的页脚引述同步。纯入口与提示文字，零结算零数据零存档变化。
- 【零回归面】未动 talk.onKey 的 v21.43 E 别名（对话打字机补全/翻页/结束三状态原样）；未动 world.onKey 的 v21.29 E 交互；未动 pause.onKey 的 `if (e.key !== 'Enter') return;`（暂停菜单无 E 别名需求，全文件仅此一处合法保留）；未动 story→world 的 renderHUD/tutDone/教程触发条件与 STRONG_MSG_MS 旅程报文；未动 STORY 前四页文案与 drawStory 淡入/行距/配色。只改 4 个游戏文件（`js/main.js`：story.onKey 条件 + 注释；`js/view/menus.js`：drawStory 页脚 + 注释；`js/data.js`：版本号 + 注释 + STORY 末行；`js/view/hud.js`：注释引述）+ `README.md`（快速上手表 Enter/E 行补「开场叙事页 E 同效翻页——v21.74」+ tests 树收录 + 七十件套（六十九件套清除）+ v21.74 守护描述）+ `CHANGELOG.md`（本条）+ `package.json`（test 串第 70 份）+ 新增 `tests/smoke_v2174_storye.mjs`（仓库常驻，承 v21.10-v21.73 冒烟入库先例）+ 顺带把 `tests/smoke_v2173_aegis.mjs` 的 README 件套断言按 v21.7 惯例去硬化（存活性口径，实件数由本版冒烟守护）。
- 验证：`node --check js/main.js js/data.js js/view/menus.js js/view/hud.js tests/smoke_v2174_storye.mjs tests/smoke_v2173_aegis.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 七十件套全绿（既有 69 件逐项零回归——含 smoke_v2173 去硬化后断言与 smoke_v2130 教程 Enter 路径—— + 新增 v21.74 **29/29 全过**——GAME_VERSION 格式合法且越过 v21.73、data.js 含 v21.74 注释、STORY 末行新文案落位与旧文案零残留 + 五页契约 + 行宽预算、main.js 新条件落位与旧条件零残留（pause 一处合法保留）、menus.js 页脚新文案落位与旧裸文案零残留、KEY 无 e 映射、运行期实证八档（第 1 页按 e 翻页 / 第 2 页按 E 翻页 / 无关键 x 原地不动 / 第 3 页 Enter 翻页零回归 / 末页后按 e 进 world 且 tutDone 落账并真实发出 💡 教程（Enter/E对话口径）/ 非首次档末页按 E 进 world 不抛错 / drawStory 渲染不抛错 / 页脚瞬时提示逐字「按 Enter / E 继续」）、README/package.json 同步、smoke_v2173 件套断言去硬化确认）。
- 【另附：开工摸底】整目录基线备份 `/tmp/my-jrpg_backup_20260909_0736`（铁律：整目录 cp -R）；基线 `npm test` 六十九件套全绿后方开工；并发检查双测 mtime 无变化（上次落盘 06:49，本次开工 ~07:36）；选题排查：「Enter/E 同效」主线（v21.29 大地图/v21.30 教程/v21.32 面向提示/v21.43 对话）逐场景核对，story 是最后一个只认 Enter 的「确认/翻页」场景（pause 无 E 需求、create 方向键体系、title/win/dead/ending 各有专属键位提示）。

## v21.73 新成就「龙鳞加身」（最强铠甲里程碑）+ buyArmor 当场判定成就——装备向里程碑空白补齐：武器端「黎明归剑」（legend）早有里程碑，防具端四件铠甲毫无纪念（新内容，承 v20.4 灯火记忆 / v21.59 诸技通明 / v21.68 逆风行灯 单成就先例；开工摸底：上轮中期生存模拟 村100%/林99.67%/矿100%/廊100% 数值无需再调，近六轮均为体验打磨/对话差分，本轮轮换为新内容）

- 【新成就：「龙鳞加身」——装备最强铠甲（当前为龙鳞甲，防+13），ACH_LIST 25→26】`js/data.js`：ARMORS 之后新增 `BEST_ARMOR` 派生常量（`Object.keys(ARMORS).reduce` 取防御值最大者，单一数据源——增删铠甲/调防只改 ARMORS 一处、成就自动跟随，零裸字面量；ARMORS 声明在 ACH_LIST 之前，无 v21.68「困难」的 TDZ 困扰，无需字面量钉死）；ACH_LIST 末尾追加 `{id:'aegis', name:'龙鳞加身', d:…派生…, ok:g=>g.armor===BEST_ARMOR}`——判定读既有 hero.armor 存档字段（newGame 建档「布衣」、buyArmor/rollDrop 换装写定、applyStats 同源），零新计数/零新状态/零迁移（旧档无 armor 字段时 undefined !== BEST_ARMOR 不误解锁）；无 r 无 prog 纯里程碑（与 legend 不同：最强铠甲商店有售、非掉落奖励）。
- 【buyArmor 补当场成就判定（反馈不迟到）】`js/shop.js`：buyArmor 在 applyStats 后补 `applyAchievements()`——buyWeapon 早已当场判定（legend 当场解锁），buyArmor 此前漏接；最强铠甲唯一来源就是商店（rollDrop 装备档只到锁子甲），不当场判定要等下一场战斗胜利才解锁，承 world.js 开箱当场判定同一惯例。applyAchievements 幂等（已解锁不重报、perfection 不重复加奖），购买判定/扣款/换装/报文逐字未动，零结算零数值变化。
- 【零回归面】未动 ARMORS 四件铠甲与价格；未动 rollDrop 装备档（仍只到锁子甲）；未动既有 25 成就任何判定/描述/奖励；未动 drawAch 渲染（分母读 ACH_LIST.length 自动跟随，三页滚动容量不变）；winBattle 六处 applyAchievements 通路原样兜底。只改 2 个游戏文件（`js/data.js`：GAME_VERSION v21.72→v21.73 + 版本注释 + BEST_ARMOR + ACH_LIST 一行；`js/shop.js`：buyArmor 一行 + 注释）+ `README.md`（成就口径 25→26 双处 + tests 树收录 + 六十九件套（六十八件套清除）+ v21.73 守护描述）+ `CHANGELOG.md`（本条）+ `package.json`（test 串第 69 份）+ 新增 `tests/smoke_v2173_aegis.mjs`（仓库常驻，承 v21.10-v21.72 冒烟入库先例）+ 顺带把 `tests/smoke_v2172_helpfrag.mjs` 的 README 件套断言按 v21.7 惯例去硬化 + `tests/smoke_v2168_hardtrue.mjs` 的 ACH_LIST 总数断言（===25→>=25 存活性口径）与 README 成就 pin（25→26）随新现实更新 + `tests/smoke_v2159_skillach.mjs` 的 README 成就 pin（25→26）随新现实更新。
- 验证：`node --check js/data.js js/shop.js tests/smoke_v2159_skillach.mjs tests/smoke_v2168_hardtrue.mjs tests/smoke_v2172_helpfrag.mjs tests/smoke_v2173_aegis.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 六十九件套全绿（既有 68 件逐项零回归——含 smoke_v2172 去硬化后断言与 smoke_v2168/smoke_v2159 更新后断言 + 新增 v21.73 **42/42 全过**——GAME_VERSION 锚点越过 v21.72、data.js 含 v21.73 注释、BEST_ARMOR 派生契约（===龙鳞甲、===ARMORS 最大防御者、源级 reduce 零裸字面量）、ARMORS 契约、ACH_LIST 契约（aegis 唯一/总数精确 26/无 r 无 prog/d 派生）、ok 谓词逐值六档、既有 25 成就 id 零回归、allquests 分母 0/7 不受影响、unlockedAchievements 集成三档、shop.js 源级双函数落位、运行期全链路（newGame 布衣不解锁 → armor=龙鳞甲 解锁 → 重复去重）、buyArmor 运行期三档（龙鳞甲当场解锁+扣款换装+双段报文 / 锁子甲不解锁 / 金币不足不解锁不扣款不换装）、drawAch 两态+三页滚动渲染不抛错、README/package.json 同步、smoke_v2172 去硬化确认、smoke_v2168/smoke_v2159 更新确认）。
- 【另附：开工摸底】整目录基线备份 `/tmp/my-jrpg_backup_20260909_0637`（铁律：整目录 cp -R）；基线 `npm test` 六十八件套全绿后方开工；并发检查双测 mtime 无变化（上次落盘 05:54，本次开工 ~06:37）；选题轮换：近六轮（v21.67-72）均为对话差分/防误触/按键提示/帮助页打磨，数值面近三轮模拟复核无需再调——本轮承 v21.68 单成就先例做新内容：装备向里程碑版图中防具端的唯一空白（武器端 legend 早有）。

## v21.72 帮助页「试炼进阶」补「记忆碎片」行——H 页战前知识中枢对记忆碎片系统的最后空白收口：四枚强敌首胜掉落的记忆碎片（FRAGMENTS：石心魔像/幽冥魔王/洞窟领主/终焉之神）是触发真结局「全记忆」加页（drawEnding ENDING_TRUE_FRAG）的关键收集，巡灯人支线（side_name）/成就「灯火记忆」/状态页「记忆 X/N」/名字石碑同读此源——但 H 页四页（操作/地图指南/魔物状态/试炼进阶）此前查无一行，玩家通关前无从知晓「强敌首胜会掉记忆、集齐有真结局差分」，只能在掉落瞬间的报文（battle.js「拾起一段记忆…按 J 日志回看」）里偶遇（体验打磨·信息透明·可发现性，承 v21.2 精英出没 / v21.15 高级灵药配方 / v21.23 三 Boss 机制预览 / v21.25 试炼赏金 / v21.46 推荐等级同一「H 页知识中枢收口」主线；开工摸底中期生存模拟 村100%/林99.67%/矿100%/廊100%，数值无需再调——故本轮选体验打磨；承 v21.70-71 防误触/互切提示两连打磨，本轮继续「功能存在就必须能看到入口」）

- 【修正：试炼进阶页新增「记忆碎片」行，插入三 Boss 机制行之前，枚数由 FRAGMENTS.length 派生】`js/data.js`：HELP_PAGES[3] 在「蘑菇宝箱」行后、「幽冥魔王」行前插入 `['记忆碎片','强敌首胜各掉一枚 · 集齐 ' + FRAGMENTS.length + ' 枚真结局追加全记忆页 · J 日志回看']`——枚数派生自 FRAGMENTS.length 单一数据源（增删碎片帮助页自动跟随，零裸字面量）；「J 日志回看」与掉落报文同口径；插入位置在三 Boss 机制行之前（本行「强敌首胜」正指下方三 Boss 与魔物状态页石心魔像）；末行金色收尾仍是终焉之神（drawHelp `i === P.length-1` 金色口径不变）。行数 9→10（仍 ≤10，drawHelp `sp = P.length > 10 ? 25 : 34` 页长自适应档不变）；新行无 r[2]（本页 r[2] 数仍 2：试炼三连战/终焉之神）；14px estW ≈400.9 ≤470 面板预算（v21.20 全页行宽巡检自动纳入）；末行（终焉之神）基线 402、其 r[2] 420 不触页脚 452。其余三页（操作 14/地图指南 5/魔物状态 10）逐字未动，纯文字零逻辑零结算零存档变化。
- 【零回归面】未动 FRAGMENTS 任何条目（4 枚/id/name/enemy/text 逐字未动）；未动 battle.js 碎片掉落判定与报文（「拾起一段记忆…按 J 日志回看」原样）；未动 drawEnding 真结局全记忆差分、巡灯人支线、成就「灯火记忆」、名字石碑任何逻辑；未动 drawHelp 任何渲染逻辑（行距/颜色/页脚逐字未动，仅数据源 HELP_PAGES[3] 增一行）。只改 2 个游戏/文档文件（`js/data.js`：GAME_VERSION v21.71→v21.72 + 版本注释 + HELP_PAGES[3] 新增一行；`README.md`：tests 树收录 + 六十八件套（六十七件套清除）+ v21.72 守护描述；`CHANGELOG.md`：本条）+ `package.json`（test 串第 68 份）+ 新增 `tests/smoke_v2172_helpfrag.mjs`（仓库常驻，承 v21.10-v21.71 冒烟入库先例）+ 顺带把 `tests/smoke_v2171_journalikey.mjs` 的 README 件套断言按 v21.7 惯例去硬化（存活性口径，实件数由 v21.72 冒烟守护）+ 顺带把七件旧冒烟的试炼进阶页「9 行」pin 按新现实 9→10 更新（承 v21.23 更新 smoke_v2120 同款先例：smoke_v2120_helprush/smoke_v2123_bosshelp/smoke_v2125_rushreward/smoke_v2127_stelenpc/smoke_v2128_mapstele/smoke_v2146_trialrec 的 page3.length pin + smoke_v2131_helptitle 的 PAGE_COUNTS [14,5,10,9]→[14,5,10,10]，其中 v2123/v2125/v2127 的页长派生预算断言同步改写为「末行基线 402、其 r[2] 420 ≤440」新口径，r[2] 数仍 2 不变）。
- 验证：`node --check js/data.js tests/smoke_v2120_helprush.mjs tests/smoke_v2123_bosshelp.mjs tests/smoke_v2125_rushreward.mjs tests/smoke_v2127_stelenpc.mjs tests/smoke_v2128_mapstele.mjs tests/smoke_v2131_helptitle.mjs tests/smoke_v2146_trialrec.mjs tests/smoke_v2171_journalikey.mjs tests/smoke_v2172_helpfrag.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 六十八件套全绿（既有 67 件逐项零回归——含 smoke_v2171 去硬化后断言与七件 9→10 更新后断言 + 新增 v21.72 **37/37 全过**——GAME_VERSION 格式合法且越过 v21.71、data.js 含 v21.72 注释、试炼进阶页 10 行/其余三页 14·5·10 零回归、记忆碎片行落位索引 6（蘑菇宝箱之后/幽冥魔王之前）、新行逐字口径与 FRAGMENTS.length 派生零裸字面量、新行无 r[2] 且本页 r[2] 数仍 2、末行仍是终焉之神、FRAGMENTS 契约（4 枚/四强敌）、battle.js 掉落报文口径零回归、新行 estW ≤470、全页行宽巡检、页长派生 sp=34/末行基线 402/r[2] 420 ≤440、运行期实证八档（drawHelp 第 4 页渲染不抛错/新行 (100,300) 普通色 #e8eef1 落位/枚数与 J 日志回看文本实证/终焉之神 (100,402) 金色 #ffd24a 零回归/幽冥魔王 +34px 移位落位 (100,334)/页脚「第 4/4 页」(320,452) 零回归/其余三页渲染不抛错）、README/package.json 同步、smoke_v2171 件套断言去硬化确认、七件旧冒烟旧 pin 零残留确认）。
- 【另附：开工摸底】整目录基线备份 `/tmp/my-jrpg-backup-20260909-052656`（铁律：整目录 cp -R）；基线 `npm test` 六十七件套全绿后方开工；并发检查双测 mtime 无变化（上次落盘 04:41，本次开工 ~05:27）；中期生存模拟 `/tmp/jrpg_mid_sim.mjs` 复核（村100%/林99.67%/矿100%/廊100%，数值无需再调——故本轮选体验打磨而非平衡）；选题排查：战报缺数主线（v21.49-66）已收官、NPC 通关差分（v21.67-69）已收官、按键提示口径（v21.70-71）已收官，本轮收口 H 页四页对「记忆碎片」系统的最后空白（碎片是唯一直通真结局差分的收集系统，H 页此前无一字）。

## v21.71 任务日志页脚补「I 状态页」互切提示——状态↔日志双向互切口径成对收官：main.js journal.onKey 自早前版本起就支持 `I` 直达状态页（main.js L65，与 status.onKey 的 `J` 直达日志互为双向互切），状态页页底也早已常驻「J 任务日志」提示（menus.js drawStatus 末行），唯独日志页两处页脚（空日志/有日志）只写「按 J / Esc 关闭」——玩家翻日志想对号属性时无从知晓按 I 可直切状态页（体验打磨·可发现性·口径一致，承 v21.4 帮助页 A/D 别名 / v21.18「按键提示必须如实反映可用键」/ v21.38 README 选槽口径同步主线；承 v21.67-69 内容三连后本轮轮换为体验打磨）

- 【修正：drawJournal 两处页脚同式补「· I 状态页」，既有口径逐字保留】`js/view/menus.js`：空日志分支页脚由 `按 J / Esc 关闭` 补为 `按 J / Esc 关闭   ·   I 状态页`；有日志分支页脚由 `按 J / Esc 关闭[· ↑↓ 滚动浏览（还有 N 条）]` 补为 `按 J / Esc 关闭[· ↑↓ 滚动浏览（还有 N 条）]   ·   I 状态页`——「按 J / Esc 关闭」与 v21.45「↑↓ 滚动浏览（还有 N 条）」口径逐字未动，仅尾部追加互切段；页脚 12px/320,432 居中/灰 `#7d93a3` 逐字未动，纯文字零逻辑零结算零存档变化。
- 【零回归面】未动 main.js 任何按键分派（journal.onKey 的 I→status 与 status.onKey 的 J→journal 为既有通路，本版仅补提示）；未动 drawStatus 页底「J 任务日志」（互切对侧原样）；未动 questJournal/drawQuestCard/滚动钳制任何逻辑；codex「按 B / Esc 关闭」/ach「按 C / Esc 关闭」/help「H/Esc 关闭」页脚口径原样（图鉴/成就无互切键，不在此列）。只改 3 个游戏/文档文件（`js/view/menus.js`：v21.71 注释 + 两处页脚文案；`js/data.js`：GAME_VERSION v21.70→v21.71 + 版本注释；`README.md`：tests 树收录 + 六十七件套（六十六件套清除）+ 快速上手表 I/J 两行互切口吻 + v21.71 守护描述；`CHANGELOG.md`：本条）+ `package.json`（test 串第 67 份）+ 新增 `tests/smoke_v2171_journalikey.mjs`（仓库常驻，承 v21.10-v21.70 冒烟入库先例）+ 顺带把 `tests/smoke_v2170_winreset.mjs` 的 README 件套断言按 v21.7 惯例去硬化（存活性口径，实件数由 v21.71 冒烟守护）。
- 验证：`node --check js/data.js js/view/menus.js tests/smoke_v2170_winreset.mjs tests/smoke_v2171_journalikey.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 六十七件套全绿（既有 66 件逐项零回归——含 smoke_v2170 去硬化后断言与 smoke_v2145 页脚子串断言（「按 J / Esc 关闭」+「↑↓ 滚动浏览（还有」两子串逐字保留，不受尾部追加影响）—— + 新增 v21.71 **29/29 全过**——GAME_VERSION 格式合法且越过 v21.70、data.js/menus.js 含 v21.71 注释、menus.js 源级落位（两处新文案落位/空日志旧裸文案零残留/两既有口径逐字保留）、main.js 双向互切源级落位、运行期实证（有日志档页脚三要素齐备且逐字「按 J / Esc 关闭   ·   ↑↓ 滚动浏览（还有 N 条）   ·   I 状态页」落位 (320,432)、行宽 estW ≤500 面板预算两档、空日志档（hero=null 防御分支）页脚逐字「按 J / Esc 关闭   ·   I 状态页」、「没有进行中的任务。」零回归、journal.onKey('i')→status 与 status.onKey('j')→journal 双向互切实证、J/Esc 关闭回 world 零回归、drawStatus 渲染不抛错且页底「J 任务日志」零回归）、README/package.json 同步、smoke_v2170 件套断言去硬化确认）。
- 【另附：开工摸底】整目录基线备份 `/tmp/my-jrpg_backup_20260909_042500`（铁律：整目录 cp -R）；基线 `npm test` 六十六件套全绿后方开工；并发检查双测 mtime 无变化（上次落盘 03:52，本次开工 ~04:25）；承 v21.70 防误触打磨，本轮继续「按键提示如实反映可用键」主线收尾（状态↔日志互切是全游戏唯一「键已通、提示只标单侧」的双向通路）。

## v21.70 胜利画面 R 重开补两按确认——「一键丢档」最后漏网场景收口：win 结算屏（击败幽冥魔王「灯芯回来了」）不自动存档（saveGame 仅 P/暂停菜单手动触发），此前 R 单击即 resetRun 丢掉整个未存档的胜利战果（圣光之剑/等级/金币全在内存），与 v21.16 修掉的标题页同款隐患（体验打磨·防误触，承 v21.6 旅行防误触 / v21.16 标题页 R 两按「破坏性操作两段触发」家族——dead 场景 R 维持单击：战败语境下重开是显式三选一的常态出口，且 _bossRetry 快照保底可 B 再战，胜利语境无此保底）

- 【修正：win.onKey 的 R 由单击直调改为两按确认，与标题页逐字同构】`js/main.js`：win 场景 onKey 顶部补 `if (e.key !== 'r' && e.key !== 'R') S.titleResetArm = 0;`（任一非 R 键含 Enter 去尾声立即解除武装），R 分支由 `resetRun()` 直调改为复用 `core.titleResetCheck(S.titleResetArm || 0, Date.now(), true)` 纯状态机（data.js TITLE_RESET_CONFIRM_MS 单一数据源）：首次按 R 仅武装 + SFX.cancel + boxMsg「🔁 再按一次 R 确认重开新档（当前冒险进度将丢弃）」（与标题页提示逐字同口径），窗口内再按 R 才执行 resetRun（执行后武装归零不连发）。`js/view/menus.js`：drawWin 页脚由「按 Enter 观看尾声 · 按 R 重新开始」改为「按 Enter 观看尾声 · 按 R 重开新档（连按两次）」（与标题页提示行「R 重开新档（连按两次）」同口径，承 v21.18「按键提示必须如实反映防误触口径」主线；字号/基线/颜色逐字未动）。`js/data.js`：GAME_VERSION v21.69→v21.70 + 版本注释。Enter→ending 分支、resetRun/startRun 建档逻辑、title/dead 场景按键全部逐字未动，纯入口层改动，零结算零存档变化。
- 【零回归面】未动 core.js titleResetCheck/resetRun/retryBoss 任何逻辑（两按状态机为 v21.16 既有纯函数，本版仅新增第二处调用点）；未动 title.onKey 的 R 两按确认（v21.16 原样）；未动 dead.onKey 的 R 单击（本版刻意只动 win 一处）；未动 state.js（S.titleResetArm 既有字段复用）；未动 HELP_PAGES（H 页无 win 场景行，README 快速上手表新增「胜利画面 Enter/R」行承担可发现性）。只改 3 个游戏/文档文件（`js/main.js`：win.onKey 两按确认 + 注释；`js/view/menus.js`：drawWin 页脚文案 + 注释；`js/data.js`：版本号 + 注释；另 `README.md`：tests 树收录 + 六十六件套（六十五件套清除）+ 快速上手表胜利行 + v21.70 守护描述；`CHANGELOG.md`：本条）+ `package.json`（test 串第 66 份）+ 新增 `tests/smoke_v2170_winreset.mjs`（仓库常驻，承 v21.10-v21.69 冒烟入库先例）+ 顺带把 `tests/smoke_v2169_chiefdone.mjs` 的 README 件套断言按 v21.7 惯例去硬化（存活性口径，实件数由 v21.70 冒烟守护）。
- 验证：`node --check js/main.js js/data.js js/view/menus.js tests/smoke_v2169_chiefdone.mjs tests/smoke_v2170_winreset.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 六十六件套全绿（既有 65 件逐项零回归——含 smoke_v2169 去硬化后断言 + 新增 v21.70 **34/34 全过**——版本锚点越过 v21.69、data.js 含 v21.70 注释、TITLE_RESET_CONFIRM_MS/EVENT_MSG_MS 常量契约、main.js win 块源级落位（titleResetCheck 调用/同口径提示/非 R 解武装/Enter 分支零回归/旧单击直调零残留）与 title·dead 两场景零回归、drawWin 页脚新口径落位与旧文案零残留（drawDead 单击口径不动）、运行期实证七档（win 首按 R 停留且武装+提示 / 窗口内再按真实 resetRun 落 story 新档且武装归零 / 执行后再按 R 重新武装不连发不二次重建新档 / Enter 去尾声零回归并解武装 / 武装后按无关键解武装 / 解武装后首按 R 仅重新武装 / title 场景 R 两按确认零回归）+ drawWin 真实渲染不抛错、README/package.json 同步、smoke_v2169 件套断言去硬化确认）。
- 【另附：开工摸底】整目录基线备份 `/tmp/my-jrpg_backup_20260909_032500`（铁律：整目录 cp -R）；基线 `npm test` 六十五件套全绿后方开工；并发检查双测 mtime 无变化（上次落盘 02:34，本次开工 ~03:20）；数值面近三轮开工模拟均已复核无需再调（v21.68 中期生存模拟 村100%/林99.72%/矿100%/廊100%），本轮承 v21.6/v21.16「破坏性操作两段触发」家族做防误触打磨：标题页 R（v21.16）之后，win 结算屏是同隐患的最后漏网场景（dead 有 _bossRetry 保底、create 无破坏性键、travel 已 v21.6 收口）。

## v21.69 灯长通关差分对话（支线「灯长的委托」done 页按主线进度三档分档）——任务 NPC 通关差分的最后一块空白：灯长是全游戏唯一 done 页嵌着「会过期的主线状态陈述」的任务 NPC——原页「可广场那盏大灯，还在等灯芯」在击败幽冥魔王后即成事实错误（主线日志 main_demon.done 自述「镇上的灯重新亮了」、胜利画面更是「灯芯回来了」），终焉之神散后（井也不鸣了）更与时态不符（内容补全，承 v20.8 side_ember / v21.67 side_bone done 页函数分档先例——v21.67 把灯长归入「静态 done 页讲支线余韵」，但猎手「林子交给我」/守书记「旧矿最后那页读完了」均为纯支线余韵、不引用主线状态，唯独灯长这页余韵里嵌着主线状态陈述，是同类中的漏网；本轮继续内容轮换）

- 【修正：done 页由静态数组升级为主线进度三档函数页】`js/data.js`：side_mushroom 的 `talk.done` 由 `[[…]]` 静态页改为 `(hero)=>hero && hero.trueBoss ? [[真结局页]] : (hero && hero.bossDefeated ? [[讨回灯芯页]] : [[原页]])`——talkPagesOf 对函数型 done 页调用期求值（quests.js 既有 `typeof pages === 'function' ? pages(hero) : pages` 分支，与本支线 active 函数页、side_ember/side_bone done 函数页同机制）。三档：!bossDefeated 原页逐字保留（「谢谢你。井泉的灯油续上了，泉水亮了一点。可广场那盏大灯，还在等灯芯。」——灯芯未讨回时该陈述为真）/ bossDefeated（讨回灯芯·真结局前）新页「灯芯回来了，广场的大灯亮起来了。可你听——井还在低鸣。」（与 main_demon.done「镇上的灯重新亮了——可井还在低鸣」同口径）/ trueBoss 新页「井也不鸣了。镇上的灯一盏一盏，都亮回了自己的名字。」（与 main_true.done「记忆回到镇上」同口径）。三档均保留「（支线任务·已完成）」标记与「[Enter] 继续」收尾；防御式求值 null/无旗标字段均落未讨回档不抛错；新两档行宽 estW 405/375 ≤440 对话面板预算内（原页为 v21.52 既有文案逐字保留，其长单句由 drawTalk 的 wrapTalkLine 像素折行兜底，不在本次改动面内）。cond/obj/offer/turnin/reward（40+lv×10 金 + 2 药）、MUSHROOM_GOAL 单一数据源、ACH_LIST「三株荧光」判定全部逐字未动，零结算零数值零存档变化。
- 【零回归面】未动 QUESTS 其余任何条目（side_mist/side_stone done 保持静态数组、side_ember/side_bone 分档函数先例原样、四条主线任务逐字未动）；未动 NPCS.chief（仍无静态 lines/after/linesByStage——任务页是其唯一对话通道，任务页恒优先）；未动 quests.js talkPagesOf/npcQuestPages/resolveNpcTalk 任何逻辑（函数页求值通路既有）；未动 world.js 开箱 turnin 触发与 sprites.js 的 chief→mwChief 映射。只改 3 个游戏/文档文件（`js/data.js`：GAME_VERSION v21.68→v21.69 + 版本注释 + side_mushroom done 三档；`README.md`：tests 树收录 + 六十五件套（六十四件套清除）+ v21.69 守护描述；`CHANGELOG.md`：本条）+ `package.json`（test 串第 65 份）+ 新增 `tests/smoke_v2169_chiefdone.mjs`（仓库常驻，承 v21.10-v21.68 冒烟入库先例）+ 顺带把 `tests/smoke_v2168_hardtrue.mjs` 的 README 件套断言按 v21.7 惯例去硬化（存活性口径，实件数由 v21.69 冒烟守护）。
- 验证：`node --check js/data.js tests/smoke_v2168_hardtrue.mjs tests/smoke_v2169_chiefdone.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 六十五件套全绿（既有 64 件逐项零回归——含 smoke_v2168 去硬化后断言 + 新增 v21.69 **31/31 全过**——GAME_VERSION 格式合法且越过 v21.68、data.js 含 v21.69 注释、done 函数三档落位（未讨回档与 v21.52 前原页逐字一致 / 讨回灯芯档「灯芯回来了……井还在低鸣」逐字 / 真结局档「井也不鸣了……亮回了自己的名字」逐字，且完成标记与 [Enter] 收尾三档齐备）、防御式求值 null→未讨回档与仅 trueBoss 异常档→真结局档、新两档行宽 ≤440 预算、main_demon.done/main_true.done 口径来源零回归、side_ember/side_bone 先例与 side_mist/side_stone 静态 done 零回归、offer/turnin 静态页与 active 函数页零回归、NPCS.chief 无静态对话契约、运行期实证十一档（done 三档 npcQuestPages 逐字落位 / active 页「1/3 株」进度零回归 / offer 页「灯芯灭了」与 turnin 页「领取奖励」零回归 / done 状态机 questStatus===done + npcQuestMark 归零 + resolveNpcTalk 不重发奖 / drawTalk 三档真实渲染不抛错）、README/package.json 同步、smoke_v2168 件套断言去硬化确认）。
- 【另附：开工摸底】整目录基线备份 `/tmp/my-jrpg_backup_20260909_021330`（铁律：整目录 cp -R）；基线 `npm test` 六十四件套全绿后方开工；并发检查双测 mtime 无变化（上次落盘 01:41，本次开工 ~02:13）；承 v21.67 拾骨人 / v21.68 困难成就两连内容后，本轮继续内容补全轮换（灯长通关差分 = 任务 NPC 通关差分版图的最后一块空白：v21.67 后 12 位任务/非任务 NPC 的通关后语境逐一核对，唯灯长 done 页含过期主线陈述）。

## v21.68 新成就「逆风行灯」（困难模式通关里程碑）——难度向里程碑的空白补齐：主线三关（讨回灯芯/井之守望/记得一切）、试炼（百炼成钢）、收集向（记忆守护者/开箱寻宝/灯火同心/灯火记忆/诸技通明）都有里程碑，唯独建档时自选「困难」顶着 魔物HP×1.35/攻×1.15/防×1.12 走完全程没有任何纪念（新内容·单成就，承 v20.4「灯火记忆」/v21.59「诸技通明」单成就先例——v21.60-66 连续七版「信息透明·战报缺数」打磨、v21.67 内容补全后，本轮继续新内容轮换；开工摸底中期生存模拟 村100%/林99.72%/矿100%/廊100%，数值无需再调——故本轮选新内容而非平衡/打磨）

- 【新成就】`js/data.js`：ACH_LIST 末尾新增 `{id:'hardtrue', name:'逆风行灯', d:'以困难模式击败终焉之神', ok:g=>!!(g.trueBoss && g.diff===1)}`（24→25）——判定读既有 hero.trueBoss（终焉之神=真结局最终 Boss，「走完全程」的终极口径）与 hero.diff（DIFFS 下标：建档 createDiff 写定、core.js initGame `S.G.diff = diff || 0`、resetRun 沿用同槽）两个存档字段，零新计数/零新状态/零结算变化；描述「困难」为字面量（DIFFS 在 data.js 尾部声明、ACH_LIST 求值时仍在 TDZ 暂死区无法引用——刻意保留字面量，与 DIFFS[1] 的一致性由 smoke_v2168 钉死守护，改难度名冒烟即红）；旧档无 diff 字段（undefined）→ `===1` 为 false 不误解锁，零迁移；无 r 字段（与 lvl5/lvl10/memoir/skills 同款纯里程碑，逆风走完本身就是奖励）。解锁时机：winBattle → applyAchievements 同一场胜利即时解锁（困难档击败终焉之神当场弹「🔓 成就解锁：【逆风行灯】」）；普通档通关的玩家另开困难槽通关仍可解锁，成就页（C）25 项自此含一枚「必须换难度」的终极目标。
- 【零回归面】未动 ACH_LIST 其余 24 个条目任何字段（id/name/d/r/ok/prog 逐字未动，hardtrue 追加在末尾、displayOrder 不变）；未动 unlockedAchievements/applyAchievements 任何逻辑（新成就走既有判定通路自动接入）；未动 drawAch 任何渲染（ACH_LIST.length 动态读取，25 项仍 PAGE=10 三页滚动）；未动 DIFFS/DIFF_SCALE/createDiff/initGame 任何难度逻辑。只改 3 个游戏/文档文件（`js/data.js`：GAME_VERSION v21.67→v21.68 + 版本注释 + hardtrue 条目；`README.md`：tests 树收录 + 六十四件套（六十三件套清除）+ 成就口径「25 项」双处同步 + v21.68 守护描述；`CHANGELOG.md`：本条）+ `package.json`（test 串第 64 份）+ 新增 `tests/smoke_v2168_hardtrue.mjs`（仓库常驻，承 v21.10-v21.67 冒烟入库先例）+ 顺带把 `tests/smoke_v2167_boneafter.mjs` 的 README 件套断言按 v21.7 惯例去硬化（存活性口径，实件数由 v21.68 冒烟守护）+ 顺带把 `tests/smoke_v2159_skillach.mjs` 与 `tests/smoke_v2164_achname.mjs` 的 ACH_LIST 精确总数断言按 v21.68 新现实更新为存活性口径 >= 24（承 v21.59 对 smoke_v2152「===23 → >=23」同款先例：旧断言 pin 的「===24」系 v21.59/v21.64 口径，本版起精确总数由 smoke_v2168 守护）+ 顺带把 smoke_v2159 的 README 成就口径断言随新现实更新为「25 项」双处（承 v21.59 对「23 项」双处的同款更新）。
- 验证：`node --check js/data.js tests/smoke_v2159_skillach.mjs tests/smoke_v2164_achname.mjs tests/smoke_v2167_boneafter.mjs tests/smoke_v2168_hardtrue.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 六十四件套全绿（既有 63 件逐项零回归——含 smoke_v2167 去硬化后断言与 smoke_v2159/smoke_v2164 随新现实更新后断言 + 新增 v21.68 **37/37 全过**——GAME_VERSION 格式合法且越过 v21.67、data.js 含 v21.68 注释、DIFFS/DIFF_SCALE 数据源契约（两档难度/困难倍率 1.35/1.15/1.12）、ACH_LIST 契约（hardtrue 唯一/总数精确 25/无 r 无 prog/描述字面量与 DIFFS[1] 一致/源级落位）、ok 谓词逐值八档（空旧档/无 diff 通关档/普通通关/困难通关/仅困难未通关/两档进行中/非困难下标 全逐值）、既有 24 成就 id 零回归、allquests 支线分母 0/7 不串、unlockedAchievements 集成三档、运行期全链路（newGame 普通新档不误解锁 → 普通通关不误解锁 → 困难通关真实解锁落 hero.ach → 重复调用去重）、drawAch 两态+第三页滚动渲染不抛错、README/package.json 同步、smoke_v2167 件套断言去硬化与 smoke_v2159/smoke_v2164 总数断言随新现实更新确认）。
- 【另附：开工摸底】整目录基线备份 `/tmp/my-jrpg_backup_20260909_011136`（铁律：整目录 cp -R）；基线 `npm test` 六十三件套全绿后方开工；并发检查双测 mtime 无变化（上次落盘 00:38，本次开工 ~01:11）；中期生存模拟 `/tmp/jrpg_mid_sim.mjs` 复核（村100%/林99.72%/矿100%/廊100%，数值无需再调——故本轮选新内容而非平衡；承 v21.67 内容补全，本轮继续新内容轮换：难度向里程碑是成就版图最后一块空白）。

## v21.67 拾骨人通关后差分对话——支线「未归的矿灯」done 页按 hero.trueBoss 分档：通关后再访，拾骨人亲口点破「初灯的名字都回来了……矿道里像有人挨个点名」（内容补全，承 v20.8 side_ember done 页分档先例：讨伐采集型支线里只有 side_ember 的 done 页按 hero.trueBoss 分档，拾骨人此前是全游戏唯一「通关后仍只有静态完成页」的任务 NPC——守名者有分档、老矿工/守碑人/掌灯阿婆等非任务 NPC 有 after 彩蛋、灯长/猎手/守书记等任务 NPC 的静态 done 页讲各自支线余韵，而「亡骨名字回灯下」的主题与终焉之神被打败（名字归还）天然同脉；未通关档保持 v21.52 原页逐字不变，纯对话内容、零判定/零奖励/零存档变化）

- 【修正：done 页由静态数组升级为 hero.trueBoss 分档函数页】`js/data.js`：side_bone 的 `talk.done` 由 `[[…]]` 静态页改为 `(hero)=>hero && hero.trueBoss ? [[新页]] : [[原页]]`——talkPagesOf 对函数型 done 页调用期求值（quests.js 既有 `typeof pages === 'function' ? pages(hero) : pages` 分支，与本支线 active 函数页同机制，v20.8 side_ember 已验证该通路）；未通关档与 v21.52 原页逐字一致（「矿道安静了些。站着挨饿的兄弟少了，/灯镇的方向，又亮了一点。」），通关档新页两行点题（「初灯的名字都回来了……昨夜矿道里，/像有人挨个点名。点到名的兄弟，都歇下了。」），两档均保留「（支线任务·已完成）」标记与「[Enter] 结束」收尾；防御式求值 null/无 trueBoss 字段均落未通关档不抛错；新页行宽 estW ≤440 对话面板预算内。cond/condProg/obj/offer/turnin/reward（80 金 + 1 灵药）、BONE_GOAL 单一数据源、ACH_LIST「亡骨还乡」判定全部逐字未动，零结算零数值零存档变化。
- 【零回归面】未动 QUESTS 其余任何条目（side_mist/side_stone done 保持静态数组、side_ember 分档函数先例原样）；未动 NPCS.digger（无 after——done 函数页是通关差分唯一通道，任务页恒优先于兜底静态 lines）；未动 quests.js talkPagesOf/npcQuestPages/resolveNpcTalk 任何逻辑（函数页求值通路既有）；未动 sprites.js 的 digger→mwSage 映射与 cave.extras (5,10) 落位。只改 3 个游戏/文档文件（`js/data.js`：GAME_VERSION v21.66→v21.67 + 版本注释 + side_bone done 分档；`README.md`：tests 树收录 + 六十三件套（六十二件套清除）+ v21.67 守护描述；`CHANGELOG.md`：本条）+ `package.json`（test 串第 63 份）+ 新增 `tests/smoke_v2167_boneafter.mjs`（仓库常驻，承 v21.10-v21.66 冒烟入库先例）+ 顺带把 `tests/smoke_v2166_innheal.mjs` 的 README 件套断言按 v21.7 惯例去硬化（存活性口径，实件数由 v21.67 冒烟守护）+ 顺带把 `tests/smoke_v2152_bonequest.mjs` 的 done 页断言按 v21.67 新现实更新（承 v21.58 smoke_v2154 / v21.66 smoke_v2163 先例：旧断言 pin 的「Array.isArray(done)」系 v21.52 口径，本版起该口径由 smoke_v2167 精确守护）。
- 验证：`node --check js/data.js tests/smoke_v2152_bonequest.mjs tests/smoke_v2166_innheal.mjs tests/smoke_v2167_boneafter.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 六十三件套全绿（既有 62 件逐项零回归——含 smoke_v2166 去硬化后断言与 smoke_v2152 随新现实更新后断言 + 新增 v21.67 **25/25 全过**——GAME_VERSION 格式合法且越过 v21.66、data.js 含 v21.67 注释、done 函数页两档落位（未通关档与 v21.52 原页逐字一致 / 通关档新页逐字「初灯的名字都回来了……」且完成标记与 [Enter] 收尾两档齐备）、防御式求值 null→未通关档、行宽 ≤440 预算、side_ember 分档先例零回归、side_mist/side_stone 静态 done 零回归、offer/turnin 静态页与 active 函数页零回归、NPCS.digger 无 after 契约、运行期实证八档（done 未通关档 npcQuestPages 落原页逐字 / done 通关档落新页逐字 / active 档「已安顿 1/3 只」进度零回归且 trueBoss 不串档 / offer 页「没能走出矿洞」与 turnin 页「领取谢礼」零回归 / done 状态机 questStatus===done + npcQuestMark 归零 + resolveNpcTalk 不重发奖 / drawTalk 两档真实渲染不抛错）、README/package.json 同步、smoke_v2166 件套断言去硬化与 smoke_v2152 done 断言随新现实更新确认）。
- 【另附：开工摸底】整目录基线备份 `/tmp/my-jrpg_backup_20260909_001604`（铁律：整目录 cp -R）；基线 `npm test` 六十二件套全绿后方开工；并发检查双测 mtime 无变化（上次落盘 23:41，本次开工 ~00:16）；开局平衡蒙特卡洛复核（普通带药 100%/99.99%、普通裸刷 99.59%、困难带药 99.09%、困难裸刷 15.35% 属玩家选择，数值无需再调——故本轮选内容补全而非平衡；承 v21.49-58/v21.60-66 连续「信息透明·战报缺数」打磨主线，本轮轮换为新内容）。

## v21.66 住店结算报文补恢复量与结算后状态「HP +N（X/Y）· MP +M（A/B）完全恢复！」——住店恢复链条三端报数收官：面板预览端（drawInn「今晚将恢复 HP +N MP +M」）与免费全恢复对照端（v19.94 清泉「HP +N（X/Y）· MP +M（A/B）完全恢复！」）早已量化，唯独按下确认这一刻的结算报文只报「HP/MP 恢复！」不报数值（体验打磨·信息透明·口径一致，承 v19.94 清泉量化 / v21.65 喝药战报结算后状态 / v21.29-32/v21.38-43「同一功能所有入口口径一致」主线：【花 10 金睡一觉，看不到到底回了多少、现在满没满】`shop.js` `stayInn` 的成功结算报文自 v19.76 起只报 `HP/MP 恢复！（-N 金，剩余 M 金）`——同为全恢复的清泉端 v19.94 早已报「HP +N（X/Y）· MP +M（A/B）完全恢复！」，旅馆面板预览端 drawInn 也早已列「今晚将恢复 HP +N MP +M」，唯独付费确认这一刻缺数；玩家睡前想确认「这一晚值不值、醒来满没满」仍需瞄 HUD 或按 I 看状态页。零结算纯显示，改动 2 行取值 + 1 条文案 + 注释）

- 【修正：结算报文按清泉同式补恢复量与结算后状态，恢复量与面板预览同读一份源】`js/shop.js`：满恢复赋值前补 `const hpBefore = hero.hp;` / `const mpBefore = hero.mp;`（与 world.js onFountainStep 同位置同式取值），报文由 `🌙 你美美地睡了一晚，HP/MP 恢复！（-${INN_PRICE} 金，剩余 ${hero.gold} 金）` 补为 `🌙 你美美地睡了一晚，HP +${hero.hp - hpBefore}（${hero.hp}/${hero.hpMax}）· MP +${hero.mp - mpBefore}（${hero.mp}/${hero.mpMax}）完全恢复！（-${INN_PRICE} 金，剩余 ${hero.gold} 金）`——恢复量 = 结算前后差，与 drawInn 预览「今晚将恢复 HP +N MP +M」同读 hpMax-hp/mpMax-mp 一份源（预览的 Math.max(0,…) 防御在结算前语义下与差值逐值相等：进入本分支至少一项缺损，已满项差值恒 0、如实报 +0，与清泉满项报 +0 同口径）；v19.76 金币后缀逐字保留。恢复结算（hero.hp/mp = hpMax/mpMax 满恢复）、扣款（hero.gold -= INN_PRICE）、SFX.heal/renderHUD、v21.63 金币不足差额拦截、满状态「你现在精神饱满。」拦截全部逐字未动，零结算零数值零存档变化。
- 【零回归面】未动 `INN_PRICE`/`POTION_PRICE`/`WEAPONS`/`ARMORS` 任何数值；未动 stayInn 进入条件（HP 或 MP 不满才可住店）、金币不足判定与差额文案、满状态拦截；未动清泉端 onFountainStep 任何文案与结算（v19.94 同式对照端不受扰）；未动 drawInn 面板任何渲染（预览端「今晚将恢复」行逐字未动）；未动 SFX 时机与消息时长。只改 4 个游戏/文档文件（`js/shop.js`：v21.66 注释 + 2 行取值 + 1 条文案；`js/data.js`：GAME_VERSION v21.65→v21.66 + 版本注释；`README.md`：tests 树收录 + 六十二件套（六十一件套清除）+ v21.66 守护描述；`CHANGELOG.md`：本条）+ `package.json`（test 串第 62 份）+ 新增 `tests/smoke_v2166_innheal.mjs`（仓库常驻，承 v21.10-v21.65 冒烟入库先例）+ 顺带把 `tests/smoke_v2165_potionhp.mjs` 的 README 件套断言按 v21.7 惯例去硬化（存活性口径，实件数由 v21.66 冒烟守护）+ 顺带把 `tests/smoke_v2163_shopgold.mjs` 的住店成功档两处断言（源级模板子句 / J 档运行期逐字）按 v21.66 新现实更新（承 v21.58 smoke_v2154 先例：旧断言 pin 的「HP/MP 恢复！」系 v19.76 口径，本版起该口径由 smoke_v2166 精确守护）。
- 验证：`node --check js/shop.js js/data.js tests/smoke_v2163_shopgold.mjs tests/smoke_v2165_potionhp.mjs tests/smoke_v2166_innheal.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 六十二件套全绿（既有 61 件逐项零回归——含 smoke_v2165 去硬化后断言 + 新增 v21.66 **29/29 全过**——GAME_VERSION 格式合法且越过 v21.65、data.js/shop.js 含 v21.66 注释、shop.js 源级落位（新文案落位/旧裸文案零残留/hpBefore·mpBefore 取值/恢复·扣款结算与 v21.63 差额拦截·满状态拦截·进入条件逐字零回归）、清泉端（world.js onFountainStep）与面板预览端（menus.js drawInn）同源零回归、INN_PRICE=10 契约、运行期实证六档（双缺档报文逐字「🌙 你美美地睡了一晚，HP +30（60/60）· MP +20（30/30）完全恢复！（-10 金，剩余 40 金）」且 hp 30→60/mp 10→30/gold 50→40 结算一致、只缺 MP 档已满 HP 如实报 +0 且 hp 不动、只缺 HP 档已满 MP 如实报 +0 且 mp 不动、预览同源档报 +59/+29 与结算前差值逐值相等且 gold 10→0、金币不足档 v21.63 差额口径逐字且零结算、满状态档「你现在精神饱满。」逐字且 gold 不变）、README/package.json 同步、smoke_v2165 件套断言去硬化与 smoke_v2163 随新现实更新确认）。
- 【另附：开工摸底】整目录基线备份 `/tmp/my-jrpg_backup_20260908_232201`（铁律：整目录 cp -R）；基线 `npm test` 六十一件套全绿后方开工；并发检查双测 mtime 无变化（上次落盘 22:38，本次开工 ~23:22）；承 v21.49-58 战报缺数 / v21.60-65 信息透明主线，本轮继续「同一功能所有入口口径一致」打磨（住店=全恢复功能最后一端缺结算后数值的一端）。

## v21.65 喝药战报补恢复后 HP/MP 状态「（HP X/Y · 药水剩余 M 瓶）」——恢复链条「结算后状态」全端收官：治愈术端（v19.97「（HP X/Y）」）、防御回蓝端（v19.96「（MP X/Y）」）、中毒/受击端（v21.8/v20.2「（我方 HP X/Y）」）早已报结算后状态，唯独喝药这一端只报恢复量与库存（体验打磨·信息透明·口径一致，承 v19.74 战斗/大地图同源同改与 v21.29-32/v21.38-43「同一功能所有入口口径一致」主线：【喝药报「恢复 35」是钳制前的公式量，回完现在多少血看不到】`battle.js` `doItem` 与 `core.js` `usePotion` 两端自 v19.74 起报 `恢复 N 点 HP（药水剩余 M 瓶）`——恢复量 N 是 `rules.potionRestore/elixirRestore` 的公式量，HP 上限钳制后实际恢复可能远小于 N（HP 95/100 喝药报「恢复 35」实际只回 5）；玩家连战中想确认「回完这一口现在多少血、能不能撑过下一轮」仍需瞄 HUD 血条。零结算纯显示，改动 4 条文案 + 注释）

- 【修正：两端同式把结算后 HP（灵药含 MP）并入同一括号句首，库存量保留】`js/battle.js`：战斗内 `🍖 X 服用药水，恢复 N 点 HP（HP X/Y · 药水剩余 M 瓶）` / `🧪 X 服下高级灵药，恢复 N HP、M MP（HP X/Y · MP A/B · 高级灵药剩余 K 瓶）`；`js/core.js`：大地图同式两条（无 hero.name 前缀，与 v19.74 原口径一致）——全部读 `takePotion()` 结算后的 hero.hp/hpMax/mp/mpMax/item/potion2，与治愈术「（HP X/Y）」、防御「（MP X/Y）」同元素同「·」分隔（状态页/领悟战报同款先例）；恢复量仍如实报公式量，钳制由括号内 HP 读数体现（与 v21.58 石甲「挡下 N 点」保公式量报法的同族口径）。恢复公式（rules 同读 POTION_*/ELIXIR_* 单一数据源）、库存结算、potionAvailability 拦截判定、SFX/renderHUD/afterPlayer 编排全部逐字未动，零结算零数值零存档变化。
- 【零回归面】未动 `POTION_HP_PCT`/`POTION_HP_FLAT`/`ELIXIR_HP_PCT`/`ELIXIR_HP_FLAT`/`ELIXIR_MP_PCT` 任何数值；未动 hero.js takePotion 的 min 钳制结算与灵药优先次序、potionAvailability 的 strongOk/weakOk 判定；未动战斗「[3]恢复」预览（drawBattle 公式量预览端不受扰）、拦截文案「✅ 你气满神足/状态满满」「❌/🍖 没有可用的药水了！」四条逐字未动；未动治愈术/防御/中毒/受击各端既有报文。只改 4 个游戏/文档文件（`js/battle.js`：v21.65 注释 + doItem 2 条文案；`js/core.js`：v21.65 注释 + usePotion 2 条文案；`js/data.js`：GAME_VERSION v21.64→v21.65 + 版本注释；`README.md`：tests 树收录 + 六十一件套（六十件套清除）+ v21.65 守护描述；`CHANGELOG.md`：本条）+ `package.json`（test 串第 61 份）+ 新增 `tests/smoke_v2165_potionhp.mjs`（仓库常驻，承 v21.10-v21.64 冒烟入库先例）+ 顺带把 `tests/smoke_v2164_achname.mjs` 的 README 件套断言按 v21.7 惯例去硬化（存活性口径，实件数由 v21.65 冒烟守护）。
- 验证：`node --check js/battle.js js/core.js js/data.js tests/smoke_v2164_achname.mjs tests/smoke_v2165_potionhp.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 六十一件套全绿（既有 60 件逐项零回归——含 smoke_v2164 去硬化后断言 + 新增 v21.65 **33/33 全过**——GAME_VERSION 格式合法且越过 v21.64、data.js/battle.js/core.js 含 v21.65 注释、battle.js/core.js 源级落位（四条新文案落位/旧裸文案零残留/takePotion 钳制结算与 potionAvailability 判定/拦截分支逐字零回归）、恢复公式常量契约（POTION 50%+8 / ELIXIR 80%+20 且 MP 40%）、运行期实证八档（战斗药水档报文逐字「🍖 测试者 服用药水，恢复 38 点 HP（HP 48/60 · 药水剩余 1 瓶）」且 hp 10→48/item 2→1 结算一致、战斗灵药档逐字「🧪 …恢复 68 HP、12 MP（HP 60/60 · MP 22/30 · 高级灵药剩余 0 瓶）」且 hp 20→60 钳制/mp 10→22/potion2 1→0、大地图药水/灵药两档无 hero.name 前缀逐字且结算一致、钳制档 hp 55/60 报「恢复 38 点 HP（HP 60/60 …）」公式量与钳制读数并存、战斗无药拦截「❌ 没有可用的药水了！」零结算、大地图满状态拦截「✅ 状态满满，无需喝药！」零结算、灵药优先档 item 2+potion2 1 走灵药分支 item 不动）、README/package.json 同步、smoke_v2164 件套断言去硬化确认）。
- 【另附：开工摸底】整目录基线备份 `/tmp/my-jrpg_backup_20260908_221918`（铁律：整目录 cp -R）；基线 `npm test` 六十件套全绿后方开工；并发检查双测 mtime 无变化（上次落盘 21:40，本次开工 ~22:19）；承 v21.60-64 信息透明主线，本轮继续战报缺数打磨（喝药=恢复链条最后一端缺结算后状态的一端）。

## v21.64 图鉴全收集解锁横幅补成就名「成就解锁：【记忆守护者】」——成就解锁链条横幅端 24 项全数带名收官：通用分支 23 项早已报「🔓 成就解锁：【名】+ 描述」，唯独 perfection 的特别庆贺横幅只报事件不报名字（体验打磨·信息透明·口径一致，承 v21.29-32/v21.38-43「同一功能所有入口口径一致」主线：【集齐图鉴这一刻，看不到自己解锁的成就叫什么】`hero.js` `applyAchievements` 的 perfection 分支自 v19.37 起只报 `🏆 图鉴收集完成！额外奖励 N 金币！（剩余 M 金）`——玩家刷齐 13 种魔物图鉴、弹出庆贺横幅的瞬间，全游戏没有任何一处告诉他这枚成就叫「记忆守护者」；只有事后按 C 翻成就页，才能对号入座「原来刚才解锁的是它」。其余 23 项成就的解锁横幅都带名字，唯独这枚收集向最重里程碑缺名。零结算纯显示，改动 1 条文案 + 注释）

- 【修正：perfection 庆贺横幅句首按通用分支同款口径补「成就解锁：【名】」，名读 ACH_LIST 单一数据源】`js/hero.js`：横幅由 `🏆 图鉴收集完成！额外奖励 …` 补为 `🏆 成就解锁：【${def ? def.name : id}】图鉴收集完成！额外奖励 …`——`def` 上方早已 `ACH_LIST.find` 查好（通用分支同一份查找），成就改名只改 `data.js` 的 ACH_LIST 一处、横幅/成就页/标题摘要自动跟随，绝无第二套口径；防御式回落 `def ? def.name : id` 与通用分支逐字同式（理论死代码，由冒烟契约守护）。🏆 里程碑视觉、CODEX_MSG_MS 更长档停留、`hero.gold += PERFECTION_GOLD` 加奖结算、通用分支「🔓 成就解锁：【名】+ 描述」全部逐字未动，零结算零数值零存档变化。
- 【零回归面】未动 ACH_LIST 任何条目与字段（24 项总数/perfection 的 name/r/ok/prog 逐字未动）；未动 unlockedAchievements 判定、applyAchievements 解锁落账（hero.ach.push）与去重（includes 拦截）、SFX.levelup 音效；未动成就页 drawAch 任何渲染（v19.62 奖励揭示同源不受扰）；未动 data.js 时序常量注释里对旧文案的历史引述（v19.37 收口注记描述的是当时文案，家族指认仍准确）。只改 4 个游戏/文档文件（`js/hero.js`：v21.64 注释 + 庆贺横幅 1 条文案；`js/data.js`：GAME_VERSION v21.63→v21.64 + 版本注释；`README.md`：tests 树收录 + 六十件套（五十九件套清除）+ v21.64 守护描述；`CHANGELOG.md`：本条）+ `package.json`（test 串第 60 份）+ 新增 `tests/smoke_v2164_achname.mjs`（仓库常驻，承 v21.10-v21.63 冒烟入库先例）+ 顺带把 `tests/smoke_v2163_shopgold.mjs` 的 README 件套断言按 v21.7 惯例去硬化（存活性口径，实件数由 v21.64 冒烟守护）。
- 验证：`node --check js/hero.js js/data.js tests/smoke_v2163_shopgold.mjs tests/smoke_v2164_achname.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 六十件套全绿（既有 59 件逐项零回归——含 smoke_v2163 去硬化后断言 + 新增 v21.64 **26/26 全过**——GAME_VERSION 格式合法且越过 v21.63、data.js/hero.js 含 v21.64 注释、hero.js 源级落位（新文案落位/旧裸文案零残留/加奖结算与通用分支逐字零回归/防御式回落两处同式）、数据契约（ACH_LIST 24 项、perfection 名「记忆守护者」且 r 含 999、PERFECTION_GOLD=999/LVL5_GOAL=5/CODEX_MSG_MS=4200/ACH_MSG_MS=3200、BESTIARY_TARGET 13 种）、运行期实证五档（perfection 档报文逐字「🏆 成就解锁：【记忆守护者】图鉴收集完成！额外奖励 999 金币！（剩余 1099 金）」且 gold 100→1099 加奖一致、lvl5 通用档报文逐字零回归「🔓 成就解锁：【独当一面】 等级达到 5 级」且无加奖、双解锁档两条横幅各报各的、重复调用不再报不重复加奖、图鉴缺一种零横幅零加奖）、README/package.json 同步、smoke_v2163 件套断言去硬化确认）。
- 【另附：开工摸底】整目录基线备份 `/tmp/my-jrpg_backup_20260908_205700`（铁律：整目录 cp -R）；基线 `npm test` 五十九件套全绿后方开工；并发检查双测 mtime 无变化（上次落盘 20:05，本次开工 ~20:57）；中期生存模拟 `/tmp/jrpg_mid_sim.mjs` 复核（村100%/林99.67%/矿100%/廊100%，数值无需再调——故本轮选体验打磨而非平衡）。

## v21.63 商店/旅馆金币不足拦截报差额「（当前 M 金，还差 K 金）」——消费拦截链条三端报数收官：商店列表端（v19.78 药水行「现有N/99」持有量标注）/旅馆面板端（drawInn 红字「金币不足（还差 N 金），无法入住！」）早已量化，唯独按下确认这一刻的拦截弹条只报价格不报差额（体验打磨·信息透明，承 v19.67 消费反馈带品名/价格 / v19.69 酿造材料不足带「当前 A/B」同一「消费透明」主线：【买东西钱不够被拦下，看不到自己有多少、还差多少】`shop.js` 的 `buyPotion`/`buyWeapon`/`buyArmor`/`stayInn` 四处「金币不足」拦截弹条自 v19.67 起只报 `金币不足：X 需 N 金`——玩家刚在野外卖了蘑菇凑钱，想确认「到底还差几枚金币」仍需瞄 HUD 或按 I 看状态页；旅馆面板端 drawInn 红字差额（menus.js「还差 N 金」）与 v19.69 酿造差额「（当前 A/B）」早已量化，唯独拦截弹条这一端缺数。零结算纯显示，改动 4 条文案 + 注释）

- 【修正：四处不足拦截同式补「（当前 M 金，还差 K 金）」，差额与各判定同读一份源】`js/shop.js`：药水 → `金币不足：生命药水需 ${POTION_PRICE} 金（当前 ${hero.gold} 金，还差 ${POTION_PRICE - hero.gold} 金）`；武器/防具 → `金币不足：${name} 需 ${price} 金（当前 ${hero.gold} 金，还差 ${price - hero.gold} 金）`（两处同式）；住店 → `金币不足：住一晚需 ${INN_PRICE} 金（当前 ${hero.gold} 金，还差 ${INN_PRICE - hero.gold} 金）`（与 drawInn 红字「（还差 N 金）」同口径）。差额 = 价格 − hero.gold，与上方判定 `hero.gold >= price` 同读一份源——进入不足分支差额恒正，无需钳制；调价只改 `data.js` 常量（POTION_PRICE/WEAPONS/ARMORS/INN_PRICE），面板红字/拦截弹条/商品价签多端自动跟随，绝无第二套口径。购买判定、扣款、加库存、换装 applyStats、住店恢复结算、背包满优先拦截、满状态分支全部逐字未动，零结算零数值零存档变化。
- 【零回归面】未动 `POTION_PRICE`/`POTION_CAP`/`INN_PRICE`/`WEAPONS`/`ARMORS` 任何数值；未动 buildShopList 商品列表与 ▲▼ 箭头、卖蘑菇结算与文案、酿造差额提示（core.js）、drawInn 面板红字（menus.js）任何渲染；未动 SFX 时机与消息时长（四处不足弹条保持默认时长，与 v19.67 一致）。只改 4 个游戏/文档文件（`js/shop.js`：v21.63 注释 + 4 条拦截文案；`js/data.js`：GAME_VERSION v21.62→v21.63 + 版本注释；`README.md`：tests 树收录 + 五十九件套（五十八件套清除）+ v21.63 守护描述；`CHANGELOG.md`：本条）+ `package.json`（test 串第 59 份）+ 新增 `tests/smoke_v2163_shopgold.mjs`（仓库常驻，承 v21.10-v21.62 冒烟入库先例）+ 顺带把 `tests/smoke_v2162_phaseboost.mjs` 的 README 件套断言按 v21.7 惯例去硬化（存活性口径，实件数由 v21.63 冒烟守护）。
- 验证：`node --check js/shop.js js/data.js tests/smoke_v2162_phaseboost.mjs tests/smoke_v2163_shopgold.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 五十九件套全绿（既有 58 件逐项零回归——含 smoke_v2162 去硬化后断言 + 新增 v21.63 **37/37 全过**——GAME_VERSION 格式合法且越过 v21.62、data.js/shop.js 含 v21.63 注释、shop.js 源级落位（四条新文案落位/旧裸文案零残留/购买判定与扣款/加库存/恢复结算逐字零回归/成功与拦截分支其余文案逐字零回归）、价格常量契约（POTION_PRICE=15/INN_PRICE=10/POTION_CAP=99/MUSHROOM_PRICE=10/铁剑 80/皮甲 60）、运行期实证十二档（买药水不足档逐字「需 15 金（当前 7 金，还差 8 金）」且零结算、差额边界档 gold=14 报「还差 1 金」、买药水成功档 v19.78 口径逐字零回归且 gold 15→0/item 1→2、背包满优先拦截零回归、买武器不足档逐字「需 80 金（当前 50 金，还差 30 金）」且不换装、买武器成功档 v19.75+v20.5 口径零回归且 atkMax=base+5、买防具不足档逐字「需 60 金（当前 30 金，还差 30 金）」、买防具成功档零回归且 defMax=base+4、住店不足档逐字「需 10 金（当前 5 金，还差 5 金）」与 drawInn 红字同口径、住店成功档 v19.76 口径逐字零回归且 HP/MP 回满、住店满状态档「你现在精神饱满。」逐字且 gold 不变、卖蘑菇档逐字零回归且结算一致）、README/package.json 同步、smoke_v2162 件套断言去硬化确认）。
- 【另附：开工摸底】整目录基线备份 `/tmp/my-jrpg_backup_20260908_195555`（铁律：整目录 cp -R）；基线 `npm test` 五十八件套全绿后方开工；并发检查双测 mtime 无变化（上次落盘 19:15，本次开工 ~19:55）；中期生存模拟 `/tmp/jrpg_mid_sim.mjs` 复核（村100%/林99.67%/矿100%/廊100%，数值无需再调——故本轮选体验打磨而非平衡）。

## v21.62 Boss 变身战报补确切攻防增幅「（攻+N 防+N）」——变身链条三端报数收官：战前预告端（drawBattle 角标「变身：攻+N 防+N 回N%」）/帮助页端（phaseBoost「血量过半现出真身：攻+N 防+N 回血N%HP」）两端早已量化，唯独变身这一刻的战报只报「力量暴涨」不报涨了多少（体验打磨·信息透明，承 v21.49 中毒施毒 / v21.55 弱点抗性倍率 / v21.58 石甲挡伤同一「战报缺数」主线收尾：【Boss 现出真身攻防暴涨，看不到到底涨了多少】`enemyAI.js` `enemyAct` 的 phase2 变身战报此前只报 `🌀 X 现出真身！力量暴涨，HP 恢复 N！`——攻/防加算结算在 `enemy.atk += (phase.atk || 0)` / `enemy.def += (phase.def || 0)` 这两步把增幅数直接吞掉，玩家被真身新数值打崩，想确认「攻防到底涨了多少」只能回忆开场角标（v12.7 起常驻，变身后即消失）或翻帮助页，战报这一格缺数。零结算纯显示，改动 2 行取值提升 + 3 行 bits 拼装 + 1 条文案 + 注释）

- 【变身战报只报「力量暴涨」不报增幅】三 Boss 的 phase2 增益（幽冥魔王 攻+7/防+3、洞窟领主 攻+5/防+2、终焉之神 攻+7/防+3）从 v1.x 起就显式配在 SPECIES[].phase2 里，战前角标（drawBattle「变身：攻+7 防+3 回15%」）与帮助页（「血量过半现出真身：攻+7 防+3 回血15%HP」）两端早已如实量化——唯独变身这一刻，玩家与真身的第一次照面，战报只有「力量暴涨」四个字；drawBattle.js 的 v12.7 注释早已自述「blog 只报回血数值，攻防加成无从知晓，玩家常在真身那一下被新数值打崩还不知原因」——角标在变身瞬间消失，此后整段真身战都无处可查。
- 【修正：变身战报按角标同款 bits 口径补「（攻+N 防+N）」，SPECIES[].phase2 单一数据源同读】`js/enemyAI.js`：变身块把原 `enemy.atk += (phase.atk || 0)` / `enemy.def += (phase.def || 0)` 提升为 `const atkUp = phase.atk || 0; const defUp = phase.def || 0;` 后加算（`|| 0` 语义逐字保留，仅提升复用），战报由「力量暴涨，」补为「力量暴涨（攻+N 防+N），」——bits 拼装与 drawBattle boostBits 同口径（非零项才入列），调变身强度只改 `data.js` 的 SPECIES[].phase2 一处、角标/帮助页/战报三端自动跟随，绝无第二套口径。防御式：atk/def 全零（理论死代码，三 Boss 均显式正配）时无括号、保持原句「力量暴涨，」逐字不变。回血结算（`Math.round(hpMax × (phase.heal || PHASE2_HEAL_PCT))`）、改名/变色/forbid 落位、「 治愈被封印！」后缀逐字未动，零结算零数值零存档变化。
- 【零回归面】未动 SPECIES/BOSS_P2/phaseBoost 任何条目与字段；未动变身触发线（`hp < hpMax × (phase.at || PHASE2_AT)` 逐字未动）、重击倍率切换（HEAVY_MULT_PHASED）、石甲/回血/攻击任何招数结算、drawBattle 角标与帮助页任何渲染。只改 4 个游戏/文档文件（`js/enemyAI.js`：v21.62 注释 + atkUp/defUp 提升 + boostBits/boostTxt 拼装 + 变身文案 1 条；`js/data.js`：GAME_VERSION v21.61→v21.62 + 版本注释；`README.md`：tests 树收录 + 五十八件套（五十七件套清除）+ v21.62 守护描述；`CHANGELOG.md`：本条）+ `package.json`（test 串第 58 份）+ 新增 `tests/smoke_v2162_phaseboost.mjs`（仓库常驻，承 v21.10-v21.61 冒烟入库先例）+ 顺带把 `tests/smoke_v2161_skillhint.mjs` 的 README 件套断言与 GAME_VERSION 精确 pin 按 v21.7 惯例去硬化（存活性/前向兼容口径，实件数与精确版本由 v21.62 冒烟守护）。
- 验证：`node --check js/enemyAI.js js/data.js tests/smoke_v2161_skillhint.mjs tests/smoke_v2162_phaseboost.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 五十八件套全绿（既有 57 件逐项零回归——含 smoke_v2161 去硬化后断言 + 新增 v21.62 **34/34 全过**——GAME_VERSION 格式合法且精确 v21.62、data.js/enemyAI.js 含 v21.62 注释、enemyAI.js 源级落位（atkUp/defUp 提升与加算接入/新文案模板/旧裸文案「力量暴涨，HP 恢复」零残留/heal 回血结算与封治愈后缀逐字零回归）、SPECIES 三 Boss phase2 契约（atk/def/heal 显式正配、终焉之神封治愈）、变身链条两端同源零回归（drawBattle 角标端 boostBits / data.js phaseBoost 帮助页端）、运行期实证六档（魔王档报「力量暴涨（攻+7 防+3）」且 atk 23→30/def 13→16/HP 400→550 结算一致、领主档报「（攻+5 防+2）」且结算一致、只攻不防档报「（攻+4）」不出「防+0」且 def 不变、全零理论死代码档保持「力量暴涨，HP 恢复」逐字且攻防不变、终焉之神档「（攻+7 防+3）」与「 治愈被封印！」后缀共存且 forbid 落位、未达变身线档不变身零真身文案且攻防不变 + PHASE2_AT===0.5 阈值契约）、README/package.json 同步、smoke_v2161 件套断言与 GAME_VERSION 精确 pin 去硬化确认）。
- 【另附：开工摸底】整目录基线备份 `/tmp/my-jrpg_backup_20260908_185832`（铁律：整目录 cp -R）；基线 `npm test` 五十七件套全绿后方开工；并发检查双测 mtime 无变化（上次落盘 18:15，本次开工 ~18:58）。

## v21.61 领悟新技能战报补效果摘要「（N MP · hint · 战斗中按 2 选用）」——升级领悟瞬间一眼看清「这招干什么、耗多少蓝」（体验打磨·信息透明，承 v21.49 中毒施毒 / v21.50 灼烧命中 / v21.55 弱点抗性倍率同一「战报缺数」主线收尾：技能效果链条的菜单端 drawBattle 每招 hint 次行与状态页端「· 技能名（hint） · N MP」两端早已量化，唯独 hero.checkSkills 升级领悟这一刻只报名字——玩家升级瞬间最想确认「这招干什么、耗多少蓝」，只能事后按 2/I 翻菜单；v21.60 任务进度后回到战报缺数打磨轮换：开工摸底中期生存模拟 村100%/林99.68%/矿100%/廊100%，数值无需再调——故本轮选体验打磨而非平衡/新内容）

- 【领悟战报只报名字不报效果】`hero.checkSkills` 升级领悟此前只报「🌟 领悟了新技能【冰霜击】！」——LEARN_AT 共 6 招（Lv1 火焰斩/3 冰霜击/4 治愈术/5 雷鸣/7 陨石术/9 汲光击），每招的 mp 消耗与 hint 效果描述在 `SKILL_DATA` 里齐备（单一数据源），战斗技能菜单（drawBattle 每招带 hint 次行）与状态页（menus「· 技能名（hint） · N MP」）两端早已量化展示，唯独「领悟这一刻」——玩家与这招的第一次照面——没有任何效果说明，想知道「这招是攻击还是治疗、耗多少蓝、带什么特效」只能事后按 2 开技能菜单或按 I 翻状态页。
- 【修正：领悟战报按状态页同式补「（N MP · hint · 战斗中按 2 选用）」，SKILL_DATA 单一数据源同读】`js/hero.js`：`checkSkills` 领悟报文补效果摘要——`const sd = SKILL_DATA[skill];` 后按 `（${sd.mp} MP · ${sd.hint} · 战斗中按 2 选用）` 追加（hint 嵌套全角括号与状态页 menus.js「· 冰霜击（30%冻结（跳过敌回合）） · 5 MP」同款先例，可读性口径一致）；调技能只改 `data.js` 的 SKILL_DATA 一处、菜单/状态页/领悟战报三端自动跟随，绝无第二套口径。防御式读取：`SKILL_DATA` 漏配时保持原句「🌟 领悟了新技能【名】！」逐字不变不抛错（现 6 招全配，防御分支为理论死代码，由冒烟契约守护）。「战斗中按 2 选用」指路入口——领悟只发生在 winBattle→grantXp→checkSkills 战斗内链路，指路贴切。零结算零数值零存档变化（`skills.push` 与 `includes` 拦截逐字未动，只改 1 条文案 + SKILL_DATA import 接入）。
- 【零回归面】未动 LEARN_AT/SKILL_DATA 任何条目与字段；未动领悟判定（`learnsAt(hero.level)` 与 `!hero.skills.includes(skill)` 逐字未动）、升级结算（grantXp 属性成长/连升循环/checkSkills 调用时机）、newGame 起始技能播种（火焰斩直接入列不经 checkSkills，开局零报文）、SFX.levelup 音效、SYS_MSG_MS 时长、成就「诸技通明」判定（v21.59，读 hero.skills 与本版同字段零干扰）；未动技能菜单/状态页任何渲染。只改 4 个游戏/文档文件（`js/hero.js`：v21.61 注释 + SKILL_DATA import + 领悟文案 1 条；`js/data.js`：GAME_VERSION v21.60→v21.61 + 版本注释；`README.md`：tests 树收录 + 五十七件套（五十六件套清除）+ v21.61 守护描述；`CHANGELOG.md`：本条）+ `package.json`（test 串第 57 份）+ 新增 `tests/smoke_v2161_skillhint.mjs`（仓库常驻，承 v21.10-v21.60 冒烟入库先例）+ 顺带把 `tests/smoke_v2160_questprog.mjs` 的 README 件套断言按 v21.7 惯例去硬化（存活性口径，实件数由 v21.61 冒烟守护）。
- 验证：`node --check js/hero.js js/data.js tests/smoke_v2160_questprog.mjs tests/smoke_v2161_skillhint.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 五十七件套全绿（既有 56 件逐项零回归——含 smoke_v2160 去硬化后断言 + 新增 v21.61 **25/25 全过**——GAME_VERSION 格式合法且精确 v21.61、data.js/hero.js 含 v21.61 注释、hero.js 源级落位（SKILL_DATA import/新文案模板/旧裸文案零残留/skills.push 与 includes 拦截逐字零回归）、数据契约（learnsAt 扫描 6 招全配 SKILL_DATA、mp 正整数、hint 非空串）、运行期实证八档（Lv3 冰霜击报文逐字「（5 MP · 30%冻结（跳过敌回合） · 战斗中按 2 选用）」且 skills 追加零回归、Lv4 治愈术「（5 MP · 恢复HP并解毒 · 战斗中按 2 选用）」、Lv9 汲光击「（9 MP · 汲回伤害50%为HP·上限25%HP · 战斗中按 2 选用）」、已会该招再 checkSkills 零报文、无新技等级 Lv2 零报文、起始技能火焰斩不误报、SKILL_DATA 漏配防御档不抛错且保持原句逐字、grantXp 集成档 Lv2→3 升级领悟链路报文同式逐字）、README/package.json 同步、smoke_v2160 件套断言去硬化确认）。
- 【另附：开工摸底】整目录基线备份 `/tmp/my-jrpg_backup_20260908_175900`（铁律：整目录 cp -R）；基线 `npm test` 五十六件套全绿后方开工；并发检查双测 mtime 无变化（上次落盘 17:25，本次开工 ~17:59）。

## v21.60 讨伐支线击杀进度战报——击杀支线目标怪后一眼看清「讨伐进度 N/3 只 / 目标达成·回谁交付」（体验打磨·信息透明，承 v19.68 酿造带进度 / v19.93 宝箱蘑菇带进度同一「任务进度即时透明」主线；v21.59 新内容后回到打磨轮换：开工摸底中期生存模拟 村100%/林99.68%/矿100%/廊100%，数值无需再调——故本轮选体验打磨而非平衡）

- 【四条讨伐采集型支线的进度都读 bestiary 计数，但击杀目标怪后的胜利战报只报金币/经验】side_mist（雾灵）/side_stone（石魔像）/side_ember（残焰魔像）/side_bone（骷髅兵）的 cond/condProg 全部读 `hero.bestiary` 计数（单一数据源），玩家接取支线后每杀一只目标怪，胜利战报只有「🏆 胜利！获得 N 金币、N 经验」——想确认「离交付还差几只」只能按 J 翻日志或跑回 NPC 对话看 active 页；与 v19.93 前「宝箱开出蘑菇不报任务进度」完全同构的信息缺口。
- 【修正：winBattle 结算前快照、落账后对比补报，condProg/turnin 单一数据源同读】`js/battle.js`：`winBattle` 在 `hero.bestiary[bookName] +1` 结算前快照所有「进行中」condProg 支线的进度串（`questProgBefore`），胜利/升级/掉落/记忆碎片结算全部落账后逐一对比——进度串有变化的支线补一条战报：未集齐报 `📜 支线【名】讨伐进度 N/M`（与日志 J、NPC active 页同读 condProg 一份源），条件达成转可交付的报 `📜 支线【名】目标达成（N/M）· def.turnin`（交付去向与日志 turnin 条目同一份源），调支线目标只改 `data.js` 的 *_GOAL 一处、日志/NPC 页/战报三端自动跟随。side_name（记忆碎片）按 id 排除——其进度只在碎片块推进，v19.90 🕯️ 拾取报文已带 N/N 进度，避免双报；未接取（offer）/已可交付（turnin）/已完成（done）支线不在快照内，不多报不刷屏。零结算零数值零存档变化，只追加显示。
- 【零回归面】未动 bestiary 计数结算（`hero.bestiary[bookName] = (hero.bestiary[bookName] || 0) + 1;` 逐字未动）、普通胜利/升级/精英蘑菇/掉落/碎片任何文案与结算、questStatus 状态机、QUESTS 任何字段、消息队列机制。只改 4 个游戏/文档文件（`js/battle.js`：QUESTS/questStatus import 接入 + 结算前快照块 + 碎片块后对比补报循环 + v21.60 注释；`js/data.js`：GAME_VERSION v21.59→v21.60 + 版本注释；`README.md`：tests 树收录 + 五十六件套（五十五件套清除）+ v21.60 守护描述；`CHANGELOG.md`：本条）+ `package.json`（test 串第 56 份）+ 新增 `tests/smoke_v2160_questprog.mjs`（仓库常驻，承 v21.10-v21.59 冒烟入库先例）+ 顺带把 `tests/smoke_v2159_skillach.mjs` 的 README 件套断言按 v21.7 惯例去硬化（存活性口径，实件数由 v21.60 冒烟守护）。
- 验证：`node --check js/battle.js js/data.js tests/smoke_v2159_skillach.mjs tests/smoke_v2160_questprog.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 五十六件套全绿（既有 55 件逐项零回归——含 smoke_v2159 去硬化后断言 + 新增 v21.60 **27 项断言全过**——版本锚点、battle.js 源级落位（快照块/side_name 排除/双模板/import 接入 + bestiary 结算与 🏆 胜利文案逐字零回归）、四条支线 condProg/turnin 契约、运行期实证六档：进度档报「📜 支线【未归的矿灯】讨伐进度 2/3 只」且胜利文案/金币/经验结算一致、达成档报「目标达成（3/3 只）· 亡骨都安顿了！回矿脉找拾骨人」且 questStatus 转 turnin、已可交付再杀目标怪不多报、未接取杀目标怪不多报、杀非目标怪（进度未变）不多报、side_name 排除（碎片拾取 🕯️ 带 4/4 进度且 📜 不双报）、README/package.json 同步、smoke_v2159 件套断言去硬化确认）。
- 【另附：开工摸底】整目录基线备份 `/tmp/my-jrpg_backup_20260908_170728`（铁律：整目录 cp -R）；基线 `npm test` 五十五件套全绿后方开工；并发检查双测 mtime 无变化（上次落盘 16:25，本次开工 ~17:07）。

## v21.59 新成就「诸技通明」（技能全领悟里程碑）——收集向里程碑最后一块空白补齐：图鉴/宝箱/支线/碎片都有纪念，唯独领悟全部技能没有（新内容·单成就，承 v20.4「灯火记忆」单成就先例 / v21.52 拾骨人后再次新内容轮换——v21.53-58 连续六版「战报缺数」体验打磨已收官；开工摸底中期生存模拟 村100%/林99.68%/矿100%/廊100%，数值无需再调——故本轮选新内容而非平衡/打磨）

- 【收集向里程碑家族的唯一空白：技能全领悟无任何纪念】ACH_LIST 既有 23 项里，收集向里程碑有 图鉴全收集（perfection）/宝箱（chests）/支线全清（allquests）/记忆碎片（memoir），成长向有 lvl5/lvl10——唯独「领悟全部技能」这一枚空白：LEARN_AT 共 6 招（Lv1 火焰斩/3 冰霜击/4 治愈术/5 雷鸣/7 陨石术/9 汲光击），玩家练到 Lv9 领悟最后一招「汲光击」时，checkSkills 只报一句「🌟 领悟了新技能」，成就页没有任何对应里程碑记下这段成长。
- 【修正：新增成就「诸技通明」，判定/进度同读 LEARN_AT 单一数据源】`js/data.js`：ACH_LIST 末尾追加 `{id:'skills', name:'诸技通明', …}`——描述「领悟全部 N 个技能」的 N 由 `Object.keys(LEARN_AT).length` 派生（与 hero.checkSkills 升级领悟、core.newGame 起始技能同一份表），未来增删技能（LEARN_AT 增删一条）成就自动跟随，绝无第二套口径；判定读既有 hero.skills 存档字段（newGame 播种 learnsAt(1)、v21.48 migrateQuests 读档按等级补学漏学技能），`(g.skills||[])` 防御式读取与 `(g.bestiary||{})` 同款，旧档零迁移零影响；无 r 字段（与 lvl5/lvl10/memoir 同款纯里程碑，成长本身即奖励）。解锁走既有 unlockedAchievements→applyAchievements 通路：winBattle 的 grantXp→checkSkills 领悟最后一招后，同场胜利的 applyAchievements 即时解锁，反馈不迟到。
- 【零回归面】未动既有 23 个成就任何字段（id 全数保留由冒烟逐项断言）；未动 LEARN_AT/SKILL_DATA 任何条目；未动 allquests 支线分母（仍 0/7——skills 不是支线）；成就页/状态页/标题摘要的成就计数全部由 ACH_LIST.length 派生自动跟随（24）；未动任何结算/掉落/任务/存档结构。只改 4 个游戏/文档文件（`js/data.js`：GAME_VERSION v21.58→v21.59 + 版本注释 + ACH_LIST skills 条目及注释；`README.md`：快速上手表/图鉴&成就双处「24 项」+ tests 树收录 + 五十五件套（五十四件套清除）+ v21.59 守护描述；`CHANGELOG.md`：本条）+ `package.json`（test 串第 55 份）+ 新增 `tests/smoke_v2159_skillach.mjs`（仓库常驻，承 v21.10-v21.58 冒烟入库先例）+ 顺带把 `tests/smoke_v2158_shieldblock.mjs` 的 README 件套断言按 v21.7 惯例去硬化（存活性口径，实件数由 v21.59 冒烟守护）+ 顺带把 `tests/smoke_v2152_bonequest.mjs` 的 ACH_LIST 精确计数断言（===23）与 README「23 项」双处断言按新现实更新（承 v21.48 smoke_v2115 r[2] 计数 2→3 先例：bone 入列语义保留为 ≥23 存活性口径、README 双处随新现实为「24 项」，精确总数移交当版冒烟守护）。
- 验证：`node --check js/data.js tests/smoke_v2152_bonequest.mjs tests/smoke_v2158_shieldblock.mjs tests/smoke_v2159_skillach.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 五十五件套全绿（既有 54 件逐项零回归——含 smoke_v2158 去硬化后断言与 smoke_v2152 随新现实更新后断言 + 新增 v21.59 **31/31 全过**——GAME_VERSION 格式合法且越过 v21.58、data.js 含 v21.59 注释、learnsAt 扫描 6 招排序与 SKILL_DATA 一致性、ACH_LIST skills 契约（唯一 id/总数精确 24/无 r 字段/d 由 LEARN_AT 派生非裸字面量/源级落位）、ok/prog 谓词逐值七档（无字段 false 不抛错/空表 false/仅起始 false/缺末招 false/六招 true 顺序无关/prog 0·1·5·6/6）、既有 23 成就 id 零回归、allquests 分母不受扰（0/7）、unlockedAchievements 集成三档（六招含/缺招不含/已解锁不重复报）、运行期全链路（newGame 起始 1 招不解锁 → applyAchievements 不误解锁 → 补全六招 → 真实解锁落 hero.ach → 重复调用不重复解锁）、成就页 drawAch 两态渲染不抛错、README/package.json 同步、smoke_v2158/smoke_v2152 去硬化与随新现实更新确认）。
- 【另附：开工摸底】整目录基线备份 `/tmp/my-jrpg_backup_20260908_155744`（铁律：整目录 cp -R）；基线 `npm test` 五十四件套全绿后方开工；并发检查双测 mtime 无变化（上次落盘 15:20，本次开工 ~15:57）；中期生存模拟 `/tmp/jrpg_mid_sim.mjs` 复核（村100%/林99.68%/矿100%/廊100%）。

## v21.58 石甲挡伤命中战报补「挡下 N 点」——石甲链条四端报数收官：凝结端（enemyAI「累计 N 层，所受伤害降低 X%」）/击碎端（v21.54「石甲碎裂，剩余 N 层」）/HUD 角标端（🪨 石甲×N）三端早已量化，唯独任意攻击打在石甲上的命中这一端只报「挡下了部分伤害」不报挡了多少（体验打磨·信息透明，承 v21.54 击碎石甲剩余层数 / v21.55 弱点抗性倍率 / v21.56 防御格挡挡下点数 / v21.57 普攻暴击倍率同一「战报缺数」主线：【砍在有甲的怪上，看不到这层甲到底替我挡了多少】`battle.js` `attackMove` 的石甲分支战报此前只报 `石甲挡下了部分伤害！（剩余 N 层）`——减伤结算在 `dmg = max(1, round(dmg × SHIELD_MULT))` 这一步把「减伤前的原始伤害」直接覆盖丢弃，玩家一刀砍在石心魔像的甲上，想确认「这甲挡了多少、没甲这一刀会掉多少血」只能心算对照 HUD 角标（角标只标层数与百分比，不标本刀挡点），战报这一格缺数。零结算纯显示，改动 2 行取值/求差 + 1 条文案 + 注释）

- 【修正：命中战报按角标同口径补「挡下 N 点」，SHIELD_MULT 单一数据源同读】`js/battle.js`：减伤式前补 `const rawDmg = dmg;`（结算前捕获原伤）、减伤式后补 `const blocked = rawDmg - dmg;`（减伤前后差）；石甲战报由 `石甲挡下了部分伤害！` 改为 `石甲挡下了部分伤害${blocked > 0 ? `，挡下 ${blocked} 点` : ''}！`——挡下点数与减伤结算同读一份源（rawDmg − dmg），调石甲强度只改 `data.js` 的 SHIELD_MULT 一处，凝结端/击碎端/HUD 角标端/命中战报四端自动跟随；保底 1 钳到时 N=0 不标数值，保持原句逐字不变（承 v21.54 碎至 0 层不标剩余、v21.56 格挡保底钳到同口径）。未改变 `enemy.shield--` 层数扣减（逐字未动）、减伤结算（`max(1, round(dmg × SHIELD_MULT))` 逐字未动）、SHIELD_MULT=0.6 任何数值、陨石术 breakShield 击碎分支任何逻辑。
- 【零回归面】未动凝结端 enemyAI「（累计 N 层，所受伤害降低 X%）」与击碎端「（石甲碎裂，剩余 N 层）」（层数口径逐字保留，由本版冒烟与 smoke_v2154 双守护）；未动 HUD 角标「🪨 石甲×N（受击-X%）」SHIELD_MULT 派生与图鉴石甲标注；未动 doSkill 伤害技能/灼烧/暴击任何战报文案（承「不要并行改同屏多处」原则，本次只收挡伤命中一端）；未动任何结算/掉落/任务/存档结构。只改 4 个游戏/文档文件（`js/battle.js`：rawDmg 取值 + blocked 求差 + 挡伤战报 1 条文案 + v21.58 注释；`js/data.js`：GAME_VERSION v21.57→v21.58 + 版本注释；`README.md`：tests 树收录 + 五十四件套（五十三件套清除）+ v21.58 守护描述；`CHANGELOG.md`：本条）+ `package.json`（test 串第 54 份）+ 新增 `tests/smoke_v2158_shieldblock.mjs`（仓库常驻，承 v21.10-v21.57 冒烟入库先例）+ 顺带把 `tests/smoke_v2154_breakshield.mjs` 的挡伤端整行模板断言按 v21.58 新现实更新为「（剩余 N 层）」子句锁定（attackMove 整行已插入挡下点数，层数口径子句逐字保留——随新现实更新，承 v21.48  smoke_v2115 r[2] 计数断言 2→3 先例）+ 顺带把 `tests/smoke_v2157_critmult.mjs` 的 README 件套断言按 v21.7 惯例去硬化（存活性口径，实件数由 v21.58 冒烟守护）。
- 验证：`node --check js/battle.js js/data.js tests/smoke_v2154_breakshield.mjs tests/smoke_v2157_critmult.mjs tests/smoke_v2158_shieldblock.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 五十四件套全绿（既有 53 件逐项零回归——含 smoke_v2154 随新现实更新后断言 + 新增 v21.58 **29/29 全过**——GAME_VERSION 格式合法且越过 v21.57、data.js 含 v21.58 注释、battle.js 源级落位（v21.58 注释 + rawDmg/blocked 接入 + 新文案落位 + 旧裸文案 `石甲挡下了部分伤害！${enemy.shield` 零残留 + 减伤式与 shield-- 逐字零回归）、SHIELD_MULT 契约（===0.6）、石甲链条三端同源零回归（enemyAI 凝结端/doSkill 击碎端/drawBattle 角标端）、运行期实证四档（普攻+3 层盾 → 战报「挡下 84 点！（剩余 2 层）」且伤害 125=209×0.6 与敌 HP 4875 结算一致、无盾档零石甲文案且全额 209、保底钳到 atk1/def999 → 挡下 0 不标数值保持「石甲挡下了部分伤害！」逐字且碎至 0 层不标剩余、蓄力+盾 → 战报「挡下 126 点」且伤害 188=round(209×1.5)×0.6 与敌 HP 4812 一致，挡下点数随倍率同向 84→126）、README/package.json 同步、smoke_v2157 件套断言去硬化确认）。
- 【另附：开工摸底】整目录基线备份 `/tmp/my-jrpg_backup_20260908_150507`（铁律：整目录 cp -R）；基线 `npm test` 五十三件套全绿后方开工；并发检查双测 mtime 无变化（上次落盘 14:19，本次开工 ~15:05）；中期生存模拟 `/tmp/jrpg_mid_sim.mjs` 复核（v21.58 前 村100%/dungeon 99.57%/矿100%/廊100%，数值无需再调——故本轮选体验打磨而非平衡）。

## v21.57 普攻暴击命中战报补确切倍率「（暴击×1.8！）」——暴击链条两端报数收官：结算端（attackMove ×CRIT_MULT）/状态页端（「普攻12%暴击 ×1.8」派生）早已量化，唯独命中战报只报「（暴击！）」（体验打磨·信息透明，承 v21.49 中毒施毒文案 / v21.50 灼烧命中文案 / v21.54 击碎石甲剩余层数 / v21.55 弱点抗性倍率 / v21.56 防御格挡挡下点数同一「战报缺数」主线：【普攻暴击这一刀到底多打了多少看不到】`battle.js` `doAttack` 的暴击 note 此前只报 `（暴击！）`——而暴击链条的另外两端早已带确切倍率：结算端 `attackMove` 的 `isCrit ? CRIT_MULT : 1` 读 `CRIT_MULT`（CRIT_RATE 0.12 / CRIT_MULT 1.8 单一数据源），状态页端 menus.js「· 普攻12%暴击 ×1.8」由 CRIT_RATE/CRIT_MULT 派生——唯独命中这一刻只报性质；玩家普攻出了暴击，想确认「这 80% 加成到底吃没吃到」只能翻状态页，战报这一格缺数。零结算纯显示，改动 1 条 fmt 文案 + 注释）

- 【修正：命中战报按状态页同口径补 ×N 倍率，CRIT_MULT 单一数据源同读】`js/battle.js`：doAttack 的暴击 note 由 `（暴击！）` 改为 `` `（暴击×${CRIT_MULT}！）` ``——倍率值与 attackMove 结算逐值同源（isCrit 恒乘 CRIT_MULT，无第二档）；调暴击强度只改 `data.js` 的 CRIT_RATE/CRIT_MULT 一处，结算/状态页/战报三端自动跟随。未改变暴击判定（`Math.random() < CRIT_RATE` 逐字未动）、结算（`isCrit ? CRIT_MULT : 1` 逐字未动）、浮字/震屏触发线（`dmg >= BIG_DMG || isCrit` 逐字未动）、CRIT_RATE/CRIT_MULT 任何数值；暴击仅普攻可触发（doSkill 调 attackMove 的 crit 形参恒传 false），技能战报零影响。
- 【零回归面】未动 attackMove/CHARGE_MULT/蓄力语义（v14.0「蓄力只加成威力」口径）；未动状态页「普攻N%暴击 ×N」派生与帮助页任何行；未动敌方 HP 剩余读数（v20.0）与 crit 图标 💥/浮字加粗（BIG_DMG 同源）任何逻辑。只改 4 个游戏/文档文件（`js/battle.js`：v21.57 注释 + fmt 暴击 note 1 条文案；`js/data.js`：GAME_VERSION v21.56→v21.57 + 版本注释；`README.md`：tests 树收录 + 五十三件套（五十二件套清除）+ v21.57 守护描述；`CHANGELOG.md`：本条）+ `package.json`（test 串第 53 份）+ 新增 `tests/smoke_v2157_critmult.mjs`（仓库常驻，承 v21.10-v21.56 冒烟入库先例）+ 顺带把 `tests/smoke_v2156_defendblock.mjs` 的 README 件套断言按 v21.7 惯例去硬化（存活性口径，实件数由 v21.57 冒烟守护）。
- 验证：`node --check js/battle.js js/data.js tests/smoke_v2156_defendblock.mjs tests/smoke_v2157_critmult.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 五十三件套全绿（既有 52 件逐项零回归——含 smoke_v2156 去硬化后断言 + 新增 v21.57 **27 项断言全过**——GAME_VERSION 格式合法且越过 v21.56、data.js 含 v21.57 注释、battle.js 源级落位（v21.57 注释 + 新文案落位 + 旧裸文案 `${crit ? '（暴击！）' : ''}' 零残留 + crit 判定与 ×CRIT_MULT 结算逐字零回归 + CRIT_MULT import 接入）、CRIT_RATE/CRIT_MULT 契约（===0.12 / ===1.8）、暴击链条两端同源零回归（attackMove 结算端/状态页派生端）、运行期实证三档（startBattle+playerAction('attack') 真实路径强制 Math.random：暴击档报「（暴击×1.8！）」且伤害 311=raw×1.8 与敌 HP 4689 结算一致、非暴击档零暴击文案且伤害 209 原浮动值与敌 HP 4791 一致、蓄力+暴击双 note「（暴击×1.8！）（蓄力爆发！）」共存且伤害 467=raw×1.8×1.5 两倍率同乘与敌 HP 4533 一致、暴击档>非暴击档与倍率同向、💥 crit 图标与敌方 HP 剩余读数零回归）、README/package.json 同步、smoke_v2156 件套断言去硬化确认）。
- 【另附：开工摸底】整目录基线备份 `/tmp/my-jrpg_backup_20260908_*`（铁律：整目录 cp -R）；基线 `npm test` 五十二件套全绿后方开工；并发检查双测 mtime 无变化（上次落盘 13:32，本次开工 ~13:47）。

## v21.56 防御格挡命中战报补「挡下 N 点」——防御链条四端报数收官：摆架势端（doDefend「本回合受到的伤害减半」）/HUD 角标端（「防御中 · 减伤50%」DEFEND_MULT 派生）/受击预判端（「防御后-N血」×DEFEND_MULT 列值）三端早已量化，唯独实际命中这一端只报「被防御格挡」不报挡了多少（体验打磨·信息透明，承 v21.49 中毒施毒文案 / v21.50 灼烧命中文案 / v21.54 击碎石甲剩余层数 / v21.55 弱点抗性倍率同一「战报缺数」主线：【防御后挨一刀，看不到这一刀防御到底替我挡了多少】`enemyAI.js` `enemyAct` 的受击战报此前只报 `（被防御格挡！）`——减伤结算在 `dmg = max(1, round(dmg × DEFEND_MULT))` 这一步把「减伤前的原始伤害」直接覆盖丢弃，玩家按 5 防御后挨一刀，想确认「减半到底减了多少、这刀没防御会掉多少血」只能心算对照受击预判行（drawBattle 预判行早已按 ×DEFEND_MULT 列「防御后-N血」），战报这一格缺数。零结算纯显示，改动 2 行取值/求差 + 1 条文案 + 注释）

- 【修正：命中战报按预判同口径补「挡下 N 点」，DEFEND_MULT 单一数据源同读】`js/enemyAI.js`：减伤式前补 `const rawDmg = dmg;`（结算前捕获原伤）、减伤式后补 `const blocked = hero.defending ? rawDmg - dmg : 0;`（减伤前后差，非防御恒 0）；受击战报格挡段由 `'（被防御格挡！）'` 改为 `` `（被防御格挡${blocked > 0 ? `，挡下 ${blocked} 点` : ''}！）` ``——挡下点数与减伤结算同读一份源（rawDmg − dmg），调防御强度只改 `data.js` 的 DEFEND_MULT 一处，摆架势文案/HUD 角标/受击预判/命中战报四端自动跟随；保底 1 钳到时 N=0 不标数值，保持「（被防御格挡！）」逐字不变（承 v21.54 碎至 0 层不标剩余同口径）。未改变 cmdDmg 调用（`cmdDmg(enemy.atk, hero.defMax, mult)` 逐字未动）、减伤结算（`max(1, round(dmg × DEFEND_MULT))` 逐字未动）、反击判定（`hero.defending && Math.random() < COUNTER_CHANCE` 逐字未动）、DEFEND_MULT 任何数值。
- 【零回归面】未动 doDefend 摆架势文案「本回合受到的伤害减半」与回蓝（DEFEND_MP）；未动 drawBattle 防御角标「防御中 · 减伤50% · 回2MP/回合 · 50%几率反击」（DEFEND_MULT/COUNTER_CHANCE 派生）与受击预判「防御后-N血」列值（三端同源零回归由冒烟守护）；未动中毒/灼烧/石甲/变身/回血任何敌方行为文案；未动任何结算/掉落/任务/存档结构。只改 4 个游戏/文档文件（`js/enemyAI.js`：rawDmg 取值 + blocked 求差 + 格挡战报 1 条文案 + v21.56 注释；`js/data.js`：GAME_VERSION v21.55→v21.56 + 版本注释；`README.md`：tests 树收录 + 五十二件套（五十一件套清除）+ v21.56 守护描述；`CHANGELOG.md`：本条）+ `package.json`（test 串第 52 份）+ 新增 `tests/smoke_v2156_defendblock.mjs`（仓库常驻，承 v21.10-v21.55 冒烟入库先例）+ 顺带把 `tests/smoke_v2155_weakresist.mjs` 的 README 件套断言按 v21.7 惯例去硬化（存活性口径，实件数由 v21.56 冒烟守护）。
- 验证：`node --check js/enemyAI.js js/data.js tests/smoke_v2155_weakresist.mjs tests/smoke_v2156_defendblock.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 五十二件套全绿（既有 51 件逐项零回归——含 smoke_v2155 去硬化后断言 + 新增 v21.56 **26 项断言全过**——GAME_VERSION 格式合法且越过 v21.55、data.js 含 v21.56 注释、enemyAI.js 源级落位（rawDmg 取值/blocked 求差接入 + 新文案条件派生落位 + 旧裸文案 `hero.defending ? '（被防御格挡！）'` 零残留 + 减伤式与 cmdDmg 调用逐字零回归）、DEFEND_MULT 契约（===0.5）、防御链条三端同源零回归（doDefend 摆架势端/drawBattle 角标端/受击预判端）、运行期实证四档（enemyAct 真实路径：防御中普攻 atk30/def10 → raw 50、实伤 25、报「（被防御格挡，挡下 25 点！）」且 HP 120→95 与「（我方 HP 95/120）」零回归；未防御 → 零格挡文案且全额 50 伤害 HP 120→70；保底钳到 atk1/def999 → raw 1、实伤 1、报「（被防御格挡！）」逐字不标挡下且 HP 120→119；重击+防御 → raw 95、实伤 48、报「（被防御格挡，挡下 47 点！）」且 HP 120→72，挡下点数随倍率同向）、README/package.json 同步、smoke_v2155 件套断言去硬化确认）。
- 【另附：开工摸底】整目录基线备份 `/tmp/my-jrpg_backup_20260908_131619`（铁律：整目录 cp -R）；基线 `npm test` 五十一件套全绿后方开工；并发检查双测 mtime 无变化（上次落盘 12:38，本次开工 13:16）；中期生存模拟 `/tmp/jrpg_mid_sim.mjs` 复核（v21.55 后 村100%/林99.72%/矿100%/廊100%，数值无需再调——故本轮选体验打磨而非平衡）。

## v21.55 弱点/抗性命中战报补确切倍率——克制链条四端报数收官：结算端（elemMult 读 ELEM_MULT）/帮助页「技能克制」行（弱点伤害×1.35·抗性伤害×0.7）/图鉴 codexTag（弱点·火×1.35）三端早已带确切倍率，唯独命中战报这一端只报性质不报数值（体验打磨·信息透明，承 v21.49 中毒施毒文案 / v21.50 灼烧命中文案 / v21.54 击碎石甲剩余层数同一「战报缺数」主线：【放技能命中弱点多打了多少、被抗性削了多少看不到】`battle.js` `doSkill` 的克制 note 此前只报 `（弱点）`/`（抗性）`——而克制链条的另外三端早已带确切倍率：结算端 `rules.elemMult` 读 `ELEM_MULT`（weak 1.35/resist 0.7 单一数据源），帮助页「技能克制」行由 `ELEM_MULT` 派生「弱点伤害×1.35 · 抗性伤害×0.7」，图鉴 `codexTag` 标注「弱点·火×1.35 / 抗性·冰×0.7」——唯独命中这一刻只报性质；玩家放火焰斩打骷髅兵想确认「这 35% 加成到底吃没吃到」只能翻图鉴或帮助页，战报这一格缺数。零结算纯显示，改动 2 条文案+注释+import）

- 【修正：命中战报按图鉴同口径补 ×N 倍率，ELEM_MULT 单一数据源同读】`js/battle.js`：import 补 `ELEM_MULT`（与 v21.50 灼烧补 `BURN_PCT` 同法）；克制 note 由 `note += '（弱点）'`/`note += '（抗性）'` 改为 ``note += `（弱点×${ELEM_MULT.weak}）` ``/``note += `（抗性×${ELEM_MULT.resist}）` ``——倍率值与 `elemMult` 判定逐值同源（>1 恒为 ELEM_MULT.weak、<1 恒为 ELEM_MULT.resist，无第三档）；调克制强度只改 `data.js` 一处，结算/帮助页/图鉴/战报四端自动跟随。未改变 elemMult 两次判定调用（逐字未动）、mult 结算（`skill.mult * elemMult(...)` 逐字未动）、ELEM_MULT 任何数值。
- 【零回归面】未动 SKILL_DATA 任何技能 element/weak/resist 字段；未动 MON_BASE/SPECIES 任何 weak/resist 设定；未动帮助页「技能克制」行与图鉴 codexTag 标注（三端同源零回归由冒烟守护）；未动战斗画面敌情行「· 弱点火 / · 抗火」常驻标注（承「不要并行改同屏多处」原则，本次只收战报一端）；未动任何结算/掉落/任务/存档结构。只改 4 个游戏/文档文件（`js/battle.js`：import + 克制 note 2 条文案 + v21.55 注释；`js/data.js`：GAME_VERSION v21.54→v21.55 + 版本注释；`README.md`：tests 树收录 + 五十一件套（五十件套清除）+ v21.55 守护描述；`CHANGELOG.md`：本条）+ `package.json`（test 串第 51 份）+ 新增 `tests/smoke_v2155_weakresist.mjs`（仓库常驻，承 v21.10-v21.54 冒烟入库先例）+ 顺带把 `tests/smoke_v2154_breakshield.mjs` 的 README 件套断言按 v21.7 惯例去硬化（存活性口径，实件数由 v21.55 冒烟守护）。
- 验证：`node --check js/battle.js js/data.js tests/smoke_v2154_breakshield.mjs tests/smoke_v2155_weakresist.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 五十一件套全绿（既有 50 件逐项零回归——含 smoke_v2154 去硬化后断言 + 新增 v21.55 **27 项断言全过**——GAME_VERSION 格式合法且越过 v21.54、data.js 含 v21.55 注释、battle.js 源级落位（ELEM_MULT import 接入 + 新文案落位 + 旧裸文案 `note += '（弱点）';`/`note += '（抗性）';` 零残留 + v21.55 注释 + elemMult 两次判定逐字零回归）、ELEM_MULT 契约（weak 1.35/resist 0.7）、克制链条三端同源零回归（rules 结算端/帮助页/图鉴）、运行期实证三档（火焰斩真实路径：弱点档报「（弱点×1.35）」且伤害落 ×1.35 浮动区间 416~508、抗性档报「（抗性×0.7）」且伤害落 ×0.7 浮动区间 215~263、中性档零克制文案；三档伤害排序 弱点>中性>抗性 与倍率同向；MP 读数「（MP 36/40）」与灼烧 note「（灼烧 2 回合·每回合 -200 HP）」零回归）、README/package.json 同步、smoke_v2154 件套断言去硬化确认）。
- 【另附：开工摸底】整目录基线备份 `/tmp/my-jrpg_backup_20260908_122348`（铁律：整目录 cp -R）；基线 `npm test` 五十件套全绿后方开工；并发检查双测 mtime 无变化（上次落盘 11:39，本次开工 12:23）。

## v21.54 陨石术击碎石甲战报补「剩余 N 层」——石甲链条三端报数收官：凝结端「累计 N 层」/受击挡伤端「剩余 N 层」早已量化，唯独技能击碎这一端只说「碎裂」不报还剩几层（体验打磨·信息透明，承 v21.49 中毒施毒文案 / v21.50 灼烧命中文案 / v21.53 逃脱失败后果提示同一「战报缺数」主线：【放陨石术砸碎石甲后看不到还剩几层】`battle.js` `doSkill` 的 breakShield 分支（陨石术 breakShield:1）此前只报 `（石甲碎裂）`——而石甲链条的另外两端早已报层数：敌方凝结石甲时 `enemyAI.js` 报 `（累计 ${enemy.shield} 层，所受伤害降低 X%）`，任意攻击打在石甲上时 `attackMove` 报 `（剩余 ${enemy.shield} 层）`，战斗 HUD 右上角也有 `🪨 石甲×N` 常驻角标——唯独玩家主动用陨石术击碎这一端只报「碎裂」不说剩余；玩家花 14MP 放全游戏最贵一招，想确认「这怪石甲还剩几层、下回合要不要再来一发」只能瞄角标，战报这一格缺数。零结算纯显示，改动一行文案+注释）

- 【修正：碎后余层报「（石甲碎裂，剩余 N 层）」，碎至 0 层保持「（石甲碎裂）」逐字不变，与 attackMove 挡伤端同口径】`js/battle.js`：breakShield 分支战报由 `note += '（石甲碎裂）'` 改为 `note += `（石甲碎裂${enemy.shield > 0 ? `，剩余 ${enemy.shield} 层` : ''}）``——剩余层数读赋值后的 `enemy.shield`（单一数据源，与凝结端/挡伤端/HUD 角标同一份源）；碎至 0 层时不标剩余，与 attackMove 末层挡伤「不标剩余」同口径（敌甲角标随 shield=0 消失亦不误导）。未改变击碎赋值（`enemy.shield = Math.max(0, enemy.shield - skill.breakShield)` 逐字未动）、挡伤结算（attackMove 盾分支逐字未动）、SHIELD_MULT 任何数值。
- 【零回归面】未动 SKILL_DATA 任何技能字段（陨石术 breakShield:1/mult 4.2/trueBonus 1.25 逐字）；未动敌方凝结石甲的层数上限（maxShield）/判定/提示；未动 HUD 角标与图鉴石甲标注；未动任何结算/掉落/任务/存档结构（enemy.shield 赋值与原行逐值同式，纯追加显示）。只改 4 个游戏/文档文件（`js/battle.js`：breakShield 战报 1 行 + v21.54 注释；`js/data.js`：GAME_VERSION v21.53→v21.54 + 版本注释；`README.md`：tests 树收录 + 五十件套（四十九件套清除）+ v21.54 守护描述；`CHANGELOG.md`：本条）+ `package.json`（test 串第 50 份）+ 新增 `tests/smoke_v2154_breakshield.mjs`（仓库常驻，承 v21.10-v21.53 冒烟入库先例）+ 顺带把 `tests/smoke_v2153_fleefail.mjs` 的 README 件套断言按 v21.7 惯例去硬化（存活性口径，实件数由 v21.54 冒烟守护）。
- 验证：`node --check js/battle.js js/data.js tests/smoke_v2153_fleefail.mjs tests/smoke_v2154_breakshield.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 五十件套全绿（既有 49 件逐项零回归——含 smoke_v2153 去硬化后断言 + 新增 v21.54 **21 项断言全过**——GAME_VERSION 格式合法且越过 v21.53、data.js 含 v21.54 注释、battle.js 源级落位（新文案条件派生表达式 + 旧裸文案 `note += '（石甲碎裂）';` 零残留 + v21.54 注释）、石甲链条两端口径零回归（enemyAI 凝结端「累计 N 层」/attackMove 挡伤端「剩余 N 层」逐字）、碎裂赋值结算逐字零回归、运行期实证三档（护盾 3 层 → 挡伤行报「剩余 2 层」+ 技能战报含「（石甲碎裂，剩余 1 层）」且 enemy.shield 归 1、2 层 → 技能战报含「（石甲碎裂）」且不再标剩余 + enemy.shield 归 0、0 层 → 不触发任何碎裂文案且不误触挡伤分支）、README tests 树收录 smoke_v2154_breakshield + 五十件套（四十九件套清除）+ v21.54 守护描述、package.json 已收录（第 50 份）、smoke_v2153 件套断言去硬化确认）。
- 【另附：开工摸底】整目录基线备份 `/tmp/my-jrpg_backup_20260908_113219`（铁律：整目录 cp -R）；基线 `npm test` 四十九件套全绿后方开工；并发检查双测 mtime 无变化（上次落盘 10:52，本次开工 11:32）。

## v21.53 逃脱失败战报追加后果提示「（X 即将行动）」——按 4 逃跑失败不再静默让回合，与 Boss 气场分支「本回合行动保留」形成对照口径（体验打磨·信息透明，承 v19.94-96 / v21.49-50 同一主线；v21.52 新内容后回到体验打磨轮换：开工摸底中期生存模拟 村100%/林99.69%/矿100%/廊100%，数值无需再调——故本轮选体验打磨而非平衡）

- 【逃跑失败只报「❌ 逃脱失败！」，玩家往往没意识到这一回合已经让给敌方】`battle.js` `doFlee()` 普通逃跑失败分支此前只有一句结论——而失败的真实后果是 `afterPlayer()` 推进 battleTurn 并在 600ms 后编排 enemyAct（敌方白打一回合）；对照 Boss 气场分支早已明示「（本回合行动保留）」，普通失败分支恰恰是「行动被消耗」却无任何提示，两分支缺一端对照口径。指令栏虽有「[4]逃跑·成功率约60%」（FLEE_SUCCESS 单一数据源），但「失败后会发生什么」这一信息全程缺失。
- 【修正：失败战报补「（X 即将行动）」，X 读 enemy.name 单一数据源，纯显示零结算】`battle.js` 改为：`❌ 逃脱失败！（${enemy.name} 即将行动）`——与 `afterPlayer()` 实际推进 battleTurn + 编排 enemyAct 的结算同口径；成功率口径仍由指令栏承载（FLEE_SUCCESS 单一数据源），战报不重复标注。未改变逃跑判定（`Math.random() < FLEE_SUCCESS` 逐字未动）、成功分支（「🏃 成功逃脱了！」逐字未动）、Boss 气场分支（逐字未动）、回合推进与 600ms 敌方行动编排任何逻辑。
- 【零回归面】未动 FLEE_SUCCESS=0.6、doFlee 三分支结算、afterPlayer/enemyAct 编排、指令栏「约60%」派生、帮助页战斗行任何逻辑。只改 4 个游戏/文档文件（`js/battle.js`：doFlee 失败分支 1 条 blog 文案 + 注释；`js/data.js`：GAME_VERSION v21.52→v21.53 + 版本注释；`README.md`：tests 树收录 + 四十九件套（四十八件套清除）+ v21.53 守护描述；`CHANGELOG.md`：本条）+ `package.json`（test 串第 49 份）+ 新增 `tests/smoke_v2153_fleefail.mjs`（仓库常驻，承 v21.10-v21.52 冒烟入库先例）+ 顺带把 `tests/smoke_v2152_bonequest.mjs` 的 README 件套断言按 v21.7 惯例去硬化（存活性口径，实件数由 v21.53 冒烟守护）。
- 验证：`node --check js/battle.js js/data.js tests/smoke_v2152_bonequest.mjs tests/smoke_v2153_fleefail.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 四十九件套全绿（既有 48 件逐项零回归——含 smoke_v2152 去硬化后断言 + 新增 v21.53 **24 项断言全过**——版本锚点、battle.js 源级落位（新文案落位 + 旧裸文案零残留 + 成功/Boss 两分支逐字零回归 + 判定读 FLEE_SUCCESS 单一数据源）、FLEE_SUCCESS===0.6 与指令栏派生口径零回归、运行期实证五档（强制 Math.random：失败档报「❌ 逃脱失败！（练功木桩 即将行动）」且 battleTurn 1→2 回合消耗与文案同口径、换名敌人「（史莱姆 即将行动）」敌名动态派生、边界 rand===FLEE_SUCCESS 判负「< 严格小于」语义守护、成功档「🏃 成功逃脱了！」逐字且 battleTurn 不变回 world、Boss 档「气场压制…（本回合行动保留）」逐字且 battleTurn 不变）、README/package.json 同步、smoke_v2152 件套断言去硬化确认）。
- 【另附：开工摸底】整目录基线备份 `/tmp/my-jrpg_backup_20260908_103723`（铁律：整目录 cp -R）；基线 `npm test` 四十八件套全绿后方开工；并发检查双测 mtime 无变化；中期生存模拟 `/tmp/jrpg_mid_sim.mjs` 复核（v21.52 后 村100%/林99.69%/矿100%/廊100%，数值无需再调——故本轮选体验打磨而非平衡）。

## v21.52 星井矿脉新 NPC「拾骨人」+ 讨伐支线「未归的矿灯」——矿脉补齐四图最后一块讨伐采集型支线拼图（新内容·支线/NPC，承 side_mist/side_stone/side_ember 同一「讨伐采集型支线」模式 / v21.36 掌灯阿婆后首次 NPC 轮换——v21.48-51 连续四版技能/口径/平衡，本版回到新内容：星井矿脉此前是四图中唯一没有讨伐采集型支线的地图（雾语林有 雾灵/石魔像 两条、无字回廊有 残焰魔像、潮灯镇有蘑菇委托），而矿道里游荡的骷髅兵（MON_BASE weak:fire/minLv:2，矿脉全池出没）设定上正是「当年没能走出矿洞的矿工」——与全游「名字/记忆」主题同脉的现成钩子一直没人来收）

- 【数据层四件套 + 任务/成就，全部走既有通路、零新逻辑】`js/data.js`：MAPS.cave.extras 补 `{ x:5, y:10, ty:'NPC' }`（南走廊北侧凹龛，岩地可行走格、南贴 PATH 走廊可面对面对话）；NPC_SPOTS 补 `'5,10':'digger'`（全局坐标键——全图 extras 扫描 (5,10) 仅此一处，既有 16 键逐字未动）；NPCS.digger（mark:'hood'，静态 lines 仅兜底——任务页四态齐备经 npcQuestPages 恒优先，承 guard/hunter「任务遮蔽闲聊」先例）；QUESTS.side_bone（无 unlockOn → 开局即 offer，拾骨人只在矿脉可达、接取时机天然正确，承 side_ember 同款；cond/condProg 读既有 bestiary['骷髅兵'] 计数，阈值单一数据源 BONE_GOAL=3——判定/进度/目标文案/接取对话同读；奖励 80 金+1 灵药，介于 side_stone 60 与 side_ember 100 之间、与矿脉推荐 Lv.6 档次匹配；active 为函数页按 hero 实时报「已安顿 N/3 只」并附弱火提示——与图鉴 codexTag 骷髅兵「弱点·火×1.35」同一 MON_BASE 数据源）；ACH_LIST 补「亡骨还乡」（22→23，单支线成就模式读 quests.side_bone==='done'；allquests「灯火同心」支线分母由 QUESTS 派生自动跟随 6→7）；`js/view/sprites.js`：NPC_SHEET 补 digger→mwSage（兜帽长者袍，守名者/掌灯阿婆同款，贴图缺失时 drawNpcMark 'hood' 程序化兜底）。任务状态机/对话解析/奖励结算/日志/横幅/顶标全走既有 QUESTS 驱动通路，零结算零新逻辑。
- 【零回归面】未动既有 6 条支线/16 个 NPC 坐标键/任何怪物数值/遇敌池/掉落/存档结构（旧档 quests 无 side_bone 字段时 questStatus 自动落 offer，零迁移；bestiary 计数持久化既有字段，进矿前已在雾语林讨伐过的骷髅兵计数天然有效）；未动矿脉「无泉水/旅店」事实（extras 仍无 FOUNTAIN/INN）；未动 HELP_PAGES（地图指南矿脉行不枚举 NPC，与 老矿工/车夫 未列同口径）；TRAVEL_LIST 矿脉行不列 NPC 逐字未动。只改 4 个游戏/文档文件（`js/data.js`：GAME_VERSION v21.51→v21.52 + 版本注释 + BONE_GOAL + cave.extras + NPC_SPOTS + NPCS.digger + QUESTS.side_bone + ACH_LIST bone + export；`js/view/sprites.js`：NPC_SHEET 映射；`README.md`：tests 树收录 + 四十八件套（四十七件套清除）+ v21.52 守护描述 + 矿脉条目拾骨人 + 成就 23 项双处 + faceHint NPC 名单；`CHANGELOG.md`：本条）+ `package.json`（test 串第 48 份）+ 新增 `tests/smoke_v2152_bonequest.mjs`（仓库常驻，承 v21.10-v21.51 冒烟入库先例）+ 顺带把 `tests/smoke_v2151_elitegolem.mjs` 的 README 件套断言按 v21.7 惯例去硬化（存活性口径，实件数由 v21.52 冒烟守护）。
- 验证：`node --check js/data.js js/view/sprites.js tests/smoke_v2151_elitegolem.mjs tests/smoke_v2152_bonequest.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 四十八件套全绿（既有 47 件逐项零回归——含 smoke_v2151 去硬化后断言 + 新增 v21.52 **46 项断言全过**——GAME_VERSION 格式合法且越过 v21.51、BONE_GOAL===3 导出、NPC_SPOTS 落位与唯一映射与既有 16 键未动、(5,10) 全图 extras 仅 cave 一处防撞、cave.extras 源级落位、NPCS.digger 契约、QUESTS.side_bone 契约（双指 digger/无 unlockOn/cond·condProg 同源/奖励档位/四态页 [Enter] 收尾/offer 由 BONE_GOAL 派生非裸字面量）、ACH_LIST bone 唯一且 22→23、allquests 分母自动跟随 0/7 且不误判、运行期状态机全链路（offer→accept→active(2/3)→turnin(3/3)→reward(+80 金/+1 灵药)→done→成就解锁→顶标归零→不重复发奖）、active 函数页实时进度、offer/done 页内容、旧档兼容落 offer、loadMap(cave) 落位 at(5,10)===NPC 且南走廊站位可行走（cave 的 PATH 经 replaceTiles 转 CAVE 岩地）、Enter/E 真实交互开对话 curNpc===digger、矿脉无泉水事实零回归、sprites 映射源级落位、README/package.json 同步、smoke_v2151 件套断言去硬化确认）。
- 【另附：开工摸底】整目录基线备份 `/tmp/my-jrpg_backup_20260908_091743`（铁律：整目录 cp -R）；基线 `npm test` 四十七件套全绿后方开工；并发检查双测 mtime 无变化；中期生存模拟 `/tmp/jrpg_mid_sim.mjs` 复核（v21.51 后 村100%/林99.75%/矿100%/廊100%，数值无需再调——故本轮选新内容而非平衡；逃跑对照组揭示「对精英连续逃跑比硬打更危险」系模拟器不喝药 artifact，非游戏缺陷）。

## v21.51 精英「石心魔像」攻击成长收口 [12,3]→[12,2]——推荐等级名副其实：Lv3 登场不再「贴线两击」秒人，单场死亡率 35.8%→0.6%，威胁降级为「资源压力」而非「即死轮盘」（数值平衡，承 v20.9 残焰魔像校准「⚠Lv 推荐等级必须名副其实」/ v3.26「石甲机制怪不该同时是纯数值最强」/ v19.43 骷髅兵门槛 同一平衡主线：开工摸底的中期生存模拟发现——雾语林推荐 Lv3 连刷 10 场死亡 14.69%、99% 凶手是石心魔像（其余三图 100% 生还）；聚焦单场模拟（40000 场，Lv3 推荐装备 铜剑+皮甲、3 药水、技能优先的真实策略）死亡率高达 35.8%：它 Lv3 登场即 atk 21，对 def 13 的玩家一击均伤 29——均值两击 58 已贴满血 59 的线，含 ±10% 浮动上限 32×2=64 ≥ 59，满血硬打两发高飞拳直接被带走、连喝药窗口都没有——而它的进战预警只是「⚠ 此敌有些棘手」（threat 1，v21.47 口径），v20.9 给残焰魔像校准的精英标尺是「重击占满血 92%、满血可存活」，这只「机制怪」的普攻两击却已越线；它还是村长蘑菇支线的必掉蘑菇来源（🍄 tag），玩家为做任务要主动反复撞它）

- 【修正：只收攻击成长，铁壁身份与全部生态零改动】`js/data.js`：ELITE_GOLEM `atk:[12,3]` → `atk:[12,2]`（基础 12 未动，仅每级成长 3→2——Lv3 atk 21→18：均伤 23、浮动上限 25，两击上限 50 < 满血 59，满血两击可存活、三击才倒，喝药/防御窗口回归；改后 40000 场单场死亡率 0.60%、场均喝药 ≈1.01 瓶——仍是雾语林最难啃的遭遇（def [15,2] 铁壁 + 石甲耗回合未动），只是从「即死轮盘」降级为「资源压力」）。hp [58,10]/def [15,2]/xp [40,6]/gold [45,6]、ELITE_GATE_LV=3、ELITE_CHANCE=0.07、石甲 acts（attack 55/shield 45·血<50%·至多3层）、🍄 必掉蘑菇 tag 逐字未动；单一数据源不变——encounter.eliteEncounter 生成与 rules.codexStats 图鉴参考同读此表，改一处全端自动跟随；属性遇敌时现算，旧档零迁移零影响。
- 【零回归面】未动 MON_BASE 七种普通怪任何字段（同图石魔像 atk [8,2] 等逐字）；未动 EMBER_GOLEM（atk 44，v20.9 校准）；未动三 Boss/试炼/遇敌池权重/zone 倍率/DIFF_SCALE 困难倍率（精英分支不走 zone，困难 ×1.15 仍乘在收口后数值上，困难档威胁同步等比下降）；未动威胁预警（threatWarn 通用分支两击线本就越不过 66hp 测试档，「有些棘手」口径逐字不变——预警与实感终于名副其实）；未动任何战斗结算/掉落/任务/存档结构。只改 4 个游戏/文档文件（`js/data.js`：GAME_VERSION v21.50→v21.51 + 两处注释 + ELITE_GOLEM atk 成长；`README.md`：tests 树收录 + 四十七件套（四十六件套清除）+ v21.51 守护描述；`CHANGELOG.md`：本条）+ `package.json`（test 串第 47 份）+ 新增 `tests/smoke_v2151_elitegolem.mjs`（仓库常驻，承 v21.10-v21.50 冒烟入库先例）+ 顺带把 `tests/smoke_v2150_burnhint.mjs` 的 README 件套断言按 v21.7 惯例去硬化（存活性口径，实件数由 v21.51 冒烟守护）+ 基件 `tests/smoke.mjs` 的图鉴同源断言随新现实更新（石心魔像 Lv4 期望 98/24/23 → 98/20/23——v6.9「codexStats 与 eliteEncounter 逐字同源」公式断言跟随数据层，与 v21.48 更新 smoke_v2115 r[2] 计数同款先例）。
- 验证：`node --check js/data.js tests/smoke_v2150_burnhint.mjs tests/smoke_v2151_elitegolem.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 四十七件套全绿（既有 46 件逐项零回归——含 smoke_v2150 去硬化后断言 + 新增 v21.51 **22 项断言全过**——GAME_VERSION 格式合法且越过 v21.50、data.js 含 v21.51 注释、ELITE_GOLEM.atk 落位 [12,2]（基础 12 未动）、hp/def/xp/gold/color/name 零回归、旧基准 `atk:[12,3]` 源级零残留、设计标尺（Lv3 推荐装备：无浮动两击 46 < 59 且含浮动上限两击 50 < 59）+ 判别力（旧成长 3 同标尺失守 64 ≥ 59）、单一数据源（eliteEncounter Lv3 生成 atk===18 且石甲 acts 合入、codexStats 同源跟随 atk===18）、v21.47 威胁预警口径零回归（仍「有些棘手」不升级）、运行期蒙特卡洛（4000 场真实 eliteEncounter+pickAct+cmdDmg：死亡率 <3%、场均喝药 ≥0.5 瓶资源压力保留）、同图石魔像/残焰魔像/门槛/概率/石甲无 heavy 零回归、README/package.json 同步、smoke_v2150 件套断言去硬化确认）。
- 【另附：开工摸底】整目录基线备份 `/tmp/my-jrpg_backup_20260908_081918`（铁律：整目录 cp -R）；基线 `npm test` 四十六件套全绿后方开工；并发检查双测 mtime 无变化；中期生存模拟 `/tmp/jrpg_mid_sim.mjs` 摸底发现 dungeon 死亡率尖峰（14.69%，99% 凶手石心魔像）→ 聚焦单场模拟 `/tmp/jrpg_elite_sim.mjs` 定位（35.8%）→ 候选成长 3/2/1 三档对比（35.83%/0.60%/0.00%）选定 2（威胁保留、即死消除）——故本轮选数值平衡而非体验打磨。

## v21.50 灼烧命中瞬间战报追加「灼烧 N 回合·每回合 -N HP」——火焰斩上火文案按 tick 同式 max(DOT_MIN, round(hpMax×BURN_PCT)) 量化，回合数读赋值后 enemy.burn（灼烧可叠加，与角标同源），灼烧链条三端全部量化（体验打磨·信息透明，承 v21.49 中毒瞬间战报同一补法 / v21.8 灼烧 tick 带伤害值与敌方剩余 HP / v19.94-96 信息透明同一主线：【命中上火这一刻看不到每回合烧多少、还要烧几回合】`battle.js` `doSkill` 的灼烧战报此前只报 `（灼烧）`——而灼烧链条的另外三端早已量化：HUD 灼烧角标（drawBattle「🔥 灼烧 N · 每回合 -N血」）、灼烧 tick 战报（v21.8「灼烧令…受到 N 点伤害！（敌方 HP 剩余…）」）、技能 hint（「灼烧2回合·每回合约-4%最大HP」，由 BURN_PCT 派生）。玩家花 4MP 放火焰斩，想确认「这灼烧每回合到底烧多少、值不值这口蓝」仍需等首次 tick 或瞄角标——命中战报是灼烧决策的第一现场，唯独它缺数）

- 【修正：上火战报按 tick 同式补每回合烧血数与剩余回合数，单一数据源同读】`js/battle.js`：import 补 `BURN_PCT`（与 DOT_MIN 同读 data.js 单一数据源）；灼烧战报由 `note += '（灼烧）'` 改为 `note += `（灼烧 ${enemy.burn} 回合·每回合 -${Math.max(DOT_MIN, Math.round(enemy.hpMax * BURN_PCT))} HP）``——烧血数与 `enemyAI.js` 灼烧 tick 结算式逐字符同式（enemy.hpMax 战斗中不变，预估值与后续 tick 实扣逐值相等，非「约」）；回合数读赋值后的 `enemy.burn`——灼烧与中毒的关键差异是可叠加（`enemy.burn += skill.burn`，中毒是 `= POISON_TURNS` 重置），连放两发火焰斩叠到 4 回合时如实报 4，与角标「🔥 灼烧 N」同一份源。调灼烧强度只改 data.js 一处，战报/角标/tick 结算三端自动跟随，零裸字面量。纯显示零结算零数值零存档变化（`enemy.burn` 赋值与原行逐值同式，只改 1 条 blog 文案）。
- 【零回归面】未动 BURN_PCT/DOT_MIN/火焰斩 burn:2 任何数值；未动灼烧叠加赋值（`enemy.burn = (enemy.burn || 0) + skill.burn` 逐字保留）、enemyAI 灼烧 tick 结算与文案、drawBattle 灼烧角标、技能 hint「灼烧2回合·每回合约-4%最大HP」（敌人 hpMax 未知时只能给比例，「约」口径保留）、冻结/石甲碎裂/汲回/弱点/抗性 note 拼接、MP 读数（v20.7）与「敌方 HP 剩余」后缀（v20.0）任何逻辑。只改 4 个游戏/文档文件（`js/battle.js`：import + 灼烧 note 文案 + v21.50 注释；`js/data.js`：GAME_VERSION v21.49→v21.50 + 注释；`README.md`：tests 树收录 + 四十六件套（四十五件套清除）+ v21.50 守护描述；`CHANGELOG.md`：本条）+ `package.json`（test 串第 46 份）+ 新增 `tests/smoke_v2150_burnhint.mjs`（仓库常驻，承 v21.10-v21.49 冒烟入库先例）+ 顺带把 `tests/smoke_v2149_poisonhint.mjs` 的 README 件套断言按 v21.7 惯例去硬化（存活性口径，实件数由 v21.50 冒烟守护）。
- 验证：`node --check js/battle.js js/data.js tests/smoke_v2150_burnhint.mjs tests/smoke_v2149_poisonhint.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 四十六件套全绿（既有 45 件逐项零回归——含 smoke_v2149 去硬化后断言 + 新增 v21.50 **22 项断言全过**——GAME_VERSION 格式合法且越过 v21.49、data.js 含 v21.50 注释、battle.js BURN_PCT import 与新文案源级落位、旧裸文案「（灼烧）」零残留、叠加赋值逐字零回归、三端同式一致性（battle 战报/enemyAI 灼烧 tick 结算/drawBattle 角标同一公式）、技能 hint 与 burn:2 零回归、运行期实证（startBattle+playerAction 真实路径：hpMax=500 →「（灼烧 2 回合·每回合 -20 HP）」、enemy.burn===2 叠加语义零回归、耗蓝 4+MP 读数、hpMax=30 → DOT_MIN 下限钳制「（灼烧 2 回合·每回合 -2 HP）」且木桩存活、两档战报数值与 tick 同式逐值相等、连放两发叠加 →「（灼烧 4 回合·每回合 -20 HP）」且 enemy.burn===4、MP 连扣两次 60→52）、README/package.json 同步、smoke_v2149 件套断言去硬化确认）。
- 【另附：开工摸底】整目录基线备份 `/tmp/my-jrpg_backup_20260908_072321`（铁律：整目录 cp -R）；基线 `npm test` 四十五件套全绿后方开工；并发检查双测 mtime 无变化；`node` 直跑 balance_sim（开局生存率 普通带药 100%/普通裸刷 99.55%/困难带药 99.16%，新手保护健康、数值无需调整——故本轮选体验打磨而非平衡）。

## v21.49 中毒瞬间战报追加「每回合 -N HP」——施毒文案按 tick 同式补每回合扣血数，中毒链条四端全部量化（体验打磨·信息透明，承 v21.8 中毒 tick 带伤害值与剩余 HP / v20.0-v20.2 战斗数值反馈 / v19.94-96 信息透明同一主线：【中毒这一刻看不到每回合掉多少】`enemyAI.js` 施毒成功文案此前只报 `☠️ 中了【毒】！每回合扣血，持续 N 回合`——而中毒链条的另外三端早已量化：HUD 中毒角标（drawBattle「☠️ 中毒 N 回合 · 每回合 -N血」）、帮助页「毒蛇中毒」行（「约N%最大HP…持续N回合」，由 POISON_PCT/POISON_TURNS 派生）、中毒 tick 战报（v21.8「受到 N 点伤害！（剩余 N 回合）（我方 HP X/Y）」）。玩家中招瞬间想确认「这毒每回合到底掉多少、要不要立刻喝药/放治愈术」仍需等首次 tick 或翻帮助页——中招提示是中毒决策的第一现场，唯独它缺数）

- 【修正：施毒文案按 tick 同式补每回合扣血数，单一数据源同读】`js/enemyAI.js`：import 补 `POISON_PCT`（与 POISON_TURNS/DOT_MIN 同读 data.js 单一数据源）；施毒战报改为 `☠️ 中了【毒】！每回合 -N HP，持续 N 回合`（N = `Math.max(DOT_MIN, Math.round(hero.hpMax * POISON_PCT))`，与 `battle.js` `applyPoisonTick` 结算式逐字符同式——hero.hpMax 战斗中不变，预估值与后续 tick 实扣逐值相等，非「约」）；调中毒强度只改 data.js 一处，施毒文案/中毒角标/帮助页标注/tick 结算四端自动跟随，零裸字面量。纯显示零结算零数值零存档变化（只改 1 条 blog 文案）。
- 【零回归面】未动 POISON_PCT/POISON_TURNS/DOT_MIN/POISON_CHANCE 任何数值；未动施毒判定（`Math.random() < enemy.poison`）、中毒回合数赋值（hero.poison = POISON_TURNS）、applyPoisonTick 结算与文案、drawBattle 中毒角标、帮助页「毒蛇中毒/中毒自救」行、图鉴毒蛇 tag、灼烧/重击/反击/变身/石甲任何逻辑。只改 4 个游戏/文档文件（`js/enemyAI.js`：import + 1 条 blog 文案 + v21.49 注释；`js/data.js`：GAME_VERSION v21.48→v21.49 + 注释；`README.md`：tests 树收录 + 四十五件套（四十四件套清除）+ v21.49 守护描述；`CHANGELOG.md`：本条）+ `package.json`（test 串第 45 份）+ 新增 `tests/smoke_v2149_poisonhint.mjs`（仓库常驻，承 v21.10-v21.48 冒烟入库先例）+ 顺带把 `tests/smoke_v2148_drain.mjs` 的 README 件套断言按 v21.7 惯例去硬化（存活性口径，实件数由 v21.49 冒烟守护）。
- 验证：`node --check js/enemyAI.js js/data.js tests/smoke_v2149_poisonhint.mjs tests/smoke_v2148_drain.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 四十五件套全绿（既有 44 件逐项零回归——含 smoke_v2148 去硬化后断言 + 新增 v21.49 **18 项断言全过**——GAME_VERSION 格式合法且越过 v21.48、data.js 含 v21.49 注释、enemyAI.js POISON_PCT import 与新文案源级落位、旧文案「每回合扣血，持续」零残留、三端同式一致性（施毒文案/applyPoisonTick 结算/中毒角标同一公式）、帮助页「毒蛇中毒」行零回归、运行期实证（必中毒敌人 enemyAct 真实路径：hpMax=200 →「每回合 -10 HP」、hpMax=20 → DOT_MIN 下限钳制「每回合 -2 HP」、hero.poison=POISON_TURNS 与存活零回归、无 poison 字段敌人不施毒零回归、战报数值与 tick 同式逐值相等）、README/package.json 同步、smoke_v2148 件套断言去硬化确认）。
- 【另附：开工摸底】整目录基线备份 `/tmp/my-jrpg_backup_20260908_063633`（铁律：整目录 cp -R）；基线 `npm test` 四十四件套全绿后方开工；并发检查双测 mtime 无变化。

## v21.48 Lv9 新技能「汲光击」——全游戏首个汲回招：伤害×2.0 并汲回伤害 50% 为 HP（单次上限 25% 最大HP），光元素恒 ×1 中性；祸乱形态「封印治愈」对其同封；旧档读档按等级补学（新内容·战斗机制，承 v21.36 掌灯阿婆后首次内容轮换——v21.37-47 连续十一版文档/口径/入口类打磨，本版回到「新内容」层：技能领悟表自 v17.x 起停在 Lv7 陨石术，Lv8-12（正是残焰魔像/终焉之神/试炼的高压区间）再无新招；v17.7 MAX_LEARN_LV 收口注释早已明示「想新增 9 级技能时上界会悄然过期」——位置是预留好的，招一直缺着）

- 【新招定位：续航招，与治愈术错位】治愈术（Lv4，5MP 回 55%HP）是纯回血、让出伤害回合；汲光击（Lv9，9MP，×2.0 低于雷鸣 2.8/陨石 4.2）边打边回——命中后把伤害 ×DRAIN_PCT(50%) 汲回，单次上限 DRAIN_HP_CAP(25%)×hpMax（随最大 HP 缩放，后期不因攻击力膨胀失控：Lv12 满血 122 恒回 31）。光元素（ELEM_NAME 补「光」）不被任何魔物弱/抗引用、恒 ×1——不扰动既有克制表，弱冰的残焰魔像仍以冰霜击为最优解，汲光击占「重击高压战的续航」生态位而非伤害上位。
- 【不钻 Boss 机制空子：drain 招并入 heal 封印口径】终焉之神祸乱形态 forbid:['heal'] 是招牌机制（v21.x 「封印治愈技能·喝药不受影响」），若只封 kind==='heal'，汲回招将绕过封印成为独家续航漏洞——`battle.js` skillForbidden 与 `view/drawBattle.js` 技能菜单 ⛔封印 标注双端同步改为 `(skill.kind === 'heal' || skill.drain)` 同封，被封印时战报如实报「⛔ 祸乱气场封印了【汲光击】！」（MP 不扣、回合不耗，与治愈术被封同口径）。
- 【六招容量排版：技能菜单 36→34 / 状态页 16→14】技能至多 6 招后：drawSkillMenu 原 36px 行距下第 6 招提示行基线 356 与页脚「当前 MP」368 字面几乎相触（≈1px）——行距收紧为 ROW_SP=34（第 6 招提示行 346，页脚/按键行 368/394 逐字未动，光标底条留 2px 行隙）；drawStatus 原 16px 行距第 6 行基线 388 与「📖 下一技能」396 字顶相触——收紧为 14（第 6 行 378，留 6px 净隙，承 v2.8「状态页垂直空间占满需先压缩」惯例）。H 页「技能克制」行补「汲光回血」：单行追加 estW ≈520 超预算 470，按 v19.59/v21.11 先例拆 r[1]（机制枚举 ≈313）+ r[2]（弱点/抗性倍率规则 ≈150，ELEM_MULT 派生零裸字面量）——行数不变仍 10，本页 r[2] 数 2→3、末行基线 418→434 仍不触页脚 452。
- 【旧档兼容：migrateQuests 按等级补学】hero.checkSkills 只在升级瞬间触发、永不回头——v21.48 前已达 Lv≥9 的老存档会永远学不会汲光击；quests.js migrateQuests（读档/建档必经）新增「按等级补齐漏学技能」循环（与 seen/fragments 兜底同族），老档读档即补领、新档与升级路径幂等零变化；无 skills 字段的更老档兜底为 [learnsAt(1)]。MAX_LEARN_LV 由 LEARN_AT 派生自动升 9，skillXpHint「距下一技能」提示自动找到新招（v17.7 收口生效）。
- 【零回归面】未动既有五招任何字段（火焰斩/冰霜击/雷鸣/陨石术/治愈术逐字）；未动 checkSkills 领悟通路（learnsAt(level) 单招制自动覆盖 Lv9）；未动 elemMult/skillEstimate/skillDefUsed 公式（drain 字段不参与估算，预览 ≈N伤 与结算同式）；未动任何怪物/Boss/掉落/遇敌/经验/存档结构（skills 数组追加沿用既有快照持久化）；未动帮助页其余行/页数（仍 4 页）。`GAME_VERSION` v21.47→v21.48。只改 6 个游戏/文档文件（`js/data.js`：GAME_VERSION + v21.48 注释 + LEARN_AT 9 级 + DRAIN_PCT/DRAIN_HP_CAP 常量 + SKILL_DATA 汲光击 + ELEM_NAME 光 + HELP_PAGES 技能克制行拆 r[2] + export 块；`js/quests.js`：migrateQuests 补学 + learnsAt import；`js/battle.js`：skillForbidden drain 同封 + doSkill 汲回结算；`js/view/drawBattle.js`：ROW_SP=34 + banned 同口径；`js/view/menus.js`：状态页技能行距 14；`README.md`：战斗行/职业成长行/数值速查技能领悟行/tests 树/四十四件套（四十三件套清除）/v21.48 守护描述；`CHANGELOG.md`：本条）+ `package.json`（test 串第 44 份）+ 新增 `tests/smoke_v2148_drain.mjs`（仓库常驻，承 v21.10-v21.47 冒烟入库先例）+ 顺带把 `tests/smoke_v2115_elixir.mjs` 的 r[2] 计数断言随新现实更新（===2→===3，标签补技能克制行）+ `tests/smoke_v2147_heavywarn.mjs` 的 README 件套断言按 v21.7 惯例去硬化（实件数由 v21.48 冒烟守护）。
- 验证：`node --check`（改动文件全查）与 `npm run check`（25 模块）全部通过；`npm test` 四十四件套全绿（既有 43 件逐项零回归——含 smoke_v2115 更新后断言 + 新增 v21.48 **39 项断言全过**——GAME_VERSION 格式合法且越过 v21.47、数据层落位（learnsAt(9)/MAX_LEARN_LV=9/SKILL_DATA 字段/DRAIN 常量导出/hint 同源/ELEM_NAME 光/光元素对全 SPECIES 恒 ×1）、battle/drawBattle/menus/quests 源级落位与旧式零残留、H 页技能克制行拆分与宽度预算（r[1] ≈313/r[2] ≈150 ≤470）、末行基线 434 不触页脚 452、运行期实证（Lv10 旧档补学六招/Lv8 不越级/无 skills 字段兜底 [火焰斩]/汲回恒=31 上限钳制决定性/耗蓝 9+MP 读数/满血汲回 0 HP/封印同效 MP 敌血双不动/未领悟被拒/skillEstimate 预览正值/六招菜单渲染不抛错）、README/package 同步、smoke_v2115/2147 断言更新确认）。
- 【另附：开工摸底】检查时以 `node` 直跑 balance_sim（开局生存率 普通带药 100%/普通裸刷 99.61%/困难带药 99.14%，新手保护健康、数值无需调整——故本轮选新内容而非平衡）与既有 43 件套基线（全绿）；基线目录备份 `/tmp/my-jrpg_backup_20260908_053814`（铁律：整目录 cp -R）。

## v21.47 威胁预警补「重击线」——threatWarn 通用分支对持有 heavy 招的敌人按真实重击倍率再判一次两击线，残焰魔像/洞窟领主由「有些棘手」升级「明显强于你」（体验打磨·信息透明·预警名副其实，承 v19.13 威胁预警 / v20.9 残焰魔像战力校准 / v21.8-9 战报反馈同一主线：【预警只看普攻，对重击失灵】`battle.js` `threatWarn()` 通用分支的「两击即死」判定是 `enemyHit * 2 >= hero.hpMax`——enemyHit 按普攻（cmdDmg mult 1）估算；但 acts 表带 heavy 招的敌人（重击 ×HEAVY_MULT 1.9）：残焰魔像在 Lv10 推荐装备下一发重击 99 点（占满血 108 的 92%，v20.9 自己写进 data.js 的设计声明），普攻线 52×2=104 < 108 只报「⚠️ 此敌有些棘手」；洞窟领主重击 48×2=96 ≥ Lv7 满血 87 同样失灵——玩家按「有些棘手」的预期硬打，被一发重击送走时毫无预警）\n\n- 【修正：重击线与普攻线并行判定，与 enemyAI 结算同源】`js/battle.js`：import 补 HEAVY_MULT；threatWarn 通用分支在普攻两击线之后新增 `const hasHeavy = (enemy.acts || []).some((a) => a.type === 'heavy'); if (hasHeavy) { const heavyHit = cmdDmg(enemy.atk, hero.defMax, HEAVY_MULT, false); if (heavyHit * 2 >= hero.hpMax) threat = Math.max(threat, 2); }`——重击伤害与 enemyAI 未变身重击结算同读 cmdDmg×HEAVY_MULT 单一数据源（调 HEAVY_MULT 只改 data.js 一处，预警自动跟随）；文案沿用既有 threat 2「⚠️ 强敌：它明显强于你，小心应对！」（与普攻线同一条，零新文案族）。纯显示零结算：threatWarn 只影响进战 blog 首行文案，不读/写任何战斗数值。\n- 【零回归面】未动 HEAVY_MULT/HEAVY_MULT_PHASED/任何 acts 表/任何数值；未动 isTrue/isBoss/isRush 三分支固定文案（提前 return，不走通用分支）；未动普攻两击线/血线（enemyHit×2、hpMax>playerHit×5 逐字保留）；未动石心魔像口径（acts 仅 attack/shield 无 heavy——「石甲机制怪不该同时是纯数值最强」v3.26 原则的另一面：它的预警维持「有些棘手」）；未动 enemyAI/drawBattle/任何结算。只改 5 个游戏/文档/测试文件（`js/battle.js`：import HEAVY_MULT + threatWarn 重击线 + v21.47 注释；`js/data.js`：GAME_VERSION v21.46→v21.47 + 注释；`README.md`：tests 树收录 smoke_v2147_heavywarn + 冒烟四十三件套（四十二件套清除）+ v21.47 守护描述；`tests/smoke_v2146_trialrec.mjs`：README 件套断言按 v21.7 惯例去硬化——其写死的「四十二件套（四十一件套清除）」在本次件数递增时必然失败，改存活性口径，实件数由 v21.47 冒烟守护）+ `package.json`（test 串第 43 份）+ 新增 `tests/smoke_v2147_heavywarn.mjs`（仓库常驻，承 v21.10-v21.46 冒烟入库先例）+ `CHANGELOG.md`：本条。\n- 验证：`node --check js/battle.js js/data.js tests/smoke_v2146_trialrec.mjs tests/smoke_v2147_heavywarn.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 四十三件套全绿（既有 42 件逐项零回归 + 新增 v21.47 **24 项断言全过**——GAME_VERSION 格式合法且越过 v21.46、data.js/battle.js 含 v21.47 注释、battle.js HEAVY_MULT import 与 hasHeavy/重击两击线源级落位、普攻两击线零回归、数值复核（残焰魔像 Lv10 普攻线 52×2=104<108 失灵实证 + 重击 99 点占满血 92% 与 v20.9 口径逐值一致 + 洞窟领主 Lv7 普攻线 50<87 失灵 + 重击 48×2=96≥87 + 两敌持有 heavy 招数据面 + 石心魔像无 heavy 招）、运行期实证（DOM 桩 + main.js 真实导入 + startBattle 真实进战 blog：Lv10 残焰魔像→「明显强于你」、Lv7 洞窟领主→「明显强于你」、Lv3 石心魔像→仍「有些棘手」不升级、Lv1 史莱姆→无任何预警、幽冥魔王→「旧灯卫的影子」固定文案零回归、Lv12 残焰魔像→仍报「明显强于你」承伤视角口径一致）、README tests 树收录 smoke_v2147_heavywarn + 四十三件套（四十二件套清除）+ v21.47 守护描述、package.json 已收录（第 43 份）、smoke_v2146 README 件套断言已去硬化（存活性口径落位、旧精确表达式零残留））。\n- 【另附：开工摸底】选题前以两轮自建蒙特卡洛摸底（/tmp/jrpg_mid_sim.mjs，真实 encounter.randomEncounter + 同源 cmdDmg 镜像，2 万局×4 图）：开局 balance_sim 健康（普通带药 100%/困难带药 99.21%，与 v21.45 口径一致）；推荐等级+技能口径下 village/cave/gallery 连刷 10 场全 100% 生还，dungeon Lv3 硬刚石心魔像 14% 死亡（7% 稀有精英、有战前预警、可 60% 逃跑——设计闭环完整，非破缺）；唯一脱节信号是 threatWarn 普攻线对 heavy 招失灵（残焰魔像 99 点重击只报「有些棘手」）——即本版修正点。基线目录备份 /tmp/my-jrpg_backup_20260908_*（铁律：整目录 cp -R）。\n\n## v21.46 试炼推荐等级标注——RUSH_REC_LV = max(三关 SPECIES[].lv) 派生（8/7/12 → 12），守碑人台词/碑上标签/H 页「试炼三连战」行三端同读（体验打磨·信息透明·推荐等级名副其实，承 v19.56 地图 recLv / v19.52 试炼阵容预览 / v20.6「⚠Lv 推荐等级必须名副其实」同一「难度透明」主线：全游戏的每个挑战都有显式等级口径——四张图有 recLv（进图预警+快速旅行「推荐 Lv.X 起」）、祭坛有 ⚠Lv 标签（魔王8/领主7/神12）、图鉴有「Lv.X 起出没」、精英有门槛+概率、残焰魔像 v20.9 也按 Lv10-11 推荐装备校准——唯独**试炼场（星井矿脉试炼碑三连战，全游戏最重、唯一需主动踏入的长线考验）**没有任何「该练到多少级再来」的显式标注：碑上只有阵容（幽冥魔王Lv8→洞窟领主Lv7→终焉之神Lv12，v19.52）、守碑人只讲机制/恢复/赏金（v21.24）、H 页「试炼进阶」只有恢复/赏金/机制（v21.23/v21.25），玩家只能从阵容末位「终焉之神Lv12」自行推断，而 v20.6 自己的蒙特卡洛结论是 Lv11 约 42% / Lv12 约 100% 通关——「12」这个有效门槛游戏内从未写出来）\n\n- 【修正：推荐等级显式化——单一数据源 max 派生，三端同时落位】`js/data.js`：RUSH_BOSSES 之后新增 `const RUSH_REC_LV = Math.max(...RUSH_BOSSES.map(b => (SPECIES[b.name] && SPECIES[b.name].lv) || 1));`（与碑上阵容/祭坛 ⚠Lv/enemyLv 同读 SPECIES[].lv 单一数据源，当前 8/7/12 → 12；调任一 Boss 的 lv 只改 data.js 一处、三端自动跟随，零裸字面量）并加入 export 块。①守碑人第一页台词插入新段 `这碑的阵仗，是按 ${RUSH_REC_LV} 级刻的——没到 ${RUSH_REC_LV} 级，碑光可护不住你。`（其余台词/第二页/[Enter] 行逐字未动，trueBoss 后 after 彩蛋零回归）；②`view/drawWorld.js` 碑上标签 `ready ? \`⚔️ 试炼三连战 ${roster}\`` → 追加 ` · 建议Lv.${RUSH_REC_LV}`（v19.52 阵容/等级与 v19.60 通关奖标签逐字保留；未解锁态 `试炼·未解锁` 逐字未动，不提前剧透）；③HELP_PAGES[3]「试炼三连战」行 r[2] 末补 ` · 建议Lv.${RUSH_REC_LV}`（恢复/赏金公式派生逐字未动；r[2] 追加不增行——行数仍 9、r[2] 数仍 2、sp=34 档不变，12px 次行 estW ≈322 ≤470 面板预算；v21.11 口径）。纯显示/纯文案，零结算零数值零存档变化（推荐等级只读不改战斗/遇敌/掉落任何数值）。`GAME_VERSION` v21.45→v21.46。\n- 【零回归面】未动 RUSH_BOSSES/SPECIES[].lv/DIFF 任何数值（RUSH_REC_LV 只是既有数据的 max 派生）；未动 sentinelPages 其余台词与 after 彩蛋、未动 npcQuestPages/对话流程；未动试炼碑未解锁提示/踩碑判定/startRush/奖励结算；未动 HELP_PAGES 其余行与宽度预算（仅 r[2] 字符串追加）；未动 drawWorld 既有标签/渲染；未动任何战斗/掉落/任务/存档/遇敌/成就/数值。只改 4 个游戏/测试文件（`js/data.js`：RUSH_REC_LV + sentinelPages 新段 + H 页 r[2] + GAME_VERSION/注释 + export；`js/view/drawWorld.js`：import + 标签追加 + 注释；`tests/smoke_v2145_journalscroll.mjs`：README 件套断言按 v21.7 惯例去硬化——其写死的「四十一件套（四十件套清除）」在本次件数递增时必然失败，改存活性口径，实件数由 v21.46 冒烟守护）+ `package.json`（test 串第 42 份）+ 新增 `tests/smoke_v2146_trialrec.mjs`（仓库常驻，承 v21.10-v21.45 冒烟入库先例）+ `README.md`（tests 树收录 smoke_v2146_trialrec + 四十二件套（四十一件套清除）+ v21.46 守护描述 + 星井矿脉守碑人段/J 系统清单试炼碑条/数值速查新增「试炼推荐等级」行）+ `CHANGELOG.md`：本条。\n- 验证：`node --check js/data.js js/view/drawWorld.js tests/smoke_v2145_journalscroll.mjs tests/smoke_v2146_trialrec.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 四十二件套全绿（既有 41 件逐项零回归 + 新增 v21.46 **36 项断言全过**——GAME_VERSION 格式合法且越过 v21.45、data.js 含 v21.46 注释、`RUSH_REC_LV` 已导出且 ===12、与 max(各关 SPECIES[].lv) 逐值等价、三 Boss lv 8/7/12 真源健全、守碑人台词运行期（npcQuestPages 第一页含「按 12 级刻的」/「碑光可护不住你」，既有 三道刻痕/恢复/赏金/[Enter] 继续 零回归，第二页机制与 trueBoss after 彩蛋零回归且不含新段）、H 页 r[2] 含「建议Lv.12」且既有 回血%/赏金 派生零回归、r[2] estW ≈322 ≤470、试炼进阶页行数仍 9、r[2] 数仍 2、drawWorld.js import RUSH_REC_LV + `· 建议Lv.${RUSH_REC_LV}` 落位 + 旧无建议标签源级零残留、运行期实证（DOM 桩 + main.js 真实导入后 loadMap('cave')：双徽记 ready 态 drawWorld 碑上标签含 终焉之神Lv12/建议Lv.12 + 通关奖标签零回归、未解锁态只画「试炼·未解锁」零建议等级剧透）、README tests 树收录 smoke_v2146_trialrec + 四十二件套（四十一件套清除）+ v21.46 守护描述 + 数值速查「试炼推荐等级」行 + 系统清单「建议Lv.12」口径、package.json 已收录（第 42 份）、smoke_v2145 README 件套断言已去硬化（v21.7 惯例：存活性口径落位、旧精确表达式 `readme.includes('四十一件套（四十件套清除）')` 零残留））。\n- 【另附：开工摸底】检查时以 `node` 直跑 balance_sim（开局生存率 普通带药 100%/普通裸刷 99.61%/困难带药 99.12%，新手保护健康）复核既有数值无回归；基线目录备份 `/tmp/my-jrpg_backup_v2146_*`（铁律：整目录 cp -R）。\n\n## v21.45 任务日志（J）滚动——drawJournal 内容超可视区时 ↑↓ 可滚（state.js S.journalScroll 与 codexScroll/achScroll 同族），「可接」支线卡/记忆碎片不再被 clip 静默裁掉（体验打磨·信息透明·可达性，承 v21.37 图鉴「已遭遇 ✕N」/v21.32 面向提示同一「信息透明」主线：图鉴（codexScroll）与成就（achScroll）自 v19.x 起就有 ↑↓ 滚动，任务日志是全项目唯一没有滚动的多分区面板——主线/支线/记忆碎片三区同屏且是内容最长的面板，而它内部却用「画不下就不画」的两道提前截断：drawQuestCard 的 `if (y + h > yMax) return 0`（第一张装不下的卡起全部不画）与碎片区的 `if (y + 24 < yMax)` 门 + `if (y + 15 > yMax) break`（整段不画）——渲染捕获实证：**开局新档日志只画得出 2 张支线卡，星砂之约/旧灯卫的名字/石壳里的记忆/残焰的安息 四张（其中两张是「可接」委托！）连影子都没有**，玩家按 J 看不到部分可接任务，与「可接/进行中/可交付 一眼可见」的设计承诺相悖）\n
- 【修正：日志改为「全量条目 + 滚动偏移」绘制，删除两道提前截断】`js/view/menus.js`：drawQuestCard 去掉 `yMax` 参数与 `if (y + h > yMax) return 0;`（视口外的卡由外层 clip 裁掉，滚动后仍可见——提前截断会让滚动中的下方条目永远画不出来）；drawJournal 重构为：先按原布局规则排出全量条目高度序列（`items`：主线/支线 区头 + 卡 + 记忆碎片行，`cardH` 与原 h 分支逐值同源 done34/locked50/其余70），`totalH = Σh`，滚动偏移 `S.journalScroll`（state.js 注册，与 codexScroll/achScroll 同族）绘制期钳制到 `[0, totalH - viewH]`（viewH = yMax-top = 418-66 = 352），然后 `CTX.translate(0, -scroll)` 整页上移、`CTX.clip` 只裁视口外内容——所有条目（含此前被裁的 4 张支线卡与 4 行碎片）全部落画；页脚按 codex 同款口径补 `↑↓ 滚动浏览（还有 N 条）`（N = 视口底以下仍未滚到的卡+碎片行数，区头/间隔不计），无溢出时不显示该后缀（`remain>0` 条件，与 codex 的「还有 N 种」同构）。零行为变化面：卡片 h 分支/区头/碎片行内容/ellipsize 宽度/字体/坐标全部逐字沿用（仅 y 加一重 scroll 偏移），clip 矩形 (78,54,484,364) 逐字未动，页脚 y=432 未动。`js/main.js` journal.onKey 补 `else onArrow(e, journalScroll++/-- + SFX.select)`（codex/ach 同款；J/Esc 关闭、I 跳状态 两分支逐字未动；内容未超可视区时 scroll=0 按了也不越界——绘制期钳制兜底）。`js/state.js` 注册 `journalScroll: 0`。纯显示/纯入口，零结算零数值零存档变化。`GAME_VERSION` v21.44→v21.45。
- 【零回归面】未动 questJournal/questStatus/objectiveText/任务数据（QUESTS/FRAGMENTS）任何逻辑与文案；未动 drawQuestCard 卡面绘制（填充/描边/状态色/顶标/名称 pill/目标行/地点行/奖励行逐字）；未动 drawSectionHead/ellipsize；未动图鉴/成就/帮助页/状态页任何渲染与滚动（codexScroll/achScroll 逐字未动）；未动任何战斗/掉落/任务/存档/遇敌/数值；未动 README 上手表其余行/数值速查/H 页/进度徽记。只改 5 个游戏/测试文件（`js/view/menus.js`：drawJournal 重构 + drawQuestCard 去截断 + v21.45 注释；`js/state.js`：journalScroll 注册；`js/main.js`：journal.onKey 滚动分派 + v21.45 注释；`js/data.js`：GAME_VERSION + 注释；`tests/smoke_v2144_run.mjs`：README 件套断言按 v21.7 惯例去硬化——其写死的「四十件套（三十九件套清除）」在本次件数递增时必然失败，改存活性口径，实件数由 v21.45 冒烟守护）+ `package.json`（test 串第 41 份）+ 新增 `tests/smoke_v2145_journalscroll.mjs`（仓库常驻，承 v21.10-v21.44 冒烟入库先例）+ `README.md`（tests 树收录 smoke_v2145_journalscroll + 四十一件套（四十件套清除）+ v21.45 守护描述 + J 行「内容超出可视区时 ↑↓ 滚动浏览」 + 系统清单 J 任务日志条目补滚动口径）+ `CHANGELOG.md`：本条。
- 验证：`node --check js/view/menus.js js/state.js js/main.js js/data.js tests/smoke_v2144_run.mjs tests/smoke_v2145_journalscroll.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 四十一件套全绿（既有 40 件逐项零回归 + 新增 v21.45 **33 项断言全过**——GAME_VERSION 格式合法且越过 v21.44、data.js/state.js/main.js/menus.js 含 v21.45 注释、state.js `journalScroll: 0` 落位、menus.js `S.journalScroll`/`maxScroll` 钳制/`CTX.translate(0, -scroll)`/页脚「↑↓ 滚动浏览（还有 N 条）」源级落位、drawQuestCard 旧 `if (y + h > yMax) return 0;` 源级零残留、签名去 yMax、main.js journalScroll++/-- 分派落位、运行期实证（DOM 桩 + main.js 真实导入后开局新档 drawJournal 滚动=0：主线两支线/六支线全量落画——v21.45 前缺失的 星砂之约/旧灯卫的名字/石壳里的记忆/残焰的安息 全部画出 + 记忆碎片区 4 行 🕯️ 落画 + 页脚含「↑↓ 滚动浏览（还有」+ translate(0,-0) 零平移零回归 → scroll=5 时 translate(0,-5) 整页上移 → scroll=99999 被钳制到独立布局重算的 totalH-viewH（开局 328，布局数学逐值等价）→ journal.onKey（ArrowDown/s/ArrowUp 滚动 1/2/1、j/Escape 关闭回 world、i 跳状态页，J/Esc/I 零回归））、README tests 树收录 smoke_v2145_journalscroll + 四十一件套（四十件套清除）+ v21.45 守护描述 + J 行 ↑↓ 滚动口径、package.json 已收录（第 41 份）、smoke_v2144 README 件套断言已去硬化（存在性口径落位、旧精确表达式 `readme.includes('四十件套（三十九件套清除）')` 零残留））。
- 【另附：开工摸底】检查时以 `node` 直跑 balance_sim（开局生存率 普通带药 100%/普通裸刷 99.68%/困难带药 99.17%，新手保护健康）与真实 drawJournal 渲染捕获（CTX.fillText 桩）定位缺口；基线目录备份 `/tmp/my-jrpg_backup_v2145_*`（铁律：整目录 cp -R）。

## v21.44 按住 Shift 奔跑——world.js 新增 RUN_MS=115（约 1.57×），main.js keydown/keyup/blur 三处 setRun 分派，H 页/教程/README 同步口径（体验打磨·操作手感，承 v21.3 战斗开场警报音「感官反馈」之后的常规轮换——v21.38-43 连续六个版本都是文档/口径/入口类打磨，本次轮换回「操作手感」层：四张地图 20×14 起、矿脉/回廊走廊密集，WALK_MS=180 是单文件时代沿用至今的唯一移动节拍——著名 JRPG 的移动加速（Shift/奔跑）在 14 张图遍历 + 扫箱/刷怪动线里是常规诉求，而本作从未有过任何加速入口：README/H 页/教程只写「WASD 按住连走」，玩家横穿星井矿脉约 55+ 步（≈10 秒）只能一步一动画地走——加速对「跑图体验」是结构性的缺一格）

- 【加速入口缺失：移动节拍只有一档】`world.js` `move()` 的行走动画长度写死 `dur: WALK_MS`（180ms/格，约 5.5 格/秒），`main.js` keydown/keyup 只维护 `heldDirs`（按住连走）——没有任何「更快移动」的受控入口（v19.47 小地图/快旅等外部入口都齐了，唯独「自己走」这件事的速率不能调）；与「蓄力 ×1.5」「奔跑」同为 JRPG 常规操作红利，后者始终缺席（承 v21.3「二十余版把文本/数值反馈做满后首次碰嗅觉/听觉」同一「感官/手感层从未被覆盖」的轮换逻辑）。
- 【修正：按住 Shift 奔跑——dur 改由 runHeld 派生，零新通道】`js/world.js`：`const RUN_MS = 115;`（与 WALK_MS 同文件一族，约 1.57×）+ `let runHeld` + `export function setRun(on)`；`move()` 唯一改动 `dur: runHeld ? RUN_MS : WALK_MS`——行走动画/插值（drawWorld.heroDrawPos 读 `S.walk.dur`）/预输入 walkBuf/传送/踩踏/遇敌槽全走既有 move→S.walk 通道，每步仍按 `dangerAt` 结算遇敌槽（加速只快「走」、不跳「结算」：跑图 ≠ 跳过遭遇，只是更快撞到；非危险格降温/喷泉 -25 分支逐字未动）；`SFX.step` 节拍随走速自然变快（脚步更快是奔跑的正常听感，无新音效）。`js/main.js`：keydown 在 screen 分派之前 `if (e.key === 'Shift') { setRun(true); return; }`（与 M 静音 handler 同位；Shift 是修饰键、KEY 无映射、无场景消费，早退零行为影响）；keyup 在 KEY 检查之前 `if (e.key === 'Shift') { setRun(false); return; }`；另补 `window.addEventListener('blur', () => setRun(false))`（Alt-Tab 失焦时 keyup 可能不派发——与 heldDirs「松开即移除」同一防粘滞口径）。教程行（main.js）`WASD移动` → `WASD移动/Shift奔跑`（v21.30 教程口径同族）；H 页「移动 / 传送门」行补 `按住 Shift 奔跑(更快)`（行宽估算 ≈448.5 ≤470 面板预算，行数不变仍 14，v21.11 estW 口径）；README 快速上手表 W 行补「按住 `Shift` 奔跑——更快，v21.44」（与 H 页/教程三处同口径）。
- 【零回归面】未动 `WALK_MS` 值/`heldDirs`/`holdStep`/`setHeldDir`/`walkBuf` 预输入/`onStep`/`tickEncounter`/`dangerAt`/`useGate`/`useExit`/`transition` 任何逻辑（仅 `move()` 的 dur 一处、`main.js` 两处 Shift 分支 + blur + import + 教程行文案、`data.js` 版本注释与 H 页行）；未动 KEY 表（无 Shift 映射）；未动战斗/遇敌/掉落/任务/存档/图鉴/成就/帮助页其余行/数值速查任何数值；`GAME_VERSION` v21.43→v21.44。只改 5 个游戏/文档文件（`js/world.js`：RUN_MS + setRun + move dur + export；`js/main.js`：import + keydown/keyup/blur 三分派 + 教程行；`js/data.js`：GAME_VERSION + 版本注释 + H 页移动行；`README.md`：上手表 W 行 + tests 树 + 四十件套（三十九件套清除）+ v21.44 守护描述；`CHANGELOG.md`：本条）+ `package.json`（test 串第 40 份）+ 新增 `tests/smoke_v2144_run.mjs`（仓库常驻，承 v21.10-v21.43 冒烟入库先例）+ 顺带把 `tests/smoke_v2143_talkekey.mjs` 的 README 件套断言按 v21.7 惯例去硬化（其写死的「三十九件套/三十八件套清除」在本次件数递增时必然失败；改存活性口径，实件数由 v21.44 冒烟守护）+ 顺带把 `tests/smoke_v2130_tutor.mjs` 的教程行全字面量随 v21.44 同步（其 `TUTOR_LINE` 断言写死 `WASD移动 · Enter/E对话` 旧整行，教程行合法插入 Shift奔跑 后必然失败；改新整行、E 键口径与 TUTOR_MSG_MS 收尾断言对象不变，旧「Enter对话」清零断言原样）+ 顺带把 `tests/smoke_v2137_seencount.mjs` 的 README 件套断言前缀去硬化（其 `readme.includes('冒烟三十')` 在件数升到四十后必然失败——v21.38 去硬化时漏网的前缀字面量；改 `冒烟`/`件套清除）` 存在性口径，实件数由最新版冒烟守护）。
- 验证：`node --check js/world.js js/main.js js/data.js tests/smoke_v2130_tutor.mjs tests/smoke_v2137_seencount.mjs tests/smoke_v2143_talkekey.mjs tests/smoke_v2144_run.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 四十件套全绿（既有 39 件逐项零回归 + 新增 v21.44 **31 项断言全过**——GAME_VERSION 格式合法且越过 v21.43、data.js/world.js/main.js 含 v21.44 注释、world.js `const RUN_MS = 115;`/`runHeld`/`export function setRun` 落位、`dur: runHeld ? RUN_MS : WALK_MS` 全文件唯一、旧 `dur: WALK_MS` 源级零残留、export 块导出 RUN_MS、RUN_MS(115)<WALK_MS(180)、main.js import setRun + keydown `Shift → setRun(true)` 全文件唯一 + keyup `Shift → setRun(false)` 先于 KEY 检查 + blur `setRun(false)` 兜底、教程行 `WASD移动/Shift奔跑`、KEY 无 Shift 映射、运行期实证（DOM 桩 + main.js 真实导入后启动引导 S.G 已建档/场景 title/loadMap(village) 就绪 → 世界场景：默认 runHeld=false 首步 dur===WALK_MS 零回归 → setRun(true) 后 上/下/左/右 四向 `S.walk.dur` 均 ===RUN_MS → 奔跑态 `walking()` 立即 true（Date.now()-t0 < RUN_MS 消费 dur 零回归）→ setRun(false) 回落 WALK_MS → 奔跑踩 (15,15) 危险格 'G' 遇敌槽仍按 dangerAt 递增（10-18）→ 奔跑踩 (10,14) 路径格走「降温」分支 encGauge 保持 0 非负——加速只快「走」不跳「结算」）、H 页「移动 / 传送门」行含 Shift+奔跑且行宽 ≈448.5 ≤470、操作说明页行数仍 14、README tests 树收录 smoke_v2144_run + 四十件套（三十九件套清除）+ v21.44 守护描述 + 上手表 W 行 `Shift` 奔跑、package.json 已收录（第 40 份）、smoke_v2143 README 件套断言已去硬化（改存活性口径 `!readme.includes('三十八件套')`、旧精确表达式零残留））。



- 【写进文档的键按了没反应：对话进行中 E 无任何分派】`main.js` `screens.talk.onKey` 只有 `Enter → talkNext()` 与 `Esc → backWorld()` 两行——v21.29 的世界交互 E 别名只管「发起对话」（world.interact），对话屏幕本身是另一个场景：README/H 页/教程都写着「Enter / E 对话/确认」，玩家在对话中按 e/E（方向键旁最常见的确认键，v21.29 选它的初衷就是「JRPG 确认在 E」的肌肉记忆）却落到 talk.onKey 末尾的静默分支——与 v21.33 标题页 L 空槽同一类「提示引导的键无反应」；观感上 E 只对「进入对话」有效、「推进对话」无效，同一组键在对话流程前后语义割裂（H 页「对话 / 确认」行/README 上手表「对话 / 确认 / 进入界面」的字面承诺与真实分派对不上——「确认」恰恰发生在对话内）。
- 【修正：e/E 与 Enter 完全同路径调用 talkNext，三种状态逐字同行为】`main.js` talk.onKey 首行改 `if (e.key === 'Enter' || e.key === 'e' || e.key === 'E') talkNext();`——talkNext（core.js，逐字未动）对三种状态的处理与 Enter 完全相同：本页打字机未打完（`Date.now()-talkLineAt < pageTotalMs(page)`）→ 只补全本页不翻页；打完 → `S.talkPage++` 翻页；末页翻完 → `goto('world')` 结束对话。`isEsc(e) → backWorld()` 分支逐字未动；KEY 无 'e' 映射（不与移动键冲突，v21.29 守护口径不变）；world.onKey 的 E 交互分派与 talkNext/openTalk/npcQuestPages 逐字未动。纯入口零结算：不读/写任何状态与数值、零对话数据变化、零渲染变化（对话内本无「按 Enter」字样提示，翻页指示 ▼ 与打字机节奏与键位无关，drawTalk 无需改）。
- 【零回归面】未动 core.js talkNext/openTalk/npcQuestPages 逻辑与对话数据（NPCS/QUESTS/STORY）；未动 world.js interact 与 v21.29 E 分派；未动 HELP_PAGES 任何行（「对话 / 确认」行 v21.29 口径逐字保留——本就为双键，无需改动）；未动 draw* 渲染与 README 其余任何行（仅 Enter/E 行补一句）；`GAME_VERSION` v21.42→v21.43。只改 3 个游戏/测试文件（`js/main.js`：talk.onKey 首行 + v21.43 注释；`js/data.js`：GAME_VERSION + 注释；`tests/smoke_v2142_createesc.mjs`：README 件套断言按 v21.7 惯例去硬化——其写死的「三十八件套/三十七件套清除」在本次件数递增时必然失败，改「冒烟/件套」存在性口径，实件数由 v21.43 冒烟守护）+ `package.json`（test 串第 39 份）+ 新增 `tests/smoke_v2143_talkekey.mjs`（仓库常驻，承 v21.10-v21.42 冒烟入库先例）+ `README.md`（tests 树收录 smoke_v2143_talkekey + 冒烟三十九件套（三十八件套清除）+ v21.43 守护描述 + 快速上手表 Enter/E 行补「对话进行中 `E` 同效继续/翻页——v21.43」）+ `CHANGELOG.md`：本条。
- 验证：`node --check js/main.js js/data.js tests/smoke_v2142_createesc.mjs tests/smoke_v2143_talkekey.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 三十九件套全绿（既有 38 件逐项零回归 + 新增 v21.43 **24 项断言全过**——GAME_VERSION 格式合法且越过 v21.42、data.js/main.js 含 v21.43 注释、main.js `if (e.key === 'Enter' || e.key === 'e' || e.key === 'E') talkNext();` 全文件唯一落位、旧单 Enter 行源级零残留、isEsc→backWorld 保留、KEY 无 'e'/'E' 映射、运行期实证：DOM 桩 + main.js 真实导入后 talk 场景（S.talkPages 两页 + curNpc granny 无委托 → resolveNpcTalk 返回 null 走翻页路径）`onKey('e')` 完成页→翻第 2 页、`onKey('E')` 末页→结束对话回 world、打字机未打完按 'e' 只补全本页（talkPage 不变 + talkLineAt 推进到 pageTotalMs 完成态同源实证）、Enter 完成页翻页零回归、Escape/Esc 关闭回 world 零回归、world.onKey('e')/('Enter') 交互 boxMsg「（这里没什么特别的）」零回归（v21.29 口径）、H 页「对话 / 确认」行 `Enter / E（镇民需面对面）` 零回归、README tests 树收录 smoke_v2143_talkekey + 三十九件套（三十八件套清除）+ 「对话翻页 E 键别名守护」守护描述 + 快速上手表「对话进行中 E 同效继续/翻页」、package.json 已收录（第 39 份）、smoke_v2142 README 件套断言已去硬化（存在性口径落位、旧精确表达式零残留））。

## v21.42 创建角色页补 Esc 返回标题——create.onKey 补 `isEsc(e) → goto('title')`，drawCreate 提示行补「Esc 返回」（体验打磨·反馈·Esc 语义一致，承 v21.16「标题页每个按键都该有反应」/v21.33 标题 L 空槽反馈/v21.29-41 入口口径主线：screens 场景表里 create 是唯一没有 Esc 处理的场景——shop/inn/brew/status/journal/codex/ach/help/travel/talk/pause/world/battle 全部有 Esc 语义（返回/关闭），唯独 title 与 create 没有；title 是根场景（无返回需求，Esc 静默合理），create 却是「有上层的叶子场景」：标题页按 Enter 即进入创建页，误按一下就被困住——只能 Enter 出发开始新档（毁掉当前冒险的入口），或先进游戏再经 Esc 菜单「返回标题」绕一圈回来。Esc 在所有其他界面都是「取消/返回」，唯独创建页按下毫无反应——正是 v21.16/v21.33 反复修过的那类「按键无反应/入口看不见」）

- 【入口缺口：创建页无退路】`screens.create.onKey`（main.js）只处理 ArrowRight/ArrowLeft → 姓名、ArrowUp/ArrowDown → 难度、Enter → beginAdventure，Esc 在 create 场景直接落到 `screens[S.scene].onKey(e)` 末尾的静默分支——而 README 快速上手表标题行、H 页「标题页…Enter 开始新冒险」都引导玩家从标题按 Enter 进入创建页（v21.38 刚补齐的 ←/→ 选槽同页），入口文档写满了「怎么进」，出口只字未提；玩家（尤其第一次玩、误按 Enter）在创建页按 Esc 毫无反应——与其他界面「Esc=取消」的肌肉记忆冲突（v19.86 读档摘要/v21.33 同族的「提示与实际分派对不上」的近亲：这次是「界面惯例不存在」）。
- 【修正：补 Esc 返回 + 提示行如实标注】`main.js` create.onKey 首行加 `if (isEsc(e)) { goto('title'); return; }`——与 dead.onKey T 回标题同构（goto('title') 单步）；不重启 BGM：title→create 从不 stopBgm，title 主题曲自进入创建页起仍在播，goto('title') 无缝返回（无需 startBgm 重启打断旋律）。`S.createName`/`S.createDiff` 保留——返回标题后再 Enter 进创建页仍是刚才的选择（纯会话状态，不建档、不进存档）。`view/menus.js` drawCreate 提示行 `'← → 选择姓名     ↑ ↓ 选择难度    Enter 出发！'` → 末补 `    Esc 返回`（13px 提示行同 y=432 单行，行宽 ~55 字符 < 640 画布，与既有提示行同字号同基线；「功能存在就必须能看到入口」v21.14/v21.18/v21.29 主线——出口和入口一样要写进提示行）。README 快速上手表在「标题 `1/2/3`」行后补创建页行（`创建页 ←→/↑↓` 选择姓名/难度 · `Enter` 出发 · `Esc` 返回标题，v21.42 标注「误入可退，不会建档；返回后姓名/难度选择保留」）。
- 【零回归面】未动 title.onKey 任何分派（Enter 进创建页原样）；未动 beginAdventure/newGame/建档/难度倍率任何逻辑与数值；未动 drawCreate 姓名/难度/成长标注任何行（仅末尾提示行追加）；未动存档/读取/存档槽；`GAME_VERSION` v21.41→v21.42。只改 5 个游戏/文档文件（`js/main.js`：create.onKey 首行 Esc 分支 + v21.42 注释；`js/view/menus.js`：drawCreate 提示行 + 注释；`js/data.js`：GAME_VERSION + 注释；`README.md`：tests 树/快速上手表创建页行/三十八件套（三十七件套清除）/v21.42 守护描述；`CHANGELOG.md`：本条）+ `package.json`（test 串第 38 份）+ 新增 `tests/smoke_v2142_createesc.mjs`（仓库常驻，承 v21.10-v21.41 冒烟入库先例）+ 顺带把 `tests/smoke_v2141_deadhint.mjs` 的 README 件套断言按 v21.7 惯例去硬化（其写死的「三十七件套/三十六件套清除」在本次件数递增时必然失败；改存活性口径，实件数由 v21.42 冒烟守护）。
- 验证：`node --check js/main.js js/view/menus.js js/data.js tests/smoke_v2141_deadhint.mjs tests/smoke_v2142_createesc.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 三十八件套全绿（既有 37 件逐项零回归 + 新增 v21.42 **26 项断言全过**——GAME_VERSION 格式合法且越过 v21.41、data.js/main.js/menus.js 含 v21.42 注释、main.js `if (isEsc(e)) { goto('title'); return; }` 全文件唯一落位、既有个键分支源级原样（姓名/难度/beginAdventure）、menus.js 新提示行落位 + 旧提示行 `Enter 出发！',CV.width/2,432)` 源级零残留、README tests 树收录 smoke_v2142_createesc + 三十八件套（三十七件套清除）+ v21.42 守护描述 + 快速上手表创建页行（含「Esc 返回标题」）、package.json 已收录（第 38 份）、运行期实证：DOM 桩 + main.js 真实导入后初始 title→title.onKey Enter→create→create.onKey Escape→title→（Esc 别名键同）→再 Enter→create→ArrowRight/ArrowDown/d/s 选名选难零回归→Esc 返回后 createName/createDiff 保留→drawCreate 渲染含「Esc 返回」且旧整行零渲染→world Esc→pause、pause Esc→world 零回归、smoke_v2141 README 件套断言已去硬化）。

## v21.41 战败画面图鉴入口口径修正——drawDead 普通战败提示「用记忆图鉴(B)」改为「再战前先用记忆图鉴看清魔物强度（图鉴在世界画面按 B 打开）」（体验打磨·口径一致·按键与提示对号，承 v21.29-32/v21.38-40「同一功能所有入口口径一致」主线：战败画面普通怪分支的提示告诉玩家「用记忆图鉴(B)查看魔物强度」——但 dead 画面（main.js `dead.onKey`）的 B 从初始提交起就绑定 `retryBoss`：强敌战败=重整旗鼓、普通战败/试炼 rush=无反应（`retryBoss` 无 `_bossRetry` 或 `bossId==='rush'` 时直接 `return false`），记忆图鉴入口只在世界画面（`world.onKey` 的 B → codex）；且 `codex.onKey` 关闭走 `backWorld()`——从 dead 场景打开图鉴再关会错回 world，故正确修法是如实标注入口而非把 B 绑成图鉴。玩家按提示按 B：普通战败时屏幕毫无反应（与 v21.16/v21.33「每个按键都该有反应」同族的「提示引导的键无反应」）——正是 v21.30/v21.38/v21.39 反复修过的那类「提示口径与实际分派对不上」）

- 【提示与分派对不上：dead 画面没有图鉴入口，B 是 retryBoss】`drawDead`（menus.js）普通战败分支 y=292 的 💡 建议行写「用记忆图鉴(B)查看魔物强度后再战」——武断地把 B 标注为图鉴键，而该场景 B 的真实语义是「强敌战败后重整旗鼓（retryBoss）」、普通战败时按下毫无反应；图鉴（B）只在世界场景有效。同一屏幕下方 y=392 才在强敌战败时补「按 B 重整旗鼓，再战强敌！」——普通战败分支却提前把 B 指给了图鉴，玩家按提示操作一次即「键无反应」的困惑（与 v19.86 读档摘要/v21.15 药水配方同族的口径漂移）。
- 【修正：提示如实标注「图鉴在世界画面按 B 打开」，分派零改动】menus.js 普通战败分支改 `'💡 建议：回村 旅馆/喷泉 补给，再战前先用记忆图鉴看清魔物强度（图鉴在世界画面按 B 打开）。'`——图鉴是记忆图鉴、入口如实注明在世界画面（与 world.onKey B→codex 逐字同源），不再让玩家在 dead 屏幕按 B；Boss 战败分支（「再按 B 重整旗鼓挑战{强敌}！」）、R/T 两行、余粮行、败于/等级/金币/讨伐/时长行逐字未动；main.js dead.onKey / world.onKey / codex.onKey 三个分派逐字未动（不绑 B→图鉴：codex 关闭走 backWorld，从 dead 进会错回 world，这正是本次「只改文字」的设计依据）。纯文字、零逻辑、零结算。
- 【零回归面】未动任何战斗/掉落/任务/存档/遇敌/数值；未动 main.js 任何分派；未动 drawDead 其余行与 Boss 分支；未动 help 页/README 快速上手表（B 行本就标注在世界语境「记忆图鉴」，阵亡 B/R/T 行本就写「强敌战败后 B 重整旗鼓」——如实未改）；`GAME_VERSION` v21.40→v21.41。只改 3 个游戏/测试文件（`js/view/menus.js`：普通战败提示 + v21.41 注释；`js/data.js`：GAME_VERSION + 注释；`tests/smoke_v2140_norecover.mjs`：README 件套断言去硬化）+ 新增 `tests/smoke_v2141_deadhint.mjs`（仓库常驻，承 v21.10-v21.40 冒烟入库先例）+ `package.json`（test 串第 37 份）+ `README.md`（tests 树/系统清单战败复盘口径/三十七件套（三十六件套清除）/v21.41 守护描述）+ `CHANGELOG.md`：本条。
- 验证：`node --check js/view/menus.js js/data.js tests/smoke_v2140_norecover.mjs tests/smoke_v2141_deadhint.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 三十七件套全绿（既有 36 件逐项零回归 + 新增 v21.41 **26 项断言全过**——GAME_VERSION 格式合法且越过 v21.40、data.js/menus.js 含 v21.41 注释、menus.js 源级新提示落位（「再战前先用记忆图鉴看清魔物强度（图鉴在世界画面按 B 打开）」）、旧口径「用记忆图鉴(B)」源级零残留、Boss 分支「再按 B 重整旗鼓」逐字零回归、main.js 分派事实源级落位（dead.onKey B=retryBoss / world.onKey B=codex / codex.onKey 关闭=backWorld——证明「只改文字」的设计依据）、README 同步（tests 树收录 smoke_v2141_deadhint + 三十七件套（三十六件套清除）+ v21.41 守护描述 + 战败复盘「看清魔物强度（图鉴入口在世界画面按 B 打开）」 + 阵亡行「强敌战败后 B 重整旗鼓」保留）、package.json 已收录（第 37 份）、运行期实证：DOM 桩 + main.js 真实导入后 drawDead 普通战败（无 _bossRetry）渲染新提示且旧口径/「按 B 重整旗鼓」零渲染、Boss 战败（_bossRetry main/幽冥魔王）渲染「再按 B 重整旗鼓挑战幽冥魔王！」且无普通分支提示（if/else 互斥）、试炼 rush（bossId='rush'）走普通分支提示（与 retryBoss 对 rush 早退同口径）、screens.dead.onKey 按 b/B（普通战败）场景不变（retryBoss 空 _bossRetry 早退实证）、smoke_v2140 件套断言已去硬化（v21.7 惯例：改用 readme.includes('冒烟')&&includes('件套')、旧精确表达式零残留））。

## v21.40 无泉水/旅店图进图补给提醒——world.transition 对无免费恢复点的图补「没有泉水/旅店 · 出发前请补给」（体验打磨·信息透明·入口决策点，承 v21.17 老矿工「矿脉没有泉水/旅店」/v21.29-39 同一「功能/事实存在就必须在决策点可见」主线：全图唯二没有泉水/旅店的图是 星井矿脉 与 无字回廊（潮灯镇 rows 含 F/I、雾语林 extras 营地泉 12,9；矿脉/回廊既无 F/I 瓦片也无 FOUNTAIN/INN），而「这图没有免费恢复点」这一高代价事实此前只活在两处——老矿工 NPC 台词（矿脉入口，要对话才知道）与 v21.17/v21.36 的叙述；玩家在 快速旅行/传送门/出口/水晶开门 四条入口切进矿脉或回廊的瞬间，屏幕上只有「推荐 Lv.X 起 · 高难」级别的定性提示——药水带少了才进矿、半路被堵在深处的教训，只能靠死一次自己学（与 v19.56「低等级冒进强区」同款「入口处就该知道」的结构性空白；v19.56 给了等级预警，唯独「无补给」这一格从没进过入口提示）

- 【入口无补给提示：四入口都走同一函数，判定必须数据化】`world.transition` 是 传送门 useGate/useExit（world.js v：179）、快速旅行 doTravel（core.js）、终焉水晶开门（world.js 297）四条入口的共同必经点；在它里面按「该图有无泉水/旅店」追加第二句 boxMsg 即可统一生效。判定不硬编码图名——data.js 新增纯函数 `hasRecoveryPoint(def)`：rows 含 'F'(喷泉)/'I'(旅店) 瓦片或 extras 含 FOUNTAIN/INN 即 true（与 world.placeExtras/chToTy 同一份 MAPS 真源，零裸字面量）；当前 cave/gallery 恒 false，以后给某图加泉水只改 MAPS 一处、提醒自动消失。
- 【修正：transition 在等级预警/进入提示之后追加无补给提醒】`world.js` transition() 末尾：`if (!hasRecoveryPoint(MAPS[name])) bind.boxMsg(`⚠️ ${MAPS[name].name}没有泉水/旅店 · 出发前请补给！`, NARR_MSG_MS)`——等级达标走「进入了【星井矿脉】」、等级不足走「⚠️ 前方【星井矿脉】的魔物远强于你…」两分支之后都会追加（v21.10 消息队列保证两条都逐条显示）；文案与 v21.17 老矿工「矿脉没有泉水/旅店」逐字同源头（map 名读 MAPS[].name 单一数据源）；纯显示零结算：不读 hero 补给量、不动遇敌/陷阱/任何数值，仅多一句进图消息。
- 【零回归面】未动 loadMap/useGate/useExit/doTravel/水晶开门任何传送逻辑与目标；未动遇敌/掉落/任务/存档/数值（transition 的等级预警 if/else 逐字未动，仅其后追加纯显示分支）；未动任何界面排版与帮助页；`GAME_VERSION` v21.39→v21.40。只改 4 个游戏/测试文件（`js/data.js`：GAME_VERSION + hasRecoveryPoint 纯函数；`js/world.js`：import + transition 追加分支；`tests/smoke_v2139_helpkey.mjs`：README 件套断言去硬化；`README.md`：T 行/系统清单/tests 树/三十六件套/v21.40 守护描述；`package.json`：test 串第 36 份）+ 新增 `tests/smoke_v2140_norecover.mjs`（仓库常驻，承 v21.10-v21.39 冒烟入库先例）+ `CHANGELOG.md`：本条。
- 验证：`node --check js/data.js js/world.js tests/smoke_v2139_helpkey.mjs tests/smoke_v2140_norecover.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 三十六件套全绿（既有 35 件逐项零回归 + 新增 v21.40 **28 项断言全过**——GAME_VERSION 格式合法且越过 v21.39、data.js 含 v21.40 注释、hasRecoveryPoint 已导出、四图逐值（village/dungeon true、cave/gallery false）、null 兜底、数据面 cave/gallery rows 无 F/I 瓦片与 extras 无 FOUNTAIN/INN（false 是地图事实而非函数事故）、world.js import 落位 + `if (!hasRecoveryPoint(MAPS[name]))` 判定 + 文案落位 + v21.40 注释、README T 行含「无泉水/旅店」与「出发前请补给」+ 系统清单同步 + tests 树收录 smoke_v2140_norecover + 三十六件套（三十五件套清除）+ v21.40 守护描述、package.json 已收录（第 36 份）、运行期实证（DOM 桩 + main.js 真实导入后 bind.boxMsg 捕获：等级达标进 cave 首句「进入了【星井矿脉】」+ 次句无补给提醒、进 gallery 同、低等级进 cave 首句「魔物远强于你」预警零回归 + 无补给提醒仍紧随、进 village/dungeon 只有进入句且零无补给提醒不误报）、smoke_v2139 件套断言已去硬化（v21.7 惯例））。

## v21.39 帮助页翻页入口 A/D 口径——drawHelp 页脚与 README H 行补 A/D 别名（体验打磨·可发现性·口径收尾，承 v21.29-32 E 键口径 / v21.38 README 标题行「A/D 亦可」同一「同一功能所有入口口径一致」主线：main.js help.onKey 的翻页分派自始就是双入口——ArrowRight/ArrowLeft 与 d/D/a/A（与标题页选槽 v21.4 A/D 别名同族），玩家按 README/H 页用 A/D 翻页时却见不到任何入口说明——H 页页脚（drawHelp「← → 翻页」）与 README 快速上手表 H 行（「←→ 翻页，含地图指南」）是操作清单里最后两处没点明 A/D 的翻页口径）

- 【漏网：页脚与 H 行都只认 ←/→】`view/menus.js` drawHelp 页脚 `第 X/4 页 · ← → 翻页 · H/Esc 关闭` 与 `README.md` 快速上手表 H 行 `操作说明（←→ 翻页，含地图指南）` 都只写方向键——而 `main.js` help.onKey 真实分派是 `ArrowRight/d/D → 下一页`、`ArrowLeft/a/A → 上一页`（与标题页选槽 A/D 别名、v21.29 E 键别名同族）；按「功能存在就必须能看到入口」（v21.14/v21.18/v21.29）与 v21.38「A/D 亦可」如实口径，翻页入口与实际按键对不上号——玩家用 A/D 翻页靠的是猜，而非文档。
- 【修正：页脚补「(A/D亦可)」+ README H 行补「A/D 亦可」，零逻辑零结算】`js/view/menus.js` 页脚改 `第 ${S.helpPage+1}/${HELP_PAGES.length} 页 · ←/→ 翻页(A/D亦可) · H/Esc 关闭`（其余页脚内容与位置逐字未动，12px 居中不越面板）；`README.md` H 行改 `操作说明（←/→ 翻页，A/D 亦可；含地图指南）`。`main.js` 翻页分派（ArrowRight/ArrowLeft/d/D/a/A）逐字未动，纯文字口径、零行为变化。
- 【零回归面】未动 main.js 任何分派、未动 HELP_PAGES 任何行（行数仍 4 页 14/5/10/9）、未动 drawHelp 行渲染/页长自适应/标题、未动任何战斗/掉落/任务/存档/遇敌/数值；`GAME_VERSION` v21.38→v21.39。只改 4 个文件（`js/view/menus.js`：页脚 + 注释；`js/data.js`：GAME_VERSION + 注释；`README.md`：H 行 + tests 树 + 「冒烟三十五件套（三十四件套清除）」+ v21.39 守护描述；`package.json`：test 串第 35 份）+ 新增 `tests/smoke_v2139_helpkey.mjs`（仓库常驻，承 v21.10-v21.38 冒烟入库先例）+ 顺带把 `tests/smoke_v2138_titleslotkey.mjs` 的 README 件套断言按 v21.7 惯例去硬化（其写死的「三十四件套/三十三件套清除」在本次件数递增时必然失败；改存在性口径，实件数由 v21.39 冒烟守护）+ `CHANGELOG.md`：本条。
- 验证：`node --check js/view/menus.js js/data.js tests/smoke_v2138_titleslotkey.mjs tests/smoke_v2139_helpkey.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 三十五件套全绿（既有 34 件逐项零回归 + 新增 v21.39 **32 项断言全过**——GAME_VERSION 格式合法且越过 v21.38、data.js 含 v21.39 注释、menus.js 含 v21.39 注释且页脚源级含「←/→ 翻页(A/D亦可)」、旧页脚表达式「← → 翻页   ·   H/Esc 关闭」源级零残留、README H 行存在且含「←/→」「A/D 亦可」「含地图指南」、旧口径「`←→` 翻页，含地图指南」源级零残留、README tests 树收录 smoke_v2139_helpkey + 「三十五件套（三十四件套清除）」+ v21.39 守护描述、main.js help 翻页分派源级落位（ArrowRight/ArrowLeft + d/D/a/A）、运行期实证：DOM 桩 + main.js 真实导入后 screens.help.onKey ArrowRight 1→2、连翻回第 1 页循环、ArrowLeft 反向环绕、小写/大写 d/a 与方向键同效、ArrowLeft 从第 1 页环绕末页零回归、Esc/h 关闭回 world 零回归、drawHelp 四页逐页渲染页脚均含「←/→ 翻页(A/D亦可)」且页数标签正确、package.json 已收录（第 35 份）、smoke_v2138 README 件套断言已去硬化（不再以「三十四件套/三十三件套清除」断言件数，实件数由本版守护））。

## v21.38 README 快速上手表标题行补「←/→」选槽口径——标题页选槽三入口里唯一没被 README 点名的 ←/→（文档·口径收尾，承 v21.29 E 键别名 / v21.30 教程行 / v21.32 面向提示同一「同一功能所有入口口径一致」主线：标题页选槽自 v21.4 起有三个入口——数字键 1/2/3、←/→（A/D 亦可）循环切换——标题页提示行（menus.drawTitle「按 1/2/3 或 ←/→ 选择存档槽」）与 H 页存档槽行（HELP_PAGES[0]「标题按 1/2/3 或 ←/→ 选择」）都早已同口径，唯独 README 快速上手表标题行仍写「标题 `1/2/3` | 选择存档槽，`L` 读档，`R` 重开新档」——README 是玩家最先读的操作清单，按它操作的人不知道 ←/→ 也能选槽（而 v21.4 的 changelog 当时只同步了游戏内提示行与 H 页，README 正是那轮漏网）

- 【操作清单缺一格：三入口只写了一个】`README.md` 快速上手表「标题 `1/2/3`」行描述为「选择存档槽，`L` 读档，`R` 重开新档（连按两次 R 确认执行——防误触：Esc 返回标题时进行中的冒险仍在内存，误按一次不会丢档）」——与 main.js title.onKey 的真实分派（`1..SAVE_SLOTS` 直选 + `ArrowRight/ArrowLeft` 及 `a/d/A/D` 循环切换）相比漏掉了 ←/→ 与 A/D 两个入口；H 页「存档槽」行与标题页提示行（均为「或 ←/→ 选择」口径）早已补全，README 是最后一个停留在 v21.4 之前的操作清单。按本项目「同一入口三处文档口径一致」的既有主线（v21.29-32 E 键口径连收三处漏网），这是操作清单层的同类单点遗漏。
- 【修正：README 标题行改「1/2/3 或 ←/→」口径，行为零变化】`README.md` 该行改为 `标题 1/2/3 | 选择存档槽（←/→ 与数字键同效循环切换，A/D 亦可）；L 读档，R 重开新档（连按两次 R 确认执行——防误触：Esc 返回标题时进行中的冒险仍在内存，误按一次不会丢档）`——「←/→ 与数字键同效」与 H 页「或 ←/→ 选择」、标题页提示行「按 1/2/3 或 ←/→ 选择存档槽」逐字同源（A/D 亦可是一并如实标注，与 main.js 分派逐字相符）；纯文档零行为：未动 main.js 任何分派、未动 H 页/提示行、未动任何游戏逻辑与数值。
- 【零回归面】未动 main.js / menus.js / data.js 任何行为（仅 data.js GAME_VERSION v21.37→v21.38 与注释；drawTitle 提示行、HELP_PAGES 存档槽行、L/R 分派逐字未动）；未动任何战斗/掉落/任务/存档/遇敌/数值；`GAME_VERSION` v21.37→v21.38。只改 3 个文档/数据文件（`README.md`：标题行 + tests/ 目录树与「冒烟三十四件套」、三十三件套清除 + 守护描述；`data.js`：GAME_VERSION + 注释；`CHANGELOG.md`：本条）+ `package.json`（test 串第 34 份）+ 新增 `tests/smoke_v2138_titleslotkey.mjs`（仓库常驻，承 v21.10-v21.37 冒烟入库先例）+ 顺带把 `tests/smoke_v2137_seencount.mjs` 的 README 件套断言按 v21.7 惯例去硬化（其写死的「三十三件套/三十二件套清除」在本次件数递增时必然失败；改存在性口径，实件数由 v21.38 冒烟守护）。
- 验证：`node --check js/data.js tests/smoke_v2137_seencount.mjs tests/smoke_v2138_titleslotkey.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 三十四件套全绿（既有 33 件逐项零回归 + 新增 v21.38 **24 项断言全过**——GAME_VERSION 格式合法且越过 v21.37、data.js 含 v21.38 注释、README 标题行存在且含「←/→」「与数字键同效」「A/D 亦可」、旧口径「选择存档槽，`L` 读档」源级零残留、L 读档/R 两按确认/防误触文案逐字保留、README tests 树收录 smoke_v2138_titleslotkey + 三十四件套（三十三件套清除）+ v21.38 守护描述、main.js 标题分派源级落位（ArrowRight/ArrowLeft/d/D/a/A + 数字键 ≤SAVE_SLOTS）、drawTitle 提示行与 H 页行同口径（「或 ←/→ 选择存档槽」/「或 ←/→ 选择」）、运行期实证：DOM 桩 + main.js 真实导入后 title.onKey ArrowRight 1→2→3→1 三连跳、ArrowLeft 1→3→2→1 反向、小/大写 d/a 与方向键同效、数字键 1/2/3 直选零回归、SAVE_SLOTS===3、L 读空槽停留 title 且提示给出（v21.33 零回归）、R 首按仅武装（v21.16 零回归）、drawTitle 渲染含「按 1/2/3 或 ←/→ 选择存档槽」且 L/R 提示零回归、package.json 已收录（第 34 份）、smoke_v2137 README 件套断言已去硬化）。



- 【写死的 0：已遭遇只有布尔没有次数】`startBattle` 记录点为 `if (S.G.seen) S.G.seen[_seenKey] = true; else S.G.seen = { [_seenKey]: true }`（布尔）；`view/menus.js` `drawCodex` 已遭遇行右列直接 `text('已遭遇 ✕0', …)`——同一列、同样式，讨伐行是 `讨伐 ✕${hero.bestiary[n]}` 真实计数，已遭遇行却永远 ✕0；「信息透明」主线（v19.14/v19.41/v20.0-v20.2/v21.8/v21.9 敌方剩余 HP 反馈一族）在「我撞过它几次」这一格仍是空——对刷图鉴/刷蘑菇/死磕某怪的玩家，「这怪我撞过几次还没打死」是战报之外最想确认的数字。
- 【修正：battle 侧计数 + 图鉴侧 ✕N，旧档布尔 |0 归一，零结算零存档格式变化】`js/battle.js` startBattle 记录点改为 `if (S.G.seen) S.G.seen[_seenKey] = (S.G.seen[_seenKey] || 0) + 1; else S.G.seen = { [_seenKey]: 1 };`（每次进战 +1，逃跑/战败/B 重整旗鼓重进战同样累计；名字仍经 canonicalName 归一，真身→本体与 bestiary 同口径）；`js/view/menus.js` rows 增补 `seenCt:((hero.seen||{})[n])|0`（`|0` 归一：旧档布尔 `true`→1（至少撞见一次）、undefined/0→0，不显示 NaN/true），已遭遇行改 `text(\`已遭遇 ✕${r.seenCt}\`, …)`；bestiary/讨伐/掉落/逃跑成功率/经验结算逐字未动，纯追加语义。
- 【零回归面】未动战斗/掉落/任务/存档/遇敌/数值（startBattle 其余分支与 seen 无关的 hpMax/困难倍率/警报音/_bossRetry 逐字未动；hero.seen 仅 battle.js 一处写入、menus.js 一处读取）；未动图鉴 got 分支（`讨伐 ✕N` 逐字保留）与页脚（记忆收录/累计讨伐/额外掉落）；`GAME_VERSION` v21.36→v21.37。只改 4 个游戏/测试文件（`js/battle.js`：count 逻辑 + 注释；`js/view/menus.js`：seenCt + ✕N + 注释；`js/data.js`：GAME_VERSION + 注释；`tests/smoke_v2136_granny.mjs`：README 件套断言去硬化）+ 新增 `tests/smoke_v2137_seencount.mjs`（仓库常驻，承 v21.10-v21.36 冒烟入库先例）+ `package.json`（test 串第 33 份）+ `README.md`（tests 树 +「冒烟三十三件套」/「三十二件套清除」+ 系统清单图鉴&成就条目追加「已遭遇未讨伐行显示真实撞见次数 `已遭遇 ✕N`」+ v21.37 守护描述）+ `CHANGELOG.md`：本条。
- 验证：`node --check js/battle.js js/view/menus.js js/data.js tests/smoke_v2137_seencount.mjs tests/smoke_v2136_granny.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 三十三件套全绿（既有 32 件逐项零回归 + 新增 v21.37 **31 项断言全过**——GAME_VERSION 格式合法且越过 v21.36、data.js/battle.js/menus.js 含 v21.37 注释、battle 计数落位 `(S.G.seen[_seenKey] || 0) + 1` 与首见分支 `{ [_seenKey]: 1 }`、旧布尔赋值零残留、menus rows `seenCt:((hero.seen||{})[n])|0`、`已遭遇 ✕${r.seenCt}` 落位、写死「已遭遇 ✕0」源级零残留、讨伐行 `✕${hero.bestiary[n]}` 零回归、运行期实证：DOM 桩 + main.js 真实导入后真实 startBattle 史莱姆×3 → seen 1/2/3 递增、`幽冥魔王·真身` 经 canonicalName 归一为 `幽冥魔王` 且无「·真身」键、bestiary 零影响（碰过≠击败）、旧档布尔 `(true|0)===1`、`(undefined|0)===0`、drawCodex 渲染 `已遭遇 ✕3`/`已遭遇 ✕1`（旧档 true 归一）/`讨伐 ✕2`/`未讨伐` 占位/无 `已遭遇 ✕0` 渲染/页脚 `记忆收录：1/13` 零回归、README 已同步（smoke_v2137_seencount + 三十三件套/三十二件套清除 + 「已遭遇 ✕N」口径 + v21.37 守护描述）、package.json 已收录（第 33 份）、smoke_v2136 README 件套断言已去硬化（v21.7 惯例））。



## v21.36 潮灯镇新增风味 NPC「掌灯阿婆」——水塘南岸讲旧故事的老人（内容扩充·纯风味，承 v21.24 守碑人 / v21.17 老矿工 NPC 先例：全图四大关键地标旁各有 NPC 守望之后，本次补的是「镇子的人情味」——潮灯镇现有 灯长/镇民/巡灯人/守书记/井巫 五名可对话者，但全是「指路/支线/点破真相」的功能位，没有一个纯讲旧故事、随主线进度更新心境的角色；且水塘（广场北井塘）作为镇子的景观地标，始终没有任何人物在场——玩家每个阶段回镇补给都会路过它，却听不到一句关于「灯」的旧话）

- 【空洞：塘边无人，镇子缺一句「旧话」】`MAPS.village` 水塘南岸 (14,8) 是整张镇图最显眼的景观空白——喷泉/井在北、塘水居中、广场在南，从广场往北走必然路过；而潮灯镇的 NPC 阵容（灯长/镇民/巡灯人/守书记/井巫）功能面已满（灯油支线/闲聊/圣光之剑情报/石壳支线/真相点破），全是「对玩家有用的角色」。参照 v21.24 守碑人「关键地标旁有个人在」的既有面貌，塘边缺的是一双「看着水、念叨灯」的眼睛；按「信息透明」主线无关，但按叙事完整度与 v21.17「矿脉无补给/回廊无宝箱」同类「结构性空洞」的选题方向，这是镇子最后一个没有人在场守望的角落。
- 【修正：data.js 三件套 + sprites 造型映射，台词走既有 linesByStage 机制（零新逻辑）】`MAPS.village.extras` 新增 `{ x: 14, y: 8, ty: 'NPC' }`（塘水南岸、广场路北一格的草地；NPC_SPOTS 全局坐标键 '14,8' 未被他图占用——14,7 为水面、14,9 为广场路、13,6 为灯长，左右不撞；瓦片由 world.placeExtras 既有通路覆盖为 TY.NPC）；`NPC_SPOTS` 新增 `'14,8':'granny'`；`NPCS.granny={name:'掌灯阿婆', mark:'lamp', linesByStage:[…], after:[…]}`——台词分四段：默认（灯灭那晚/雾从井底来/出门带药水）→ bossDefeated（灯芯回来了/那影子竟是旧灯卫）→ galleryOpen（回廊开了/替我看看最旧那块碑上有没有「阿灯」）→ trueBoss 后 after 彩蛋（塘水倒映整座镇的灯/他的名字现在刻在你心里）。选段走 quests.js `npcQuestPages` 既有 `linesByStage` 分支（井巫/巡灯人/雾径猎手同款，本版零改动），trueBoss 彩蛋走既有 `after` 契约（镇民/守碑人同款）；`view/sprites.js` `NPC_SHEET` 新增 `granny:'mwSage'`（长者袍造型，与守名者/守碑人同款、跨图不重名；贴图缺失时退回既有 `mark:'lamp'` 程序化绘制的灯形占位——与老矿工 v21.17 同款分支）。无任务（npcQuestMark 恒 null、无 ❕ 顶标）、零状态、零结算、零裸字面量新逻辑。
- 【零回归面】未动任何战斗/掉落/任务/存档/遇敌/数值与既有 NPC（NPC_SPOTS 既有 15 键逐字未动；v2124 冒烟的「既有键未动」断言自动覆盖）；未动 world/quests/core/battle（全部走数据与既有分支）；未动 help/图鉴/成就/小地图；`GAME_VERSION` v21.35→v21.36。只改 3 个游戏文件（`data.js`：GAME_VERSION + extras + NPC_SPOTS + NPCS.granny；`view/sprites.js`：NPC_SHEET 一行）+ 新增 `tests/smoke_v2136_granny.mjs`（仓库常驻，承 v21.10-v21.35 冒烟入库先例）+ `package.json`（test 串第 32 份）+ `README.md`（潮灯镇条目/系统清单 NPC 名单/tests 树/冒烟三十二件套 + v21.36 守护描述）+ 顺带把 `tests/smoke_v2135_levelpace.mjs` 的 README 件套断言按 v21.7 惯例去硬化（其写死的「三十一件套/三十件套清除」在本次件数递增时必然失败；改「冒烟/件套」存在性口径，实件数由 v21.36 冒烟守护）+ `CHANGELOG.md`：本条。
- 验证：`node --check js/data.js js/view/sprites.js tests/smoke_v2135_levelpace.mjs tests/smoke_v2136_granny.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 三十二件套全绿（既有 31 件逐项零回归 + 新增 v21.36 **31 项断言全过**——GAME_VERSION 格式合法且越过 v21.35、data.js 含 v21.36 注释、NPC_SPOTS['14,8']==='granny' 且仅一键、既有 15 键未动、全图 extras 扫描 (14,8) 仅 village 一处 NPC（全局坐标防撞）、data.js 源级 `{ x: 14, y: 8, ty: 'NPC' }` + v21.36 注释、NPCS.granny 契约（name 掌灯阿婆/mark lamp）、linesByStage 三段门 null→bossDefeated→galleryOpen、trueBoss after 2 页、全部 8 页字符串数组 + [Enter] 收尾、选段运行期实证（npcQuestPages 空 hero→默认段/ bossDefeated→灯芯回来了/ galleryOpen→回廊开了+阿灯/ trueBoss→塘水彩蛋）、npcQuestMark(S.G,'granny')===null（无任务顶标）、loadMap('village') 运行期 at(14,8)===TY.NPC、灯长 (13,6) 零回归、站位 (14,9) PATH 与塘水 (14,7) WATER 零回归、Enter 面向交互 → scene==='talk' && curNpc==='granny'、talkPages 第 1 页含「灯灭那晚」且共 2 页、E 键同效（v21.29 别名零回归）、sprites.js `granny:'mwSage'` 源级落位、README 已同步（smoke_v2136_granny + 三十二件套口径、三十一件套清除 + 潮灯镇条目/系统清单「掌灯阿婆」+ v21.36 守护描述）、package.json 已收录（第 32 份）、smoke_v2135 README 件套断言与 GAME_VERSION 精确锚点均已去硬化（不再以「三十一件套」/`=== 'v21.35'` 断言）。

## v21.35 README 升级节奏参考「Lv1→2 约 1.5 场」口径修正——潮灯镇 Lv1 单场经验上限 18 < 阈值 20，恒 2 场升 Lv2，1.5 是线性均摊的低估（文档·数值口径，承 v21.34 升级经验曲线链式口径 / v19.15 酿造配方漂移同一「文档必须与结算逐字同源」主线：README 升级节奏参考宣称「按各图遇敌池权重实测」，但 Lv1→2 写「约 1.5 场」——这是 20 ÷ 每场期望经验 13.25 ≈ 1.51 的**线性均摊**算法，忽略了「单场经验上限」这一取整约束：Lv1 潮灯镇遇敌池（史莱姆 11 / 野狼 15 / 哥布林 13 / 毒蛇 18，权重 4/3/4/1）单场最高只有 18 经验 < 升级阈值 XP_INIT(20)，一场永远到不了 Lv2；两场最低 22 ≥ 20，恒两场即 Lv2——文档与真实随机遭遇链脱节，新玩家按表算「约 1.5 场」却发现自己固定打两场，进度预期对不上）

- 【线性均摊 vs 单场上限：升级场数不能按除法估】`data.js` `XP_INIT=20` / `MON_BASE` 各怪 `xp:[基础,每级]` 是唯一真源，`randomEncounter`（encounter.js，读 `MAPS.village.pool` 与 `encounterWeight` 的 minLv）生成 Lv1 潮灯镇池恰四基础怪——史莱姆 `xp[8,3]→11`（w=4）/ 野狼 `[12,3]→15`（w=3）/ 哥布林 `[10,3]→13`（w=4）/ 毒蛇 `[15,3]→18`（w=1），期望 13.25、线性均摊 20÷13.25≈1.51 即「约 1.5 场」的来历；但单场经验是离散取值、上限 18 < 20，且两场最低 11+11=22 ≥ 20——因此真实结果是**恒 2 场**：1 场不可能（上限不够）、2 场必胜（下限足够）。5000 局真实 `randomEncounter` + `monReward` 链蒙特卡洛：1/2/3 场 = 0/5000/0；另复核其余三档（Lv5→6 ≈2.1 / Lv9→10 ≈5.1 / Lv11→12 ≈7.0，50000 局），与表内「约 2.2 / 5.0 / 7.0」一致，唯 Lv1→2 需要修正。
- 【修正：README 升级节奏参考行 Lv1→2 改为「恰 2 场」+ 成因注记，结算数据逐字不动】`README.md` 该行改为 `Lv1→2 **恰 2 场**（潮灯镇；Lv1 单场经验上限 18 < 阈值 20，一场不够；两场最低 22 ≥ 20，恒两场——「约 1.5 场」是 20÷13.25 线性均摊的低估，v21.35 按真实 randomEncounter 链修正）`，行末补 v21.35 复核注记；数值速查「此表据 v21.11 代码实测整理」→「据 v21.35 代码实测整理（v21.34 起逐值与 data.js 结算复核）」。`XP_INIT` / `XP_GROW` / `MON_BASE` / `randomEncounter` / `monReward` / `hero.grantXp` 逐字未动，纯文档零结算、零数值变化。
- 【零回归面】未动 `XP_INIT`/`XP_GROW`/`MON_BASE`/`baseStats`/任何经验·升级·遇敌结算；未动任何数值、地图、战斗、任务、存档、UI；未动升级节奏参考其余三档数值（复核一致）；`GAME_VERSION` v21.34→v21.35。只改 4 个文档/测试文件（`README.md`：升级节奏参考行 + 数值速查表头 + tests/ 目录树 + 「冒烟三十一件套」、三十件套清除 + 系统说明追加 v21.35 守护描述；`data.js`：GAME_VERSION + 注释；`package.json`：test 串第 31 份）+ 新增 `tests/smoke_v2135_levelpace.mjs`（仓库常驻，承 v21.10-v21.34 冒烟入库先例）+ 顺带把 `tests/smoke_v2134_xpcurve.mjs` 按 v21.7 惯例去硬化（其「data.js GAME_VERSION 逐字等 v21.34」精确锚与「冒烟三十件套」件数断言在本次递增时必然失败；改单调界 + 件套存在性口径）+ `CHANGELOG.md`：本条。
- 验证：`node --check js/data.js tests/smoke_v2134_xpcurve.mjs tests/smoke_v2135_levelpace.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 三十一件套全绿（既有 30 件逐项零回归 + 新增 v21.35 **23 项断言全过**——GAME_VERSION 格式合法且越过 v21.34、`data.js GAME_VERSION === 'v21.35'`、data.js 含 v21.35 注释、XP_INIT===20 / XP_GROW===1.42 / baseStats(1) 未动（HP45 攻9 防5）零回归、Lv1 潮灯镇池数据层派生恰四基础怪（史莱姆/野狼/哥布林/毒蛇）且权重 4/3/4/1、单场上限 18 < 20、两场下限 22 ≥ 20、期望经验 ≈13.25（旧「约 1.5 场」来历实证）、运行期 2000 局真实 randomEncounter+monReward 链 1/2/3 场 = 0/2000/0、README 含「恰 2 场」与成因句（上限 18/下限 22）、旧「Lv1→2 约 1.5 场」清零（仅成因引号内）、v21.35 复核注记（≈2.1/≈5.1/≈7.0）、数值速查据 v21.35、tests 树收录 smoke_v2135_levelpace、README 三十一件套（三十件套清除）、v21.35 守护描述、package.json 已收录（第 31 份）、smoke_v2134 版本锚点与件套断言去硬化）。



- 【闭式幂 vs 链式取整：同一经验曲线的两套口径】`data.js` `XP_INIT=20` / `XP_GROW=1.42` 是唯一真源，`hero.grantXp` 的升级循环是 `hero.xpNext = Math.round(hero.xpNext * XP_GROW)`（逐级链式：20→28→40→57→81→115→163→231→328→466→662→940…），`hero.skillXpHint` 的「距下一技能还差 N 经验」预估也用同式链式；README 数值速查却写成闭式 `round(20 × 1.42^(lv-1))` 并示例「Lv11→12 需 667」——闭式在第 6 级起就与链式分道（round(20×1.42^6)=164 vs 链式 163；Lv11→12 667 vs 662），正是 v21.15/v21.22 反复点名的「多套口径漂移」：结算跑链式、文档写闭式，两端对不上。
- 【修正：README 升级经验行改为链式口径（含逐级全表），结算数据逐字不动】`README.md` 数值速查「升级经验」行改为 `XP_INIT(20) 起**链式**逐级 round(上一级 × 1.42)——每级四舍五入后累积（**非闭式幂**；闭式 20×1.42^(lv-1) 自 Lv7→8 起漂移：闭式 164 vs 链式 163）：Lv1→2 20 · Lv2→3 28 · … · Lv11→12 **662**（v21.34 修正：旧行闭式示例 667）`；`hero.grantXp` / `XP_INIT` / `XP_GROW` / `skillXpHint` 链式预估逐字未动，纯文档修正零结算、零数值变化。
- 【零回归面】未动 `hero.grantXp`/`skillXpHint`/`XP_INIT`/`XP_GROW`/任何经验·升级·技能结算；未动任何数值、地图、战斗、任务、存档、UI；未动帮助页/图鉴/状态页（游戏内无经验曲线展示，仅实测档位）；`GAME_VERSION` v21.33→v21.34。只改 4 个文档/测试文件（`README.md`：升级经验行 + tests/ 目录树 + 「冒烟三十件套」、二十九件套清除 + 系统说明追加 v21.34 守护描述；`data.js`：GAME_VERSION + 注释；`package.json`：test 串第 30 份）+ 新增 `tests/smoke_v2134_xpcurve.mjs`（仓库常驻，承 v21.10-v21.33 冒烟入库先例）+ 顺带把 `tests/smoke_v2133_loadempty.mjs` 按 v21.7 惯例去硬化（其「data.js GAME_VERSION 逐字等 v21.33」精确锚与「冒烟二十九件套」件数断言在本次递增时必然失败；改单调界 + 件套存在性口径）+ `CHANGELOG.md`：本条。
- 验证：`node --check js/data.js tests/smoke_v2133_loadempty.mjs tests/smoke_v2134_xpcurve.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 三十件套全绿（既有 29 件逐项零回归 + 新增 v21.34 **25 项断言全过**——GAME_VERSION 格式合法且越过 v21.33、`data.js GAME_VERSION === 'v21.34'`、data.js 含 v21.34 注释、XP_INIT===20 / XP_GROW===1.42 常量导出、由常量重算的链式全表 [20,28,40,57,81,115,163,231,328,466,662,940] 逐值正确（Lv1→2..Lv11→12）、Lv11→12 实需 662（旧行 667 为闭式漂移值）、Lv7→8 实需 163 且闭式 round(20×1.42^6)=164 漂移 1 实证、hero.js 链式源级落位（`hero.xpNext = Math.round(hero.xpNext * XP_GROW)`）且无闭式幂写法（无 Math.pow/指数与 XP_GROW 组合）、core.js 建档起点同源（`xpNext: XP_INIT`）、运行期实证：DOM 桩 + main.js 真实导入 + hero.grantXp（Lv7 设 xpNext=163、给 164 经验）→ 升到 Lv8（阈值 163 而非闭式 164）、余经验 1、下一级需求 231（链式 round(163×1.42) 而非闭式 232）、hpMax 87→94 零回归、README 升级经验行含「链式/非闭式幂」字样与逐级关键值（Lv1→2 20 / Lv7→8 163 / Lv11→12 **662**）、旧「Lv11→12 需 667」示例清零、升级节奏参考行仍与链式一致（Lv11→12 约 7.0 场）、tests 树收录 smoke_v2134_xpcurve + 「冒烟三十件套」（二十九件套清除）+ v21.34 守护描述、package.json 已收录（第 30 份）、smoke_v2133 的 GAME_VERSION 精确锚点与件套断言已去硬化（v21.7 惯例，实件数由本版冒烟守护）。

## v21.33 标题页 L 读空槽静默反馈——load() 返回 false 时按 L 「毫无反应」的唯一静默键补一句短提示（体验打磨·反馈，承 v21.16 标题页 R 两按确认「标题页每个按键都该有反应」同族：v21.16 给破坏性的 R 加了防误触反馈、v19.86 给读档成功补了角色摘要，唯独「按了 L 却什么都没发生」这条空槽路径从头到尾没有任何反馈——标题页 1-3 选槽 / ←→ / Enter / L / R 七个输入里，选槽有 SFX.select、Enter 进创建页、R 有两按确认提示，只有 L 在槽位为空（或存档损坏）时整体条件落空、无声无息）

- 【静默键：L 在空槽位毫无反馈】`main.js` `screens.title.onKey` 的读档分派是 `(e.key === 'l' || e.key === 'L') && load()`——`load()` 返回 false（该槽无存档 JSON 或读取失败）时整个 if 条件落空：玩家站在标题页按 L，既没有音效、没有提示框、画面纹丝不动，与「按了键」的预期完全脱节（对比 R 的「🔁 再按一次 R 确认」提示与选槽的 select 音）；槽位选择已把 L 写进标题页提示行（v21.4/v21.18 同口径「L 读档」），玩家照提示按键却可能撞上这无声分支——「功能存在就必须能看到入口」（v21.14/v21.18/v21.29 主线）在反馈层的对称反例：入口标了、按了没反应。
- 【修正：else-if 空槽分支补一句短提示，读取路径逐字不动】`main.js` title.onKey 在既有 `(e.key==='l'||'L') && load()` 分支之后新增 `else if (e.key === 'l' || e.key === 'L')`：`SFX.cancel()` + `boxMsg('💤 槽 N 还没有存档，按 Enter 开始新的冒险吧。', EVENT_MSG_MS)`——「槽 N」读 S.curSaveSlot（与读档摘要同源）、时长用 EVENT_MSG_MS 小事件档（与标题页 R 两按确认提示（v21.16）同款 1600 族，玩家看得到提示即窗口节奏一致）；load() 判定、读取路径、有档分支（v19.86 读档摘要）逐字未动——纯反馈零结算：有档行为不变，无档从「沉默」变成「一句引导」。
- 【零回归面】未动 load()/saveGame/存档格式/任何结算；未动 R 两按确认状态机（titleResetCheck）与提示；未动选槽/Enter 分派；未动 data.js 任何数据（仅 GAME_VERSION 递增）；未动 README 快速上手表/帮助页/状态页/图鉴；`GAME_VERSION` v21.32→v21.33。只改 3 个游戏/文档文件（`main.js`：title.onKey +1 分支 + 注释；`data.js`：GAME_VERSION + 注释；`README.md`：tests/ 目录树与「冒烟二十九件套」两处同步、二十八件套清除 + 系统说明追加 v21.33 守护描述）+ `package.json`（test 串第 29 份）+ 新增 `tests/smoke_v2133_loadempty.mjs`（仓库常驻，承 v21.10-v21.32 冒烟入库先例）+ 顺带把 `tests/smoke_v2132_facesign.mjs` 的 README 守护按 v21.7 惯例去硬化（其写死的「冒烟二十八件套」在本次件数递增时必然失败；实件数由 v21.33 冒烟守护 README「二十九件套」口径）+ `CHANGELOG.md`：本条。
- 验证：`node --check js/main.js js/data.js tests/smoke_v2132_facesign.mjs tests/smoke_v2133_loadempty.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 二十九件套全绿（既有 28 件逐项零回归 + 新增 v21.33 **19 项断言全过**——GAME_VERSION 格式合法且越过 v21.32、`data.js GAME_VERSION === 'v21.33'`、main.js 含 v21.33 注释与空槽 else-if 分支（源级落位 `} else if (e.key === 'l' || e.key === 'L') {`）、提示文案「还没有存档，按 Enter 开始新的冒险吧」落位且与 EVENT_MSG_MS 同源（同句含 `, EVENT_MSG_MS)`）、既有读取分支逐字保留（`&& load()` + `读取槽 ${S.curSaveSlot}`）、R 两按确认提示逐字保留、data.js 含 v21.33 注释、运行期实证：DOM 桩 + main.js 真实导入后 `screens.title.onKey({key:'L'})` 空槽 → #msg 文本含「槽 1 还没有存档」且 S.scene 停留 'title'、S.G 未被 load() 替换（同一对象、无档不误读）、`newGame('测试')` + `saveGame()` 真实写入后同键 L → #msg 为「💾 读取槽 1：测试 Lv.1 …」且 S.scene==='world'、curMap()==='village'（读取成功路径零回归）、空槽下 `onKey({key:'R'})` 仍走武装分支（`S.titleResetArm>0` + 「再按一次 R 确认重开新档」提示、不误触 resetRun）、README 已同步（smoke_v2133_loadempty + 二十九件套口径、二十八件套清除 + v21.33 守护描述）、package.json 已收录 smoke_v2133_loadempty、smoke_v2132 README 守护表达式已去硬化（不再以「二十八件套」断言件数）。

## v21.32 世界画面可交互对象上下文提示 E 键口径收尾——faceHint 面向提示「⏎ 对话·名字」→「⏎/E 对话·名字」，交互类五类提示补 E 键（体验打磨·可发现性·口径收尾，承 v21.29 大地图 E 键交互别名 / v21.30 新手教程 E 键同口径：v21.29/v21.30 把 README 快速上手表、H 页「对话 / 确认」行、新手教程提示三处都同步成 Enter/E——唯独世界画面玩家正盯着的「面向提示」仍是单键 ⏎：交互入口已是双键，提示却仍只认一个符号）

- 【漏网：面向提示仍只画 ⏎】v21.29 起 E 与 Enter 同效调用 interact（NPC/商店/旅馆/酿造/读碑/祭坛/传送门），README 快速上手表、H 页、新手教程三处口径都同步为 Enter/E；但 `view/drawWorld.js` `faceHint()`（世界画面常驻的「面向什么就提示什么」一行）的交互类标签仍是 `⏎ 对话 · 名字` / `⏎ 商店` / `⏎ 旅馆` / `⏎ 酿造` / `⏎ 读碑 · 名字石碑`——玩家按文档用 E 交互时，面前提示与真实输入对不上号（「功能存在就必须能看到入口」主线（v21.14/v21.18/v21.29）在提示层的对称反例：入口有了、提示没跟上）。
- 【修正：交互类标签统一改 ⏎/E，踩踏类逐字不动】`drawWorld.js` `faceHint()` 五个交互分支改为 `⏎/E 对话 · 名字` / `⏎/E 对话` / `⏎/E 商店` / `⏎/E 旅馆` / `⏎/E 酿造` / `⏎/E 读碑 · 名字石碑`（「⏎/E」与 HELP_PAGES「对话 / 确认」行、README 上手表的「Enter / E」逐字同源、符号版口径；NPC 名仍读 NPCS 单一数据源）。踩踏类提示（喷泉 `踩上回血 · HP+N MP+N` / 宝箱 `踩上开启` / 祭坛 `踩上开战` / 试炼碑 `踩上挑战` / 传送门 `踩上通行 → X` / `⛔ 门锁着`）是踩上触发、与按键无关，逐字不动。纯显示零结算：未动 interact/onStep/世界按键分派/任何判定与数值。
- 【零回归面】未动 main.js world.onKey（v21.29 E 分支逐字保留）、未动 interact/useGate/useExit/onStep、未动任何战斗/掉落/任务/存档/遇敌逻辑与数值；未动 HELP_PAGES 行数与内容、drawHelp 渲染；未动 README 快速上手表既有口径（本版仅系统清单面向提示句 + tests 部分）；`GAME_VERSION` v21.31→v21.32。只改 3 个游戏/文档文件（`js/view/drawWorld.js`：faceHint 五分支 + 注释；`js/data.js`：GAME_VERSION + 注释；`README.md`：tests/ 目录树与「冒烟二十八件套」两处同步、二十七件套清除 + 系统清单「面向可交互对象上下文提示」句 ⏎/E 双键口径与 v21.32 标注）+ `package.json`（test 串第 28 份）+ 新增 `tests/smoke_v2132_facesign.mjs`（仓库常驻，承 v21.10-v21.31 冒烟入库先例）+ 顺带把 `tests/smoke_v2131_helptitle.mjs` 的 README 守护按 v21.7 惯例去硬化（其写死的「二十七件套/二十六件套清除」在本次件数递增时必然失败；实件数由 v21.32 冒烟守护 README「二十八件套」口径）+ `CHANGELOG.md`：本条。
- 验证：`node --check js/data.js js/view/drawWorld.js tests/smoke_v2131_helptitle.mjs tests/smoke_v2132_facesign.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 二十八件套全绿（既有 27 件逐项零回归 + 新增 v21.32 **19 项断言全过**——GAME_VERSION 单调越过 v21.31、faceHint 五分支 ⏎/E 源级落位（NPC 含名字派生/商店/旅馆/酿造/读碑）、旧单键标签零残留（⏎ 对话/商店/旅馆/酿造/读碑）、踩踏类提示逐字未动（喷泉/宝箱/开战/挑战/通行/门锁）、运行期实证：DOM 桩 + main.js 真实导入后逐类 `drawWorld()`（面向灯长 → `⏎/E 对话 · 灯长`、商店 → `⏎/E 商店`、酿造 → `⏎/E 酿造`、旅馆 → `⏎/E 旅馆`、无字回廊名字石碑 → `⏎/E 读碑 · 名字石碑`）、喷泉仍绘 `踩上回血 · 状态已满`（踩踏类零回归）、README 已同步（smoke_v2132_facesign + 二十八件套口径、二十七件套清除 + `⏎/E 对话·名字`/`⏎/E 商店/旅馆/酿造` 双键口径与 v21.32 标注）、package.json 已收录 smoke_v2132_facesign、smoke_v2131 README 守护表达式已去硬化（不再以「二十七件套」断言件数）+ 其 GAME_VERSION 精确锚定同步去硬化（`=== 'v21.31'` 改「已越过 v21.31」单调界，精确值由本版冒烟守护）。

## v21.31 帮助页面板标题随页切换——「— 操作说明 —」→ 按页读 HELP_TITLES（体验打磨·排版一致性·信息透明，承 v19.59 页长自适应 / v21.11 行宽巡检 / v21.20 两页行宽修复同一主线：面板内侧一切文字必须与所在页内容一致，帮助页四页标题却是唯一脱节处）

- 【面板标题是翻页后残留的旧页名】`view/menus.js` `drawHelp()` 的面板标题从 v1.x 起硬编码 `panel(70,28,500,424,'— 操作说明 —')`——玩家按 H 翻到第 2 页（地图指南）、第 3 页（魔物状态）、第 4 页（试炼进阶）时，面板顶端标题仍写着「操作说明」，而页脚却正确显示「第 X/4 页」：同一块面板里「标题说操作说明、页脚说第 3 页」自相矛盾；四页的正文关键词（地图指南/魔物状态/试炼进阶）此前也从未被任何界面点名（v21.2-v21.28 逐版往页里补内容，唯独没人看面板标题）。
- 【修正：data.js 新增 HELP_TITLES 单一数据源，drawHelp 改读 `HELP_TITLES[S.helpPage]`】`data.js` 在 `HELP_PAGES` 之后新增 `const HELP_TITLES = ['操作说明', '地图指南', '魔物状态', '试炼进阶']` 并加入 export 块（页数增减只改这一处标题即自动跟随，与 HELP_PAGES 一一对应，零裸字面量散落）；`menus.js` `drawHelp()` 标题行改为 `panel(70,28,500,424,'— ' + (HELP_TITLES[S.helpPage] || '操作说明') + ' —')`（`|| '操作说明'` 兜底越界索引，与既有 helpPage 翻页模运算同家族）。纯显示零结算：未动任何行内容/行数/r[2]/页长自适应分支/页脚/←→ 翻页逻辑。
- 【零回归面】未动 HELP_PAGES 四页任何一行（行数仍 14/5/10/9、r[2] 仍 2 处、全部派生式逐字不动——v21.11-v21.28 的行宽/页长预算自动不受影响）；未动 drawWorld/drawBattle/HUD/音频/战斗/任务/存档任何逻辑与数值；`GAME_VERSION` v21.30→v21.31。只改 3 个游戏/文档文件（`data.js`：GAME_VERSION + HELP_TITLES + 导出 + 注释；`view/menus.js`：drawHelp 标题行 1 行 + import + 注释；`README.md`：tests/ 目录树与「冒烟二十七件套」两处同步、二十六件套清除 + 系统清单「四页操作帮助」句补 v21.31 标题随页口径）+ `package.json`（test 串第 27 份）+ 新增 `tests/smoke_v2131_helptitle.mjs`（仓库常驻，承 v21.10-v21.30 冒烟入库先例）+ 顺带把 `tests/smoke_v2130_tutor.mjs` 的 README 守护按 v21.7 惯例去硬化（其写死的「冒烟二十六件套/二十五件套清除」在本次件数递增时必然失败；实件数由 v21.31 冒烟守护 README「二十七件套」口径）。
- 验证：`node --check js/data.js js/view/menus.js tests/smoke_v2131_helptitle.mjs tests/smoke_v2130_tutor.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 二十七件套全绿（既有 26 件逐项零回归 + 新增 v21.31 **26 项断言全过**——GAME_VERSION 格式合法且越过 v21.30、`data.js GAME_VERSION === 'v21.31'`、HELP_TITLES 已导出且长度与 HELP_PAGES 相等、四页标题逐字 操作说明/地图指南/魔物状态/试炼进阶、标题无重复、menus.js 已 import HELP_TITLES、drawHelp 面板标题读 `HELP_TITLES[S.helpPage]`（含兜底原串）、旧硬编码 `'— 操作说明 —'` 字面量已从 drawHelp 移除、menus.js/data.js 含 v21.31 注释、运行期实证：DOM 桩 + main.js 真实导入后逐页 `drawHelp()` 四页面板标题分别为 `— 操作说明 —/— 地图指南 —/— 魔物状态 —/— 试炼进阶 —` 且各自页脚「第 X/4 页」、四页行数 14/5/10/9 零回归、既有行关键词抽样全在（对话/确认、存档槽、星井矿脉、通关之路、战斗掉落、魔物强度、试炼三连战、终焉之神）、README 已同步（smoke_v2131_helptitle + 二十七件套口径、二十六件套清除）、package.json 已收录 smoke_v2131_helptitle、smoke_v2130 README 守护表达式已去硬化（不再以「二十六件套」断言件数）。

## v21.30 新手教程提示补 E 键——「💡 教程」行 Enter对话 → Enter/E对话（体验打磨·可发现性·口径收尾，承 v21.29 大地图 E 键交互别名：v21.29 自述「README 快速上手表、H 页操作说明、新手教程提示三处都只写 Enter」，但只改了前两处——教程行仍是「交互只有一个入口」的最后一处漏网）

- 【漏网：教程行只写 Enter】`v21.29` 引入大地图 E 与 Enter 同效交互后，README 快速上手表与 H 页操作说明「对话 / 确认」行都同步为 `Enter / E`；但 `main.js` `screens.story.onKey` 首次进图的「💡 教程：WASD移动 · Enter对话 · Esc菜单 · …」提示行（唯一一处教程引导，`TUTOR_MSG_MS` 时长档）仍是 `Enter对话`——v21.29 的 changelog 自己把「新手教程提示」列为三处只写 Enter 之一，实际改掉的只有两处；按「功能存在就必须能看到入口」（v21.14/v21.18）与「同一功能三处文档口径一致」的既有主线，这是入口口径的最后一块漏网。
- 【修正：教程行改 Enter/E对话，零逻辑变化】`main.js` 教程提示行由 `Enter对话` 改为 `Enter/E对话`（其余按键提示 WASD/Esc/P/F/I/J/B/C/T/H/M 逐字不动）；`TUTOR_MSG_MS` 常量与 `tutDone` 触发机制（首次进 world 弹一次、此后改发「踏上旅途！」）逐字未动——纯文案二字之差 + v21.30 注释。`data.js` `GAME_VERSION` v21.29→v21.30。
- 【零回归面】未动任何交互/世界/战斗/掉落/任务/存档/遇敌逻辑与数值（interact/world.onKey/HELP_PAGES/README 快速上手表逐字不动——v21.29 口径由本版冒烟零回归守护）；未动 draw*/HUD/音频；教程行其余提示按位保持。
- 只改 2 个游戏文件（`main.js`：教程行 + 注释；`data.js`：GAME_VERSION + 注释）+ `package.json`（test 串第 26 份）+ `README.md`（tests/ 目录树「冒烟二十六件套」、二十五件套清除、系统说明追加 v21.30 守护描述）+ 新增 `tests/smoke_v2130_tutor.mjs`（仓库常驻，承 v21.10 起冒烟入库先例）+ 顺带把 `tests/smoke_v2129_ekey.mjs` 的 README 守护按 v21.7 惯例去硬化（其写死的「二十五件套/二十四件套」在本次件数递增时必然失败；实件数由 v21.30 冒烟守护 README「二十六件套」口径）+ `CHANGELOG.md`：本条。
- 验证：`node --check js/main.js js/data.js tests/smoke_v2129_ekey.mjs tests/smoke_v2130_tutor.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 二十六件套全绿（既有 25 件逐项零回归 + 新增 v21.30 **19 项断言全过**——GAME_VERSION 格式合法且越过 v21.29 并逐字等于 'v21.30'、main.js 教程行已含 `Enter/E对话` 且旧口径片段（WASD移动 · Enter对话 · Esc菜单）清零、教程行仍以 TUTOR_MSG_MS 收尾（常量沿用、无裸字面量）、main.js/data.js 含 v21.30 注释、tutDone 触发守卫逐字保留、运行期实证：story 收尾进 world 后 `#msg` 文本以「💡 教程」开头且含 `Enter/E对话`、不含旧口径、S.scene==='world' 且 S.G.tutDone 置位、二次触发改发「踏上旅途」欢迎语（教程不再重复）、v21.29 口径零回归（H 页「对话 / 确认」行仍 `Enter / E（镇民需面对面）`、KEY 无 'e' 映射）、README 已同步（smoke_v2130_tutor + 二十六件套口径、二十五件套清除）、package.json 已收录 smoke_v2130_tutor、smoke_v2129 README 守护表达式已去硬化（不再以「二十五件套」断言件数）。

## v21.29 大地图新增 E 键交互别名——E 与 Enter 同效调用 interact，对话/商店/旅馆/酿造/祭坛/传送门一按即达（体验打磨·可发现性，承 v21.14/v21.18「功能存在就必须能看到入口」主线：交互此前只有 Enter 一个入口——README 快速上手表、H 页操作说明、新手教程提示三处都只写 Enter，而 WASD 移动的玩家右手驻留方向键、左手去按 Enter 是键盘上最远的组合；E 是 JRPG 通行的交互键位（「确认在 E」的肌肉记忆），大地图没有第二交互入口）

- 【交互入口是单键：Enter 一处，且是最远的组合】`main.js` `screens.world.onKey` 的交互分派只有 `e.key === 'Enter'` 一行——对话（NPC/守碑人/灯长…）、商店/旅馆/酿造、祭坛开战、传送门/出口、试炼碑全走这一个键；而移动是 WASD（左手）与方向键（右手）双支持，交互却要求右手从方向键横穿键盘去按 Enter——JRPG 玩家从《宝可梦》到《传说》系列都习惯「面向后按 E/Z 确认」，本作交互键与移动键的最优手位冲突，且是三处文档（README/H 页/教程）从未提过可替代键的第一处单入口功能面（与 v20.3 快捷键可发现性/v21.14 战斗日志回看同族：入口存在但只有一条路）。
- 【修正：world.onKey E 分支 + H 页与 README 同口径】`main.js` `screens.world.onKey` 在 Enter 行后新增 `if (e.key === 'e' || e.key === 'E') { interact(); return; }`——与 Enter 完全同路径（interact 内部对 NPC/STELE/BREW/SHOP/INN/GATE/EXIT/BOSS 的分派逐字不动，纯新入口）；`data.js` `HELP_PAGES[0]`「对话 / 确认」行由 `Enter（镇民需面对面）` 改为 `Enter / E（镇民需面对面）`（行数不变仍 14 行、sp=25 长页档；行宽官方 estW 口径实测 ≈226.8 ≤470 面板预算（追加前 ≈207.0）），`README.md` 快速上手表 Enter 行同步为 `Enter / E`（大地图 E 与 Enter 同效…）；`KEY` 无 `'e'` 映射（不与移动/其他键冲突），教程提示/战斗/商店/标题等其余场景按键分派逐字不动。
- 【零回归面】未动任何交互/世界/战斗/掉落/任务/存档/遇敌逻辑与数值（interact/useGate/useExit/onStep/openTalk 逐字不动）；未动 drawWorld/drawBattle/菜单渲染；未动 HELP_PAGES 其余 13 行与其余三页（操作说明页行宽由本版冒烟巡检守护）；`GAME_VERSION` v21.28→v21.29。只改 3 个游戏/文档文件（`js/main.js`：world.onKey E 分支 + 注释；`js/data.js`：GAME_VERSION + H 页「对话 / 确认」行 + 注释；`README.md`：tests/ 目录树与「冒烟二十五件套」两处同步、二十四件套清除 + 快速上手表 Enter 行改 `Enter / E`、v21.29 守护描述）+ `package.json`（test 串第 25 份）+ 新增 `tests/smoke_v2129_ekey.mjs`（仓库常驻，承 v21.10-v21.28 冒烟入库先例）+ 顺带把 `tests/smoke_v2128_mapstele.mjs` 的 README 守护按 v21.7 惯例去硬化（其写死的「二十四件套」在本次件数递增时必然失败；实件数由 v21.29 冒烟守护 README「二十五件套」口径）+ `CHANGELOG.md`：本条。
- 验证：`node --check js/main.js js/data.js tests/smoke_v2128_mapstele.mjs tests/smoke_v2129_ekey.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 二十五件套全绿（既有 24 件逐项零回归 + 新增 v21.29 **16 项断言全过**——GAME_VERSION 单调越过 v21.28、main.js world.onKey 含 `'e' || 'E' → interact` 分支（源级落位）与 v21.29 注释、KEY 无 'e' 映射、运行期实证：DOM 桩 + main.js 真实导入后 `screens.world.onKey({key:'E'})` 与 `{key:'e'}` 与 `{key:'Enter'}` 三者均触发 interact（和平村 (10,15) 面向上方路径格 → bind.boxMsg 「这里没什么特别的」逐次命中）、Enter 行为逐字保留、world.onKey("I") 仍进状态页（其余键零回归）、帮助页操作说明「对话 / 确认」行存在且文案逐字等于 `Enter / E（镇民需面对面）`、该行 estW ≈226.8 ≤470、data.js 含 v21.29 注释、README 已同步（smoke_v2129_ekey + 二十五件套口径、二十四件套清除 + `Enter` / `E` 行）、package.json 已收录 smoke_v2129_ekey、smoke_v2128 README 守护表达式已去硬化（不再以「二十四件套」断言件数）。

## v21.28 帮助页「地图指南」星井矿脉行补试炼碑入口——「试炼碑（可问守碑人）」走进战前知识中枢的地图层（体验打磨·信息透明·可发现性，承 v21.24 守碑人 / v21.27 试炼碑位置行守碑人指路同一主线：H 页四页是战前知识中枢（v21.2/v21.14/v21.15 确立），试炼碑是星井矿脉除终焉水晶外最重的可选挑战——三连战（幽冥魔王→洞窟领主→终焉之神）全游戏唯一需主动踏入的长线考验，但「地图指南」页的星井矿脉行只写「更强魔物·迷宫·中央终焉水晶·双徽记化为门」——对比同页其他三图行（潮灯镇标灯长/守书记、雾语林标魔王祭坛、无字回廊标守名者/残焰魔像/终焉之神），唯独星井矿脉行没有任何自己的地标；v21.23-v21.27 五版扫过的「试炼信息战前不可见」在「地图指南」这页仍是漏网）

- 【地图指南星井矿脉行是全图地标准入口的唯一空洞】`data.js` `HELP_PAGES[1]`（地图指南）四图行 + 通关之路，行末列了各图的关键地标/人物（镇：商店·旅馆·酿造锅·灯长/守书记(支线)·喷泉回血；林：精英·魔王祭坛·裂洞；廊：名字石碑·守名者(支线)·残焰魔像·终焉之神）——唯独星井矿脉行只有「更强魔物·迷宫·中央终焉水晶·双徽记化为门」：终焉水晶（开门机关）有名字、试炼碑（玩家主动做功课最多的挑战，v21.23-v21.27 主线主角）却只字未提；想「去不去试炼」的玩家在 H 页逐页翻：试炼进阶页（v21.27 已有守碑人指针）是第四页，而第一页地图指南的矿脉行连「试炼碑」三个字都没有——与 v21.14/v21.18 反复点名的「功能存在就必须能看到入口」是同一类入口漏网。
- 【修正：星井矿脉行描述列中部补试炼碑指针，NPC 名读单一数据源】`HELP_PAGES[1]` 星井矿脉行 r[1] 由 `更强魔物·迷宫·中央终焉水晶·双徽记化为门` 改为 `更强魔物·迷宫·试炼碑（可问` + `NPCS.sentinel.name` + `）·中央终焉水晶·双徽记化为门`——「试炼碑」地标 + v21.27 同款守碑人短指针（NPC 名派生自 `NPCS.sentinel.name`：调守碑人名字只改 data.js 一处、地图指南自动跟随，零裸字面量，与 v21.27 试炼碑位置行/v21.24 台词同族）；行数不变仍 5（不触发页长自适应），r[1] 官方 estW 口径 ≈446.6 ≤470 面板预算（追加前 ≈321.3，面板右缘 570 − 起点 100），v21.20/v21.25 全页行宽巡检自动纳入；纯显示零结算，drawHelp 渲染路径逐字未动。
- 【零回归面】未动任何试炼/战斗/掉落/任务/存档/遇敌逻辑与数值（RUSH_*/SPECIES/trialSteleHint/守碑人数据与台词逐字不动）；未动 HELP_PAGES[0]/[2]/[3] 与地图指南页其余 4 行（防误动由本版冒烟关键词守护）；未动 drawHelp/drawWorld/README 既有口径之外任何文件内逻辑；`GAME_VERSION` v21.27→v21.28。只改 3 个游戏/文档文件（`data.js`：GAME_VERSION + 地图指南星井矿脉行派生指针 + 注释；`README.md`：tests/ 目录树与「冒烟二十四件套」两处同步、二十三件套清除 + 系统清单地图指南句补 v21.28 口径）+ `package.json`（test 串第 24 份）+ 新增 `tests/smoke_v2128_mapstele.mjs`（仓库常驻，承 v21.10-v21.27 冒烟入库先例）+ 顺带把 `tests/smoke_v2127_stelenpc.mjs` 的 README 守护按 v21.7 惯例去硬化（其写死的「二十三件套」在本次件数递增时必然失败；实件数由 v21.28 冒烟守护 README「二十四件套」口径）+ `CHANGELOG.md`：本条。
- 验证：`node --check js/data.js tests/smoke_v2127_stelenpc.mjs tests/smoke_v2128_mapstele.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 二十四件套全绿（既有 23 件逐项零回归 + 新增 v21.28 **20 项断言全过**——GAME_VERSION 单调越过 v21.27、帮助页仍 4 页、地图指南页仍 5 行（追加不增行）、星井矿脉行存在且以 MAPS.cave.recLv 派生（无 r[2]）、守碑人 NPC 定义契约（name==='守碑人'）、r[1] 与「旧文案 + NPCS.sentinel.name 派生指针」逐字相等（测试侧独立复算非快照）、r[1] 含试炼碑与「可问+NPC 名」指针、旧口径关键词（更强魔物/迷宫/中央终焉水晶/双徽记化为门）全在、行宽 ≈446.6 ≤470、全页 5 行行宽巡检通过、其余 4 行关键词全在（镇商店/旅馆/酿造锅/灯长/守书记/喷泉、林强魔物/精英/魔王祭坛/裂洞、廊名字石碑/守名者/残焰魔像/终焉之神/极高难、通关之路讨回灯芯/洞窟领主/双徽记开门/终焉之神）、其余三页行数未动（14/10/9）、试炼进阶页「试炼碑位置」行 v21.27 指针保留、data.js 含 v21.28 注释与 `NPCS.sentinel.name` 派生式（零裸字面量）、README 已同步（smoke_v2128_mapstele + 冒烟二十四件套口径、二十三件套清除 + 地图指南句 v21.28 口径）、package.json 已收录 smoke_v2128_mapstele、smoke_v2127 README 守护表达式已去硬化（不再以「二十三件套」断言件数）。



- 【H 页四页无守碑人：碑前答疑者不在战前知识簿】`data.js` `HELP_PAGES[3]`（试炼进阶）首行「试炼碑位置」只写 `星井矿脉中央的蓝色石碑 —— 先凑齐两枚胜利徽记`；v21.24 的守碑人（`NPCS.sentinel`，`npcQuestPages` 函数型台词调用期求值、trueBoss 后彩蛋）在 README 星井矿脉条目有记录、游戏内却只活在 (17,12) 那格——玩家在镇里/雾语林规划「要不要练级去试炼」时，帮助页查不到任何「到碑前有事可以问人」的线索；对比同类地标（祭坛有雾径猎手、水晶有井巫、回廊有守名者均入 H 页地图指南/README 口径），这是守碑人信息在「战前知识中枢」的唯一缺口。
- 【修正：试炼碑位置行末尾补短指针，NPC 名读单一数据源】`HELP_PAGES[3][0]` 的 r[1] 由 `星井矿脉中央的蓝色石碑 —— 先凑齐两枚胜利徽记` 改为 `…（可问` + `NPCS.sentinel.name` + `）`——名字派生自 `NPCS.sentinel.name`（调 NPC 名只改 data.js 一处、帮助页自动跟随，零裸字面量，与 v21.24 台词/v21.26 徽记名同族）；行数不变仍 9（不触发 v19.59「≤10 行 sp=34 档」页长自适应、r[2] 仍 2 个 → 末行基线 80+9×34+2×16=418 不触页脚 452）；r[1] 官方 estW 口径 ≈421（追加前 ≈358）≤470 面板预算（起点 100 → 右缘 570），v21.20/v21.23 全页行宽巡检自动纳入。纯显示零结算，`drawHelp` 渲染路径逐字未动。
- 【零回归面】未动任何试炼/战斗/掉落/任务/存档/遇敌逻辑与数值（RUSH_*/SPECIES/trialSteleHint/守碑人数据与台词逐字不动）；未动 HELP_PAGES[0]/[1]/[2] 与试炼进阶页其余 8 行（防误动由本版冒烟关键词守护）；未动 drawHelp/drawTitle/README 既有口径之外任何文件内逻辑；`GAME_VERSION` v21.26→v21.27。只改 3 个游戏/文档文件（`data.js`：GAME_VERSION + 试炼碑位置行派生指针 + 注释；`README.md`：tests/ 目录树与「冒烟二十三件套」两处同步、二十二件套清除 + 系统清单试炼碑句补 v21.27 口径）+ `package.json`（test 串第 23 份）+ 新增 `tests/smoke_v2127_stelenpc.mjs`（仓库常驻，承 v21.10-v21.26 冒烟入库先例）+ 顺带把 `tests/smoke_v2126_trialstep.mjs` 的 README 守护按 v21.7 惯例去硬化（其写死的「二十二件套」在本次件数递增时必然失败；实件数由 v21.27 冒烟守护 README「二十三件套」口径）+ `CHANGELOG.md`：本条。
- 验证：`node --check js/data.js tests/smoke_v2126_trialstep.mjs tests/smoke_v2127_stelenpc.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 二十三件套全绿（既有 22 件逐项零回归 + 新增 v21.27 **19 项断言全过**——GAME_VERSION 单调越过 v21.26、帮助页仍 4 页、试炼进阶页仍 9 行（追加不增行）、试炼碑位置行存在且无 r[2]、守碑人定义契约（name==='守碑人'/lines 为函数）、r[1] 与「旧文案 + NPCS.sentinel.name 派生指针」逐字相等（测试侧独立复算非快照）、旧口径关键词（星井矿脉/蓝色石碑/先凑齐两枚胜利徽记）全在、行宽 ≈421 ≤470、全页 9 行行宽巡检通过、r[2] 仍 2 个、末行基线 418 ≤440、其余 8 行关键词全在（双徽记/试炼三连战/重整旗鼓/快旅/蘑菇/Boss 三行）、其余三页行数未动（14/5/10）、data.js 含 v21.27 注释与 `NPCS.sentinel.name` 派生式（零裸字面量）、README 已同步（smoke_v2127_stelenpc + 冒烟二十三件套口径、二十二件套清除 + 试炼碑句 v21.27 口径）、package.json 已收录 smoke_v2127_stelenpc、smoke_v2126 README 守护表达式已去硬化（不再以「二十二件套」断言件数）。

## v21.26 试炼碑未解锁提示按徽记进度实时报数——先败其一 Boss 后再踩碑，一眼看清「已得/所差」（体验打磨·信息透明·进度即时，承 v19.72 宝箱计数 / v21.22 宝箱双口径 / v21.25 试炼赏金公式同主线：双徽记是试炼碑的门槛（「先凑齐两枚徽记」），但进度本身——「我已经拿到几枚、还差哪一枚」——此前只活在 I 状态页五徽记（✓/✗ 两格）与 J 日志里，玩家站到碑前，唯一的当面反馈是一句静态「…试炼碑需要两枚徽记（幽冥魔王 + 洞窟领主）。」：先击败幽冥魔王（或先打洞窟领主）的玩家回头踩碑，提示句依旧不差分毫，仿佛这趟白打；「进度实时」是 宝箱计数/任务进度/记忆碎片 3/4 全家桶里唯一没接进试炼碑提示的一格）

- 【碑前提示是「黑盒」：不报已得与所差】`world.js` `onTrialStele` 在未集齐双徽记时无条件输出同一句静态文案（v19.x 起逐字未动）：0 枚与 1 枚的玩家看到的是完全相同的提示——信息含量为零的「阈值文案」；对比同屏信息透明体系（宝箱「已开 X/全图 N · 成就 X/M」、蘑菇支线「当前 X/3 株」、记忆碎片「N/4 段已集齐」），这是进度计数唯一没进「条件消息」的入口（I 页五徽记虽是同源数据，但要求玩家离开碑前自查——「信息透明」主线的反面：信息存在，但不在决策点）。
- 【修正：data.js 新增纯函数 trialSteleHint(hero)，world.js 未解锁分支改调它】`data.js` 新增 `trialSteleHint`（导出、纯函数、零副作用、调用期求值，与 sentinelPages/chestTotal 同族）：按 `hero.bossDefeated` / `hero.caveBoss` 派生「已得徽记」列表——0 枚 → `…试炼碑需要两枚徽记（幽冥魔王 + 洞窟领主）。`（与原文案逐字一致，零显示变化）；1 枚 → `…还差一枚徽记：你已得【X】的徽记，去击败【Y】吧。（1/2）`（X/Y 由 have 列表反推，互斥不重复，附 (1/2) 实时计数）；双徽记 → `''`（契约兜底，调用方 startRush 分支不引用）。两位 Boss 名全读 `BOSS.name` / `CAVE_BOSS.name` 单一数据源——调 Boss 名只改 data.js 一处，提示自动跟随，绝无第二套口径（零裸字面量，与 v21.24 守碑人台词 / v21.23 phaseBoost 同族）。`world.js` `onTrialStele` 改为 `else bind.boxMsg(trialSteleHint(hero), NARR_MSG_MS)`：startRush 分支（双徽记判定）、水晶/祭坛/开箱/遇敌/传送逐字未动。
- 【零回归面】未动任何试炼/战斗/掉落/任务/存档/遇敌逻辑与数值（RUSH_* / SPECIES / MAPS / 徽记判定 逐字不动）；未动 H 页「试炼碑位置」行与 README 既有口径（仅补一句）；未动守碑人台词/成就/五徽记；`GAME_VERSION` v21.25→v21.26。只改 3 个游戏/文档文件（`data.js`：GAME_VERSION + trialSteleHint 纯函数 + 导出 + 注释；`world.js`：onTrialStele 未解锁分支 1 行 + import + 注释；`README.md`：tests/ 目录树与「冒烟二十二件套」两处同步、二十一件套清除 + 系统清单「试炼碑状态标签」句补 v21.26 口径）+ `package.json`（test 串第 22 份）+ 新增 `tests/smoke_v2126_trialstep.mjs`（仓库常驻，承 v21.10-v21.25 冒烟入库先例）+ 顺带把 `tests/smoke_v2125_rushreward.mjs` 的 README 守护按 v21.7 惯例去硬化（其写死的「二十一件套/二十件套」在本次件数递增时必然失败；实件数由 v21.26 冒烟守护 README「二十二件套」口径）+ `CHANGELOG.md`：本条。
- 验证：`node --check js/data.js js/world.js tests/smoke_v2125_rushreward.mjs tests/smoke_v2126_trialstep.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 二十二件套全绿（既有 21 件逐项零回归 + 新增 v21.26 **19 项断言全过**——GAME_VERSION 单调越过 v21.25、trialSteleHint 导出为纯函数、0 枚（空对象/hero 缺失/无旗标字段）三路径均与原文案逐字一致、0 枚派生式与 BOSS.name/CAVE_BOSS.name 逐字相等、已得魔王→「已得【幽冥魔王】/去击败【洞窟领主】/（1/2）」、已得领主→反向、双徽记→'' 契约、1 枚分支互斥（不含反向往文案）、data.js 含 v21.26 注释与 `BOSS.name + ' + ' + CAVE_BOSS.name` 派生表达式（零裸字面量）、world.js 已 import trialSteleHint 且未解锁分支改调它、旧静态文案字面量已从 world.js 移除、startRush 双徽记判定逐字未动、README 已同步（smoke_v2126_trialstep + 二十二件套口径、二十一件套清除 + 试炼碑状态标签句 v21.26 口径）、package.json 已收录 smoke_v2126_trialstep、smoke_v2125 README 守护已去硬化）。

## v21.25 帮助页「试炼进阶」试炼三连战行补通关赏金——「150+等级×20金」从 NPC/成就页/战斗预览走进战前知识中枢（体验打磨·信息透明，承 v21.20 试炼三连战行 r[2] 拆分 / v21.23 三 Boss 机制预览 / v21.24 守碑人同主线：试炼「值不值得打」的三块信息——恢复比例（v21.20 已进 H 页）、三 Boss 机制（v21.23 已进 H 页）、通关赏金——唯独赏金仍只活在 守碑人台词（调用期按 hero.level 报数）/成就页 C「百炼成钢」奖励行/战斗画面临时预览（README 战斗段「试炼通关另奖 N 金币」）三处，H 页四页战前翻遍查不到；玩家在碑前决定「要不要现在挑战」的最后一个未知数，正是这趟打完拿多少金）

- 【试炼赏金是「战前该知道的事」，却只活在站到碑前之后/游戏外】`rules.rushReward` 的 `RUSH_BASE_GOLD + lv*RUSH_GOLD_PER_LV`（150+等级×20）在游戏内共有三个可见口径：v21.24 守碑人（玩家已到碑前）、成就一览 C（要按 C 专门去查，且成就行标注的顺序在「百炼成钢」条目内）、战斗横幅的「试炼通关另奖 N 金币」（已开打）。「试炼值不值得打、要不要先攒一波等级再来」这个决策点只差最后一块拼图——H 页「试炼进阶」作为战前知识中枢（v21.2/v21.14/v21.15/v21.23 逐版确立的职能），同页已把恢复比例（v21.20）与三 Boss 机制（v21.23）讲清，唯独「全胜另得 X 金」这一格缺席——与「信息透明」主线（v19.62 成就奖励揭示 / v19.83-v19.85 各收入入口余额反馈）同族的最后一块黑盒。
- 【修正：试炼三连战行 r[2] 追加赏金公式，全部由 RUSH_* 常量派生】`data.js` `HELP_PAGES[3]` 试炼三连战行 r[2]（12px 次级灰）由 `每胜一关回血35%HP/50%MP` 追加为 `每胜一关回血${Math.round(RUSH_RECOVER.hp*100)}%HP/${Math.round(RUSH_RECOVER.mp*100)}%MP · 全胜另得${RUSH_BASE_GOLD}+等级×${RUSH_GOLD_PER_LV}金`——与 rules.rushReward / 守碑人 sentinelPages / 成就页 a.r 同一真源，调试炼赏金只改 data.js 一处、四处同步，绝无第二套口径（零裸字面量，公式表述与战斗预览「另奖 N 金币一致」同源）；r[1] 主行（连战/全胜/百炼成钢）逐字不动、行数不变仍 9（r[2] 追加不增行，drawHelp sp=34 页长档不变，末行基线 80+9*34+2*16=418 不触页脚 452）；官方 estW 口径 r[2] ≈276.5 ≤470 面板预算（原 ≈142.8，追加 ≈133.7）；「等级×」随等级表达式让玩家自行代入当前等级（H 页无 hero 上下文，与 v21.23 公式式行同款）。
- 【零回归面】未动任何试炼/战斗/掉落/任务/存档/遇敌逻辑与数值（RUSH_RECOVER/RUSH_BASE_GOLD/RUSH_GOLD_PER_LV/winBattle 连胜结算/成就判定逐字不动）；未动 HELP_PAGES[3] 其余 8 行与其余三页（逐字不动由本版冒烟关键词守护）；未动 drawHelp 渲染（r[2] 通路/sp 自适应/页脚）；`GAME_VERSION` v21.24→v21.25。只改 2 个游戏/文档文件（`data.js`：GAME_VERSION + 试炼三连战行 r[2] 派生串 + 注释；`README.md`：tests/ 目录树与「冒烟二十一件套」两处同步、二十件套清除 + v21.25 守护描述）+ `package.json`（test 串第 21 份）+ 新增 `tests/smoke_v2125_rushreward.mjs`（仓库常驻，承 v21.10-v21.24 冒烟入库先例）+ 顺带把 `tests/smoke_v2120_helprush.mjs` 与 `tests/smoke_v2123_bosshelp.mjs` 两处「试炼三连战 r[2] 逐字相等」断言同步为 v21.25 新串（r[2] 内容随版本演进是预期事件，其余断言逐字不变、自动适配）+ `CHANGELOG.md`：本条。
- 验证：`node --check js/data.js tests/smoke_v2120_helprush.mjs tests/smoke_v2123_bosshelp.mjs tests/smoke_v2125_rushreward.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 二十一件套全绿（既有 20 件逐项零回归 + 新增 v21.25 **21 项断言全过**——GAME_VERSION 单调越过 v21.24、帮助页仍 4 页、试炼进阶页仍 9 行（r[2] 追加不增行）、试炼三连战行 r[1]+r[2] 结构、RUSH 常量真源健全（恢复/赏金均为正）、r[1] 主行逐字未动且不含赏金/回血、r[2] 与 RUSH_RECOVER+RUSH_BASE_GOLD/RUSH_GOLD_PER_LV 派生串逐字相等（35%HP/50%MP · 150+等级×20金，测试侧独立复算非快照）、r[2] 含「全胜另得/等级×」公式表述、data.js 源码含 RUSH_BASE_GOLD 派生表达式与 v21.25 注释（零裸字面量）、r[1] ≈299.8 / r[2] ≈276.5 均 ≤470、试炼进阶页全 9 行行宽巡检 + 地图指南页巡检全过、r[2] 仍 2 个（不增行）、末行基线 418 ≤440、其余 8 行关键词全在（含 v21.23 三 Boss 行）、其余三页行数未动（14/5/10）、README 已同步（smoke_v2125_rushreward + 二十一件套口径、二十件套清除）、package.json 已收录 smoke_v2125_rushreward）。

## v21.24 星井矿脉试炼碑旁新增 NPC「守碑人」——全图四大关键地标最后一个没有人在场守望的空洞填上（内容扩充·战前知识中枢补位，承 v21.17 老矿工「地图事实讲解员」模式 / v21.23 帮助页试炼进阶 Boss 机制预览主线：四大地标——镇广场（灯长/镇民）、雾语林祭坛（雾径猎手/巡灯人）、终焉水晶（井巫/老矿工/星砂车夫）、无字回廊（守名者）——旁各有 NPC 指路，唯独星井矿脉的试炼碑只靠 H 页「试炼进阶」与碑上刻字；玩家在碑前决定「要不要现在挑战」时，唯一能问的人只有碑自己）

- 【空洞：试炼碑旁没有「人」】试炼场是全游戏最重的考验（三连战：幽冥魔王→洞窟领主→终焉之神），战前知识中枢 H 页（v21.23 三 Boss 机制预览）已经把规则写在纸上，但「碑前做决定」那一刻身边没有人物——与「这是个每个关键地标都有人在场的 JRPG」的既有面貌（v21.17 老矿工的矿脉生存指南、守名者/猎手/巡灯人各守一隅）相比，试炼碑是全图唯一无人守望的关键交互点。
- 【修正：data.js 新增 sentinel NPC 数据 + 函数型台词，全部由试炼数值唯一真源派生】`NPC_SPOTS` 新增 `'17,12':'sentinel'`（全局坐标键，未与他图任何键/占用撞车——17,11 为星砂车夫、左右不撞）；`MAPS.cave.extras` 新增 `{x:17,y:12,ty:'NPC'}`（试炼碑 TRIAL 18,12 正西一格、双侧 16,12/18,13 均可站人，不挡碑路）；`NPCS.sentinel={name:'守碑人', mark:'staff', lines: sentinelPages, after:[…]}`——`sentinelPages(hero)` 为新增纯函数（data.js，调用期求值）：三连战阵容 `RUSH_BOSSES.map(b=>b.name)`、关间恢复 `${RUSH_RECOVER.hp*100}%HP/${mp}%MP`、通关赏金 `RUSH_BASE_GOLD + lv*RUSH_GOLD_PER_LV`（随 hero.level 实时重算，与 rules.rushReward 同式）——调任何试炼数值只改 data.js 对应常量一处，碑旁对话自动跟随，绝无第二套口径（与 HELP_PAGES 派生 / v21.23 phaseBoost 同族）；`quests.js` npcQuestPages 新增一处分支：`typeof ent.lines === 'function'` → 调用期求值（静态 NPC 数组直返路径逐字不动）；`view/sprites.js` NPC_SHEET 增 `sentinel:'mwSage'`（与守名者同款长袍剪影，跨图不重名）。台词 2 页：页 1 讲碑刻/恢复/赏金/双徽记前提（并指路 H 页「试炼进阶」），页 2 按三 Boss 机制提示（真身/石甲/封印治愈，与 v21.23 H 页同源口径）+「灵药省着点」实战忠告；trueBoss 后 after 彩蛋 2 页（npcQuestPages 既有契约 after > lines 自动生效）。
- 【零回归面】未动任何战斗/试炼/掉落/任务/存档/遇敌逻辑与数值（RUSH_RECOVER/RUSH_BASE_GOLD/RUSH_GOLD_PER_LV/RUSH_BOSSES/试炼碑触发 onTrialStele 逐字不动）；未动既有 NPC_SPOTS 14 键与 13 名 NPC（miner 等逐字不动）；未动 quests.js 既有分支（函数型 lines 为纯追加路径）；未动 drawWorld/drawTalk/openTalk/faceHint（全部数据驱动自动跟随）；`GAME_VERSION` v21.23→v21.24。只改 3 个游戏文件（`data.js`：GAME_VERSION + NPC_SPOTS + MAPS.cave.extras + NPCS.sentinel + sentinelPages 函数 + 注释；`quests.js`：npcQuestPages 函数型分支；`view/sprites.js`：NPC_SHEET 一行）+ `package.json`（test 串第 20 份）+ `README.md`（星井矿脉条目 / 系统清单 NPC 名单 / tests 目录树与「冒烟二十件套」两处同步）+ 新增 `tests/smoke_v2124_sentinel.mjs`（仓库常驻，承 v21.10-v21.23 冒烟入库先例）+ `CHANGELOG.md`：本条。
- 验证：`node --check js/data.js js/quests.js js/view/sprites.js tests/smoke_v2124_sentinel.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 二十件套全绿（既有 19 件逐项零回归 + 新增 v21.24 **26 项断言全过**——GAME_VERSION 单调越过 v21.23、NPC_SPOTS['17,12']==='sentinel' 且仅一键、既有 14 键未动、MAPS.cave.extras 含 (17,12) NPC 且既有 5 项未动、(17,12) 仅 cave 一处放 NPC（他图同坐标无占用）、试炼碑仍在 (18,12)、两侧站位可行走、sentinel 名称/lines 函数/after 2 页、三连战阵容与 RUSH_BOSSES 同源、恢复 35%HP/50%MP 与 RUSH_RECOVER 同源、Lv.10 赏金 350 金与公式同式、Lv.12 实时重算 390 金、空 hero 兜底 170 金、页 2 三 Boss 机制提示、trueBoss 彩蛋 2 页、villager 静态路径/miner linesByStage/未知 NPC 契约零回归、quests.js 函数型分支与 sprites.js 造型映射源码级落位）。

## v21.23 帮助页「试炼进阶」补三 Boss 机制预览——幽冥魔王/洞窟领主/终焉之神 的 变身/石甲/封印治愈 从「只在战斗里可见」走进战前知识中枢（体验打磨·信息透明，承 v21.2 精英出没预览 / v21.14 战斗日志回看 / v21.15 高级灵药配方同主线：三 Boss 的机制此前只以战斗内角标出现（二段变身线 ·HP<130、「变身：攻+7 防+3 回15%」、⛔ 治愈封印、敌方招数一览——v19.x/v20.x 逐版加的战斗内预判），但帮助页作为「战前知识中枢」四页都查不到——玩家第一次面对 幽冥魔王/洞窟领主/终焉之神 前，无从预习「血过半会现真身+回血」「洞窟领主要防石甲」「终焉之神祸乱形态封印治愈」，与 v21.2「两种精英的出没/守门信息」同款「战前预告缺页」结构）

- 【Boss 机制是战前该知道的事，却只活在战斗画面里】`drawBattle` 常驻的 变身线/变身增益/敌方招数一览 与 `enemyAI` 的变身结算（phase2）把 攻防暴涨、回血比例、石甲、封印治愈 全部揭示给「已进战斗」的玩家——但战前（H 页）只有 石心魔像/残焰魔像 两条精英机制行（v21.2/v21.11），三位 主线+试炼 Boss 的机制在四页帮助页零提及；玩家在 祭坛/试炼碑 前做功课（该带几瓶药、该蓄力还是防御、灵药要不要省着）时，唯一的信息源是 README（游戏外）。与「H 页是战前知识中枢、功能/数值存在就必须能在 H 页看到」主线（v21.11/v21.14/v21.15）的漏网结构一致。
- 【修正：试炼进阶页新增 3 行，全部由 SPECIES.phase2/acts 派生】`data.js` 新增 `BOSS_P2`（三 Boss 的 phase2 引用表）/ `BOSS_SHIELD_MAX`（洞窟领主 acts 中 shield 招的 maxShield）/ `BOSS_TRUE_FORBID`（终焉之神 forbid 是否含 heal）/ `phaseBoost(p)`（`攻+X 防+Y 回血Z%HP` 派生串，与 enemyAI 变身结算、drawBattle「变身：攻…」逐字同式）四个本文件内常量（不导出，与 GOLEM_SHIELD_ACT 同族）；`HELP_PAGES[3]`（试炼进阶）末尾追加三行：`幽冥魔王  血量过半现出真身：攻+7 防+3 回血15%HP`、`洞窟领主  石甲（至多2层）· 血量过半现出真身：攻+5 防+2 回血10%HP`、`终焉之神  血量过半进入祸乱形态：攻+7 防+3 回血15%HP`（r[2] 次行 `祸乱形态封印治愈技能（喝药不受影响）`——「喝药不受影响」与 battle.doItem 无 forbid 检查逐字吻合，封印只封治愈术类技能，battle.skillForbidden 同源）。数值全部派生、调 Boss 数据只改 data.js 一处、帮助页自动跟随，绝无第二套口径。行数 6→9（仍 ≤10 → drawHelp 的 sp=34 页长自适应档不变；r[2] 仅 试炼三连战+终焉之神 两行 → 末行基线 80+9×34+2×16=418，不触页脚 452）；官方 estW 口径三行 r[1] 实测 301.7/403.3/326.0 ≤470、r[2] 186.8 ≤470（最宽 洞窟领主 ≈403，面板右缘 570 − 起点 100 = 470 预算内）。纯显示零结算：未动 SPECIES/acts/phase2 任何数值、未动 enemyAI/battle/规则、未动其余三页与试炼进阶页原 6 行。
- 【零回归面】未动任何战斗/变身/石甲/封印/掉落/任务/存档/遇敌逻辑与数值（SPECIES 三 Boss 定义逐字不动）；未动 HELP_PAGES[0]/[1]/[2] 与试炼进阶页原 6 行（试炼碑位置/双徽记条件/试炼三连战 r[1]+r[2]/重整旗鼓/快速旅行/蘑菇宝箱 逐字不动）；未动 drawHelp 渲染（sp 自适应/r[2] 通路/页脚）与 GAME_VERSION 以外任何 data.js 常量；`GAME_VERSION` v21.22→v21.23。只改 2 个游戏/文档文件（`data.js`：GAME_VERSION + BOSS_P2/BOSS_SHIELD_MAX/BOSS_TRUE_FORBID/phaseBoost 派生常量 + 帮助页 3 行 + 注释；`README.md`：系统清单「试炼进阶」句 + tests/ 目录树与「冒烟十九件套」两处同步、十八件套清除）+ `package.json`（test 串第 19 份）+ 新增 `tests/smoke_v2123_bosshelp.mjs`（仓库常驻，承 v21.10-v21.22 冒烟入库先例）+ 顺带把 `tests/smoke_v2120_helprush.mjs` 的「试炼进阶页仍 6 行」断言同步为 9 行（v21.23 起行数递增是预期事件，行宽巡检自动覆盖新行）、把 `tests/smoke_v2122_chesttotal.mjs` 的 README 守护按 v21.7 惯例去硬化（其写死的「十八件套/十七件套」在本次件数递增时必然失败——v21.22 对 v2121 做过同款处理、本次轮到 v2122；实件数由 v21.23 冒烟守护 README「十九件套」口径）+ `CHANGELOG.md`：本条。
- 验证：`node --check js/data.js tests/smoke_v2120_helprush.mjs tests/smoke_v2122_chesttotal.mjs tests/smoke_v2123_bosshelp.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 十九件套全绿：**89/89 + 6/6 + 8/8 + 10/10 + 17/17 + 18/18 + 23/23 + 12/12 + 15/15 + 14/14 + 17/17 + 21/21 + 28/28 + 12/12 + 18/18 + 14/14 + 25/25 + 17/17（既有 18 件）+ 21/21（新增）**（回归不受影响——既有冒烟无 HELP_PAGES[3] 行数/Boss 机制行断言；v2120 冒烟行数断言已随 6→9 同步、其余 13 项逐字不变，其「其余 5 行关键词」守护对本版新增三行自动生效；v2122 冒烟 README 守护去硬化后其余 16 项逐字不变）；新增 v21.23 专项冒烟 21 项断言全过——GAME_VERSION 单调越过 v21.22、帮助页仍 4 页、试炼进阶页 9 行、三 Boss 行存在、三行与 SPECIES.phase2/acts 派生串逐字相等（测试侧独立复算 phaseBoost/石甲层数/forbid）、真源健全（三 Boss phase2 全定义且数值为正、洞窟领主 maxShield===2、终焉之神 forbid 含 heal）、三行 r[1]/r[2] 估算宽 ≤470、全页 9 行行宽巡检通过、drawHelp 页长派生 sp=34 且末行基线 418 ≤440、原 6 行关键词全在、试炼三连战 r[1]+r[2] 逐字未动（RUSH_RECOVER 派生 35%HP/50%MP）、其余三页行数未动（14/5/10）、data.js 含派生常量定义与「三 Boss 变身/机制预览派生」注释、README 已同步（smoke_v2123_bosshelp + 冒烟十九件套口径、十八件套清除 + 试炼进阶 Boss 机制句）、package.json 已收录 smoke_v2123_bosshelp、smoke_v2122 README 守护表达式已去硬化（不再以件数断言）。

## v21.22 全图宝箱总数数据化（chestTotal）+ 状态页宝箱计数双口径——「已开 X/全图 N · 成就 X/M」（体验打磨·信息透明/单一数据源收口，承 v19.47 小地图暖金「扫箱可规划」主线：扫箱规划此前只知道「成就开了 6 只就够」，不知道全图一共几只箱、还剩几只没开——全图总量只存在于 README 一句「全图第 12 只」与 data.js 一处旧注释「共 11 个」里，游戏内任何界面都查不到；且 v21.19 加了回廊遗物宝箱后那条注释已经脱节成错数（README 早已写 12、注释还写 11——正是本项目最忌讳的「多套口径漂移」））

- 【背景：全图宝箱总数是「裸奔数字」+ 已经脱节】`data.js` TREASURE_GOAL 的注释在 v19.47 写死「全图可开启宝箱共 11 个（潮灯镇 2 + 雾语林 3 + 星井矿脉 2 + 洞窟宝藏 4）」——此后 v21.19 在无字回廊北壁加了第 12 只遗物宝箱，README 与 H 页口径都同步到了「12」，唯独这条注释留在 11；更根本的是总数从没进过任何代码路径：状态页资源行只画 `📦 已开 X/6`（6=TREASURE_GOAL 成就阈值），玩家在游戏里任何界面都看不到「全图一共几只箱、还剩几只」，想补箱只能靠翻 README（游戏外）——与「信息透明」主线（v19.47 小地图暖金让扫箱可规划以来，规划依据却缺全图总量这一格）相悖。
- 【修正：data.js 新增 chestTotal() 单一数据源纯函数】`data.js` 新增 `chestTotal()`：逐图数 MAPS rows 中的 `'C'` 瓦片（镇2+林3+矿2+廊1=8）+ `CAVE_TREASURE` 2×2 星砂宝藏（4，领主后 revealCaveTreasure 显形）= 12，与 v21.19 冒烟守护的「十二只」口径、README「全图第 12 只」逐字恒等；此后加/删任何宝箱只改 MAPS 一处，全图总数自动跟随，绝无第二套口径（与 chestCount/TREASURE_GOAL 同一「宝箱计数数据化」体系）。TREASURE_GOAL 注释同步改写为指向 chestTotal() 的口径说明，陈年「11 个」字面量删除。
- 【修正：状态页资源行改双口径】`view/menus.js` drawStatus 资源行 `📦:${chestCount(hero)}/${TREASURE_GOAL}` 改为 `📦:${chestCount(hero)}/${chestTotal()}·成就${chestCount(hero)}/${TREASURE_GOAL}`——「已开 X/全图 12」与「成就 X/6」并列，消除「6/12 像开关 12 只」的歧义（6 是成就目标、12 是全图总数，各自与 TREASURE_GOAL/chestTotal() 同源，调任一阈值只改 data.js 一处）。14px 左起 x=110、面板右缘 570，estW 口径最宽组合（📦 12/12·成就12/6 + 金币 99999 + 时长 999:59:59）≈374px ≤460 预算，纯显示零结算零状态。
- 【零回归面】未动任何开箱/掉落/成就判定（world.onChestStep、ACH_LIST、TREASURE_GOAL=6、chestCount 逐字不动）；未动 H 页/图鉴/小地图/help 任何行；未动 MAPS/BOSS/遇敌/战斗/数值；hud.js 未动（HUD 无 📦 行，本次仅状态页）；`GAME_VERSION` v21.21→v21.22。只改 3 个游戏/文档文件（`data.js`：GAME_VERSION + chestTotal + 注释收口 + 导出；`view/menus.js`：+ import + 状态行 1 行 + 注释；`README.md`：系统清单一句 + tests 树 + 冒烟十八件套两处同步）+ 新增 `tests/smoke_v2122_chesttotal.mjs`（仓库常驻，承 v21.10-v21.21 冒烟入库先例）+ `package.json`（test 串第 18 份）+ `CHANGELOG.md`：本条。顺带把 `tests/smoke_v2121_sndpersist.mjs` 的 README 守护按 v21.7 惯例去硬化（其写死的「十七件套/十六件套」在本次件数递增时必然失败——v21.21 对 v2120 做过同款处理、本次轮到 v2121；实件数由 v21.22 冒烟守护 README「十八件套」口径）。
- 验证：`node --check js/data.js js/view/menus.js tests/smoke_v2121_sndpersist.mjs tests/smoke_v2122_chesttotal.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 十八件套全绿：**89/89 + 6/6 + 8/8 + 10/10 + 17/17 + 18/18 + 23/23 + 12/12 + 15/15 + 14/14 + 17/17 + 21/21 + 28/28 + 12/12 + 18/18 + 14/14 + 25/25（既有 17 件）+ 17/17（新增）**（回归不受影响——既有冒烟无 chestTotal/状态行 📦 分母断言；v2121 冒烟去硬化后其 README 守护从「十七件套」降级为「冒烟/件套」存在性断言，其余 24 项逐字不变；v2119 冒烟早已守护「12 只」口径，本次 chestTotal()===12 与其恒等）；新增 v21.22 专项冒烟 17 项断言全过——GAME_VERSION 单调越过 v21.21、chestTotal 导出为纯函数、逐图静态宝箱 2/3/2/1 + 星砂 4 = 12（与 v21.19「全图十二只」逐字恒等）、派生式与手工再数恒等（写死不进代码）、TREASURE_GOAL 仍 6 且 < 12 语义自洽、menus.js 已导入 chestTotal 且状态行为「📦 已开/全图·成就 X/M」双口径、旧单一口径表达式已不存在、最宽组合行宽 ≈374px ≤460 预算、data.js 陈年「宝箱共 11 个」字面量已删除、README 已同步（smoke_v2122_chesttotal + 十八件套口径、十七件套清除 + chestTotal 宝箱总数口径）、v2121 冒烟 README 守护已去硬化。

## v21.21 音频开关（M 静音）偏好持久化——静音状态刷新页面不再回到有声（体验打磨·偏好记忆，承 v21.14/v21.18「功能存在就必须能看到入口」的对称面：HUD 早已常驻 🔊/🔇 指示（v17.x）与 README 快速上手表 M 行，但偏好本身只活在内存——按 M 静音后刷新/重开页面即回到有声，指示留得住、偏好留不住；「能看」与「能记住」之间差了这一格）

- 【S.SND 是纯内存态：M 静音后刷新页面即回到有声】`state.js` `SND: true` 只作为运行时默认值，全库唯一写入点是 `main.js` 的 M 键 `S.SND = !S.SND`——无任何 localStorage 读写：玩家按 M 静音（程序化 BGM 在夜深/工作场合播放，静音是高频操作），刷新页面（或重开标签页）后 BGM 立刻回来；而 HUD 常驻 🔊/🔇（hud.js `s-snd`）与 README 都在向玩家承诺「M 是开关」，这两处却都没有「记住上次选择」的能力——与存档槽（jrpg_saveN）、标题页两按确认（TITLE_RESET_CONFIRM_MS）这类「玩家状态应该被记住」的既有先例不对称。
- 【修正：单一数据源 + 仅两个读写方 + 启动恢复】`data.js` 新增 `SND_KEY='jrpg_snd'`（键名与 jrpg_saveN 存档键同一命名族）与纯编码函数 `sndPrefToState(raw)`（`raw!=='0'`，读不到/未知值一律按默认开，与 state.js `SND:true` 同口径）/ `sndPrefToString(on)`（`'1'`/`'0'`）并导出；`audio.js` 新增 `loadSndPref()`/`saveSndPref()`（仅有的两个读写方，try/catch 包裹：localStorage 禁用/隐私模式时静默降级为内存态，绝不影响游戏运行；模块顶层零副作用——只在被调用时读写）；`main.js` 在挂键盘监听前调用 `loadSndPref()` 恢复（window 分支内，node 下不执行），M 键切换后紧接 `saveSndPref()` 落盘。行为面：新玩家无存储值 → 默认开（逐字不变）；M 以后 → 刷新/重开保持静音，HUD 🔊/🔇 指示与真实状态依旧同源（hud.js 未动）。
- 【零回归面】未动任何战斗/掉落/任务/存档/数值：state.js 仅保持 `SND:true` 默认值（未改）；audio.js 既有函数（ac/tone/SFX/MUSIC/startBgm/stopBgm/bgmTick/bgmFromScene/resumeBgm 全部原样，仅追加两个导出）；main.js 仅追加 1 行导入、1 行启动调用、1 行落盘调用 + 注释，title/world/battle 各场景按键分派逐字不动；hud.js/README 快速上手表只扩了 M 行一句「静音偏好记忆、刷新页面保留」，其余逐字不动；`GAME_VERSION` v21.20→v21.21。只改 4 个游戏/文档文件（`data.js`：GAME_VERSION + SND_KEY + 两个纯函数 + 导出；`audio.js`：+loadSndPref/saveSndPref + 导出；`main.js`：+导入 + 启动恢复 + M 落盘；`README.md`：M 行 + tests/ 目录树与「冒烟十七件套」两处同步）+ 新增 `tests/smoke_v2121_sndpersist.mjs`（仓库常驻，承 v21.10-v21.20 冒烟入库先例）+ `package.json`（test 串第 17 份）+ `CHANGELOG.md`：本条。顺带把 `tests/smoke_v2120_helprush.mjs` 的 README 守护按 v21.7 惯例去硬化（其写死的「十六件套/十五件套/十四件套」在本次件数递增时必然失败——v21.20 对 v2119 做过同款处理、本次轮到 v2120；实件数由 v21.21 冒烟守护 README「十七件套」口径）。
- 验证：`node --check js/audio.js js/data.js js/main.js tests/smoke_v2120_helprush.mjs tests/smoke_v2121_sndpersist.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 十七件套全绿：**89/89 + 6/6 + 8/8 + 10/10 + 17/17 + 18/18 + 23/23 + 12/12 + 15/15 + 14/14 + 17/17 + 21/21 + 28/28 + 12/12 + 18/18 + 14/14（既有 16 件）+ 25/25（新增）**（回归不受影响——既有冒烟无 SND_KEY/loadSndPref/S.SND 持久化断言；v2120 冒烟去硬化后其 README 守护从「十六件套」降级为「冒烟/件套」存在性断言，其余 13 项逐字不变）；新增 v21.21 专项冒烟 25 项断言全过——GAME_VERSION 单调越过 v21.20、SND_KEY 格式合法（jrpg_ 命名族）、sndPrefToState/sndPrefToString 纯函数全路径（'0'→静音/'1'→开/null/undefined/空串/乱值→默认开/往返互逆）、localStorage 桩下 loadSndPref/saveSndPref 真实读写 S.SND（无存储保持默认开 → '0' 恢复静音 → 开声落盘 '1' → 静音落盘 '0' → '1' 恢复开声 → 乱值按默认开 → 桩移除后两函数静默降级不抛错）、main.js 已导入并先恢复后挂监听、M 切换后即落盘、state.js SND 默认仍 true、hud.js 常驻指示未动、README 已同步（smoke_v2121_sndpersist + 十七件套口径、十六件套清除 + 「静音偏好记忆」）、smoke_v2120 守护表达式已去硬化。

## v21.20 帮助页「试炼进阶」页「试炼三连战」行越界修复——r[2] 拆分 + 地图指南/试炼进阶两页纳入全页行宽巡检（体验打磨·排版，承 v19.59/v21.11/v21.12/v21.13「面板内侧一切文字必须落在面板内」主线：那四版分别体检了操作说明页、魔物状态页、快速旅行列表、状态页徽记行，四页帮助页的「操作说明」「魔物状态」两页也在 v21.11/v21.14 各建了常驻全页行宽巡检——唯独「地图指南」「试炼进阶」两页从未做过宽度体检，也没有任何常驻巡检；本次用官方 estW 口径（CJK=0.865×size，@napi-rs/canvas 真值标定，v21.11/v21.14 冒烟同款）扫全四页，唯一越界行是试炼进阶页的「试炼三连战」）

- 【试炼三连战行 ≈478.6，超面板预算 470——四页帮助页中唯一越界行】`data.js` `HELP_PAGES[3]`「试炼三连战」行单行 14px：`连战三名最强 Boss，全胜获「百炼成钢」；每胜一关回血35%HP/50%MP`——官方 estW 口径 ≈478.6，面板可用宽 470（右缘 570 − 文本起点 100）超 8.6px；@napi-rs/canvas 真实字形度量 ≈460 虽尚在面板内，但末字距右缘仅 ~10px——v21.2/21.15 这类加长行（「魔物状态」页是 649 越界、本行也已逼近）随时可能把末字推出面板，且「试炼进阶」页与「地图指南」页从未纳入 v21.11/v21.14 建立过的「全页行宽巡检」常驻冒烟（那两版分别只巡了魔物状态页/操作说明页），行宽治理在帮助页还差两格没封口。
- 【修正：r[2] 拆分，全部信息逐字保留】`data.js` 改为 r[1] 主行（14px）+ r[2] 次行（12px 次级灰，drawHelp 既有 r[2] 通路，v19.59 战斗行/v21.11 战斗掉落·宝箱掉落同款）：r[1] 收 `连战三名最强 Boss，全胜获「百炼成钢」`（≈299.8）、r[2] 收 `每胜一关回血${Math.round(RUSH_RECOVER.hp*100)}%HP/${Math.round(RUSH_RECOVER.mp*100)}%MP`（≈142.8，35%/50% 仍由 `RUSH_RECOVER` 派生、调比例只改 data.js 一处）——行数不变（仍 6 行、sp=34 档，不触发 v19.59 页长自适应）、末行基线 266 仍不触页脚 452、其余 5 行与其余 3 页逐字不动；纯显示零结算，未动 `rect`、`drawHelp` 渲染路径、`RUSH_RECOVER`/`RUSH_BASE_GOLD` 任何数值与战斗横幅/结算。
- 【零回归面】未动任何战斗/掉落/任务/存档/遇敌/数值；未动 HELP_PAGES[0]/[1]/[2] 与 page[3] 其余 5 行（防误动由本版冒烟关键词守护）；只改 2 个游戏/文档文件（`data.js`：GAME_VERSION v21.19→v21.20 + 试炼三连战行拆 r[1]+r[2] + 注释；`README.md`：tests/ 目录树与「冒烟十六件套」两处同步、十五件套清除）+ `package.json`（test 串第 16 份）+ 新增 `tests/smoke_v2120_helprush.mjs`（仓库常驻，承 v21.10-v21.19 冒烟入库先例）+ 顺带把 `tests/smoke_v2119_gallerychest.mjs` 的 README 守护按 v21.7 惯例去硬化（其写死的「十五件套」在本次件数递增时必然失败——v21.19 对 v2118 做过同款处理、本次轮到 v2119；实件数由 v21.20 冒烟守护 README「十六件套」口径）+ `CHANGELOG.md`：本条。
- 验证：`node --check js/data.js tests/smoke_v2120_helprush.mjs tests/smoke_v2119_gallerychest.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 十六件套全绿：**89/89 + 6/6 + 8/8 + 10/10 + 17/17 + 18/18 + 23/23 + 12/12 + 15/15 + 14/14 + 17/17 + 21/21 + 28/28 + 12/12 + 18/18（既有 15 件）+ 14/14（新增）**（回归不受影响——既有冒烟无 HELP_PAGES[3]/试炼三连战行断言；v2119 冒烟去硬化后其守护从「十五件套」降级为「冒烟/件套」存在性断言 + 本版专有内容断言，其余 17 项逐字不变）；新增 v21.20 专项冒烟 14 项断言全过——GAME_VERSION 单调越过 v21.19、帮助页仍 4 页、试炼进阶页仍 6 行（拆分不增行）、试炼三连战行 r[1]+r[2] 结构存在、r[1] 含连战/全胜/百炼成钢且不含回血比例、r[2] 与 RUSH_RECOVER 派生逐字相等（35%HP/50%MP）、r[1] ≤470（修复前 ≈478.6 越界）、r[2] ≤470、试炼进阶页与地图指南页全页行宽巡检全过（两页此前均无巡检）、其余 5 行关键词全在（蓝色石碑/洞窟领主/原地再战/瞬移/金光脉动）、README 已同步（smoke_v2120_helprush + 十六件套口径、十五件套清除）、smoke_v2119 守护表达式已去硬化（不再以十五/十四件套断言件数）。

## v21.19 无字回廊遗物宝箱——北壁名字石碑群间的唯一宝箱（新内容·终局区探索奖励，v21.17 老矿工 NPC 后轮换回「内容扩充」方向——四张图的宝箱分布是 镇 2 / 林 3 / 矿 2+4(领主后显形)，唯独 无字回廊 为 0：全游戏唯一没有宝箱的地图，且它还是终局区——玩家带双徽记进来就有既视感，「被忘掉的名字存放处」只有石碑/精英/祭坛三样硬内容，危险岩地全程遇敌、无泉水无旅店，跑完整张图零探索奖励；与「终局区反而最穷」的不对称观感相悖（v21.17 老矿工同款「矿脉无补给」结构性空白，这版补的是「回廊无宝箱」））

- 【回廊 0 宝箱：终局区没有一处「付出探索→拿到东西」的反馈】`MAPS` 四图宝箱清点（data.js rows 的 'C' + CAVE_TREASURE 延迟显形）：village 2（东门/南门各一）、dungeon 3（宝箱蘑菇线含支线 3 株来源之一）、cave 2 + 领主后 2×2 星砂宝箱，独 gallery 0——而 gallery 是全图唯一全程危险格（zone hp×1.3/atk×1.2/def×1.15）且遇敌池限定（雾灵/石魔像/骷髅兵）的图，玩家在此每一步都在赌遇敌，却没有一个「值得绕路」的目标；打开 H 页「宝箱掉落」行也只写「城镇/矿脉」，连回廊都不在列——「这图没有宝箱」这件事甚至没被文档承认。
- 【修正：北壁石碑群间新增遗物宝箱 (16,1)】`data.js` `MAPS.gallery.rows[1]` 第 16 列 '0'→'C'（唯一改动的一个字符；行宽不变仍 24 列、总行数仍 9）：位于第 3/4 块名字石碑（15,1 / 20,1）之间、北壁纪念区——玩家探索石碑群时自然撞见，避开主线必经走廊（1,4 入口—12,4 残焰魔像—21,4 祭坛）不强制踩踏；走 world.onStep 既有开箱管线（TY.CHEST 非 SOLID 可踩、hero.chests 持久化「x,y」键、开箱即判成就「开箱寻宝」、已开绘制回落地色 TILE_CHEST_OPEN 全部自动生效，零新状态/零新函数）；掉落走「非雾语林」档（45% 金币 12+级×5 / 55% 药水，与 onChestStep else 分支、H 页同源）；全游戏宝箱 11→12，TREASURE_GOAL=6 阈值成就不受影响（已达标的旧档/新档均可继续达成）。同步收口文档口径：H 页「宝箱掉落」r[2] 「城镇/矿脉」→「城镇/矿脉/无字回廊」（估算宽 ≈186 ≤470 面板内、45/55 仍由 CHEST_GOLD 派生零裸字面量），README 无字回廊条目补遗物宝箱、帮助页描述「镇/林/矿三地」→「镇/林/矿/廊四地」、数值速查「镇矿 45% 金」→「镇/矿/廊 45% 金」。
- 【零回归面】未动任何地图行布局/zone/dangerTiles/遇敌池/传送（仅 gallery.rows[1] 一个字符）；未动 world.js 开箱/成就/掉落任何逻辑（宝箱走既有通路）；未动 CAVE_TREASURE/显形时机；未动 CHEST_*/TREASURE_GOAL/成就（数据逐字不动）；未动任何战斗/任务/存档；GAME_VERSION v21.18→v21.19。
- 验证：`node --check js/data.js tests/smoke_v2119_gallerychest.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 十五件套全绿：**89/89 + 6/6 + 8/8 + 10/10 + 17/17 + 18/18 + 23/23 + 12/12 + 15/15 + 14/14 + 17/17 + 21/21 + 28/28 + 12/12（既有 14 件）+ 18/18（新增）**（回归不受影响——既有冒烟无 gallery 行/宝箱计数/宝箱掉落 r[2] 断言，v2118 的 README 守护按 v21.7 惯例同步去硬化）；新增 v21.19 专项冒烟 18 项断言全过——GAME_VERSION 单调越过 v21.18、回廊恰 1 只宝箱且位于 (16,1)、行宽/行数/其余行未误动、TY.CHEST 非 SOLID 可行走、chToTy('C') 同源、不与石碑/残焰魔像/终焉祭坛/守名者/玩家起点冲突、四块石碑坐标未变、静态宝箱 7→8 且全游戏 12 只（≥TREASURE_GOAL=6 仍可达成）、H 页 r[2] 已扩「无字回廊」且 45/55 由 CHEST_GOLD 派生、r[1] 金币公式未误动、r[2] 估算宽 ≈186 ≤470、README 已同步（十五件套 + 遗物宝箱 + smoke_v2119 收录、十四件套清除）、v2118 守护已去硬化；运行期以 BOM 桩真实导入模块链 `initGame→loadMap('gallery')`：at(16,1)===TY.CHEST 落位正确、STEP_HANDLERS[TY.CHEST] 开箱不抛错且 hero.chests 记入 '16,1'、重载后宝箱仍为 CHEST 瓦片（已开态由绘制层区分）。

## v21.18 标题页快捷提示行补「R 重开新档」——两按确认的功能入口收口到它实际的按键行（体验打磨·可发现性，承 v20.3「快捷键可发现性」/ v21.14「H 页是知识中枢」主线：v21.16 给标题页 R 重开新档加了「两按确认」（core.titleResetCheck 状态机 + data.js TITLE_RESET_CONFIRM_MS 单一数据源），并把口径写进了 H 帮助页「存档槽」行与 README 快速上手表——但标题画面 drawTitle 的快捷提示行（y=420，承 v21.4 选槽提示行）仍只写「L 读档 · WASD 移动 · Esc 菜单 · P 存档 · M 静音」，R 是标题页提示行里唯一「实际存在却没有屏幕入口」的按键：玩家正站在按 R 的地点，防误触设计做得再稳，入口不可见仍是 v20.3/v21.14 反复点名的那类「功能存在就必须能看到」的反例）

- 【R 键在标题页提示行缺位】`view/menus.js` `drawTitle()` y=420 行是标题页唯一的按键提示行（选槽/读档/移动/菜单/存档/静音全在列），而 R「重开新档」是标题页独有的破坏性按键（阵亡画面 R 有自己的提示、Esc 菜单里没有 R）；v21.16 只补了 H 页与 README 两处入口，唯独没补玩家正盯着的那一行——新玩家在标题页按 R 之前无任何屏幕提示（两按确认再好，也要先知道有 R 这个功能），与 v21.14「战斗日志回看只挂战斗内角标」是同一类「入口漏网」结构。
- 【修正：提示行 L 读档后补「· R 重开新档(连按两次)」】`menus.js` 该行改为 `按 1/2/3 或 ←/→ 选择存档槽 · L 读档 · R 重开新档(连按两次) · WASD 移动 · Esc 菜单 · P 存档 · M 静音`——「(连按两次)」与 v21.16 两按确认口径、H 页存档槽行「连按两次确认」、README「连按两次 R 确认执行」逐字同口径；模板串仍由 SAVE_SLOTS 派生（加档位自动跟随），行数/字号 12px/基线 y=420 逐字不动；真实字形度量（@napi-rs/canvas 12px sans-serif）481.4px ≤640 画布、中心对齐两侧余量 79.3px。纯显示零结算：未动 title.onKey 分派、titleResetCheck 状态机、TITLE_RESET_CONFIRM_MS、resetRun，也未动第二行快捷一览（I/J/B/C/T/F/H）与版本脚注行。
- 【零回归面】未动任何战斗/掉落/任务/存档/数值：数据层仅 GAME_VERSION v21.17→v21.18；未动 drawTitle 其余任何行（标题/副标题/存档槽行/存档预览/第二行快捷一览/版本脚注全部原样）；未动 HELP_PAGES 与 README 快速上手表既有口径（H 页存档槽行与 README 表 R 行本就已含两按确认）。只改 2 个游戏文件（`view/menus.js`：drawTitle 提示行 1 行 + 注释；`data.js`：GAME_VERSION）+ 新增 `tests/smoke_v2118_titlerow.mjs`（仓库常驻，承 v21.10-v21.17 冒烟入库先例）+ `package.json`（test 串第 14 份）+ `README.md`（tests/ 目录树收录 smoke_v2118_titlerow + 「冒烟十四件套」口径、十三件套清除）+ `CHANGELOG.md`：本条。
- 验证：`node --check js/view/menus.js js/data.js tests/smoke_v2118_titlerow.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 十四件套全绿：**89/89 + 6/6 + 8/8 + 10/10 + 17/17 + 18/18 + 23/23 + 12/12 + 15/15 + 14/14 + 17/17 + 21/21 + 28/28（既有 13 件）+ 12/12（新增）**（回归不受影响——既有冒烟无 drawTitle 提示行断言）；新增 v21.18 专项冒烟 12 项断言全过——GAME_VERSION 单调越过 v21.17、提示行存在且已补「R 重开新档」/「连按两次」、既有 token（L 读档/WASD/Esc/P 存档/M 静音/选槽/←/→）逐字保留、模板串按 SAVE_SLOTS 派生展开、12px 估算宽 ≈485 ≤620、仍为单行模板串且字号仍 12px（未触发纵向位移）、第二行快捷一览（I/J/B/C/T/F/H）未误动、版本脚注行仍在、README 已同步（tests 树收录 smoke_v2118_titlerow + 冒烟十四件套口径、十三件套清除）。

## v21.17 星井矿脉新 NPC「老矿工」——矿脉唯一的补给提示站（新支线/NPC·内容扩充，v20.8 残焰支线后轮换回「新 NPC」方向——承 v19.53 守名者 NPC / v19.46 守书记支线：四张地图里星井矿脉是**全图唯一没有泉水/旅店/任何免费恢复点**的地图（village 8,14 泉 / dungeon 12,9 泉水营地 / gallery 0 恢复点但为终局冲刺区），玩家初进矿脉（Lv6 起）最容易犯的错就是「药水带少了才进矿，半路被堵在深处」——而这份「矿脉没有补给」的知识此前只藏在跑图过程中自己吃亏，没有任何 NPC 或帮助页明说（H 页地图指南只说「更强魔物」）。本版新增纯闲聊 NPC 老矿工，把矿脉生存要点做成一站式口径：无泉水/旅店 + 最近免费恢复点（雾语林营地泉）+ 药水补给前置、水晶需双徽记、星砂宝箱显形时机、试炼提示——全部与地图/常量事实逐字相符（dungeon FOUNTAIN 12,9 / SB 12,11 / CAVE_TREASURE 东侧四条矿道），台词随主线旗标分段推进（承 sage/guard 同款无任务 NPC 模式），trueBoss 后走 after 彩蛋。纯数据 + 一个纯显示 mark 分支，零新状态/零新计数/零结算/零任务系统改动。\n\n- 【矿脉无补给这一「高代价事实」没有任何在途提示】`data.js` MAPS.cave 无 FOUNTAIN（village/dungeon 各有一处泉水，唯独 Lv6+ 的高危练级图没有免费恢复点），但唯一入口是在泉边补给——玩家从 dungeon（12,9 泉水）进入 cave 后，最近的免费恢复点回退到 dungeon 营地，而进矿前该补多少药、矿里哪有宝箱可开、水晶什么时候能碰，全部只能靠 H 页「地图指南」的定性描述（「更强魔物·迷宫·中央终焉水晶」）自己猜；与「信息透明」主线（v21.2/v21.11/v21.15 帮助页知识中枢）相比，这是地图层唯一一处「关键生存知识不在任何 NPC/帮助页里」的空白。\n- 【修正：入口矿道旁新增老矿工（data.js 纯数据 + sprites.js 一个 mark 分支）】`NPCS.miner`（name 老矿工 / mark pick）三阶段 `linesByStage`（gate null→bossDefeated→caveBoss）+ `after` 彩蛋，与 sage/guard 同为无任务契约纯闲聊 NPC（`npcQuestPages` 按旗标选段、trueBoss 后优先 after，`npcQuestMark` 恒 null 不干扰任务顶标/可接委托判定）；开局段即明说「矿脉没有泉水/旅店」并指出雾语林营地泉是最近免费点、水晶双徽记前别碰、车夫有活可托（对应 side_cart 支线 giver cartman）；魔王后段讲洞窟领主扣下星砂与宝箱显形（对应 CAVE_TREASURE 东侧 2×2）、领主后段讲回廊门开与守名者（与 v19.53 守名者同口径）。落位 `MAPS.cave.extras {x:2,y:3,ty:NPC}` + `NPC_SPOTS['2,3']='miner'`——(2,3) 为可行走岩地、入口矿道旁（紧邻玩家 Start 1,2 的必经走廊），且是全键表中唯一未被占据的坐标（键为全局坐标跨地图共用，避开 13,6/10,13/19,8/12,8/2,4/13,9/3,1/17,11/5,1/10,1/15,1/20,1/8,5）；SPRITES `drawNpcMark` 新增 `mark==='pick'` 矿镐程序化绘制分支（贴图缺失时头像侧小镐，同 hat/lamp/sword 纯显示惯例；有贴图时走 sprite 路径不绘制）。NPC 头顶 `⏎ 对话·老矿工` 上下文提示与 `drawNpcSprite` 回退（mwVillager）自动生效。\n- 【零回归面】未动任何任务（QUESTS/questStatus/setSideQuest/日志/奖励零接触）；未动 MAPS 行布局/zone/危险格/传送/遇敌（仅 cave.extras 追加一项）；未动 NPC_SPOTS 既有键（只新增 '2,3'）；未动 SOLID/移动/碰撞（TY.NPC 本就在 SOLID）；未动帮助页/状态页/图鉴/成就/存档/读档/战斗/掉落任何逻辑与数值；`GAME_VERSION` v21.16→v21.17。只改 4 个游戏/文档文件（`data.js`：GAME_VERSION + NPCS.miner + NPC_SPOTS + cave.extras；`view/sprites.js`：+pick mark 分支；`README.md`：矿脉条目/系统清单 NPC 名单/目录树/「冒烟十三件套」四处同步；`CHANGELOG.md`：本条）+ `package.json`（test 串第 13 份）+ 新增 `tests/smoke_v2117_miner.mjs`（仓库常驻，承 v21.10-v21.16 冒烟入库先例）。\n- 验证：`node --check js/data.js js/view/sprites.js tests/smoke_v2117_miner.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 十三件套全绿：**89/89 + 6/6 + 8/8 + 10/10 + 17/17 + 18/18 + 23/23 + 12/12 + 15/15 + 14/14 + 17/17 + 21/21（既有 12 件）+ 28/28（新增）**（回归不受影响——既有冒烟无 NPCS/NPC_SPOTS/cave.extras 断言；v21.16 冒烟的 README 守护按 v21.7 去硬化惯例降级为「冒烟/件套」存在性断言——件数 12→13 是预期事件，实件数由本版冒烟守护）；新增 v21.17 专项冒烟 28 项断言全过——GAME_VERSION 单调越过 v21.16、NPCS.miner 名称/标记/pick 分支存在、NPC_SPOTS['2,3']===miner 且无重复映射、13 个既有坐标键全在位、cave.extras 含 (2,3) NPC 且**无 FOUNTAIN（老矿工「没有泉水」提示与地图事实必须逐字相符）**、既有 4 项 extras 未误动、linesByStage 三段 gate 依次 null/bossDefeated/caveBoss、每段 2 页带 [Enter] 收尾、after 2 页、npcQuestPages 按 4 档旗标选段正确（含 trueBoss→after）、台词含关键知识词（没有泉水/雾语林营地那口泉/两枚徽记/星砂扣下/矿道/无字回廊的门开/守名人/矿车还能跑十年）、台词无 emoji、npcQuestMark 恒 null、loadMap(cave) 后 at(2,3)===TY.NPC 且 井巫/车夫/水晶/试炼碑/出口 全在位、全部 23 条台词估算宽 ≤440（drawTalk MAX_W=440 实测口径）、sprites.js 源码含 mark==='pick'、README 已同步（老矿工 + smoke_v2117_miner + 十三件套口径、十二件套清除）。\n\n## v21.16 标题页 R 重开新档防误触——resetRun 从「零确认秒清档」改为「两按确认」，并让 R 重开走进 H 页自助查得到（体验打磨·防误触+可发现性，承 v21.6 快速旅行防误触 / v20.3·v21.14「功能存在就必须能看到入口」主线：v21.6 给快速旅行做了「选中当前所在地不再重载地图」的防误触拦截，本版处理的是同一家族的漏网——标题页的 R；v20.3 把 I/J/B/C/T/F/H 补进标题页快捷一览、v21.14 把「↑↓ 回看战报」补进 H 页操作清单，而标题页与 H 页都只写了「L 读档」、从没写过 R——R 在标题页是真实按键（main.js title.onKey → core.resetRun，README 一直有记录），却正好是全游戏唯一「零确认执行破坏性操作」的按键）

- 【标题页按 R 零确认直接 resetRun，误按必丢档】`main.js` title 场景 `e.key==='r'/'R'` 直接调用 `core.resetRun()`——`resetRun` 是破坏性重置（`startRun` 重建新档覆盖当前 `S.G`）；玩家从 Esc 菜单「返回标题」时进行中的冒险仍挂在 `S.G` 上（未存档的全部进度都在内存里），此时误按一次 R（R 紧邻 WASD，极易顺手）就瞬间丢档、连提示都没有——而同一按键在其他场景都有保护：阵亡画面 B/R/T 是用户此时明确想重来（意图清晰）、Esc 菜单里的操作都有菜单选中前置，唯独标题页 R 是直接裸触发。这与 v21.6「快速旅行误触选中当前地」是同一类「破坏性操作需要两段触发」问题，也是 v20.3/v21.14 反复强调的「功能存在就必须能看到入口」的反例——R 重开恰好是标题页唯一没写进任何游戏内说明的按键。
- 【修正：两按确认 + 帮助页/标题页口径补全】`core.js` 新增纯函数 `titleResetCheck(armT, now, isR)`（零副作用、可单测）：首次按 R 仅武装（返回 `{arm:now, fire:false}`），`TITLE_RESET_CONFIRM_MS`（data.js 单一数据源，1600ms）内再按 R 才 `fire:true` 并解除武装；任一非 R 键立即解除武装；超时后再按重新武装（不连发）；`main.js` title.onKey 接入：非 R 键先清 `S.titleResetArm`（state.js 声明 `titleResetArm: 0` 字段），R 分支调 `titleResetCheck`，未确认时 `SFX.cancel()` + `boxMsg('🔁 再按一次 R 确认重开新档（当前冒险进度将丢弃）')`（EVENT_MSG_MS，提示停留时长与确认窗口同长——玩家看到提示期间即窗口有效）；确认后走原 `resetRun()`（行为逐字不变，仍按原姓名/难度/叙事开局）。同时收口可发现性：`data.js` HELP_PAGES[0]「存档槽」行补 `· R 重开新档(连按两次确认)`（行数不变仍 14 行，估算宽 ≤470），README 快速上手表标题行补「连按两次 R 确认执行」防误触说明。
- 【零回归面】未动 `resetRun`/`startRun`/`newGame`/存档/读档任何逻辑（确认后才执行原函数）；未动阵亡画面 B/R/T、Esc 菜单、创建页、世界内按键；未动状态页/图鉴/成就/旅行；未动 `TITLE_RESET_CONFIRM_MS` 以外的任何 data.js 数值；帮助页仅「存档槽」行内文字（其余 13 行与 3 页逐字不动，防误动由冒烟守护）；`titleResetCheck` 为新增纯函数、不改变任何既有函数签名。只改 5 个游戏文件（`data.js`：GAME_VERSION v21.15→v21.16 + `TITLE_RESET_CONFIRM_MS` + 存档槽行；`core.js`：+`titleResetCheck` 纯函数 + 导出；`state.js`：+`titleResetArm` 字段；`main.js`：title.onKey 接入；`README.md`：快速上手表 + tests/ 目录树与「冒烟十二件套」两处同步）+ 新增 `tests/smoke_v2116_titlereset.mjs`（仓库常驻，承 v21.10-v21.15 冒烟入库先例）+ `package.json`（test 串第 12 份）+ `CHANGELOG.md`：本条。顺带把 `tests/smoke_v2115_elixir.mjs` 的 README 守护中写死的「十一件套」断言升级为「冒烟/件套」存在性断言（v21.7 去硬化惯例：件数随版本递增，数字写死必然脱节——本次件数 11→12 即触发其失败；实件数由 v21.16 冒烟守护 README「十二件套」口径），属测试口径维护、非游戏逻辑。
- 验证：`node --check js/data.js js/core.js js/state.js js/main.js tests/smoke_v2116_titlereset.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 十二件套全绿：**89/89 + 6/6 + 8/8 + 10/10 + 17/17 + 18/18 + 23/23 + 12/12 + 15/15 + 14/14 + 17/17（既有 11 件）+ 21/21（新增）**（回归不受影响——既有冒烟无标题页 R/存档槽行断言）；新增 v21.16 专项冒烟 21 项断言全过——GAME_VERSION 单调越过 v21.15、`TITLE_RESET_CONFIRM_MS` 为正整数且有界、状态机全路径（未武装点非 R 不执行/首按仅武装/武装后非 R 解除/窗口内再按执行/恰在窗口末执行/超时重新武装/执行后不连发）、state.js `titleResetArm` 字段已声明、操作说明页仍 14 行且关键词不重复、存档槽行仍派生 SAVE_SLOTS 与 ←/→、已补「R 重开新档」与「连按两次确认」、战斗行主行与静音行未误动、存档槽行估算宽 ≤470 且全页行宽巡检通过、README 已同步（连按两次 + smoke_v2116_titlereset + 十二件套口径、十一件套清除）。

## v21.15 帮助页「高级灵药」行补全配方与恢复数值——配方与恢复量从酿造锅走进 H 页知识中枢，顺带修掉 README 数值速查「酿造 3 菇」与 BREW_MUSHROOMS=2 的脱节口径（体验打磨·信息透明，承 v21.2/v21.11/v21.14「帮助页是战前知识中枢、功能/数值存在就必须能在 H 页看到」主线：v21.11 修了魔物状态页越界、v21.14 补了战斗日志回看，本版补的是这条主线在「高级灵药」行上的漏网——该行只有「酿造或掉落获得 🧪：可同时恢复 HP/MP」一句定性描述，配方（几株蘑菇+几金）与确切恢复量（80%HP+40%MP）只在酿造锅 drawBrew 一处可见；玩家在矿脉/回廊想决定「要不要专程回镇酿一瓶」时，翻遍 H 页也查不到成本与效果）

- 【H 页查不到灵药配方与恢复量】`data.js` `HELP_PAGES[2]`（魔物状态页）「高级灵药」行写的是「酿造或掉落获得 🧪：可同时恢复 HP/MP，战斗 [3] 自动优先使用」——「几株蘑菇+几金」「恢复多少」都藏在酿造锅界面（`menus.drawBrew` 配方行 `配方：2 株魔法蘑菇 + 10 金币 → 高级灵药 ×1` 与效果行 `恢复 80% HP + 40% MP`，读 `BREW_MUSHROOMS`/`BREW_GOLD`/`ELIXIR_HP_PCT`/`ELIXIR_MP_PCT`），而酿造与否直接决定「背包灵药余额怎么补」「蘑菇是卖是酿」——这本是 H 页该回答的问题；与 v21.14「战报回看入口只挂在角标」同一「知识中枢缺页」结构。
- 【修正：该行改为全部单一数据源派生】`data.js` `HELP_PAGES[2]`「高级灵药」行改为 `酿造：${BREW_MUSHROOMS}株蘑菇+${BREW_GOLD}金 → 🧪 恢复${Math.round(ELIXIR_HP_PCT*100)}%HP+${Math.round(ELIXIR_MP_PCT*100)}%MP（战斗[3]优先）`——四个数值与 `brewNow` 结算、`drawBrew` 配方/效果行逐字同源，调配方或恢复量只改 data.js 一处，H 页自动跟随；行数不变（仍 10 行，不触发 v19.59 页长自适应），估算宽 ≈427px ≤470（面板右缘 570 − 起点 100，余量 ≈43px）；「战斗[3]优先」沿用旧口径，「或掉落获得」信息由同页「战斗掉落」行（6% 高级灵药）承接，不重复。纯显示零结算，未动 `brewNow`/`drawBrew`/`takePotion` 任何逻辑与 `BREW_*`/`ELIXIR_*` 数值。
- 【顺带修复 README 数值速查脱节项】`README.md` 数值速查表「经济」行原写「酿造 3 菇+10 金→灵药」——与代码单一数据源 `BREW_MUSHROOMS=2` 脱节（同表其余 6 项：新档 30 金+3 药/药水 15 金(50%HP+8)/旅馆 10 金/卖菇 10 金 全部与 `START_*`/`POTION_*`/`INN_PRICE`/`MUSHROOM_PRICE` 逐字一致，唯独酿造这项是 v3.x 时代残留的旧配方）；改为「酿造 2 菇+10 金→灵药（80%HP+40%MP）」并把数据源列补 `ELIXIR_*`，与游戏内 drawBrew/H 页同口径。
- 【零回归面】未动 `BREW_MUSHROOMS`/`BREW_GOLD`/`ELIXIR_HP_PCT`/`ELIXIR_MP_PCT` 任何数值与 `brewNow`/`drawBrew`/`takePotion` 结算；未动 HELP_PAGES 其它三页与魔物状态页其余 9 行（逐字不动，防误动由冒烟守护）；未动 drawHelp 渲染（行距/页脚/r[2] 路径零改动）；未动任何战斗/掉落/任务/存档/遇敌。只改 3 个文件（`data.js`：GAME_VERSION v21.14→v21.15 + 高级灵药行 1 行派生化 + 注释；`README.md`：数值速查「经济」行 + tests/ 目录树与「冒烟十一件套」两处同步）+ 新增 `tests/smoke_v2115_elixir.mjs`（仓库常驻，承 v21.10-v21.14 冒烟入库先例）+ `package.json`（test 串第 11 份）+ `CHANGELOG.md`：本条。
- 验证：`node --check js/data.js tests/smoke_v2115_elixir.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 6/6 + 8/8 + 10/10 + 17/17 + 18/18 + 23/23 + 12/12 + 15/15 + 14/14 + 22/22** 全绿（回归不受影响——既有冒烟无高级灵药行断言）；新增 v21.15 专项冒烟 17 项断言全过——GAME_VERSION 单调越过 v21.14、魔物状态页仍 10 行且关键词不重复、r[2] 行数仍 2、高级灵药行与派生串逐字相等（零裸字面量）、配方/恢复量/「战斗[3]优先」在位、旧文案「酿造或掉落获得」清除、行宽 ≈427 ≤470、全页行宽巡检通过、相邻 9 行关键词全在且战斗掉落/宝箱掉落仍同源、README「酿造 2 菇+10 金」与「smoke_v2115_elixir/十一件套」已同步且「酿造 3 菇」口径清除。

## v21.14 帮助页操作说明补战斗日志回看——「↑↓ 回看战斗记录」从战斗内角标走进 H 页与 README 操作清单（体验打磨·可发现性，承 v21.5 战斗日志回看 / v20.3「快捷键可发现性」主线：v21.5 给战斗画面右下角加了「↑↓ 回看战斗记录 · 新 N 条 · ↓ 回最新」的常驻提示，但帮助页操作说明（HELP_PAGES[0]）与 README 快速上手表这两处「操作总清单」都没收录——H 页是战前知识中枢，功能存在就必须让玩家在任何入口看见）

- 【H 页操作清单漏掉战斗日志回看】v21.5 给战斗战报窗口加了回看（↑↓ 翻历史、回看中右下角显示「新 N 条 · ↓ 回最新」），提示只挂在战斗画面右下角常驻角标上；H 帮助页「操作说明」页的「战斗」行仍只有 1-6 指令与蓄力/Boss 规则（v19.59 拆出的 r[2] 次行），README 快速上手表也没有这一行——玩家战前翻 H 页做功课、或读 README 对按键时，都看不到「战斗中还能翻战报」，与 v20.3「功能存在就必须能看到入口」口径不一致（那版把 I/J/B/C/T/F/H 全部补进标题页快捷一览，战斗日志回看是操作清单里唯一漏网的按键）。
- 【修正：r[2] 次行追加说明 + README 上手表补行】`data.js` `HELP_PAGES[0]` 战斗行 r[2] 由「蓄力：下击/技能×1.5 · Boss无法逃跑」扩为「蓄力：下击/技能×1.5 · Boss无法逃跑 · ↑↓ 回看战斗记录」（×1.5 仍由 `CHARGE_MULT` 派生）——「回看战斗记录」与 v21.5 战斗画面右下角提示同文案；仍在原 r[2] 12px 次级灰一行内、行数不变（操作说明页仍 14 行，不触发 v19.59 页长自适应变化）、估算宽 ≈268px ≤470（面板右缘 570 − 起点 100）；README 快速上手按键表在「阵亡 B/R/T」后补一行「战斗中 `↑↓` | 回看战斗记录（战报超出一屏时右下角显示 新 N 条 · ↓ 回最新，随战报追加实时提示）」。纯文档/可发现性，零结算：r[1] 主行逐字不动、FLEE_SUCCESS/CHARGE_MULT 派生口径不变、drawHelp 渲染路径（r[2] 基线 y+18 / 页长自适应）零改动。
- 【零回归面】未动任何战斗/掉落/任务/存档/数值：FLEE_SUCCESS/CHARGE_MULT 与 6 指令/蓄力/Boss 规则原文逐字不动；未动 drawHelp（页长自适应 sp=25、r[2] y+18 基线、页脚 y=452 全部原样）；未动 S.blogView / battle 按键分派 / 战报窗口 BLOG_WIN；未动其它三页（地图指南/魔物状态/试炼进阶）。只改 2 个游戏文件（`data.js`：HELP_PAGES[0] 战斗行 r[2] 追加 + GAME_VERSION v21.13→v21.14；`README.md`：快速上手表 1 行 + tests/ 目录树与「冒烟十件套」注释两处同步——后者顺带修掉 v21.13 遗留的「冒烟八件套/缺 v2113」过时口径）+ 新增 `tests/smoke_v2114_helpblog.mjs`（仓库常驻，承 v21.10-v21.13 冒烟入库先例）+ `package.json`（test 串第 10 份）+ `CHANGELOG.md`：本条。
- 验证：`node --check js/data.js tests/smoke_v2114_helpblog.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 6/6 + 8/8 + 10/10 + 17/17 + 18/18 + 23/23 + 12/12 + 15/15 + 14/14** 全绿（回归不受影响——既有冒烟无 HELP_PAGES[0] 战斗行/r[2] 断言）；新增 v21.14 专项冒烟 14 项断言全过——GAME_VERSION 单调越过 v21.13、操作说明页仍 14 行且关键词不重复、战斗行 r[1] 主行逐字不变（含 FLEE_SUCCESS 派生 60%）、r[2] 仍含蓄力×CHARGE_MULT/Boss 规则、已追加「↑↓ 回看战斗记录」、r[1]/r[2] 估算宽 ≤470（r[2] 实测 ≈268）、全页行宽巡检通过、README 上手表已同步、存档槽行未误动（SAVE_SLOTS 派生同源）。

## v21.13 状态页冒险进度徽记压出面板修复——五格徽记按数量派生行距，第 5 格「试炼场」收进面板（体验打磨·排版，承 v19.59/v21.11/v21.12「面板内侧一切文字必须落在面板内且互不重叠」主线：那三版分别体检了帮助页操作说明、帮助页魔物状态页、快速旅行列表三个版面，状态页的冒险进度徽记行是这条主线上唯一的漏网版面——v19.89 给它加过一行（当前地图），却从没量过徽记行的横向宽度）

- 【第 5 格徽记压出面板右缘 13–19px】`menus.js` `drawStatus()` 徽记行 `190+i*88` 是 v3.20 四徽记时代的固定步距；v13.7 四幕重构把 `adventureProgress()` 增到五格（灯芯/星井/回廊/初灯/试炼场）时只加了数据没调布局——第 5 格「✓ 试炼场」x=190+4×88=542，12px 字形宽 @napi-rs/canvas 实测 40.8px（浏览器含 ✓ 全宽字形 ≈47px），右缘 583–589 越过面板右缘 570（panel(70,28,500,424)），徽记文字压在面板边框上并伸出到面板外的画布上——玩家按 I 看状态时「试炼场」这格进度直接被面板切掉半截，与「信息透明」主线（v2.8 起徽记行就是让玩家一眼看清还差什么）直接矛盾：五格里信息量最大的第四幕进度恰恰看不全。README 系统清单同期写的「冒险进度四徽记」也还是四格口径（v13.7 没同步），同一处两套说法。
- 【修正：徽记行距按数量派生 + README 同步五格】`menus.js` 新增导出纯函数 `progressSpacing(n)=Math.min(88, Math.floor((570−190−60)/(n−1)))`（与 v21.12 travelFootY 同一「派生化防回归」族：可用宽 = 面板右缘 570 − 起点 190 − 预留 60，60 = 最宽徽记「✓ 试炼场」实宽 ≈47px + 13px 余量），`drawStatus` 徽记行改 `190+i*progressSpacing(prog.length)`：n=5 → 80（第 5 格 x=510，实测右缘 550.8 ≤570，余量 19px；原文案/颜色/基线 y=412/✓✗ 口径逐字不动）；n≤4 → min(88, ≥106)=88，与旧四格布局逐值一致（旧档行为零变化）；n≥6 自动收紧（6 格 → 64），将来再加徽记忘记调行距也不会再压出面板。README.md 系统清单「四徽记（灯芯/星井/初灯/试炼场）」同步为「五徽记（灯芯/星井/回廊/初灯/试炼场）」，杜绝第二套口径。
- 【零回归面】未动 `adventureProgress()` 数据（五格逐字不动——灯芯/星井/回廊/初灯/试炼场与判定标志 bossDefeated/caveBoss/galleryOpen/trueBoss/rushDone 全原样，v13.7 起该数据即如此，本次只修显示）；未动徽记文案/颜色/基线（y=412、✓ 亮绿 ✗ 灰 逐字不动）；未动 drawStatus 其余任何行（姓名/地图/经验条/HP/MP/攻防/武器防具/资源/技能/下一技能/主线/支线全部原样）；未动 quests.js/core.js/data.js 任何逻辑（本次 data.js 仅 GAME_VERSION v21.12→v21.13）；未动任何战斗/掉落/任务/存档/遇敌。只改 2 个游戏文件（`view/menus.js`：+1 纯函数、徽记行 1 行改用派生式；`data.js`：GAME_VERSION v21.12→v21.13）+ `README.md`（四徽记→五徽记一句）+ 新增 `tests/smoke_v2113_progress.mjs`（仓库常驻，承 v21.10/v21.11/v21.12 冒烟入库先例）+ `package.json`（test 串第 9 份）+ `CHANGELOG.md`：本条。
- 验证：`node --check js/view/menus.js js/data.js tests/smoke_v2113_progress.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 6/6 + 8/8 + 10/10 + 17/17 + 18/18 + 23/23 + 12/12 + 15/15** 全绿（回归不受影响——既有冒烟无徽记行断言）；新增 v21.13 专项冒烟 15 项断言全过——GAME_VERSION 单调越过 v21.12、adventureProgress 仍五格且名称逐字、progressSpacing(5)=80／(4)=88 与旧布局一致／(6)=64 自动收紧、单调不增、第 5 格 x=510 右缘 550.9 ≤570 余量 ≥10px、行距 80 > 最宽徽记 ≈47 互不重叠、drawStatus 五格实绘同基线且 x 序列 190/270/350/430/510、源码无 `i*88` 硬编码残留；@napi-rs/canvas 真实字形度量（actualBoundingBoxAscent/Descent）复测修复前 542+40.8=582.8 越界 → 修复后 510+40.8=550.8 落面板内（余 19.2px），修复前「试炼场」压框 → 修复后整格完整可见。

- 【页脚 270 与末行「无字回廊」重叠 6px，且页脚被夹在标题与描述之间】`view/menus.js` `drawTravel()` 页脚 `↑↓ 选择 · Enter 传送 · Esc 取消` baseline 硬编码 270——TRAVEL_LIST 第 4 条目的地（无字回廊，v19.53 加入）后，列表行 `110+i*52` 的末行 baseline=266（16px 字形底 267）、其描述行 baseline=285（11px 字形底 286），页脚 12px 字形顶 261：与末行标题字形区 [253,267] 实际重叠 6px（@napi-rs/canvas actualBoundingBoxAscent/Descent 实测），且页脚文字正好被夹在「无字回廊」标题（266）与下方描述（285）之间——末行信息被页脚一分为二，选到第四项时「标题→操作提示→描述」交错排布。这是 v19.59/v21.11 做横向体检时漏掉的纵向维度：面板 400×320（60..380）有 110px 富余空间，页脚本可以稳稳坐在列表下方。
- 【修正：页脚位置派生化 + 列表条目解耦】`menus.js` 新增 `travelFootY(n)=110+(n-1)*52+19+35`（与行距 52 / 描述偏移 19 同式派生）：n=4 → 320，页脚字形顶 311 与描述底 286 相隔 25px、距 panel 底 380 富余 60px；页脚不再夹标题/描述之间。派生式让将来再加目的地自动让位（n=5 → 372 仍留 8px 余量、n=3 → 268 亦安全），彻底消灭「加条目忘挪页脚」这一类回归；硬编码 270 清零。纯显示零结算：动 `TRAVEL_LIST` 行距/条目数与 `drawTravel` 无任何交互，未动 `doTravel`/`transition`/`visited`/存档。
- 【零回归面】未动 `TRAVEL_LIST` 四条数据逐字不动；未动 `drawTravel` 列表渲染（标题/📍/❓未探索/desc/hint 的坐标、颜色、字号全部原样）；未动 `S.travelSel`/`main.js` 旅行按键分派；未动 `doTravel` 防误触拦截。只改 3 个文件（`view/menus.js`：新增 travelFootY + 页脚 1 行改为派生；`data.js`：GAME_VERSION v21.11→v21.12）+ 新增 `tests/smoke_v2112_travelfoot.mjs`（仓库常驻，承 v21.6/v21.9/v21.10/v21.11 冒烟入库先例）+ `package.json`（test 串第 8 份）+ `README.md`（tests/ 注释同步八件套）+ `CHANGELOG.md`：本条。
- 验证：`node --check js/view/menus.js js/data.js tests/smoke_v2112_travelfoot.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 6/6 + 8/8 + 10/10 + 17/17 + 18/18 + 23/23 + 12/12** 全绿（回归不受影响——既有冒烟无页脚坐标断言）；新增 v21.12 专项冒烟 12 项断言全过——GAME_VERSION 单调越过 v21.11、TRAVEL_LIST 仍 4 条、页脚派生值 320、与末行标题/描述纵向间距预算全过（重叠 6px→25px 间隔）、panel 内余量 ≥40px、派生式对 n=3/5 均安全（列表增减不复发）、源码无 270 硬编码残留且已用 travelFootY、drawTravel 绘制零抛错；@napi-rs/canvas 真渲染对照确认修复前 270 压 266（重叠 6px）→ 修复后 320 落于描述之下；横向预算复核末行标题↔提示 110px 间隔、描述终点 482.4 < 面板右缘 520，全部无越界。

## v21.11 帮助页「魔物状态」页三行越界修复——战斗掉率/宝箱掉率完整可见（体验打磨·排版，v21.10 消息队列收口后轮换回 v19.59 操作页排版修复同族的「面板内侧一切文字必须落在画布内」主线：v19.59 修过操作说明页 14 行挤到画布底（y=488/522 压页脚）与战斗行超宽拆 r[2]，但「魔物状态」页（v19.57/19.60/21.2 逐版添加的长行）从没做过宽度体检）

- 【魔物状态页三行 14px 超出面板右缘，宝箱掉率末尾被画布裁掉】`data.js` `HELP_PAGES[2]` 三行实测（@napi-rs/canvas 真渲染测量）：石心魔像·石甲 578 / 战斗掉落 632 / 宝箱掉落 649——面板右缘 570（panel(70,28,500,424)），宝箱掉落行 649 已越过画布 640 右缘，「…%药水；城镇/矿脉 45%金币·55%药水」的末尾四五字被画布整体裁掉：这本是 v19.60 起「战前知识中枢」里唯一一处「宝箱开箱出货构成」的完整说明（60%蘑菇·18%金币·22%药水；城镇/矿脉 45%金币·55%药水），玩家在雾语林开箱前却只能读到一行被裁的半截，与「信息透明」主线相悖——文字虽然写了，但它出了屏幕。
- 【修正：压缩 + r[2] 拆分，全部数值仍由常量化派生】`data.js` 三处：①石甲行「每层使下一次攻击伤害 -40%」→「每层使下一击伤害 -40%」（语义逐字等价，-40% 仍由 `SHIELD_MULT` 派生，554px 落回面板内）；②战斗掉落行 r[1] 收「胜利后约 38% 触发随机掉落：8% 装备或+60金 · 12% 药水」、新增 r[2] 次行（12px 灰）收「12% 蘑菇 · 6% 高级灵药」——drawHelp 已支持 r[2]，v19.59 战斗行同款；③宝箱掉落行 r[1] 收「开箱掉落：雾语林 60%蘑菇·18%金币（12+级×5）·22%药水」、r[2] 收「城镇/矿脉 45%金币·55%药水」——四档百分比、金币公式、两图构成逐字保留，只拆行不删信息。修复后三行实测 554/486/420 + r2 115/226，全部落于面板右缘 570 之内。
- 【零回归面】未动任何掉落/宝箱判定与数值：DROP_EQUIP/DROP_POTION/DROP_MUSHROOM/DROP_ELIXIR/DROP_GOLD/CHEST_MUSHROOM/CHEST_GOLD/CHEST_GOLD_BASE/CHEST_GOLD_PER_LV/SHIELD_MULT 全部逐字不动；帮助页行数不变（10 行，drawHelp sp=34 档），r[2] 各 +16px 后末行 y≈418 仍不触页脚 452；其它三页（操作/地图指南/试炼进阶）未动——专项冒烟同时给全 4 页做宽度预算巡检，未发现其它越界行。只改 2 个文件（`data.js`：HELP_PAGES[2] 三行 + GAME_VERSION v21.10→v21.11；`README.md` 无涉）+ 新增 `tests/smoke_v2111_helpwidth.mjs`（仓库常驻，承 v21.10 入库先例）+ `package.json`（test 串第 7 份）+ `README.md`（tests/ 注释同步七件套）+ `CHANGELOG.md`：本条。
- 验证：`node --check js/data.js tests/smoke_v2111_helpwidth.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 6/6 + 8/8 + 10/10 + 17/17 + 18/18 + 23/23** 全绿；对决测量（@napi-rs/canvas 真渲染）确认修复前 578/632/649 越界 → 修复后 554/486/420 + r2 115/226 全落面板内；新增 v21.11 专项冒烟 23 项断言全过——版本锚点单调越过 v21.10、行数不变、三行拆分结构存在、百分比/金币公式与常量同源逐字保留、宽度预算（估算系数经 canvas 真值标定，误差 <6%）四档全过、DROP/CHEST/SHIELD/ELITE 常量逐字未变。

## v21.10 消息队列——胜利结算多条反馈不再互相覆盖，逐条完整显示（体验打磨·信息透明，v21.9 战报家族收口后转向「消息通道」层：v19.80 起给 胜利/掉落/记忆碎片 逐条补的 余额/经验/进度 读数，在结算 4-5 连发 boxMsg 下只剩最后一条可见，信息透明积累自我抵消）

- 【boxMsg 覆盖：结算瞬间的多条反馈只有最后一条可见】`view/hud.js` `boxMsg()` 每次调用直接覆盖当前文本——而 `battle.js` `winBattle()` 与 `enemyAI` 击杀分支是同步连发：精英蘑菇（💎 剩余 N 株）→ 胜利/升级（🏆 获得 X 金 Y 经验 · 剩余 Z 金）→ 额外掉落（🎁 剩余 N）→ 记忆碎片（🕯️ N/4 段）→ Boss 专属奖励（⚔️ 圣光之剑 / ✨ 初灯 +300 金）。DOM #msg 只有一块，终焉之神一战四连发里 v19.80 起的 余额/经验/掉落/记忆进度 四条全部静默消失（只剩「初灯的意志散了」），幽冥魔王一战 v19.92 刚补的「获得 300 金币、150 经验 · 剩余 X 金」也被「圣光之剑」覆盖——二十多版「信息透明」给同一条通道加的内容在连发下互相吞没。
- 【修正：正片消息排队、瞬时消息可抢占】`hud.js` 新增模块级 `MSG_QUEUE`：正片消息（ms>0）显示期间，不同文本的新消息按序入队，当前条到点后自动接续下一条——胜利结算的 蘑菇→胜利→掉落→记忆→奖励 逐条各显示自己的时长，一条不丢；同文本连发只刷新时长、不重复入队（drawStory 逐帧「按 Enter 继续」不刷屏）；瞬时/清理消息（ms<=0：💀 闪字、drawTalk 空串清空、逐帧提示）立即显示并带 `curTransient` 标记——随后到达的正片消息（talkNext「接受了…」、出场教程提示）可立即抢占，不被 2500ms 默认时长阻塞；瞬时到点后接续队列（如有）。单文件、无新增游戏状态、零结算变化：所有时长仍读各自常量，只改「何时显示」，不改「显示什么」。
- 【零回归面】未动任何战斗/掉落/任务/存档/绘制逻辑：winBattle 各 boxMsg 文案、顺序、时长常量逐字不动；boxMsg 默认时长（1700 / ms||2500 兜底）不变；S.blog 战报、HUD、render 循环零改动；`S.msgTO` 仍为唯一计时句柄（无新增状态字段）。只改 2 个文件（`view/hud.js`：boxMsg + 队列；`data.js`：GAME_VERSION v21.9→v21.10）+ 新增 `tests/smoke_v2110_msgqueue.mjs`（仓库常驻——承 v21.6/v21.7 冒烟入库先例，不再经 /tmp 中转）+ `package.json`（test 串第 6 份）+ `README.md`（tests/ 注释同步六件套）+ `CHANGELOG.md`：本条。
- 验证：`node --check js/view/hud.js js/data.js tests/smoke_v2110_msgqueue.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 6/6 + 8/8 + 10/10 + 17/17 + 18/18** 全绿（回归不受影响——既有冒烟无 boxMsg 显示断言）；新增 v21.10 专项冒烟 18 项断言全过——GAME_VERSION 单调越过 v21.9、单条消息立即显示且到点隐藏、三连发按序 A→B→C 逐条接续且全部放完才隐藏、同文本连发只刷新时长不二次排队、队列中重复文本去重且不抢占队列、瞬时消息（ms=0）立即显示、正片消息可抢占瞬时提示（「按 Enter 继续」→「接受了…」不被 2.5s 阻塞）、空串清空语义保留、空闲时新消息直达。

## v21.9 变身回血战报追加敌方当前 HP——现出真身补上「结算后血量」，回血反馈家族最后一只黑盒收口（体验打磨·信息透明，承 v20.1 敌方回血后当前 HP / v21.8 灼烧中毒反击剩余 HP 后唯余变身这条：v20.1 已给暗影回血补「敌方 HP X/Y」，变身（现出真身）的 HP 恢复仍只报恢复量）

- 【变身回血只报恢复量，不报结算后血量】`enemyAI.js` 变身分支战报 `🌀 X 现出真身！力量暴涨，HP 恢复 N！（…治愈被封印！）` 无「敌方 HP X/Y」——Boss 血条虽常驻二段变身线与变身增益数值（攻+7 防+3 回15%），但变身这一口回血是整场最关键的恢复（旧灯卫 15% / 洞窟领主 10%），玩家看到「恢复 N」仍不知道结算后它到底站在多少血；与 v20.1 已收口的普通回血不对称（暗影回血早已带「敌方 HP X/Y」，同样是回血，变身这条反而没有）。
- 【修正：战报追加结算后当前 HP，读结算后实时值，零结算变化】`enemyAI.js` 变身分支在 `enemy.hp = Math.min(enemy.hpMax, enemy.hp + heal)` 之后、`S.blog.push` 前追加 `（敌方 HP ${enemy.hp}/${enemy.hpMax}）`，与 v20.1「敌方 HP X/Y」同源同式；`治愈被封印！` 标注逐字保留、仍排其后（先报这口血，再报封印）。数值/时序/公式零改动：PHASE2_AT、PHASE2_HEAL_PCT、atk/def 加成、变身阈值判定、全屏闪光、音效全部原样；变身必回血且 hp>0（hp<=0 早已胜负结算），无需致死保护分支。
- 【零回归面】未动任何伤害公式/变身判定/胜负时序/掉落/经验/金币；未动 v20.0-20.2/v21.8 既有反馈（逐字不动）；未动血条变身线/增益标注/帮助页/README 说明（本就是数值全透明，此行只补战报读数）。只改 3 个文件（`enemyAI.js`：1 条战报 + 注释；`data.js`：GAME_VERSION v21.8→v21.9）+ 新增 `tests/smoke_v219_transform.mjs`（仓库常驻、挂进 `npm test`——承 v21.6 冒烟入库先例，不再经 /tmp 中转）+ `package.json`（test 串追加第 5 份冒烟）+ `README.md`（tests/ 注释同步五件套）+ `CHANGELOG.md`：本条。
- 验证：`node --check js/enemyAI.js js/data.js tests/smoke_v219_transform.mjs` 与 `npm run check`（25 模块）全部通过；`npm test` 全绿（回归不受影响——既有冒烟无变身战报文案断言）；新增 v21.9 专项冒烟全过——GAME_VERSION 单调越 v21.8、PHASE2_AT/PHASE2_HEAL_PCT 单源、变身触发（40<50）后战报含「HP 恢复 15！（敌方 HP 55/100）」且 hp=55（攻+7 防+3 落位）、自定义 60% 回血钳制后战报读钳制值（100/100）、封印治愈战报保留「治愈被封印！」且排在 HP 读数之后、未到阈值（60≥50）不变身走普通攻击、v20.1 暗影回血战报逐字未动。



- 【灼烧/中毒/反击战报只报伤害，不报结算后血量】`enemyAI.js` 灼烧 tick（`🔥 灼烧令 X 受到 N 点伤害！`）与防御反击（`⚔️ X 趁隙反击，对 Y 造成 N 伤害！`）、`battle.js` 中毒 tick（`☠️ 毒素发作，X 受到 N 点伤害！（剩余 M 回合）`）三条战报均无「结算后血量」——玩家放完火焰斩看到敌方挂着灼烧，下回合烧掉 N 血后想确认「它还剩多少、再烧几轮死」仍需瞄血条；防御中白赚反击同理；中毒后每回合掉血，想确认「再毒几轮会不会死」也看不到自己还剩多少血。与 v20.0/20.1/20.2 已建成的主线不对称（主动伤害/回复/受击都有血量，DoT/反击这三处被动结算反而没有）。
- 【修正：三条战报追加结算后血量，全部读结算后实时值，零结算变化】`enemyAI.js` 灼烧分支在 `enemy.hp -= burnDmg` 后、`S.blog.push` 前追加 `（敌方 HP 剩余 X/Y）`；反击分支在 `enemy.hp = Math.max(0, …)` 后追加同款后缀；`battle.js` 中毒分支在 `hero.hp -= damage` 后追加 `（我方 HP X/Y）`（与 v20.2 敌方攻击后缀同源同式），原有 `（剩余 M 回合）` 逐字保留、仍排在前。三条全部遵守「致死不追加」既有口径：灼烧/反击导致 enemy.hp <= 0、毒发导致 hero.hp <= 0 时不加后缀，避免与「被击败了/被反杀倒地/败北」重复。数值/时序/公式零改动：BURN_PCT、DOT_MIN、COUNTER_MULT、POISON_PCT、POISON_TURNS、cmdDmg、befores/afters 全部原样，仅战报文案多一段读数，结算后血量与 HUD 血条/战斗画面血条同源。
- 【零回归面】未动任何伤害公式/DoT 结算/反击判定/胜负时序/掉落/经验/金币；未动 v20.0-20.2 既有三条反馈（逐字不动）；灼烧/中毒角标（view/drawBattle.js）与 H 帮助页、README 的 DoT 说明原样（角标本已显示「每回合 -X 血」，此改动只补战报侧）。只改 3 个文件（`enemyAI.js`：2 条战报 + 注释；`battle.js`：1 条战报 + 注释；`data.js`：GAME_VERSION v21.7→v21.8）+ `CHANGELOG.md`：本条。README 无需同步（未新增/改名文件、未改启动方式，README 未描述这三条战报的精确文案）。
- 验证：`node --check js/battle.js js/enemyAI.js js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` 全绿（回归不受影响——测试无以上三条战报文案断言）；新增 v21.8 专项冒烟（`/tmp/jrpg_smoke_v218_hpfb.mjs`）全过——GAME_VERSION 递增 v21.8、灼烧 tick 战报含「敌方 HP 剩余 X/Y」且 X=结算后实时值、反击战报同款后缀、中毒 tick 战报含「我方 HP X/Y」且与「剩余 M 回合」共存、三条均在结算后读数（与血条同源）、致死灼烧/反击/毒发不追加后缀、既有 v20.0-20.2 战报逐字未动、战报视窗/回看不受影响。

- 【腐化测试：v19.65 存档槽冒烟自诞生起就从未挂进 `npm test`，版本锚点已失效】`tests/smoke_v1965_playtime.mjs`（存档槽预览追加累计游玩时长的专项冒烟）首行断言 `GAME_VERSION==='v19.65'`——这是「一次性」锚点：版本只增不减，v19.66 起它就必然失败（实测 v21.6 下 5/6 通过、1 项 ✗）；且它从未进过 `package.json` 的 test 串（git 历史里 v19.65 提交时 test = smoke + freeze，之后每次版本递增也无人同步它）——「挂了但没被发现」比「没写」更糟：`npm test` 全绿会给人「套件健康」的错觉，实际有一条核心逻辑（slotPreview 时长/旧档兼容/空槽）的回归网是静默烂掉的。
- 【修正：锚点去硬化（单调断言）+ 挂回全量套件】`smoke_v1965_playtime.mjs` 与 `smoke_v216_travel.mjs` 的版本断言统一改为「格式合法（/^v\d+\.\d+$/）+ 已越过本测试诞生版本（v19.65 / v21.6）」的**单调**校验——随版本递增免维护、永久绿；版本意外回退（如误把 GAME_VERSION 改回旧值）仍会立刻报警，保住「版本校验」的原始意图；`package.json` test 串插入 `node tests/smoke_v1965_playtime.mjs`（v19.65 → v21.6 时序顺序），全量回归从 3 份扩到 4 份。
- 【零回归面】未动任何游戏代码/数值/界面/存档：`data.js` 仅 GAME_VERSION v21.6→v21.7（标题页脚注随之更新）；两条冒烟除版本断言外其余断言逐字不动（playtime 的 5 项内容断言、travel 的 9 项行为断言全部保留）；`npm run check`（25 模块）不变；README 仅同步 tests/ 一行注释（列出全量冒烟四件套）。CHANGELOG：本条。
- 验证：`node --check tests/smoke_v1965_playtime.mjs tests/smoke_v216_travel.mjs js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 6/6 + 8/8 + 10/10** 全绿（v19.65 冒烟改后 6/6——版本断言换单调后通过、其余 5 项逐字通过——并首次进入 npm test 串跑）。



- 【📍标注防误传，行却可点出误传】`menus.js` `drawTravel` 早已为当前所在地常驻绿标 `📍`（注释明写「一眼看出自己在哪，避免误传送」），但该行仍可被 ↑↓ 选中——对当前所在地按 Enter 会走 `core.js` `doTravel()` 的既有路径 `transition(key)`：`loadMap` 整图重载 + `playerStart` 复位 + BGM 重开 + 进图文案/强度预警重弹（实测站在雾语林中段 9,9 选「雾语林」传送 → 被丢回入口 1,2）——列表自己防误传、自己又能误传，玩家想回某图补给却把「正在这张图」的点重刷成入口，且完全无提示地丢了位置（与 v21.5 同一「反馈可读性/意图一致」原则：标注了防误传就该真的防住）。
- 【修正：同图拦截 + 明确提示，纯拦截零结算】`core.js` `doTravel()` 在已到访检查之后加 `if (key === curMap()) { SFX.cancel(); bind.boxMsg('已经在这里了！', EVENT_MSG_MS); return; }`——判定读 `state.curMap`（与 `G.map` 单一真相，core.js 补 import 一行），拦截后不 transition：不重载、不移位、不改 visited/BGM/遇敌/存档；提示文案与 v20.x「尚未探索此地」同级（同样是「这个选择没有发生传送」的反馈口径），音效与未探索/门锁一致用 `SFX.cancel()`。未探索拦截（逐字保留）、其它已到访图的 `SFX.door()→transition→goto('world')` 逐字不动；传送门/水晶开门等入口不走本函数，不受影响。
- 【零回归面】未动任何旅行数据（TRAVEL_LIST 四条逐字不动）、未动 drawTravel 绘制/📍 标注/未探索文案、未动 transition/loadMap/移动/存档。只改 3 个文件（`core.js`：import +1、分支 +1 段注释+4 行；`data.js`：GAME_VERSION v21.5→v21.6；`package.json`：test 串入新冒烟）+ 新增 `tests/smoke_v216_travel.mjs`（仓库常驻——承 improve-plan §1.4「冒烟脚本散落 /tmp 无法复现，应收进 tests/」，本次专项冒烟直接入库并挂进 `npm test`，不再经 /tmp 中转）。README 无需同步（未新增/改名游戏文件、未改启动方式、无按键变化——T 键行为不变，只是「当前所在地」不再误传）。
- 验证：`node --check js/core.js js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8 + 10/10** 全绿（回归不受影响——测试无 doTravel 断言）；新增 v21.6 专项冒烟（`tests/smoke_v216_travel.mjs`）**10 项断言全过**——GAME_VERSION 递增 v21.6、旅行列表含当前地图行、同图拦截后地图不变/位置不变（9,9 不再丢回入口）/visited 不变、换选其它已到访图仍正常传送并落位目标图、未探索图仍被拦截（逐字保留）、curMap 与 G.map 同源、drawTravel 绘制零抛错；修复前后对照（改前 9,9→1,2，改后 9,9→9,9）。



- 【回看后行动，新战报被顶出视窗且零提示】`main.js` battle 的 ↑↓ 调 `S.blogView`（回看偏移：0=最新、越大越旧；drawBattle 每帧 clamp 到 maxV）——玩家在战斗里等敌方回合时滚回上面重读「敌方招数」「某条伤害数字」是常态；读完后**发起任何行动**（1 攻击/2 技能/3 药水/4 逃跑/5 防御/6 蓄力），本次行动产生的新战报（「你造成 N 伤害（敌方 HP 剩余 X/Y）」「敌方攻击预判」等）按偏移被**顶出 3 行视窗**——战报窗口只显示 BLOG_WIN=3 行，回看 3 条以上时最新进度完全不可见，而右下角指示在「中途位置」只显示中性的「↑↓ 战斗记录」，玩家行动完看到的是「画面没动静」（实际战报在视窗外）——与「信息透明」主线直接矛盾（v19.x 起所有反馈都保证「结算即所见」，唯独回看偏移会悄悄藏掉结算）。
- 【修正：行动即回最新 + 指示直接报「离开最新几条」】`battle.js` `playerAction()` 在派发任何指令前加 `if (S.blogView > 0) S.blogView = 0`——发起行动 = 已读完历史、正在行动，视图跟随本次行动的战报（伤害/剩余 HP/敌方招数），零游戏逻辑、零结算，只复位显示偏移；被拒指令（MP 不足/未学技能/无药）同样复位（其拒绝战报也值得立刻看到）。`view/drawBattle.js` 指示两分支合并为单分支：最新（blogView<=0）保留 `↑↓ 回看战斗记录` 逐字；回看中（>0）直接显示 `新 N 条 · ↓ 回最新`（N=blogView，被顶出视窗的最新行数，与回看偏移同源，clamp 后自动按 clamp 值报数）——回看期间「离开最新多少条」明码，比旧「↓ 回到最新 / ↑↓ 战斗记录」两档中性文案更明确，且不区分是否到顶（信息量严格大于旧文案）。
- 【零回归面】未动任何战斗结算/遇敌/掉落/收益/BLOG_WIN/日志内容/时序；`main.js` ↑↓ 分派逐字不动（回看能力保留）；`startBattle` 的 `S.blogView=0` 开场复位逐字不动；drawBattle 战报窗口切片/行距/颜色/位置逐字不动，仅指示行文案分支合并；`S.blogView` 无新增字段（state.js 零改动）。只改 3 个文件（`battle.js`：playerAction +1 行 + 注释；`view/drawBattle.js`：指示 1 行 + 注释；`data.js`：GAME_VERSION v21.4→v21.5）+ `CHANGELOG.md`：本条。README 无需同步（未新增/改名文件、未改启动方式、无按键变化）。
- 验证：`node --check js/battle.js js/view/drawBattle.js js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响——测试无 blogView 断言）；新增 v21.5 专项冒烟（/tmp/jrpg_smoke_v215_blogfollow.mjs）**17 项断言全过**——版本递增 v21.5、BLOG_WIN=3 单一数据源、战报窗口切片逐值未变（view=2 → L1/L2/L3 可见、L0/L4/L5 不可见）、指示三态落位（最新「↑↓ 回看战斗记录」逐字保留 / 回看 2 条「新 2 条 · ↓ 回最新」/ 超界 99 被 clamp 到 3 后按 3 报）、旧中性文案「↑↓ 战斗记录」「↓ 回到最新」已不存在、日志<=3 行时不显示指示、playerAction 复位：防御/攻击/逃跑/被拒技能四路径 blogView 均归 0、blogView=0 行动不受影响、非 battle 场景短路不越界、startBattle 开场复位保留、main.js ↑↓ 分派原文保留；平衡不受影响（纯显示偏移复位 + 指示文案）。



- 【标题选槽只认数字键，方向键是死键】`main.js` `screens.title.onKey` 只处理 `/^[1-9]$/`（数字键直选）与 Enter/L/R——商店（S.shopSel ↑↓）、快速旅行（S.travelSel ↑↓）、暂停菜单（S.pauseSel ↑↓）、技能菜单（v19.48 光标）、图鉴/成就滚动（↑↓）全部方向键可导航，玩家在游戏任意其它界面养成「↑↓ 选、Enter 定」的肌肉记忆，回到标题页换存档槽却只能挪手去按数字键；且标题页槽位横排（槽1/槽2/槽3）天然是「←→ 循环」的横向列表。功能虽小，却与 v20.3「快捷键可发现性/一致性」主线直接冲突——0 引导的新玩家甚至不知道 1/2/3 能选槽。
- 【修正：←/→（含 A/D）循环切槽，纯输入层追加，零状态零结算】`main.js` title onKey 数字键分支后追加两支：`ArrowRight/d/D → S.curSaveSlot = (S.curSaveSlot % SAVE_SLOTS) + 1`、`ArrowLeft/a/A → S.curSaveSlot = ((S.curSaveSlot - 2 + SAVE_SLOTS) % SAVE_SLOTS) + 1`——槽位数量仍读 data.js `SAVE_SLOTS` 单一数据源（与数字键分支同源，加档位自动跟随），右向 1→2→3→1、左向 1→3→2→1 双向循环、首尾无缝；复用既有 `S.curSaveSlot` 与 `SFX.select()`（与数字键分支同样的声音反馈），零新增状态/导出/依赖。数字键 1/2/3、Enter、L 读档、R 重开四大既有分支逐字不动。
- 【可发现性同步收口：标题提示行 + 帮助页按键行各补一句】`view/menus.js` `drawTitle` 快捷键提示行改为 `按 1/2/3 或 ←/→ 选择存档槽 · …`（压缩「/」两侧空格抵消新增长度，整行估宽 ≈426px < 640 画布，不越界；与 main.js 分派逐字同源）；`data.js` HELP_PAGES 第 1 页「存档槽」行同步补 `或 ←/→`（估宽 ≈231px，远小于面板右缘 570 可用宽）——功能入口与帮助文档永远同步，杜绝「标题能按但没人知道」。
- 【零回归面】未动任何存档格式/槽位读写（saveKey/hasSlot/load/saveGame 逐字不动）、未动标题/创建/世界任何其它按键分派；A/D 仅在 title 场景生效（world 的 WASD 移动分派在 scenes.world.onKey，与 title 互不干扰）；帮助页 `>10 行才收紧 25px` 分支不受影响（第 1 页仍 14 行、行距 25 逐值不变）；只改 3 个文件（`main.js`：+2 分支；`menus.js`：1 行提示文案；`data.js`：GAME_VERSION v21.3→v21.4 + 帮助页 1 行）+ `CHANGELOG.md`：本条。README 无需同步（未新增/改名文件、未改启动方式）。
- 验证：`node --check js/main.js js/view/menus.js js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v21.4 专项冒烟（/tmp/jrpg_smoke_v214_titleslot.mjs）**25 项断言全过**——版本递增 v21.4、`SAVE_SLOTS===3`、右向 ArrowRight 从 1→2→3→1 三连跳逐值落位、左向 ArrowLeft 从 1→3→2→1 反向落位、A/D 键与方向键同效、数字键 1/2/3 行为逐字不变（直选不改符号）、越界键（ArrowUp/'x'/数字4）不改槽位、Enter 仍进 create、无档时 L 键不崩溃（load() 短路）、drawTitle 全流程渲染零抛错且提示行 fillText 含「←/→」与旧「/ 选择存档槽」被替换、HELP_PAGES 首行含「或 ←/→」、S.bgmTimer/场景未受影响。

## v21.3 战斗开场警报音——进战瞬间的听觉钩子，Boss/试炼与普通遭遇一听即分（体验打磨·音效反馈，v21.2 帮助页信息收口后首次轮换到「感官反馈」层——承 v20.7 施法反馈/v20.0 命中反馈等「反馈家族」：前三十余版已把文本/数值反馈做满，唯独「听觉」从未碰过——进战只有 BGM 换轨，遭遇瞬间没有自己的声音，且强敌战与杂兵战听觉零差别）

- 【进战瞬间全静默，强敌与杂兵无听觉区分】`battle.js` `startBattle()` 弹出「⚔️ 遭遇了 X！」后仅 `startBgm('battle')`——遭遇那一拍（含祭坛/水晶/试炼碑触发后延迟进场的整段前奏）没有任何瞬时音效；玩家只能靠 BGM 换轨判断进战，Boss/试炼与普通遭遇在听觉上无法区分——与视觉侧「威胁预警 ⚠️ / 敌方招数一览」的强敌辨识度相比，声音侧是空白（B 键重整旗鼓再战同样无声）。
- 【修正：战斗开场警报音，纯音效零结算】`audio.js` SFX 新增两支一次性音效：`SFX.alert()`（普通遭遇·两连下坠 494→392Hz square，起始 0/0.09s）、`SFX.boss()`（Boss/试炼·低沉双滑落 110→80 / 165→143Hz sawtooth + 330→200Hz triangle 渐弱尾音，起始 0/0.16/0.34s——全部只调既有 `tone()` 原语，零新增依赖）；`battle.js` `startBattle()` 在进战博客与 BGM 之间按 `isBossFoe(S.enemy)` 分派（与威胁预警 / `_bossRetry` / 图鉴口径同一函数）：普通与精英 = alert，幽冥魔王 / 洞窟领主 / 终焉之神 / 试炼三连 = boss。B 重整旗鼓与祭坛延迟进场都走 startBattle 自然复用；静音（M）时 `tone` 检查 `S.SND` 直接哑掉，零新增状态、零存档变化。
- 【零回归面】未动任何战斗公式 / 遇敌 / 掉落 / 任务 / 结算逻辑；`startBattle` 其余行（BGM / `_bossRetry` / 遭遇记录 / `enqueue(700)`）逐字不动；`MUSIC` / `bgmTick` / 既有 15 支 SFX 音色零变化。只改 3 个文件（`data.js`：GAME_VERSION v21.2→v21.3；`audio.js`：SFX 增 2；`battle.js`：startBattle 增 2 行）+ `README.md` 音效条目补一句。
- 验证：`node --check js/audio.js js/battle.js js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v21.3 专项冒烟（/tmp/jrpg_smoke_v213_battleentry.mjs）**21 项断言全过**——版本递增 v21.3、SFX.alert/boss 存在且为函数、静音态与无 AudioContext 态调用零抛错（tone 短路 / try-catch 兜底）、假 AudioContext 下 alert 调度 2 振荡器（494/392 · 0/0.09s）、boss 调度 3 振荡器（110/165/330 · 滑落 80/143/200 · 0/0.16/0.34s）逐项落位、startBattle 按 isBossFoe 分派：幽冥魔王 / 洞窟领主 / 终焉之神 / 试炼首关 → boss，史莱姆 / 石心魔像精英 → alert，进战流程（场景 / blog）完整走通。

## v21.2 帮助页「魔物状态」补两种精英的出没/守门信息——进图前一眼看清精英在哪、多稀有、用什么打（体验打磨·信息透明·战前知识中枢收口，v21.1 换装反馈收口后轮换回「帮助文档级」透明度——承 v19.59 帮助页排版修复 / v19.57 区域难度透明化：H 页已是战前知识中枢，却唯独缺两种精英的「出没条件」这一格）

- 【精英信息只存在于图鉴，而图鉴要已遭遇/已讨伐才揭示——战前是黑盒】H 帮助第 3 页「魔物状态」只有 石心魔像·石甲 的机制行（血<50%凝甲/至多3层/每层减伤40%），但「石心魔像在哪遇见、多稀有（雾语林 Lv3 起 · 约 7% 精英）、必掉什么（魔法蘑菇）」与「残焰魔像（无字回廊守门）用什么属性打（弱冰抗火）、有守名者委托」这两块战前关键信息只写在 图鉴（B）的已讨伐行与 README（游戏外）——玩家在进雾语林/无字回廊之前，从游戏内任何界面都无从得知「这图可能有精英、该不该准备雷/冰属性」，与「威胁预警/推荐等级/区域强度」同在的战前信息透明体系差了这一格（石心魔像的「约7%」是唯一一处图鉴与帮助页不同源的信息）。
- 【修正：第 3 页新增两行，字段全部单一数据源派生，零硬编码】`data.js` `HELP_PAGES[2]` 在「石心魔像·石甲」行后插入：`['石心魔像·出没','雾语林 Lv.' + ELITE_GATE_LV + ' 起 · 约' + Math.round(ELITE_CHANCE * 100) + '% 精英 · ' + (SPECIES['石心魔像'].tag || '')]` 与 `['残焰魔像','无字回廊守门 · 弱' + ELEM_NAME[SPECIES['残焰魔像'].weak] + '抗' + (ELEM_NAME[SPECIES['残焰魔像'].resist] || '') + ' · ' + ((NPCS[QUESTS.side_ember.npc] && NPCS[QUESTS.side_ember.npc].name) || '守名者') + '委托']`——门槛/概率读遇敌判定同源常量、必掉蘑菇读 SPECIES tag、弱点/抗性读 SPECIES weak/resist、委托 NPC 名读 NPCS/QUESTS，调任何一处这里的预告自动跟随（与同一页「战斗掉落/宝箱掉落」行的派生风格逐字一致）。行数 8→10 仍 ≤10，drawHelp 的 `>10 才收紧 25px` 分支不触发，既有 8 行渲染位逐像素不动；两行动基线 y=386、页脚 452，越界检查通过。
- 【零回归面】未动任何战斗/数值/遇敌/掉落/任务逻辑：`ELITE_CHANCE`/`ELITE_GATE_LV`/`SPECIES`/`NPCS`/`QUESTS`/`ELEM_NAME` 全部原样（仅被帮助页字符串新增引用）；未改 drawHelp/drawCodex 任何绘制；未动 HELP_PAGES 既有 4 页任何行。只改 2 个文件（`data.js`：GAME_VERSION v21.1→v21.2 + HELP_PAGES 第 3 页 2 行 + 8 行注释；`CHANGELOG.md`：本条）。README 无需同步（未新增/改名文件、未改启动方式）。
- 验证：`node --check js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响——测试无 HELP_PAGES 断言）；新增 v21.2 专项冒烟（/tmp/jrpg_smoke_v212_helpelite.mjs）**18 项断言全过**——版本递增 v21.2、帮助页仍 4 页、第 3 页 8→10 行（不触发 25px 重排）、两新行落位、石心魔像行门槛/概率/必掉蘑菇三项与 ELITE_GATE_LV(3)/ELITE_CHANCE(7%)/SPECIES.tag 逐字同源、残焰魔像行文本与 weak(冰)/resist(火)/守名者/委托 逐字同源、既有 石甲/战斗掉落/宝箱掉落/毒蛇中毒 四行逐字未动、drawHelp 布局数学（sp=34 末基线 420 < 页脚 452）不越界。

## v21.1 战斗随机掉落自动换装反馈追加面板攻/防前后对比——胜利结算白嫖升级一眼看清涨了多少（体验打磨·信息透明，v21.0 美术轮换后收口「换装反馈」最后一处黑盒——承 v20.5 商店购买「攻击 X→Y / 防御 X→Y」：换装事件只剩「战斗随机掉落自动装备」这一处没有面板对比，免费升级与花 220/180 金买装备的反馈不对称）

- 【商店买装备带前后对比，白嫖掉落却只报「已装备」】`rules.js` `rollDrop()` 装备档两分支（木剑/铁剑→秘银剑、布衣/皮甲→锁子甲）结算后仅返回「⚔️ 掉落武器：秘银剑 已装备！」——玩家在胜利结算拿到免费升级，想确认面板涨了多少仍需再按 I 看状态页；与 v20.5 商店购买换装反馈（`· 攻击 X→Y` / `· 防御 X→Y`）同一「换装反馈」家族，唯独掉落侧没有。
- 【修正：两条消息追加结算后面板对比，纯显示零结算】`rollDrop()` 两分支在改动 `hero.weapon`/`hero.armor` 前捕获 `hero.atkMax`/`hero.defMax`（typeof 兜底，旧档无该字段时保持原文案、绝不出现 undefined），`applyStats()` 结算后在同条消息末尾追加 `（攻击 X→Y）` / `（防御 X→Y）`——读数与商店购买/状态页/战斗对比行同源（applyStats 后 hero.atkMax/defMax），零数值变化；金币/药水/蘑菇/灵药四档与「已尽善→+60 金」兜底消息逐字不动。
- 【零回归面】未动 `DROP_EQUIP`/`DROP_POTION`/`DROP_MUSHROOM`/`DROP_ELIXIR`/`DROP_GOLD` 任何档位概率与数值；未改装备判定、`applyStats` 结算、`winBattle` 掉落后调用时序、`drops` 计数。只改 2 个文件（`rules.js`：rollDrop 两分支消息 + 6 行注释；`data.js`：GAME_VERSION v21.0→v21.1）。
- 验证：`node --check js/rules.js js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响——冒烟对 rollDrop 无字符串断言）；新增 v21.1 专项冒烟（/tmp/jrpg_smoke_v211_dropequip.mjs）**15 项断言全过**——`GAME_VERSION==='v21.1'` 且导出、rollDrop 装备档强制 roll=0 走通两分支、武器消息含「（攻击 X→Y）」且前后值 = applyStats 前后实测（baseStats(1) 攻 9 + 木剑 2 = 11 → + 秘银剑 9 = 18）、防具消息含「（防御 X→Y）」（baseStats(1) 防 5 + 布衣 1 = 6 → + 锁子甲 8 = 13）、秘银剑/锁子甲已持有档不再触发装备（+60 金兜底消息与 v19.83 口径逐字一致）、无 atkMax 字段旧档保持原文案无 undefined、药水/蘑菇/灵药三档消息未动（仍「剩余 N」）、drops 计数仅装备档 +1。

## v21.0 战斗背景按地图分区——雾语林/无字回廊不再共用通用星空，四张图四套战场（美术·分场景背景，v20.9 数值平衡后轮换回美术方向——承 improve-plan §2.2「战斗分场景背景：村/林/洞三套底 + Boss 径向光」：村外草地与星井矿洞早有两套底（v3.x），唯 雾语林 与 无字回廊 两图从拆模块起就落入兜底星空——进回廊打残焰魔像与在矿脉打骷髅兵视觉无差别，「恒暗被遗忘之地」的终局区战场毫无叙事在场感）

|- 【四张地图只有两套战斗背景】`view/drawBattle.js` `drawArena()` 只按 `curMap()` 分 `village`(草地渐变)/`cave`(矿洞渐变+青色晶尘) 两支，其余一律落入兜底 `else`（深蓝渐变+白色星点）——v13.5 加入的无字回廊、以及最早的雾语林，四年来都在共用这套通用星空；大地图侧早已是「村/林/洞/回廊」四套观感（草地/树荫/岩地晶尘/恒暗字符残屑），一进战斗却全部坍缩成同一片星空（improve-plan §2.2 第 3 项只完成到「村/洞两套底」）。
|- 【修正：`arenaTheme(map)` 单源入口 + 两支新主题】本文件新增导出 `arenaTheme(map)`：village→grass / dungeon→mist / cave→cave / gallery→ember / 其它→night 兜底——与 `drawArena` 绘制分支一对一；`drawArena` 改为按 theme 分支（同一 `createLinearGradient` 实例逐分支上色，村庄/矿洞两支的渐变色值与星尘循环逐字节不动）：`mist`（雾语林·月下之森）= 暗青绿渐变 + 7 团底部缓慢横漂的雾团（透明度随相位呼吸，与大地图「雾径晨雾」观感一致）；`ember`（无字回廊·被忘掉的暗廊）= 近黑紫渐变 + 14 粒上升渐隐的残焰火星（明灭闪烁，是这条走廊唯一的光——与 `drawTimeTint` 的「恒暗」与残焰魔像/初灯叙事对齐，替代通用星空白色星点）；night 兜底保留给未知/测试场景，通用星空不再是任何正式地图的默认。
|- 【零回归面】纯显示零结算：未动战斗公式/遇敌/掉落/存档/任务/成就任何逻辑；`drawBattle` 其余绘制（Boss 径向光、状态角标列、血条数值、战报窗口、震屏/闪光）逐字不动；新主题仅用既有 `CTX`/`CV` 能力（渐变/ellipse/fillRect），零新增依赖、零新增状态。只改 2 个文件（`view/drawBattle.js`：arenaTheme + drawArena 重写；`data.js`：GAME_VERSION v20.9→v21.0）+ `README.md` 视觉条目补一句。未改 package.json / index.html / 启动方式。
- 验证：`node --check js/view/drawBattle.js js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响——冒烟本就以 dungeon 地图调用 drawBattle，新分支被真实执行）；新增 v21.0 专项冒烟（`/tmp/jrpg_smoke_v210_arena.mjs`）**12 项断言全过**——版本递增 v21.0、五档主题映射逐项落位（village→grass/dungeon→mist/cave→cave/gallery→ember/未知→night）、四图主题两两互不重复、MAPS 恰为四图、四张图逐个 loadMap+置敌 drawBattle（新 mist/ember 分支与无敌人纯背景）零抛错、绘制后 curMap 与 G.map 同源、终焉之神（isTrue+phase2）在 ember 背景下绘制不抛错。

## v20.9 无字回廊守门精英「残焰魔像」战力与终局区标签脱节修复——守门战不再被普通怪比下去（数值平衡·精英基准抬升，v20.8 内容扩充后轮换回「数值平衡」方向——承 v20.6 三主线 Boss 战力修复：同一张地图里精英战力应高于普通怪、低于真 Boss）

|- 【走廊中段精英作为「守门考验」，数值却比 zone 缩放后的普通怪还低】`data.js` `EMBER_GOLEM` 自 v13.5 起固定 170/19/12，而玩家持双徽记、圣光之剑、龙鳞甲于 Lv10-11 进入无字回廊时，走廊 zone（hp×1.3/atk×1.2/def×1.15）缩放后的普通怪：石魔像 147/36/24、雾灵 103/37/17、骷髅兵 120/36/18——精英在攻击、防御、有效威胁上全面低于同图杂兵；实测普通怪普攻对 Lv10 玩家造成 34-38 伤害，残焰魔像仅 2 点，且陨石术约 378 伤害可一击秒杀（1 回合结束），与「终局区·极高难」「守门考验」叙事完全脱节。
|- 【修正：残焰魔像兵力抬升到「走廊精英」档位，单一数据源只改 data.js 一行常量】`data.js` `EMBER_GOLEM` 改为 340/44/28（hp/hpMax/atk/def 同步更新，xp 130、gold 260、火焰弱点/精英标签/任务链/守名者台词全部不变）——高于同图 Lv11 zone 缩放后的普通怪，整体战力仍远低于终焉之神（700/33/19，HP 仅约其一半）。Lv10 推荐装备下重击约 99 点（占满血 108 的 92%，满血可存活、低血需防御/用药），陨石术+普攻约 2 回合击杀；Lv9 以下/未换毕业装会承受真实压力，与 v20.6 Boss 修复同一「⚠Lv 推荐等级必须名副其实」原则。
|- 【零回归面】未动 `MON_BASE`/`SKILL_DATA`/`BOSS_*`/`ENCOUNTER`/`DIFF_SCALE`/装备/药水/掉落/成就/任务/存档逻辑；`rules.monReward`/`codexStats` 直读 `EMBER_GOLEM` 自动跟随新值；`world.js` 祭坛触发、`view/menus.js` 图鉴地点、`SPECIES` 火焰弱点/精英标记全部不变。只改 2 个文件（`data.js`：GAME_VERSION v20.8→v20.9 + `EMBER_GOLEM` 数值与注释；`CHANGELOG.md`：本条）。
|验证：`node --check js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响——测试对精英数值无硬编码）；新增 v20.9 专项冒烟（`/tmp/jrpg_smoke_v209_ember_balance.mjs`）**12 项断言全过**——版本 `v20.9`、`EMBER_GOLEM` 新值落位、`codexStats`/`monReward` 同源跟随、走廊 Lv11 zone 缩放后普通怪 hp/atk/def 全低于精英、终焉之神综合属性仍远高于精英、Lv10 推荐装备下重击不秒杀满血、Lv10 陨石术+普攻约 2 回合击杀、困难模式 Diff 倍数未动。

## v20.8 无字回廊新支线「残焰的安息」+ 新成就「残焰已熄」——终局区终于有了一条自己的委托（新支线/NPC·内容扩充，v20.7「关键资源即时可见」主线收尾后轮换回「新内容」方向——承 v19.46 守书记支线 / v19.53 守名者 NPC / v19.61 支线全清成就：四张地图里唯独无字回廊没有任何支线，中段精英「残焰魔像」作为终焉之神的守门考验没有任务挂钩）

- 【无字回廊是全游戏唯一没有支线的地图，守名者只有静态台词】`data.js` `QUESTS` 六条支线全部落在潮灯镇/雾语林/星井矿脉，终局区无字回廊空无一「线」：守名者（v19.53）点破回廊立意、指引残焰魔像/初灯，但只是科普性台词——玩家双徽记开门走进终局区、击败中段守门的残焰魔像后，这件事没有留下任何任务叙事/回报/里程碑（图鉴/bestiary 只计数），与「支线全清(灯火同心)」这一终局成就的完成路径也恰好缺这条主线尾巴。
- 【新增第六条支线 side_ember：讨伐残焰魔像换 100 金 + 1 高级灵药】`data.js` 新增 `QUESTS.side_ember`（守名者 giver/npc, 无 unlockOn → 与 side_mist/side_stone 同款完整 offer→accept→讨伐→交付流程，开局 J 日志即示「可接 · 无字回廊」，❕ 顶标自动跟随）；判据/进度/目标文案/接取对话同读新常量 `EMBER_GOAL=1`（与 MIST_GOAL/STONE_GOAL 同一「支线目标单一数据源」家族，已 export）；`cond` 读既有 `hero.bestiary['残焰魔像']` 计数（击杀即达成，零新状态/零存档结构变更）；reward `{gold:100, potion2:1}`（终局补给档）；对话四态齐全——offer 单页（保留原回廊立意口吻 + 委托钩子）、active、turnin 单页、done 页按 `hero.trueBoss` 分档：通关前讲残焰刻碑（不剧透初灯），通关后保留原「初灯的名字」彩蛋口吻（该 NPC 的 after 在有任务后由 quests.js 对话路由取代，彩蛋并入 done 档，无内容丢失）。
- 【新增成就「残焰已熄」+ 灯火同心自动扩为 6/6】`ACH_LIST` 新增 `{id:'ember', name:'残焰已熄', d:'完成守名者的嘱托（残焰魔像）'}`（与 cartman/mist/stone 同一「单支线成就」模式，读既有 g.quests.side_ember==='done'）；`allquests`（灯火同心）判定/描述/进度本就动态读 `QUESTS` 的 kind==='side' 列表，自动跟随为「完成全部 6 个支线任务」（已解锁存档不受影响——成就存于 g.ach 不回溯）。
- 【零回归面】未动任何战斗/数值/地图/存档逻辑：`MON_BASE`/`SKILL_DATA`/`BOSS_*`/`ENCOUNTER`/装备/药水/`rollDrop`/`winBattle` 逐字不动；未动 `NPC_SPOTS`/`MAPS.extras`（守名者坐标 8,5 原样，零新坐标零撞键）；未动 quests.js/world.js/battle.js/view 任何函数（任务状态机/对话路由/顶标/日志全部既有通用逻辑自动服务新条目）；未动旧五条支线任何字段。只改 3 个文件（`data.js`：GAME_VERSION v20.7→v20.8 + EMBER_GOAL + QUESTS.side_ember + ACH_LIST.ember + 帮助页/快速旅行无字回廊行「守名者(支线)」标注；`README.md`：成就数 21→22 两处 + 无字回廊条目补支线说明）。
- 验证：`node --check js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v20.8 专项冒烟（/tmp/jrpg_smoke_v208_emberquest.mjs）**46 项断言全过**——版本/定义/EMBER_GOAL 单源/开局 offer→accept→active→turnin→done 全链路（含 resolveNpcTalk 返回值、100 金+1 灵药入账、condProg 1/1 只、❕ 顶标三态）、done 页 trueBoss 分档（未通关不剧透初灯、通关后保留彩蛋）、ACH_LIST=22、ember 成就判定、灯火同心 5/6 与 6/6、横幅首位仍为灯长、既五条支线状态与对话零回归。

## v20.7 伤害技能施放反馈追加当前 MP——放完一招一眼看清还能不能再放（体验打磨·信息透明，「关键资源即时可见」主线收尾：v19.96 防御回蓝已报 MP / v19.97 治疗已报 HP / v20.0 命中已报敌方剩余 HP，唯独「伤害技能扣蓝」这一侧没有结算值）

- 【伤害技能命中后只报伤害与敌方剩余 HP，玩家看不到自己还剩多少 MP】`battle.js` `doSkill()` 在 `hero.mp -= skill.mp` 扣蓝后，`finishPlayer()` 仅把伤害/元素标注/蓄力与（v20.0 起的）敌方剩余 HP 拼进战报——玩家刚放完一招大额耗蓝的陨石术/雷鸣，想确认「当前 MP 还能不能再放一招、是否该转防御回蓝」仍需瞄 HUD 或按 I 看状态页；与 v19.96「防御回蓝带当前 MP」、v19.97「治疗回血带当前 HP」的同一「关键资源即时可见」主线不一致——「花蓝」是唯一没有结算反馈的资源动作。
- 【修正：伤害技能战报追加结算后当前 MP，纯显示零结算】`battle.js` `doSkill()` 伤害分支模板改为 `…造成 <dmg> 伤害${note}${charged ? '（蓄力）' : ''}！（MP ${hero.mp}/${hero.mpMax}）`——`hero.mp`/`hero.mpMax` 为 `hero.mp -= skill.mp` 扣减后的实时值（结算后、与技能菜单/状态页 MP 显示同源），v20.0 的「敌方 HP 剩余」后缀仍由 `finishPlayer` 按既有口径追加在句尾；治疗分支（v19.97 只报 HP）与普攻（不耗蓝）不在此列，不误带 MP 信息；未改变伤害公式、MP 消耗、元素克制、蓄力/石甲/灼烧/冻结判定、`afterPlayer()` 与胜负时序。
- 【零回归面】未动 `SKILL_DATA` 任何耗蓝/倍率数值；未改 `doAttack`/`finishPlayer`/`attackMove`/`enemyAct` 任何逻辑（敌方侧、普攻侧逐字不动）；未改 MP 不足拦截（v19.95 文案原样）；未动存档/成就/任务。只改 2 个文件（`data.js`：GAME_VERSION v20.6→v20.7；`battle.js`：`doSkill()` 1 条模板 + 4 行注释）。
- 验证：`node --check js/data.js js/battle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v20.7 专项冒烟（/tmp/jrpg_smoke_v207_skillmp.mjs）**12 项断言全过**——BOM 桩真实拉起 main.js 模块链，`playerAction('skill','火焰斩')` 走完 doSkill→attackMove→finishPlayer 全链路：扣蓝数值与旧版恒等（mp=先前-耗蓝）、战报含「（MP 扣后值/上限）」、同条战报仍含「（敌方 HP 剩余…）」（v20.0 完好）、普攻战报不含（MP 、蓄力施法（蓄力）与（MP ）同在且 charge 照常消耗、治疗战报含（HP ）不含（MP ）（v19.97 不受影响）、MP 不足拦截仍带所需/当前 MP 且不扣蓝不推进回合、GAME_VERSION 递增为 v20.7。

## v20.6 三主线 Boss 战力与 ⚠Lv 警告脱节修复——幽冥魔王/洞窟领主/终焉之神不再被陨石术一击秒杀（数值平衡·Boss 基准抬升，v19.38 时序体系收口后轮换回常规改进的第二十五版，本次切「数值平衡」方向——承 v19.43 骷髅兵门槛 / v19.58 经验曲线 / v19.66 金币曲线后，四个碾压级假问题里最后一个：主线 Boss 战力落后于技能分化时代）

|- 【三场主线 Boss 在自己的 ⚠Lv 推荐等级下是一键演出】`data.js` 三 Boss 基准（魔王 140HP/15攻/6防、领主 110/16/10、神 260/22/13）沿袭单文件时代——彼时技能未分化（火焰斩 1.8 / 冰霜击 2.2 / 雷鸣 2.8 / 陨石术 4.2 + 弱点×1.35），而玩家 atkMax=7+2·lv 线性成长、装备再叠加；实测（`rules.skillEstimate` 与 HUD 伤害预览同源函数）：⚠Lv（8/7/12）+ 推荐装备下 陨石术 期望伤害 305/263/415，对 140/110/260 全部一击秒杀；20k 蒙特卡洛（镜像 `enemyAct`/`attackMove`/phase2/石甲/回血/防御反击全公式）更显示 0 药裸打胜率 100%、战斗 1 回合——BOSS 的 phase2 变身/重击/回血机制根本没机会演出，「⚠Lv.X 推荐」这套全游戏信息透明体系对最大敌人完全失真是它的最大黑盒（普通怪反而比 Boss 打得疼：星井矿脉 Lv8 骷髅兵打我 29-35/血 vs 洞窟领主仅 7）。
|- 【修正：三 Boss 基准抬升到「⚠Lv 正面战」档位，单一数据源整链自动跟随】`data.js` BOSS_BASE/CAVE_BOSS_BASE/TRUE_BOSS_BASE 三行改为 魔王 420/23/13 · 领主 380/25/15 · 神 700/33/19——数值来自 12 组×6000 次蒙特卡洛参数扫描（真实行动表/石甲/回血/phase2/防御反击镜像）：⚠Lv 推荐装备下 2.6-3.0 回合正面战（魔王 Lv8 94.9% / 领主 Lv7 99.3% / 神 Lv12 100% 胜率，3 药与 0 药同档——药水不再决定胜负，phase2 重击×2.3 才是威胁：76/81/92 对上限血 94/87/122，满血可存活、低血必防御/用药）；低于 ⚠Lv 2 级胜率骤降（魔王 Lv6 3.2% / 神 Lv10 13.2%）——「该练到几级再来」第一次有真实后果；试炼三连战同源自动跟随（Lv11 约 43% / Lv12 100% 通关），仍是全游戏最重考验。改动面 100% 数据层：xp/gold/phase2 增益/acts/掉落/成就/剧情/困难倍率/装备/玩家曲线逐字不动；图鉴（rules.BOSS_DEFS→codexStats/monReward）、战斗预览（atkEstimate/skillEstimate）、威胁预警（threatWarn）、RUSH_BOSSES、BOSS.xp 报酬全部直读本表自动跟随，无第二套口径。
|- 【零回归面】未动 MON_BASE/ELITE_GOLEM/EMBER_GOLEM 任何数值（普通怪/精英/残焰魔像原样）；未动技能倍率/元素克制/暴击/蓄力/石甲/灼烧/中毒/冻结/防御反击公式；未动 DIFF_SCALE/TRUE_BONUS_GOLD/奖励 xp·gold；未动 ⚠Lv 标签（8/7/12/11）——标签首次与真实战力一致；未动存档结构/成就/任务/台词。只改 1 个文件（data.js：GAME_VERSION v20.5→v20.6 + 三行 BASE 数值 + 注释），零新增运行时状态/依赖。
|- 验证：`node --check js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响——测试对 Boss 均为「同源相对断言」boss.hp===BOSS.hp，自动跟随）；新增 v20.6 专项冒烟（/tmp/jrpg_smoke_v206_bossstats.mjs）**31 项断言全过**——版本递增、三 Boss 逐字段落位、RUSH_BOSSES 与主线逐值恒等、codexStats/monReward 同源且 xp/gold 未动、陨石期望×1.2 最大伤害 < 新 HP（不再一击秒）、phase2 重击 < ⚠Lv 满血（可存活）、残焰魔像/石心魔像/MON_BASE/⚠Lv 标签/phase2 增益/DIFF_SCALE/装备/玩家曲线逐值未动；真实 BOSS 常量蒙特卡洛复测（6000 次×12 场景）与扫描预期一致；开局平衡模拟（30k 蒙特卡洛）五档生还率与基线一致（普通裸刷 0.39% vs 基线 0.46%，噪声范围）。

## v20.5 商店购买反馈追加面板攻/防对比——买完武器/防具一眼确认这笔钱花得值不值（体验打磨·信息透明，把「关键资源即时可见」延伸到「装备效果可见」）

||- 【购买武器/防具只报价格与剩余金币，看不到新装备带来的面板变化】`shop.js` `buyWeapon()`/`buyArmor()` 结算后仅提示「装备了 X（-N 金，剩余 M 金）」——玩家刚大额消费换装，想确认「这钱花得值不值、攻击/防御变成多少」仍需再按 `I` 看状态页（「基础+装备=总面板」）；v19.75 已补剩余金币，但「装备效果」这一栏仍是盲区：换装是最直接的成长动作，面板变化却不落在购买反馈里。
||- 【修正：购买反馈追加面板总攻/防前后对比，纯显示零结算】`shop.js` 改为：在改动 `hero.weapon`/`hero.armor` 之前先捕获 `hero.atkMax`/`hero.defMax`（与状态页「基础+装备=总面板」同源读数），`applyStats()` 结算后在同一消息末尾追加 `· 攻击 X→Y` / `· 防御 X→Y`；当前值取自 `applyStats` 后的 `hero.atkMax`/`hero.defMax`（战斗伤害公式/状态页同源），且对旧档（max 值非数字）做了 `typeof` 兜底——此时保持 v19.75 原文案不显示对比，绝不出现 `undefined`。未改变价格、装备数值、购买判定、`applyStats`/`applyAchievements` 调用时序。
||- 【零回归面】未动 `WEAPONS`/`ARMORS` 数值与价格；未改 `buyPotion`/`sellMushroom`/`stayInn` 及其它商店分支；未改状态页/装备栏绘制、战斗伤害公式、掉落自动装备（rules.rollDrop）提示。只改 2 个文件（`data.js`：版本号 `v20.4→v20.5`；`shop.js`：`buyWeapon()`/`buyArmor()` 各 2 行捕获 + 1 条消息模板）。
||- 验证：`node --check js/data.js js/shop.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）。

## v20.4 新成就「灯火记忆」——记忆碎片全收集也有里程碑（新内容·收集向成就，v19.61「支线全清」后把 milestone 补全收尾——收集向主线里 图鉴全收集(perfection)/宝箱(chests)/支线全清(allquests) 都有成就，唯独贯穿强敌首胜掉落的 4 枚记忆碎片（守门人/灯卫/星砂/初灯，真结局加页·任务日志·名字石碑同读 FRAGMENTS 表）没有任何纪念）

|- 【记忆碎片作为核心叙事收集品却没有成就】`ACH_LIST` 20 项成就覆盖 战斗/等级/金币/掉落/图鉴/任务/宝箱/支线，而「集齐全部 4 枚记忆碎片」——需逐一击败 石心魔像/幽冥魔王/洞窟领主/终焉之神 四强收集、且是真结局「全记忆」页前置——作为收集向主线的最深一环，成就一览里连进度都没有：收藏型玩家在 J 日志看到「记忆 X/4」后，无法在 C 成就页得到对应里程碑回馈。
|- 【修正：ACH_LIST 末尾追加一条通用成就，零逻辑改动】`data.js` 新增 `{id:'memoir', name:'灯火记忆', d:'集齐全部 ${FRAGMENTS.length} 枚记忆碎片', ok:g=>FRAGMENTS.every(f=>(g.fragments||[]).includes(f.id)), prog:g=>`${(g.fragments||[]).length}/${FRAGMENTS.length}`}`——判定/进度全部同源于 FRAGMENTS 表（数量由 length 推导不写死，未来增删碎片自动跟随），读既有 g.fragments 存档字段（core.newGame 建档 `[]`、migrateQuests 旧档兜底均已就绪），与 perfection/chests/allquests 同一「通用 ACH_LIST + ok/prog」模式；解锁时机自动落在第 4 枚碎片（终焉之神首胜）写入之后的 hero.applyAchievements 既有调用点（battle.winBattle isTrue 分支），零新增计数/状态/依赖/结算；不设 r 字段（碎片本身无金币奖励，与 16 条无奖励成就一致，成就页不显示回报以免误导）。
|- 【零回归面】未动 battle/quests/world/core 任何逻辑；未动 ACH_LIST 既有 20 条（仅末尾追加 1 条）；成就判定/解锁横幅/成就页滚动/胜屏分母全走既有通用遍历（rules.unlockedAchievements / hero.applyAchievements / menus.drawAch「成就 X/21」自动跟随）；未动 FRAGMENTS/真结局/任务日志/名字石碑任何读取。只改 2 个文件（data.js：GAME_VERSION v20.3→v20.4 + ACH_LIST 追加 1 条；README.md：成就数 20→21 两处）。
|- 验证：`node --check js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v20.4 专项冒烟（/tmp/jrpg_smoke_v204_memoir.mjs）。

## v20.3 快捷键可发现性收口——成就一览(C)/快速旅行(T)/操作说明(H)补齐到常驻帮助条与开局教程（体验打磨·按键透明，把「信息透明」主线延伸到「按键表本身」）

|- 【常驻帮助条（index.html #help）与开局教程只列了部分快捷键，C(成就一览)/T(快速旅行) 两个核心系统完全不可发现】`index.html` 底部帮助条列了 WASD/Enter/Esc/P/F/I/J/B/M + 战斗 1-6 + 标题 L，唯独没有 `C`(成就一览) 与 `T`(快速旅行)，也未提示存在 `H`(操作说明/完整按键表)；`main.js` story 结束后的开局教程消息（`💡 教程：…H帮助`）同样只列 WASD/Enter/Esc/P/I/J/H——玩家照常驻帮助条操作，永远不会知道「C 看成就」「T 快速旅行」：成就一览是收集向玩家的核心回馈入口，快速旅行是主线后期频繁回城补给/前往下一区域的刚需，而这两个功能在 `screens.world.onKey` 早已实现（v2.x 起），唯独入口对玩家不可见——功能形同虚设。
|- 【修正：两处按键表补齐缺失入口，纯文本零逻辑】`index.html` #help 帮助条在 `B` 图鉴后补 `C` 成就 · `T` 旅行，并在 `M` 静音后补 `H` 操作说明（顺序与 main.js 教程消息、HELP_PAGES 完整表一致）；`main.js` 教程消息扩为 `WASD移动 · Enter对话 · Esc菜单 · P存档 · F喝药 · I状态 · J任务 · B图鉴 · C成就 · T旅行 · H帮助 · M静音`——把所有在 world 场景可用的按键一次性列出，与 HELP_PAGES 键位表（data.js）逐键一致。未增加、未更改任何按键分派：C/T/H 的处理链路（`screens.world.onKey`/`goto('ach')`/`goto('travel')`/`goto('help')`）逐字不动。
|- 【零回归面】未动 `main.js` 的 `screens` 按键表、`scene.js` goto、成就/快速旅行/图鉴/帮助任何逻辑；未动 `data.js` 任何数值（仅版本号 `v20.2→v20.3`，与 CHANGELOG 顶部同步）；未动 `index.html` 任何 DOM/CSS/脚本。只改 3 个文件（`index.html`：帮助条 1 行文本；`main.js`：教程 boxMsg 1 条字符串；`data.js`：`GAME_VERSION`）。
|- 验证：`node --check js/main.js js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；另写 `/tmp/jrpg_smoke_v203_hotkeys.mjs` 静态冒烟断言：帮助条与教程文本包含 C/T/H 键位、`GAME_VERSION==='v20.3'`。

## v20.2 敌方攻击反馈追加我方剩余 HP——挨打后一眼看清还扛不扛得住（体验打磨·信息透明，把「关键资源即时可见」主线延伸到敌方攻击命中反馈）

|||||- 【敌方攻击命中后只报伤害值，玩家看不到自己还剩多少血】`enemyAI.js` `enemyAct()` 在 `hero.hp -= dmg`（含防御减伤/重击倍率）结算后，`S.blog.push` 仅提示「造成 N 伤害」与是否被格挡/深渊之怒——玩家刚挨一刀，想确认「这血量还扛得住吗、是否该防御或喝药」仍需瞄 HUD 血条或凭记忆估算；与 v20.0「玩家攻击命中后追加敌方剩余 HP」、v20.1「敌方回血后追加敌方当前 HP」的同一「关键资源即时可见」主线不一致——敌我两侧的受击反馈只齐了「我打它」，还缺「它打我」这一半。
|||||- 【修正：敌方攻击命中后追加我方剩余 HP，纯显示零结算】`enemyAI.js` 改为：结算 `hero.hp -= dmg` 后，若 `hero.hp > 0` 在战报文案末尾追加 `（我方 HP ${hero.hp}/${hero.hpMax}）`。`hero.hp` / `hero.hpMax` 为防御减伤/重击倍率结算后的实时值，与 HUD 血条/状态页同源；致死一击（`hero.hp <= 0`）不追加，避免与后续败北提示重复（同 v20.0 击杀时不追加敌方剩余 HP 的口径）。未改变伤害公式（普攻/重击/防御格挡/深渊之怒）、`cmdDmg` 调用、反击判定、中毒施加、`loseBattle()` 时序任何逻辑。
|||||- 【零回归面】未动 `cmdDmg`/`DEFEND_MULT`/`HEAVY_MULT`/`HEAVY_MULT_PHASED`/`COUNTER_CHANCE` 任何数值；未改敌方行动表 `acts`/`pickAct()`；未改 `winBattle()` / `loseBattle()` / 毒与灼烧结算；未改 `view/drawBattle.js` 血条渲染。只改 2 个文件（`data.js`：版本号 `v20.1→v20.2`；`enemyAI.js`：`enemyAct()` 攻击分支 1 条 blog 文案 + 3 行注释）。
|||||- 验证：`node --check js/data.js js/enemyAI.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）。

## v20.1 敌方回血反馈追加当前 HP——敌人使用回血技能后一眼看清它当前还剩多少血（体验打磨·信息透明，把「状态一目了然」主线延伸到敌方治疗/回复行为）

|||||- 【敌方回血技能/行动只报恢复量，玩家看不到回血后的当前 HP】`enemyAI.js` `enemyAct()` 在 `act.type === 'heal'` 分支结算 `enemy.hp += heal` 后，`S.blog.push` 仅提示「恢复 N HP」——玩家刚看到敌方回血，想确认「它现在到底多少血、这回合输出是不是白打、还要几轮才能击杀」仍需瞄 HUD 血条或凭记忆估算；与 v20.0「玩家攻击命中后追加敌方剩余 HP」的同一「关键资源即时可见」主线不一致，且 Boss/精英回血（幽冥魔王/洞窟领主/终焉之神）往往数值较大，信息缺口更明显。
|||||- 【修正：敌方回血后追加当前 HP，纯显示零结算】`enemyAI.js` 改为：回血结算后立即在原战报文案后追加 `（敌方 HP ${enemy.hp}/${enemy.hpMax}）`。`enemy.hp` / `enemy.hpMax` 为 `enemy.hp += heal` 后的实时值，与 `battle.js` `attackMove()` 扣减后追加剩余 HP 同源、与 HUD 血条/战斗画面敌我血条数值同源；未改变回血公式、回血触发阈值、Boss 二阶段变身、石甲/灼烧/冻结/中毒/反击等任何结算逻辑，也不影响 `winBattle()` / `loseBattle()` 时序。
|||||- 【零回归面】未动 `MON_BASE`/`SKILL_DATA`/`HEAL_PCT`/`PHASE2_*`/敌方行动表 `acts` 任何数值；未改 `pickAct()` 行动挑选、`cmdDmg()` 伤害公式、`bind.drawBattle()` 血条渲染。只改 2 个文件（`data.js`：版本号 `v20.0→v20.1`；`enemyAI.js`：`enemyAct()` 治疗分支 1 条 blog 文案 + 注释）。
|||||- 验证：`node --check js/data.js js/enemyAI.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）。

## v20.0 玩家攻击/技能反馈追加敌方剩余 HP——命中后一眼看清怪物还剩多少血（体验打磨·信息透明，把「状态一目了然」主线延伸到战斗内进攻反馈）

||||- 【普通攻击与伤害技能命中后只报伤害值，玩家看不到敌方剩余 HP】`battle.js` `finishPlayer()` 在 `attackMove()` 扣减 `enemy.hp` 后，仅把 `<dmg>` 替换进文案推入战报——玩家刚用掉 MP 或一回合行动打出伤害，想确认「这怪还要几轮才死、是否该转防御/用药」仍需瞄 HUD 的血条或凭记忆估算；与 v19.97「治疗反馈带当前 HP」、v19.96「防御反馈带当前 MP」的同一「关键资源即时可见」主线不一致。
||||- 【修正：非击杀命中后追加敌方剩余 HP，纯显示零结算】`battle.js` 改为：若 `enemy.hp > 0`，在原战报文案后追加 `（敌方 HP 剩余 ${enemy.hp}/${enemy.hpMax}）`。`enemy.hp` / `enemy.hpMax` 为 `attackMove()` 扣减后的实时值，与 HUD 血条/状态页同源；未改变伤害公式、暴击/蓄力/石甲/弱点判定、胜负结算、`afterPlayer()` 与 `winBattle()` 时序。击杀时仍保持原流程（先推「被击败了」再调 `winBattle`），不追加剩余 HP 避免与击杀提示重复。
||||- 【零回归面】未动 `MON_BASE`/`SKILL_DATA`/`CRIT_*`/`CHARGE_MULT`/`SHIELD_MULT`/`BIG_DMG` 任何数值；未改 `doAttack()`/`doSkill()`/`attackMove()` 伤害结算与特效时序；未改 `enemyAI.js` 敌方行动；未改 `view/drawBattle.js` 血条渲染。只改 2 个文件（`data.js`：版本号 `v19.99→v20.0`；`battle.js`：`finishPlayer()` 新增 2 行后缀拼接 + 注释）。
||||- 验证：`node --check js/data.js js/battle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）。

## v19.99 接取任务反馈追加当前目标——接受 NPC 委托后一眼看清下一步要做什么（体验打磨·信息透明，把「进度一目了然」主线延伸到任务接取瞬间）

|||- 【接受任务后只报「接受了 XX」并提示按 J，玩家看不到当前目标】`core.js` `talkNext()` 在 `act.kind === 'accept'` 分支结算 `setSideQuest()` 后，`boxMsg` 仅提示「接受了「${act.name}」！按 J 可查看任务日志」——玩家刚接下灯长的蘑菇委托或猎手的雾灵委托，想确认「现在要做什么 / 还差多少」仍需再按 J 翻日志或看 HUD；与 v19.98「任务奖励带剩余量」、v19.94「喷泉恢复带数值」、v19.93「宝箱蘑菇带任务进度」的同一「信息透明」主线不一致。
|||- 【修正：接取任务文案追加实时目标，纯显示零结算】`core.js` 改为：`接受了「${act.name}」！当前目标：${questObjective(hero)}（按 J 查看任务日志）`。`questObjective(hero)` 与任务日志/状态页 HUD 同源（由 `quests.js` `questLines` / `objectiveText` 推导），任务进度、地点后缀、讨伐/采集计数均实时跟随；未改变任何任务接取判定、奖励结算、`goto('world')` 时序。
|||- 【零回归面】未动 `QUESTS` 各任务目标/奖励/解锁条件；未改 `resolveNpcTalk()` 接取逻辑；未改 `questObjective()` / `questJournal()` 返回值。只改 2 个文件（`data.js`：版本号 `v19.98→v19.99`；`core.js`：`talkNext()` 接受分支 1 条 boxMsg + 注释）。
|||- 验证：`node --check js/data.js js/core.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）。

# 潮灯记 · 变更日志

模块化 Canvas JRPG（v1.x 为单文件 `index.html`，v2.0 起拆分 `index.html` + `js/` ES Modules）。v4.0 执行 `improve-plan.md`。v5.0 换上全新剧情。

## v19.98 任务奖励反馈追加药水/灵药剩余量——交任务获得补给后一眼看清库存（体验打磨·信息透明，把「收入/获得后即时反馈库存」主线延伸到 NPC 任务奖励）

|||- 【任务奖励包含生命药水或高级灵药时只报「+1 药水/灵药」，玩家看不到剩余库存】`core.js` `talkNext()` 在 `act.kind === 'reward'` 分支已结算 `applyQuestReward()` 并在 v19.77 追加剩余金币，但当奖励 `act.item > 0` 或 `act.potion2 > 0` 时，文案仅显示 `+N 药水` / `+N 灵药`——玩家刚拿到任务补给，想确认「药水买到上限没有 / 高级灵药还剩几瓶」仍需再按 `I` 看状态页；与 v19.74「喝药带剩余量」、v19.78「购买药水带剩余量」、v19.83「战斗掉落带剩余量」的同一「补给库存即时可见」主线不一致。
|||- 【修正：任务奖励文案追加结算后药水/灵药剩余量，纯显示零结算】`core.js` 改为：若奖励含生命药水则追加「药水 ${hero.item}/${POTION_CAP} 瓶」、含高级灵药则追加「灵药 ${hero.potion2} 瓶」，接在剩余金币之后，用「；」分隔。`hero.item` / `hero.potion2` 为 `applyQuestReward()` 结算后的实时值，`POTION_CAP` 与 `buyPotion()` 背包上限同源；未改变任何任务奖励数值、任务接取/完成判定、成就结算、`goto('world')` 时序。
|||- 【零回归面】未动 `QUESTS` 各任务 `reward` 数值与 `applyQuestReward()` 结算；未改 `talkNext()` 其它分支（接受任务/翻页/离开）；未改存档结构/任务状态/成就判定。只改 2 个文件（`data.js`：版本号 `v19.97→v19.98`；`core.js`：`talkNext()` 奖励分支 1 条 boxMsg + 新增 `POTION_CAP` import + 注释）。
|||- 验证：`node --check js/data.js js/core.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）。

## v19.97 治疗技能反馈追加当前 HP——战斗中回血/解毒后一眼看清剩余血量（体验打磨·信息透明，把「状态一目了然」主线延伸到战斗内治疗/净化技能）

||- 【治疗/净化技能释放后只报恢复量，玩家看不到当前 HP】`battle.js` `doSkill()` 在 `skill.kind === 'heal'` 分支结算 `hero.hp = Math.min(hero.hpMax, hero.hp + heal)` 后，`S.blog.push` 仅提示「恢复 N 点 HP」与是否净化毒素——玩家刚用掉 MP 回血或解毒，想确认「当前血量是否安全、能否撑过下一轮敌方攻击」仍需瞄 HUD 或按 `I` 看状态页；与 v19.96「防御反馈追加当前 MP」的同一「关键资源即时可见」主线不一致。
||- 【修正：治疗文案追加结算后当前 HP，纯显示零结算】`battle.js` 改为：`💚 ${hero.name} 使出【${skillName}】，恢复 ${heal} 点 HP${extra}${charged ? '（蓄力保留）' : ''}！（HP ${hero.hp}/${hero.hpMax}）`。`hero.hp` / `hero.hpMax` 为回血/解毒后的实时值，直接读结算后的状态，与状态页 HP 显示同源；未改治疗量公式、MP 消耗、净化逻辑、蓄力保留机制、`bind.renderHUD()` 与 `afterPlayer()` 调用时序。
||- 【零回归面】未动 `SKILL_DATA` 治疗类技能数值/MP/效果；未改 `doSkill()` MP 不足拦截、气场封印、伤害技能分支；未改 `applyStats`/`grantXp`/战斗胜负结算；未改 `enemyAI.js` 敌方行动；未改 `SFX.heal()` 时机。只改 2 个文件（`data.js`：版本号 `v19.96→v19.97`；`battle.js`：`doSkill()` 治疗分支 1 条 blog 文案 + 注释）。
||- 验证：`node --check js/data.js js/battle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）。

## v19.87 存档成功反馈追加角色摘要——按 P 或菜单存档后一眼确认写入哪一档（体验打磨·信息透明，把「进度/金币即时可见」主线延伸到手动存档操作）

||- 【存档成功时只报「已存档到槽 N」，玩家看不到角色进度与金币】`core.js` `saveGame()` 在写入 localStorage 后，仅设置 `S.saveMsg = 💾 已存档到槽 ${S.curSaveSlot}`——玩家多槽存档或想确认「当前角色/等级/所在地图/身上多少金币」是否已写入，仍需再按 `I` 看状态页；与 v19.86「读档追加角色摘要」的同一信息透明主线不一致。
||- 【修正：存档成功文案追加角色名/等级/地图名/金币，纯显示零结算】`core.js` 改为：`💾 已存档到槽 ${S.curSaveSlot}：${hero.name} Lv.${hero.level} · ${MAPS[hero.map].name} · ${hero.gold} 金`。`hero` 为 `saveGame()` 写入前的实时档，`MAPS[...].name` 与读档摘要/状态页地图名同源；调地图名/金币显示只改 `data.js` 一处，提示自动跟随；未改变存档结构/写入/迁移、`localStorage` 键值、`SFX.select()` 时机、`saveMsg` 清除逻辑（仍 1800ms 后清空并刷新 HUD）。覆盖 `P` 键快速存档与 `Esc` 菜单「存档」两种入口。
||- 【零回归面】未动存档结构/读写/迁移；未改标题页渲染、创建页、难度选择；未改 `load()` 返回值与异常处理；未改 `S.curSaveSlot` 选择逻辑。只改 2 个文件（`data.js`：版本号 `v19.86→v19.87`；`core.js`：`saveGame()` 1 条 saveMsg + 注释）。
||- 验证：`node --check js/data.js js/core.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）。

## v19.86 读档反馈追加角色摘要——读取存档后一眼确认读到哪一档（体验打磨·信息透明，把「进度/金币即时可见」主线延伸到标题页读取操作）

|- 【读取存档成功后只报「读取了槽 N 的存档」，玩家看不到角色进度与金币】`main.js` 标题场景 `L` 键读取成功后的 `boxMsg` 仅提示槽号——玩家多槽存档或久未游玩时，想确认「读到哪个角色、在哪张图、身上多少金币」仍需再按 `I` 看状态页；与 v19.65「存档预览摘要」、状态页信息的同一「进度一目了然」主线不一致。
|- 【修正：读档文案追加角色名/等级/地图名/金币，纯显示零结算】`main.js` 改为：`💾 读取槽 ${S.curSaveSlot}：${S.G.name} Lv.${S.G.level} · ${MAPS[S.G.map].name} · ${S.G.gold} 金`。`S.G` 为 `load()` 成功后的实时档，`MAPS[...].name` 与存档预览/状态页地图名同源；调地图名/金币显示只改 `data.js` 一处，提示自动跟随；未改变读档流程、`goto('world')`、HUD 刷新、标题页其它按键任何逻辑。
|- 【零回归面】未动存档结构/读写/迁移；未改标题页渲染、创建页、难度选择；未改 `load()` 返回值与异常处理；未改 `S.curSaveSlot` 选择逻辑。只改 2 个文件（`data.js`：版本号 `v19.85→v19.86`；`main.js`：标题 `L` 键读取成功 1 条 boxMsg + 注释 + `MAPS` import）。
|- 验证：`node --check js/data.js js/main.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）。

## v19.85 图鉴收集完成奖励追加剩余金币——解锁「尽善尽美」后一眼看清兜里还剩多少（体验打磨·信息透明，v19.84 补齐真结局额外金币余额后，继续把「收入后即时反馈余额」主线延伸到图鉴全收集成就奖励）

|- 【图鉴收集完成（尽善尽美）时只报「额外 300 金币」，玩家看不到剩余金币】`hero.js` `applyAchievements()` 在判定 `id === 'perfection'` 后结算 `hero.gold += PERFECTION_GOLD`，boxMsg 仅提示额外奖励数额——玩家刚完成全魔物图鉴收集拿到一笔大额金币收入，想确认「兜里现在一共多少」仍需再按 `I` 看状态页；与 v19.80「普通战斗胜利带剩余金币」、v19.81「升级成功带剩余金币」、v19.82「试炼通关带剩余金币」、v19.83「随机掉落带持有/剩余量」、v19.84「终焉之神胜利带剩余金币」的同一信息透明主线不一致。
|- 【修正：图鉴完成奖励文案追加结算后剩余金币，纯显示零结算】`hero.js` 改为：`🏆 图鉴收集完成！额外奖励 ${PERFECTION_GOLD} 金币！（剩余 ${hero.gold} 金）`。`hero.gold` 为 `hero.gold += PERFECTION_GOLD` 结算后的实时值，调 `PERFECTION_GOLD` 只改 `data.js` 一处，提示自动跟随；未改变图鉴统计、成就判定、解锁时机、`applyAchievements` 其它成就分支、存档任何逻辑。
|- 【零回归面】未动 `PERFECTION_GOLD`/`ACH_LIST` 数值与成就判定；未改 `unlockedAchievements`/图鉴计数；未改 `view/drawStatus` 状态页绘制；未改其它成就解锁文案；未改 `SFX.levelup()` 时机。只改 2 个文件（`data.js`：版本号 `v19.84→v19.85`；`hero.js`：`applyAchievements()` 1 条 boxMsg + 注释）。
|- 验证：`node --check js/data.js js/hero.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）。

## v19.84 终焉之神胜利反馈追加剩余金币——击败真 Boss 拿到额外 300 金币后一眼看清兜里还剩多少（体验打磨·信息透明，v19.83 补齐战斗随机掉落持有/剩余量后，继续把「收入后即时反馈余额」主线延伸到真结局之战的额外金币奖励）

|- 【击败终焉之神时只报「额外 300 金币」，玩家看不到剩余金币】`battle.js` `winBattle()` 在 `enemy.isTrue` 分支结算 `hero.gold += TRUE_BONUS_GOLD` 后，boxMsg 仅提示额外奖励数额——玩家刚完成真结局之战拿到一笔大额金币收入，想确认「兜里现在一共多少」仍需再按 `I` 看状态页；与 v19.80「普通战斗胜利带剩余金币」、v19.81「升级成功带剩余金币」、v19.82「试炼通关带剩余金币」、v19.83「随机掉落带持有/剩余量」的同一信息透明主线不一致。
|- 【修正：终焉之神胜利文案追加结算后剩余金币，纯显示零结算】`battle.js` 改为：`✨ 初灯的意志散了。记忆回到镇上。额外 ${TRUE_BONUS_GOLD} 金币！（剩余 ${hero.gold} 金）`。`hero.gold` 为 `hero.gold += TRUE_BONUS_GOLD` 结算后的实时值，调 `TRUE_BONUS_GOLD` 只改 `data.js` 一处，提示自动跟随；未改变真结局结算、记忆碎片掉落、终焉之神状态置位、ending 跳转任何逻辑。
|- 【零回归面】未动 `TRUE_BONUS_GOLD` 数值与战斗胜负结算；未改 `enemy.isBoss`/`isCaveBoss`/`isRush` 分支文案；未改 `applyAchievements`/成就判定；未改 `goto('ending')`/结局绘制；未改 `stopBgm`。只改 2 个文件（`data.js`：版本号 `v19.83→v19.84`；`battle.js`：`winBattle()` 真结局分支 1 条 boxMsg + 注释）。
|- 验证：`node --check js/data.js js/battle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）。

## v19.83 战斗随机掉落反馈追加持有/剩余量——击败魔物后的额外掉落一眼看清兜里还剩多少（体验打磨·信息透明，v19.82 补齐试炼通关余额后，继续把「收入后即时反馈余额/库存」主线延伸到 38% 概率触发的随机额外掉落）

|||||||- 【战斗随机掉落金币/药水/蘑菇/高级灵药时只报本次收益，看不到持有总量或剩余数量】`rules.js` `rollDrop()` 在装备档已尽善（武器/防具已为秘银剑/锁子甲）后转为 `DROP_GOLD` 金币、或触发药水/蘑菇/高级灵药掉落时，分别返回 `✨ 宝箱：金币 +N`、`🍖 掉落：生命药水 ×1`、`🍄 掉落：魔法蘑菇 ×1`、`🧪 掉落：高级灵药 ×1`——玩家刚拿到一笔战斗额外收入或补给，想确认「兜里现在一共有多少金币 / 药水还剩几瓶 / 蘑菇几株 / 灵药几瓶」仍需再按 `I` 看状态页；与 v19.80「普通胜利带剩余金币」、v19.74「用药带剩余量」、v19.79「精英蘑菇带剩余量」的同一信息透明主线不一致。
|||||||- 【修正：掉落文案追加结算后持有总量/剩余数量，纯显示零结算】`rules.js` 改为：`✨ 宝箱：金币 +${DROP_GOLD}（共 ${hero.gold} 金）`、`🍖 掉落：生命药水 ×1（剩余 ${hero.item} 瓶）`、`🍄 掉落：魔法蘑菇 ×1（剩余 ${hero.mushrooms || 0} 株）`、`🧪 掉落：高级灵药 ×1（剩余 ${hero.potion2 || 0} 瓶）`。`hero.gold`/`hero.item`/`hero.mushrooms`/`hero.potion2` 为加法/扣减结算后的实时值（与结算同源），调 `DROP_GOLD`/掉落倍率只改 `data.js` 一处，提示自动跟随；未改变掉落概率、档位判定、装备升级分支、蘑菇非森林/矿脉地图回退药水的任何逻辑。
|||||||- 【零回归面】未动 `DROP_EQUIP`/`DROP_POTION`/`DROP_MUSHROOM`/`DROP_ELIXIR`/`DROP_GOLD` 数值与 `rollDrop` 档位判定/结算；未改 `battle.winBattle()` 调用时机与 `bind.boxMsg('🎁 额外掉落：' + drop)` 拼接逻辑；未改图鉴/成就判定；未改帮助页「战斗掉落」标注（仍同读 `data.js` 的 `DROP_*`）。只改 2 个文件（`data.js`：版本号 `v19.82→v19.83`；`rules.js`：`rollDrop()` 4 条返回文案 + 注释）。
|||||||- 验证：`node --check js/data.js js/rules.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）。



||||||- 【试炼三连战通关后只报奖励数额，玩家看不到剩余金币】`battle.js` `winBattle()` 在 `enemy.isRush` 分支完成三场连胜后，结算 `hero.gold += reward`（`reward = rushReward(hero.level)`）并弹出 `🌈 试炼通关！奖励 N 金币！…` 横幅——玩家刚拿到一笔大额金币收入，想确认「接下来能不能买药水/装备/住店」仍需额外按 `I` 看状态页；与 v19.80「普通战斗胜利带剩余金币」、v19.81「升级成功带剩余金币」的同一信息透明主线不一致。
||||||- 【修正：试炼通关文案追加结算后剩余金币，纯显示零结算】`battle.js` 改为：`🌈 试炼通关！奖励 ${reward} 金币！灯火记得你的名字！（剩余 ${hero.gold} 金）`。`hero.gold` 为 `hero.gold += reward` 结算后的实时值，调 `RUSH_BASE_GOLD`/`RUSH_GOLD_PER_LV` 只改 `data.js` 一处，提示自动跟随；未改变试炼生成、关间回血、连胜判定、rushReward 公式、真结局/普通战斗/升级分支任何逻辑。
||||||- 【零回归面】未动 `RUSH_BOSSES`/试炼三连战阵容/关间恢复 `RUSH_RECOVER` 数值与结算；未改试炼碑阵容/奖励预览徽章；未改 `rules.rushReward` 计算；未改 `applyAchievements`/成就判定；未改 `winBattle` 其它分支（精英蘑菇/普通胜利/升级/Boss 战/真结局）。只改 2 个文件（`data.js`：版本号 `v19.81→v19.82`；`battle.js`：试炼通关 `boxMsg` 1 条 + 注释）。
||||||- 验证：`node --check js/data.js js/battle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.82 专项冒烟 `/tmp/jrpg_smoke_v1982_trialgold.mjs` **9/9 全过**——版本号递增、试炼通关文案同时含「奖励 N 金币」与「剩余 X 金」、余额与 `hero.gold` 结算后同源、金币确实增加、`ACH_MSG_MS` 停留时长未变、rush 状态机正确归零置位。



|||||- 【升级成功时只报属性增长，玩家看不到剩余金币】`battle.js` `winBattle()` 在 `g.leveled` 分支结算 `hero.gold += enemy.gold` 后，boxMsg 仅提示等级与属性提升——玩家刚因一场胜仗升级，往往立刻想确认「接下来能不能买药水/装备/住店」却仍要额外按 `I` 看状态页；与 v19.80「普通战斗胜利带剩余金币」的同一信息透明主线不一致。
|||||- 【修正：升级文案追加结算后剩余金币，纯显示零结算】`battle.js` 改为：`🎉 等级提升到 Lv.${hero.level}！HP+${g.hp} MP+${g.mp} 攻+${g.atk} 防+${g.def}…（剩余 ${hero.gold} 金）`。`hero.gold` 为 `hero.gold += enemy.gold` 结算后的实时值，调怪物金币掉落只改 `data.js` 一处，提示自动跟随；未改变经验结算、升级判定、属性成长、试炼结算、Boss 战后特殊文案任何逻辑。
|||||- 【零回归面】未动 `MON_BASE`/怪物 `gold` 数值与战斗胜负结算；未改普通胜利分支文案；未改试炼 `isRush` 分支；未改 `rollDrop` 额外掉落文案；未改 HUD/存档/成就判定。只改 2 个文件（`data.js`：版本号 `v19.80→v19.81`；`battle.js`：`winBattle()` 1 条 boxMsg + 注释）。
|||||- 验证：`node --check js/data.js js/battle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）。

## v19.80 普通战斗胜利反馈追加剩余金币——击败魔物后一眼看清兜里还剩多少（体验打磨·信息透明，v19.79 补齐精英蘑菇掉落库存后，继续把「收入后即时反馈余额」主线延伸到最频繁的金币来源）

||||- 【普通战斗胜利时只报「获得 N 金币」，玩家看不到剩余金币】`battle.js` `winBattle()` 在非升级、非试炼分支结算 `hero.gold += enemy.gold` 后，boxMsg 仅提示获得的金币与经验——玩家刚拿到一笔战斗收入，想确认「接下来还能不能买药水/装备/住店」仍需额外按 `I` 看状态页；与 v19.75「商店购买装备带剩余金币」、v19.76「旅馆住宿带剩余金币」、v19.77「任务奖励带剩余金币」的同一信息透明主线不一致。
||||- 【修正：普通胜利文案追加结算后剩余金币，纯显示零结算】`battle.js` 改为：`🏆 胜利！获得 ${enemy.gold} 金币、${enemy.xp} 经验 · 距 Lv.${hero.level + 1} 升级还需 ${hero.xpNext - hero.xp} 经验（剩余 ${hero.gold} 金）`。`hero.gold` 为 `hero.gold += enemy.gold` 结算后的实时值，调怪物金币掉落只改 `data.js` 一处，提示自动跟随；未改变经验结算、升级判定、掉落判定、试炼结算、Boss 战后特殊文案任何逻辑。
||||- 【零回归面】未动 `MON_BASE`/怪物 `gold` 数值与战斗胜负结算；未改升级分支文案；未改试炼 `isRush` 分支；未改 `rollDrop` 额外掉落文案；未改 HUD/存档/成就判定。只改 2 个文件（`data.js`：版本号 `v19.79→v19.80`；`battle.js`：`winBattle()` 1 条 boxMsg + 注释）。
||||- 验证：`node --check js/data.js js/battle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）。

|||- 【击败石心魔像时只报「捡到 1 株魔法蘑菇」，玩家看不到剩余库存】`battle.js` `winBattle()` 在 `enemy.isElite` 分支增加 `hero.mushrooms` 后，boxMsg 仅提示「💎 从魔像残骸中捡到 1 株魔法蘑菇！」——玩家刚获得任务/酿造材料，想确认「包里现在几株 / 能不能直接交任务或酿造」仍需再按 I 看状态页；与 v19.73「卖菇带剩余库存」、v19.74「用药带剩余量」的同一信息透明主线不一致。
|||- 【修正：精英掉落文案追加结算后剩余蘑菇数，纯显示零结算】`battle.js` 改为：`💎 从魔像残骸中捡到 1 株魔法蘑菇！（剩余 ${hero.mushrooms || 0} 株）`。`hero.mushrooms` 为结算后的实时值（与 `hero.mushrooms++` 同源），调 `MUSHROOM_GOAL`/`BREW_MUSHROOMS` 只改 `data.js` 一处，提示自动跟随；未改变精英遭遇率、蘑菇掉落数量、任务保护判定、酿造配方、商店出售任何逻辑。
|||- 【零回归面】未动 `MON_BASE`/`encounter.js` 精英生成公式与 `rollDrop` 掉落判定；未改 `MUSHROOM_PRICE`/`MUSHROOM_GOAL` 数值；未改 `canSellMushroom` / `sellMushroom` 出售逻辑；未改 `mushroomQuestProtects` 任务保护。只改 2 个文件（`data.js`：版本号 `v19.78→v19.79`；`battle.js`：`winBattle()` 1 条 boxMsg + 注释）。
|||- 验证：`node --check js/data.js js/battle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）。

## v19.78 商店购买生命药水反馈追加剩余药水与金币——消费后一眼看清还剩多少（体验打磨·信息透明，v19.77 补齐任务奖励余额后，继续把「消费后即时反馈余额与库存」主线延伸到药水购买）

|||- 【商店购买生命药水时只报「购买成功：生命药水 +1（-N 金）」，玩家看不到剩余药水与金币】`shop.js` `buyPotion()` 在扣款、增加药水后，boxMsg 仅提示价格——玩家刚花掉一笔补给费用，想确认「药水买到上限没有 / 兜里还剩多少金币」仍需瞄 HUD 或按 I 看状态页；与 v19.74「喝药带剩余量」、v19.75「装备购买带余额」、v19.76「旅馆住宿带余额」、v19.77「任务奖励带余额」的同一信息透明主线不一致。
|||- 【修正：药水购买成功文案追加结算后剩余药水与金币，纯显示零结算】`shop.js` 改为：`购买成功：生命药水 +1（-${POTION_PRICE} 金，剩余 ${hero.item}/${POTION_CAP} 瓶 / ${hero.gold} 金）`。`hero.item`/`hero.gold` 为结算后的实时值（与 `buyPotion` 扣款/加库存同源），调 `POTION_PRICE`/`POTION_CAP` 只改 `data.js` 一处，提示自动跟随；未改变购买失败拦截、背包已满拦截、HUD 刷新、商店列表、其他商品购买/出售任何逻辑。
|||- 【零回归面】未动 `POTION_PRICE`/`POTION_CAP` 数值与 `buyPotion` 购买判定/扣款/加库存结算；未改金币不足拦截文案 `金币不足：生命药水需 ${POTION_PRICE} 金`；未改背包已满拦截文案；未改 `buildShopList` 商品列表与现有药水显示。只改 2 个文件（`data.js`：版本号 `v19.77→v19.78`；`shop.js`：`buyPotion()` 1 条 boxMsg + 注释）。
|||- 验证：`node --check js/data.js js/shop.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）。

## v19.77 任务奖励反馈追加剩余金币——交任务后一眼看清兜里还剩多少（体验打磨·信息透明，v19.76 补齐旅馆住宿剩余金币后，继续把「消费/收入后即时反馈余额」主线延伸到任务奖励这一高频收入交互）

|||- 【NPC 任务交付成功后只报「金币 +N」，看不到余额】`core.js` `talkNext()` 在 `resolveNpcTalk` 判定为 `reward` 时，由 `quests.applyQuestReward` 完成金币/药水/灵药结算后，boxMsg 仅提示 `🎁 「${act.name}」完成：金币 +${act.gold}…`——玩家刚拿到一笔任务收入，想确认「接下来还能不能买药水/装备/住店」仍需额外按 `I` 看状态页；与 v19.75「商店购买装备带剩余金币」、v19.76「旅馆住宿带剩余金币」的同一信息透明主线不一致。
|||- 【修正：任务奖励成功文案追加结算后剩余金币，纯显示零结算】`core.js` 改为：`🎁 「${act.name}」完成：金币 +${act.gold}${extra}（剩余 ${hero.gold} 金）`。`hero.gold` 为 `applyQuestReward` 加法结算后的实时值（与结算同源），调任务奖励金额只改 `js/quests.js` 一处，提示自动跟随；未改变任务接受、奖励发放、成就判定、NPC 对话翻页、任务日志任何逻辑。
|||- 【零回归面】未动 `QUESTS` 奖励数值与 `applyQuestReward` 结算；未改 `setSideQuest` / `questStatus` / `npcQuestPages` 任何任务状态流转；未改 `bind.renderHUD` / `applyAchievements` 调用时机；未改接任务 `accept` 分支文案 `接受了「X」！按 J 可查看任务日志`。只改 2 个文件（`data.js`：版本号 `v19.76→v19.77`；`core.js`：`talkNext()` 1 条 boxMsg + 注释）。
|||- 验证：`node --check js/data.js js/core.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.77 专项冒烟 `/tmp/jrpg_smoke_v1977_questgold.mjs` **11/11 全过**——版本号递增、任务奖励文案同时含「+N 金」与「剩余 X 金」、金币增加与余额同源、药水奖励同步显示、接任务分支与无奖励分支行为不变。

## v19.76 旅馆住宿反馈追加剩余金币——住店后一眼看清兜里还剩多少（体验打磨·信息透明，v19.75 补齐装备购买剩余金币后，继续把「消费后即时反馈余额」主线延伸到旅馆这一高频恢复交互）

||- 【旅馆住店成功后只报恢复与扣价，看不到余额】`shop.js` `stayInn()` 在 HP/MP 不满、金币足够时扣款并回满状态，boxMsg 只提示「🌙 你美美地睡了一晚，HP/MP 恢复！（-${INN_PRICE} 金）」——玩家刚花掉一笔住宿费，想确认「接下来还能不能继续买药水/装备」仍需额外瞄 HUD；与 v19.75「装备购买成功带剩余金币」、v19.73「卖菇带剩余库存/金币总量」的同一信息透明主线不一致。
||- 【修正：住宿成功文案追加结算后剩余金币，纯显示零结算】`shop.js` 改为：`🌙 你美美地睡了一晚，HP/MP 恢复！（-${INN_PRICE} 金，剩余 ${hero.gold} 金）`。`hero.gold` 为扣减后的实时值（与结算同源），调 `INN_PRICE` 只改 `data.js` 一处，提示自动跟随；未改变住店失败拦截、满状态不消费、HP/MP 恢复、成就判定、商店列表/购买/卖菇/酿造任何逻辑。
||- 【零回归面】未动 `INN_PRICE` 数值与住宿判定/恢复结算；未改金币不足拦截文案 `金币不足：住一晚需 ${INN_PRICE} 金`；未改满状态提示 `你现在精神饱满。`；未改 `applyStats` / 商店武器防具购买 / 卖蘑菇 / 酿造锅 / HUD 任何逻辑。只改 2 个文件（`data.js`：版本号 `v19.75→v19.76`；`shop.js`：`stayInn()` 1 条 boxMsg + 注释）。
||- 验证：`node --check js/data.js js/shop.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.76 专项冒烟 `/tmp/jrpg_smoke_v1976_inn.mjs` **10/10 全过**——版本号递增、住宿成功文案同时含「-INN_PRICE 金」与「剩余 X 金」、金币扣减与 HP/MP 回满同源、满状态不消费分支与金币不足分支行为不变。

## v19.75 商店购买武器/防具反馈追加剩余金币——大额换装后一眼看清兜里还剩多少（体验打磨·信息透明，v19.74 把消耗品使用反馈补全后，继续把「装备消费反馈」纳入同一信息透明主线）

|- 【商店购买武器/防具成功后只报「装备了 X（-N 金）」，玩家看不到余额】v19.67 已给商店/旅馆消费成功/失败文案带上品名与价格，但武器、防具属于一次性大额支出（勇者之剑 600 金、龙鳞甲 480 金），玩家确认「还能不能继续买药水/住店」时仍需额外瞄 HUD；与 v19.70「酿造成功带剩余材料」、v19.73「卖菇带剩余库存/金币总量」的「消费后即时反馈余额」主线不一致。
|- 【修正：武器/防具购买成功文案追加结算后剩余金币，纯显示零结算】`shop.js` `buyWeapon()` / `buyArmor()` 在扣款、换装、刷新属性后，boxMsg 改为：`装备了 ${name}（-${price} 金，剩余 ${hero.gold} 金）`。`hero.gold` 为扣减后的实时值（与结算同源），调 `WEAPONS`/`ARMORS` 价格只改 `data.js` 一处，提示自动跟随；未改变购买失败拦截、装备属性结算、成就判定、商店列表、旅馆/药水/蘑菇交易任何逻辑。
|- 【零回归面】未动 `WEAPONS`/`ARMORS` 数值与 `applyStats` 换装结算；未改金币不足拦截文案；未改 `buildShopList` 商品列表与攻防 delta 箭头；未改音效、渲染、存档、战斗/世界逻辑。只改 2 个文件（`data.js`：版本号 `v19.74→v19.75`；`shop.js`：`buyWeapon()` / `buyArmor()` 2 条 boxMsg + 注释）。
|- 验证：`node --check js/data.js js/shop.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.75 专项冒烟 `/tmp/jrpg_smoke_v1975_shopgold.mjs` **9/9 全过**——铁剑/秘银剑/皮甲购买后金币扣减正确、文案同时含「-价格 金」与「剩余 X 金」、余额与 HUD 同源。

## v19.74 喝药反馈追加剩余数量——F 键/战斗[3]用药后一眼看清还剩几瓶（体验打磨·信息透明，v19.73 补齐卖蘑菇剩余量后，继续把「消耗品使用反馈」做完整：购买/酿造/开箱/卖菇都已带剩余量，只有喝药还只报本次恢复量）

|- 【使用药水/高级灵药时只报「恢复 N HP/MP」，玩家看不到背包还剩几瓶】`core.js` `usePotion()`（大地图按 `F`）与 `battle.js` `doItem()`（战斗中按 `3`）在 `takePotion()` 扣减库存并回血回蓝后，仅弹 `使用药水，恢复 N 点 HP` / `服下高级灵药，恢复 N HP、M MP`——玩家刚在凶险之地或 Boss 战中嗑了一瓶，想确认「高级灵药还剩几瓶 / 药水还有几瓶」，必须再按 `I` 看状态页，与 v19.70「酿造成功带剩余材料」、v19.71「宝箱带持有总量」、v19.73「卖菇带剩余库存」的信息透明主线不一致。
|- 【修正：用药成功文案追加结算后剩余数量，纯显示零结算】`core.js` 改为：`🍖 使用药水，恢复 ${result.h} 点 HP（药水剩余 ${hero.item} 瓶）` / `🧪 服下高级灵药，恢复 ${result.h} HP、${result.m} MP（高级灵药剩余 ${hero.potion2} 瓶）`；`battle.js` 战斗日志同源追加 `（药水剩余 ${hero.item} 瓶）` / `（高级灵药剩余 ${hero.potion2} 瓶）`。`hero.item`/`hero.potion2` 为 `takePotion()` 扣减后的实时值（与结算同源），调药水/灵药上限只改 `data.js` 一处，提示自动跟随；未改变喝药判定、恢复量、满状态不浪费、战斗回合结算、存档任何逻辑。
|- 【零回归面】未动 `potionAvailability`/`takePotion` 可用性/恢复量公式、未改 `POTION_CAP`/`POTION_HP_PCT`/`POTION_HP_FLAT`/灵药常量数值、未改 HUD 计数/状态页资源行/商店购买逻辑。只改 3 个文件（`data.js`：版本号 `v19.73→v19.74`；`core.js`：`usePotion()` 2 条 boxMsg + 注释；`battle.js`：`doItem()` 2 条 blog 文案 + 注释）。
|- 验证：`node --check js/core.js js/battle.js js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.74 专项冒烟 `/tmp/jrpg_smoke_v1974_potionmsg.mjs` **4/4 全过**——大地图 F 键普通药水/高级灵药、战斗中[3]普通药水/高级灵药四路文案均含「剩余 X 瓶」，扣减数量、恢复量与库存计数逐字不变，满状态/无药拦截文案未改。

## v19.73 出售魔法蘑菇反馈追加剩余库存与金币总量——卖菇后一眼看清还剩多少（体验打磨·信息透明，v19.72 补齐状态页宝箱计数后，继续把「经济交互反馈」做完整：购买/酿造/开箱都已带剩余量，只有卖蘑菇还只报本次收入）

|- 【出售魔法蘑菇时只报「得 N 金」，玩家看不到剩余蘑菇和金币总量】`shop.js` `sellMushroom()` 在扣除 1 株蘑菇、增加 `MUSHROOM_PRICE` 金币后，仅弹 `售出 1 株魔法蘑菇，得 10 金`——玩家刚卖掉任务富余的蘑菇，想确认「背包还剩几株 / 兜里现在一共多少金币」，必须再按 `I` 看状态页，与 v19.71「宝箱带持有总量」、v19.70「酿造成功带剩余材料」的信息透明主线不一致。
|- 【修正：成功文案追加结算后剩余蘑菇与金币总量，纯显示零结算】`shop.js` 卖菇成功分支改为：`售出 1 株魔法蘑菇，得 ${MUSHROOM_PRICE} 金（剩余 ${hero.mushrooms} 株 / 共 ${hero.gold} 金）`。`hero.mushrooms`/`hero.gold` 为扣除/加法结算后的实时值（与结算同源），调 `MUSHROOM_PRICE` 只改 `data.js` 一处，提示自动跟随；未改变任务蘑菇保护拦截、商店列表、购买/住宿/酿造任何逻辑。
|- 【零回归面】未动蘑菇获取/任务保护判定/商店列表/购买换装/住宿恢复/酿造任何结算；`MUSHROOM_PRICE`/`MUSHROOM_GOAL` 数值逐字未动；「没有可出售的蘑菇」与任务保护拦截文案（v19.68）逐字保留。只改 2 个文件（`data.js`：版本号 `v19.72→v19.73`；`shop.js`：`sellMushroom()` 1 条 boxMsg + 注释）。
|- 验证：`node --check js/data.js js/shop.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；运行期冒烟验证：卖 1 株蘑菇后弹出「得 10 金（剩余 X 株 / 共 Y 金）」，库存与金币计数与 HUD 逐字一致；任务保护期拦截文案与行为不变。

## v19.72 状态页资源总览追加宝箱计数——按 I 一眼看清开箱进度（体验打磨·信息透明，v19.71 补齐宝箱反馈后继续把「探索深度指标」收进状态页）

|- 【状态页资源行仍缺「已开几个宝箱」】v19.40 补齐魔法蘑菇计数后，金币/药水/灵药/蘑菇/时间已在 I 状态面板资源总览行聚合，但「累计开启宝箱数」仍只活在 HUD 无此数字、成就页与图鉴页——玩家想核对「开箱寻宝」成就（累计 ${TREASURE_GOAL} 个）还差几个，必须切到 C 成就页或看图鉴底部，与「按 I 看角色全貌」的入口不一致。
|- 【修正：资源行追加 📦X/6，纯显示零结算】`view/menus.js` `drawStatus()` 资源行改为紧凑图标式：`金币:${hero.gold}  🍖:${hero.item} 🧪:${hero.potion2||0} 🍄:${hero.mushrooms||0} 📦:${chestCount(hero)}/${TREASURE_GOAL}  ⏱️${fmtTime(hero.time)}`。`chestCount(hero)` / `TREASURE_GOAL` 与成就「开箱寻宝」判定/进度同源于 `data.js`，调成就阈值只改 `data.js` 一处，状态页自动跟随；未改变宝箱掉落、开启结算、成就判定任何逻辑。`data.js` 同步导出 `chestCount`（此前为内部函数），保持单一数据源。
|- 【零回归面】未动宝箱掉落概率/蘑菇/金币/药水增减结算、未改成就判定、未改 `CHEST_GOLD_BASE`/`CHEST_GOLD_PER_LV`/`TREASURE_GOAL` 数值、未改地图/移动/遇敌/战斗逻辑。只改 2 个文件（`data.js`：版本号 `v19.71→v19.72` + 导出 `chestCount`；`view/menus.js`：导入 2 个常量 + `drawStatus()` 资源行 + 注释）。
|- 验证：`node --check js/data.js js/view/menus.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.72 专项冒烟 `/tmp/jrpg_smoke_v1972_statuschest.mjs` **12/12 全过**——`GAME_VERSION='v19.72'` 递增且导出、`TREASURE_GOAL=6` 与 `chestCount` 导出、计数函数对缺失/Set/数组三态正确、`drawStatus()` 在 0/3/6 箱均不抛错且资源行实际绘出 `📦:0/6`、`📦:3/6`、`📦:6/6`。

## v19.71 宝箱金币/药水反馈追加持有总量——开箱后一眼看清累计收入与药水库存（体验打磨·信息透明，v19.70 酿造成功追加剩余材料之后，把同一信息透明思路延伸到探索开箱）

|- 【开箱获得金币/药水时只报本次收益，玩家看不到持有总量】`world.js` `onChestStep()` 在金币分支弹「📦 宝箱！获得 N 金币」、药水分支弹「📦 宝箱！获得 1 个🍖 生命药水」——玩家刚拿到战利品，想确认「兜里现在一共有多少金币/药水」，必须再按 `I` 看状态页，与 v19.70「酿造成功带剩余蘑菇/金币」的信息透明主线不一致；而蘑菇分支此前已带「共 X 株」，三档掉落文案不齐。
|- 【修正：金币/药水成功文案追加结算后持有总量，纯显示零结算】`world.js` 金币分支改为：`📦 宝箱！获得 ${gold} 金币（共 ${hero.gold} 枚）`；药水分支改为：`📦 宝箱！获得 1 个🍖 生命药水（共 ${hero.item} 瓶）`。`hero.gold`/`hero.item` 为加法结算后的实时值（与结算同源），调金币基数/药水上限只改 `data.js` 一处，提示自动跟随；未改变宝箱掉落概率、蘑菇分支、喷泉/祭坛/传送任何逻辑。
|- 【零回归面】未动宝箱掉落判定与概率、未改蘑菇/金币/药水增减结算、未改成就判定、未改 `CHEST_GOLD_BASE`/`CHEST_GOLD_PER_LV`/`POTION_CAP` 数值、未改地图/移动/遇敌/战斗逻辑。只改 2 个文件（`data.js`：版本号 `v19.70→v19.71`；`world.js`：`onChestStep()` 2 条 boxMsg + 注释）。
|- 验证：`node --check js/world.js js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；运行期冒烟验证：金币宝箱弹出「获得 N 金币（共 X 枚）」、药水宝箱弹出「获得 1 个🍖 生命药水（共 Y 瓶）」、蘑菇宝箱「共 Z 株」行为不变，持有计数与 HUD 逐字一致。

## v19.70 酿造成功反馈追加剩余材料——高级灵药到手后一眼看清还剩多少蘑菇/金币（体验打磨·信息透明，v19.69 补齐酿造「材料不足」差额提示之后继续收尾成功分支的黑盒）

- 【酿造成功时只报「酿造成功」，玩家看不到剩余材料】`core.js` `brewNow()` 在材料充足分支扣除蘑菇/金币、产出高级灵药后，boxMsg 仅提示「🧪 酿造成功！高级灵药已放入背包（F/战斗[3]使用）」——玩家刚交出 2 株蘑菇 + 10 金币，想确认包里还剩多少蘑菇/金币，必须再按 `I` 看状态页，与 v19.67「商店/旅馆消费反馈带价格」、v19.68「任务蘑菇保护带进度」、v19.69「材料不足带差额」的信息透明主线不一致。
- 【修正：成功文案追加结算后剩余蘑菇/金币，纯显示零结算】`core.js` 酿造成功分支改为：`🧪 酿造成功！高级灵药 +1（剩余 ${hero.mushrooms} 蘑菇 / ${hero.gold} 金币；F/战斗[3]使用）`。`hero.mushrooms`/`hero.gold` 为扣除配方后的实时值（与结算同源），调配方只改 `data.js` `BREW_MUSHROOMS`/`BREW_GOLD` 一处，提示自动跟随；未改变酿造消耗/产出、任务保护、蘑菇获取/出售、成就判定任何逻辑。
- 【零回归面】未动酿造材料不足拦截分支、未改蘑菇任务保护判定、未改 `BREW_MUSHROOMS`/`BREW_GOLD` 数值与消耗、未改高级灵药产出/使用、未改 NPC 对话/商店/世界逻辑。只改 2 个文件（`data.js`：版本号 `v19.69→v19.70`；`core.js`：`brewNow()` 1 条 boxMsg + 注释）。
- 验证：`node --check js/core.js js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；运行期冒烟验证：酿造成功时弹出「高级灵药 +1（剩余 X 蘑菇 / Y 金币；F/战斗[3]使用）」，扣除数量、灵药增量与金币/蘑菇计数逐字不变；材料不足拦截文案 v19.69 行为不变。

## v19.69 酿造锅材料不足提示具体化——差多少蘑菇/金币一眼看清（体验打磨·信息透明，v19.68 蘑菇支线提示之后继续补齐酿造交互反馈的最后一处黑盒）

- 【酿造锅点击时若材料不足，只报笼统「材料不足，无法酿造！」】core.js `brewNow()` 在 `hero.mushrooms < BREW_MUSHROOMS || hero.gold < BREW_GOLD` 时拦截，但提示只有一句「材料不足」——玩家刚在野外采了蘑菇、卖了装备凑钱，想确认「到底还差几株蘑菇或几枚金币」必须再按 `I` 看状态页，与 v19.67「商店/旅馆反馈带品名/价格」、v19.68「任务蘑菇保护带当前进度」的信息透明主线不一致。
- 【修正：拦截文案追加配方与当前持有，纯显示零结算】`core.js` 酿造材料不足拦截改为：`材料不足：酿造需要 ${BREW_MUSHROOMS} 株蘑菇 + ${BREW_GOLD} 金币（当前 ${hero.mushrooms}/${hero.gold}）`。`BREW_MUSHROOMS`/`BREW_GOLD` 由 data.js 单一数据源导入，调配方只改 data.js 一处，提示自动跟随；未改变酿造消耗/产出、任务保护、蘑菇获取/出售、成就判定任何逻辑。
- 【零回归面】未动酿造成功分支、未改蘑菇任务保护判定、未改 `BREW_MUSHROOMS`/`BREW_GOLD` 数值与消耗、未改高级灵药产出/使用、未改 NPC 对话/商店/世界逻辑。只改 2 个文件（`data.js`：版本号 `v19.68→v19.69`；`core.js`：`brewNow()` 1 条 boxMsg + 注释）。
- 验证：`node --check js/core.js js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；运行期冒烟验证：材料不足时（0 蘑菇 0 金 / 1 蘑菇 10 金 / 2 蘑菇 5 金）分别弹出「需要 2 株蘑菇 + 10 金币（当前 0/0 / 1/10 / 2/5）」，拦截行为与金币/蘑菇计数逐字不变；材料充足时酿造成功文案与灵药产出不变。

## v19.68 任务蘑菇保护提示追加当前进度——酿造/出售被阻止时一眼看清还差几株（体验打磨·信息透明，v19.67 商店/旅馆反馈具体化之后继续补齐「蘑菇支线」交互反馈的最后一处黑盒）

- 【酿造锅/商店阻止出售任务蘑菇时，玩家看不到「当前已集齐几株」】core.js `brewNow()` 与 shop.js `sellMushroom()` 都会在灯长支线未交付且蘑菇数 ≤ `MUSHROOM_GOAL` 时拦截，但提示仅说「任务蘑菇尚未上交」「集齐 3 株前不能卖」——玩家刚在野外开了几个箱，想核对「到底还差几株才能交任务」还得再按 `I` 看状态页或瞄 HUD，与 v19.67「消费反馈带品名/价格」的信息透明主线不一致。
- 【修正：两处拦截文案追加 `当前 X/3 株` 进度，纯显示零结算】① `core.js` 酿造拦截：`🍄 任务蘑菇尚未上交（当前 ${hero.mushrooms}/${MUSHROOM_GOAL} 株），先去找灯长吧！`；② `shop.js` 出售拦截：`🍄 这是灯长委托的蘑菇，当前 ${hero.mushrooms}/${MUSHROOM_GOAL} 株，集齐 ${MUSHROOM_GOAL} 株前不能卖！`。`MUSHROOM_GOAL` 已由 data.js 单一数据源导入，调目标数只改 data.js 一处，提示文案自动跟随；未改变任何购买/出售/酿造/任务结算逻辑。
- 【零回归面】未动蘑菇获取/消耗/任务交付/成就判定；未改 `mushroomQuestProtects` 判定条件；未改 `canSellMushroom`/`buildShopList` 商店列表；未改酿造配方与消耗；未改世界开箱/精英掉落/宝箱逻辑。只改 3 个文件（`data.js`：版本号 `v19.67→v19.68`；`core.js`：1 条 boxMsg；`shop.js`：1 条 boxMsg）。
- 验证：`node --check js/core.js js/shop.js js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；运行期冒烟验证：任务蘑菇保护状态下酿造与出售均弹出带 `0/3`、`2/3`、`3/3` 进度的提示，拦截行为与金币/蘑菇计数逐字不变。

## v19.67 商店/旅馆反馈具体化——消费成功与失败都带品名和价格（体验打磨·信息透明，v19.66 金币曲线后轮换回「体验打磨」方向——商店购买成功只报「购买成功」、失败只报「金币不足」，旅馆成功只说「恢复」、失败只说「金币不足！」，玩家看不见这口消费到底花了多少、差多少，与 README 和界面其它地方「精确差价」的信息透明主线不一致）

- 【商店/旅馆的 boxMsg 反馈仍停留在笼统词组】购买生命药水成功时只弹「购买成功」，失败时只弹「金币不足」；购买武器/防具成功时只弹「装备了 X」，失败时只弹「金币不足」；旅馆住店成功只弹「🌙 你美美地睡了一晚，HP/MP 恢复！」，失败只弹「金币不足！」。这些提示没有价格，玩家需要自己在商品列表里核对金额，尤其是失败时不知道差多少、成功时不知道扣了多少。
- 【修正：shop.js 五处 boxMsg 带出品名/价格，纯显示零结算】生命药水成功 → `购买成功：生命药水 +1（-${POTION_PRICE} 金）`，失败 → `金币不足：生命药水需 ${POTION_PRICE} 金`；武器成功 → `装备了 ${name}（-${price} 金）`，失败 → `金币不足：${name} 需 ${price} 金`；防具同理；旅馆成功 → `🌙 你美美地睡了一晚，HP/MP 恢复！（-${INN_PRICE} 金）`，失败 → `金币不足：住一晚需 ${INN_PRICE} 金`。全部价格读 data.js 已有常量（POTION_PRICE / WEAPONS[].price / ARMORS[].price / INN_PRICE），与商品列表/结算同源，绝无第二套口径；停留时长保持默认，未引入新时序常量。
- 【零回归面】未动任何购买/出售/住宿结算逻辑，金币增减、装备换装、成就判定、药水上限、旅馆恢复逐字不变；商品列表 `buildShopList` 逐字未动；酿造锅/卖蘑菇/使用药水/死亡等其它 boxMsg 未改。只改 1 个文件（`shop.js`：5 条 boxMsg + 注释）。
- 验证：`node --check js/shop.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.67 专项冒烟 `/tmp/jrpg_smoke_v1967_shopmsgs.mjs` **15/15 全过**——药水/武器/防具/旅馆四路购买成功/失败消息均含品名与价格、金币扣除/未扣行为逐值恒等、满状态旅馆消息不变。

## v19.66 后期金币曲线平滑——四强怪每级金币 +1，让 Lv10「经验毕业」与「金币毕业」更同步（数值平衡，v19.38 时序体系收口后轮换回常规改进的第二十六版，本次切「数值平衡·金币节奏」方向——v19.58 已把四强怪每级经验 +1，将 Lv10→12 的升级场数从 6.1/8.1 场缓到 5.2/6.9 场；但金币收入仍严格线性 /lv 增长，而顶级装备价格固定，导致推荐等级进无字回廊时金币缺口仍在）

- 【v19.58 经验曲线平滑后，金币曲线仍是固定价格 vs 线性收入的缺口】Lv10 进无字回廊是设计上的「终局备战点」，按各图推荐等级自然刷级（镇→林→矿→廊）到 Lv10 的累计金币约 762 金（不含宝箱/任务/成就/卖蘑菇），而全身顶级装备 `勇者之剑 600 + 龙鳞甲 480 = 1080 金` 仍有约 318 金缺口，玩家必须额外 farming 约 5 场才能凑齐；经验曲线已放缓，金币曲线却没有同步，毕业装备的购买点略滞后于推荐等级。
- 【修正：data.js MON_BASE 四处 gold[1] 3→4，纯奖励曲线微调】与 v19.58 同一思路，只改上场较晚、承担中后期刷级/攒钱主力的「四强怪」骷髅兵/雾灵/树精/石魔像的 `gold[1]`（每级金币成长）3→4；四基础怪（史莱姆/野狼/哥布林/毒蛇）`gold[1]=2` 一处不动，确保 Lv1-2 新手收入逐字不变。抬升后 Lv10 自然累计金币增至约 876 金，缺口缩到约 204 金（少 farming 约 2 场），让玩家在推荐等级更接近「经验与金币同步毕业」。hp/atk/def/xp/权重/门槛/区域倍率全程未动，遇敌/掉落/战斗/成就/存档零影响；`rules.monReward` 与图鉴预览仍同读 MON_BASE 单一数据源，调一处两端自动跟随。
- 【零回归面】未动任何战斗/掉落/经验/难度/技能/成就/存档/任务判定与结算；WEAPONS/ARMORS 价格逐字未动；MON_BASE 其他 10 字段（hp/atk/def/xp/color/weak/resist/draw/minLv/poison）逐值不变，只有 4 个 `gold[1]` 条目变化。只改 1 个文件（`data.js`：GAME_VERSION v19.65→v19.66 + MON_BASE 四行 + 注释）。
- 验证：`node --check js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.66 专项冒烟 `/tmp/jrpg_smoke_v1966_goldcurve.mjs` **27/27 全过**——`GAME_VERSION='v19.66'` 递增且导出、四强怪 gold[1]=4 落位、四基础怪 gold[1]=2 未动、hp/atk/def/xp 逐值恒等、`monReward(骷髅兵/雾灵/树精/石魔像,10)` 金币分别为 55/54/56/60（修复前 45/44/46/50）、`monReward(史莱姆/哥布林,10)` 仍为 28/30、商店价格未动、Lv10 顶级装备缺口 <250 金；平衡模拟复测：困难裸刷 Lv1 死亡率等关键指标无变化（本改动不触战斗结算，生还率曲线与基线一致）。

## v19.65 存档槽预览追加「累计游玩时长」——多槽玩家一眼区分哪个档玩了多久（体验打磨·信息透明，v19.64 时间戳之后继续补全存档槽信息维度）

- 【标题画面存档槽只有「最后保存时间」，没有「这个档累计玩了多久」】v19.64 给 slotPreview 加上了相对时间（刚刚/ N分钟前），让玩家能识别最新档；但多槽玩家同时维护多个进度时，仍无法一眼看出「哪个档是主档、哪个只是随手试玩」——两个槽都显示「5分钟前」时，游玩深度无法从预览区分，只能进游戏按 I 查状态页或看结尾结算才能对比时长。
- 【修正：core.js 复用 fmtTime 算法，slotPreview 追加累计时长，零状态零结算】① 在 core.js 本地新增纯函数 `fmtTime(s)`：与 view/canvas.js / view/menus.js 状态页/结算页/死亡页的同算法逐字一致（`HH:MM:SS`，旧档无 time 字段按 0 显示 `00:00:00`），slotPreview 不依赖 view/ 层即可读取同源格式；② `slotPreview()` 在原有预览串末尾追加 ` · ⏱${fmtTime(hero.time)}`（ hero.time 由 main.js 游戏时钟按真实秒累进，既有字段）；③ `data.js` `GAME_VERSION` `v19.64 → v19.65` 同步。纯显示变化，未改保存/读取/时间累进/任何结算逻辑。
- 【零回归面】未动 saveGame / load / snapshotHero / 标题画面绘制/选槽/读档流程；`fmtAgo(savedAt)` 与难度标注字符串原样保留；新增 `fmtTime` 只被 slotPreview 调用；空槽仍返回 null；旧档无 `time` 字段兼容显示 `00:00:00`。只改 2 个文件（`data.js`：版本号；`core.js`：新增 `fmtTime` + `slotPreview` 一行 + 注释）。
- 验证：`node --check js/data.js js/core.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.65 专项冒烟 `tests/smoke_v1965_playtime.mjs` **6/6 全过**——`GAME_VERSION='v19.65'` 递增且导出、`slotPreview(1)` 返回字符串并包含姓名/等级/金币/地图/进度/难度/游玩时长 `02:03:04`、旧档无 `time` 字段显示 `00:00:00`、空槽返回 null。

## v19.64 存档槽「最后游玩时间」——多槽玩家一眼识别最新档（体验打磨·信息透明，v19.38 时序体系收口后轮换回常规改进的第二十五版，本次切「存档管理·时间戳」方向——此前标题画面三个存档槽只显示「✓ 有档」和角色摘要，玩家同时维护多槽时无法区分哪个槽最近玩过，只能凭记忆或逐槽读档确认）

- 【标题画面存档槽没有时间维度】三个槽的 slotPreview 只报 `名字 Lv.X 金币Y 地图·进度 · 困难`（v19.45 补了难度），但「这个档是刚刚玩的还是三天前的」完全没提示；多槽玩家想继续上次的进度，得凭记忆选槽，或 L 读档进去才知道是不是对的。
- 【修正：saveGame 写入 savedAt，slotPreview 显示相对时间，零状态零结算】① `core.js` 新增 `fmtAgo(ms)`：`<60s` 显示「刚刚」、`<1h` 显示「N分钟前」、`<24h` 显示「N小时前」、否则「N天前」；缺失/非数字时返回空串，旧档无 savedAt 字段完全兼容、不显示时间戳也不抛错。② `saveGame()` 在存档 JSON 顶层写入 `savedAt: Date.now()`（与 `G`、`chests` 同级，不影响 `load()` 只读 `data.G`/`data.chests` 的结构）。③ `slotPreview()` 在原有预览串后追加 `${fmtAgo(data.savedAt)}`，新档即时显示「刚刚」，旧档逐档在下次存档后获得时间戳；存档槽选择光标移动时实时刷新，无额外状态。
- 【零回归面】未动任何战斗/掉落/经验/金币/遇敌/成就/任务/存档读取逻辑；`load()` 仍只读 `data.G`/`data.chests`，顶层新增 savedAt 被忽略；`slotPreview` 原有姓名/等级/金币/地图/进度/难度字段逐字保留；标题画面绘制、选槽、读档流程逐字不变。只改 2 个文件（`data.js`：`GAME_VERSION` v19.63→v19.64；`core.js`：新增 `fmtAgo` + `saveGame` 一行 + `slotPreview` 一行）。
- 验证：`node --check js/core.js js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.64 专项冒烟（`/tmp/jrpg_smoke_v1964_save_time.mjs`）**7 项断言全过**——`GAME_VERSION='v19.64'` 递增且导出、`saveGame` 写入顶层 `savedAt` 数字时间戳、`slotPreview` 新档显示「刚刚」、旧档无 `savedAt` 兼容不显示、2 分钟前/3 小时前/2 天前分档正确、清理后无残留。

## v19.63 图鉴「残焰魔像」兵力/报酬揭示——无字回廊精英不再是图鉴里唯一的信息黑洞（信息透明·纯显示，v19.38 时序体系收口后轮换回常规改进的第二十四版，本次切「图鉴·信息空洞补格」方向——承 v19.50 普通魔物出没地 / v19.62 成就奖励揭示的「一览页信息透明」主线后，图鉴 13 种里只剩 残焰魔像 这一格没有兵力/报酬：石心魔像与三 Boss 各有分支（rules.monReward/codexStats 直读 ELITE_GOLEM/BOSS 常量），唯独 残焰魔像（无字回廊中段精英，170HP/19攻/12防、130 经验/260 金币的晚期强敌）漏了分支返回 null——图鉴已讨伐行因 `if(rw)` 守卫整行不绘，玩家辛辛苦苦打败这位回廊守门考验，翻图鉴却连它「值多少/多强」都看不到，与同类精英/三 Boss 的完整标注不一致）

- 【图鉴 12/13 种都有兵力/报酬行，只有残焰魔像整行缺失】drawCodex 已讨伐行 `if(rw) text('→ 击败可得：经验… 金币… HP… 攻… 防…')` 的 rw 来自 rules.monReward——普通 8 种读 MON_BASE、精英石心魔像读 ELITE_GOLEM、三 Boss 经 BOSS_DEFS[canonicalName] 直达，四路都有返回；残焰魔像(EMBER_GOLEM) 是五路里唯一没有分支的：monReward('残焰魔像')=null、codexStats('残焰魔像')=null，于是这行「击败可得」整行被 `if(rw)` 跳过，讨伐计数旁边只剩空白——它比 石心魔像 出现得更晚（Lv.10+ 回廊中段）、强度更高（170HP vs 148HP@Lv9），玩家最需要参考数据的正是这种晚期战前查阅场景。
- 【修正：rules.js 两处补 EMBER_GOLEM 单源分支，零结算零状态】① import 增 EMBER_GOLEM（data.js 已导出）；② monReward 增 `if (name === EMBER_GOLEM.name) return { xp: EMBER_GOLEM.xp, gold: EMBER_GOLEM.gold }`——固定兵力档（不随玩家等级成长，与 BOSS/CAVE_BOSS/TRUE_BOSS 同款「固定值直达」样式，注释注明此前漏分支导致的整行不绘）；③ codexStats 增同款分支返回 `{hp: EMBER_GOLEM.hp, atk: EMBER_GOLEM.def, def: EMBER_GOLEM.def}`。两函数本就只被 view/menus.js drawCodex 消费（纯图鉴显示），返回由 null→真值后唯一行为变化就是图鉴已讨伐行如实绘出 `→ 击败可得：经验 130 · 金币 260 · HP170 攻19 防12 · 🔥 回廊精英 · 弱点·冰×1.35 · 抗性·火×0.7`（兵力/报酬/标签全部直读 EMBER_GOLEM·SPECIES 同源数据，出没地沿用 v19.50 的 LOC 表「无字回廊中段」）；已遭遇未讨伐仍只揭示名字/出没地/「尚未讨伐」不剧透兵力（既有行为逐字不变）。宽度实测（@napi-rs/canvas 11px 真实字体）：新行 416.6px（124→540.6），仅比既有最长已讨伐行（雾灵 402.7px→526.7）长约 14px，两者都在面板右缘 560 内，不扩展图鉴渲染包络。
- 【零回归面】未动任何遇敌/掉落/经验/金币/难度/技能/战斗/成就/存档/任务判定与结算；monReward/codexStats 既有五路返回值逐值不变（普通/精英/三 Boss/未知名 null 全与旧版恒等，v19.63 冒烟逐值断言）；drawCodex 其余绘制（名字/出没地/讨伐计数/图鉴底部计数/滚动/未讨伐与已遭遇分支）逐字未动；残焰魔像实战掉落/经验/金币结算（winBattle 读 enemy.xp/enemy.gold 本体）与此纯显示补全互不相关。只改 2 个文件（data.js：GAME_VERSION v19.62→v19.63；rules.js：import + monReward/codexStats 各一分支 + 注释）。
- 验证：`node --check js/data.js js/rules.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.63 专项冒烟（/tmp/jrpg_smoke_v1963_embercodex.mjs）**31 项断言全过**——`GAME_VERSION='v19.63'` 递增且导出、EMBER_GOLEM 导出且为残焰魔像（hp170 atk19 def12 xp130 gold260）、BESTIARY_TARGET 含残焰魔像（13 种）、monReward(残焰魔像,任意级)={xp:130,gold:260}（固定档不随等级，修复前 null）、codexStats(残焰魔像)={hp:170,atk:19,def:12}（修复前 null）、两函数体直读 EMBER_GOLEM（无裸数值）、monReward/codexStats 既有条目（史莱姆/石心魔像/幽冥魔王/终焉之神/洞窟领主）逐值恒等、未知名仍 null；运行期以 BOM 桩真实导入模块链 drawCodex 已讨伐残焰魔像（第二页）渲染不抛错且实绘报酬行（经验 130 · 金币 260 · HP170 攻19 防12 · 🔥 回廊精英 · 弱点·冰×1.35 · 抗性·火×0.7）、行宽不扩展既有最长行包络（真实字体实测 540.6<560 面板右缘）、石心魔像/幽冥魔王报酬行零回归、空档页无任何报酬行（未讨伐不剧透）、已遭遇未讨伐标「尚未讨伐」且无兵力；旧版专项复跑仅「版本串钉死」断言随版本递增失败（v1962 37/38、v1961 29/30、v1950 21/23、v1941 29/31 各自 1-2 项为钉死失败，功能断言全过零回归）、v1960 14/14 全过。

## v19.62 成就页奖励揭示——四条带专享奖励的成就在一览页即标注回报（信息透明·纯显示，v19.38 时序体系收口后轮换回常规改进的第二十三版，本次切「成就·回报透明」方向——承 v19.61 给「支线全清」补了收官成就后，回头补齐成就页最后一处信息黑洞：20 项成就里 记忆守护者(+999金)、记得一切(+300金)、百炼成钢(试炼通关奖)、黎明归剑(圣光之剑) 四项带着实打实的专享回报，却只在解锁瞬间的横幅或战斗预览里一闪而过——未解锁的玩家翻成就页时完全无从得知「这项成就值多少」）

- 【成就页只报「是什么」不报「给什么」】ACH_LIST 20 项里四项有专属回报：记忆守护者 解锁即发 PERFECTION_GOLD 999 金（hero.applyAchievements 结算）、记得一切 战胜终焉之神另奖 TRUE_BONUS_GOLD 300 金（battle.winBattle 结算）、百炼成钢 的奖励即试炼三连战通关奖 150+lv×20 金（rules.rushReward 结算）、黎明归剑 的回报即传说武器圣光之剑本体（battle.winBattle 掉落）；这些数额此前只在 解锁瞬间横幅（「🏆 图鉴收集完成！额外奖励 999 金币！」）、战斗预览行（「战胜另+300金」「另奖 N 金币」）、试炼碑奖励预告（v19.60）出现——成就页 drawAch 只画 ✔/✘ 名称 + 描述 + 进度条，玩家在「该刷哪项成就」做决策时看不到回报，与「信息透明」主线相悖（任务日志早在卡片上标奖励，成就页是唯一没有回报标注的进度清单）。
- 【修正：data.js 四项 r 字段 + 试炼公式数据化 + menus.js 一处绘制，零结算零状态】① ACH_LIST 四项各增可选奖励串 `r`（其余 16 条无 r 不显示）：记忆守护者 `奖励 ' + PERFECTION_GOLD + ' 金币`、记得一切 `击败另奖 ' + TRUE_BONUS_GOLD + ' 金币`、百炼成钢 `通关另奖 ' + RUSH_BASE_GOLD + '+' + RUSH_GOLD_PER_LV + '×等级 金币`、黎明归剑 `获得 圣光之剑（攻+' + WEAPONS['圣光之剑'].atk + '）`——均与结算同源逐字恒等、绝无第二套口径；② 试炼公式 150+lv×20 由 rules.js 裸字面量收口为 data.js RUSH_BASE_GOLD=150 / RUSH_GOLD_PER_LV=20 两常量（rules.rushReward 改读此源，输出逐值恒等——成就标注与结算/预览/预告自此同一份数据，调试炼通关奖只改 data.js 一处）；③ view/menus.js drawAch 描述行右侧紧随以 12px 金色绘制 r（测宽基于描述同字体，实测四条合并行款 ≤273px 远小于面板右缘 560、亦不撞进度列 470），已解锁 #e8a858 / 未解锁 #a08a50 两档色。纯显示零结算变化，未动任何判定/奖励/阈值/存档。
- 【零回归面】未动任何战斗/掉落/经验/金币/遇敌/成就判定/任务/存档；PERFECTION_GOLD/TRUE_BONUS_GOLD/WEAPONS/试炼结算逐字未动（rushReward 输出 150+lv×20 恒等，v19.60 冒烟 14/14 复跑全过）；ACH_LIST 仍 20 条、ok/prog 逐字未动；成就页其余绘制（名称/描述/进度列/翻页/计数）逐值不变。只改 3 个文件（data.js：GAME_VERSION v19.61→v19.62 + RUSH_* 两常量 + 注释 + ACH_LIST 四项 r + export；rules.js：import + rushReward 一行改读常量；menus.js：drawAch 一段奖励绘制）。
- 验证：`node --check js/data.js js/rules.js js/view/menus.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.62 专项冒烟（/tmp/jrpg_smoke_v1962_achreward.mjs）**38 项断言全过**——`GAME_VERSION='v19.62'` 递增且导出、RUSH_BASE_GOLD=150/RUSH_GOLD_PER_LV=20 导出、rushReward(1/9/12)=170/330/390 逐值恒等且函数体已改读新常量（单一数据源）、四条成就 r 与常量/WEAPONS 同源逐字恒等（击败另奖 300 金币 · 通关另奖 150+20×等级 金币 · 奖励 999 金币 · 获得 圣光之剑（攻+24））、其余 16 条无 r、ACH_LIST 仍 20 条、源码层 ACH_LIST 块内恰 4 条 r 均引用常量无裸数值、运行期以 BOM 桩真实导入模块链：全成就页第二页渲染不抛错且实绘四条奖励串、奖励紧随描述同行（x=132+描述宽逐值一致）、合计右缘 <470 不撞进度列且 <560 不越面板、进度列仍右对齐 470（零回归）、空档第二页同样实绘四条奖励（r 不依赖解锁态）、第一页无奖励串、perfection/rush 判定不受 r 干扰；`/tmp/jrpg_smoke_v1961_allquests.mjs` 复跑 29/30 仅「版本串钉死 v19.61」1 项随版本递增失败（预期，功能断言全过零回归）、`/tmp/jrpg_smoke_v1960_trialreward.mjs` 14/14 全过（rushReward 数据化零行为变化）。

## v19.61 新成就「灯火同心」——支线全收集的收官徽记（新内容·成就，v19.38 时序体系收口后轮换回常规改进的第二十二版，本次切「成就补格」方向——v19.51「开箱寻宝」给宝箱这类高频行为补了成就，但「把五条支线全部做完」这个典型的晚期完成主义目标始终没有一项成就照应：灯长/星砂车夫/巡灯人/雾径猎手/守书记各有一条单支线成就，却缺一个「全勤」徽记把五条线收成一个整体目标；且五条支线横跨主线各阶段、做完即相当于把潮灯镇的委托网络整个维护了一遍，正是终局玩家回望一整局时最自然的成就节点）

- 【五条支线都有单成就，唯独没有「全部做完」的收官成就】19 项成就里 quest/cartman/names/mist/stone 五条单支线成就各自达成即可解锁，但没有一项成就回答「五条支线全部完成了吗」——玩家把 灯长·魔法蘑菇、星砂车夫·星砂之约（caveBoss 后）、巡灯人·旧灯卫的名字（4 枚碎片）、雾径猎手·雾灵×3、守书记·石壳记痕（石魔像×3）逐条做完后，成就一览仍不会有任何一项为此亮起；对完成主义者来说「支线全清」是最自然的收尾目标，游戏却只回报了五条分散的解锁。
- 【修正：data.js 一处落齐 成就条目，零新状态零新逻辑】ACH_LIST 增补 `allquests`「灯火同心」：完成全部支线任务。三条口径（判定 ok / 描述 d / 进度 prog）**全部同源于 QUESTS 的 `kind==='side'` 列表**——数量（当前 5）由 `Object.values(QUESTS).filter(q=>q.kind==='side').length` 推导、绝不写死：未来增删支线（QUESTS 增删一条）成就自动跟随扩/缩，绝无第二套口径（与 side_mist/side_stone 的 STONE_GOAL/MIST_GOAL 阈值单源体系同一思路，只是这里连「目标清单」本身也单源化）；判定读的是既有 `g.quests.<id>==='done'` 存档字段——与五条单支线成就同口径（status 由 quests.setSideQuest 交付侧写定），旧档已完成全支线者下次任意 applyAchievements 触发点（战斗胜利/开箱/商店/任务交付）自动补解锁，零迁移。事件型成就（与 quest/boss/trueboss 同款，无 prog 之外的额外字段），走 hero.applyAchievements 通用机制（解锁音 + `🔓 成就解锁：【灯火同心】 …` 横幅），与 perfection 的 999 金专享奖励互不相扰、不新设奖励。
- 【零回归面】未动任何结算法则——遭遇/掉落/经验/金币/难度/技能/战斗/存档格式逐字未动；未改 quests.js/core.js/battle.js/hero.js/rules.js/world.js 任何逻辑与既有条目；既有 19 项成就逐字保留（总数 19→20，unlockedAchievements/achCheck 泛化遍历 ACH_LIST 自动覆盖新条目）；成就页 drawAch 本就 `已解锁 N/${ACH_LIST.length}` 动态读数、20 项自动分两页（PAGE=10）；未新增任何运行时状态/计数/依赖（判定只读既有 g.quests）。只改 1 个文件（data.js：GAME_VERSION v19.60→v19.61 + ACH_LIST 一条 + 注释），README 两处成就计数 19→20 同步（快速上手表「全部 20 项进度」+ 系统清单「20 项成就」）。
- 验证：`node --check js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.61 专项冒烟（/tmp/jrpg_smoke_v1961_allquests.mjs）**30 项断言全过**——`GAME_VERSION='v19.61'` 递增且导出、ACH_LIST 19→20、QUESTS 支线恰 5 条、新条目 id/name/d/ok/prog 齐全、描述 `完成全部 5 个支线任务` 与 QUESTS 派生逐字恒等、ok/prog 均引用 QUESTS（单一数据源）、源码层断言 d 模板为派生式无裸 5、判定/进度行为（空 0/5 · 部分 2/5 · 全完成 5/5 → ok=true · null quests 字段兜底 0/5 · turnin 状态不算 done）、既有 19 条 id 逐条在位、五条支线 store:true（done 持久化）、运行期以 BOM 桩真实导入模块链：unlockedAchievements 全完成含 allquests / 未完成与单支线完成均不含、applyAchievements 全完成执行不抛错且 ach 入列、【重复调用去重】、drawAch 20 项两页均渲染不抛错且实绘「灯火同心」/新描述/「已解锁 20/20」、空档页「✘ 灯火同心」/「已解锁 0/20」、chests 成就（v19.51）判定不受影响；`/tmp/jrpg_smoke_v1960_trialreward.mjs` **14/14 全过**（无版本钉死断言，功能零回归）、v1959/v1958 复跑仅「版本串钉死」各自 3/2 项随版本递增失败（预期，功能断言全过零回归）、README「19 项」残留计数为 0。

## v19.60 试炼碑「通关奖励预告」——三连战值多少金不再是黑盒（信息透明·纯显示，v19.38 时序体系收口后轮换回常规改进的第二十一版，本次切「信息透明补缺」方向——v19.52 给试炼碑加了阵容预览却只列了「打谁」，没列「打赢给多少」：奖励数额要等玩家真正踩碑进战、在战斗预览行里才看得到，凑齐双徽记的玩家在碑前做「值不值得开打」的决策时仍是黑盒）

- 【试炼碑阵容徽章只报「打谁」不报「赢给多少」】v19.52 在 drawWorld 试炼碑上方挂的徽章 `⚔️ 试炼三连战 幽冥魔王Lv8→洞窟领主Lv7→终焉之神Lv12` 把三名强敌与推荐等级透明化了，但「三连战通关奖 `150+lv×20` 金」（rules.rushReward）这一半只在 battle.drawBattle 的战斗预览行（`试炼通关另奖 N 金币，随等级提升`）与试炼通关横幅里出现——玩家在碑前决策时看不见数额，得先进战才解锁「这趟值多少」，与 v19.52 想消灭的「踩碑前黑盒」同源。
- 【修正：view/drawWorld.js 试炼碑在 ready（双徽记已凑齐）时增挂一枚奖励徽章，纯显示零结算】阵容徽章下方 +2px 处叠一枚针面底、金边描边的独立徽章 `💰 通关奖 N 金（随等级）`：N 直接读 `rules.rushReward(S.G.level)`——与 battle.winBattle 试炼通关结算（行 430-431 `hero.gold += reward`）、drawBattle 战斗预览行（行 202 `rushReward(...)`）逐字同源、随玩家当前等级实时显示，永不漂移；未解锁（双徽记未齐）时仍只画原「试炼·未解锁」徽章、不画奖励，既有行为逐值不变。坐标用 `ly+2..ly+19` 与阵容徽章（`ly-22..`）上下错开不叠字；行宽由 `measureText+12` 自适应（实测约 180px、中心 x≈336，与右上小地图起点 x≈560 无重叠）。零状态、零依赖、零结算。
- 【零回归面】未动任何遇敌/掉落/经验/金币/难度/技能/战斗/成就/存档/任务判定与结算；drawWorld 其余绘制（地砖/精灵/小地图/遇敌槽/宝箱三态/其他徽章/Boss 标签/挡栅等）逐字未动；只改 2 个文件（data.js：GAME_VERSION v19.59→v19.60；drawWorld.js：新增 import { rushReward } + ready 分支下一段奖励徽章绘制，全部代码路径只读 S.G.level）。
- 验证：`node --check js/view/drawWorld.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响，tests/ 不引用 drawWorld 试炼碑分支）；新增 v19.60 专项冒烟（/tmp/jrpg_smoke_v1960_trialreward.mjs）**14 项断言全过**——`rushReward(1/9/12)` 与 `150+lv×20` 逐值恒等且负数兜底 0 级、BOM 桩真实导入模块链后把英雄放在星井矿脉试炼碑(18,12)旁 drawWorld ready 态渲染不抛错且实绘「通关奖 330 金（随等级）」徽章（数额与 rushReward(9) 逐字一致、含 💰 图标与「随等级」标注、行宽不越画布）、阵容徽章仍完整绘制（v19.52 零回归）、未解锁态仅「试炼·未解锁」不画奖励、Lv6/8/10/12 四档奖励数额随级渲染逐档一致；`/tmp/jrpg_smoke_v1959_helpfit.mjs` 等旧版专项复跑仅「版本串钉死」断言随版本递增失败（预期，功能断言全过零回归）。

- 【操作说明页 14 行固定 34px 行距，最后两行（战斗/存档槽）渲染在画布 480px 之外并盖住页脚】帮助页第 1 页从 v2.0 起逐版加内容（v19.48 又把「战斗」行扩写引入 ↑↓/Enter 选招），现共 14 行，而 drawHelp 始终以 `y=80+i*34` 平铺：i=12（战斗）baseline y=488、i=13（存档槽）y=522——画布只有 480 高，这两行整体落进画布下方不可见区（fillText 超界即被画布裁掉），玩家开「操作说明」页永远看不到 v19.48 才更新的战斗指令说明与存档槽说明；页脚「第 X/4 页」在 y=452 也被挤在面板底缘，整页视觉上是「缺了最后两行」的半页。另有并发缺陷：战斗行（14px 单行直排 6 指令 + 蓄力细节 + Boss 规则）估算整行结束 x≈765，远超面板右缘 x=570 与画布右缘 640——一旦修好纵向可见，必然横向溢出画布被裁，必须同版处理。
- 【修正：data.js 战斗行拆两行 + menus.js drawHelp 页长自适应行距，零逻辑零状态】① `data.js` HELP_PAGES[0] 战斗行（原第 13 行）改为三元素 `[标签, 首行, 次行]`：首行只列 6 个指令（`1攻击 2技能(↑↓/Enter 选招) 3药水 4逃跑(约' + FLEE_SUCCESS + '%) 5防御 6蓄力`，估算结束 x≈548 不越界），次行 12px 次级灰专述 `蓄力：下击/技能×' + CHARGE_MULT + ' · Boss无法逃跑`（x≈310）——FLEE_SUCCESS/CHARGE_MULT 仍与原文同源派生，不新增任何裸字面量，「2技能(↑↓/Enter 选招)」等 v19.48 措辞逐字保留；② `view/menus.js` drawHelp 行距按页长自适应：`sp = P.length > 10 ? 25 : 34`——14 行的操作说明页收紧到 25px（倒数第二行战斗 y=380、次行 y=398、末行存档槽 y=421，全部落进面板且与页脚 y=452 留白充足），其余三页（地图 5 行 / 机制 8 行 / 试炼 6 行）≤10 行保持 34px、渲染逐位不变；行若带 r[2]（仅战斗行）在其 +18px 处以 12px 次级灰绘制并额外让出 16px 行高，避免与下一行叠字。
- 【零回归面】未动任何遇敌/掉落/经验/金币/难度/技能/战斗/成就/存档/任务判定与结算；HELP_PAGES 仍 4 页、操作说明页仍 14 行、其余 13 行文案与三页短页逐字未动；战斗行的逃跑概率/蓄力倍率仍由 FLEE_SUCCESS/CHARGE_MULT 派生（数值未动，仅展示分行）；页脚、翻页循环（main.js 仍按 HELP_PAGES.length 取模）、帮助页入口与按键逐字未动；未新增任何运行时状态/依赖。只改 2 个文件（data.js：GAME_VERSION v19.58→v19.59 + 战斗行一行；menus.js：drawHelp 一个函数内行距与次行分支）。
- 验证：`node --check js/data.js js/view/menus.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响，tests/ 不引用 HELP_PAGES/drawHelp）；新增 v19.59 专项冒烟（/tmp/jrpg_smoke_v1959_helpfit.mjs）**52 项断言全过**——`GAME_VERSION='v19.59'` 递增且导出、HELP_PAGES 仍 4 页且操作说明页 14 行、战斗行三元素结构（标签/6 指令首行/蓄力 Boss 次行）、首行保留 v19.48 措辞且编号 1..6、逃跑概率与蓄力倍率由 FLEE_SUCCESS/CHARGE_MULT 派生无裸字面量、操作说明页其余 13 行文案与引用计数逐字不变、14 行（含战斗行首行 x≈548 与次行 x≈310）行宽全 ≤570、drawHelp 行距表达式与 r[2] 分支各恰 1 处、页脚行逐字未动；运行期以 BOM 桩真实导入模块链 drawHelp 四页不抛错且操作说明页 14 个标签行全部绘制、最大行 y=421 ≤452（存档槽实测 y=421）、战斗行首行/次行实际落字且次行恰在首行 +18px、页脚「第 1/4 页」y=452、地图指南四行仍按 34px 等差数列（短页零变化）、四图 recLv 派生标签（含 守名者/极高难）不受影响；`/tmp/jrpg_smoke_v1958_xpcurve.mjs` 等旧版专项复跑仅「版本串钉死」断言随版本递增失败（预期，功能断言全过零回归）。

## v19.58 后期经验曲线平滑——四强怪每级经验 +1，升到 Lv12 累计少打约 4 场（数值平衡，v19.38 时序体系收口后轮换回常规改进的第十九版，本次切「数值平衡·经验节奏」方向——v19.42/43/44 连做开局至中盘的开荒/遇敌池平衡后，把「升级所需经验 ×1.42 指数增长 vs 怪物经验只按玩家等级线性增长」这一整条曲线后半段的进度放缓收一收：每级所需战斗场数从 Lv1 的 1.51 场一路涨到 Lv11 的 8.11 场（按各图遇敌池权重 × zone 修正的加权平均实测），而推荐终局等级正是 Lv10（无字回廊），玩家拿推荐等级进终局区时恰好撞上曲线最陡的一段）

- 【升级需求指数增长而怪物经验线性增长，后期每级所需场数翻倍递增】升级所需经验逐级 ×XP_GROW(1.42) 取整（20→28→40→…→466→662），而 MON_BASE 普通魔物经验只按玩家等级线性产出（基础怪 /lv+3、强怪 /lv+4，再加 cave 区 ×1.15、回廊 ×1.3 修正）——两端一涨一平，「每级所需战斗场数」随等级单调翻倍：Lv1 镇 1.51 场 → Lv3 林 1.58 → Lv6 矿 2.57 → Lv8 矿 4.27 → Lv10 廊 6.10 → Lv11 廊 8.11（加权平均×zone 实测 /tmp/jrpg_xp_probe.mjs 基线表）。设计上升级本就该越来越慢，但后半段斜率明显陡出设计意图：推荐 Lv10 进无字回廊正是曲线最陡（Lv10→11 要 6.1 场、Lv11→12 要 8.1 场），终局区/终焉之神的备战期反而成了全场最磨的区间。
- 【修正：data.js MON_BASE 一处四值，纯奖励曲线微调】四强怪（骷髅兵/雾灵/树精/石魔像）`xp[1]` 4→5：它们全部 Lv.2/3+ 才出没（minLv 门槛）、正是中后期刷级主力，每级经验产出 +1 后：Lv10 廊 6.10→5.21、Lv11 廊 8.11→6.90、升到 Lv12 累计 37.9→33.6 场（约 -11%），曲线仍单调递增（升级变慢的设计意图保留）；四基础怪（史莱姆/野狼/哥布林/毒蛇）`xp[1]=3` 一处不动——它们独占 Lv1-2 潮灯镇新手池，开局「每级 1.51 场」逐值不变、零前期影响。hp/atk/def/gold/权重 w/minLv/遇敌池/精奖/掉落全程未动，纯经验产出微调：所有读取方（encounter.scaleEnemy 遇敌生成、rules.monReward 图鉴「击败可得经验」预览）本就同读 MON_BASE 单一数据源，调一处两端自动跟随、永不漂移。
- 【零回归面】未动任何战斗面：遇敌生成/权重/门槛/兵力/区域倍率（zone 仅 hp/atk/def 未动、xp/gold 修正系数未动）/掉落/药水/难度/成就/存档/任务全部逐字未变；金币曲线逐值恒等（镇 Lv1 12.08、矿 Lv8 42.85 加权均值不变，gold 字段未动）；XP_GROW/XP_INIT/ELITE_GOLEM/BOSS 均未动；四强怪仅 xp[1] 一个数变了。只改 1 个文件（data.js：GAME_VERSION v19.57→v19.58 + MON_BASE 四行 xp[1] + 注释），零新增运行时状态/依赖。
- 验证：`node --check js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；平衡模拟复测趋势不变（本改动不触任何战斗结算，生还率/凶手分布与基线一致）；新增 v19.58 专项冒烟（/tmp/jrpg_smoke_v1958_xpcurve.mjs）**145 项断言全过**——`GAME_VERSION='v19.58'` 递增且导出、四强怪 xp[1]=5 / 四基础怪 xp[1]=3 落位、与 v19.57 整目录备份 /tmp/my-jrpg_backup_20260831_082057 逐字段比对（8 怪 × 10 字段 hp/atk/def/gold/color/weak/resist/draw/minLv/poison）全部逐值不变且整表仅 4 个 xp[1] 条目变化、ELITE_GOLEM/BOSS/XP_GROW/XP_INIT 对撞面逐字未动、scaleEnemy↔monReward 同源链 8 怪 × 4 级 32 项逐值恒等、经验节奏数值（Lv1 镇 ≈1.51 不变 / Lv10 廊 ≈5.21 / Lv11 廊 ≈6.90 / 曲线仍单调递增）、金币曲线零影响（镇 12.08 / 矿 42.85）、运行期以 BOM 桩真实导入模块链 randomEncounter 200 次实例 xp 均与 monReward×zone 同源、无字回廊 100 次实例仍在限定池内、CHANGELOG 顶部 v19.58 在 v19.57 之前。

## v19.57 帮助页「地图指南」四图推荐等级 recLv 化——区域难度透明体系最后一块文档死角收口（文档·信息透明，v19.38 时序体系收口后轮换回常规改进的第十八版，本次切「难度透明·帮助页补格」方向——v19.56 已把 MAPS.recLv（1/3/6/10）接进快速旅行提示与进图危险预警，但帮助页「地图指南」页四行仍是手写裸字面量，玩家在菜单里核对「某张图什么等级去合适」时看到的是最后一个没有推荐等级的入口）

- 【帮助页地图指南页是 recLv 体系唯一没接入的显示点】v19.56 确立 MAPS[].recLv 为「进图推荐等级」唯一数据源后，两个消费端（TRAVEL_LIST 旅行提示、world.transition 进图预警）都已派生接入；唯独帮助页「地图指南」页（HELP_PAGES[1]）的四行（潮灯镇/雾语林/星井矿脉/无字回廊）仍只有地标描述、没有推荐等级——玩家不切快速旅行、没走过进图预警路径，单看帮助页无法判断「这张图现在能不能去」，与「信息透明」主线形成最后一段文档死角。
- 【修正：data.js 一处四行派生标签，零逻辑零状态】四行标签改为 `图名 Lv.' + MAPS.xxx.recLv` 拼接（潮灯镇 Lv.1 / 雾语林 Lv.3 / 星井矿脉 Lv.6 / 无字回廊 Lv.10）——与 TRAVEL_LIST / world.transition 同源，调门槛只改 MAPS.recLv 一处三处同步、永不漂移；描述列按行宽（面板右缘 x570，14px 正文）微调断句、信息不流失：潮灯镇 保留 商店·旅馆·酿造锅·灯长/守书记(支线)·喷泉回血；雾语林 保留 强魔物·精英·魔王祭坛·右下裂洞进矿脉；星井矿脉 保留 更强魔物·迷宫·中央终焉水晶·双徽记化为门（原「…集齐双徽记后化为门」压缩断句）；无字回廊 保留 名字石碑·守名者·残焰魔像·终焉之神·极高难（v19.53/19.55 收口的地标与难度标注逐项原样）。
- 【零回归面】未动任何遇敌/掉落/经验/金币/难度/技能/战斗/成就/存档/任务/状态；MAPS.recLv 四值逐字未动（1/3/6/10）；TRAVEL_LIST 与 world.transition 预警逐字未动；帮助页其余三页（操作/机制/试炼）、通关之路行、HELP_PAGES 总页数（4）逐字未变；绘制行为仍走 drawHelp 既有 `label+desc` 一行拼接路径，仅标签串由派生式产生。只改 1 个文件（data.js：GAME_VERSION v19.56→v19.57 + HELP_PAGES[1] 四行标签派生），零新增运行时状态/依赖。
- 验证：`node --check js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.57 专项冒烟（/tmp/jrpg_smoke_v1957_helpreclv.mjs）**50 项断言全过**——`GAME_VERSION='v19.57'` 递增且导出、四图 recLv 仍 1/3/6/10、地图指南四行标签逐行与 `'图名 Lv.'+MAPS.xxx.recLv` 派生同源（唯一数据源）、data.js HELP_PAGES 区仍无裸「图名」标签残留且 4 处 recLv 引用在位、描述列 v19.53/19.55 收口的 守名者/残焰魔像/终焉之神/极高难/守书记/(支线)/双徽记 等逐项保留、四行行宽按真实字形（CJK 14px·ASCII 7px）估算结束 x=443–499 全部 ≤570、通关之路行与其余三页首行逐字原样、HELP_PAGES 仍 4 页、v19.56 成果不受影响（TRAVEL_LIST 派生提示逐字不变且无裸字面量）、运行期以 BOM 桩真实导入模块链 drawHelp 四页不抛错且地图指南页实际绘出四条派生标签「潮灯镇 Lv.1 / 雾语林 Lv.3 / 星井矿脉 Lv.6 / 无字回廊 Lv.10」及 守名者/极高难 整行、绘制行估算结束 x≈513 不越界、drawTravel 仍正常绘出「推荐 Lv.10 起 · 极高难」、CHANGELOG 顶部 v19.57 在 v19.56 之前。

## v19.56 区域「推荐等级」单一数据源——快速旅行提示派生自 MAPS.recLv，进图低等级即时预警（体验打磨·信息透明，v19.38 时序体系收口后轮换回常规改进的第十七版，本次切「难度透明·进图预警」方向——承 v19.47 小地图暖金 / v19.52 试炼阵容预览 / ALTAR_TAG 祭坛 ⚠Lv 标签的「难度透明」体系后，把「某张图现在能不能进」这件事从口头经验变成进图第一眼的硬提示）

- 【低等级冒进强区没有进图预告，推荐等级散落三处互不相关】四张图的「推荐等级」此前只以手写裸字面量存在于 TRAVEL_LIST 快速旅行提示（「推荐 Lv.3 起」等四个串），与真实进图体验各自为政——玩家从潮灯镇东门走进雾语林（推荐 Lv.3）、经雾语林裂洞下星井矿脉（推荐 Lv.6）时，没有任何进图提示，只有撞上第一场高伤遇敌/祭坛 ⚠Lv 标签后才反应过来；ALTAR_TAG 的 ⚠Lv 只标注 Boss 祭坛，普通遇敌的图级难度是全游戏「信息透明」主题最后一块黑盒。且 4 个「推荐等级」数值（1/3/6/10）一旦要调，得改 TRAVEL_LIST 四行文案，与任何真实关卡数据都无绑定。
- 【修正：data.js 一处落齐 + world.js 单一出入口预警，零新状态】`MAPS[].recLv`（village 1 / dungeon 3 / cave 6 / gallery 10）成为「进图推荐等级」唯一数据源，四行加注释说明与 TRAVEL_LIST / world.transition 同源；TRAVEL_LIST 的「安全区 · Lv.X 即可 / 推荐 Lv.X 起」提示改为 `'…Lv.'+MAPS.xxx.recLv+'…'` 拼接（输出串与旧字面量逐字相等 1/3/6/10，零显示变化，未来调门槛只改 MAPS 一处两处同步永不漂移）。world.transition（快速旅行/传送门/终焉水晶开门三条入口共用同一函数）在 `S.G.level < MAPS[name].recLv` 时把原「进入了【X】」提示替换为进图预警 `⚠️ 前方【X】的魔物远强于你（推荐 Lv.N · 当前 Lv.M），先补给再战！`（停留时长 NARR_MSG_MS=1800）；等级达标或回村级安全图维持原提示，行为逐字不变。
- 【零回归面】未动任何遇敌/掉落/经验/金币/难度/技能/战斗/成就/存档/任务结算与概率；遇敌池、zone 区域修正、ALTAR_TAG、图鉴出没地逐字未动；TRAVEL_LIST 长度仍 4、描述列（第 3 列）与提示列输出串逐字不变，仅提示列改由派生式拼接产生相同文本；world.transition 原先的「进入了【X】」仅在低等级进强图时被预警替换，其余路径逐字保留；未新增任何运行时状态/依赖。只改 2 个文件（data.js：GAME_VERSION v19.55→v19.56 + 四图 recLv + TRAVEL_LIST 提示派生；world.js：transition 一处预警分支）。
- 验证：`node --check js/data.js js/world.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.56 专项冒烟（/tmp/jrpg_smoke_v1956_reclv.mjs）**41 项断言全过**——`GAME_VERSION='v19.56'` 递增且导出、四图 recLv 恰为 1/3/6/10、TRAVEL_LIST 仍 4 条且四条提示派生串与旧字面量逐字恒等、描述列逐字不受影响（含守名者）、data.js 四条旧裸字面量清零、world.js 已接 recLv 出警且保留原「进入了」提示、运行期以 BOM 桩真实导入模块链：Lv.1 进雾语林出警含「推荐 Lv.3·当前 Lv.1」且地图/visited 正确切换、Lv.3 进雾语林维持「进入了【雾语林】」不出警、Lv.5 进星井矿脉出警含「推荐 Lv.6」、Lv.1 回潮灯镇安全图不出警、CHANGELOG 顶部 v19.56 在 v19.55 之前；`/tmp/jrpg_smoke_v1955_guardloc.mjs` / `_v1953_guard.mjs` / `_v1952_trialroster.mjs` / `_v1950_wherefind.mjs` 复跑均仅「版本串钉死」各 2 项随版本递增失败（预期，24/26、45/47、19/21、21/23 功能断言全过零回归）。

## v19.55 无字回廊位置文案补「守名者」——帮助页与快速旅行列表与新 NPC 对口径（文档·信息透明，v19.38 时序体系收口后轮换回常规改进的第十六版，本次切「文档口径·游戏内位置清单」方向——承 v19.50 图鉴出没地 / v19.54 README 成就数口径的「信息透明/单一数据源」主线后，把 v19.53 新增 NPC 守名者 落进口语化的两处游戏内区域清单：README 第 4 条区域介绍早已同步收录守名者，但游戏内帮助页「地图指南」页与快速旅行列表的无字回廊条目仍是 v19.53 之前的旧口径——玩家就算在路上遇到守名者，游戏内任何「该区有什么」的清单都没把他算进回廊地标）

- 【游戏内两处「无字回廊」位置文案停在 v19.53 之前】帮助页「地图指南」页（data.js HELP_PAGES[1]）无字回廊 行只写「名字石碑·残焰魔像·东端祭坛藏终焉之神（极高难）」；快速旅行列表 gallery 条（data.js TRAVEL_LIST）只写「被遗忘者的回廊：名字石碑·残焰魔像·终焉之神」——两条都缺 v19.53 落地为终局区唯一可对话 NPC 的守名者，与已同步的 README（「中段守名者（终局区唯一可对话 NPC…）」）口径不一致；玩家开帮助页/快速旅行核对回廊有什么时，看到一个「比实际少一个地标」的回廊。
- 【修正：data.js 两行文案，零逻辑零状态】帮助页回廊行补「守名者」（置于名字石碑之后）：`名字石碑·守名者·残焰魔像·终焉之神（极高难）`——同步删除同行的冗余定位「东端祭坛藏」以免 14px 整行超出面板右缘（旧口估算已贴 x≈569，新增 4 字必然越界；「回廊尽头面对终焉之神」位置信息仍由同页「通关之路」行、旅行列表、图鉴 whereFind 三处继续标注，信息未流失）；旅行列表回廊描述补「守名者」：`被遗忘者的回廊：守名者·名字石碑·残焰魔像·终焉之神`（11px 起于 x180、估算结束 x≈477 < 面板右缘 520，未超宽）。两处均为纯显示文案，与 README 已收录口径对齐。
- 【零回归面】未动任何游戏常量/逻辑/结算/遇敌/掉落；HELP_PAGES 其余 12 行、TRAVEL_LIST 其余 3 条逐字未动；GAME_VERSION 递增 v19.54→v19.55 为版本惯例（与 CHANGELOG 顶部同步）。
- 验证：`node --check js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.55 专项冒烟（/tmp/jrpg_smoke_v1955_guardloc.mjs）**26 项断言全过**——`GAME_VERSION='v19.55'` 递增且导出、帮助页回廊行含 守名者 且保留 名字石碑/残焰魔像/终焉之神/极高难、旅行列表回廊条含 守名者 且保留原要素与「极高难」提示、两处旧口径字符串残留清零、TRAVEL_LIST 仍 4 条且既有 3 条与帮助页其余行逐字不受影响、两处新增文案行宽估算不越界（帮助页 ≤570 / 旅行列表 ≤520）、CHANGELOG 顶部 v19.55 在 v19.54 之前。

## v19.54 README 成就数口径修正——「17 项成就」陈旧文本与真实成就数脱节，改与 ACH_LIST 单一数据源一致（文档·信息透明，v19.38 时序体系收口后轮换回常规改进的第十五版，本次切「文档口径」方向——承 v19.50 图鉴出没地 / v19.52 试炼阵容预览等「信息透明」主线后，把 README 里唯一一处与游戏内成就总数对不上的文字补正：ACH_LIST 历经 v19.46「旧账已结」18 项、v19.51「开箱寻宝」19 项连续两版增长，README 的「17 项成就」停留在更早的口径没跟上）

- 【README 两处成就计数停在 17，游戏内已 19 项】快速上手表「C 成就一览（全部 17 项进度）」与系统清单「图鉴 & 成就…17 项成就」两处，与真实 ACH_LIST（19 项）对不上——游戏内成就一览（menus.js drawAch）早已 `已解锁 N/${ACH_LIST.length}` 动态读数、从不写死；唯独 README 手写「17 项」成了项目里最后一处硬编码的成就总数（其余数值如 掉落 8/12/12/6%、宝箱 60/18/22% 均与 data.js 常量逐字一致，抽查核对无其它漂移）。
- 【修正：README 两处 17→19】快速上手表一行 + 系统清单一行，与 `ACH_LIST.length`（当前 19）对齐；不改任何游戏逻辑/常量/UI，仅文档文本。
- 【零回归面】纯文档改动：未动 data.js 任何常量（仅 GAME_VERSION 递增 v19.53→v19.54，与 CHANGELOG 顶部版本同步惯例）、未动任何代码/结算/遇敌/掉落/成就判定；游戏内动态读数本就不受影响；README 其它数值口径（13 种图鉴、5 档武器、4 档防具、38% 掉落、40% 宝箱等）逐项抽查均与 data.js 单一数据源一致，无其它陈旧处。
- 验证：`node --check js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；README 成就数两处均已无「17 项」残留（`grep -c '17 项' README.md` 为 0）、`GAME_VERSION='v19.54'` 与 CHANGELOG 顶部一致、`ACH_LIST.length===19`（节点导入实测）；`/tmp/jrpg_smoke_v1953_guard.mjs` 复跑仅「版本串钉死 v19.53」2 项随版本递增失败（预期，47 项功能断言全过零回归）。

## v19.53 无字回廊新增 NPC「守名者」——终局区唯一的对话者（新支线/NPC·内容扩充，v19.38 时序体系收口后轮换回常规改进的第十四版，本次切「新 NPC」方向——承 v19.46 守书记支线 / v19.51 开箱成就后，给全游戏最后一个零对话的区域补上人：无字回廊此前只有 4 块名字石碑（STELE 只读展示）与 残焰魔像/终焉之神 两个强敌，玩家双徽记开门走进终局区后，一路上没有任何 NPC 可说一句话——回廊是什么、前面要面对谁、该怎么准备，全得靠图鉴/帮助页自己悟）

- 【终局区「无字回廊」是四张地图里唯一零 NPC 的区域】潮灯镇有 灯长/镇民/巡灯人/井巫/守书记/雾径猎手/星砂车夫，雾语林有 雾径猎手，星井矿脉有 井巫/星砂车夫——唯独耗时最久、难度最高的终局区（gallery，恒暗无晨昏）只有 4 块把 FRAGMENTS 碑文原样展示的名字石碑（STELE 瓦片走独立 interact 分支，不是可对话 NPC）；双徽记开门进来后，这里连一句「踏入此地意味着什么」的交代都没有，与全游戏「信息透明」主题的最后一段旅程脱节。
- 【修正：data.js 一处落齐 NPC 定义+全局坐标+瓦片，view/sprites.js 一行造型映射，零逻辑零状态】`NPCS.guard`「守名者」（mark:'staff'）：默认台词 2 页——阐明回廊「碑是刻给还活着的人看的」立意 + 面向 残焰魔像/初灯 的务实引导（「先把图鉴翻完、把药水灌满」）；真结局（trueBoss）后 `after` 彩蛋 2 页（与 镇民/巡灯人/雾径猎手/井巫 同款通关彩蛋惯例，quests.npcQuestPages 的 `hero.trueBoss → ent.after` 分支逐字复用，零新增代码）。落位：`NPC_SPOTS['8,5']='guard'` + gallery extras `{x:8,y:5,ty:'NPC'}`——(8,5) 位于回廊中段主视线旁（残焰魔像 12,4 之前），原始瓦片为可行走格（rows[5][8]=='dot'），且该全局坐标键未被任何地图占用（NPC_SPOTS 键跨地图共用，避开已用键 13,6/10,13/19,8/12,8/2,4/13,9/3,1/17,11/5,1/10,1/15,1/20,1 即可安全新增）；造型映射 `view/sprites.js NPC_SHEET.guard → mwSage`（hooded 神秘形象，与井巫同款 CC0 图集，贴图缺失时退回程序化 mark）。只改 2 个文件（data.js 版本号 v19.52→v19.53 + NPC_SPOTS 一行 + NPCS 一条 + gallery extras 一行；sprites.js NPC_SHEET 一行），零新增依赖、零新增状态。
- 【零回归面】纯内容追加——未动任何遇敌/掉落/经验/金币/难度/技能/战斗/成就/存档/任务判定；既有 NPC 逐条原样保留（NPC_SPOTS 键、NPCS 定义、对话解析顺序逐字未动）；STELE 石碑、残焰魔像、终焉之神 位置/交互逐字未动；通用对话管线（npcQuestPages → resolveNpcTalk → talkNext）未改一行，新 NPC 完全走既有机制；未改 README 之外的任何文档口径。gallery 地图仅新增一格 NPC 瓦片（TY.NPC 属 SOLID，不改变走廊连通性——主路线 y4 畅通，NPC 位于旁侧格）。
- 验证：`node --check js/data.js js/view/sprites.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；平衡模拟（30k 蒙特卡洛）五档生还率与前版基线一致（纯内容改动不影响战斗结算）；新增 v19.53 专项冒烟（/tmp/jrpg_smoke_v1953_guard.mjs）**47 项断言全过**——`GAME_VERSION='v19.53'` 递增且导出、NPC_SPOTS['8,5']→'guard' 且键恰 1 处、四图全部 NPC 瓦片坐标可解析且 (8,5) 仅 gallery 使用（全局坐标防撞演练）、NPCS.guard 结构（名字/造型/lines 2 页/after 2 页）、既有 13 个 NPC_SPOTS 键逐条可解析、gallery extras 恰 1 个 NPC 且原始瓦片可行走、sprites.js guard→mwSage 恰恰 1 处且既有 6 条映射原样保留；运行期以 BOM 桩真实导入模块链、种子 hero（回廊已开/未打初灯）loadMap('gallery')：at(8,5)===TY.NPC 落位正确、npcQuestPages 返回 2 页默认台词、drawTalk() 渲染不抛错且落字「守名者」/第 1 页「回廊里躺的名字」/翻页后第 2 页落字「残焰魔像」，trueBoss 置位后 npcQuestPages 切回 after 彩蛋 2 页、drawTalk() 落字「初灯的名字」不抛错；`/tmp/jrpg_smoke_v1952_trialroster.mjs` / `_v1951_chests.mjs` / `_v1950_wherefind.mjs` 复跑均仅「版本串钉死」各 2 项随版本递增失败（预期，19/21、35/37、21/23 功能断言全过零回归）。

## v19.52 试炼碑「三连战阵容预览」——踩碑前一眼看清要连战的三名强敌与推荐等级（体验打磨·信息透明，v19.38 时序体系收口后轮换回常规改进的第十三版，本次切「信息透明·开战前情报」方向——承 v19.47 小地图暖金 / v19.49 胜利战绩 / v19.50 图鉴出没地 / v19.51 开箱成就的「信息透明」主线后，试炼机制是这条线上仅存的黑盒：凑齐双徽记的玩家踩上星井矿脉的试炼碑，只知道「可以挑战」，不知道碑里埋的是哪三名强敌、各自什么等级——帮助页也只笼统写「连战三名最强 Boss」，到了第一关才知道对手是谁）

- 【试炼碑标签只有「⚔️ 试炼·可挑战 / 试炼·未解锁」两态，阵容信息完全缺席】试炼场是全游戏最重的一次考验（三连战 Boss 连打、关间仅自动回血 35%HP/50%MP），而玩家踩碑前对「要面对谁」零情报：线上 Boss（幽冥魔王/洞窟领主/终焉之神）的祭坛都已在世界画面有 ⚠Lv 推荐等级标签（ALTAR_TAG）、图鉴/战斗面也各自揭示兵力，唯独试炼碑这个同样要开打的入口没有任何阵容预览；帮助页「试炼三连战」一行只写「连战三名最强 Boss」不带名字不带等级，试炼开始的 boxMsg 也只说「连战场上的三名强敌！」——三连战的对手/等级在踩碑进入前是全游戏「信息透明」主题里唯一没被揭开的一处。
- 【修正：试炼碑世界标签直接列出三连战阵容，顺序/等级全部单一数据源派生】`view/drawWorld.js` 试炼碑标签（cave 专属绘制的上方浮标）由「⚔️ 试炼·可挑战」扩为 `⚔️ 试炼三连战 幽冥魔王Lv8→洞窟领主Lv7→终焉之神Lv12`：顺序读 `RUSH_BOSSES`（与 battle.startRush 逐字同源，改试炼关序只改 data.js 一处、预览自动跟随），推荐等级读 `SPECIES[name].lv`（与战斗面 enemyLv、祭坛 ⚠Lv 标签 ALTAR_TAG 同源，真身/普通同名归一），`(SPECIES[b.name] && SPECIES[b.name].lv) || 1` 防御性兜底；未解锁态（双徽记未齐）仍原样显示「试炼·未解锁」，不剧透阵容。只改 2 个文件（data.js 版本号 v19.51→v19.52 + drawWorld.js 导入 RUSH_BOSSES + 标签一行），零新增依赖、零新增状态。
- 【零回归面】纯显示改动——未动任何结算：试炼战生成/奖励/关间回血（RUSH_RECOVER）/Boss 兵力/经验金币/难度/存档/遇敌逐字未动；未改 battle.js/world.js 任何逻辑；既有「⚔️ 试炼·可挑战 / 试炼·未解锁」配色与未解锁态文案原样保留，仅可挑战态文案扩写；试炼碑坐标（cave extras TY.TRIAL at 18,12）、ALTAR_TAG 标签、图鉴/帮助页逐字未动（帮助页仍写「连战三名最强 Boss」，与碑面阵容信息互补不冲突）。
- 验证：`node --check js/view/drawWorld.js js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.52 专项冒烟（/tmp/jrpg_smoke_v1952_trialroster.mjs）**21 项断言全过**——`GAME_VERSION='v19.52'` 递增且导出、drawWorld.js RUSH_BOSSES import 到位、roster 派生式剔注释后恰 1 处且带兜底、派生串与单源推算一致（幽冥魔王Lv8→洞窟领主Lv7→终焉之神Lv12）、RUSH_BOSSES 恰 3 名且顺序与 startRush 同源、每名强敌 SPECIES.lv 可读、cave 试炼碑落位仍在、未解锁态文案保留/可挑战态前缀恰 1 处、drawWorld.js 不 import battle.js（零结算面）；运行期以 BOM 桩真实导入模块链、种子 hero（双徽记已齐）loadMap('cave') + drawWorld()：试炼碑渲染落字 `⚔️ 试炼三连战 幽冥魔王Lv8→洞窟领主Lv7→终焉之神Lv12`、未解锁态（双徽记未齐）落字「试炼·未解锁」且不出现阵容串、已解锁态与 trueBoss 无关仍渲染阵容，全程不抛错；`/tmp/jrpg_smoke_v1951_chests.mjs` 与 `/tmp/jrpg_smoke_v1950_wherefind.mjs` 复跑仅「版本串钉死 v19.51/v19.50」各 2 项随版本递增失败（预期，35/37 与 21/23 项功能断言全过零回归）。

## v19.51 新成就「开箱寻宝」——扫箱这一口探勘深度从此有里程碑（新成就·内容扩充，v19.38 时序体系收口后轮换回常规改进的第十二版，本次切「新成就」方向——承 v19.46 守书记支线 / v19.47 小地图暖金后，把「开箱」这件玩家频繁做、小地图暖金又在引导的事，补上全游戏唯一缺失的那类里程碑：宝箱开启计数此前只活在世界侧 hero.chests 坐标集里，全库无任何成就/文案引用过「开了几个箱」）

- 【开箱这件高频行为没有任何成就照应】19 项成就覆盖了胜场/掉落/等级/金币/图鉴/五条支线/四名 Boss/传说装备，唯独「开箱」缺位——v19.47 小地图暖金让扫箱成为可规划的活动以来，凑齐 6 个宝箱恰好是「村落+雾语林+矿脉常规探索量」的自然节点（全图可开 11 箱：潮灯镇 2 + 雾语林 3 + 星井矿脉 2 + 洞窟宝藏 4，中期访遍前三区约半数即达成，无需进矿脉深处/回廊）；宝箱开启计数其实一直存在——hero.chests 是 world.onChestStep 开箱即写入的坐标 Set（坐标键 "x,y"，newGame/restoreChests/resetRun 均归一为 Set），只是没有任何判定读过它。
- 【修正：data.js 一处落齐 常量+计数函数+新成就条目，world.js 开箱当场即时判定，零新状态】TREASURE_GOAL=6（与 RICH_GOLD/SCHOLAR_GOAL/LUCKY_GOAL/HUNT_GOAL/LVL5_GOAL/LVL10_GOAL/FIRSTBLOOD_GOAL 同一「成就阈值数据化」家族，v18.5 该家族数值门槛收口完毕后第一条全新门槛，沿用同一单源模式；判定 ok / 描述 d / 进度 prog 三处同读此源）；chestCount(g) 纯只读计数（Set 读 size / 数组读 length / 缺失计 0，防御性兜底，绝无第二套口径）；ACH_LIST 增补 {id:'chests', 开箱寻宝, 「累计开启 6 个宝箱」}。配套即时判定：world.onChestStep 开箱后立刻 applyAchievements()（与 quests 交付的「先 applyAchievements 再报奖励」同一惯例，把「等下一场战斗胜利才解锁」的迟到反馈改为开箱当场弹成就/响升级音），导入面 world→hero 单向、hero 仍零反向引用，无新增循环依赖。
- 【零回归面】未动任何结算法则——遭遇/掉落/经验/金币/难度/技能/战斗/存档格式逐字未动；未改 quests.js/core.js/battle.js/hero.js 任何逻辑；既有 18 项成就（含 v19.46 旧账已结）逐条原样保留（总数 18→19），成就阈值家族原值未动；世界侧开箱掉落判定（CHEST_*/蘑菇/金币/药水）与宝箱格已开复核（hero.chests.has）逐字未动，仅在其后追加一行即时判定；未新增任何运行时状态（计数直接读既有 hero.chests）。只改 2 个文件（data.js：版本号 + TREASURE_GOAL + chestCount + ACH_LIST 一条 + export 一行；world.js：import + onChestStep 一行调用），零新增依赖。
- 验证：`node --check js/data.js js/world.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.51 专项冒烟（/tmp/jrpg_smoke_v1951_chests.mjs）**37 项断言全过**——`GAME_VERSION='v19.51'` 递增且导出、TREASURE_GOAL=6 导出与定义、ACH_LIST 新条目（id/名称/描述模板串/ok/prog 均为函数）、判定式/进度串剔注释后各恰 1 处、总数 18→19 且既有 18 项 id 逐条保留、同族阈值原值未动、world.js import+调用点各恰 1 处、hero.js 无反向引用（无新循环）、运行期以 BOM 桩真实导入模块链：开 5 箱不解锁 → 第 6 箱 unlockedAchievements 含 chests、prog(5)='5/6'、chests 走数组/缺失分支防御兜底正确、applyAchievements() 开箱当场入列且去重不重复、未见其他成就误锁、drawAch 滚到末页渲染 19 项含「开箱寻宝 6/6」、drawCodex 不受影响；`/tmp/jrpg_smoke_v1950_wherefind.mjs` 复跑仅「版本串钉死 v19.50」2 项随版本递增失败（预期，21 项功能断言全过零回归）。

## v19.50 图鉴普通魔物「出没地」遇敌池单一数据源派生——8 种普通魔物不再笼统标「草丛随机遇敌」（体验打磨·信息透明，v19.38 时序体系收口后轮换回常规改进的第十一版，本次切「信息透明·出没地口径」方向——承 v19.41 图鉴已遭遇揭示 / v19.44 区域遇敌池收口后，把图鉴每个普通魔物标在哪里出没这件事与真实遇敌池对齐：此前 8 种普通魔物的出没地一律写死「草丛随机遇敌」，v19.44 潮灯镇限定四基础怪、无字回廊限定雾灵/石魔像/骷髅兵后图鉴信息仍不变——玩家会以为树精/石魔像也撞得到在镇内草地，而那版恰恰把强怪清出了镇子，图鉴口径与真实遇敌判据就此脱钩）

- 【普通魔物出没地显示与真实遇敌池脱钩】图鉴已讨伐行/已遭遇行的出没标注此前只有 BOSS/精英走剧情遭遇表（LOC：石心魔像/幽冥魔王/洞窟领主/终焉之神/残焰魔像），8 种普通魔物一律回落到笼统的「草丛随机遇敌」——它既没说清「这个怪到底在哪几张图出没」，又与现实判据相悖：v19.44 起潮灯镇遇敌池只剩史莱姆/野狼/哥布林/毒蛇四基础怪、无字回廊只剩雾灵/石魔像/骷髅兵，图鉴却仍对树精/石魔像标「草丛随机遇敌」，等于告诉玩家它们也出现在镇内草丛（那版正是把它们清出镇子才收的口）；矿脉/雾语林为无 pool 全 8 种、回廊 3 种、镇 4 种——「每个怪在哪几张图出没」是一份真实存在且随版本演进的分布表，玩家刷图鉴/找特定怪时唯一可信的来源就是图鉴，而图鉴这份表是错的。
- 【修正：whereFind 普通魔物出没地改为与 encounter.randomEncounter 同源派生，图名读同一 MAPS[].name】判定式与遇敌完全同构：某魔物出现在一张图 ⇔ 该图无 pool 限定（雾语林/星井矿脉 = 全 8 种）或 pool 含其名——`Object.values(MAPS).filter((def)=>!def.pool || def.pool.includes(name)).map((def)=>def.name).join(' · ')`；落位效果：史莱姆/野狼/哥布林/毒蛇 → `潮灯镇 · 雾语林 · 星井矿脉`，骷髅兵/雾灵/石魔像 → `雾语林 · 星井矿脉 · 无字回廊`，树精 → `雾语林 · 星井矿脉`（树精/石魔像无「潮灯镇」，v19.44 镇池语义如实呈现）；既有「Lv.X起出没」门槛标注（spawnLv 同源 minLv）与「约N%」精英概率标注（ELITE_CHANCE 同源）逐字追加在出没地之后，BOSS/精英仍走 LOC 剧情遭遇表一字未动——今后调任何图的遇敌池（v19.44 式的收口），图鉴出没地自动跟随、永不漂移。
- 【零回归面】纯显示改动：只改 menus.js 的 whereFind 一个函数 + 导入行加 MAPS（data.js 仅 GAME_VERSION 递增），不碰任何遇敌生成/权重/门槛/倍率/掉落/经验/金币/难度/战斗/存档/成就判定；BOSS/精英 LOC 表原样保留，「草丛随机遇敌」兜底清零；既有图鉴行的名字/讨伐计数/兵力参考/弱点/标签逐字未动，仅位置文本由固定串变为同源派生串。未新增任何状态、任何依赖。
- 验证：`node --check js/view/menus.js js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.50 专项冒烟（/tmp/jrpg_smoke_v1950_wherefind.mjs）**23 项断言全过**——`GAME_VERSION='v19.50'` 递增且导出、menus.js MAPS import 到位、「草丛随机遇敌」剔注释后 0 处、派生表达式恰 1 处、LOC 剧情遭遇表 5 键原样、spawnLv/ELITE_CHANCE 标注逻辑保留、8 种普通魔物出没地逐怪与本期期望一致（含树精/石魔像不含潮灯镇、四基础怪含潮灯镇、无字回廊只含雾灵/石魔像/骷髅兵三项 v19.44 语义守卫）；运行期以 BOM 桩真实导入模块链、种子 bestiary 调 `drawCodex()` 捕获 fillText 全链：史莱姆/毒蛇落字 `（潮灯镇 · 雾语林 · 星井矿脉）`、树精 `（雾语林 · 星井矿脉 · Lv.3起出没）`、石魔像 `（雾语林 · 星井矿脉 · 无字回廊 · Lv.3起出没）`、骷髅兵/雾灵 `（…无字回廊 · Lv.2起出没）`、石心魔像 seen 行 `（雾语林·稀有精英 · Lv.3起出没 · 约N%）`，空白图鉴绘制不抛错；v19.49/v19.48 专项冒烟复跑仅「版本串钉死」断言随版本递增失败（预期，18/20 与 34/35 项功能断言全过零回归）。

## v19.49 胜利画面战绩补全——run 总结屏唯一缺口的战绩口径补齐（体验打磨·信息透明，v19.38 时序体系收口后轮换回常规改进的第十版，本次切「信息透明·战绩口径一致性」方向——承 v19.47 小地图 / v19.48 技能菜单后，把「总结一趟冒险的战绩」这套口径四屏对齐：图鉴（累计讨伐）/ 阵亡画面（等级·金币·累计讨伐·时间）/ 尾声战绩页（讨伐·成就·记忆·金币·时间）三处早已齐备，唯独【胜利画面】打赢终焉之神的瞬间只报 等级/金币——想回味这趟的累计讨伐/成就数/时长得先按 Enter 进尾声）

- 【「灯芯回来了」的 victory 结算只报 等级/金币，是 run 总结屏里唯一缺战绩口径的一屏】阵亡画面（drawDead）早有 `当前 Lv.X · 金币 N · 累计讨伐 K 只 · ⏱️时间`，尾声战绩页（drawEnding）更有 `累计讨伐 K 只 · 成就 X/18 · 记忆 M/4 · 金币 N · ⏱️时间`——唯独最终战胜利瞬间（drawWin）只有 `最终等级 Lv.X · 金币 N`：打穿终焉之神的玩家想回看这趟打了多少只、解锁几项成就、跑了多久，只能按 Enter 进尾声页；三处 run 总结屏的「战绩口径」因此参差，与全游戏「信息透明」主题相悖，也是整个跑局总结体系里唯一的空档。
- 【修正：drawWin 在 等级/金币 行之下补一行战绩，与 drawDead/drawEnding 同源读 hero.bestiary/hero.ach/hero.time，成就上限读 ACH_LIST 单一数据源】`累计讨伐 ${Object.values(bestiary||{}).reduce(求和)} 只 · 成就 ${(ach||[]).length}/${ACH_LIST.length} · ⏱️${fmtTime(time)}`——求和式、成就上限计数、时长格式化与阵亡/尾声逐字同源（drawEnding 用同款 `Object.values(hero.bestiary||{}).reduce((a,b)=>a+b,0)`，本处换 S.G 同一对象），绝无第二套口径；居中 `bold 14px` 亮绿 `#a8ff8a`，落在 等级/金币 行（y=340）与按钮行（y=380）之间的 y=362，不抢主标题与按钮焦点；空战绩档（无 bestiary/ach/time）经 `||{}` / `||[]` / fmtTime 兜底如实显示 0，不抛错。纯显示零结算变化。
- 【零回归面】drawWin 仅追加一行绘制 + 两行局部派生（kills 求和）：原有 主标题/最终等级·金币行/按钮行 逐字未动；图鉴「累计讨伐」行、阵亡画面、尾声战绩页逐字未动；未动任何掉落/经验/金币曲线/难度/技能/支线/成就判定/存档/战斗；未新增任何状态或依赖。只改 2 个文件（menus.js drawWin 一处 + data.js 版本号递增 v19.48→v19.49），零新增依赖。
- 验证：`node --check js/view/menus.js js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.49 专项冒烟（/tmp/jrpg_smoke_v1949_winstats.mjs）**20 项断言全过**——`GAME_VERSION='v19.49'` 递增且导出、menus.js ACH_LIST/fmtTime import 到位、战绩行「成就 ${…}/${ACH_LIST.length}」「⏱️${fmtTime(…) }」剔注释后各恰 1 处、「累计讨伐」四屏同口径（图鉴/阵亡/胜利/尾声剔注释后恰 4 处）、kills 求和各屏同式、drawWin 既有行与按钮行保留；运行期以 BOM 桩真实导入模块链、种子 hero（bestiary 史莱姆×3+野狼×2 / ach 2 项 / time 3725s）调 `drawWin()` 捕获 fillText 全链：主标题/等级·金币行/按钮行逐位落字，战绩行落字 `累计讨伐 5 只 · 成就 2/18 · ⏱️01:02:05`，空战绩档（0/0/00:00:00）绘制不抛错；`/tmp/jrpg_smoke_v1948_skillnav.mjs` 复跑仅「版本串钉死 v19.48」1 项随版本递增失败（预期，34 项功能断言全过零回归）。

## v19.48 战斗技能菜单 ↑↓ 光标选招 + Enter 施放——补齐全游戏唯一不能方向键操作的菜单（体验打磨·输入体验，v19.38 时序体系收口后轮换回常规改进的第九版，本次切「输入体验」方向——近两版连续做小地图/标题预览显示后，回到与 shop/travel/pause 同一套 onArrow 交互惯例：全游戏所有菜单都能 ↑↓ 导航、Enter 确认，唯独战斗技能菜单从 v1.0 起只能按数字键 1-7 直发技能，打开菜单后 ↑↓ 按了没反应——按键习惯在别处练成、到技能菜单就要换一套操作，是输入层唯一一处「没跟上惯例」的缺口）

- 【技能菜单打开后 ↑↓ 无功能，只能按数字快捷直发】`openSkillMenu`（view/drawBattle.js）打开技能菜单后，main.js 的 `battle` 场景按键分派里 `skillMenuOpen` 分支只认数字键 1-7（直发技能）与 Esc（取消）——方向键在别处（商店 `shopSel` / 快速旅行 `travelSel` / 暂停 `pauseSel` / 图鉴滚动）都是标准移动/滚动键，唯独技能菜单里 ↑↓ 被静默忽略：想让光标「先看准再按 Enter」完全不可能，只能记数字键位硬按；对 7 级全技能（5 招）+ 需要盯着 MP/伤害预览选招的场合尤其生硬。
- 【修正：`S.skillSel` 光标（state.js 注册）+ ↑↓ 移动 + Enter 施放，数字键快捷逐字保留】main.js 技能菜单分支新增两路：`Enter` → 施放 `S.skillSel` 光标所在技能（越界 clamp，与 shop/travel Enter 确认同一惯例）；`↑/↓` → `S.skillSel` 循环移动（`(x+1)%len` / `(x-1+len)%len`，与 shopSel/travelSel/pauseSel 同款 onArrow，含 `SFX.select` 反馈）。数字键 1-7 快捷直发行为逐字保留，并顺手把 `S.skillSel` 记为该招索引——下次打开菜单直接高亮上次用的那招。`openSkillMenu()` 打开菜单时 `S.skillSel=0` 复位（与 shopSel/travelSel 同款复位惯例）。`view/drawBattle.js drawSkillMenu()` 增补聚焦行高亮（`rgba(255,210,74,.15)` 金色底条 + `▶` 前缀，与商店/旅行/暂停高亮同族调性），底部操作提示行由「[数字键] 选择」改为「[↑↓]选择 [Enter]施放 [数字键]快捷 [Esc]取消」；帮助页「战斗」行与 README 战斗说明同步标注（`2技能(↑↓/Enter 选招)`）。纯输入层/显示层改动，不碰任何技能结算公式、MP、解锁、封印或平衡数值。
- 【零回归面】未动任何结算——技能伤害/灼烧/冻结/穿防/碎甲/治疗/蓄力公式、`SKILL_DATA`、`playerAction`/`doSkill`、`skillEstimate`、`elemMult`、`skillForbidden`、MP 扣减、战斗时序逐字未动；数字键快捷直发行为与旧版逐值恒等；`Esc` 取消、`S.battleBusy` 行动锁定、blogView 日志滚动（技能菜单关闭时的 ↑↓ 仍滚日志）逐字未动；技能菜单各行的标签/MP 标注/还差 N MP/封印 ⛔/概率 hint/伤害预览原位逐字未动（仅聚焦行叠加 ▶ 前缀与底色条，未聚焦行与旧版渲染逐字一致）。未动任何掉落/经验/金币/难度/支线/成就/存档/遇敌。只改 4 个文件（state.js 注册 1 行 + drawBattle.js 高亮/提示/复位 + main.js 两路按键 + data.js 版本号/帮助页一行文案）+ README 一行，零新增依赖。
- 验证：`node --check js/main.js js/state.js js/data.js js/view/drawBattle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；平衡模拟（30k 蒙特卡洛）五档生还率与前版基线一致（普通 100% / 无药 99.57% / 困难 99.13%，纯输入改动不影响战斗结算）；新增 v19.48 专项冒烟（/tmp/jrpg_smoke_v1948_skillnav.mjs）**35 项断言全过**——`GAME_VERSION='v19.48'` 递增且导出、state.js 注册 `skillSel:0`、drawBattle 聚焦行 ▶/金色底条/新提示行落位、旧「[数字键] 选择」提示已移除、openSkillMenu 复位、main.js 技能菜单 Enter+↑↓ 双路与数字键记光标、帮助页「战斗」行同步；运行期以 DOM 桩真实导入 main.js 全链：搭三招战斗场景后 `openSkillMenu()` 打开并复位、`drawSkillMenu()` 渲染捕获 `▶ [1] 火焰斩` 聚焦行/次行无 ▶、`S.skillSel=1` 后 ▶ 迁移到 `[2]`、真实按键分派验证 ↓↓↑ 光标 0→1→2→1、Enter 施放冰霜击（菜单关闭 + MP-5 + battleBusy 锁定 + skillSel=1）、数字键 3 直发治愈术（MP-5 + skillSel 记 2）、↑ 从 0 上翻循环到末招、Esc 取消全部符合预期。

## v19.47 小地图「未开启宝箱」常驻暖金——扫图一眼看清哪还有宝可开（体验打磨·信息透明，v19.38 时序体系收口后轮换回常规改进的第八版，本次切「小地图显示」方向——承 v19.45 标题预览 / v19.40 状态页蘑菇计数 / v19.41 图鉴已遭遇的「信息透明」主线：小地图作为「进图第一眼的全局视图」，唯一一类与世界画面不一致的图块就是宝箱——世界侧始终画着宝箱精灵（TILE_PROP/TILE_CHEST_OPEN），小地图侧却只有引导态才显色）

- 【小地图上的宝箱与草地一色，扫图看不出「哪还有宝可开」】`minimapColor`（view/drawWorld.js）里 未开启宝箱 只有两条出路：处「金光脉动」引导态（蘑菇支线进行中 / 洞窟宝藏显形的 v3.x+v3.36 同判）→ 金/暗金脉动；否则 → 与草地同色的 `#2f6b2f`——开启过的宝箱格也回落同一草色。于是小地图整体上只分「引导中的宝箱」与「其余一切（含没被引导的未开宝箱、已开宝箱、普通草地）」，玩家扫图时无法从全局视图中发现那些「没人引导、但确实还没开」的宝箱（如潮灯镇两箱、雾语林非任务箱、洞窟宝藏），只能靠走进世界画面一格格找——与「信息透明」主题相悖；而世界画面那侧 `drawWorld` 对 未开启/已开启 两态画的就是不同精灵（TILE 宝箱 vs TILE_CHEST_OPEN 开盖），两处各画一套、小地图这侧缺了「未开启」这一档。
- 【修正：view/drawWorld.js `minimapColor` 补上「未开启常态」档，三态一眼分明】未开启且处引导态 → 维持既有金光脉动逐字不变（蘑菇支线 `side_mushroom==='active'` / 洞窟宝藏 `caveBoss` 同判，`UI_PULSE_MS` 脉动节奏同源）；未开启且非引导态 → 常驻暖金 `#c9a86a`（与既有 SHOP `#ffd24a`、金脉动 `#8a5a00` 同族调性、与小地图暗底对比清晰，一眼认出「这格还有宝」）；开启过（`hero.chests.has(x,y)`）→ 回落草地色 `#2f6b2f`（已开 = 已采空，与地面无异）。三态都只读 `hero.chests` / 任务态两个既有数据源，与画布侧 `TILE_CHEST_OPEN` 同一真源（`hero.chests`），纯显示零结算变化。
- 【零回归面】未动任何结算——遭遇/掉落/经验/金币/难度/技能/战斗/存档/遇敌槽/传送逐字未动；未改 data.js 数值（仅版本号递增 v19.46→v19.47）；未动敌人、Boss、支线、成就、帮助页任何文案；`UI_PULSE_MS` 脉动逻辑、`dangerAt` 危险格红色叠加、NPC/祭坛/试炼碑/小地图其余所有图块颜色逐字不变；仅 `minimapColor` 一处内部三分支重组，其余图块（TREE/ROCK/WATER/TOWN/PATH/BOSS/SHOP/INN/FOUNTAIN/BREW/NPC/MB/SB/TRIAL/CAVE/CAVEWALL/STELE/GATE/EXIT）配色逐字未动。只改 1 个文件（view/drawWorld.js：minimapColor 注释 + 三态分支）+ 版本号，零新增依赖。
- 验证：`node --check js/view/drawWorld.js js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.47 专项冒烟（/tmp/jrpg_smoke_v1947_minimap_chest.mjs）**14 项断言全过**——`GAME_VERSION='v19.47'` 递增且导出、drawWorld.js 含常驻暖金 `#c9a86a`、三态判定同源读 `hero.chests.has`、引导态金光脉动判定保留（`side_mushroom==='active'` / `caveBoss` 同判）且仍读 `UI_PULSE_MS`、旧的「仅引导态单变量直通」分支已移除、运行期以 DOM 桩真实导入模块链：loadMap('village') 建图成功、village 未开宝箱格恰 2 个（TY.CHEST 计数）、默认态/已开启态（chests.add 一箱）/蘑菇支线引导态/洞窟宝藏引导态（caveBoss）四档 `drawWorld()` 渲染全部不抛错。`/tmp/jrpg_smoke_v1946_clerk.mjs` 复跑仅「版本字符串钉死 v19.46」1 项随版本递增失败（预期，其余 43 项全过、功能零回归）；平衡模拟 5 档生还率与前版一致（纯显示改动不影响遇敌/伤害）。

## v19.46 新支线「石壳里的记忆」——潮灯镇新增 NPC「守书记」，讨伐 3 只石魔像换旧矿最后一页（新支线/NPC·内容扩充，v19.38 时序体系收口后轮换回常规改进的第七版，本次切「新支线/NPC」方向——近四版（v19.42-44 遇敌池平衡 + v19.45 标题预览显示）连续做平衡/显示后，长期缺席的新内容轮换回来：石魔像是旧矿的守门人，石壳里封着矿脉的记忆，此故事缺口此前无人提及，也无任何支线/成就与其挂钩）

- 【石魔像这条数值最硬的中期怪没有任何叙事事主】游戏里 8 种基础魔物中，毒蛇/骷髅兵/树精各有图鉴伏笔、雾灵有「雾里的新住客」支线、史莱姆/野狼/哥布林是新手接待——唯独 石魔像（hp36+7lv·def10+1lv 的「数值强者」）只有野外出没，没有对话、没有委托、没有成就照应；而它作为旧矿守门人的设定（v19.24 祭坛旁白、井巫/车夫台词都提到「旧矿守门人」却没人把石壳与记忆串起来）正是「信息透明·剧情扣题」的现成落点：矿脉的记忆由守门人的石壳封存，打败 3 尊石魔像、带回「石壳记痕」由守书记解读旧矿最后一页。
- 【修正：data.js 一处落齐 新NPC + 新支线 + 新成就，全部走既有通用机制，零跨模块改动】潮灯镇广场喷泉南侧（12,8，瓦片原为草地、四邻全可走、起点 10,15 可达、不堵任何路径；该全局坐标键未被任何地图占用）新增 NPC_SPOTS '12,8'='clerk' + village extras `{x:12,y:8,ty:'NPC'}` + NPCS.clerk（守书记·mark hood，静态台词 + after 通关彩蛋）；QUESTS.side_stone「石壳里的记忆」：讨伐 STONE_GOAL(=3) 只【石魔像】（bestiary 计数，与 side_mist 同一「讨伐采集型支线」模式，阈值单一数据源 STONE_GOAL——判定 cond / 进度 condProg / 目标 obj / 接取对话 offer 全同读此源），奖励金 60 + 高级灵药 ×1（potion2，与 side_mist 的药水互补、为酿造之外的另一条灵药获取途径）；ACH_LIST 增补成就「旧账已结」（stone）。世界观文案与井巫/灯卫/星砂线同构，头/尾呼应。对话/任务推进/❕头顶标记/击败计数全部复用 quests.js 通用状态机与 drawWorld 通用渲染，只改 data.js 一个文件。
- 【零回归面】未动任何结算法则——遭遇/掉落/经验/金币/难度/技能/战斗/存档格式逐字未动；未改 quests.js/core.js/world.js 任何逻辑（新支线纯数据注入即可接入既有 npcQuestPages/resolveNpcTalk/questStatus/npcQuestMark）；既有四条支线（灯长/星砂车夫/巡灯人/雾径猎手）与既有 17 项成就逐条未动；NPC_SPOTS 全部 12 个全局坐标键保持唯一、无撞键；遇敌池（v19.42-44 收口的 village 四基础怪限定等）逐字不变，石魔像仍按原权重在雾语林/矿脉出没（支线可正常完成）；帮助页潮灯镇行同步提及守书记。只改 1 个文件（data.js：版本号 + STONE_GOAL 常量 + 3 处数据插入 + export 一行 + 一行帮助文案），零新增依赖。
- 验证：`node --check js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.46 专项冒烟（/tmp/jrpg_smoke_v1946_clerk.mjs）**44 项断言全过**——`GAME_VERSION='v19.46'` 递增且导出、STONE_GOAL=3 导出、NPC_SPOTS['12,8']='clerk' 且 12 键全局唯一/全值入 NPCS/clerk 仅一落点、village extras 含 {12,8,NPC} 且瓦片原为草地、loadMap('village') 后 at(12,8)=TY.NPC、四邻可走且玩家起点 BFS 可达（不堵路）、NPCS.clerk/QUESTS.side_stone 结构（id/kind/store/giver/npc）与奖励 {gold:60,potion2:1}、obj/talk 引用阈值、MON_BASE/BESTIARY_TARGET 含石魔像（cond 关键字合法）、ACH_LIST.stone、帮助页「灯长/守书记(支线)」、支线状态机全链路（offer→❕可接→accept→active 0/3份→bestiary2仍active→bestiary3 turnin→❕可交→reward 金+60 灵药+1→done→无❕→日志转完成）、unlockedAchievements 触发 stone、migrateQuests 保留 done 且兜底 seen、既有四支线/成就保留、village 遇敌仍只出四基础怪且石魔像野外权重非零（可完成）、drawWorld/renderHUD 含守书记渲染不抛错、核心导出齐全。运行期以 DOM 桩真实导入模块链验证落点/建图/对话/交付/成就五步全通。

## v19.45 标题页存档预览补「难度」——多存档槽一眼区分普通/困难档（体验打磨·信息透明，v19.38 时序体系收口后轮换回常规改进的第六版，本次切回「体验/显示」方向——近三版（v19.42-44）连续做遇敌池数值平衡后轮换，承 v19.39 版本号 / v19.40 蘑菇计数 / v19.41 图鉴已遭遇的「信息透明」主线）

- 【标题页存档槽预览无难度，多存档槽分不清普通/困难】标题画面 `slotPreview`（core.js）整行只有「名字 Lv.x 金币N 地图·进度」——同时存了 普通 与 困难 两档的玩家，在标题页切槽时完全看不出哪档是哪个难度：只能先进游戏、开 I 状态页（或翻创建页的倍率说明）才能确认档位；状态面板（drawStatus）早已显示 `[困难 · 魔物HP×…]`，唯独标题页这处「选档入口」没有难度口径，与「信息透明」主题不一致。
- 【修正：core.js `slotPreview` 读档预览追加难度标注，同源 DIFFS】core.js 从 data.js 导入本就导出的 `DIFFS=['普通','困难']`，预览行尾部按 `hero.diff`（DIFFS 下标，新Game/开始旅途同源）追加 ` · 困难`（普通默认档不刷屏，与 drawStatus 只标困难的惯例一致；`DIFFS[hero.diff] || '困难'` 兜底旧档脏值）。标题页 `drawTitle` 那行绿色预览（y=360）原样渲染，样式/位置零改动。收益：多存档槽区分一目了然，且难度名同读 DIFFS 单一数据源（改难度名只动 data.js 一处、创建页/状态页/预览三处同步），绝无第二套口径。
- 【零回归面】未动任何结算——slotPreview 只是读档预览的纯显示拼接，其余字段（名字/Lv/金币/地图/进度文案）逐字未动；`hasSlot`/`hasSave`/`saveKey`/存档读写/难度倍率（startBattle 的 DIFF_SCALE）/创建页/状态页逐字未动；普通档老存档 `hero.diff=0` 输出逐值恒等（不追加标注只加空串）。未动任何掉落/经验/金币曲线/难度倍率/技能/支线/成就/存档格式/战斗。只改 1 个 import + 1 行返回表达式 + 注释，零新增依赖。
- 验证：`node --check js/core.js js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.45 专项冒烟（/tmp/jrpg_smoke_v1945_diffpreview.mjs）**19 项断言全过**——`GAME_VERSION='v19.45'` 递增且导出、DIFFS 双难度名原样且可执行定义/export 恰一处、core.js import DIFFS 且 `DIFFS[hero.diff]` 条件拼接恰一处、slotPreview 困难档（hero.diff=1）预览含「· 困难」且追加在进度文案之后、普通档（0）不追加/与旧版逐值恒等、哨兵档（diff=9 脏值）兜底为「困难」、创建页/状态页难度口径不受影响、既有核心导出（MAPS/TRAVEL_LIST/BOSS）不受影响、运行期以 DOM 桩真实导入模块链 + localStorage 种子调 `slotPreview()` 分别以困难/普通/脏值三档验证预览落字。

## v19.44 潮灯镇遇敌池收口「四基础怪」——补上 `pool` 限定，安全枢纽不再撞出树精/石魔像（数值平衡·场景分层，承 v19.43 骷髅兵门槛后「每图遇敌池」分层思路的第三块拼图——无字回廊早有 `pool:['雾灵','石魔像','骷髅兵']` 限定、雾语林/星井矿脉按全池渐进，唯独潮灯镇这座「安宁之地」安全枢纽此前与雾语林共享全部 8 种魔物池——Lv.3+ 回镇补给整备时，镇内几片草地竟会随机撞出只该在雾语林/矿脉出没的树精/石魔像，与 v19.42「高等级图由强怪池主导」、v19.43「Lv.1 新手池只剩四基础怪」的池子分层设计缺最后一格）

- 【潮灯镇无 pool 限定，回镇补给会随机撞出强怪】MAPS 里 无字回廊 早已用 `pool` 把遇敌池限定为三强怪（`encounter.randomEncounter` 的 `poolDef` 分支只在 pool 存在时过滤），雾语林/星井矿脉则为全池渐进——唯独 潮灯镇（`village`）这座「安宁之地」（zone hp×0.9/atk×0.9 的安全枢纽）没写 pool，与雾语林共享全部 8 种魔物：镇内 G 高草格在 Lv.3 起即可随机出 树精/石魔像（minLv:3）、Lv.2 起出 雾灵/骷髅兵——玩家打完矿脉回来补给、炼药、存档的路上，在镇子草丛里被一只按当前等级缩放的 石魔像（hp36+7lv×0.9）或 树精 拦路，与「安宁之地」的定位自相矛盾，也是「高难图强怪主导 / 安全镇子只该有害虫级魔物」这张池子分层表上唯一空着的一格。
- 【修正：data.js `MAPS.village` 补 `pool:['史莱姆','野狼','哥布林','毒蛇']`（与 v19.43 确立的「四基础怪 / Lv.1 新手池」同一分类）】`encounter.randomEncounter` 的 `poolDef` 分支原样读取（零逻辑改动），镇子遇敌池从此恒为四基础怪：Lv.1-2 行为与旧版逐值恒等（其余四怪本就被 minLv 门槛挡住，池子收窄不改变任何既有出没分布），Lv.3+ 由「全 8 种」变为「四基础怪」——树精/石魔像/雾灵/骷髅兵彻底退出镇子（它们在雾语林/矿脉的权重/门槛/倍率逐字不动）；权重曲线（史莱姆/哥布林 `max(1,4-⌊lv/2⌋)`、野狼 `max(1,3-⌊lv/4⌋)`、毒蛇 `2+⌊lv/3⌋`）、minLv 门槛、zone ×0.9 倍率全部原样。收益：潮灯镇回归「安全枢纽」语义——回镇补给不被打扰，强怪留在野外图（与无字回廊限定三强怪严格对称：镇子限定四基础怪、回廊限定三强怪、中段两图全池渐进，每图池子一口径）。
- 【零回归面】只新增 `MAPS.village` 一行 `pool` + 注释：遇敌机制（`poolDef` 分支 / `encounterWeight` / `pickWeightedIndex`）、全部魔物属性（hp/atk/def/xp/gold）、权重表达式、minLv 门槛、区域倍率（village hp×0.9/atk×0.9）、精英判定（`eliteEncounter` 只属 dungeon）、掉落/经验/金币/难度/技能/支线/成就/存档逐字未动；雾语林/星井矿脉全池行为、无字回廊三强怪池逐字不变；HUD 区域标注、图鉴出没标注、帮助页不受影响（都不读 pool）。只改 1 行数据 + 注释，零新增依赖。
- 验证：`node --check js/data.js js/encounter.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.44 专项冒烟（/tmp/jrpg_smoke_v1944_villagepool.mjs）**16 项断言全过**——`GAME_VERSION='v19.44'` 递增且导出、`MAPS.village.pool` 深等于「四基础怪」且名字全在 MON_BASE、gallery 池三强怪不受影响、dungeon/cave 无 pool（全池保留）、树精/石魔像 Lv.5 权重仍非零（证明挡它们的机制是 pool 过滤而非等级门槛）、运行期真实 `randomEncounter`（village × Lv.1/3/5/10/20 各 400 次）只出四基础怪、Lv.1 镇子权重占比≈4/3/4/1（实测 史莱姆33.1% 野狼25.5% 哥布林32.9% 毒蛇8.6%，与旧版逐值恒等）、对照组 dungeon Lv.10 ×400 仍有骷髅兵/雾灵/树精/石魔像（只有镇子收窄）、核心导出齐全、`encounter.js` 的 pool 分支原样。

## v19.43 骷髅兵 Lv.1 出没门槛收口——补上 `minLv:2`，Lv.1 新手池只剩四基础怪（数值平衡·新手保护，v19.38 时序体系收口后轮换回常规改进的第五版，承 v19.42 野狼权重后同走「渐进遇敌」曲线的第二块拼图——上次修高等级段野狼不该常驻，这次修低等级段骷髅兵不该提前登场）

- 【骷髅兵权重本以 `lv>=2?2+⌊lv/3⌋:1` 表达「正式登场 Lv.2」，却让 Lv.1 以权重 1 漏进新手池】MON_BASE 的 minLv 门槛族里 雾灵=2、树精/石魔像=3、精英 Lv.3，唯独 骷髅兵 只把门槛写死在权重函数的三元分支里、没走 minLv 字段——遇敌判定 `encounterWeight` 先查 minLv 再查 w(lv)，骷髅兵缺省 minLv=1 使 Lv.1 以权重 1 出没；而它是 Lv1 可遇怪里 HP/防 最高（hp26+6lv·def5+1lv）、攻击与毒蛇并列最高（atk8+2lv）的数值强者：期望伤害占 Lv1 血比 31%，40k 蒙特卡洛里 Lv1 无药裸刷 43% 的死亡由它单独造成，是其 7.7% 出没占比的 5.6 倍——新手期最危险的一刀不是机制怪毒蛇、更难缠的树精石魔像，而是本该 Lv.2 才登场的骷髅兵，「正式登场 Lv.2」的语义被 `:1` 兜底悄悄拆穿。
- 【修正：MON_BASE 骷髅兵行补上 `minLv:2`（与雾灵同款「门槛字段」）】`encounterWeight` 的 minLv 门槛先于 w(lv) 判定，Lv.1 权重归零、骷髅兵彻底退出新手池；Lv.2 起 `2+⌊lv/3⌋` 逐字不变。收益：Lv.1 新手池收口为 史莱姆/野狼/哥布林/毒蛇 四基础怪（与「Lv1 不遇树精石魔像、Lv3 起遇精英」的保护阶梯同构），最危险一刀让位给机制明确的毒蛇；图鉴「Lv.X起出没」标注（rules.spawnLv 直读 minLv）自动同步为「Lv.2起出没」，与真实遇敌分布绝无第二套口径。
- 【零回归面】只改骷髅兵一个 `minLv` 字段 + 头部注释：兵力（hp/atk/def/xp/gold）与 w 表达式逐字不动，Lv.2+ 权重/遇敌分布与旧版逐值恒等（w 的 `lv>=2` 分支与 minLv:2 完全同语义，纯粹把散在权重函数里的门槛搬回门槛字段）；遇敌率（16% 步进触发）、掉落/经验/金币曲线/难度/技能/支线/成就/存档逐字未动；其余 7 种怪物 minLv 与权重逐字未动。只改 1 个字段 + 注释，零新增依赖。
- 验证：`node --check js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；平衡模拟复测（skill 脚本 30k 蒙特卡洛）**Lv1 无药裸刷生还率 97.83%→99.51%**、困难·带药生还率 94.83%→99.20%、骷髅兵彻底退出 Lv1 凶手榜，普通·带药 100% 与困难·裸刷 8.78%（保留硬核）逐字不变；新增 v19.43 专项冒烟（/tmp/jrpg_smoke_v1943_skeletongate.mjs）**24 项断言全过**——`GAME_VERSION='v19.43'` 递增且导出、MON_BASE 8 种结构不受影响、骷髅兵行含 `minLv:2` 且 w 表达式保留、代码行仅 雾灵+骷髅兵 两处 minLv:2、encounterWeight 全等级（Lv.1=0 / Lv.2=2 / Lv.3=3 / Lv.7=4 / Lv.11=5 / Lv.12=6，Lv.1-60 全扫与公式逐值恒等）、spawnLv 图鉴标注同步（骷髅兵=2·雾灵=2·树精/石魔像=3·史莱姆=1）、Lv.1 新手池精确为四基础怪、Lv.2 池六种回归、运行期 randomEncounter（Lv.1 village ×400）零骷髅兵 /（Lv.2 ×400）实得 56 次、eliteEncounter 直出石心魔像不受影响。

## v19.42 野狼遇敌权重随级渐进衰减——基础怪「渐进退场」补齐最后一格（数值平衡·低等级零变化，v19.38 时序体系收口后轮换回常规改进的第四版，本次切「数值平衡」方向——史莱姆/哥布林本就 `max(1,4-⌊lv/2⌋)` 随级衰减，唯独野狼以恒定权重 3 常驻全等级：Lv.10+ 在星井矿脉野狼出现频率与 Lv.1 在潮灯镇时一样高，弱怪强度/奖励却明显掉队，高等级图理应由强怪池主导）

- 【野狼权重裸 `w:()=>3` 恒定不随级衰减】MON_BASE 的遇敌权重除史莱姆/哥布林 `max(1,4-⌊lv/2⌋)` 随级逐档退场、骷髅兵/毒蛇/雾灵 `2+⌊lv/3⌋` 与树精/石魔像 `2+⌊lv/2⌋` 随级渐长外，野狼是唯一的静止常数 `()=>3`——Lv.1 与 Lv.12 出没频率逐字相同：它在 Lv.8+ 的权重（3/29≈10%）与 Lv.1（3/13≈23% 相对占比）虽随池子增大摊薄，却远超同为基础怪的史莱姆/哥布林（权重兜底 1），而野狼兵力/奖励（hp22+5lv·攻7+2lv·xp12+3lv）低于同级骷髅兵/毒蛇/雾灵一档——高等级战斗里「本该由强怪主导」的池子被一只弱怪以恒定频率占据，与「渐进」的遇敌曲线相悖，也拖慢高等级刷怪经验效率。
- 【修正：野狼权重改为 `w:(lv)=>Math.max(1,3-Math.floor(lv/4))`】Lv.1-3 逐字不变（⌊lv/4⌋=0 → 3，与旧 `()=>3` 恒等）；Lv.4-7 降至 2（⌊1⌋→3-1）；Lv.8-11 降至 1（⌊2⌋→3-2）；Lv.12+ 兜底 1（⌊3+⌋→3-⌊lv/4⌋≤0 → max(1,·)=1）——与史莱姆/哥布林同款「薄存在 · 偶遇」兜底，永不归零（高等级仍可偶遇野狼保留怀旧），但池子主体移交 骷髅兵/毒蛇/雾灵/树精/石魔像。收益：高等级遇敌分布更贴合「渐进」曲线、刷怪经验效率小幅回升、星井矿脉（区修正 hp×1.2/atk×1.15）等高难图不再被弱怪稀释威胁感；纯权重曲线调整，零新增依赖、零新增状态。
- 【零回归面】只改 MON_BASE 野狼一项 `w` 表达式与注释：所有怪物兵力（hp/atk/def/xp/gold）与其余 7 种权重逐字未动；遇敌判定（encounter.randomEncounter/encounterWeight/pickWeightedIndex）原样——仅「Lv.4 起野狼被抽样到的相对概率」变化，遇敌率（16% 步进触发）、掉落/经验/金币曲线/难度/技能/支线/成就/存档逐字未动；Lv.1-3（新手到雾语林前中期）遇敌分布与旧版逐值恒等，毫无行为差异。数据源仍为 MON_BASE.w 单一真源，图鉴「Lv.X起出没」标注与帮助页不受影响（不标权重）。只改 1 处表达式 + 5 行注释，零新增依赖。
- 验证：`node --check js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.42 专项冒烟（/tmp/jrpg_smoke_v1942_wolfw.mjs）**16 项断言全过**——`GAME_VERSION='v19.42'` 递增且导出、MON_BASE 8 种结构不受影响、野狼权重函数化（不再是 `()=>3`）、Lv.1-3 权重逐字 =3（与旧恒等）、Lv.4=2 / Lv.7=2 / Lv.8=1 / Lv.11=1 / Lv.12=1（⌊lv/4⌋ 边界全踩）、全等级 ≥1 永不归零、史莱姆/哥布林/骷髅兵/毒蛇/雾灵/树精/石魔像权重表达式逐字未变、依赖野狼权重的 `encounterWeight('野狼',lv)` 端到端同值、既有核心导出（T/MON_BASE/BESTIARY_TARGET 13 种）不受影响。

## v19.41 图鉴「已遭遇·未讨伐」揭示——新增 hero.seen 进战即记，图鉴对撞见过却未击败的敌人揭示名字与出没地（体验打磨·信息透明，v19.8 起「时序/显示参数数据化」体系收口毕（v19.38 台账清空、轮换回常规改进）后第三版，轮换回「新内容/体验」方向——此前图鉴只认「击败」（hero.bestiary），逃跑/战败的敌人永远显示 ❓？？？「从没遇到」，与本项目「信息透明」主题相悖）

- 【图鉴把「撞见过却打不过」的敌人伪装成从没遇到】`menus.js drawCodex` 的行仅由 `hero.bestiary`（击败计数）驱动：没打赢就显示 `❓ ？？？ / 未讨伐`——玩家在雾语林撞见过 Lv.3 的精英石心魔像、在矿脉撞见过洞窟领主，逃跑/战败后再开图鉴，界面却声称「从没见过这怪」；玩家自问「到底哪来的这东西」，图鉴作为唯一怪物手册给不出答案，刷全图鉴（完美收藏成就）时更是无法判断「还差谁没撞见」，只能靠撞。
- 【修正：battle.startBattle 进战即记 `hero.seen`，drawCodex 增补「已遭遇」分支】`battle.js` 开局 `S.enemy = deep(enemyDef)` 之后新增独立语句块：`canonicalName(S.enemy.name)` 归一（真身→本体，与 bestiary 同口径）后写入 `S.G.seen[key]=true`（`migrateQuests` 对新档/旧档兜底 `if(!hero.seen) hero.seen={}`；`snapshotHero` 全量快照自动持久化，零存档结构变更）。`menus.js drawCodex` 的 rows 增补 `seen:!!((hero.seen||{})[n])`，`!r.got` 分支内分叉：`seen` 为真 → 灰色揭示名字与（`whereFind` 同源的）出没地 + 右对齐「已遭遇 ✕0」+ 次行「⚠️ 尚未讨伐 · 兵力待收复」；未见 → 原 `❓ ？？？/未讨伐` 空态逐字不变。收益：图鉴如实回答「这怪我撞见过没有、在哪撞的」，出没等级/精英概率随 whereFind 同步揭示，而兵力/弱点/战利品仍在讨伐后才显示（不剧透）；纯追加状态零结算变化（逃跑成功率/掉落/经验/难度均不受影响）。
- 【零回归面】未动任何结算法则——startBattle 只在设置 S.enemy 之后追加一条 seen 写入，后续难度倍率/血条/hpMax/回合/blog/掉落/经验/成就/存档全部原位逐字未动；drawCodex 仅 `got` 为假时的渲染分支新增 `seen` 判据，`got` 真分支（讨伐行名字/出没地/讨伐数/强度参考/战利品预览）逐字未动；`seen` 是纯状态+纯显示，不参与任何经验/金币/掉落/成就/存档读写。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档格式/战斗时序。新增 1 个全量快照字段 + 1 处进战标记 + 1 处迁移兜底 + 1 个渲染分支，零新增依赖。
- 验证：`node --check js/battle.js js/quests.js js/view/menus.js js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.41 专项冒烟（/tmp/jrpg_smoke_v1941_seen.mjs）**31 项断言全过**——`GAME_VERSION='v19.41'` 递增且导出、migrateQuests 兜底 seen={} 且不覆盖已有值、startBattle 内 seen 标记经 canonicalName 归一且独立成句、drawCodex rows 增补 seen 且「已遭遇 ✕0」「兵力待收复」落位、未遭遇 ❓ 空态与讨伐行收益行逐字保留、menus 无 seen 结算表达式（纯只读）、既有核心导出（T/MON_BASE 8种/BESTIARY_TARGET 13种）不受影响、运行期以 BOM 桩真实导入模块链调用 `startBattle()` 分别以史莱姆/幽冥魔王·真身 验证 seen 落位与真身归一、`drawCodex()` 三态混合渲染不抛错并捕获「石心魔像 + 已遭遇 ✕0 + 雾语林·稀有精英 + 不剧透 HP58 + ❓ 未遭遇 + 讨伐行击败可得」等关键文案、讨伐后转正为已收录行。

## v19.40 状态页「魔法蘑菇」计数补全——I 状态面板资源总览行同源读 hero.mushrooms（体验打磨·信息透明，v19.8 起「时序/显示参数数据化」体系收口毕（v19.38 台账清空、轮换回常规改进）后第二版（承 v19.39 版本号元数据）——此前 HUD 顶栏虽有 🍄(s-mushroom) 读数，唯独 I 状态面板的资源总览行（金币/药水/灵药/时间）漏了蘑菇：蘑菇是灯长支线进度（MUSHROOM_GOAL=3 株集齐转交付）与酿造高级灵药的材料，状态页作为「角色全貌」却看不到它）

- 【状态面板资源行漏蘑菇、HUD 却已有】顶栏 HUD（index.html `s-mushroom` + hud.js `set('s-mushroom', hero.mushrooms||0)`）早就有 🍄 常驻读数，而 I 状态页（menus.js drawStatus 行 83）的资源总览一行（`金币：N 药水🍖:N 灵药🧪:N ⏱️时间`）唯独缺蘑菇——想核对「还差几株蘑菇交灯长支线 / 够不够酿高级灵药」要么开任务条、要么瞄 HUD，状态页自身看不了；资源清单里「金币/药水/灵药/时间」四样都在、独缺第五样材料，是本主题下最后一处资源自足性缺口。
- 【修正：menus.js 状态页资源行补 🍄 计数，与 HUD 同源读 hero.mushrooms】`text(...)` 表达式在灵药之后、时间之前插入 `  🍄:${hero.mushrooms||0}` ——同一行 `x=110,y=264,'14px'`，与金币/药水/灵药/时间同为左对齐连续展示；读数与顶栏 HUD 逐字同源（都读 `hero.mushrooms`，`||0` 兜底旧档缺字段），绝无第二套口径。收益：状态页作为角色全貌补全第五项资源，支线进度/酿造材料一眼可核；零新增状态、零结算变化（蘑菇的增减结算仍在 world 开箱 / battle 精英掉落 / shop 售卖 / core 酿造各自原地）。
- 【零回归面】未动任何逻辑——drawStatus 其余行（经验条/HP/MP/攻防/技能/下一技能/冒险进度/主线支线）逐字未动；HUD、任务条（quests.objectiveText「X/3 株」）、酿造页（drawBrew 蘑菇株数）各自既有读数原样；本次只补状态页一个只读绘制位。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档/战斗。只改 1 行表达式 + 4 行注释，零新增依赖。
- 验证：`node --check js/view/menus.js js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.40 专项冒烟（/tmp/jrpg_smoke_v1940_mushroom.mjs）**13 项断言全过**——`GAME_VERSION='v19.40'` 递增且导出、状态页资源行含 `🍄:${hero.mushrooms||0}` 且剔除注释后恰好 1 处读 hero.mushrooms、与金币/药水/灵药/时间同一行 110,264、menus 无蘑菇结算表达式（纯只读）、HUD 顶栏 s-mushroom 读数保留、既有核心导出（T/MON_BASE 8种/MUSHROOM_GOAL=3）不受影响、运行期以 BOM 桩真实导入模块链调用 `drawStatus()` 不抛错并实际绘制出「🍄:3」与蘑菇归零档「🍄:0」（捕获 fillText 计数联动实时字段）。

## v19.39 游戏版本号单一数据源——data.js 新增 GAME_VERSION 常量，标题画面底部脚注「潮灯记 v19.39」同读该源（体验打磨·单一口径，v19.8 起「时序/显示参数数据化」体系收口毕（v19.38 台账清空、轮换回常规改进）后首个「元数据数据化」——此前游戏内任何界面都不显示版本号，玩家汇报 bug / 排障只能靠 CHANGELOG 反查，本次给标题画面补上唯一真源）

- 【游戏内版本号此前为零散裸数据、游戏界面完全不显示】整份代码没有任何版本常量——每次发布只存在于 CHANGELOG 顶部一行与 git 提交信息里；玩家（及维护者）在游戏内无从得知自己在玩哪个版本。想排查「是不是旧版缓存 / 某版引入的问题」只能打开 CHANGELOG 对号，汇报时还要手动抄版本号。
- 【修正：data.js 在文件头部（`T=32` 之前）新增 `GAME_VERSION = 'v19.39'`（游戏版本号，单位：版本串，纯命名空间元数据）为唯一真源，注释写明「与 CHANGELOG 顶部当前版本一致、每版递增时同步」，随原 export 块导出（置于块首 `GAME_VERSION, T, TY, …`），零新增依赖】`view/menus.js` import 列表同步加 GAME_VERSION，标题画面 `drawTitle()` 在快捷键行（y=442）之下新增一行脚注 `fillText(\`潮灯记 ${GAME_VERSION}\`, CV.width/2, 464)`（10px、暗灰 #3d4a58，不抢主信息）。收益：玩家一眼看出版本、汇报/排障可精确引用；调版本只改 data.js 一处、标题同步，绝无第二套口径；不改任何存档 / 数值 / 流程，纯显示层新增。
- 【零回归面】未动任何游戏逻辑——标题画面其余绘制（星空 / 山峦 / 标题字 / 槽位 / 提示行）逐字未动；GAME_VERSION 仅被 menus.js import 一次、调用点恰好 1 处（脚注）。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档/回合制。只新增一个常量 + 1 处 import + 3 行绘制 + 注释，零新增依赖。
- 验证：`node --check js/data.js js/view/menus.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.39 专项冒烟（/tmp/jrpg_smoke_v1939_version.mjs）**14 项断言全过**——`GAME_VERSION='v19.39'` 导出且定义/export 齐全（位于文件头部）、menus.js import 且调用点去注释后恰好 1 处、脚注正文由该源拼接、运行期以 BOM 桩真实导入模块链调用 `drawTitle()` 不抛错并实际绘制出「潮灯记 v19.39」脚注（捕获 7 次 fillText）、既有核心导出（T / SKILL_DATA / MON_BASE 8 种）不受影响。

## v19.38 事后收尾延时「回场/熄灭」单一数据源——两处异步收尾同读 WRAP_GAP_MS（纯时序·单一口径，与 v19.34 BATTLE_GAP_MS 同一「setTimeout 切入/过渡延时」家族——本版收口的是历版注释从 v19.25 起反复标注「两处 setTimeout 1800 延时 · 刻意保留/逐类保留不动」的那批值，也是该延时家族自 v19.23 起连续第四块拼图、账台上最后一行「尚未收口」）

- 【事后收尾延时「1800ms」以裸字面量写两个文件两处、两处互不相关】事件后的异步收尾以完全相同的 `setTimeout(..., 1800)` 裸写两个文件：`core.js` 存档回写 saveMsg 熄灭（行 223，`setTimeout(() => { S.saveMsg = ''; bind.renderHUD(); }, ...)`——「💾 已存档到槽 N」横幅停留后自动熄灭）、`battle.js` 试炼通关回世界过渡（行 425，试炼三连胜后 `goto('world')` 并清理战斗态）——两处同值互不引用：想调这类事后收尾节奏（如放慢到 2000 让「已存档」横幅多亮一拍/通关余韵多停一秒、收紧到 1600 更利落）要改两个地方、还极易只改一处漏改另一处——存档提示熄灭与试炼通关回场的收尾延时悄然脱钩，而这个「事后收尾停多久」的延时参数始终没有名字（v19.23 收口 ALTAR_LEAD_MS=600 强敌祭坛战前切入、v19.29 收口 FINAL_LEAD_MS=700 终局/传送门关切入、v19.34 收口 BATTLE_GAP_MS=1400 战间换关/回归时都已在注释里把 1800 明确标注「刻意保留/逐类保留不动」，正是它们共同的下一块拼图）。
- 【修正：data.js 在 `CODEX_MSG_MS` 之后新增 `WRAP_GAP_MS = 1800`（事后收尾延时「回场/熄灭」，单位 ms，纯时序）为唯一真源，注释说明两处派生形态与刻意保留的其余档（boxMsg 各停留时级 1200–4200 不属本家族；battle.js 行 425 的 1800 与行 462 的 1400 同为「通关回世界」却是不同档——试炼通关比普通胜利多留一拍余韵，刻意分常量、不并入上方 BATTLE_GAP_MS），随原 export 块导出，零新增依赖】`core.js` 一处、`battle.js` 一处 `setTimeout(..., 1800)` 改读 `setTimeout(..., WRAP_GAP_MS)`（两模块 import 列表同步加 WRAP_GAP_MS；STRONG_MSG_MS / WIN_MSG_MS / ACH_MSG_MS / BATTLE_GAP_MS / MEMORY_MSG_MS / TUTOR_MSG_MS / CODEX_MSG_MS / NARR_MSG_MS 等注释里「两处 setTimeout 1800 延时 · 刻意保留/逐类保留不动/另一语义保留」同步改写为指向新源）。收益：调事后收尾节奏只改 data.js 一处、core/battle 两处同步，绝无第二套口径；行为逐字不变（`WRAP_GAP_MS` 仍为 1800，两处延时与旧字面量逐值恒等），零存档/试炼/终局流程回归。
- 【零回归面】未动收尾机制本身——两处业务判定（saveGame 存档提示时机与 renderHUD 熄灭、试炼三连胜通关分支）全部原样；其余 setTimeout 延时逐字未动：ALTAR_LEAD_MS=600 三处强敌祭坛战前、FINAL_LEAD_MS=700 三处终局/传送门关、BATTLE_GAP_MS=1400 两处（试炼换关切入 / 普通通关回世界）、HIT_FB_MS=220 受击反馈全部保留；boxMsg 各停留时级（短档 1200 / 小事件档 1600 / 强敌文案 1600 / 中长档 1800 / 更强档 2000 / 常规档 2200 / 里程碑档 2400 / 胜利档 2600 / 更长档 2800 / 成就档 3200 / 教程 3600 / 图鉴 4200）逐字未动。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只新增一个常量 + 改 2 处表达式 + 2 处 import + 1 处 export + 注释，零新增依赖。
- 验证：`node --check js/data.js js/core.js js/battle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.38 专项冒烟（/tmp/jrpg_smoke_v1938_wrapgap.mjs）**34 项断言全过**——`WRAP_GAP_MS=1800` 导出且定义/export 齐全、2 个模块（core/battle）import 且调用点恰好 2 处（core ×1 存档回写 / battle ×1 试炼通关）均读该源、可执行裸「, 1800)」清零（二十个模块剩 0 处、data.js 仅注释里的文档化字面量）、行为逐值恒等（1800===WRAP_GAP_MS）、「setTimeout 延时」家族既有成员（ALTAR_LEAD_MS 600 / FINAL_LEAD_MS 700 / BATTLE_GAP_MS 1400）连同 boxMsg 各停留时级（SHORT 1200 / EVENT 1600 / ALTAR_TXT 1600 / NARR 1800 / STRONG 2000 / SYS 2200 / MILESTONE 2400 / WIN 2600 / MEMORY 2800 / ACH 3200 / TUTOR 3600 / CODEX 4200）与 HIT_FB_MS 220 / UI_PULSE_MS 400 / DAY_PHASE_S 90 / BLOG_WIN 3 各自保留不受影响。

## v19.37 图鉴「成就类更长档」停留时长单一数据源——一处全图鉴捷报改读 CODEX_MSG_MS（纯时序·单一口径，与 v19.36 TUTOR_MSG_MS / v19.35 MEMORY_MSG_MS / v19.34 BATTLE_GAP_MS / v19.33 ACH_MSG_MS / v19.32 WIN_MSG_MS / v19.31 STRONG_MSG_MS / v19.30 EVENT_MSG_MS / v19.29 FINAL_LEAD_MS / v19.28 NARR_MSG_MS / v19.27 SHORT_MSG_MS / v19.26 MILESTONE_MS / v19.25 SYS_MSG_MS / v19.24 ALTAR_TXT_MS / v19.23 ALTAR_LEAD_MS 同一「提示框停留时长」家族——本版收口的是历版注释从 v19.25 起反复标注「图鉴 4200 · 刻意保留」的那批值，也是该家族自 v19.23 起连续第十四块拼图、成就类「更长档」里最后的独立档）

- 【图鉴档「4200ms」以裸字面量写 hero.js 仅一处】成就「完美收藏」图鉴全收集的里程碑式回馈以 `bind.boxMsg('🏆 图鉴收集完成！额外奖励 … 金币！', 4200)` 裸写 `hero.js` 成就应用（行 115，applyAchievements 的 PERFECTION 分支、额外发 PERFECTION_GOLD 金币）——单点裸奔：想调这类图鉴捷报读多久（如放慢到 4600 让全图鉴更有仪式感、收紧到 4000 更利落）要改一个地方、而这个参数始终没有名字（v19.32 收口 WIN_MSG_MS、v19.33 收口 ACH_MSG_MS、v19.35 收口 MEMORY_MSG_MS、v19.36 收口 TUTOR_MSG_MS 时都已在注释里把 4200 明确标注「图鉴 4200 · 刻意保留」，正是这个「成就类更长档」继 3200 成就档之后再收口的「更长」示例，也是台账此前「尚未收口」表的榜首）。
- 【修正：data.js 在 `TUTOR_MSG_MS` 之后新增 `CODEX_MSG_MS = 4200`（图鉴「成就类更长档」停留时长，单位 ms，纯时序）为唯一真源，注释说明一处派生形态与刻意保留的其余停留时级（两处 `setTimeout(..., 1800)` 延时与战间 1400 家族同为「延时/过渡」另一语义；boxMsg 各停留时级 1200–3600 均已逐版收口），随原 export 块导出，零新增依赖】`hero.js` 一处 `bind.boxMsg(..., 4200)` 改读 `bind.boxMsg(..., CODEX_MSG_MS)`（import 列表同步加 CODEX_MSG_MS；STRONG_MSG_MS / WIN_MSG_MS / ACH_MSG_MS / MEMORY_MSG_MS / TUTOR_MSG_MS 注释里「图鉴 4200 · 刻意保留」同步改写为指向新源）。收益：调图鉴全收集捷报停留节奏只改 data.js 一处、hero.js 同步，绝无第二套口径；行为逐字不变（`CODEX_MSG_MS` 仍为 4200，停留时长与旧字面量逐值恒等），零成就/图鉴流程回归。
- 【零回归面】未动提示机制本身——boxMsg 内容与业务判定（PERFECTION 全图鉴成就分支）原样；其余 boxMsg 停留时级逐字未动：短档 1200 / 小事件档 1600 / 强敌文案 1600 / 中长档 1800 / 更强档 2000 / 常规档 2200 / 里程碑档 2400 / 胜利档 2600 / 更长档 2800 / 成就档 3200 / 教程 3600 与战间 1400 家族、两处 `setTimeout(..., 1800)` 延时全部刻意保留，并逐一由本版专项冒烟钉死。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只新增一个常量 + 改 1 处表达式 + 1 处 import + 1 处 export + 注释，零新增依赖。
- 验证：`node --check js/data.js js/hero.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.37 专项冒烟（/tmp/jrpg_smoke_v1937_codexmsg.mjs）**34 项断言全过**——`CODEX_MSG_MS=4200` 导出且定义/export 齐全、hero.js import 且调用点恰好 1 处（图鉴收集完成）读该源、成就解锁仍读 ACH_MSG_MS、可执行裸「, 4200)」清零（二十四模块剩 0 处）、行为逐值恒等（4200===CODEX_MSG_MS）、时序家族既有成员（TUTOR_MSG_MS 3600 / MEMORY_MSG_MS 2800 / ACH_MSG_MS 3200 / BATTLE_GAP_MS 1400 / WIN_MSG_MS 2600 / STRONG_MSG_MS 2000 / EVENT_MSG_MS 1600 / ALTAR_TXT_MS 1600 / ALTAR_LEAD_MS 600 / SYS_MSG_MS 2200 / MILESTONE_MS 2400 / SHORT_MSG_MS 1200 / NARR_MSG_MS 1800 / FINAL_LEAD_MS 700 / HIT_FB_MS 220 / UI_PULSE_MS 400 / DAY_PHASE_S 90 / BLOG_WIN 3）以及两处 setTimeout 1800 延时各自保留不受影响。

## v19.36 教程档「入门引导」停留时长单一数据源——一处教程提示改读 TUTOR_MSG_MS（纯时序·单一口径，与 v19.35 MEMORY_MSG_MS / v19.34 BATTLE_GAP_MS / v19.33 ACH_MSG_MS / v19.32 WIN_MSG_MS / v19.31 STRONG_MSG_MS / v19.30 EVENT_MSG_MS / v19.29 FINAL_LEAD_MS / v19.28 NARR_MSG_MS / v19.27 SHORT_MSG_MS / v19.26 MILESTONE_MS / v19.25 SYS_MSG_MS / v19.24 ALTAR_TXT_MS / v19.23 ALTAR_LEAD_MS 同一「提示框停留时长」家族——本版收口的是历版注释从 v19.25 起反复标注「教程 3600 · 刻意保留」的那批值，也是该家族自 v19.23 起连续第十三块拼图）

- 【教程档「3600ms」以裸字面量写 main.js 仅一处】新手教程入门引导以 `boxMsg('💡 教程：…', 3600)` 裸写 `main.js` 启动引导（行 191，踏上旅途后首次进入世界的 WASD/Enter/Esc/P/I/J/H 操作提示）——单点裸奔：想调这类教程引导读多久（如放慢到 3800 让新手多读两秒、收紧到 3400 更利落）要改一个地方、而这个参数始终没有名字（v19.25 收口 SYS_MSG_MS、v19.28 收口 NARR_MSG_MS、v19.32 收口 WIN_MSG_MS、v19.35 收口 MEMORY_MSG_MS 时都已在注释里把 3600 明确标注「教程 3600 · 刻意保留」，正是这个「更长档」家族继 2800 记忆碎片之后再收口的「更长档」示例）。
- 【修正：data.js 在 `MEMORY_MSG_MS` 之后新增 `TUTOR_MSG_MS = 3600`（教程档「入门引导」停留时长，单位 ms，纯时序）为唯一真源，注释说明一处派生形态与刻意保留的其余停留时级（图鉴 4200 同属「更长档」却是更长的独立档、台账下一候选；两处 `setTimeout(..., 1800)` 延时与战间 1400 家族同为「延时/过渡」另一语义），随原 export 块导出，零新增依赖】`main.js` 一处 `boxMsg(..., 3600)` 改读 `boxMsg(..., TUTOR_MSG_MS)`（import 列表同步加 TUTOR_MSG_MS；STRONG_MSG_MS / WIN_MSG_MS / ACH_MSG_MS / MEMORY_MSG_MS 注释里「教程 3600 · 刻意保留」同步改写为指向新源）。收益：调教程引导停留节奏只改 data.js 一处、main.js 同步，绝无第二套口径；行为逐字不变（`TUTOR_MSG_MS` 仍为 3600，停留时长与旧字面量逐值恒等），零启动/教程流程回归。
- 【零回归面】未动提示机制本身——boxMsg 内容与业务判定（tutDone 新手门控分支）原样；其余 boxMsg 停留时级逐字未动：短档 1200 / 小事件档 1600 / 强敌文案 1600 / 中长档 1800 / 更强档 2000 / 常规档 2200 / 里程碑档 2400 / 胜利档 2600 / 更长档 2800 / 成就档 3200 / 图鉴 4200 与战间 1400 家族、两处 `setTimeout(..., 1800)` 延时（core 存档回写 / battle 通关回世界）全部刻意保留，并逐一由本版专项冒烟钉死。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只新增一个常量 + 改 1 处表达式 + 1 处 import + 1 处 export + 注释，零新增依赖。
- 验证：`node --check js/data.js js/main.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.36 专项冒烟（/tmp/jrpg_smoke_v1936_tutormsg.mjs）**33 项断言全过**——`TUTOR_MSG_MS=3600` 导出且定义/export 齐全、main.js import 且调用点恰好 1 处（教程提示）读该源、踏上旅途提示仍读 STRONG_MSG_MS、可执行裸「, 3600)」清零（二十四模块剩 0 处）、行为逐值恒等（3600===TUTOR_MSG_MS）、时序家族既有成员（MEMORY_MSG_MS 2800 / ACH_MSG_MS 3200 / BATTLE_GAP_MS 1400 / WIN_MSG_MS 2600 / STRONG_MSG_MS 2000 / EVENT_MSG_MS 1600 / ALTAR_TXT_MS 1600 / ALTAR_LEAD_MS 600 / SYS_MSG_MS 2200 / MILESTONE_MS 2400 / SHORT_MSG_MS 1200 / NARR_MSG_MS 1800 / FINAL_LEAD_MS 700 / HIT_FB_MS 220 / UI_PULSE_MS 400 / DAY_PHASE_S 90 / BLOG_WIN 3）以及图鉴 4200、两处 setTimeout 1800 延时各自保留不受影响。

## v19.35 记忆碎片「更长档」停留时长单一数据源——一处拾遗播报改读 MEMORY_MSG_MS（纯时序·单一口径，与 v19.34 BATTLE_GAP_MS / v19.33 ACH_MSG_MS / v19.32 WIN_MSG_MS / v19.31 STRONG_MSG_MS / v19.30 EVENT_MSG_MS / v19.29 FINAL_LEAD_MS / v19.28 NARR_MSG_MS / v19.27 SHORT_MSG_MS / v19.26 MILESTONE_MS / v19.25 SYS_MSG_MS / v19.24 ALTAR_TXT_MS / v19.23 ALTAR_LEAD_MS 同一「提示框停留时长」家族——本版收口的是历版注释从 v19.25 起反复标注「更长档 2800 · 刻意保留」的那批值，也是该家族自 v19.23 起连续第十二块拼图）

- 【更长档「2800ms」以裸字面量写 battle.js 仅一处】强敌首胜掉落记忆碎片的拾遗播报以 `boxMsg(..., 2800)` 裸写 `battle.js` 收关流程（行 411，FRAGMENTS 强敌首胜掉落记忆碎片「（按 J 日志回看）」）——单点裸奔：想调这类记忆回馈读多久（如放慢到 3000 让拾遗更有余韵、收紧到 2600 更利落）要改一个地方、而这个参数始终没有名字（v19.25 收口 SYS_MSG_MS、v19.26 收口 MILESTONE_MS、v19.27 收口 SHORT_MSG_MS、v19.28 收口 NARR_MSG_MS、v19.32 收口 WIN_MSG_MS、v19.33 收口 ACH_MSG_MS 时都已在注释里把 2800 明确标注「更长档 · 刻意保留」，正是这个家族继 3200 成就档、1400 战间延时之后最后一批「更长档」拼图之一）。
- 【修正：data.js 在 `BATTLE_GAP_MS` 之后新增 `MEMORY_MSG_MS = 2800`（记忆碎片「更长档」停留时长，单位 ms，纯时序）为唯一真源，注释说明一处派生形态与刻意保留的其余停留时级（教程 3600 / 图鉴 4200 同属「更长档」另类语义各为独立档；两处 `setTimeout(..., 1800)` 延时与战间 1400 家族同为「延时/过渡」另一语义），随原 export 块导出，零新增依赖】`battle.js` 一处 `boxMsg(..., 2800)` 改读 `boxMsg(..., MEMORY_MSG_MS)`（import 列表同步加 MEMORY_MSG_MS；STRONG_MSG_MS / WIN_MSG_MS / ACH_MSG_MS 注释里「更长档 2800 · 刻意保留」同步改写为指向新源）。收益：调记忆拾遗播报停留节奏只改 data.js 一处、battle.js 同步，绝无第二套口径；行为逐字不变（`MEMORY_MSG_MS` 仍为 2800，停留时长与旧字面量逐值恒等），零战斗/记忆碎片流程回归。
- 【零回归面】未动提示机制本身——boxMsg 内容与业务判定（FRAGMENTS 首胜掉落分支）原样；其余 boxMsg 停留时级逐字未动：短档 1200 / 小事件档 1600 / 强敌文案 1600 / 中长档 1800 / 更强档 2000 / 常规档 2200 / 里程碑档 2400 / 胜利档 2600 / 成就档 3200 / 教程 3600 / 图鉴 4200 与战间 1400 家族、两处 `setTimeout(..., 1800)` 延时全部刻意保留，并逐一由本版专项冒烟钉死。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只新增一个常量 + 改 1 处表达式 + 1 处 import + 1 处 export + 注释，零新增依赖。
- 验证：`node --check js/data.js js/battle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.35 专项冒烟（/tmp/jrpg_smoke_v1935_memorymsg.mjs）**31 项断言全过**——`MEMORY_MSG_MS=2800` 导出且定义/export 齐全、battle.js import 且调用点恰好 1 处（记忆碎片拾取播报）读该源、可执行裸「, 2800)」清零（二十四个模块剩 0 处）、行为逐值恒等（2800===MEMORY_MSG_MS）、时序家族既有成员（ACH_MSG_MS 3200 / BATTLE_GAP_MS 1400 / WIN_MSG_MS 2600 / STRONG_MSG_MS 2000 / EVENT_MSG_MS 1600 / ALTAR_TXT_MS 1600 / ALTAR_LEAD_MS 600 / SYS_MSG_MS 2200 / MILESTONE_MS 2400 / SHORT_MSG_MS 1200 / NARR_MSG_MS 1800 / FINAL_LEAD_MS 700 / HIT_FB_MS 220 / UI_PULSE_MS 400 / DAY_PHASE_S 90 / BLOG_WIN 3）以及教程 3600、图鉴 4200、两处 setTimeout 1800 延时各自保留不受影响。

## v19.34 战间延时「换关/回归」单一数据源——两处转场停顿同读 BATTLE_GAP_MS（纯时序·单一口径，与 v19.23 ALTAR_LEAD_MS / v19.29 FINAL_LEAD_MS 同一「setTimeout 切入/过渡延时」家族——本版收口的是历版注释从 v19.25 起反复标注「战间 1400 家族 · 刻意保留」的那批值，也是该延时家族自 v19.23 起连续第三块拼图）

- 【战间延时「1400ms」以裸字面量写 battle.js 两处、两处互不相关】战斗与战斗之间的转场停顿以完全相同的 `setTimeout(..., 1400)` 裸写一个文件两处：`battle.js` 试炼连胜换关切入下一战（行 433，`startBattle(deep(RUSH_BOSSES[stage]))`）、通关回世界过渡（行 462，`goto('world')` 并清理战斗态）——两处同值互不引用：想调这类战间节奏（如放慢到 1600 让换关更有间歇感、收紧到 1200 更利落）要改两个地方、还极易只改一处漏改另一处——试炼换关/通关回场的停顿悄然脱钩，而这个「战间停多久」的延时参数始终没有名字（v19.23 收口 ALTAR_LEAD_MS=600、v19.29 收口 FINAL_LEAD_MS=700 时都已在注释里把 1400 明确标注「战间 1400 家族 · 刻意保留」，正是它们共同的下一块拼图）。
- 【修正：data.js 在 `ACH_MSG_MS` 之后新增 `BATTLE_GAP_MS = 1400`（战间延时「换关/回归」延时，单位 ms，纯时序）为唯一真源，注释说明两处派生形态与刻意保留的其余停留时级（同为「延时/过渡」另一语义的两处 `setTimeout(..., 1800)`——core 存档回写 / battle 试炼通关后回世界——同值同语义却由各自语义分别保留；boxMsg 各停留时级不属本家族），随原 export 块导出，零新增依赖】`battle.js` 两处 `setTimeout(..., 1400)` 改读 `setTimeout(..., BATTLE_GAP_MS)`（import 列表同步加 BATTLE_GAP_MS；STRONG_MSG_MS / WIN_MSG_MS / ACH_MSG_MS 注释里「战间 1400 · 刻意保留」同步改写为指向新源）。收益：调战间转场节奏只改 data.js 一处、battle.js 两处同步，绝无第二套口径；行为逐字不变（`BATTLE_GAP_MS` 仍为 1400，两处延时与旧字面量逐值恒等），零战斗/试炼/终局流程回归。
- 【零回归面】未动切入机制本身——两处业务判定（rush 换关连胜逻辑与 `goto('world')` 后清理 S.enemy/battleBusy + resumeBgm 的收尾动作）全部原样；其余 setTimeout 延时逐字未动：ALTAR_LEAD_MS=600 三处强敌祭坛战前、FINAL_LEAD_MS=700 三处终局/传送门关（world ×2 / battle ×1）、HIT_FB_MS=220 受击反馈、两处 `setTimeout(..., 1800)`（core 存档回写 / battle 试炼通关后回世界）与 boxMsg 各停留时级（短档 1200、小事件档 1600、强敌文案 1600、中长档 1800、更强档 2000、常规档 2200、里程碑档 2400、胜利档 2600、更长档 2800、成就档 3200、教程 3600、图鉴 4200 与战间 1400 之外全部原样。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只新增一个常量 + 改 2 处表达式 + 1 处 import + 1 处 export + 三处注释指向，零新增依赖。
- 验证：`node --check js/data.js js/battle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.34 专项冒烟（/tmp/jrpg_smoke_v1934_battlegap.mjs）**31 项断言全过**——`BATTLE_GAP_MS=1400` 导出且定义/export 齐全、battle.js import 且调用点恰好 2 处（试炼换关 + 通关回世界）均读该源、可执行裸「, 1400)」清零（二十个模块剩 0 处）、行为逐值恒等（1400===BATTLE_GAP_MS）、时序家族既有成员（ACH_MSG_MS 3200 / WIN_MSG_MS 2600 / STRONG_MSG_MS 2000 / EVENT_MSG_MS 1600 / ALTAR_TXT_MS 1600 / ALTAR_LEAD_MS 600 / SYS_MSG_MS 2200 / MILESTONE_MS 2400 / SHORT_MSG_MS 1200 / NARR_MSG_MS 1800 / FINAL_LEAD_MS 700 / HIT_FB_MS 220 / UI_PULSE_MS 400 / DAY_PHASE_S 90 / BLOG_WIN 3）以及更长档 2800、教程 3600、图鉴 4200、两处 setTimeout 1800 延时各自保留不受影响。

## v19.33 成就/终局类「成就档」停留时长单一数据源——三处捷报同读 ACH_MSG_MS（纯时序·单一口径，与 v19.32 WIN_MSG_MS / v19.31 STRONG_MSG_MS / v19.30 EVENT_MSG_MS / v19.29 FINAL_LEAD_MS / v19.28 NARR_MSG_MS / v19.27 SHORT_MSG_MS / v19.26 MILESTONE_MS / v19.25 SYS_MSG_MS / v19.24 ALTAR_TXT_MS / v19.23 ALTAR_LEAD_MS 同一「提示框停留时长」家族——本版收口的是历版注释从 v19.25 起反复标注「成就类 · 刻意保留」的 3200 档，也是该家族自 v19.23 起连续第十一块拼图）

- 【成就档「3200ms」以裸字面量写两个文件三处、三处互不相关】里程碑式成就/终局回馈以完全相同的 `boxMsg(..., 3200)` 裸写两个文件：`battle.js` 试炼通关（行 424）、真结局落幕（行 455）、`hero.js` 成就解锁（行 117）——三处同值互不引用：想调这类成就回馈读多久（如放慢到 3400 让通关/解锁更有仪式感、收紧到 3000 更利落）要改三个地方、还极易只改一处漏改另几处——试炼通关/真结局/成就解锁的停留时长悄然脱钩，而这个参数始终没有名字（v19.25 收口 SYS_MSG_MS、v19.26 收口 MILESTONE_MS、v19.27 收口 SHORT_MSG_MS、v19.28 收口 NARR_MSG_MS、v19.30 收口 EVENT_MSG_MS、v19.31 收口 STRONG_MSG_MS、v19.32 收口 WIN_MSG_MS 时都已在注释里把 3200 明确标注「成就类 · 刻意保留」，正是它们共同的下一块拼图）。
- 【修正：data.js 在 `WIN_MSG_MS` 之后新增 `ACH_MSG_MS = 3200`（成就/终局类「成就档」停留时长，单位 ms，纯时序）为唯一真源，注释说明三处派生形态与刻意保留的其余停留时级（图鉴 4200 同属「成就类」却是更长的独立档；更长档 2800 记忆碎片 / 教程 3600 各为独立语义；战间 1400 家族与两处 `setTimeout(..., 1800)`「延时/过渡」另一语义刻意不并入），随原 export 块导出，零新增依赖】`battle.js` 两处、`hero.js` 一处 `boxMsg(..., 3200)` 改读 `boxMsg(..., ACH_MSG_MS)`（两模块 import 列表同步加 ACH_MSG_MS；STRONG_MSG_MS / WIN_MSG_MS 注释里「成就类 3200 · 刻意保留」同步改写为指向新源）。收益：调成就捷报停留节奏只改 data.js 一处、两个模块三处同步，绝无第二套口径；行为逐字不变（`ACH_MSG_MS` 仍为 3200，三处停留时长与旧字面量逐值恒等），零战斗/成就/终局流程回归。
- 【零回归面】未动提示机制本身——boxMsg 内容、三处业务判定（winBattle 试炼通关分支、真结局落幕分支、applyAchievements 成就解锁）全部原样；其余 boxMsg 停留时级逐字未动：短档 1200（SHORT_MSG_MS）、中长档 1800（NARR_MSG_MS 八处）、常规档 2200（SYS_MSG_MS）、里程碑档 2400（MILESTONE_MS）、小事件档 1600（EVENT_MSG_MS 六处）、强敌文案 1600（ALTAR_TXT_MS）、更强档 2000（STRONG_MSG_MS 三处）、胜利档 2600（WIN_MSG_MS 五处）、更长档 2800（记忆碎片）、教程 3600、图鉴 4200（成就类更长的独立档，刻意不并入）与战间 1400 家族、两处 `setTimeout(..., 1800)` 延时（core 存档回写 / battle 通关回世界）全部刻意保留，并逐一由本版专项冒烟钉死。未动任何掉落/经验/金币曲线/难度/技能/支线/存档。只新增一个常量 + 改 3 处表达式 + 2 处 import + 1 处 export + 注释，零新增依赖。
- 验证：`node --check js/data.js js/battle.js js/hero.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.33 专项冒烟（/tmp/jrpg_smoke_v1933_achmsg.mjs）**32 项断言全过**——`ACH_MSG_MS=3200` 导出且定义/export 齐全、2 个模块（battle/hero）import 且调用点恰好 3 处（battle ×2 / hero ×1）均读该源、可执行裸「boxMsg(…, 3200)」清零（剩 0 处、data.js 仅注释里的文档化字面量）、行为逐值恒等（3200===ACH_MSG_MS）、时序家族既有成员（STRONG_MSG_MS 2000 / EVENT_MSG_MS 1600 / ALTAR_TXT_MS 1600 / ALTAR_LEAD_MS 600 / SYS_MSG_MS 2200 / MILESTONE_MS 2400 / SHORT_MSG_MS 1200 / NARR_MSG_MS 1800 / FINAL_LEAD_MS 700 / HIT_FB_MS 220 / UI_PULSE_MS 400 / DAY_PHASE_S 90 / BLOG_WIN 3 / WIN_MSG_MS 2600）以及更长档 2800、教程 3600、图鉴 4200、战间 1400×2、两处 setTimeout 1800 延时各自保留不受影响。

## v19.32 胜利类回馈「胜利档」停留时长单一数据源——五处捷报同读 WIN_MSG_MS（纯时序·单一口径，与 v19.31 STRONG_MSG_MS / v19.30 EVENT_MSG_MS / v19.29 FINAL_LEAD_MS / v19.28 NARR_MSG_MS / v19.27 SHORT_MSG_MS / v19.26 MILESTONE_MS / v19.25 SYS_MSG_MS / v19.24 ALTAR_TXT_MS / v19.23 ALTAR_LEAD_MS 同一「提示框停留时长」家族——本版收口的是历版注释从 v19.24 起反复标注「更强档之上更长 · 刻意保留」的 2600 档，也是该家族自 v19.23 起连续第十块拼图）

- 【胜利档「2600ms」以裸字面量写两个文件五处、五处互不相关】战斗大捷/任务落袋的捷报回馈以完全相同的 `boxMsg(..., 2600)` 裸写两个文件：`battle.js` 胜利结算（行 389）、额外掉落（行 397）、圣光之剑觉醒（行 442）、洞窟领主倒下（行 451）、`core.js` 任务完成（行 154）——五处同值互不引用：想调这类胜利回馈读多久（如放慢到 2800 让捷报多停一秒、收紧到 2400 更利落）要改五个地方、还极易只改一处漏改另几处——胜利/掉落/圣剑/首领扑街/任务落袋的停留时长悄然脱钩，而这个参数始终没有名字（v19.25 收口 SYS_MSG_MS、v19.27 收口 SHORT_MSG_MS、v19.28 收口 NARR_MSG_MS、v19.30 收口 EVENT_MSG_MS、v19.31 收口 STRONG_MSG_MS 时都已在注释里把 2600 明确标注「胜利类 · 刻意保留」，正是它们共同的下一块拼图）。
- 【修正：data.js 在 `STRONG_MSG_MS` 之后新增 `WIN_MSG_MS = 2600`（胜利类回馈「胜利档」停留时长，单位 ms，纯时序）为唯一真源，注释说明五处派生形态与刻意保留的其余停留时级（成就类 3200 三处 / 更长档 2800 记忆碎片 / 教程 3600 / 图鉴 4200 各为独立语义；战间 1400 家族与两处 `setTimeout(..., 1800)`「延时/过渡」另一语义刻意不并入），随原 export 块导出，零新增依赖】`battle.js` 四处、`core.js` 一处 `boxMsg(..., 2600)` 改读 `boxMsg(..., WIN_MSG_MS)`（两模块 import 列表同步加 WIN_MSG_MS）。收益：调胜利捷报停留节奏只改 data.js 一处、两个模块五处同步，绝无第二套口径；行为逐字不变（`WIN_MSG_MS` 仍为 2600，五处停留时长与旧字面量逐值恒等），零战斗/任务流程回归。
- 【零回归面】未动提示机制本身——boxMsg 内容、五处业务判定（winBattle 结算分支、rollDrop 额外掉落、圣光之剑觉醒、洞窟领主倒下、任务完成领奖）全部原样；其余 boxMsg 停留时级逐字未动：短档 1200（SHORT_MSG_MS）、中长档 1800（NARR_MSG_MS 八处）、常规档 2200（SYS_MSG_MS）、里程碑档 2400（MILESTONE_MS）、小事件档 1600（EVENT_MSG_MS 六处）、强敌文案 1600（ALTAR_TXT_MS）、更强档 2000（STRONG_MSG_MS 三处）、成就类 3200（试炼通关/真结局/成就解锁共三处）、更长档 2800（记忆碎片）、教程 3600、图鉴 4200 与战间 1400 家族、两处 `setTimeout(..., 1800)` 延时（core 存档回写 / battle 通关回世界）全部刻意保留，并逐一由本版专项冒烟钉死。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只新增一个常量 + 改 5 处表达式 + 2 处 import + 1 处 export + 注释，零新增依赖。
- 验证：`node --check js/data.js js/battle.js js/core.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响，且两套测试均 import core/battle/data 全链接、运行时模块图解析无碍）；新增 v19.32 专项冒烟（/tmp/jrpg_smoke_v1932_winmsg.mjs）**33 项断言全过**——`WIN_MSG_MS=2600` 导出且定义/export 齐全、2 个模块（battle/core）import 且调用点恰好 5 处（battle ×4 / core ×1）均读该源、可执行裸「boxMsg(…, 2600)」清零（剩 0 处、data.js 仅注释里的文档化字面量）、行为逐值恒等（2600===WIN_MSG_MS）、时序家族既有成员（STRONG_MSG_MS 2000 / EVENT_MSG_MS 1600 / ALTAR_TXT_MS 1600 / ALTAR_LEAD_MS 600 / SYS_MSG_MS 2200 / MILESTONE_MS 2400 / SHORT_MSG_MS 1200 / NARR_MSG_MS 1800 / FINAL_LEAD_MS 700 / HIT_FB_MS 220 / UI_PULSE_MS 400 / DAY_PHASE_S 90 / BLOG_WIN 3）以及成就类 3200×3、2800、3600、4200、战间 1400×2、两处 setTimeout 1800 延时各自保留不受影响。

## v19.31 更强档「拦路/推进公告」停留时长单一数据源——三处强提示同读 STRONG_MSG_MS（纯时序·单一口径，与 v19.30 EVENT_MSG_MS / v19.29 FINAL_LEAD_MS / v19.28 NARR_MSG_MS / v19.27 SHORT_MSG_MS / v19.26 MILESTONE_MS / v19.25 SYS_MSG_MS / v19.24 ALTAR_TXT_MS / v19.23 ALTAR_LEAD_MS 同一「提示框停留时长」家族——本版收口的是历版注释反复标注「更强档 · 刻意保留」的 2000 档，也是该家族自 v19.23 起连续第九块拼图）

- 【更强档「2000ms」以裸字面量写三个文件三处、三处互不相关】比常规档更有存在感的阶段推进提示以完全相同的 `boxMsg(..., 2000)` 裸写三个文件：`core.js` 任务蘑菇未上交拦路（行 89）、`battle.js` 试炼开始（行 18）、`main.js` 踏上旅途（行 193）——三处同值互不引用：想调这类提示读多久（如放慢到 2200 让试炼/启程更有仪式感、收紧到 1800 节奏更利落）要改三个地方、还极易只改一处漏改另两处——拦路/试炼/启程的停留时长悄然脱钩，而这个参数始终没有名字（v19.25 收口 SYS_MSG_MS、v19.27 收口 SHORT_MSG_MS、v19.28 收口 NARR_MSG_MS、v19.30 收口 EVENT_MSG_MS 时都已在注释里把 2000 明确标注「更强档 · 刻意保留」，正是它们共同的下一块拼图）。
- 【修正：data.js 在 `EVENT_MSG_MS` 之后新增 `STRONG_MSG_MS = 2000`（更强档「拦路/推进公告」停留时长，单位 ms，纯时序）为唯一真源，注释说明三处派生形态与刻意保留的其余停留时级（胜利类 2600 五处 / 成就类 3200 三处 / 更长档 2800 记忆碎片 / 教程 3600 / 图鉴 4200 各为独立语义；战间 1400 家族与两处 `setTimeout(..., 1800)`「延时/过渡」另一语义刻意不并入），随原 export 块导出，零新增依赖】`core.js` / `battle.js` / `main.js` 三处 `boxMsg(..., 2000)` 改读 `boxMsg(..., STRONG_MSG_MS)`（import 列表同步加 STRONG_MSG_MS）。收益：调更强提示停留节奏只改 data.js 一处、三个模块三处同步，绝无第二套口径；行为逐字不变（`STRONG_MSG_MS` 仍为 2000，三处停留时长与旧字面量逐值恒等），零战斗/世界/界面流程回归。
- 【零回归面】未动提示机制本身——boxMsg 内容、三处业务判定（酿造拦路判定、startRush 试炼时机、进入世界后的 tutDone 分支）全部原样；其余 boxMsg 停留时级逐字未动：短档 1200（SHORT_MSG_MS）、中长档 1800（NARR_MSG_MS 八处）、常规档 2200（SYS_MSG_MS）、里程碑档 2400（MILESTONE_MS）、小事件档 1600（EVENT_MSG_MS 六处）、强敌文案 1600（ALTAR_TXT_MS）、胜利类 2600（胜利/额外掉落/圣光之剑/洞窟领主倒下/任务完成共五处）、成就类 3200（试炼通关/真结局/成就解锁共三处）、更长档 2800（记忆碎片）、教程 3600、图鉴 4200 与战间 1400 家族、两处 `setTimeout(..., 1800)` 延时（core 存档回写 / battle 通关回世界）全部刻意保留，并逐一由本版专项冒烟钉死。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只新增一个常量 + 改 3 处表达式 + 3 处 import + 1 处 export + 注释，零新增依赖。
- 验证：`node --check js/data.js js/core.js js/main.js js/battle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.31 专项冒烟（/tmp/jrpg_smoke_v1931_strongmsg.mjs）**33 项断言全过**——`STRONG_MSG_MS=2000` 导出且定义/export 齐全、3 个模块（core/battle/main）import 且调用点恰好 3 处（core ×1 / battle ×1 / main ×1）均读该源、可执行裸「boxMsg(…, 2000)」清零（剩 0 处、data.js 仅注释里的文档化字面量）、行为逐值恒等（2000===STRONG_MSG_MS）、时序家族既有成员（ALTAR_TXT_MS 1600 / ALTAR_LEAD_MS 600 / SYS_MSG_MS 2200 / MILESTONE_MS 2400 / SHORT_MSG_MS 1200 / NARR_MSG_MS 1800 / FINAL_LEAD_MS 700 / EVENT_MSG_MS 1600 / HIT_FB_MS 220 / UI_PULSE_MS 400 / DAY_PHASE_S 90 / BLOG_WIN 3）以及胜利类 2600×5、成就类 3200×3、2800、3600、4200、战间 1400×2、两处 setTimeout 1800 延时各自保留不受影响。

## v19.30 小事件通知「小事件档」停留时长单一数据源——六处小事件回馈同读 EVENT_MSG_MS（纯时序·单一口径，与 v19.29 FINAL_LEAD_MS / v19.28 NARR_MSG_MS / v19.27 SHORT_MSG_MS / v19.26 MILESTONE_MS / v19.25 SYS_MSG_MS / v19.24 ALTAR_TXT_MS / v19.23 ALTAR_LEAD_MS 同一「提示框停留时长」家族——本版收口的正是自 v19.24 起历版注释反复标注「喷泉 1600 是『小事件通知』另一语义、值同为 1600 却互不相干、刻意不并入」的那一批值，也是该家族自 v19.23 起连续第八块拼图，与 v19.29 收口「700 家族」同为历版注释留下的「最后一块拼图」式留白）

- 【小事件通知「1600ms」以裸字面量写三个文件六处、六处互不相关】短促的「小事件回馈」提示框停留时长以完全相同的 `bind.boxMsg(..., 1600)` 裸写三个文件六处：`core.js` 酿造失败（行 94）、旅行未开（行 109）、新的冒险开始（行 274）、重整旗鼓（行 302）、`world.js` 喷泉恢复（行 238）、`main.js` 读取存档（行 154）——六处同为「小事件通知」语义、同值互不引用：想调这类回馈读多久（如放慢到 1800 让回馈多停一拍、收紧到 1400 更利落）要改六个地方、还极易只改一处漏改另几处——酿造/旅行/重开/读档/喷泉的短促反馈悄然脱钩，而这个「小事件通知停多久」的时序参数本身始终没有名字（v19.24 收口 ALTAR_TXT_MS=1600 时已把喷泉处明确标注「小事件通知另一语义的独立停留时长」，v19.25–28 的 SYS/MILESTONE/SHORT/NARR 注释里又把「1600 小事件档（喷泉/酿造失败/旅行未开/新冒险/重整旗鼓/读档）」逐类点名为「另为独立语义 · 刻意保留」，正是它们共同的下一块拼图）。
- 【修正：data.js 在 `FINAL_LEAD_MS` 之后新增 `EVENT_MSG_MS = 1600`（小事件通知「小事件档」停留时长，单位 ms，纯时序）为唯一真源，注释说明六处派生形态与刻意保留的其余停留时级（ALTAR_TXT_MS 同为 1600 却是「强敌祭坛出场文案」另一语义、与 FINAL_LEAD_MS/ALTAR_LEAD_MS「同族不同值」同款另立常量不混同；短档 1200 / 中长档 1800 / 常规档 2200 / 里程碑档 2400 各为独立语义），随原 export 块导出，零新增依赖】`core.js` 四处 / `world.js` 一处 / `main.js` 一处 `boxMsg(..., 1600)` 改读 `boxMsg(..., EVENT_MSG_MS)`（import 列表同步加 EVENT_MSG_MS；ALTAR_TXT_MS 注释「喷泉 1600 ... 刻意不并入」与 SYS/MILESTONE/SHORT/NARR 注释里「1600 小事件档 · 另为独立语义」同步改写为指向新源）。收益：调小事件通知停留节奏只改 data.js 一处、三个模块六处同步，绝无第二套口径；行为逐字不变（`EVENT_MSG_MS` 仍为 1600，六处停留时长与旧字面量逐值恒等），零战斗/世界/界面流程回归。
- 【零回归面】未动提示机制本身——boxMsg 内容、六处业务判定（酿造扣料失败、旅行未开拦截、重开/重整旗鼓时机、喷泉回复判定、读档时机）全部原样；其余 boxMsg 停留时级逐字未动：短档 1200（SHORT_MSG_MS）、中长档 1800（NARR_MSG_MS 八处）、常规档 2200（SYS_MSG_MS 五处）、里程碑档 2400（MILESTONE_MS 三处）、更强档 2000（任务蘑菇未上交/试炼开始/踏上旅途三处，另一语义）、更长档 2800（记忆碎片）、成就 3200、胜利 2600、教程 3600、图鉴 4200 与 ALTAR_TXT_MS 强敌 1600 全部刻意保留；两处 `setTimeout(..., 1800)` 延时（core 存档回写 / battle 通关回世界）原样。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只新增一个常量 + 改 6 处表达式 + 3 处 import + 1 处 export + 注释微调，零新增依赖。
- 验证：`node --check js/data.js js/core.js js/world.js js/main.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.30 专项冒烟（/tmp/jrpg_smoke_v1930_eventmsg.mjs）**35 项断言全过**——`EVENT_MSG_MS=1600` 导出且定义/export 齐全、3 个模块（core/world/main）import 且调用点恰好 6 处（core ×4 / world ×1 / main ×1）均读该源、可执行裸「boxMsg(…, 1600)」清零（剩 0 处、data.js 仅注释里的文档化字面量）、行为逐值恒等（1600===EVENT_MSG_MS）、时序家族既有成员（ALTAR_TXT_MS 1600 / ALTAR_LEAD_MS 600 / SYS_MSG_MS 2200 / MILESTONE_MS 2400 / SHORT_MSG_MS 1200 / NARR_MSG_MS 1800 / FINAL_LEAD_MS 700 / HIT_FB_MS 220 / UI_PULSE_MS 400 / DAY_PHASE_S 90 / BLOG_WIN 3）以及 2000 档/1400/两处 setTimeout 1800 延时各自保留不受影响。

## v19.29 终局/传送类「切入下一阶段」延时单一数据源——三处门关切入同读 FINAL_LEAD_MS（纯时序·单一口径，与 v19.23 ALTAR_LEAD_MS 同为「切入延时」家族但语义不同——上一版收口的是「强敌祭坛」600 战前切入，本版收口的是其注释中反复标注「值同为 7 开头却互不相干，刻意不并入」的「真结局/传送一类」700——也是该「时序数据化」体系自 v19.8 起的又一块拼图，块块都有名字）

- 【门关切入延时「700ms」以裸字面量写两文件三处、三处互不相关】三处「关键门关切入前」的停顿以完全相同的 `setTimeout(..., 700)` 裸写两个文件：`world.js` 终焉之神战前（`onTrueCrystal` 行 268，`startBattle(deep(TRUE_BOSS))`）、`world.js` 回廊传送（`onTrueCrystal` 行 279，`transition('gallery')`）、`battle.js` 试炼首战（`startRush` 行 21，`startBattle(deep(RUSH_BOSSES[0]))`）——三处同值互不引用：想调这类门关切入节奏（如放慢到 900 让终局悬念多停一拍、收紧到 500 更快开门）要改三个地方、还极易只改一处漏改另外两处——终焉之战/回廊传送/试炼首战的切入停顿悄然脱钩，而这个「切入下一阶段停多久」的时序参数本身始终没有名字（v19.23 收口 ALTAR_LEAD_MS=600 时已在同一注释把该 700 明确标注「真结局/传送一类另一语义 · 刻意不并入」，正是它的下一块拼图）。
- 【修正：data.js 在 `NARR_MSG_MS` 之后新增 `FINAL_LEAD_MS = 700`（终局/传送类「切入下一阶段」的演出延时，单位 ms，纯时序）为唯一真源，注释说明三处派生形态与 ALTAR_LEAD_MS 600 的「同族不同值」关系（属性同属「切入延时」语义却不同：600 是强敌祭坛战前、700 是真结局/传送/试炼门关），随原 export 块导出，零新增依赖】`world.js` 两处与 `battle.js` 一处 `setTimeout(..., 700)` 改读 `setTimeout(..., FINAL_LEAD_MS)`（import 列表同步加 FINAL_LEAD_MS；`ALTAR_LEAD_MS` 注释里「700 家族刻意不并入」及 `SYS_MSG_MS` / `MILESTONE_MS` / `SHORT_MSG_MS` / `NARR_MSG_MS` 注释里「700 家族（真结局切入/回廊传送/试炼首战）逐类保留不动」同步改写为指向新源）。收益：调终局/传送门关切入节奏只改 data.js 一处、两个模块三处同步，绝无第二套口径；行为逐字不变（`FINAL_LEAD_MS` 仍为 700，三处延时与旧字面量逐值恒等），零战斗/世界/终局流程回归。
- 【零回归面】未动切入机制本身——三处判定（终焉水晶 gallery 分支 trueBoss 判定、双徽记开门判定、试炼碑 startRush 时机）与 `S.scene === 'world'` 防重叠守卫全部原样；ALTAR_LEAD_MS=600 三处强敌祭坛切入、ALTAR_TXT_MS=1600 强敌出场文案、喷泉 1600、战间 1400、两处 setTimeout 1800 延时（core 存档回写 / battle 通关回世界）与 boxMsg 各停留时级全部逐字未动。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只新增一个常量 + 改 3 处表达式 + 2 处 import + 1 处 export + 五处注释指向，零新增依赖。
- 验证：`node --check js/data.js js/world.js js/battle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.29 专项冒烟（/tmp/jrpg_smoke_v1929_finallead.mjs）**27 项断言全过**——`FINAL_LEAD_MS=700` 导出且定义/export 齐全、2 个模块（world/battle）import 且调用点恰好 3 处（world ×2：TRUE_BOSS 战前 + gallery 传送 / battle ×1：RUSH_BOSSES[0] 试炼首战）均读该源、可执行裸「setTimeout(…, 700)」清零（二十个模块剩 0 处）、行为逐值恒等（700===FINAL_LEAD_MS）、ALTAR_LEAD_MS=600 三处祭坛调用点身体在线、时序家族既有成员（ALTAR_TXT_MS 1600 / SYS_MSG_MS 2200 / MILESTONE_MS 2400 / SHORT_MSG_MS 1200 / NARR_MSG_MS 1800 / HIT_FB_MS 220 / UI_PULSE_MS 400 / DAY_PHASE_S 90 / BLOG_WIN 3）以及战间 1400、×2 处 setTimeout 1800 延时各自保留不受影响。

## v19.28 叙事/任务反馈「中长档」停留时长单一数据源——八处中长提示同读 NARR_MSG_MS（纯时序·单一口径，与 v19.27 SHORT_MSG_MS / v19.26 MILESTONE_MS / v19.25 SYS_MSG_MS / v19.24 ALTAR_TXT_MS / v19.23 ALTAR_LEAD_MS 同一「提示框停留时长」家族——本版收口的是 boxMsg 提示框家族里分布第二广的 1800 中长档，也是该家族自 v19.23 起连续第七块拼图，与 v19.24–27 注释中反复标注「中长档 1800 家族 · 刻意保留」的正是同一批值）

- 【叙事/任务反馈「1800ms」以裸字面量写八处、八处互不相关】中长叙事/任务反馈提示框停留时长以完全相同的 `bind.boxMsg(..., 1800)` 裸写四个文件八处：`world.js` 终焉之神水晶文案家族五处（`onTrueCrystal` 行 267/273/277/281 + 试炼碑 `onTrialStele` 行 287）、`core.js` 接受任务（行 144）、`battle.js` 拾取魔法蘑菇（行 382）、`shop.js` 背包已满（行 25）——八处同值互不引用：想调这类中长提示读多久（如放慢到 2000 让剧情台词多停一秒、收紧到 1600 节奏更利落）要改八个地方、还极易只改一处漏改另一处——剧情/任务/拾取/背包的停留时长悄然脱钩，而这个「中长提示停多久」的时序参数本身始终没有名字（v19.25 收口 SYS_MSG_MS、v19.26 收口 MILESTONE_MS、v19.27 收口 SHORT_MSG_MS 时都已在注释里把 1800 明确标注「中长档 · 刻意保留未并入」，正是它们共同的下一块拼图）。
- 【修正：data.js 在 `SHORT_MSG_MS` 之后新增 `NARR_MSG_MS = 1800`（叙事/任务反馈「中长档」停留时长，单位 ms，纯时序）为唯一真源，注释说明八处派生形态与刻意保留的其余停留时级（更长档 2800 记忆碎片 / 战间 1400 / 700 家族 / 1600 小事件档与 ALTAR_TXT_MS 强敌档 / 成就 3200 / 胜利 2600 / 教程 3600 各为独立语义；尤其值同为 18 开头却互不相干的 core.js 存档回写 / battle.js 试炼通关回世界两处 `setTimeout(..., 1800)`「延时/过渡」另一语义刻意不并入），随原 export 块导出，零新增依赖】`core.js` / `world.js` 五处 / `battle.js` / `shop.js` 八处 `boxMsg(..., 1800)` 改读 `boxMsg(..., NARR_MSG_MS)`（import 列表同步加 NARR_MSG_MS；`SYS_MSG_MS` / `MILESTONE_MS` / `SHORT_MSG_MS` 注释里「中长档 1800 逐类保留不动」同步改写为指向新源）。收益：调中长提示停留节奏只改 data.js 一处、四个模块八处同步，绝无第二套口径；行为逐字不变（`NARR_MSG_MS` 仍为 1800，八处停留时长与旧字面量逐值恒等），零战斗/世界/界面流程回归。
- 【零回归面】未动提示机制本身——boxMsg 内容、八处业务判定（终焉水晶开/关/已通三态、试炼碑双徽记判定、接受任务时机、精英魔像蘑菇拾取、背包满拦截）全部原样；其余 boxMsg 停留时级逐字未动：短档 1200（SHORT_MSG_MS）、常规档 2200（SYS_MSG_MS）、里程碑档 2400（MILESTONE_MS）、更长档 2800（记忆碎片，仅一处）、战间 1400、700 家族、1600 小事件档（喷泉/酿造失败/旅行未开/新冒险/重整旗鼓/读档）与 ALTAR_TXT_MS 强敌 1600、成就 3200、胜利 2600、教程 3600 全部刻意保留；两处 `setTimeout(..., 1800)` 延时（core 存档回写 saveMsg 清除 / battle 试炼通关回世界过渡）原样。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只新增一个常量 + 改 8 处表达式 + 4 处 import + 1 处 export + 注释微调，零新增依赖。
- 验证：`node --check js/data.js js/core.js js/world.js js/battle.js js/shop.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.28 专项冒烟（/tmp/jrpg_smoke_v1928_narrmsg.mjs）**33 项断言全过**——`NARR_MSG_MS=1800` 导出且定义/export 齐全、4 个模块（core/world/battle/shop）import 且调用点恰好 8 处（core ×1 / world ×5 / battle ×1 / shop ×1）均读该源、可执行裸「boxMsg(…, 1800)」清零（剩 0 处、data.js 仅注释里的文档化字面量）、两处 setTimeout 1800 延时（core 存档回写 / battle 通关回世界）另一语义保留、行为逐值恒等（1800===NARR_MSG_MS）、时序家族既有成员（SHORT_MSG_MS 1200 / SYS_MSG_MS 2200 / MILESTONE_MS 2400 / ALTAR_TXT_MS 1600 / ALTAR_LEAD_MS 600 / HIT_FB_MS 220 / UI_PULSE_MS 400 / DAY_PHASE_S 90 / BLOG_WIN 3）以及停留时级 2000/2800/2600/3200/3600/700/1600 各自保留不受影响。

## v19.27 环境短提示「短档」停留时长单一数据源——三处短促回馈同读 SHORT_MSG_MS（纯时序·单一口径，与 v19.26 MILESTONE_MS / v19.25 SYS_MSG_MS / v19.24 ALTAR_TXT_MS / v19.23 ALTAR_LEAD_MS 同一「提示框停留时长」家族——本版收口的是 boxMsg 提示框家族里最「短」的 1200 短档，也是该家族自 v19.23 起连续第六块拼图）

- 【环境短提示「1200ms」以裸字面量写三处、三处互不相关】短促环境回馈提示框停留时长以完全相同的 `bind.boxMsg(..., 1200)` 裸写两个文件三处：`world.js` 进入地图（`transition`，行 350）、踏无发现（`interact` 兜底，行 337）、`main.js` 静音开关（M 键，行 280）——三处同值互不引用：想调这类瞬时提示读多久（如放慢到 1500 让回馈更舒适、收紧到 900 节奏更利落）要改三个地方、还极易只改一处漏改另一处——进入地图/踏无发现/静音反馈的停留时长悄然脱钩，而这个「短提示停多久」的时序参数本身始终没有名字（v19.25 收口 SYS_MSG_MS、v19.26 收口 MILESTONE_MS 时都已在注释里把该 1200 明确标注「短档 · 刻意保留未并入」，正是它们共同的下一块拼图）。
- 【修正：data.js 在 `MILESTONE_MS` 之后新增 `SHORT_MSG_MS = 1200`（环境短提示「短档」停留时长，单位 ms，纯时序）为唯一真源，注释说明三处派生形态与刻意保留的其余停留时级（中长档 1800 家族 / 更长档 2800 / 战间 1400 / 700 家族 / 喷泉 1600 / 成就 3200 / 胜利 2600 / 教程 3600 各为独立语义），随原 export 块导出，零新增依赖】`world.js` 两处与 `main.js` 一处 `boxMsg(..., 1200)` 改读 `boxMsg(..., SHORT_MSG_MS)`（import 列表同步加 SHORT_MSG_MS；`SYS_MSG_MS` / `MILESTONE_MS` 注释里「短档 1200 逐类保留不动」同步改写为指向新源）。收益：调短提示停留节奏只改 data.js 一处、两个模块三处同步，绝无第二套口径；行为逐字不变（`SHORT_MSG_MS` 仍为 1200，三处停留时长与旧字面量逐值恒等），零战斗/世界/界面流程回归。
- 【零回归面】未动提示机制本身——boxMsg 内容、三处业务判定（进入地图时机、interact 兜底路径、静音开关判定）全部原样；其余 boxMsg 停留时级逐字未动：中长档 1800（终焉之神水晶文案家族四处 + 拾取蘑菇/接受任务/存档回写/背包满等）、更长档 2800（记忆碎片，仅一处）、战间 1400（试炼切入/战斗结束回世界）、700 家族（终焉之神战前/回廊传送/试炼首战）、成就/试炼 3200、胜利 2600、教程 3600 与喷泉 1600 全部刻意保留。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只新增一个常量 + 改 3 处表达式 + 2 处 import + 1 处 export + 注释微调，零新增依赖。
- 验证：`node --check` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.27 专项冒烟（/tmp/jrpg_smoke_v1927_shortmsg.mjs）**25 项断言全过**——`SHORT_MSG_MS=1200` 导出且定义/export 齐全、2 个模块（world/main）import 且调用点恰好 3 处（world ×2 / main ×1）均读该源、可执行裸「boxMsg(…, 1200)」清零（剩 0 处、data.js 仅注释里的文档化字面量）、行为逐值恒等（1200===SHORT_MSG_MS）、时序家族既有成员（SYS_MSG_MS 2200 / MILESTONE_MS 2400 / ALTAR_TXT_MS 1600 / ALTAR_LEAD_MS 600 / HIT_FB_MS 220 / UI_PULSE_MS 400 / DAY_PHASE_S 90 / BLOG_WIN 3）以及停留时级 1800/2800/1400/700/2600/3200/3600 各自保留不受影响。

## v19.26 里程碑/阶段达成公告「停留时长」单一数据源——三处里程碑公告同读 MILESTONE_MS（纯时序·单一口径，与 v19.25 SYS_MSG_MS / v19.24 ALTAR_TXT_MS / v19.23 ALTAR_LEAD_MS 同一「提示框停留时长」家族——SYS_MSG_MS 收口了 2200 常规档，本版收口的是紧邻其后的 2400 长档「里程碑档」）

- 【阶段达成公告「2400ms」在三个文件以裸字面量写三处、三处互不相关】三处「阶段性里程碑达成」的祝贺式公告以完全相同的 `bind.boxMsg(..., 2400)` 裸写三个文件：`core.js` 困难模式开启（行 171）、`world.js` 蘑菇集齐（行 217）、`battle.js` 等级提升（行 387）——三处同为「里程碑/阶段达成」语义的庆祝提示、同值互不引用：想调这类公告读多久（如放慢到 2600 让升级更有仪式感、收紧到 2200 节奏更利落）要改三个地方、还极易只改升级漏改集齐——三类里程碑公告的停留时长悄然脱钩，而这个「里程碑公告停多久」的时序参数本身始终没有名字（v19.25 收口 SYS_MSG_MS 时已把该 2400 明确标注「长档 · 刻意保留未并入」，正是它的下一块拼图）。
- 【修正：data.js 在 `SYS_MSG_MS` 之后新增 `MILESTONE_MS = 2400`（里程碑/阶段达成公告停留时长，单位 ms，纯时序）为唯一真源，注释说明三处派生形态与刻意保留的其余停留时级（更长档 2800 记忆碎片 / 中长档 1800 家族 / 短档 1200 / 战间 1400 / 700 家族 / 喷泉 1600 各为独立语义），随原 export 块导出，零新增依赖】`core.js` / `world.js` / `battle.js` 三处 `bind.boxMsg(..., 2400)` 改读 `bind.boxMsg(..., MILESTONE_MS)`（import 列表同步加 MILESTONE_MS；`SYS_MSG_MS` 注释里「长档 2400 逐类保留不动」同步改写为指向新源）。收益：调里程碑公告停留节奏只改 data.js 一处、三个模块同步，绝无第二套口径；行为逐字不变（`MILESTONE_MS` 仍为 2400，三处停留时长与旧字面量逐值恒等），零战斗/世界/界面流程回归。
- 【零回归面】未动公告机制本身——boxMsg 内容、三处业务判定（困难模式开启时机、蘑菇集齐转 turnin、等级提升判定）全部原样；其余 boxMsg 停留时级逐字未动：更长档 2800（记忆碎片，仅一处）、中长档 1800（终焉之神水晶文案家族四处 + 拾取蘑菇/接受任务/存档回写/背包满/战斗结束回世界等）、短档 1200（进入地图/踏无发现/静音开关）、战间 1400（试炼切入/战斗结束回世界）、700 家族（终焉之神战前/回廊传送/试炼首战）与喷泉 1600 全部刻意保留。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只新增一个常量 + 改 3 处表达式 + 3 处 import + 1 处 export + 注释微调，零新增依赖。
- 验证：`node --check js/data.js js/core.js js/world.js js/battle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.26 专项冒烟（/tmp/jrpg_smoke_v1926_milestone.mjs）**23 项断言全过**——`MILESTONE_MS=2400` 导出且定义/export 齐全、3 个模块（core/world/battle）import 且恰好各 1 处调用点均读该源、可执行裸「boxMsg(…, 2400)」清零（剩 0 处、data.js 仅注释里的文档化字面量）、行为逐值恒等（2400===MILESTONE_MS）、时序家族既有成员（SYS_MSG_MS 2200 / ALTAR_TXT_MS 1600 / ALTAR_LEAD_MS 600 / HIT_FB_MS 220 / UI_PULSE_MS 400 / DAY_PHASE_S 90 / BLOG_WIN 3）以及停留时级 2800/1800/1200/1400 各自保留不受影响。

## v19.25 系统提示框「常规」停留时长单一数据源——五处常规提示同读 SYS_MSG_MS（纯时序·单一口径，与 v19.24 ALTAR_TXT_MS / v19.23 ALTAR_LEAD_MS / v19.8 HIT_FB_MS / v19.15 UI_PULSE_MS / v19.21 DAY_PHASE_S 同一「时序数据化」体系——本版收口的是 boxMsg 提示框家族里分布最广的「常规档」，也是全库单值重复次数仅次于 1800 的第二大裸奔值）

- 【常规系统提示「2200ms」在四个文件以裸字面量写五处、五处互不相关】普通系统提示框停留时长以完全相同的 `bind.boxMsg(..., 2200)` 裸写四个文件：`battle.js` 试炼第 N 关登场（行 432）、`core.js` 酿造成功（行 102）、`hero.js` 领悟新技能（行 46）、`shop.js` 任务蘑菇保护·不可卖（行 47）、`world.js` 传送门锁定提示 `lockedMsg`（行 176）——五处同值互不引用：想调常规提示读多久（如放慢到 2500 让台词多停一秒、收紧到 2000 节奏更利落）要改五个地方、还极易只改一处漏改另外几处——里程碑/成功/锁定五类提示的停留时长悄然脱钩，而这个「常规提示框停多久」的时序参数本身始终没有名字。
- 【修正：data.js 在 `ALTAR_TXT_MS` 之后新增 `SYS_MSG_MS = 2200`（系统提示框「常规」停留时长，单位 ms，纯时序）为唯一真源，注释说明五处派生形态与刻意保留的其余停留时级（短档 1200 / 中长档 1800 / 长档 2400 / 更长档 2800 / 700 家族 / 喷泉 1600 各为独立语义），随原 export 块导出，零新增依赖】`battle.js` / `core.js` / `hero.js` / `shop.js` / `world.js` 五处 `bind.boxMsg(..., 2200)` 改读 `bind.boxMsg(..., SYS_MSG_MS)`（import 列表同步加 SYS_MSG_MS）。收益：调常规提示停留节奏只改 data.js 一处、五个模块同步，绝无第二套口径；行为逐字不变（`SYS_MSG_MS` 仍为 2200，五处停留时长与旧字面量逐值恒等），零战斗/世界/界面流程回归。
- 【零回归面】未动提示机制本身——boxMsg 内容、五处业务判定（试炼换关回血、酿造扣料、技能领悟时机、蘑菇保护判定、传送门锁判定）全部原样；其余 boxMsg 停留时级逐字未动：短档 1200（进入地图 / 踏无发现）、中长档 1800（终焉之神水晶文案家族四处 + 拾取蘑菇/接受任务/存档回写/背包满等）、长档 2400（等级提升）、更长档 2800（记忆碎片）、战间 1400（试炼切入/战斗结束回世界）、700 家族（终焉之神战前/回廊传送/试炼首战）与喷泉 1600 全部刻意保留。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只新增一个常量 + 改 5 处表达式 + 5 处 import + 1 处 export + 注释，零新增依赖。
- 验证：`node --check js/data.js js/battle.js js/core.js js/hero.js js/shop.js js/world.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.25 专项冒烟（/tmp/jrpg_smoke_v1925_sysmsg.mjs）**17 项断言全过**——`SYS_MSG_MS=2200` 导出且定义/export 齐全、5 个模块（battle/core/hero/shop/world）import 且恰好各 1 处调用点均读该源、可执行裸「boxMsg(…, 2200)」清零（剩 0 处、data.js 仅注释里的文档化字面量）、行为逐值恒等（2200===SYS_MSG_MS）、时序家族既有成员（ALTAR_TXT_MS 1600 / ALTAR_LEAD_MS 600 / HIT_FB_MS 220 / UI_PULSE_MS 400 / DAY_PHASE_S 90 / BLOG_WIN 3）以及停留时级 1200/1800/2400/2800/700 各自保留不受影响。

## v19.24 强敌祭坛「出场提示框停留时长」单一数据源——三个强敌的入场文案同读 ALTAR_TXT_MS（纯时序·单一口径，v19.23 ALTAR_LEAD_MS 的伴生量：上一版收口了「踩祭坛到切入战斗」的延时，本版收口的是紧贴其前的「出场提示框停多久」——与 v19.8 HIT_FB_MS / v19.15 UI_PULSE_MS / v19.18 IDLE_BOB / v19.21 DAY_PHASE_S 同一「时序数据化」体系，且正是 v19.23 中刻意标注「未动」的 1600）

- 【强敌出场文案「1600ms」在 world.js 以裸字面量写三处、三处互不相关】强敌祭坛三处「先弹出场提示框、再延时切入战斗」的提示框停留时长以完全相同的 `bind.boxMsg(..., 1600)` 裸写 world.js 三处：`onBossAltar` 的幽冥魔王（雾语林祭坛，行 245）、`onCaveAltar` 的残焰魔像（无字回廊祭坛，行 253）与洞窟领主（星井矿脉祭坛，行 258）——三处同值互不引用：想调强敌出场文案停多久（如放慢到 2000 让台词多读一秒、收紧到 1200 更快入战）要改三个地方、还极易只改一个祭坛漏改另外两处——三个强敌的出场提示框时长悄然脱钩，而这个「提示框停多久」的时序参数本身始终没有名字（v19.23 收口 ALTAR_LEAD_MS=600 时已把该 1600 明确标注「刻意保留未并入」，正是它的下一块拼图）。注：喷泉「⛲ HP/MP 完全恢复」的 1600（onFountainStep，行 238）是「小事件通知」另一语义的独立停留时长，值同为 1600 却互不相干，与 ALTAR_LEAD_MS 注释里「700 家族」同款刻意保留。
- 【修正：data.js 在 `ALTAR_LEAD_MS` 之后新增 `ALTAR_TXT_MS = 1600`（强敌祭坛出场提示框文案停留时长，单位 ms，纯时序）为唯一真源，注释说明三处派生形态与喷泉 1600 另一语义的保留，随原 export 块导出，零新增依赖】`world.js` 三处祭坛出场提示的 `bind.boxMsg(..., 1600)` 改读 `bind.boxMsg(..., ALTAR_TXT_MS)`（import 列表同步加 ALTAR_TXT_MS）。收益：调强敌出场文案停留节奏只改 data.js 一处、三个祭坛同步，绝无第二套口径；行为逐字不变（`ALTAR_TXT_MS` 仍为 1600，三处停留时长与旧字面量逐值恒等），零战斗/世界流程回归。
- 【零回归面】未动强敌触发机制本身——`onBossAltar`/`onCaveAltar` 的文案内容、入场判定、`ALTAR_LEAD_MS` 切入延时、Boss 登场前后 flag 判定（bossDefeated/caveBoss/gallery 分支）全部原样；喷泉回复的 1600、终焉之神系 1800 文案家族（onTrueCrystal 四处 + onTrialStele 一处，行 267-287）与 ALTAR_LEAD_MS 注释中「700 家族」全部逐字未动；`WALK_MS=180` 等世界节奏常量原样。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只新增一个常量 + 改 3 处表达式 + 1 处 import + 1 处 export + 注释，零新增依赖。
- 验证：`node --check js/data.js js/world.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.24 专项冒烟（/tmp/jrpg_smoke_v1924_altartxt.mjs）**18 项断言全过**——`ALTAR_TXT_MS=1600` 导出且定义/export 齐全、world.js import 与 3 处调用点（幽冥魔王/残焰魔像/洞窟领主）均读该源、裸「boxMsg(…, 1600)」清零（剩 1 处恰为喷泉另一语义）、喷泉 1600 文案逐字保留、行为逐值恒等（1600===ALTAR_TXT_MS）、时序家族既有成员（ALTAR_LEAD_MS 600 / HIT_FB_MS 220 / UI_PULSE_MS 400 / IDLE_BOB 500·0.13 / DAY_PHASE_S 90 / BLOG_WIN 3 / BATTLE_MON 248 / BATTLE_HERO 96·400）不受影响。

## v19.23 强敌祭坛「战前切入战斗延时」单一数据源——三个强敌的对峙停顿同读 ALTAR_LEAD_MS（纯时序·单一口径，与 v19.8 HIT_FB_MS / v19.15 UI_PULSE_MS / v19.18 IDLE_BOB / v19.21 DAY_PHASE_S 同一「时序数据化」体系——本版收口的是世界交互「踩祭坛到切入战斗」的演出节奏，与 v19.22 BLOG_WIN 同为「一处语义三处复制」的收口形态）

- 【强敌战前停顿「600ms」在 world.js 以裸字面量写三处、三处互不相关】强敌祭坛「先弹出场提示框、再延时切入战斗」的对峙停顿以完全相同的 `setTimeout(..., 600)` 裸写 world.js 三处：`onBossAltar` 的幽冥魔王（雾语林祭坛，行 246）、`onCaveAltar` 的残焰魔像（无字回廊祭坛，行 254）与洞窟领主（星井矿脉祭坛，行 259）——三处同值互不引用：想调强敌战前演出节奏（如放慢到 800 让出场台词多停一拍、收紧到 400 更快入战）要改三个地方、还极易只改一个祭坛漏改另外两处——三个强敌的对峙停顿悄然脱钩，而这个「战前停顿多久」的时序参数本身始终没有名字（同一文件里 BOSS 祭坛/回廊祭坛/矿脉祭坛三处恰好都凑巧写 600，无任何一处能说明这是设计值还是随手写的数）。
- 【修正：data.js 在 `BATTLE_HERO` 之后新增 `ALTAR_LEAD_MS = 600`（强敌祭坛战前演出到切入战斗的延时，单位 ms，纯时序）为唯一真源，注释说明三处派生形态与刻意保留的 700 家族（终焉之神战前 700 / 回廊传送 700 / 试炼首战 700 是「真结局/传送一类」另一语义、值同为 7 开头却互不相干），随原 export 块导出，零新增依赖】`world.js` 三处祭坛切入延时的 `setTimeout(..., 600)` 改读 `setTimeout(..., ALTAR_LEAD_MS)`（import 列表同步加 ALTAR_LEAD_MS）。收益：调强敌战前演出节奏只改 data.js 一处、三个祭坛同步，绝无第二套口径；行为逐字不变（`ALTAR_LEAD_MS` 仍为 600，三处延时与旧字面量逐值恒等），零战斗/世界流程回归。
- 【零回归面】未动强敌触发机制本身——`onBossAltar`/`onCaveAltar` 的出场提示框文案与 1600 时长、`S.scene === 'world'` 防重叠守卫、`deep(...)` 生成、Boss 登场前后 flag 判定（bossDefeated/caveBoss/gallery 分支）全部原样；终焉之神战前 700（`onTrueCrystal`）、回廊传送 700（`transition('gallery')`）、battle.startRush 试炼首战 700 等「另一语义」的延时逐字未动；`WALK_MS=180` 等世界节奏常量原样。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只新增一个常量 + 改 3 处表达式 + 1 处 import + 1 处 export + 注释，零新增依赖。
- 验证：`node --check js/data.js js/world.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.23 专项冒烟（/tmp/jrpg_smoke_v1923_altarlead.mjs）**17 项断言全过**——`ALTAR_LEAD_MS=600` 导出且与字面量一致、data.js 定义/export 齐全、world.js import 与 3 处调用点（BOSS/EMBER_GOLEM/CAVE_BOSS）均读该源、裸「startBattle(...}, 600)」清零（剩 0 处）、终焉之神 700 / 回廊传送 700 另一语义保留、三处延时与旧字面量逐值恒等、时序家族既有成员（HIT_FB_MS 220 / UI_PULSE_MS 400 / IDLE_BOB 500·0.13 / DAY_PHASE_S 90 / BLOG_WIN 3 / BATTLE_MON 248 / BATTLE_HERO 96·400）不受影响。

## v19.22 战斗战报窗口「每屏行数」单一数据源——可见行数、回看偏移、溢出指示同读 BLOG_WIN（纯显示·单一口径，与 v19.8 HIT_FB_MS / v19.15 UI_PULSE_MS / v19.18 IDLE_BOB / v19.21 DAY_PHASE_S 同一「UI 显示参数数据化」体系——战斗画面这块收口的不是 ms 节奏，而是战报区「一屏摆几行」的布局阈值）

- 【战报窗口「3 行」在 view/drawBattle 以裸字面量 `3` 写三处的 `3` 处互不相关】战斗战报区「每屏显示 3 行」以裸字面量 `3` 游离在 data.js 之外：`view/drawBattle.js` 战报区**回看偏移上限** `S.blog.length - 3`（行 311）、**切片窗口** `S.blog.length - 3 - S.blogView`（行 314）、**溢出指示** `S.blog.length > 3`（行 316）三处同值互不引用——想调战报窗口高度（如放宽到 4 行让更多信息常驻，或收紧让画面更空）要改三处、还极易只改窗口漏改溢出指示——窗口已显示 4 行「↑↓ 回看」却仍只在 >3 时才出现，溢出提示与窗口高度悄然脱钩；而窗口「摆几行」这一布局阈值本身始终没有名字，只能靠翻字面量识别「3 是设计值而非随手写的数」。
- 【修正：data.js 在 `DAY_PHASE_S` 之后新增 `BLOG_WIN = 3`（战报窗口每屏可见行数，纯显示）为唯一真源，注释说明三处派生形态与刻意保留的排版参数（行距 `18`、每行显字），随原 export 块导出，零新增依赖】`view/drawBattle.js` 战报区三处改读 `S.blog.length - BLOG_WIN` / `S.blog.length - BLOG_WIN - S.blogView` / `S.blog.length > BLOG_WIN`，import 列表同步加 BLOG_WIN。收益：调战报窗口高度只改 data.js 一处、回看偏移与溢出指示同步，绝无第二套口径；行为逐字不变（`BLOG_WIN` 仍为 3，三处表达式与旧字面量在战报长度/回看偏移全扫下逐值恒等），零战报/UI 回归。
- 【零回归面】未动战报机制本身——blogView 上下翻页（main.js 按键、state.js 初始 0）、blog 每行显字与行距 18、回看提示文案（「↑↓ 回看战斗记录」/「↓ 回到最新」）、提示字号颜色位置全部原样；`3` 只在这三处是「窗口可见行数」语义，其余（战报区坐标 470、行距 18）为独立排版参数刻意不动。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只新增一个常量 + 改 3 处表达式 + 1 处 import + 1 处 export + 注释微调，零新增依赖。
- 验证：`node --check js/data.js js/view/drawBattle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.22 专项冒烟（/tmp/jrpg_smoke_v1922_blogwin.mjs）**18 项断言全过**——`BLOG_WIN=3` 导出且与历史字面量一致、data.js 定义/export 齐全、drawBattle import 与窗口逻辑 3 处读该源（共 5 处含 import/注释）、裸「blog.length - 3」与「blog.length > 3」已清零（剩 0 处）、窗口行为 L∈[0,12]×view∈[-3,12] 全扫逐值恒等（长战报 3 行+溢出指示 / 短战报 2 行无提示 / 回看尽头 maxV=5 核对）、时序家族既有成员（HIT_FB_MS 220 / UI_PULSE_MS 400 / IDLE_BOB 500·0.13 / DAY_PHASE_S 90）不受影响。

## v19.21 世界昼夜相位时长单一数据源——昼夜判定与界面注释同读 DAY_PHASE_S（纯显示·单一口径，与 v19.8 HIT_FB_MS / v19.15 UI_PULSE_MS / v19.18 IDLE_BOB / v19.17 warnFlash 同一「动画时序数据化」体系——本家族此前收口的都是 ms 级 UI/反馈节奏，唯独「一轮昼夜 4×90=360 秒」的世界时钟节奏仍是裸奔数值）

- 【世界时钟「每档跑多快」没有名字的 `2` 处互不相关】昼夜四相位（day/dusk/night/dawn）各 90 秒一轮 360 秒的相位时长以裸字面量 `90` 游离在 data.js 之外：`view/drawWorld.js` 的 `timeOfDay()` 判定 `['day','dusk','night','dawn'][Math.floor(t/90)%4]`（可执行代码 1 处）+ `view/hud.js` 注释自写一句「90 秒一档的昼夜标签」（文字第 2 处）——想调昼夜节奏（如加快到 60 秒一档、放慢到 120）要改判定 + 记得同步注释，还极易只改判定漏改注释，让「这个世界的昼夜跑多快」始终没有名字；而 `S.G.time`（main.js 游戏时钟按真实秒累进）是纯世界时钟、零结算影响，正是该家族里最后一枚裸奔读数。
- 【修正：data.js 在 `IDLE_BOB` 之后新增 `DAY_PHASE_S = 90`（每档相位时长，单位：秒）为唯一真源，注释说明与时序家族的关系与结构（4 相位各 90 秒轮转）、随原 export 块导出，零新增依赖】`view/drawWorld.js` 的 `timeOfDay()` 判定改 `Math.floor(t / DAY_PHASE_S) % 4` 并补注释；`view/hud.js` 注释旧「显示 90 秒一档的昼夜标签」改写为指向该源。收益：调昼夜节奏只改 data.js 一处，判定与界面注释绝无第二套口径；行为逐字不变（`DAY_PHASE_S` 仍为 90，`timeOfDay()` 与旧字面量在 t∈[0,9999] 全扫下逐值恒等），零渲染/世界时钟回归。
- 【零回归面】未动昼夜机制本身——四相位数组、`S.G.time` 累进（main.js）、回廊恒暗特判、村庄夜晚灯火、drawTimeTint 各相位着色、`fmtTime` 冒险时长全部原样；drawWorld 其余独立动画参数（雨 /40、洞窟萤火 /80、回廊漂浮 /120、水面 /400）与 minimap 高度 `Math.min(90,…)` 另一语义刻意保留。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只新增一个常量 + 改 1 处判定 + 2 处注释 + 1 处 import + 1 处 export，零新增依赖。
- 验证：`node --check js/data.js js/view/drawWorld.js js/view/hud.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.21 专项冒烟（/tmp/jrpg_smoke_v1921_dayphase.mjs）**14 项断言全过**——`DAY_PHASE_S=90` 导出且定义/export 齐全、时序家族既有成员（HIT_FB_MS 220 / UI_PULSE_MS 400 / IDLE_BOB 500·0.13）不受影响、drawWorld 读取该源恰好 1 处、裸「Math.floor(t / 90)」清零（剩 0 处）、timeOfDay 与旧公式 t∈[0,9999] 全扫逐值恒等、相位边界 4×90=360 一轮（0 白天→90 黄昏→180 夜晚→270 黎明→360 白天）核对通过、独立动画参数与 minimap 90 另一语义保留、hud 注释已改写且 PERIOD 四标签未动。

## v19.20 药水/灵药恢复量公式单一数据源——结算与战斗预览共读 rules.potionRestore/elixirRestore（机制·单一口径，与 v19.5 threatWarn 同一「预览-结算同公式」家族——v19.5 收口了「我方攻击预览」后，「[3]药水/🧪灵药恢复多少」是战斗提示里最后一组结算量由视图层自算的裸公式）

- 【`2` 处同公式互不相关】药水/灵药「恢复量」的计算公式裸写两处：`hero.js takePotion`（**结算**——core.js usePotion 与 battle.doItem 两条消费路径都经它出账）里 `Math.round(hpMax×POTION_HP_PCT)+POTION_HP_FLAT`（普通药水）、`Math.round(hpMax×ELIXIR_HP_PCT)+ELIXIR_HP_FLAT` 与 `Math.round(mpMax×ELIXIR_MP_PCT)`（高级灵药）；`view/drawBattle.js` 战斗指令栏「[3]恢复：🍖+…HP 🧪+…HP/+…MP」**预览**把同一公式再写一遍——两处同值互不引用，想改恢复模型（如改成比例+等级加成、或把舍入从 round 改 floor）要改两个文件四行，还极易只改结算漏改预览，界面标「+N HP」实际只回 M HP 悄然对不上（与 v19.5 threatWarn 收口前同款黑盒，这次轮到恢复量）。
- 【修正：rules.js 新增 `potionRestore(hero)`（返回 `{h}`）与 `elixirRestore(hero)`（返回 `{h,m}`）两个纯函数为唯一公式源（与 cmdDmg/atkEstimate/skillEstimate 同一「无副作用计算」家族，data.js POTION_*/ELIXIR_* 常量只在 rules.js 一处被消费）】`hero.js takePotion` 与 `view/drawBattle.js` 预览同时改调这两函数（drawBattle 的 rules import 原先只含 cmdDmg/atkEstimate/skillEstimate/rushReward/canonicalName/isBossFoe，追加两函数）。收益：改恢复公式只改 rules.js 一处，结算与预览永远同一份公式、绝无第二套口径——与 v19.5「预览与结算同公式」完全同一思路，只是这次收的是恢复量而不是伤害量。数值逐字不变（旧公式全等移入 home 函数），零恢复量回归。
- 【零回归面】未动药水机制本身——`takePotion` 的消耗顺序（灵药优先/普通药水只在掉血时用）、`Math.min(hpMax, …)` 夹取、`potionAvailability` 满状态判定、core.usePotion 的提示文案、battle.doItem 调用顺序全部原样；data.js `POTION_HP_PCT=0.5`/`POTION_HP_FLAT=8`/`ELIXIR_HP_PCT=0.8`/`ELIXIR_HP_FLAT=20`/`ELIXIR_MP_PCT=0.4` 五个常量逐字未动；menus.js 酿造页与 shop.js 商店价签显示的仍是配方百分比/固定加成（`Math.round(POTION_HP_PCT*100)%HP +FLAT` 之类），它们是常量文案而非按当前等级算的恢复量，保持原样。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只新增两个纯函数 + 改 6 行调用 + 3 处 import 整理 + 注释微调，零新增依赖。
- 验证：`node --check js/rules.js js/hero.js js/view/drawBattle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.20 专项冒烟（/tmp/jrpg_smoke_v1920_potion.mjs）**24 项断言全过**——`potionRestore/elixirRestore` 导出、data.js 五常量逐值未动、全扫 hpMax∈[1,9999]×mpMax∈[1,333] 与旧公式逐值恒等（含 round 半入 53.5→54 / 85.6→86 精确值）、takePotion 灵药优先/恢复量逐值一致/HP 夹取到上限/普通药水只回 HP 且 MP 原样/药水不足返回 null、hero.js 与 drawBattle.js 裸公式清零且改读两函数、两模块 data.js import 不再含 POTION_/ELIXIR_ 常量、rules.js import 含之。

## v19.19 传送门「面向提示」锁定判定单一数据源——锁定的村门从「毫无反馈的死区」变成「⛔ 门锁着」（体验·单一口径，与 v19.17 ENCOUNTER.warnFlash 同源不同家族——本版收口的是地图传送门锁定判定在视图层的裸写副本，同时补上一个真实 UX 死区）

- 【视图层裸写锁定判定 + 锁定时零反馈的 `2` 个问题】村庄东门（GATE）锁定条件「bossDefeated 后门常闭、只提示不传送」这一定义只在 `data.js MAPS.village.portals.GATE.locked(g)` 一处为真源，`world.usePortal` 锁定时弹 lockedMsg 拒绝通行；但视图层 `view/drawWorld.js` 的 `faceHint`（面向提示）把同一语义裸写成 `!(curMap()==='village' && S.G.bossDefeated)`——（a）与读表的 usePortal 互不引用，想给别的门加锁/换锁条件（如给 dungeon 的 GATE 按进度上锁）要改两个地方、还极易只改结算漏改提示，锁定判定悄然出现两套口径；（b）更糟的是锁定时条件为假、不进入该分支也不给任何提示——面向锁着的村门画面上什么都没有，玩家只能靠「踩上去才弹 lockedMsg」碰运气，无法从任何静态信号判断「这门通不通」。
- 【修正：视图层 GATE 分支改读 `MAPS[curMap()].portals.GATE.locked(S.G)`（与 world.usePortal 同一真源，零新增依赖——`MAPS`/`curMap`/`portalDest` 均已在本文件 import 列表），锁定时显示「⛔ 门锁着」、未锁照旧显示「踩上通行 → 目的地」】`view/drawWorld.js` `faceHint` 的 GATE 分支：先取 `(MAPS[curMap()].portals||{}).GATE`，`gate.locked && gate.locked(S.G)` 为真则 `lab='⛔ 门锁着'`，否则回落到原 `portalDest` 目的地提示。收益：锁定判定只有 data.js 一处口径、界面提示与真实传送行为绝无第二套；锁定的门第一次有了静态可交互信号（面向即见 ⛔），不用踩上去才知道锁着；dungeon/cave/gallery 的无锁 GATE/EXIT 行为逐字不变（仍走原目的地提示分支）。纯显示、零结算变化。
- 【零回归面】未动传送机制本身——`world.usePortal` 的 `p.locked(S.G)` 判定、`lockedMsg` 弹窗、`portalDest` 目的地、GATE/EXIT 踩踏与 Enter 打开全部原样；`MAPS.village.portals` 数据逐字不变；其余瓦片（NPC/SHOP/INN/BREW/STELE/FOUNTAIN/CHEST/BOSS/MB/SB/TRIAL）的 faceHint 分支原样。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只改 faceHint 一处 GATE 分支 + 注释，零新增依赖、零新增 export、零新增 import。
- 验证：`node --check js/view/drawWorld.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.19 专项冒烟（/tmp/jrpg_smoke_v1919_gatelock.mjs）**8 项断言全过**——未锁村门仍显示「踩上通行 → 雾语林」、未锁不显示「门锁着」、锁定（bossDefeated）现显示「⛔ 门锁着」、锁定不再显示「踩上通行」、MAPS 表 `locked()` 在锁定/解锁两态判定正确（与 usePortal 同源验证）、dungeon 面向 EXIT 不抛错且仍显示「踩上通行 → 潮灯镇」。

## v19.18 待机呼吸 bob 周期/相位展开单一数据源——世界与战斗精灵四处同读 IDLE_BOB（纯显示·单一口径，与 v19.15 UI_PULSE_MS / v19.17 ENCOUNTER.warnFlash 同一「动画时序数据化」体系——世界/战斗精灵「待机呼吸」的 ±1px 正弦浮动周期 500ms 与「相位随坐标错开」的展开系数 0.13 是精灵动画系列里最后一组四处复制的裸数值）

- 【`4` 处同语义互不相关】「待机呼吸」的 `Math.sin(Date.now()/500 + (px+py)*0.13)`（±1px 正弦浮动，相位随坐标错开防全场同步）以完全相同的形态裸写四处：`view/sprites.js` 的 `drawSheetChar`（32px 图集路径）、`drawSheetCustom`（定制帧位图集）、`drawSheetStrip`（条状图集）三处 + `drawMonster` 程序化回退路径一处（该处变量名 px/py 写作 x/y，同一语义）——四处同值互不引用，想调待机呼吸节奏（如放慢到 600ms、或让相位随坐标错开得更密 0.2）要改四个地方、还极易只改图集路径漏改回退路径——同屏几种怪待机呼吸悄然脱钩（v13.3 引入待机呼吸时把周期/相位参数写在各绘制路径体内，图集路径改了回退路径还按旧节奏浮）。注意：蓄力光环 320ms 正弦呼吸、任务问号 320ms、变身闪光 `t/400` 衰减、水纹相位 `/400` 是各自仅一处的单点动画参数（非四处同语义复制），数据注释沿用「刻意不并入、无需收口」的处理，本版不碰。
- 【修正：data.js 在 `UI_PULSE_MS` 之后新增 `IDLE_BOB = { period: 500, phase: 0.13 }`（period=待机呼吸正弦周期 ms、phase=相位随坐标错开的展开系数 rad/px）为唯一真源，注释说明派生形态与刻意保留的单点动画参数，随原 export 块导出，零新增依赖】`view/sprites.js` 四处改读 `Date.now()/IDLE_BOB.period + (px+py)*IDLE_BOB.phase`（回退路径同式 `(x+y)*IDLE_BOB.phase`），import 列表同步加 IDLE_BOB。收益：调待机呼吸节奏/相位错开密度只改 data.js 一处、图集与回退路径同步，绝无第二套口径；行为逐字不变（`IDLE_BOB.period` 仍为 500、`IDLE_BOB.phase` 仍为 0.13，四处表达式与旧字面量逐值恒等），零渲染/动画回归。
- 【零回归面】未动待机呼吸机制本身——±1px 浮动幅度 `Math.round(Math.sin(…))`、`opts.idle` 开关、相位随坐标错开的设计、各绘制路径的调用全部分布原样；`drawSheetChar` 的 `Math.floor(Date.now()/300)%2`（16px hurt 交替帧）、`drawSheetCustom` 的帧播放 300ms、`drawSheetStrip` 的 140ms 换帧、行走走帧 120ms、`drawHero` 程序化回退的 380ms bob / 260ms 走步均为各动画独立参数，原样不动。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只新增一个对象常量 + 改 4 处表达式 + 1 处 import + 1 处 export + 注释微调，零新增依赖。
- 验证：`node --check js/data.js js/view/sprites.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.18 专项冒烟（/tmp/jrpg_smoke_v1918_idlebob.mjs）**13 项断言全过**——`IDLE_BOB` 导出且 period=500/phase=0.13、data.js export 与 sprites.js import 均含该源、sprites.js 读 `IDLE_BOB.period`/`IDLE_BOB.phase` 各恰 4 处、裸「Date.now()/500 + (px+py)*0.13」与「(x+y)*0.13」已清零（剩 0 处）、新旧表达式 t∈[0,2000]×c∈[-64,64] 全扫逐值恒等、UI_PULSE_MS/HIT_FB_MS/warnFlash 既有动画时序常量原样。

## v19.17 遇敌预警条快闪周期单一数据源——小地图「⚠️ 危险逼近」红色预警条的 330ms 快闪开关同读 ENCOUNTER.warnFlash（纯显示·单一口径，与 v19.9 ENCOUNTER.full / v19.12 ENCOUNTER.warn 同一「遇敌槽数据化」家族——v19.12 收口 70 预警线时把「预警态闪烁 330ms 呼吸」明确标为家族留白，是 ENCOUNTER 对象之外最后一枚游离的遇敌槽读数可视反馈数值）

- 【`1` 处可执行调用点 + 注释双重留白的收口】遇敌槽「⚠️ 危险逼近」红色预警条的快闪开关周期「330」此前以裸字面量游离在遇敌槽口径之外：`view/drawWorld.js` 小地图的 `Math.floor(Date.now()/330)%2===0` 判定（可执行代码 1 处）+ data.js UI_PULSE_MS 注释对它「遇敌预警条 330ms…刻意不动」的留白标注（文字第 2 处）。遇敌槽其余一切读数口径（dangerMin/dangerVar/fountain/calm/full/warn）都已集中在 `ENCOUNTER` 对象，唯独这枚「预警条以多快的节奏快闪」是家族里最后一处游离值：想调预警闪烁节奏（如放慢到 450、或换成 250 更快闪）要改判定字面量 + 记得同步数据层注释，还极易只改界面漏改注释、让「预警闪烁」这个概念仍然没有名字。
- 【修正：data.js 的 `ENCOUNTER` 对象在 `warn` 之后新增 `warnFlash: 330`（红色预警条快闪开关周期，单位 ms）为唯一真源（随原对象一同 export，零新增导出项；对象注释补写 warnFlash 语义与家族收编说明，UI_PULSE_MS 注释里旧「遇敌预警条 330ms…刻意不动」的留白文字改写为已收口说明）】`view/drawWorld.js` 预警快闪判定改 `Math.floor(Date.now() / ENCOUNTER.warnFlash) % 2 === 0` 并补注释；`ENCOUNTER` 已在 drawWorld import 列表（v19.9/v19.12 已导入），零新增依赖。收益：调预警闪烁节奏只改 data.js 一处，界面判定与数据层注释同步，绝无第二套口径；行为逐字不变（`ENCOUNTER.warnFlash` 仍为 330，快闪开关与旧字面量在 t∈[0,2000] 全扫下逐值恒等），零遇敌/UI 回归。
- 【零回归面】未动遇敌槽机制本身——四个增减量、满槽 `full: 100`、预警线 `warn: 70` 逐字不变，`tickEncounter` 累加/触发、小地图色带与「遇敌 N%」读数、预警态配色（`#ff8a5b`/`#e14b3f`）与「⚠️ 危险逼近」文案全部原样；蓄力光环 320ms 正弦呼吸、任务问号 320ms 方块波、变身闪光 `t/400` 衰减、水纹 sine 相位 `/400` 各自动画独立参数一概不动。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只加一个对象成员 + 改 1 处判定 + 注释微调，零新增依赖、零新增 export、零新增 import。
- 验证：`node --check js/data.js js/view/drawWorld.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.17 专项冒烟（/tmp/jrpg_smoke_v1917_encflash.mjs）**14 项断言全过**——`ENCOUNTER.warnFlash=330` 导出且与历史字面量一致、对象定义与逐成员核对（dangerMin 10/dangerVar 9/fountain -25/calm -6/full 100/warn 70 不受影响）、drawWorld 可执行快闪调用点 1 处读该源、裸「Date.now()/330」已清零（剩 0 处）、快闪表达式与旧字面量 t∈[0,2000] 全扫逐值恒等、UI_PULSE_MS 注释留白说明已随收口改写、蓄力光环 320 正弦/任务问号 320/变身闪光 t/400 另一语义仍在。

## v19.16 开局经济/背包单一数据源——新档建档同读 START_GOLD/START_POTIONS（机制·单一口径，与 XP_INIT 同一「开局建档数值数据化」逻辑——建档行三枚数值里 XP_INIT（首级所需经验）早已数据化、唯独开局金币 30 / 生命药水 3 仍在 core.js 裸奔，本版补齐收口，与 INN_PRICE / BREW_GOLD / POTION_PRICE 同一「经济数据化」体系）

- 【建档行「经验已数据化、家底仍裸奔」的 2 枚遗留数值】`core.newGame` 的开局建档行 `level:1, xp:0, xpNext: XP_INIT, gold:30, item:3, potion2:0` 里，`XP_INIT` 早已由 data.js 常量供给，惟独开局金币 `30` 与开局生命药水 `3` 仍是裸字面量（`potion2:0` 为恒零的占位字段、`level/xp` 为结构性初始值，不属经济旋钮）——全库唯一定义开局家底的这一行没有自己的名字，想调开局难度（如困难档加码 50 金、或试点「轻装开局」1 瓶药水）得在 core.js 里翻字面量，且没有任何一处能看出「开局 30 金 / 3 瓶药水」是数值设计而非随手写的数；同栖一行的 `XP_INIT` 自 v14.x 起就由 data.js 供给，唯独这两位邻居漏了数据化。
- 【修正：data.js 在 `XP_INIT` 之后新增 `START_GOLD = 30`（新档开局金币）与 `START_POTIONS = 3`（新档开局生命药水）为唯一真源，注释说明这是建档行里 XP_INIT 的两位裸奔邻居，随原 export 块导出，零新增依赖】`core.js` 建档行改 `gold: START_GOLD, item: START_POTIONS`（import 列表同步加两常量）。收益：调开局家底只改 data.js 一处，建档与任何展示口径绝无第二套数值；行为逐字不变（`START_GOLD` 仍为 30、`START_POTIONS` 仍为 3，newGame 产出的开局状态与旧字面量逐值恒等），零开局/经济回归。
- 【零回归面】未动建档机制本身——`level:1`、`xp:0`、`potion2:0`（灵药恒零占位）、`xpNext: XP_INIT`、木剑/布衣、出生点、起始技能 `learnsAt(1)` 全部原样；存档槽预览/`slotPreview` 只读 `hero.gold` 运行时值、不受影响。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/后续存档解析。只新增两个常量 + 改 1 处建档行 + 2 处 import + 1 处 export + 注释微调，零新增依赖。
- 验证：`node --check js/data.js js/core.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.16 专项冒烟（/tmp/jrpg_smoke_v1916_start.mjs）**15 项断言全过**——`START_GOLD=30`/`START_POTIONS=3` 导出且与历史字面量逐值一致、`XP_INIT` 不受影响、`newGame()` 开局 gold=30/item=3/potion2=0/xpNext=20 与旧建档逐值恒等、core.js 建档行裸「gold: 30」/「item: 3」已清零（剩 0 处）且改读两常量、core.js import 与 data.js export 均含两常量。

## v19.15 UI「提示闪烁」脉冲周期单一数据源——防御/中毒角标、宝箱脉动、对话翻页指示四处同读 UI_PULSE_MS（纯显示·单一口径，与 v19.8 HIT_FB_MS 同一「UI 反馈时序数据化」家族——v19.8 收口受击反馈时长时把「盾牌/中毒角标与蓄力光环的呼吸周期」明确标为另一语义留白，本版兑现收口其中完全同型的方块波族）

- 【`4` 处同语义互不相关】UI「状态/提示闪烁」的 400ms 方块波开关表达式完全相同的 `Math.floor(Date.now()/400)%2===0` 裸写三文件四处：`view/drawBattle.js` 防御（盾牌）角标闪烁、`view/drawBattle.js` 中毒角标闪烁、`view/drawWorld.js` 蘑菇宝箱金光脉动、`view/menus.js` 对话翻页 ▼ 指示闪烁——四处同值互不引用（方块波：每 400ms 切换一次亮度，亮 400ms 灭 400ms），想调整个「提示闪烁」节奏（如放慢到 500、或加快到 300）要改三个文件里的四个地方、还极易只改角标漏改宝箱脉动——U 盾角标节奏变了蘑菇宝箱还按旧速度脉，各处提示开关悄然脱钩（自家 v19.8 收口受击时长时把「盾牌/中毒角标与蓄力光环的呼吸周期」标为另一语义留白，现兑现收口其中与旧字面量逐字同型的方块波族）。注意：蓄力光环 320ms 正弦呼吸、任务问号 320ms、遇敌预警条 330ms 快闪、变身全屏闪光 `t/400` 衰减、水纹/喷泉 sine 相位 `/400` 是各自动画的独立参数，本版刻意不碰。
- 【修正：data.js 在 `HIT_FB_MS` 之后新增 `UI_PULSE_MS = 400`（方块波闪烁的切换间隔，唯一真源，注释说明派生形态与刻意保留的独立动画参数，随原 export 块导出，零新增依赖）】`view/drawBattle.js` 盾牌/中毒两处角标、`view/drawWorld.js` 蘑菇宝箱脉动、`view/menus.js` 对话翻页指示共四处改读 `Math.floor(Date.now()/UI_PULSE_MS)%2===0`，三模块 import 列表同步加 UI_PULSE_MS。收益：调整个提示闪烁节奏只改 data.js 一处、角标/宝箱/翻页指示同步，绝无第二套口径；行为逐字不变（`UI_PULSE_MS` 仍为 400，四处开关与旧字面量在 t∈[0,4000] 全扫下逐值恒等、亮/灭切换点 0→400→800 逐值一致），零战斗/UI 回归。
- 【零回归面】未动任何闪烁渲染本身——每处闪烁的配色、出现条件（防御态、中毒回合数、支线/击破 Boss 后未开的宝箱、翻页未完）、字号/位置全部原样；`drawTileFx` 水纹 sine 相位 `Date.now()/400`、变身闪光 `(Date.now()-S.flash.t0)/400`、蓄力光环 `Date.now()/320` 正弦呼吸、任务问号 320ms、预警条 330ms 各动画面独立参数一概不动。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只新增一个常量 + 改 4 处表达式 + 3 处 import + 1 处 export + 注释微调，零新增依赖。
- 验证：`node --check js/data.js js/view/drawBattle.js js/view/drawWorld.js js/view/menus.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.15 专项冒烟（/tmp/jrpg_smoke_v1915_pulsems.mjs）**21 项断言全过**——`UI_PULSE_MS=400` 导出且与历史字面量一致、三模块 import 与四处调用点均读该源、三文件方块波裸「Math.floor(Date.now()/400)%2」已清零（剩 0 处）、水纹 sine `/400` / 变身闪光 `t/400` / 蓄力光环 320 / 问号 320 / 预警条 330 另一语义仍在、pulseNew 与旧字面量 t∈[0,4000] 全扫逐值恒等且亮/灭切换点（0/399 亮·400/799 灭·800/1600 回亮）校验。

## v19.14 战斗「实体落点」单一数据源——敌方本体/我方立绘锚点同读 BATTLE_MON/BATTLE_HERO（纯显示·单一口径，与 v19.13 FX_ENEMY/FX_HERO 同一「战斗落点数据化」家族——v19.13 收口「受击反馈落点」时把「实体/面板位置」明确标为另一语义留白，本版兑现收口：怪物本体「CV.width/2, 248」、我方立绘 (96,400) 是战斗画面里最后一片裸奔坐标）

- 【`8` 处同语义互不相关】战斗「实体落点」裸写八处：敌方本体锚点「CV.width/2, 248」两遍（`drawBattle.js` `drawArena` 竞技场辉光与 `drawBattle` 本体的 `const ey0 = 248` 各声明一遍，横坐标 ex0 同为画布居中 CV.width/2）、我方立绘 (96,400) 连带派生六处（本体 `drawHero(96,400)`、脚下阴影 `ellipse(96,408)`、蓄力光环渐变两参数组与双环 `(96,388)`、顶部辉光点 `arc(96,344)`）——八处同值互不引用，想调战斗站位（如把怪物从 248 下沉贴近地面、或把立绘挪开避开浮字）要改九个地方、还极易只改本体漏改阴影/光环——立绘挪了脚下阴影与蓄力光环还踩在旧坐标，左右悄然脱钩（自家 v19.13 收口受击浮字时把这批坐标标为「实体位置另一语义」留白，现仍裸奔在战斗画面正中央）。注意：技能面板 `panel(150,96,340,308)`、状态页缩略图 `drawMonster(CV.width/2, 210, BOSS)` 是「面板/缩略图位置」另一语义，且仅各自一处调用，本版刻意不并入。
- 【修正：data.js 在 `FX_HERO` 之后新增 `BATTLE_MON = { y: 248 }`（敌方本体锚点：横坐标恒为画布中心，与 FX_ENEMY 同式 `CV.width/2`，非魔法值）与 `BATTLE_HERO = { x: 96, y: 400 }`（我方立绘锚点）为唯一真源，注释说明派生关系，随原 export 块导出，零新增依赖】`view/drawBattle.js` 的 `ey0` 两处改读 `BATTLE_MON.y`，我方立绘本体 `drawHero(BATTLE_HERO.x, BATTLE_HERO.y)`、阴影 `BATTLE_HERO.y + 8`、蓄力光环 `BATTLE_HERO.y - 12`、辉光点 `BATTLE_HERO.y - 56` 共六处改读 BATTLE_HERO 派生，import 列表同步加 BATTLE_MON/BATTLE_HERO。收益：调战斗站位只改 data.js 两处锚点、本体/阴影/光环/辉光点同步，绝无第二套口径；行为逐字不变（`BATTLE_MON.y` 仍为 248、`BATTLE_HERO` 仍为 (96,400)，八处坐标与旧字面量逐值恒等：y+8=408 / y-12=388 / y-56=344；敌方横坐标 `CV.width/2` 是画布居中自描述式，各调用点同式保留），零战斗/UI 回归。
- 【零回归面】未动战斗画面布局本身——怪物本体与阴影椭圆（ex0, ey0+8）、敌方名字/血条/二段变身线、我方立绘比例 `BATTLE_SCALE`、蓄力光环的半径/呼吸周期/颜色、`drawHero`/`drawMonster` 仍以参数接收锚点（sprites.js 内部零硬编码）；`view/drawBattle.js` 第 77 行技能面板 `panel(150,96,340,308)`、`view/menus.js` 状态页缩略图 `drawMonster(..., 210, ...)` 另一语义不动；行 257/282/383 的 `400` 是呼吸/变身闪光时长（另一语义）不动。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只新增两个对象常量 + 改 8 处坐标 + 1 处 import + 1 处 export + 注释微调，零新增依赖。
- 验证：`node --check js/data.js js/view/drawBattle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.14 专项冒烟（/tmp/jrpg_smoke_v1914_battleanchors.mjs）**20 项断言全过**——`BATTLE_MON.y=248`/`BATTLE_HERO=(96,400)` 导出且与历史字面量逐值一致、drawBattle 两处 `BATTLE_MON.y` 读取点、八处裸坐标已清零（剩 0 处）、派生位移 y+8/y-12/y-56 逐值恒等（408/388/344）、敌方横坐标 `CV.width/2` 与技能面板 `panel(150,96,...)`、状态页缩略图 `(...,210,...)` 另一语义仍在、drawHero/drawMonster 仍参数化接收锚点。

## v19.13 受击反馈落点单一数据源——伤害浮字/爆裂粒子七处同读 FX_ENEMY/FX_HERO（纯显示·单一口径，与 v19.7 BIG_DMG / v19.8 HIT_FB_MS 同一「战斗反馈数据化」家族——v19.7 收口「多大伤害算大额」、v19.8 收口「受击反馈持续多久」后，「受击伤害数字/爆裂粒子落在屏幕哪里」是战斗视觉反馈里最后一处同值多行互不引用的裸奔坐标）

- 【`7` 处同语义互不相关】「受击伤害浮字/爆裂粒子落点」的屏幕坐标裸写七处：敌方落点「横坐标画布中心 + 纵坐标 188」四处（`battle.js` `attackMove` 攻击浮字、`enemyAI.js` 灼烧结算浮字、`enemyAI.js` 反击浮字、`view/drawBattle.js` `burstEnemy` 爆裂粒子）、我方落点「96, 340」三处（`battle.js` `applyPoisonTick` 中毒浮字、`enemyAI.js` 敌方攻击浮字、`view/drawBattle.js` `burstPlayer` 爆裂粒子）——七处同值互不引用，想调受击反馈位置（如把敌方浮字从 188 上移到 170 贴近怪物头部、或把毒字从我方头部挪开）要改三个文件里的七行，还极易只改浮字漏改爆裂粒子——伤害数字已上浮爆裂却还炸在旧位置，两侧受击反馈悄然分家（自家 v19.7/v19.8 把「阈值/时长」都收口了，唯独「位置」仍是家族里最后一处裸奔数值）。注意：怪物本体 `(ex0=CV.width/2, 248)`、我方立绘 `(96,400)`、技能面板 `panel(150,96,340,308)` 等是「实体/面板位置」另一语义，本版刻意不碰。
- 【修正：data.js 受击反馈数据化注释块在 `HIT_FB_MS` 之后新增 `FX_ENEMY = { y: 188 }`（敌方受击反馈落点：横坐标恒为画布中心，各处同式 `CV.width/2`，非魔法值）与 `FX_HERO = { x: 96, y: 340 }`（我方受击反馈落点）为唯一真源，随原 export 块导出，零新增依赖】`battle.js` 两处、`enemyAI.js` 三处、`view/drawBattle.js` 两处（`burstEnemy`/`burstPlayer`）共七处改读 `FX_ENEMY.y` / `FX_HERO.x` / `FX_HERO.y`，三模块 import 列表同步加 FX_ENEMY/FX_HERO。收益：调受击反馈落点只改 data.js 一处、浮字与爆裂粒子同步，绝无第二套口径；行为逐字不变（`FX_ENEMY.y` 仍为 188、`FX_HERO` 仍为 (96,340)，七处坐标与旧字面量逐值恒等；敌方横坐标 `CV.width/2` 是画布居中自描述式，各调用点同式保留），零战斗/UI 回归。
- 【零回归面】未动战斗画面布局本身——怪物本体与阴影椭圆（ex0, ey0=248/ey0+8）、敌方名字/血条/二段变身线、我方立绘与脚下阴影（96,400/96,408）、技能面板 `panel(150,96,340,308)`、`addFx` 的 `life:44/vy:1.5` 粒子属性、`burst` 的 n/尺寸/速度分布全部原样；`view/drawBattle.js` 第 91 行技能提示 `text(hint, 188, ...)` 的 188 是提示行横坐标（另一语义）亦不动。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只新增两个对象常量 + 改 7 处坐标 + 3 处 import + 1 处 export + 注释微调，零新增依赖。
- 验证：`node --check js/data.js js/battle.js js/enemyAI.js js/view/drawBattle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.13 专项冒烟（/tmp/jrpg_smoke_v1913_fxpos.mjs）**19 项断言全过**——`FX_ENEMY.y=188`/`FX_HERO=(96,340)` 导出且与历史字面量逐值一致、battle/enemyAI/drawBattle 三模块 import 与七处调用点均读该源、三模块受击反馈裸坐标已清零（剩 0 处）、敌方横坐标 `CV.width/2` 画布居中同式保留（非魔法值）、技能面板 `panel(150,96,...)` 另一语义仍在、坐标值与旧字面量在 CV.width∈[400,800] 全扫下逐值一致。

## v19.12 遇敌槽「危险逼近」预警线单一数据源——小地图高亮临界同读 ENCOUNTER.warn（机制·单一口径，与 v19.9 ENCOUNTER.full 同一「遇敌槽数据化」家族——v19.9 收口满槽 100 时在 data.js 注释里把 70 预警线明确预留为「另一语义（预警高亮），不并入本常量」，本版兑现收口）

- 【`2` 处的收口】遇敌槽「⚠️ 危险逼近」预警临界值「70」此前以两种形态游离在遇敌槽口径之外：`view/drawWorld.js` 小地图的判定字面量 `encPct >= 70` + `data.js` 注释里对它的描述「70 线…不并入本常量」（注释本身即第二处提到此值的文字）。遇敌槽其余一切口径（dangerMin/dangerVar/fountain/calm/full）都已集中在 `ENCOUNTER` 对象，唯独这枚「何时向玩家亮危险预警」的读数临界是家族里最后一处游离值：想调预警灵敏度（如提前到 60%、或推迟到 85%）要改判定字面量 + 记得同步数据层注释，还极易只改界面漏改注释、让「预警线」这个概念仍然没有名字。
- 【修正：data.js 的 `ENCOUNTER` 对象在 `full` 之后新增 `warn: 70` 为唯一真源（随原对象一同 export，零新增导出项；注释同步改写，原「不并入本常量」预留说明随收口收编）】`view/drawWorld.js` 预警判定改 `encPct >= ENCOUNTER.warn` 并补注释。收益：调预警灵敏度/整体遇敌槽读数口径只改 data.js 一处，界面判定与数据层注释同步，绝无第二套口径；行为逐字不变（`ENCOUNTER.warn` 仍为 70，预警判定与旧字面量在 encPct∈[0,100] 全扫下逐值恒等），零遇敌/UI 回归。
- 【零回归面】未动遇敌槽机制本身——四个增减量与满槽 `full: 100` 逐字不变，`tickEncounter` 累加/触发、小地图色带与「遇敌 N%」读数、预警态闪烁 330ms 呼吸与「⚠️ 危险逼近」文案、`drawWorld` 其它 70 语义（minimap 高度等图形值）全部原样。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只加一个对象属性 + 改 1 处判定 + 注释微调，零新增依赖、零新增 export。
- 验证：`node --check js/data.js js/view/drawWorld.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.12 专项冒烟（/tmp/jrpg_smoke_v1912_encwarn.mjs）**11 项断言全过**——`ENCOUNTER.warn` 导出且 = 70（与历史字面量一致）、`full` 仍 = 100、drawWorld 预警判定/注释均读 ENCOUNTER.warn、drawWorld 可执行代码裸「>= 70」已清零（剩 0 处）、data.js「不并入本常量」预留说明已随收口改写、预警判定与旧字面量 encPct∈[0,100] 全扫逐值恒等。

## v19.11 强敌类敌手判定单一数据源——逃跑拦截/重试快照/名字配色/Boss 预判/Boss 形象六处同读 isBossFoe（机制·单一口径，与 v19.10 DOT_MIN 同一「战斗反馈数据化」家族——战斗/UI 里判断「是不是不可逃跑的 Boss 级敌人」这个谓词一直以同一段四旗标表达式裸写六处，是本家族里最后一处「同语义复制粘贴」的判定逻辑）

- 【`6` 同语义六处互不相关】「Boss 级强敌」判定 `enemy.isBoss || enemy.isTrue || enemy.isCaveBoss || enemy.isRush`（视模块写作 `S.enemy.…` 或 `m.…`）裸写六处：`battle.js` `startBattle` 的开战重试快照写入条件、`doFlee` 的「无法逃脱（本回合行动保留）」拦截；`view/drawBattle.js` 敌方名字紫色配色（`#d88bff`）、指令栏 `[4]逃跑⛔` 置灰（isBossFlee）、Boss 战敌方攻击预判分支（isBossFlee）；`view/sprites.js` `drawMonster` 的 Boss 形象选择（`boss` vs `slime`）——六处同值互不引用，想给某类强敌增删旗标（如新增第五种「不可逃跑」的强敌旗、或给某 Boss 开放逃跑）要同步改六个地方，还极易只改拦截漏改配色——「按 4 被气场压制」但指令栏还显示「成功率约60%」、攻击预判走普通怪分支、形象缩成史莱姆，四处悄然脱钩。
- 【修正：rules.js 新增 `isBossFoe(enemy)` 纯函数为唯一真源（置于 `canonicalName` 之前，注释同步说明，随既有逐一 export 方式导出）】`battle.js` 两处、`view/drawBattle.js` 三处、`view/sprites.js` 一处改读 `isBossFoe(…)`，三模块 import 列表同步加 isBossFoe。帮身 `!!(enemy && (isBoss||isTrue||isCaveBoss||isRush))` 自带 null/undefined 短路（与旧 `enemy && (…)` 恒等），调强敌判定只改 rules.js 一处、四处界面同步，绝无第二套口径；行为逐字不变（16 旗标组合全扫与旧谓词逐值恒等），零战斗/UI 回归。
- 【零回归面】未动强敌判定本身——四旗标的含义/取值、Boss 重试快照记录范围、逃跑拦截文案、紫色名字、`[4]逃跑⛔`、Boss 攻击预判、Boss 形象全部原样。以下刻意不动：`drawBattle` 竞技场辉光的 3 旗标写（`isBoss||isTrue||isCaveBoss`，不包 isRush——试炼战无竞技场辉光是另一语义）、`drawMonster` 里 `m.isTrue?1.28:(m.isBoss||m.isCaveBoss?1.18:…)` 的分档缩放（不同旗标不同系数，不是布尔判定）。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只新增一个纯函数 + 改 6 处判定 + 3 处 import，零新增依赖、零新增数据常量。
- 验证：`node --check js/rules.js js/battle.js js/view/drawBattle.js js/view/sprites.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.11 专项冒烟（/tmp/jrpg_smoke_v1911_bossfoe.mjs）**17 项断言全过**——`isBossFoe` 导出且为函数、16 旗标组合/null/undefined/空对象与旧谓词逐值恒等、六个调用点均改读 isBossFoe、battle/drawBattle/sprites 三文件强敌判定「四连裸谓词」已清零（rules.js 内仅帮身本体 1 处保留）、竞技场辉光 3 旗标与分档缩放两处另一语义仍独立。

## v19.10 灼烧/中毒每回合扣血下限单一数据源——结算与角标四处同读 DOT_MIN（机制·单一口径，与 v19.7 BIG_DMG / v19.8 HIT_FB_MS 同一「战斗反馈数据化」家族——持续伤害的比例（BURN_PCT 4% / POISON_PCT 5%）与回合数（POISON_TURNS）早已集中在 data.js，唯独「每回合至少扣 2 血」这个低血线钳制值是家族里最后一处裸奔数值）

- 【`4` 同语义四处互不相关】持续伤害（DoT）「每回合至少扣 2 血」的钳制下限 `Math.max(2, round(hpMax×pct))` 裸写四处：`enemyAI.js` 灼烧结算、`battle.js` `applyPoisonTick` 中毒结算、`view/drawBattle.js` 灼烧角标与中毒角标——四处同值互不引用，且 DoT 的比例/回合数明明早就集中在 `BURN_PCT`/`POISON_PCT`/`POISON_TURNS`，唯独下限值游离在对象系列之外：想调 DoT 下限（如把保底 2 收紧到 1、或放宽到 3）要改四个地方，还极易只改结算漏改角标——低 HP 怪结算已扣 3、战斗角标却还标 2，界面读数与实际扣血悄然脱钩。
- 【修正：data.js 在 `POISON_TURNS` 之后新增 `DOT_MIN = 2` 为唯一真源（注释同步说明，随原 export 块导出，零新增导出项）】`enemyAI.js` 灼烧结算、`battle.js` 中毒结算、`view/drawBattle.js` 灼烧/中毒角标四处改读 `Math.max(DOT_MIN, …)`，三模块 import 列表同步加 DOT_MIN。收益：调 DoT 下限只改 data.js 一处、结算与角标同步，绝无第二套口径；行为逐字不变（`DOT_MIN` 仍为 2，四处下限与旧字面量逐值恒等），零战斗/UI 回归。
- 【零回归面】未动持续伤害机制本身——`BURN_PCT`/`POISON_PCT` 比例、`POISON_TURNS` 回合数、毒蛇施毒概率 `POISON_CHANCE`、灼烧/中毒角标的 400ms 呼吸与配色全部原样；`sprites.js` 椭圆半径 `Math.max(2, …)` 与 `drawWorld.js` 小地图格子钳制 `Math.max(2, …)` 是另一语义（图形尺寸），刻意不动。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只新增一个常量 + 改 4 处下限 + 3 处 import + 1 处 export + 注释微调，零新增依赖。
- 验证：`node --check js/data.js js/battle.js js/enemyAI.js js/view/drawBattle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.10 专项冒烟（/tmp/jrpg_smoke_v1910_dotmin.mjs）**13 项断言全过**——`DOT_MIN` 导出且 = 2（与历史字面量逐值一致）、四处 DoT 结算/角标均读 DOT_MIN、battle/enemyAI/drawBattle 三文件 DoT 语义裸「Math.max(2, Math.round(」已清零（仅注释保留说明）、`max(DOT_MIN, round(hpMax×pct))` 与旧字面量在 600 组全扫下逐值恒等、低 hpMax 钳制与高 hpMax 按比例语义验证。

## v19.9 遇敌槽满值单一数据源——world 累加/触发与 drawWorld 小地图同读 ENCOUNTER.full（机制·单一口径，与 v19.7 BIG_DMG / v19.8 HIT_FB_MS 同一「战斗反馈数据化」家族——遇敌槽的增减量（dangerMin/dangerVar/fountain/calm）早已集中在 data.js 的 ENCOUNTER 对象，唯独「满槽 100」这个决定遇敌触发频率的临界值是家族里最后一处裸奔数值）

- 【`4` 同语义四处互不相关】遇敌槽「满槽 100」裸写四处：`world.js` `tickEncounter` 累加上限 `Math.min(100, ...)`、触发判定 `S.encGauge >= 100`、`view/drawWorld.js` 小地图遇敌槽的 clamp `Math.min(100, S.encGauge || 0)` 与刻度分母 `encPct / 100`——四处同值互不引用，且遇敌槽的增减量明明早就集中在 `ENCOUNTER` 对象（`dangerMin`/`dangerVar`/`fountain`/`calm`），唯独满槽值游离在对象之外：想调遇敌频率（如把满槽从 100 收紧到 80 让战斗更密集）要改四个地方，还极易只改累计触发（world.js）漏改小地图（drawWorld.js）——实际遇敌频率变了小地图却还按旧刻度显示，满 80% 就触发战斗但读数只走到 80% 永远到不了 100%。
- 【修正：data.js 的 `ENCOUNTER` 对象新增 `full: 100` 为唯一真源（置于 calm 之后，注释同步说明，随原对象一同 export，零新增导出项）】`world.js` 累加上限改 `Math.min(ENCOUNTER.full, ...)`、触发判定改 `S.encGauge >= ENCOUNTER.full`；`view/drawWorld.js` import 列表加 ENCOUNTER，clamp 与刻度分母改读 `ENCOUNTER.full`。收益：调遇敌满槽/整体遇敌频率只改 data.js 一处、累计触发与小地图显示同步，绝无第二套口径；行为逐字不变（`ENCOUNTER.full` 仍为 100，累加 clamp、触发边界、小地图百分比读数与旧字面量逐值恒等），零遇敌/UI 回归。
- 【零回归面】未动遇敌槽机制本身——四个增减量（危险格 +10~18、喷泉 -25、其它可行走格 -6）逐字不变，`tickEncounter` 触发后 `S.encGauge = 0` 复位、Boss 区域强制遇敌、`drawWorld` 小地图色带配色/「⚠️ 危险逼近」70 线（预警高亮，独立语义，注释已注明不并入本常量）/「遇敌 N%」读数全部原样。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只加一个对象属性 + 改 4 处引用 + 1 处 import + 注释微调，零新增依赖、零新增 export。
- 验证：`node --check js/data.js js/world.js js/view/drawWorld.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.9 专项冒烟（/tmp/jrpg_smoke_v199_encfull.mjs）**9 项断言全过**——`ENCOUNTER.full` 导出且 = 100（与历史字面量逐值一致）、world 累加上限/触发判定与 drawWorld clamp/刻度分母四处均读 ENCOUNTER.full、world/drawWorld 遇敌槽语义裸「100」已清零（仅注释保留旧值说明）、累加 clamp 与触发边界 0..150×0..30 全扫与旧字面量逐值恒等。

## v19.8 受击视觉反馈时长单一数据源——三处裸 220 同读 HIT_FB_MS（纯显示·单一口径，与 v19.7 BIG_DMG 同一「战斗反馈数据化」家族——v19.7 收口「多大伤害算大额」后，「受击反馈持续多久」是战斗视觉反馈里最后一处同值三行互不引用的裸奔数值）

- 【`3` 同语义三处互不相关】受击视觉反馈时长「220ms」裸写三处：`battle.js` `attackMove` 的敌方闪红复位 `setTimeout(..., 220)`、`enemyAI.js` `enemyAct` 的我方闪红复位 `setTimeout(..., 220)`、`view/drawBattle.js` 的震屏衰减 `/ 220`（另有注释「220ms 衰减」）——三处同值互不引用，注释都把它当「受击反馈节奏」：想调反馈时长（如放慢到 280、或收紧到 180）要改三个地方，还极易只改闪红漏改震屏——闪红已熄灭震屏却还在抖（或反之），暴击/重击的受击反馈悄然分成两套节奏。
- 【修正：data.js 新增 `HIT_FB_MS = 220` 为唯一真源（置于 SHIELD_MULT 之后、宝箱块之前，紧邻「战斗反馈数据化」注释块，并加入 export）】`battle.js` / `enemyAI.js` 两处 `setTimeout` 改传 `HIT_FB_MS`，`drawBattle.js` 震屏衰减改 `(Date.now() - S.shake.t0) / HIT_FB_MS` 并更新注释，三模块 import 列表同步加 HIT_FB_MS。收益：调受击反馈节奏只改 data.js 一处、闪红与震屏同步，绝无第二套口径；行为逐字不变（`HIT_FB_MS` 仍为 220，三处时长与旧字面量逐值恒等），零战斗/UI 回归。
- 【零回归面】未动受击反馈机制本身——`enemy.hurt`/`hero.hurt` 闪红帧（sprites.js 的 hurt 红色罩）、`S.anim` 受击动画、`S.shake` 的 `pow: 4/3` 震屏强度分级与衰减曲线（`t/220` 同形）、变身全屏闪光 `S.flash` 的 400ms（v16.x 变身反馈，独立语义不动）、盾牌/中毒角标与蓄力光环的 `Date.now()%400`/`/320` 呼吸周期（独立动画体系不动）全部原样。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只新增一个常量 + 改 3 行时长 + 3 处 import + 1 处 export + 注释微调，零新增依赖。
- 验证：`node --check js/data.js js/battle.js js/enemyAI.js js/view/drawBattle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.8 专项冒烟（/tmp/jrpg_smoke_v198_hitfb.mjs）**9 项断言全过**——`HIT_FB_MS` 导出且 = 220（与历史字面量逐值一致）、三模块均 import 引用、敌方/我方闪红复位与震屏衰减三行均读 HIT_FB_MS、三处可执行代码裸「220」已清零（仅注释保留旧值说明）、行为等价校验。

## v19.7 大额伤害阈值单一数据源——attackMove 浮字加粗与震屏同读 BIG_DMG（纯显示·单一口径，与 CRIT_RATE / CRIT_MULT / FLEE_SUCCESS 同一「战斗反馈数据化」家族——v19.6 收口蓄力光环后，「多大伤害算大额」是战斗视觉反馈里最后一处同值两行互不引用的裸奔数值）

- 【`1` 同函数内两行互不相关】`battle.js` `attackMove` 的「大额伤害」判定在相邻两行各写一个 `dmg >= 25`：浮字加粗（`addFx(..., dmg >= 25 || isCrit)` 传入 bold 参数）与震屏触发（`if (isCrit || dmg >= 25) S.shake = {...}`）——两行同值互不引用，注释自己都写着「≥25，与浮字加粗同阈值」：想调「多大伤害算大额」（如震屏更频繁放宽到 20、或更克制收紧到 30）要改两个地方，还极易只改震屏漏改加粗——加粗阈值与震屏阈值悄然脱钩，暴击外的中高伤要么只加粗不震、要么只震不加粗；且全库 grep 确认该「大额伤害」语义的裸 25 仅此两行。
- 【修正：data.js 新增 `BIG_DMG = 25` 为唯一真源（置于 CRIT_MULT 之后、石甲减伤块之前，紧邻「暴击」注释块，并加入 export）】`battle.js` 两行判定改读 `dmg >= BIG_DMG`，import 列表同步加 BIG_DMG。收益：调大额伤害阈值只改 data.js 一处、浮字加粗与震屏同步，绝无第二套口径；行为逐字不变（`BIG_DMG` 仍为 25，边界 24/25 两档加粗/震屏切换点与旧字面量逐值恒等），零战斗/UI 回归。
- 【零回归面】未动大额伤害机制本身——`addFx` 的 bold 参数、`S.shake` 的 `pow: isCrit ? 4 : 3` 震屏强度分级、220ms 衰减、浮字生命期 44 全部原样；普通战/技能/Boss 战的加粗与震屏触发条件逐字不变。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只新增一个常量 + 改 2 行判定 + export 1 处 + import 1 处，零新增依赖。
- 验证：`node --check js/data.js js/battle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.7 专项冒烟（/tmp/jrpg_smoke_v197_bigdmg.mjs）**6 项断言全过**——`BIG_DMG` 导出且 = 25（与历史字面量逐值一致）、浮字加粗行与震屏行均引用 BIG_DMG、可执行代码裸「>= 25」已清零（仅注释保留旧值说明）、边界值全扫 10 组（dmg 1/24/25/26/60 × crit 两态）BIG_DMG 版与旧字面量逐值恒等、顺序（加粗在前/震屏在后）保持。

## v19.6 蓄力金色呼吸光环——主角本体 buff 可视反馈（体验·纯显示，蓄力状态从「右上角一行静态字」升级为角色身上的脉冲光环；与盾牌/中毒/灼烧/冻结/石甲角标同一「buff 可视化」体系——防御有盾牌角标、我方中毒有 ☠️ 角标、敌方灼烧/冻结/石甲各有角标，唯独蓄力这枚关系大招的 buff 缺角色身上的反馈）

- 【`1` buff 可视化不对称】战斗画面里「蓄力中」只有 drawBattle 右上角一行静态文字 `蓄力中 · 下击/技能×1.5`（且在敌方回合持蓄时该行仍在转角处、玩家视线在主角色身上）：防御有常驻盾牌角标、我方中毒有 ☠️ 角标（都带 400ms 呼吸脉冲）、敌方灼烧/冻结/石甲各有角标——唯独蓄力这一影响下一击 ×1.5 的关键 buff 没有任何角色本体信号，蓄着力进敌方回合后画面几乎看不出手里还捏着大招。
- 【修正：`view/drawBattle.js` 在 `drawHero(96,400,…)` 之后、名字/血条之前插入 v19.6 光环块】`hero.charge` 为真时给主角叠加一圈金色呼吸光环：径向渐变柔光（圆心 96,388、半径 56、随脉冲 0.10~0.28 透明度）+ 金色细环（半径 44、描边 2px、alpha 0.25~0.50）+ 顶部辉光点（在角色头顶 344 处、alpha 0.30~0.85）——三者同用 320ms 正弦周期 `0.5+0.5*sin` 呼吸（与盾牌/中毒角标的 `Math.floor(Date.now()/400)%2` 同一 `Date.now()` 动画体系）。收益：蓄力状态一眼可感、技能菜单/敌方回合/防御中持蓄都持续可见，与右上角文字标注双保险；纯显示、零结算变化。
- 【零回归面】未动蓄力机制本身——`battle.doCharge`（置 charge=true）、`rules.atkEstimate/skillEstimate`（×CHARGE_MULT 预览）、`battle.doAttack/doSkill`（爆发消耗）、技能菜单「·蓄力保留」/MP 行「蓄力×1.5」标注全部原样（本就同源）；`hero.charge=false` 时光环块整段跳过（守卫跳开、零绘制调用），普通战斗/未蓄力画面逐像素不变。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只改 view/drawBattle.js 一个文件、纯新增一段条件绘制，零新增依赖、零新增 export（复用已在 import 列表的 CTX / CHARGE_MULT）。
- 验证：`node --check js/view/drawBattle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.6 专项冒烟（/tmp/jrpg_smoke_v196_chargeglow.mjs）**4 项断言全过**——CTX 桩执行光环块 `charge=true` 产生 2 次填充（柔光+辉光点）+1 次描边（细环）+3 次 arc 且全部 rgba alpha 合式（0..1）、`charge=false` 守卫跳开零绘制（原行为不变）、引用检查仅用 CTX/Date.now 零新增依赖、位置检查光环块在 drawHero 之后绘制（角色之上）。



- 【`1` 同公式两处互不相关】`battle.js` `threatWarn`（开战威胁预警）手写裸公式估双方基础伤害：`playerHit = Math.max(1, hero.atkMax * 2 - enemy.def)`、`enemyHit = Math.max(1, enemy.atk * 2 - hero.defMax)`——而全库唯一伤害公式真源是 `rules.cmdDmg`（`raw = max(1, atk×2-def)`，无浮动档时恒等于裸公式）：伤害预览（`atkEstimate`/`skillEstimate`）与敌方攻击（`enemyAI.enemyAct` 的 `cmdDmg(enemy.atk, hero.defMax, mult)`）早已同读此源，唯独威胁预警是最后一份把 `atk×2-def` 抄成裸字面量的代码——想调伤害公式（如 atk×2 改 1.9）要改 rules.js + battle.js 两处，还极易只改结算漏改预警，实际伤害公式变了威胁预警却还按旧公式估、亮起的「⚠️ 强敌」悄然失真。
- 【修正：`battle.js` `threatWarn` 两行改调 `cmdDmg(hero.atkMax, enemy.def, 1, false)` / `cmdDmg(enemy.atk, hero.defMax, 1, false)`（cmdDmg 本就在 import 列表，零新增依赖）】收益：调伤害公式只改 rules.cmdDmg 一处，预览/敌方攻击/威胁预警三端同源，绝无第二套口径；行为逐字不变——双方攻防恒为整数（baseStats 线性式、装备 atk/def 整数、MON_BASE 整数缩放、地图/困难倍率均 Math.round），`Math.round(整数)=整数`，`cmdDmg(...,1,false)` 与裸公式对任意整数输入逐值恒等。
- 【零回归面】未动威胁等级判定本身——`threat` 的累进规则（isElite +1 / 5 刀内可击杀 +1 / 敌方两刀致死 +2）与三档文案（「明显强于你」「有些棘手」「无」）以及 Boss/终焉/试炼的固定预警分支全部原样；`cmdDmg` 随机浮动档（rollVariance）未引入（传 false，估算仍为确定值）。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只改 2 行调用 + 扩注释，零新增 export。
- 验证：`node --check js/battle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.5 专项冒烟（/tmp/jrpg_smoke_v195_threat.mjs）**7 项断言全过**——200×140 整数域全扫 `cmdDmg(atk,def,1,false) ≡ max(1,atk×2-def)`、弱敌开战无「⚠️」、强敌开战出「⚠️ 强敌」、Boss 开战固定「旧灯卫的影子」预警、battle.js 可执行代码裸 `* 2 - enemy.def / * 2 - hero.defMax` 已清零（仅注释保留旧式说明）、cmdDmg 引用在位。

## v19.4 战斗石甲受击减伤标注单一数据源——drawBattle 石甲角标「受击-40%」改由 SHIELD_MULT 推导（纯显示·单一口径，与 SHIELD_MULT / 帮助页「石心魔像·石甲」/ enemyAI 凝甲提示同一「石甲数据化」家族——v15.2 收口结算/预览/凝甲提示、v16.7 收口帮助页石甲行、v19.0 收口石甲帮助页触发线/层数后，战斗画面石甲角标是 SHIELD_MULT 家族最后幸存的裸奔数值）

- 【`1` 结算派生值互不相关】战斗画面敌方石甲角标 `🪨 石甲×N（受击-40%）` 的减伤百分比裸写「-40%」，而它的唯一真源实为 `SHIELD_MULT = 0.6`（石甲命中把最终伤害再 ×0.6 取整、保底 1）：同一减伤率在 `enemyAI.js` 凝甲提示（`Math.round((1 - SHIELD_MULT) * 100)%`）与 data.js 帮助页「石心魔像·石甲」（`-Math.round((1 - SHIELD_MULT) * 100)%`）早已同读此源，唯独战斗画面角标是 v15.2 收口时漏网的裸字面量——想调石甲强度（如放宽到 0.5 → 减伤 50%）要改结算/提示还极易只改结算漏改角标，实际减伤变了战斗画面却还报旧值「-40%」。
- 【修正：`view/drawBattle.js` 石甲角标改拼 `` 🪨 石甲×${enemy.shield}（受击-${Math.round((1 - SHIELD_MULT) * 100)}%） ``（import 列表同步加 SHIELD_MULT）】收益：调石甲强度只改 data.js 的 SHIELD_MULT 一处、角标/凝甲提示/帮助页/结算四端同步，绝无第二套口径；行为逐字不变（`1 - 0.6`×100 = 40，文案 `（受击-40%）` 原样），零战斗/UI 回归。
- 【零回归面】未动石甲机制本身——`battle.attackMove` 减伤（×SHIELD_MULT 取整保底 1）、`rules.withShield` 预览还原、`enemyAI` 凝甲结算与提示、帮助页标注全部原样（本就同源）；石甲角标的存在条件（`enemy.shield > 0` 才绘制）与居右位置（x=620）逐字不变。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只改 1 行角标文案 + import 1 处，零新增依赖、零新增 export。
- 验证：`node --check js/view/drawBattle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.4 专项冒烟（/tmp/jrpg_smoke_v194_shieldbadge.mjs）**13 项断言全过**——`SHIELD_MULT` 推导 100×(1-0.6)=40、石心魔像 shield>0 时角标绘制且「受击-40%」由常量推导逐字一致（石甲×3 / 石甲×4 两档均验）、文案与历史字面量逐字相同、shield=0 不绘制（原行为）、洞窟领主石甲×2 角标联动、多类型敌人绘制无异常、源码 grep 确认 drawBattle 导入含 SHIELD_MULT 且可执行代码裸「受击-40%」已清零（仅注释保留旧值说明）。

## v19.3 幽冥魔王最大 HP 单一数据源——BOSS.bossHpMax 改读 BOSS_BASE.hpMax（机制·单一口径，与 v18.9 BOSS_BASE 同一「三强敌基础数值数据化」家族——v18.9 收口主线/试炼兵力时此值以裸 140 留存，本次收官）

- 【`1` 同对象内两处互不相关】`data.js` 的 `BOSS` 常量（幽冥魔王主线版）里 `bossHpMax:140` 是硬编码字面量，而它的真源 `BOSS_BASE.hpMax` 同为 140（BOSS_BASE 就是 v18.9 单设的幽冥魔王兵力基准表）——同一数值两处互不引用：想调幽冥魔王最大 HP（如提到 160）要改两处，还极易只改 BOSS_BASE 漏改 bossHpMax（或反之），两处悄然脱钩；且该字段全库无任何读取方（主线判定/必掉圣光之剑/暴毙等全部由 `isBoss` 驱动，`bossHpMax` 仅是 BOSS 对象上的既有元数据——v18.9 专项冒烟断言「试炼版不带 bossHpMax」把它当作主线专属旗标保留校验），是 BOSS_BASE 家族里最后一处与真源脱钩的残留字面量。
- 【修正：`BOSS` 构造行改为 `bossHpMax: BOSS_BASE.hpMax`（同文件同作用域，直接可取）】收益：调幽冥魔王最大 HP 只改 data.js 的 BOSS_BASE.hpMax 一处，bossHpMax 自动跟随，绝无第二套口径；行为逐字不变（现两值同为 140，合并后对象逐键逐值不变——专项冒烟 17 键逐键比对通过），零 UI/战斗回归。
- 【零回归面】未动战斗机制本身——`isBoss` 驱动的全部主线判定（战前威胁/徽记/必掉圣光之剑/bossDefeated/banner）、`rules.js` `BOSS_DEFS` 直读 BOSS、`world.js` 祭坛 `deep(BOSS)` 遇敌、`SPECIES['幽冥魔王']` 经 withSpecies 并入的 phase2/acts/resist/tag/lv 全部原样。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只改 1 行构造表达式（+3 行注释说明），零新增依赖、零新增 export（BOSS_BASE 仍不对外导出）。
- 验证：`node --check js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.3 专项冒烟（/tmp/jrpg_smoke_v193_bosshpmax.mjs）**13 项断言全过**——`BOSS.bossHpMax`=140 且与 `BOSS.hpMax` 恒等（同源推导、永不漂移）、合并后 17 键逐键与旧字面量一致（含 SPECIES 并入的 lv/acts/phase2）、试炼复刻 RUSH_BOSSES[0] 与 CAVE_BOSS/TRUE_BOSS 均不带 bossHpMax（v18.9 契约不变）、`isBoss` 专属旗标保留、phase2 原样并入、构造行可执行代码引 `BOSS_BASE.hpMax` 且裸 `bossHpMax:140` 已清零（仅注释保留旧值说明）。

## v19.2 图鉴全收集金币奖励单一数据源——applyAchievements 的「记忆守护者」999 金 发奖/横幅文案两处同读 PERFECTION_GOLD（机制·单一口径，与 RICH_GOLD / SCHOLAR_GOAL / LUCKY_GOAL / HUNT_GOAL / LVL5_GOAL / LVL10_GOAL / FIRSTBLOOD_GOAL 同一「成就阈值数据化」家族——v18.7 收口 FIRSTBLOOD_GOAL 后 ACH_LIST 门槛已全部单源，本次收口的是成就触发后的专享奖励里残存的裸奔数字）

- 【`2` 同分支内两处互不相关】成就「记忆守护者」图鉴全收集的专享奖励：`hero.js` `applyAchievements` 的 perfection 分支里，发奖数额（`hero.gold += 999`）与解锁横幅文案（「🏆 图鉴收集完成！额外奖励 999 金币！」）各写一份 999——同分支内互不引用：想调奖励（如改成 500 金）要改两处，还极易只改发奖漏改文案，实际到账与横幅报数悄然脱钩；且全库无任何现成源可借鉴（TRUE_BONUS_GOLD=300 是终焉之神专属、RICH_GOLD=500 是「小富翁」持有门槛，均非图鉴全收集奖励），故必须单设常量——v18.x 系列收口 ACH_LIST 判定/描述/进度的门槛后，全库与成就相关的数值中最后一段「同对象内互不引用」的裸奔数字。
- 【修正：data.js 新增 `PERFECTION_GOLD = 999` 为唯一真源（置于 FIRSTBLOOD_GOAL 之后、药水恢复量块之前，紧邻「成就阈值」注释块）】`hero.js` `applyAchievements` 的发奖（`gold += PERFECTION_GOLD`）与横幅文案（模板串「额外奖励 ${PERFECTION_GOLD} 金币！」）两处同读此源，import 列表同步加 PERFECTION_GOLD，并加入 export。收益：调全收集奖励只改 data.js 一处、到账与横幅同步，绝无第二套口径；行为逐字不变（`PERFECTION_GOLD` 仍为 999，到账 999 金、横幅文案原样），零 UI/战斗回归。
- 【零回归面】未动成就机制本身——`ACH_LIST` 17 条逐字不变（perfection 条判定/描述/进度本就读 BESTIARY_TARGET.length）、`unlockedAchievements` 判定流程、`hero.gold` 记账原样。未动任何掉落/经验/金币曲线/难度/技能/支线/存档。只新增一个常量 + 改 2 行（发奖/文案）+ import/export 各 1 处，零新增依赖。
- 验证：`node --check js/data.js js/hero.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.2 专项冒烟（/tmp/jrpg_smoke_v192_perfectgold.mjs）**12 项断言全过**——`PERFECTION_GOLD` 导出且为 999、全收集时发奖到账 100+999、横幅文案与常量模板串逐字一致、文案把常量替换后无残留裸 999、未全收集不发奖且无专享横幅、hero.js 可执行代码裸 `999` 已清零（仅注释保留旧值说明）、发奖行与文案行均含常量标识符、同族成就阈值常量（RICH_GOLD/SCHOLAR_GOAL/LUCKY_GOAL/HUNT_GOAL/LVL5_GOAL/LVL10_GOAL/FIRSTBLOOD_GOAL）未动、ACH_LIST 17 条结构/描述不变、perfection 判定（全收集 true / 空图鉴 false）不受影响。

## v19.1 技能领悟等级上界单一数据源——skillXpHint「距下一技能」扫描上界改读 MAX_LEARN_LV（= LEARN_AT 最大领悟级，机制·单一口径，与 v17.7 LEARN_AT 单一数据源同一「技能表数据化」家族——v17.7 收口起始技能/升级领悟后，全库与技能领悟表相关的最后一处裸奔数值）

- 【`1` 隐蔽的脱钩上界】`hero.skillXpHint` 的「距下一技能还差 N 经验」提示（状态页/技能菜单展示）用裸 `8` 作扫描上界（`for (let lv = (hero.level||1)+1; lv <= 8; lv++)`）——这个 8 是当年「最末领悟级 7 + 1」的随手值，与技能领悟表 `LEARN_AT`（`{1:火焰斩,3:冰霜击,4:治愈术,5:雷鸣,7:陨石术}`，最大领悟级 7）互不相关：想新增 9 级以上的技能（或往领悟表增删更高的领悟级）时，上界会悄然过期——新技能永远进不了「距下一技能」提示，玩家升到 9 级前毫无预告；且全库 grep 确认裸 `8` 仅此一处（其余 8 均与等级无关），但它是「写死值取代真源」的典型，收敛到领悟表推导是唯一正确归宿（v17.7 只收口了「起始技能/升级领悟」两处，此上界当时未纳入、裸 8 留存至今）。
- 【修正：data.js 新增 `MAX_LEARN_LV = Math.max(...Object.keys(LEARN_AT).map(Number))` 为唯一真源（置于 learnsAt 之后、经验曲线块之前，独立注释块，并加入 export）】`hero.skillXpHint` 的扫描上界改读 `lv <= MAX_LEARN_LV`，import 列表同步加 `MAX_LEARN_LV`。收益：技能领悟表增删任意等级（含 9 级以上），「距下一技能」提示自动跟界，绝无第二套口径；行为逐字不变（`MAX_LEARN_LV` 现为 7，扫描 1..7 与旧 1..8 完全等价——`learnsAt(8)` 本就为 null、多扫的一级从不出结果）。
- 【零回归面】未动技能本身——`LEARN_AT` / `learnsAt` / `hero.checkSkills` 升级领悟 / `core.newGame` 建档起始技能逐字原样（本就同源）、`skillXpHint` 的经验预估公式（XP_GROW/XP_INIT）与返回值结构原样、状态页/技能菜单渲染只消费返回对象。未动任何掉落/经验/金币曲线/难度/技能效果/支线/成就/存档。只新增一个派生常量 + 改 1 个循环上界 + export 1 处 + import 1 处，零新增依赖、零 UI/战斗回归。
- 验证：`node --check js/data.js js/hero.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；专项冒烟（node ESM 直连）**4 项场景断言全过**——`MAX_LEARN_LV` 导出且 = LEARN_AT 最大领悟级 7（与 learnsAt 采样推导一致）、Lv1/Lv3/Lv5/Lv12 四种状态下「距下一技能」结果与旧上界 8 完全逐字相同（含 remain 经验）、全学会场景均返回 null、Lv12 超越旧上界仍正确返回 null。

## v19.0 帮助页「石心魔像·石甲」标注单一数据源——「至多 3 层」「血量过半后」改读 SPECIES 石心魔像 shield 招（机制·单一口径，与 SHIELD_MULT / HEAL_PCT / PHASE2_AT 同一「行招表数据化」家族——v14.9 收口 SHIELD_MULT 时声明保持原样的裸奔文案，本次收口收尾）

- 【`2` 帮助页一行内两处互不相关】帮助页「石心魔像·石甲」条：`血量过半后凝结石甲（至多 3 层）：每层使下一次攻击伤害 -40%`——其中「血量过半后」（= hpBelow 0.5）与「至多 3 层」（= maxShield 3）是裸字面量，而这两个数值的真正唯一真源是 `SPECIES['石心魔像'].acts` 的 shield 招（`maxShield:3,hpBelow:0.5`），且 `view/drawBattle.js` 的「敌方招数一览」早已动态读 `a.hpBelow`/`a.maxShield` 渲染「·血<50%时」「·至多3层」——帮助页与行招表两处互不相关：想调石甲强度（如层数上限提到 4、触发线改 0.4）要改两处、还极易只改行招表漏改帮助页，行招表改了帮助页却还报旧值；且该条内的 -40% 部分早已读 SHIELD_MULT，唯独触发线与层数是残存裸奔数值（v14.9 收口 SHIELD_MULT 时声明「此处保持原样」、v18.9 之后行招表数据化家族的最后一块短板）。
- 【修正：data.js 新增 `GOLEM_SHIELD_ACT = (SPECIES['石心魔像'].acts || []).find(a => a.type === 'shield') || {}` 为唯一真源（置于 SPECIES 之后、MON_BASE 之前，独立注释块）；帮助页「石心魔像·石甲」句改读模板串 `血量低至 ${hpBelow×100}% 后凝结石甲（至多 ${maxShield} 层）：每层使下一次攻击伤害 -40%`】收益：调石甲触发线/层数只改 SPECIES 石心魔像 shield 招一处，帮助页与战斗招数一览同步，绝无第二套口径；行为逐字不变（hpBelow=0.5 → 「血量低至 50% 后」、maxShield=3 → 「至多 3 层」，与旧文案「血量过半后…（至多 3 层）」语义逐字一致、数值逐字相同），零 UI/战斗回归。drawBattle 招数一览本就动态读行招表、无需改动。
- 【零回归面】未动石甲机制本身——`validator.enemyAct` 凝甲（`maxShield` 封顶、`hpBelow` 触发线）、`battle.attackMove` 石甲减伤（SHIELD_MULT）、`rules.withShield` 预览、`drawBattle` 石甲角标/招数一览全部原样（本就与行招表同源）。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只新增一个派生常量 + 改 1 行帮助页文案，零新增依赖、零新增 export（GOLEM_SHIELD_ACT 仅为装载期渲染帮助页所用，无需对外导出）。
- 验证：`node --check js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v19.0 专项冒烟（/tmp/jrpg_smoke_v19_shieldact.mjs）**6 项断言全过**——帮助页存在「石心魔像·石甲」条目、SPECIES 石心魔像 shield 招 maxShield=3 / hpBelow=0.5、渲染文本与历史文案「血量低至 50% 后凝结石甲（至多 3 层）：每层使下一次攻击伤害 -40%」逐字一致、改表（maxShield=4/hpBelow=0.4）后重装载帮助页文案联动为「40%/4 层」（证明真源联动）、源码可执行代码无裸「至多 3 层」/「血量过半后」（仅注释保留旧值说明）。

## v18.9 三强敌基础数值单一数据源——主线 BOSS/CAVE_BOSS/TRUE_BOSS 与试炼场 RUSH_BOSSES 的兵力六项同读 BOSS_BASE / CAVE_BOSS_BASE / TRUE_BOSS_BASE（机制·单一口径，与 RICH_GOLD / SAVE_SLOTS / POTION_CAP 同一「数值数据化」家族——v18.8 存档槽收口后，三强敌「主线一份 + 试炼一份」的两处兵力是当前最显眼的同值重复）

- 【`3` × 两处互不相关】三强敌（幽冥魔王 hp:140·atk:15·def:6、洞窟领主 hp:110·atk:16·def:10、终焉之神 hp:260·atk:22·def:13）的兵力数值各硬编码在两处：主线常量 `BOSS` / `CAVE_BOSS` / `TRUE_BOSS` 与试炼场复刻 `RUSH_BOSSES` 数组（试炼三连战的第 1/2/3 关就是这三强敌的原样兵力，仅 xp/gold 另计、加 isRush）——同一数值两处互不引用：想调强敌兵力（如把幽冥魔王 atk 提到 17）要改两处，还极易只改主线漏改试炼，两处强度悄然脱钩；且全库无任何现成源可借鉴（`SPECIES` 只存形态/弱点/动作等形态数据、`ELITE_GOLEM` 是独立的精英基准，均不含三强敌兵力），故必须单设基准表。
- 【修正：data.js 新增 `BOSS_BASE` / `CAVE_BOSS_BASE` / `TRUE_BOSS_BASE` 为唯一真源（置于 withSpecies 之后、`BOSS` 之前，独立注释块），各含 name/hp/hpMax/atk/def/color 六项】主线三常量改为 `withSpecies({...BASE, xp/gold/专属旗标})` 同源构建（`BOSS` 补 xp:150·gold:300·isBoss·bossHpMax、`CAVE_BOSS` 补 xp:120·gold:200·isElite·isCaveBoss、`TRUE_BOSS` 补 xp:400·gold:600·isTrue·isElite）；`RUSH_BOSSES` 三条改为 `withSpecies({...同源BASE, xp:60/60/90·gold:0·isRush})`。收益：调三强敌兵力只改 data.js 对应 BASE 一处、主线与试炼同步，绝无第二套口径；行为逐字不变（合并后对象与旧字面量逐键逐值一致——专项冒烟 17 项断言逐键比对通过，`isBoss`/`bossHpMax`/`isCaveBoss`/`isTrue` 等专属旗标仅存在于主线版、试炼版不带，不触发 bossDefeated/红字伤害/必掉剑 等主线专属判定），零 UI/战斗回归。
- 【零回归面】未动战斗机制本身——`SPECIES` 三强敌的形态/弱点/抗性/动作/相位二仍经 withSpecies 原样并入（`phase2`/`resist`/`acts`/`tag` 均不变）、`rules.js` `BOSS_DEFS` 直读 BOSS/CAVE_BOSS/TRUE_BOSS 照旧、`battle.js` 试炼流程（isRush 判定/RUSH_RECOVER 恢复/经验另计）、`drawBattle.js` 试炼进度与 Boss 栏、`sprites.js` 体型缩放全部原样。未动任何掉落/经验/金币曲线/难度/技能/支线/成就/存档。只新增三个基准常量 + 改 6 行构建表达式 + 删 3 行冗余字面量，零新增依赖。
- 验证：`node --check js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v18.9 专项冒烟（/tmp/jrpg_smoke_v189_bossbase.mjs）**17 项断言全过**——三个主线 Boss 与三条试炼复刻合并后对象逐键逐值等于旧字面量（含 SPECIES 并入的 phase2/resist/acts/tag）、主线与试炼 hp/atk/def 同源、试炼版不带 isBoss/bossHpMax/isCaveBoss/isTrue（不触发暴毙/必掉剑等主线专属判定）、试炼 xp/gold 减值逐字、主线 xp/gold 原值、`BOSS` 总键数与旧字面量一致（无多无少）。

## v18.8 存档槽数量 3 单一数据源——SAVE_SLOTS 收口「标题选槽主键 / 选槽条 / 快捷键一览 / 帮助文案」四处（机制·单一口径，与 SAVE_SLOTS 同名的隔壁常量无关，属 POTION_CAP / MUSHROOM_GOAL / INN_PRICE 同一「数值数据化」家族）

- 【`4` 四处互不相关】存档槽数量 3 硬编码在：`main.js` 标题页选槽主键（`e.key==='1'/'2'/'3'` 三条分支各设 `S.curSaveSlot`，想加档要加分支 + 还极易只加一处漏另几处）；`view/menus.js` 标题页选槽条（`[1,2,3].map(...)` 循环索引）；`view/menus.js` 标题页快捷键一览（字符串字面量「按 1 / 2 / 3 选择存档槽」）；`data.js` `HELP_PAGES`（「标题按 1/2/3 选择 · L 读档」）。四处互不引用：把档数加到 4 要改四处，且文案极易只改一半（界面选槽条多了 4 号槽、帮助/快捷键却还写 1/2/3）；且全库无任何现成源可借鉴（`core.js` 的 `saveKey/hasSlot/slotPreview/saveGame/load` 全部按「传入槽号」（`SAVE_SLOTS` 前的裸槽号参数）记账，从不迭代 1..3——纯 UI/分派层的重复），故必须单设常量。
- 【修正：data.js 新增 `SAVE_SLOTS = 3` 为唯一真源（置于药水恢复量块之后、`SPECIES` 之前，独立注释块）】四处同读：`main.js` 标题选槽键改为 `/^[1-9]$/` + `Number(e.key) <= SAVE_SLOTS` 数据驱动分派；`view/menus.js` 选槽条改为 `Array.from({length:SAVE_SLOTS},(_,i)=>i+1)` 生成、快捷键一览改为模板串 `按 ${slotNums.join(' / ')}`；`data.js` `HELP_PAGES` 存档槽条改为 `'标题按 1/2/' + SAVE_SLOTS + ' 选择 · L 读档'`，并加入 export。收益：调档数只改 data.js 一处、四处同步，绝无第二套口径；行为逐字不变（`SAVE_SLOTS` 仍为 3，选槽条「槽1 ▶槽2◀ 槽3」、快捷键「按 1 / 2 / 3 选择存档槽」、帮助「标题按 1/2/3 选择」三处文案原样），零 UI 回归。
- 【零回归面】未动存档机制本身——`core.js` 的 `saveKey/hasSlot/slotPreview/saveGame/load/resetRun` 逐字不变（本就按槽号参数化、无 1..3 迭代）、`view/hud.js` 「槽N」展示原样、`view/menus.js` 读档/续档流程（`hasSave/slotPreview/curSaveSlot`）原样、`state.js` `curSaveSlot` 字段原样。未动任何掉落/经验/金币曲线/难度/技能/支线/成就。只新增一个常量 + 改两处 UI 生成逻辑 + 改一处键分派 + 改一处帮助文案 + export 1 处，零新增依赖。
- 验证：`node --check js/data.js js/view/menus.js js/main.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v18.8 专项冒烟（/tmp/jrpg_smoke_v188_saveslots.mjs）**29 项断言全过**——`SAVE_SLOTS` 导出且为 3、帮助文案逐字、选槽键谓词 1/2/3 命中而 4/5/9/0/字母/Enter 不命中（原有行为不变）、`slotNums` 生成 [1,2,3] 且 join 「1 / 2 / 3」、选槽条格式逐字、源码 grep 确认可执行代码裸 `curSaveSlot = 1/2/3` 三分支 / `[1,2,3]` / 「按 1 / 2 / 3 选择存档槽」/「标题按 1/2/3 选择」已清零（仅注释保留旧值说明）、export 含常量、同族常量（POTION_CAP/MUSHROOM_GOAL）未动。

## v18.7 成就阈值单一数据源——「初露锋芒」1 判定/进度两处同读 FIRSTBLOOD_GOAL（机制·单一口径，与 RICH_GOLD / SCHOLAR_GOAL / LUCKY_GOAL / HUNT_GOAL / LVL5_GOAL / LVL10_GOAL 同一「成就阈值数据化」家族——v18.5 收口 hunt10 时注释已预告同计数源的 firstblood 属再下批对象、当时刻意保留其裸 1，本次收口后 ACH_LIST 全部数值型门槛一律单源化）

- 【`2` 同一条目内两处互不相关】「初露锋芒」成就：累计赢得 1 场战斗。这个 1 硬编码在 `data.js` `ACH_LIST` 该条的判定（`ok` 的 `>= 1`）、进度（`prog` 的 `X/1`）两处——同对象内互不引用：想调门槛（如放宽到 3 场）要改两处，还极易只改判定漏改进度，实际达标线变了界面却还标「/1」；且全库无任何现成源可借鉴（陪跑同计数源的 hunt10 已有 `HUNT_GOAL`，但那是 10 场的独立门槛，无 1 场常量），故必须单设常量——正是 v18.5 注释里预告的下一个收口对象（「同计数源 firstblood 的裸 1 属再下批对象刻意保留」）。注：该条描述 d「赢得第一场战斗」无数字、无需读数，故只收口判定/进度两处，与其余各条「三处同读」的结构略有不同（事件型 9 条则本无 prog）。
- 【修正：data.js 新增 `FIRSTBLOOD_GOAL = 1` 为唯一真源（置于 LVL10_GOAL 之后、药水恢复量块之前，紧邻「成就阈值」注释块）】`ACH_LIST` 的 firstblood 条 `ok`（`>= FIRSTBLOOD_GOAL`）、`prog`（`/${FIRSTBLOOD_GOAL}`）两处同读此源，并加入 export。收益：调「初露锋芒」门槛只改 data.js 一处、判定/进度同步，绝无第二套口径；行为逐字不变（`FIRSTBLOOD_GOAL` 仍为 1，文案原样），零 UI 回归。
- 【零回归面】未动成就机制本身——`ACH_LIST` 其余 16 条逐字不变（事件型 9 条本无 prog、数值型余 7 条各自取已有常量）、`view/menus.js` 成就页只读 `ok/name/d/prog`（`解锁 X/${ACH_LIST.length}` 照旧）、`rules.unlockedAchievements` 判定流程、胜负计数 `totalWins` 原样。未动任何掉落/经验/金币曲线/难度/技能/支线/存档。只新增一个常量 + 改一个条目（1 行）+ export 1 处，零新增依赖。
- 验证：`node --check js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v18.7 专项冒烟（/tmp/jrpg_smoke_v187_firstblood.mjs）**36 项断言全过**——`FIRSTBLOOD_GOAL` 导出且为 1、firstblood 判定逐字（0 胜未达标 / 1·2 胜达标 / 无 totalWins 兜底 false / null 明确未达标）、`prog` 0/1·1/1·5/1·12/1 逐字、`d` 与旧字面量「赢得第一场战斗」逐字一致、`ok/prog.toString()` 各含且各恰 1 处 `FIRSTBLOOD_GOAL` 标识符、源码 firstblood 行恰含 2 处常量且可执行代码裸 `>=1` / `/1` 已清零（仅注释保留旧值说明）、export 含常量、同族常量（RICH_GOLD/SCHOLAR_GOAL/LUCKY_GOAL/HUNT_GOAL/LVL5_GOAL/LVL10_GOAL）未动、抽验 `hunt10/lucky/lvl5/lvl10/rich/scholar` 判定不受影响、`ACH_LIST` 17 条结构不变（数值型 8 条含 prog、事件型 9 条无 prog）。

## v18.6 成就阈值单一数据源——「独当一面」5 与「守灯者」10 的双等级阈值同族收口（机制·单一口径，与 RICH_GOLD / SCHOLAR_GOAL / LUCKY_GOAL / HUNT_GOAL 同一「成就阈值数据化」家族——v18.4 注释已预告 lvl5/lvl10 的裸 5/10 属下批对象、当时刻意保留，本次一并收口同属等级口径的两条）

- 【`6` 两条目各自内部三处互不相关】「独当一面」成就：等级达到 5 级；「守灯者」成就：等级达到 10 级。这两个门槛分别硬编码在 `data.js` `ACH_LIST` 各自条目的判定（`ok` 的 `level>=5` / `level>=10`）、描述（`d` 的「等级达到 5 级」/「等级达到 10 级」）、进度（`prog` 的 `X/5` / `X/10`）三处——同对象内互不引用：想调门槛（如守灯者放宽到 12 级）要改三处，还极易只改判定漏改文案，实际达标线变了界面却还标「/10」；且全库无任何现成源可借鉴（等级上限仅由 `baseStats` / `XP_GROW` 曲线隐含，无成就门槛常量），故必须单设常量。两条同为等级口径、结构逐字同形，故一并收口为同一批次。
- 【修正：data.js 新增 `LVL5_GOAL = 5` 与 `LVL10_GOAL = 10` 为唯一真源（置于 HUNT_GOAL 之后、药水恢复量块之前，紧邻「成就阈值」注释块）】`ACH_LIST` 的 lvl5 条 `ok`（`level>=LVL5_GOAL`）、`d`（模板串「等级达到 ${LVL5_GOAL} 级」）、`prog`（`/${LVL5_GOAL}`），lvl10 条同型三处，并加入 export。收益：调等级门槛只改 data.js 一处、判定/描述/进度同步，绝无第二套口径；行为逐字不变（`LVL5_GOAL` 仍为 5、`LVL10_GOAL` 仍为 10，两条文案原样），零 UI 回归。
- 【零回归面】未动成就机制本身——`ACH_LIST` 其余 15 条逐字不变（`firstblood` 的裸 1 与 v18.5 同一延续策略、本次刻意保留为再下批对象）、`view/menus.js` 成就页只读 `ok/name/d/prog`（`解锁 X/${ACH_LIST.length}` 照旧）、`unlockedAchievements` 判定流程、存档等级字段原样。未动任何掉落/经验/金币曲线/难度/技能/支线/存档。只新增两个常量 + 改两个条目（各 1 行）+ export 1 处，零新增依赖。
- 验证：`node --check js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v18.6 专项冒烟（/tmp/jrpg_smoke_v186_lvlgoal.mjs）**32 项断言全过**——`LVL5_GOAL`/`LVL10_GOAL` 导出且为 5/10、lvl5 判定逐字（4 级未达标 / 5·6 级达标 / 无 level 兜底 false）、`prog` 1/5·3/5·5/5·8/5 与 1/10·7/10·10/10·15/10 逐字、`d` 与旧字面量「等级达到 5 级」/「等级达到 10 级」逐字一致、`ok/prog.toString()` 各含各自常量标识符、源码两条目行为均为模板串（`${LVL5_GOAL}`/`${LVL10_GOAL}` 各恰 2 处 : d/prog，ok 为裸标识符判定）、源码 grep 确认可执行代码裸 `>=5` / `/5` / `等级达到 5 级`、`>=10` / `/10` / `等级达到 10 级` 已清零（仅注释保留旧值说明）、export 含两常量、抽验 `rich/scholar/lucky/hunt10/firstblood` 判定不受影响、`ACH_LIST` 17 条结构不变。firstblood 的裸 1 属再下批对象刻意保留。


## v18.5 成就阈值单一数据源——「驱雾十战」10 判定/描述/进度三处同读 HUNT_GOAL（机制·单一口径，与 RICH_GOLD / SCHOLAR_GOAL / LUCKY_GOAL 同一「成就阈值数据化」家族——v18.4 注释已预告的 hunt10 属下批对象，当时刻意保留其裸 10）

- 【`3` 同一条目内三处互不相关】「驱雾十战」成就：累计讨伐 10 只魔物。这个 10 硬编码在 `data.js` `ACH_LIST` 该条的判定（`ok` 的 `>= 10`）、描述（`d` 的「累计讨伐 10 只魔物」）、进度（`prog` 的 `X/10`）三处——同对象内互不引用：想调门槛（如放宽到 15 只）要改三处，还极易只改判定漏改文案，实际达标线变了界面却还标「/10」；且全库无任何现成源可借鉴（累计讨伐仅 `totalWins` 计数、`view/menus.js` 只展示当前值，无成就门槛常量），故必须单设常量——正是 v18.4 注释里预告的下一个收口对象（「lvl5/lvl10/hunt10/firstblood 的裸 5/10/1 属分批收口的下批对象刻意保留」）。
- 【修正：data.js 新增 `HUNT_GOAL = 10` 为唯一真源（置于 LUCKY_GOAL 之后、药水恢复量块之前，紧邻「成就阈值」注释块）】`ACH_LIST` 的 hunt10 条 `ok`（`>= HUNT_GOAL`）、`d`（模板串「累计讨伐 ${HUNT_GOAL} 只魔物」）、`prog`（`/${HUNT_GOAL}`）三处同读此源，并加入 export。收益：调「驱雾十战」门槛只改 data.js 一处、判定/描述/进度同步，绝无第二套口径；行为逐字不变（`HUNT_GOAL` 仍为 10，文案原样），零 UI 回归。
- 【零回归面】未动成就机制本身——`ACH_LIST` 其余 16 条逐字不变（`firstblood` 的裸 1 与 `lvl5`/`lvl10` 的裸 5/10 同属下批对象、刻意保留未纳入本次以控制改动面，`lucky`/`rich`/`scholar`/`perfection` 已是模板串继续原样）、`view/menus.js` 成就页只读 `ok/name/d/prog`（`解锁 X/${ACH_LIST.length}` 照旧）、`rules.unlockedAchievements` 判定流程、胜负计数 `totalWins` 原样。未动任何掉落/经验/金币曲线/难度/支线/存档。只新增一个常量 + 改一个条目（1 行）+ export 1 处，零新增依赖。
- 验证：`node --check js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v18.5 专项冒烟（/tmp/jrpg_smoke_v185_huntgoal.mjs）**28 项断言全过**——`HUNT_GOAL` 导出且为 10、hunt10 判定逐字（9 只未达标 / 10·11 只达标 / 无 totalWins 兜底 false / totalWins=0 明确未达标）、`prog` 0/10·3/10·10/10·15/10 逐字、`d` 与旧字面量「累计讨伐 10 只魔物」逐字一致、`ok/prog.toString()` 各含且各恰 1 处 `HUNT_GOAL` 标识符、源码 hunt10 行为 `${HUNT_GOAL}` 模板串且无裸「累计讨伐 10 只魔物」、抽验 `firstblood/lucky/lvl5/lvl10/rich/scholar/perfection` 判定不受影响、`ACH_LIST` 17 条结构不变、同族常量（RICH_GOLD/SCHOLAR_GOAL/LUCKY_GOAL）未动。


## v18.4 成就阈值单一数据源——「幸运眷顾」5 判定/描述/进度三处同读 LUCKY_GOAL（机制·单一口径，与 RICH_GOLD / SCHOLAR_GOAL 同一「成就阈值数据化」家族——v18.3 注释已预告同族的 lucky 是下批对象，当时刻意保留其裸 5）

- 【`3` 同一条目内三处互不相关】「幸运眷顾」成就：累计获得 5 次额外掉落。这个 5 硬编码在 `data.js` `ACH_LIST` 该条的判定（`ok` 的 `>= 5`）、描述（`d` 的「累计获得 5 次额外掉落」）、进度（`prog` 的 `X/5`）三处——同对象内互不引用：想调门槛（如放宽到 8 次）要改三处，还极易只改判定漏改文案，实际达标线变了界面却还标「/5」；且全库无任何现成源可借鉴（`drops` 仅 `rules.js` 掉落时 `+1` 计数、`view/menus.js` 状态页展示，无成就门槛常量），故必须单设常量——正是 v18.3 注释里预告的下一个收口对象（「lucky 的裸 5 属分批收口的下批对象刻意保留」）。
- 【修正：data.js 新增 `LUCKY_GOAL = 5` 为唯一真源（置于 SCHOLAR_GOAL 之后、药水恢复量块之前，紧邻「成就阈值」注释块）】`ACH_LIST` 的 lucky 条 `ok`（`>= LUCKY_GOAL`）、`d`（模板串「累计获得 ${LUCKY_GOAL} 次额外掉落」）、`prog`（`/${LUCKY_GOAL}`）三处同读此源，并加入 export。收益：调「幸运眷顾」门槛只改 data.js 一处、判定/描述/进度同步，绝无第二套口径；行为逐字不变（`LUCKY_GOAL` 仍为 5，文案原样），零 UI 回归。
- 【零回归面】未动成就机制本身——`ACH_LIST` 其余 16 条逐字不变（`firstblood`/`hunt10`/`lvl5`/`lvl10` 等数值型条目各取其旧字面量不动，与 v18.2/v18.3 同一「分批收口」策略、未纳入本次以控制改动面）、`view/menus.js` 成就页只读 `ok/name/d`（`解锁 X/${ACH_LIST.length}` 照旧）、`rules.unlockedAchievements` 判定流程、掉落计数（`hero.drops` 五处 +1）、状态页「额外掉落」展示逐字不变。未动任何掉落/经验/金币曲线/难度/支线/存档。只新增一个常量 + 改一个条目（1 行）+ export 1 处，零新增依赖。
- 验证：`node --check js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v18.4 专项冒烟（/tmp/jrpg_smoke_v184_luckygoal.mjs）**31 项断言全过**——`LUCKY_GOAL` 导出且为 5、lucky 判定逐字（4 次未达标 / 5·6 次达标 / 无 drops 字段兜底 false / drops=0 明确未达标）、`prog` 0/5·2/5·5/5·6/5 逐字、`d` 与旧字面量「累计获得 5 次额外掉落」逐字一致、源码 lucky 行 `${LUCKY_GOAL}` 恰 2 处且 LUCKY_GOAL 恰 3 处、`ok/prog.toString()` 各含且各恰 1 处 `LUCKY_GOAL` 标识符、抽验 `firstblood/hunt10/lvl5/lvl10/rich/scholar/perfection` 判定不受影响、`ACH_LIST` 17 条结构不变；源码 grep 确认 lucky 条目可执行代码裸 `>=5` / `/5` / `累计获得 5 次` 已清零（仅注释保留旧值说明），lvl5/lvl10/hunt10/firstblood 的裸 5/10/1 属分批收口的下批对象刻意保留。

## v18.3 成就阈值单一数据源——「记忆收藏家」5 种判定/描述/进度三处同读 SCHOLAR_GOAL（机制·单一口径，与 RICH_GOLD 同一「成就阈值数据化」家族——v18.2 收口「小富翁」后同类里残存的裸奔数值之一）

- 【`1` 同一条目内三处互不相关】「记忆收藏家」成就：记忆图鉴收录 5 种魔物。这个 5 硬编码在 `data.js` `ACH_LIST` 该条的判定（`ok` 的 `>= 5`）、描述（`d` 的「收录 5 种魔物」）、进度（`prog` 的 `X/5`）三处——同对象内互不引用：想调门槛（如放宽到 6 种）要改三处，还极易只改判定漏改文案，实际达标线变了界面却还标「/5」；且全库无任何现成源可借鉴（`bestiary` 仅有 `BESTIARY_TARGET` 全量 13 种表，是「全收集」口径而非「收录 5 种」的入门门槛，`perfection` 早已读前者、与 scholar 不是一回事），故必须单设常量——正是 v18.2 注释里预留的下一个收口对象（当时「各取其旧字面量不动，未纳入本次收口以控制改动面」）。
- 【修正：data.js 新增 `SCHOLAR_GOAL = 5` 为唯一真源（置于 RICH_GOLD 之后、药水恢复量块之前，紧邻「成就阈值」注释块）】`ACH_LIST` 的 scholar 条 `ok`（`>= SCHOLAR_GOAL`）、`d`（模板串「收录 ${SCHOLAR_GOAL} 种魔物」）、`prog`（`/${SCHOLAR_GOAL}`）三处同读此源，并加入 export。收益：调「记忆收藏家」门槛只改 data.js 一处、判定/描述/进度同步，绝无第二套口径；行为逐字不变（`SCHOLAR_GOAL` 仍为 5，文案原样），零 UI 回归。
- 【零回归面】未动成就机制本身——`ACH_LIST` 其余 16 条逐字不变（`firstblood`/`hunt10`/`lucky`/`lvl5`/`lvl10`/`rich` 等数值型条目各取其旧字面量不动，与 v18.2 同一「分批收口」策略、未纳入本次以控制改动面）、`view/menus.js` 成就页只读 `ok/name/d`（`解锁 X/${ACH_LIST.length}` 照旧）、`rules.unlockedAchievements` 判定流程、存档/进度字段原样。未动任何掉落/经验/金币曲线/难度/支线/存档。只新增一个常量 + 改一个条目（1 行）+ export 1 处，零新增依赖。
- 验证：`node --check js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v18.3 专项冒烟（/tmp/jrpg_smoke_v183_scholar.mjs）**24 项断言全过**——`SCHOLAR_GOAL` 导出且为 5、scholar 判定逐字（收录 4 种未达标 / 5·6 种达标 / 无 bestiary 兜底 false）、`prog` 0/5·2/5·5/5·6/5 逐字、`d` 与旧字面量「记忆图鉴收录 5 种魔物」逐字一致、`ok/prog.toString()` 均含且各恰 1 处 `SCHOLAR_GOAL` 标识符、抽验 `firstblood/hunt10/lucky/lvl5/lvl10/rich/perfection` 判定不受影响；源码 grep 确认 scholar 条目可执行代码裸 `>=5` / `/5` / `收录 5 种` 已清零（仅注释保留旧值说明），lucky/lvl5 的裸 5 属分批收口的下批对象刻意保留。

## v18.2 成就阈值单一数据源——「小富翁」500 判定/描述/进度三处同读 RICH_GOLD、「记忆守护者」描述改读 BESTIARY_TARGET.length（机制·单一口径，与 MIST_GOAL / MUSHROOM_GOAL / POTION_PRICE 同一「数值数据化」家族）

- 【`2` 两条成就各自内部多处互不相关】①「小富翁」持有 500 金币：500 硬编码在 `data.js` `ACH_LIST` 该条的判定（`ok` 的 `>= 500`）、描述（`d` 的「持有 500 金币」）、进度（`prog` 的 `X/500`）三处——同对象内互不引用：想调门槛（如放宽到 800）要改三处，还极易只改判定漏改文案，实际达标线变了界面却还标「/500」；且全库无任何现成源（gold 相关常量仅 `DROP_GOLD=60` / `TRUE_BONUS_GOLD=300`，都不是成就门槛）。②「记忆守护者」收录全部魔物：`prog` 分母早已读 `BESTIARY_TARGET.length`、状态/图鉴页也读同源，唯独 `d` 描述「全部 13 种魔物」是裸 13——想给 `BESTIARY_TARGET` 增删一种（如新增精英残焰魔像已 13 种），判定与进度自动跟随，描述却还标旧数，正是 v18.1 同款「判定收口、文案裸奔」的漂移隐患。
- 【修正：data.js 新增 `RICH_GOLD = 500` 为唯一真源（置于 TRUE_BONUS_GOLD 之后、药水恢复量块之前）】`ACH_LIST` 的 rich 条 `ok`（`gold>=RICH_GOLD`）、`d`（模板串「持有 ${RICH_GOLD} 金币」）、`prog`（`/${RICH_GOLD}`）三处同读此源，并加入 export；`perfection` 条 `d` 改读 `BESTIARY_TARGET.length`（与同条 `ok`/`prog`、`view/menus.js` 图鉴页「记忆收录 X/N」同一真源，无需另设常量）。收益：调成就门槛只改 data.js 一处、判定/描述/进度同步，给图鉴增删魔物描述自动跟随，绝无第二套口径；行为逐字不变（`RICH_GOLD` 仍为 500、`BESTIARY_TARGET.length` 现为 13，两处文案原样），零 UI 回归。
- 【零回归面】未动成就机制本身——`ACH_LIST` 结构与其余 15 条逐字不变（`firstblood`/`hunt10`/`lvl5`/`lvl10`/`lucky`/`scholar` 等门槛不同值、各取其旧字面量不动，未纳入本次收口以控制改动面）、`view/menus.js` 成就页只读 `ok/name/d`（`解锁 X/${ACH_LIST.length}` 照旧）、`rules.unlockedAchievements` 判定流程、存档/进度字段原样。未动任何掉落/经验/金币曲线/难度/支线/存档。只新增一个常量 + 改两个条目（各 1 行）+ export 1 处，零新增依赖。
- 验证：`node --check js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v18.2 专项冒烟（/tmp/jrpg_smoke_v182_richgold.mjs）**21 项断言全过**——`RICH_GOLD` 导出且为 500、rich 判定逐字（499 未达标 / 500·501 达标 / 无 gold 字段兜底 false）、`prog` 0/500·123/500·500/500·800/500 逐字、`d` 与旧字面量「持有 500 金币」逐字一致、`perfection.d` 与「全部 13 种魔物」逐字一致且分母同源 13、`rich.ok/prog.toString()` 均含 `RICH_GOLD` 标识符、抽验 `firstblood/hunt10/legend` 判定不受影响；源码 grep 确认两条目均已是模板串（`${RICH_GOLD}` / `${BESTIARY_TARGET.length}`）且可执行代码裸 `>=500` / `/500` / `持有 500 金币` / `全部 13 种` 已清零（仅注释保留旧值说明）。

## v18.1 旧灯卫名字支线目标枚数单一数据源——「旧灯卫的名字」收集条件/实时进度/目标文案/接取对话同读 FRAGMENTS.length（机制·单一口径，与 MIST_GOAL / MUSHROOM_GOAL 同一「支线目标单一数据源」家族，并修正一处虚标注释）

- 【`4` 同一支线定义内四处互不相关，且注释虚标单一数据源】巡灯人支线「旧灯卫的名字」目标 = 集齐 4 枚记忆碎片：这个 4 硬编码在 `data.js` `QUESTS.side_name` 的收集条件（`cond` 的 `>= 4`）、实时进度（`condProg` 的 `X/4 枚`）、任务条目标文案（`obj` 的「集齐 4 枚…」）、接取对话（`offer` 的「凑齐 4 枚…」）四处——四处互不引用：想增减记忆碎片（`FRAGMENTS` 增删一段，如加一枚龙王碎片改成 5 枚）要改四个字符串，还极易只改判定漏改文案，集齐 5 枚界面却还标「/4 枚」；且该定义上方注释自称「cond 单一数据源 FRAGMENTS.length」实为**虚标**——代码里其实全是裸 4，从未真正读 `FRAGMENTS.length`；而状态页「记忆 X/4」（`view/menus.js`）却早已是 `FRAGMENTS.length` 真源，一处已收口、一处裸奔，两套口径并存。
- 【修正：`QUESTS.side_name` 四处全部改读 `FRAGMENTS.length`（唯一真源 = 记忆碎片数据表本身，无需另设常量）】`cond`（`>= FRAGMENTS.length`）、`condProg`（`/${FRAGMENTS.length} 枚`）、`obj`（模板串「集齐 ${FRAGMENTS.length} 枚…」）、`offer`（模板串「凑齐 ${FRAGMENTS.length} 枚…」）四处同源；并把虚标注释改写成如实描述（目标枚数 = 数据表长度、与状态页/真结局加页同一真源）。收益：增删 `FRAGMENTS` 一段（改掉落表即改支线目标），下次装载后 条件/进度/文案/状态页 自动同步，绝无第二套口径；行为逐字不变（`FRAGMENTS.length` 现为 4，全部文案原样），零 UI 回归。`turnin` 的「四段都在这了。守门人、灯卫、星砂、初灯……」系逐字点名四段碎片的枚举性旁白（并非「N 枚」式数值引用，且已手动列全四个名字），保持文案原样不纳入收口。
- 【零回归面】未动支线机制本身——任务状态机（`bossDefeated` 解锁自动 active → 集齐转 turnin → 对话交付 done）、`quests.js` 的 `questStatus/questJournal/resolveNpcTalk/applyQuestReward` 只读 `QUESTS` 字段、碎片掉落（`battle.winBattle` 按 enemy 从 `FRAGMENTS` 查找）、真结局加页、名字石碑（`FRAGMENTS.forEach` 生成 `NPCS.steleN`）逐字不变；奖励（120 金 + 1 灵药）原样。未动任何掉落/经验/金币曲线/难度/成就/存档。只改 data.js 同一支线定义内 4 个引用点 + 1 处注释，零新增依赖、零新增常量。
- 验证：`node --check js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v18.1 专项冒烟（/tmp/jrpg_smoke_v181_namefrag.mjs）**27 项断言全过**——`FRAGMENTS` 导出且现长 4、`cond` 恰好 FRAGMENTS.length 枚达成（3 枚 false / 4 枚 true / 无碎片兜底 false）、`condProg` 0/4·2/4·4/4 枚逐字、`obj/offer` 数据层与旧字面量逐字一致（offer 为防御性保留文案，直接校验数据源）、`turnin` 枚举旁白逐字、真实任务流端到端（auto active → 3 枚仍 active 且日志「3/4 枚」→ 4 枚转 turnin → 对话交付发 120 金+1 灵药 → done）、运行期 `FRAGMENTS.push` 增段后 cond/condProg 即刻跟随（5 枚门槛）再 pop 复原、grep 确认 data.js 可执行代码裸 `>= 4` / `/4 枚` / `集齐 4 枚` / `凑齐 4 枚` 已清零、`obj/offer` 为 `${FRAGMENTS.length}` 模板串（装载期同源）、`FRAGMENTS` 仍在 export（状态页同源）。

## v18.0 雾灵委托目标只数单一数据源——「雾里的新住客」讨伐条件/实时进度/目标文案/接取与交付对话同读 MIST_GOAL（机制·单一口径，与 MUSHROOM_GOAL 同一「支线目标单一数据源」家族）

- 【`3` 同一支线定义内五处互不相关】雾径猎手支线「雾里的新住客」目标 = 讨伐 3 只雾灵：这个 3 硬编码在 `data.js` `QUESTS.side_mist` 的讨伐条件（`cond` 的 `>= 3`）、实时进度（`condProg` 的 `X/3 只`）、任务条目标文案（`obj` 的「讨伐 3 只…」）、接取对话（`offer` 的「帮我打 3 只回来」）、交付对话（`turnin` 的「3 只，干净利落」）五处——同对象内互不引用：想调支线目标（如改成 4 只）要改五个字符串，还极易只改判定漏改文案，集齐 4 只界面却还标「/3 只」；且写着「3」，也无从考证这数从哪来——同族的灯长蘑菇支线目标早已收口（`MUSHROOM_GOAL=3`），唯独自家支线目标是支线数据里残存的裸奔数值。
- 【修正：data.js 新增 `MIST_GOAL = 3` 为唯一真源（紧邻 MUSHROOM_GOAL，同属「支线目标」常量块）】`QUESTS.side_mist` 的 `cond`（`>= MIST_GOAL`）、`condProg`（`${MIST_GOAL} 只`）、`obj`（模板串「讨伐 ${MIST_GOAL} 只…」）、`offer`（模板串「帮我打 ${MIST_GOAL} 只回来」）、`turnin`（模板串「${MIST_GOAL} 只，干净利落」）五处同读此源，并加入 export。收益：调雾灵支线目标只改 data.js 一处、判定/进度/文案五处同步，绝无第二套口径；行为逐字不变（`MIST_GOAL` 仍为 3，全部文案原样），零 UI 回归。
- 【零回归面】未动支线机制本身——任务状态机（`offer→active→turnin→done`）、`quests.js` 的 `questStatus/questJournal/resolveNpcTalk/applyQuestReward` 只读 `QUESTS` 字段、bestiary 雾灵计数（`scaleEnemy`/`winBattle` 收录）、奖励（60 金 + 1 药水）、接取/交付流程（`npcQuestPages` 选页）逐字不变；`MUSHROOM_GOAL` 及蘑菇支线、其余三条支线、掉落/经验/金币曲线/难度/成就/存档原样。只新增一个常量 + 改同文件 5 个引用点 + export 1 处，零新增依赖。
- 验证：`node --check js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v18.0 专项冒烟（/tmp/jrpg_smoke_v180_mistgoal.mjs）**23 项断言全过**——常量导出 3、`cond` 恰好 MIST_GOAL 只达成（2 只 false / 3 只 true / 无 bestiary 兜底 false）、`condProg` 2/3·3/3·0/3 只逐字、`obj/offer/turnin` 三处文案与旧字面量逐字一致、真实任务流端到端（接取写 active → 2 只仍 active 且日志「2/3 只」→ 3 只转 turnin → 对话交付发 60 金+1 药水 → done）、grep 确认 data.js 可执行代码裸 `>= 3` / `/3 只` / `讨伐 3 只` / `帮我打 3 只` / `雾径猎手：3 只` 已清零且五处同读 MIST_GOAL、全文件 8 处引用（1 定义 + 1 export + 5 支线 + 1 注释）。

## v17.9 蘑菇出售单价单一数据源——shop.sellMushroom 卖菇结账/得金提示、列表卖出价签三处同读 MUSHROOM_PRICE（机制·单一口径，与 INN_PRICE / BREW_GOLD / POTION_PRICE 同一「价格数据化」体系）

- 【`10` 同一文件三处互不相关】卖 1 株魔法蘑菇得 10 金：这个 10 硬编码在 `shop.js` `sellMushroom` 卖菇结账（`hero.gold += 10`）与成功提示（`'售出 1 株魔法蘑菇，得 10 金'`）、`buildShopList` 商店列表卖菇行价签（`'卖出魔法蘑菇 ×1 → 10金'`）三处——三处互不引用：想调卖菇价（如涨到 15）要改三行，还极易只改结账漏改价签/提示，实际卖价改了列表价签却还标旧值、扣钱账目也对不上光标；且写着「10」，也无从考证这数从哪来——同族的价格早已收口（旅馆 `INN_PRICE=10`、酿造 `BREW_GOLD=10`、药水 `POTION_PRICE=15`），唯独卖菇价是经济循环里残存的裸奔数值。
- 【修正：data.js 新增 `MUSHROOM_PRICE = 10` 为唯一真源（置于 MUSHROOM_GOAL 之后，同属「灯长蘑菇支线」常量块）】`shop.js` `sellMushroom` 结账改读 `hero.gold += MUSHROOM_PRICE`、成功提示改读模板串 `售出 1 株魔法蘑菇，得 ${MUSHROOM_PRICE} 金`；`buildShopList` 卖出价签改读 `卖出魔法蘑菇 ×1 → ${MUSHROOM_PRICE}金`。收益：调卖菇价只改 data.js 一处、结账/提示/价签三处同步，绝无第二套口径；行为逐字不变（`MUSHROOM_PRICE` 仍为 10，提示/价签文案原样），零 UI 回归。
- 【零回归面】未动卖菇机制本身——任务保护拦截（`canSellMushroom` + `mushroomQuestProtects` 判定 `side_mushroom` 为 active/turnin 且 `mushrooms <= MUSHROOM_GOAL`、拦截提示「灯长委托的蘑菇」）优先、卖菇成功流程（扣 1 株、+10 金、`售出` 提示、`renderHUD`）逐字不变；`MUSHROOM_GOAL `支线目标、酿造配方（`BREW_MUSHROOMS/BREW_GOLD`）、药水价格（`POTION_PRICE`）与商店其余行（药水/武器/防具/离开）原样。未动任何掉落/经验/金币曲线/难度/成就/存档。只新增一个常量 + 改三个引用点（shop 一处 import + 三行引用），零新增依赖。
- 验证：`node --check js/data.js js/shop.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v17.9 专项冒烟（/tmp/jrpg_smoke_v179_mushroomprice.mjs）**15 项断言全过**——常量导出 10、可卖/保护期列表行（blocked 与文案）逐字正确、真实 `sellMushroom` 端到端（mushrooms 4→3、gold 0→10、提示「得 10 金」）、保护期拦截（active 态 mushrooms/gold 不动、提示引用 MUSHROOM_GOAL）、grep 确认 shop.js 可执行代码裸 `+= 10` / `得 10 金` / `→ 10金` 已清零且三处同读 MUSHROOM_PRICE。

## v17.8 生命药水购买价单一数据源——buyPotion 购买判定/扣款、商店列表价签三处同读 POTION_PRICE（机制·单一口径，与 INN_PRICE / BREW_GOLD / POTION_CAP 同一「价格数据化」体系）

- 【`15` 同一文件三处互不相关】生命药水购买价 = 15 金币：这个 15 硬编码在 `shop.js` `buyPotion` 购买判定（`hero.gold >= 15`）与扣款（`hero.gold -= 15`）两行、`buildShopList` 商店列表药水行价签（`price: 15`）一处——三处互不引用：想调药水价（如涨到 20）要改三行，还极易只改判定漏改扣款/价签，实际价改了列表价签却还标旧值，或扣错钱；且写着「15」，也无从考证这数从哪来——同族的价格早已收口（旅馆 `INN_PRICE=10`、酿造 `BREW_GOLD=10`、支线奖励 `+40+级×10 金`），唯独生命药水售价是经济循环里残存的裸奔数值。
- 【修正：data.js 新增 `POTION_PRICE = 15` 为唯一真源（置于 POTION_CAP 之后、POTION_HP_PCT 之前，同属药水常量块）】`shop.js` `buyPotion` 判定改读 `hero.gold >= POTION_PRICE`、扣款改读 `hero.gold -= POTION_PRICE`；`buildShopList` 价签改读 `price: POTION_PRICE`。收益：调药水售价只改 data.js 一处、判定/扣款/价签三处同步，绝无第二套口径；行为逐字不变（15 原样），零 UI 回归。
- 【零回归面】未动购买机制本身——背包满拦截（`item >= POTION_CAP` 优先、不扣钱、提示）、购买成功（扣 15 金、item++、`购买成功` 提示）、金币不足（`金币不足` 提示不进货不扣钱）流程逐字不变；`POTION_CAP/POTION_HP_PCT/POTION_HP_FLAT` 药水其余常量、商店其余行（蘑菇/武器/防具/离开）与 `WEAPONS/ARMORS` 价格原样。未动任何掉落/经验/金币曲线/难度/成就/存档。只新增一个常量 + 改三个引用点（shop 一处 import + 三行引用），零新增依赖。
- 验证：`node --check js/data.js js/shop.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v17.8 专项冒烟（/tmp/jrpg_smoke_v178_potionprice.mjs）**11 项断言全过**——常量导出 15、商店列表药水行 `price` ===POTION_PRICE 且文案仍拼恢复量/POTION_CAP、真实 `buyPotion` 端到端（gold=15 恰好可购扣 15→0 且 item 5→6、gold=14 不足不扣不进货报「金币不足」、满员拦截优先不扣钱报「药水上限 99 瓶」）；grep 确认 shop.js 可执行代码裸 `gold >= 15` / `gold -= 15` / `price: 15` 已清零且三处同读 POTION_PRICE。

## v17.7 起始技能单一数据源——newGame 建档起始技能与升级领悟表同读 LEARN_AT 1 级条目（机制·单一口径，与 DEFAULT_NAME / SKIP_CHANCE / PHASE2_AT 同一「单一数据源收口」体系）

- 【`'火焰斩'` 游离在领悟表之外，与其余四招的等级归属分居两文件】新档起始技能 = 『火焰斩』：这个名字只硬编码在 `core.js` `newGame` 建档一处（`skills: ['火焰斩']`），而其余四招（冰霜击 Lv.3 / 治愈术 Lv.4 / 雷鸣 Lv.5 / 陨石术 Lv.7）的等级归属全在 `data.js` `LEARN_AT` 学习表——起始技能名与技能领悟表互不引用：想换起始技能（如让新档开局就会冰霜击）或重命名初招，只改学习表会漏掉建档、只改建档又让领悟表对不上，新档极易带一个 `SKILL_DATA` 里查无此招的技能（技能菜单/状态页渲染空行、战斗点选报「尚未领悟该技能」）；且写着「火焰斩」，也无从考证它与技能表的关系——它本质上就是「Lv.1 领悟的技能」，却没进 `LEARN_AT`。
- 【修正：data.js `LEARN_AT` 补入 1 级条目 `1: '火焰斩'` 为唯一真源（1..7 级连成一张「各等级领悟技能全表」）】`core.js` `newGame` 建档起始技能改读 `skills: [learnsAt(1)]`。收益：起始技能只是「学习表 1 级条目」，换起始技能 / 重命名初招只改 data.js `LEARN_AT` 一处、建档 / 升级领悟 / 技能全表同步，绝无第二套口径；行为逐字不变（`LEARN_AT[1]` 仍为『火焰斩』，新档起始技能原样 `['火焰斩']`），零 UI 回归。
- 【零回归面】未动升级领悟机制——`hero.checkSkills` 仅在升级（`grantXp` 升级后）调用且按**新等级**查询，运行期永远查 `learnsAt(lv∈[2..8])`，1 级条目运行期不可达（新档自带起始技能，checkSkills 的 `!includes` 防重分支本就兜底），绝无「Lv.1 又弹一次领悟」；`skillXpHint` 下一技能预估从 `level+1` 起循环同样不触及 1 级。未动 `SKILL_DATA` 五招定义、技能菜单/状态页/战斗渲染、经验/等级/掉落/金币/难度/成就/存档。只改表 1 行 + core 建档 1 行 + 1 处 import，零新增依赖。README「等级解锁技能（火焰斩灼烧→…）」描述不变仍准确。
- 验证：`node --check js/data.js js/core.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v17.7 专项冒烟（/tmp/jrpg_smoke_v177_startskill.mjs）**20 项断言全过**——`learnsAt(1)`='火焰斩'、全表 1/3/4/5/7 回归与 2/6/8 null 不变、领悟表技能名全部在 SKILL_DATA 无重复、真实 `newGame()` / `newGame('潮')` / `initGame` 起始技能均 ===[learnsAt(1)]（行为逐字 `['火焰斩']`）、Lv.1 `checkSkills` 不重复领悟、Lv.3 升级仍由同表领悟冰霜击、`applyStats` 兼容、grep 确认 core.js 可执行代码裸 `skills: ['火焰斩']` 已清零且建档改读 `learnsAt(1)`、data.js LEARN_AT 1 级条目唯一真源在位。

## v17.6 默认主角名单一数据源——newGame 兜底 / resetRun 兜底 / 启动建档同读 DEFAULT_NAME（机制·单一口径，与 POTION_CAP / SKIP_CHANCE / PHASE2_HEAL_PCT 同一「单一数据源收口」体系）

- 【`'余烬'` 散落三文件四处互不相关】默认主角名 = HERO_NAMES 首位『余烬』：这个字面量硬编码在 `core.js` `newGame` 建档兜底（`name: name || '余烬'`）、`core.js` `resetRun` 无档兜底（`S.G ? S.G.name : '余烬'`）与 `main.js` 启动建档（`initGame('余烬')`）**四处**（含 data.js `HERO_NAMES=['余烬','灯见','潮']` 真源）——三文件互不引用：想改默认名（如把首位换成『灯见』）要改三个文件，还极易只改建档漏改兜底，新档默认名与无档重置名漂移；且写着「余烬」，也无从考证这数从哪来、是否就是名单首位。
- 【修正：data.js 新增 `DEFAULT_NAME = HERO_NAMES[0]` 为唯一真源（紧随 HERO_NAMES 之后，2026-08 末档收口系列第 6 弹）】`core.js` `newGame` 兜底改读 `name || DEFAULT_NAME`、`resetRun` 兜底改读 `S.G ? S.G.name : DEFAULT_NAME`；`main.js` 启动建档改读 `initGame(DEFAULT_NAME)`。收益：改默认主角名只动 data.js `HERO_NAMES` 一处、建档/两兜底/启动四处同步，绝无第二套口径；行为逐字不变（`HERO_NAMES[0]` 仍为『余烬』，默认名原样），零 UI 回归。
- 【零回归面】未动建档其余字段（Lv.1 / 30 金 / 3 药水 / 木剑布衣 / 火焰斩 / village 出生）与角色创建流程（`beginAdventure` 单选名单 `HERO_NAMES[S.createName]` 原样）；显式传名（`newGame('潮')`、主角创建选『灯见』/『潮』）仍优先。未动任何掉落/经验/金币/难度/成就/存档。只新增一个常量 + 改三个引用点（core 两处 + main 一处）+ 两处 import，零新增依赖。
- 验证：`node --check js/data.js js/core.js js/main.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v17.6 专项冒烟（/tmp/jrpg_smoke_v176_defaultname.mjs）**16 项断言全过**——常量导出且 =HERO_NAMES[0]、data/core/main 定义·导入·导出在位、grep 确认可执行代码裸 `'余烬'` 已清零（仅 data.js HERO_NAMES 真源一行）、真实 `newGame()` 默认名 '余烬'/`newGame('潮')` 显式优先、`DEFAULT_NAME` 可用于 resetRun 无档兜底路径。

## v17.5 二段变身回血比例兜底单一数据源——enemyAI 变身回血结算与 drawBattle 变身角标同读 PHASE2_HEAL_PCT（机制·单一口径，与 PHASE2_AT 同一「变身模板兜底」体系）

- 【`0.15` 兜底散落两处且显示对不上结算】Boss 二段变身的回血量 = `maxHP × phase.heal`（数据表显式写 heal），但变身模板**漏写 heal 时**的安全默认「恢复 15%」= `phase.heal || 0.15`：这个兜底字面量只硬编码在 `enemyAI.js enemyAct` 结算一处（`Math.round(enemy.hpMax * (phase.heal || 0.15))`），而 `view/drawBattle.js` 变身「增益数值」角标用的是 `if (p2.heal)` 直判——恰好漏写 heal 的模板（目前不存在，纯防未来）结算仍回 15%、角标却不显示「回15%」，显示对不上结算；且「2×2」两处互不引用，想调默认回血比例（如改 12%）要改结算、角标还得记得同步，极易改漏。v17.2 收口变身触发阈值（PHASE2_AT）时留下了这个同族的姊妹兜底——血量线已收口、回血比例还裸奔在结算/角标两处。
- 【修正：data.js 新增 `PHASE2_HEAL_PCT = 0.15` 为唯一真源（置于 PHASE2_AT 之后，同属「变身模板兜底」块）】`enemyAI.js` 变身回血结算改读 `phase.heal || PHASE2_HEAL_PCT`；`view/drawBattle.js` 变身角标改读 `p2.heal || PHASE2_HEAL_PCT`（判定与显示同源，漏写 heal 的模板角标同样标注「回15%」，显示与结算逐字对上）。收益：调默认变身回血比例只改 data.js 一处、结算/角标两处同步，绝无第二套口径；行为逐字不变（三 Boss 变身模板均显式带 heal——幽冥魔王/终焉之神 0.15、洞窟领主 0.10，此兜底仅守护变身模板漏写 heal 时的安全默认，当前游戏内敌人无一触发），零 UI 回归。
- 【零回归面】未动变身机制本身——显式 `heal`（幽冥魔王/终焉之神 0.15、洞窟领主 0.10）优先、变身流程（phased 标记/改名/攻防加算/回血/封印/闪光/blog）、变身触发阈值（PHASE2_AT 0.5）均逐字不变；drawBattle 角标对三 Boss 的显示文本原样（均显式 heal，`p2.heal || PHASE2_HEAL_PCT` 取值不变）；未动任何掉落/经验/金币/难度/成就/存档。只新增一个常量 + 改两个引用点（enemyAI 结算、drawBattle 角标判定/显示），零新增依赖。
- 验证：`node --check js/data.js js/enemyAI.js js/view/drawBattle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v17.5 专项冒烟（/tmp/jrpg_smoke_v175_phase2heal.mjs）**24 项断言全过**——常量导出 0.15、data/enemyAI/drawBattle 定义·导入·导出在位、源码裸字面量（`phase.heal || 0.15)` / `if (p2.heal)`）已从可执行代码清除且两处同读 PHASE2_HEAL_PCT、真实 `enemyAct` 端到端（no-heal 模板兜底 0.15 → 100×0.15=15 回至 64 且博客报「HP 恢复 15」、显式 heal:0.10 仍优先回 10、幽冥魔王实盘 140×0.15=21 → 90 回归不变）、角标显示公式与结算公式逐字同源（漏写 15% / 显式 10%）、相邻机制 PHASE2_AT/HEAL_PCT 回归不变。

## v17.4 药水背包上限单一数据源——商店拦截判定/提示文案/列表「现有X/99」/购买置灰同读 POTION_CAP（机制·单一口径，与 MUSHROOM_GOAL / INN_PRICE / BREW_MUSHROOMS 同一「数值数据化」体系）

- 【`99` 散落两文件四处互不相关】药水背包上限 = 99 瓶：这个 99 硬编码在 `shop.js` `buyPotion` 购买拦截判定（`hero.item >= 99`）、背包已满提示文案（`药水上限 99 瓶`）、`buildShopList` 商店列表药水行（`[现有X/99]`）与 `view/menus.js` 购买栏 置灰判定（`hero.item<99`）四处——两文件互不引用：想调上限（如放宽到 120）要改四个地方，还极易只改判定漏改文案，实际拦截线变了列表/置灰/提示却还报旧值；且写着「99」，也无从考证这数从哪来（掉落/宝箱/任务奖励增量处不设上限，上限只在商店侧强制——四处 99 全是同一「商店侧上限」口径）。v17.0 收口支线目标株数时把「任务/保护/文案」接上同一常数，药水上限是经济循环里残存的裸奔数值。
- 【修正：data.js 新增 `POTION_CAP = 99` 为唯一真源（置于药水恢复量常量块首行，紧邻 POTION_HP_PCT）】`shop.js` 购买拦截改读 `hero.item >= POTION_CAP`、提示文案改拼 `` `药水上限 ${POTION_CAP} 瓶` ``、商店列表改拼 `[现有X/${POTION_CAP}]`；`view/menus.js` 购买置灰改读 `hero.item<POTION_CAP`（注释同步）。收益：调上限只改 data.js 一处、判定/提示/列表/置灰四端同步，绝无第二套口径；行为逐字不变（99 原样拼入），零 UI 回归。
- 【零回归面】未动购买机制本身——满员拦截（不扣金币、提示后 return）、购买成功（扣 15 金、item++、`现有X/99` 显示）、掉落/宝箱/任务奖励增量仍不设上限（既有设计，本次不碰）均逐字不变；`price:15`、`POTION_HP_PCT/POTION_HP_FLAT` 恢复量、商店其他行与置灰逻辑原样。未动任何掉落/经验/金币/难度/成就/存档。只新增一个常量 + 改四个引用点（shop 三处 + menus 一处），零新增依赖。
- 验证：`node --check js/data.js js/shop.js js/view/menus.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v17.4 专项冒烟（/tmp/jrpg_smoke_v174_potioncap.mjs）**6 项断言全过**——常量导出 99、商店列表药水行 `[现有3/99]`/`[现有98/99]` 由 POTION_CAP 推导、满员购买拦截（金币不变、提示含「药水上限 99 瓶」）、上限前最后一次购买（98→99、扣 15 金）；grep 确认可执行代码裸 `99` 已清零（仅注释提及）。

## v17.3 冰霜击冻结概率单一数据源——SKILL_DATA.skip 判定与技能提示文案同读 SKIP_CHANCE（机制·单一口径，与 BURN_PCT / POISON_CHANCE / ELITE_CHANCE 同一「概率数据化」体系）

- 【0.30 同一对象内两处各写一份互不相关】冰霜击冻结敌回合概率 = 30%：这个 0.30 硬编码在 `data.js` `SKILL_DATA['冰霜击']` 的 `skip:0.30`（判定字段）与 `hint:'30%冻结（跳过敌回合）'`（提示文案）两处——同对象内、互不引用：想调冻结率（如放宽到 40%）要改两个字段，还极易只改判定漏改提示，实际冻结率变了技能提示却还报旧值；而写着「0.30」「30%」，也无从考证这数从哪来、两处是否早已漂移（实测同值，纯结构隐患）。同族的火焰斩灼烧比例早已由 `BURN_PCT` 单一数据源化（hint 拼 `Math.round(BURN_PCT*100)`），毒蛇施毒率也已由 `POISON_CHANCE` 收口——唯独冰霜击冻结率是 SKILL_DATA 内最后一处裸奔数值。
- 【修正：data.js 新增 `SKIP_CHANCE = 0.30` 为唯一真源（置于 POISON_CHANCE 之后、SKILL_DATA 之前）】`SKILL_DATA['冰霜击'].skip` 改读 `skip:SKIP_CHANCE`；其 hint 改拼 `Math.round(SKIP_CHANCE * 100) + '%冻结（跳过敌回合）'`（与毒蛇帮助页同一推导式）。收益：调冻结率只改 data.js 一处、判定/提示两处同步，绝无第二套口径；显示文案 `30%冻结（跳过敌回合）` 逐字不变（0.30×100=30 原样推导），零 UI 回归。
- 【零回归面】未动冻结机制本身——`battle.playerAction` 判定 `Math.random() < skill.skip` 逐字不变（读的是 SKILL_DATA 里由常量拼入的 skip，值 0.30 原样）、`enemy.skipNext` 置位与 enemyAI 冻结消耗流程、`drawBattle` 冻结角标/技能预览读 SKILL_DATA.hint 显示均不变；其余四技能（火焰斩/雷鸣/陨石术/治愈术）hint 与字段逐字不变（冒烟逐条断言）。未动任何掉落/经验/金币/难度/成就/存档。只新增一个常量 + 改两个引用点（data.js 定义/导出、SKILL_DATA 一行两字段），零新增依赖。
- 验证：`node --check js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v17.3 专项冒烟（/tmp/jrpg_smoke_v173_skipchance.mjs）**15 项断言全过**——常量导出 0.30、`SKILL_DATA['冰霜击'].skip` 同读常量、hint 由常量推导且与旧字面量逐字一致（`30%冻结（跳过敌回合）`）、data.js 源码裸字面量（`skip:0.30` / `'30%冻结`）已从 SKILL_DATA 清除且定义/导出/两引用在位、真实 `playerAction` 端到端（Math.random 固定 0.2 → skipNext=true 且博客记「冻结」；固定 0.5 → skipNext=false，与 0.30 阈值逐字耦合）、其余四技能 hint 与字段零回归。

## v17.2 二段变身触发阈值兜底单一数据源——enemyAI 变身触发与战斗「二段变身线」标注同读 PHASE2_AT（机制·单一口径，与 HEAL_PCT / HEAVY_MULT / SHIELD_MULT 同一体系）

- 【0.5 散落两文件互不相关】Boss 二段变身触发血量线 = `hp < hpMax × at`（数据表显式写 at），但变身模板**漏写 at 时**的安全默认「血量过半才变身」= `phase.at || 0.5`：这个兜底字面量硬编码在 `enemyAI.js enemyAct`（变身触发判定 `enemy.hp < enemy.hpMax * (phase.at || 0.5)`）与 `view/drawBattle.js` 二段变身线标注（`Math.ceil(enemy.hpMax * (phase2.at || 0.5))`）**两处**互不相关——想调「未显式写 at 的变身默认血量线」要改两个文件，还极易只改结算漏改标注，实际触发阈值变了变身线却还报旧值；且写着「0.5」，也无从考证这数从哪来、两处是否早已漂移。v17.1 收口回血招默认比例时已把「结算/标注两处各自硬编码兜底」定为同款病根，二段变身触发阈值是本体系残留在变身流程上的兜底数值。
- 【修正：data.js 新增 `PHASE2_AT = 0.5` 为唯一真源（置于 HEAL_PCT 之后）】`enemyAI.js` 变身触发判定改读 `phase.at || PHASE2_AT`；`view/drawBattle.js` 变身线标注改读 `phase2.at || PHASE2_AT`。收益：调默认变身血量线只改 data.js 一处、触发/标注两处同步，绝无第二套口径；行为逐字不变（三 Boss 变身模板均显式带 at:0.5——幽冥魔王/洞窟领主/终焉之神，此兜底仅守护变身模板漏写 at 时的安全默认，当前游戏内敌人无一触发），零 UI 回归。
- 【零回归面】未动变身机制本身——显式 `at:0.5` 优先（三 Boss 数据表原样）、触发判定 `hp < hpMax×at`、变身流程（phased 标记/改名/攻防加算/回血/封印/闪光/blog）、变身线精确阈值 `Math.ceil(hpMax×at)`（幽冥魔王 70 / 洞窟领主 55 / 终焉之神 130）均逐字不变；未动任何掉落/经验/金币/难度/成就/存档。只新增一个常量 + 改两个引用点（enemyAI 触发判定、drawBattle 变身线标注），零新增依赖。
- 验证：`node --check js/data.js js/enemyAI.js js/view/drawBattle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v17.2 专项冒烟（/tmp/jrpg_smoke_v172_phase2at.mjs）**20 项断言全过**——常量导出 0.5、enemyAI/drawBattle 定义·导入·导出在位、源码裸字面量（`phase.at || 0.5)` / `phase2.at || 0.5`）已从可执行代码清除且两处同读 PHASE2_AT、真实 `enemyAct` 端到端（无 at 变身模板 hp51 未过 0.5 线不触发 → hp49 过线触发真身并回血 15 → 64、幽冥魔王 hp70 恰在显式 0.5 线不触发 → hp69 触发且真身回血 15% 回归不变）、变身线阈值推导（140×PHASE2_AT=70 / 260×PHASE2_AT=130）与触发判定同源。

## v17.1 敌方回血默认比例单一数据源——enemyAI 结算与战斗招数一览标注同读 HEAL_PCT（机制·单一口径，与 HEAVY_MULT / SHIELD_MULT / CRIT_MULT 同一体系）

- 【`0.12` 兜底散落两文件互不相关】敌方「回血」招的恢复量 = `maxHP × act.pct`（数据表显式写 pct），但招数表**漏写 pct 时**的安全默认「恢复 12%」= `act.pct || 0.12`：这个兜底字面量硬编码在 `enemyAI.js enemyAct`（结算 `Math.round(enemy.hpMax * (act.pct || 0.12))`）与 `view/drawBattle.js` 敌方招数一览（标注 `(a.pct || 0.12) * 100`）**两处**，两处注释还都各写一句「0.12 同款兜底」互为镜像却各写各的——想调「未显式写 pct 的回血招默认恢复多少」要改两个文件，还极易只改结算漏改标注，实际回血量变了招数一览却还报旧百分比；而写着「0.12」，也无从考证这数从哪来、两处是否早已漂移。
- 【修正：data.js 新增 `HEAL_PCT = 0.12` 为唯一真源（置于 HEAVY_MULT_PHASED 之后、DROP_* 之前）】`enemyAI.js` 结算改读 `act.pct || HEAL_PCT`；`view/drawBattle.js` 招数一览改读 `(a.pct || HEAL_PCT) * 100`（两处遗留注释同步改为 HEAL_PCT）。收益：调默认回血比例只改 data.js 一处、结算/标注两处同步，绝无第二套口径；行为逐字不变（三 Boss 回血招均显式带 pct——幽冥魔王/终焉之神 0.12、洞窟领主 0.10，此兜底仅守护招数表漏写 pct 的安全默认，当前游戏内敌人无一触发，纯收口数据源），零 UI 回归。
- 【零回归面】未动敌方回血机制本身——显式 `pct`（幽冥魔王/终焉之神 0.12、洞窟领主 0.10）优先生效、`pickAct` 按 hpBelow 触发线选招、回血结算公式 `Math.round(hpMax × pct)`、招数一览「血<40%时·恢复N%HP」文案、终焉之神 `⛔封印` 标注均逐字不变；三 Boss 行为表与 `BOSS/CAVE_BOSS/TRUE_BOSS` 模板数据原样。未动任何掉落/经验/金币/难度/成就/存档。只新增一个常量 + 改两个引用点（enemyAI 结算、drawBattle 标注）+ 两处注释同步，零新增依赖。
- 验证：`node --check js/data.js js/enemyAI.js js/view/drawBattle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v17.1 专项冒烟（/tmp/jrpg_smoke_v171_healpct.mjs）**25 项断言全过**——常量导出 0.12、data/enemyAI/drawBattle 定义·导入·导出在位、源码裸字面量（`act.pct || 0.12)` / `(a.pct || 0.12)`）已从可执行代码清除且两处同读 HEAL_PCT、真实 `enemyAct` 端到端（no-pct 回血招 100/10 → 恢复 12 → hp22 且博客「恢复 12 HP」、显式 pct:0.10 仍优先生效 10→20、均不误触发胜负分支）、真实 `drawBattle` 敌方招数一览（幽冥魔王 ·恢复12%HP、no-pct 兜底 ·恢复12%HP、洞窟领主 ·恢复10%HP、终焉之神 ·恢复12%HP·⛔封印）、三 Boss 行为表显式 pct 全回归不变。

## v17.0 灯长蘑菇支线目标株数单一数据源——QUESTS/对话文案、开箱集齐判定、酿造与出售保护同读 MUSHROOM_GOAL（机制·单一口径，与 BREW_MUSHROOMS / ELITE_GATE_LV / POISON_CHANCE 同一「数据化」体系）

- 【支线目标「3 株」散落四文件七处互不相关】灯长支线「帮我找回 3 株魔法蘑菇」：这个 3 硬编码在 `data.js` `QUESTS.side_mushroom.n`（任务框架目标株数，日志 `找回魔法蘑菇 X/3 株` 经 quests.objectiveText 推导）与 NPC 对话 offer/active 两处文案（`帮我找回 3 株，好吗？` / `（已找到 X/3 株）`）、`world.js` 开箱集齐转可交付判定（`mushrooms >= 3`）、`core.js` 酿造任务保护（`mushrooms <= 3`）、`shop.js` 出售任务保护与文案（`canSellMushroom`、`sellMushroom`、出售列表 blocked 判定共三处 + `集齐 3 株前不能卖！`）——四文件七处互不相关：想调支线目标（如放宽到 4 株）要改三四个文件，还极易只改判定漏改文案，集齐 4 株对话却还标「/3 株」、出售列表还说集齐 3 株前不能卖。v16.2 收口酿造配方时只动了「2 株蘑菇」的酿造侧，支线目标这同一个「3」却始终裸奔在任务/保护/文案三端。
- 【修正：data.js 新增 `MUSHROOM_GOAL = 3` 为唯一真源，定义置于 QUESTS 之前（紧邻 BREW_MUSHROOMS，同属蘑菇主题）】`QUESTS.side_mushroom.n` 改读 `n:MUSHROOM_GOAL`（日志/任务条 `X/3 株` 经 def.n 同源推导）、对话 offer 文案改拼 `帮我找回 ' + MUSHROOM_GOAL + ' 株`、active 文案改拼 `${hero.mushrooms||0}/${MUSHROOM_GOAL} 株`；`world.onChestStep` 集齐判定改读 `hero.mushrooms >= MUSHROOM_GOAL`；`core.brewNow` 与 `shop.js`（canSellMushroom / sellMushroom / buildShopList 三处）保护判定改读 `hero.mushrooms <= MUSHROOM_GOAL`、禁售文案改拼 `集齐 ${MUSHROOM_GOAL} 株前不能卖！`。收益：调支线目标只改 data.js 一处、任务/对话/开箱/酿造/出售五端同步，绝无第二套口径；显示文案 `0/3 株`、`帮我找回 3 株，好吗？`、`集齐 3 株前不能卖！` 逐字不变（3 原样拼入），零 UI 回归。
- 【零回归面】未动支线机制本身——开箱得蘑菇（雾语林 60% 档）、集齐后 `setSideQuest('turnin')` 转可交付、交付奖励（40+级×10 金 + 2 药水）、任务保护语义（active/turnin 且株数 ≤ 目标时禁酿禁卖）均逐字不变；顺带确认 `quests.objectiveText` 本就读 `def.n`（`X/{def.n} 株`）——日志侧本与任务框架同源，本次只把散落的另外六处一并接到同一常数上。未动任何掉落/经验/金币/难度/成就/存档。只新增一个常量 + 改十个引用点（data 定义/导出与 n/对话两处、world 一处、core 一处、shop 四处判定+一处文案），零新增依赖。
- 验证：`node --check js/data.js js/world.js js/core.js js/shop.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v17.0 专项冒烟（/tmp/jrpg_smoke_v170_mushroom.mjs）**27 项断言全过**——常量导出 3、`QUESTS.side_mushroom.n` 同读常量、真实 `npcQuestPages` offer/active 文案由常量推导且与旧字面量逐字一致（`帮我找回 3 株，好吗？` / `（已找到 2/3 株、0/3 株）`）、真实 `questJournal` 目标 `找回魔法蘑菇 2/3 株 / 3/3 株`、`canSellMushroom`/`buildShopList`/`sellMushroom` 齐全的集齐前后双态（3 株禁售不扣、5 株售出 5→4 +10 金、售出后仍可卖）、真实 `world.onStep` 开箱集齐转可交付端到端（Math.random 固定 0.5 走蘑菇档：1→2 株仍进行中、2→3 株自动转 turnin）、且 data/world/core/shop 源码旧裸字面量（`n:3` / `帮我找回 3 株` / `/3 株` / `mushrooms >= 3` / `mushrooms <= 3` / `集齐 3 株前`）已从可执行代码清除且四处引用 MUSHROOM_GOAL 在位。

## v16.9 毒蛇施毒概率单一数据源——SPECIES/MON_BASE 两处定义与帮助页标注同读 POISON_CHANCE（机制·单一口径，与 POISON_PCT / POISON_TURNS / ELITE_CHANCE 同一「概率数据化」体系）

- 【0.35 散落三处互不相关】毒蛇施毒成功率 = 每次毒蛇普攻 35% 概率使玩家中毒：这个 0.35 硬编码在 `data.js` `SPECIES['毒蛇'].poison`（图鉴「☠️ 会施毒」所属物种表）与 `MON_BASE['毒蛇'].poison`（遇敌生成/敌人模板）**两处定义**，帮助页「毒蛇中毒」行又绕一层 `MON_BASE.find(m=>m.name==='毒蛇').poison` 才取到同一数值——三处互不相关：想调毒蛇施毒率（如放宽到 40%）要改两处定义、帮助页还得记得同步，极易只改一处让实际概率与帮助页标注对不上，实际 40% 帮助页却还标「35%」；而它的病根同族兄弟 `POISON_PCT`（扣血比例）/ `POISON_TURNS`（持续回合）早已单一数据源化，唯独这「施毒率 0.35」还散落两处定义 + 一处 `.find` 绕层，是本体系残留在毒蛇上的最后一处裸奔数值。
- 【修正：data.js 新增 `POISON_CHANCE = 0.35` 为唯一真源】`SPECIES['毒蛇'].poison` 与 `MON_BASE['毒蛇'].poison` 两处定义改读 `POISON_CHANCE`；帮助页「毒蛇中毒」行改拼 `毒蛇有 ' + Math.round(POISON_CHANCE * 100) + '% 概率`，不再经 `MON_BASE.find(...)`. 收益：调毒蛇施毒率只改 data.js 一处、两处定义 + 帮助页标注三端同步，绝无第二套口径；显示文案 `毒蛇有 35% 概率…` 逐字不变（0.35×100=35 原样推导），零 UI 回归。常量定义置于 SPECIES / MON_BASE 之前（两表顶层字面量 poison 会拼入此值，前向引用会触发 TDZ）。
- 【零回归面】未动毒蛇机制本身——`enemyAI.enemyAct` 施毒判定 `Math.random() < enemy.poison` 逐字不变（monster.poison 由 `scaleEnemy` 从 MON_BASE 透传，值 0.35 原样）、`POISON_PCT` 扣血结算与 `POISON_TURNS` 回合数、毒蛇图鉴 tag（`☠️ 会施毒 · 扣血3回合`）、其余魔物两表与帮助页各行均逐字不变；未动任何掉落/经验/金币/难度/成就/存档。只新增一个常量 + 改两个引用点（SPECIES 定义、MON_BASE 定义）+ 帮助页一处（顺带去掉了 `.find` 绕层），零新增依赖。
- 验证：`node --check js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v16.9 专项冒烟（/tmp/jrpg_smoke_v169_poison.mjs）**20 项断言全过**——常量导出 0.35、SPECIES/MON_BASE 两处定义同读常量、帮助页「毒蛇有 35% 概率」由常量推导且与旧字面量逐字一致、data.js 源码裸 `poison:0.35` 已清除（定义处除外）且 `MON_BASE.find` 绕层已移除、scaleEnemy 毒蛇 poison 透传 = 0.35、毒蛇/野狼/史莱姆/雾灵图鉴 tag 与相邻帮助页行（中毒自救/技能克制/石甲/战斗掉落）原文逐字不变。

## v16.8 终焉之神额外奖励单一数据源——战胜结算/横幅、图鉴标注、战斗预览同读 TRUE_BONUS_GOLD（机制·单一口径，与 DROP_GOLD / RUSH_RECOVER / CHARGE_MULT 同体系）

- 【额外 300 金币散落四处互不相关】终焉之神的额外赏金 = 战胜后 `hero.gold += 300`（基础报酬 600 金之外的追加奖励）：这个 300 硬编码在 `battle.js winBattle` 两处（第 402 行结算 `hero.gold += 300` 与第 452 行横幅文案 `额外 300 金币！`）、`data.js` 图鉴 `SPECIES['终焉之神'].tag`（`💰 击败+300金`）、`view/drawBattle.js` 战斗预览（`· 战胜另+300金`）——跨三个文件四处互不相关：想调真结局额外赏金（如放宽到 500）要改两个文件三四处，还极易只改结算漏改标注/预览，实际打赢多了 500 金图鉴却还标「+300金」、战前预览还报「另+300金」；v16.5 收口精英出没时已把「判定旁剩下裸数值」定为同款病根，终焉之神的「+300」是 Boss 奖励体系中最后一处结算/标注/预览裸奔数值——`TRUE_BOSS.gold = 600`（基础报酬）早已单一数据源化，唯独这「额外 300」还散落四处。
- 【修正：data.js 新增 `TRUE_BONUS_GOLD = 300` 为唯一真源】`SPECIES['终焉之神'].tag` 改拼 `'💰 击败+' + TRUE_BONUS_GOLD + '金'`；`battle.winBattle` 结算改读 `hero.gold += TRUE_BONUS_GOLD`、横幅改读模板插值 `额外 ${TRUE_BONUS_GOLD} 金币！`；`drawBattle` 预览改拼 `'· 战胜另+' + TRUE_BONUS_GOLD + '金'`。收益：调真结局额外赏金只改 data.js 一处、结算/横幅/标注/预览四处同步，绝无第二套口径；显示文案 `额外 300 金币！`、`💰 击败+300金`、`· 战胜另+300金` 逐字不变（300 原样拼入），零 UI 回归。常量定义置于 SPECIES 之前（SPECIES 顶层字面量 tag 会拼入此值，前向引用会触发 TDZ）。
- 【零回归面】未动终焉之神机制本身——`TRUE_BOSS` 基础报酬（xp400 / gold600）、`isTrue` 判定、真结局流程（stopBgm/goto ending/drawEnding）、记忆碎片、成就、其余 Boss（幽冥魔王/洞窟领主/残焰魔像）tag 逐字不变；未动任何掉落/经验/难度/存档。只新增一个常量 + 改四个引用点（data 定义/导出与 tag 一处、battle 结算/横幅两处、drawBattle 预览一处），零新增依赖。
- 验证：`node --check js/data.js js/battle.js js/view/drawBattle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v16.8 专项冒烟（/tmp/jrpg_smoke_v168_truegold.mjs）**16 项断言全过**——常量导出 300、图鉴 tag 由常量拼出且与旧字面量逐字一致（`💰 击败+300金`）、TRUE_BOSS 基础报酬 gold=600 不受影响（额外奖与基础奖独立两档）、battle.js 裸结算 `hero.gold += 300` 与裸横幅 `额外 300 金币`、drawBattle.js 裸预览 `另+300金`、data.js 可执行字符串字面量 `💰 击败+300金` 均已清除且四处同读 TRUE_BONUS_GOLD、相邻 Boss tag（幽冥魔王/石心魔像/洞窟领主）与拼接产物文案逐字不变。

## v16.7 帮助页元素克制/石甲减伤标注单一数据源——HELP_PAGES 第 3 页最后两处裸字面量同读 ELEM_MULT / SHIELD_MULT（机制·单一口径，与 CHARGE_MULT / RUSH_RECOVER / DROP_* 同体系）

- 【1.35/0.7 与 -40% 只剩帮助页两行裸奔，结算/日志/图鉴早已同源】元素克制倍率 `弱点×1.35 / 抗性×0.7` 自 v12.x 起就是 `ELEM_MULT={weak:1.35, resist:0.7}`（data.js），`rules.elemMult`（结算）与图鉴 `codexTag` 弱点/抗性标注早已同读此源；石甲减伤 `×0.6 / 每层降 40%` 自 v12.4 起就是 `SHIELD_MULT=0.6`，`battle.attackMove`（结算）、`rules.shieldScale`（预览）、`enemyAI` 凝甲日志（`敌所受伤害降低 ${Math.round((1-SHIELD_MULT)*100)}%`）三处同读此源——唯独帮助页第 3 页还硬编码着两句字面量：操作说明「技能克制」行的 `弱点伤害×1.35 · 抗性伤害×0.7` 与「石心魔像·石甲」行的 `每层使下一次攻击伤害 -40%`。想调克制/石甲强度（如弱点放宽到 1.4、石甲减到 -50%）要改结算/显示之外**还要记得改帮助页**，极易只改结算漏改标注；v16.6 收口试炼恢复时把帮助页「最后一处裸奔」定为同款病根，元素克制与石甲减伤是本体系残留在帮助页的最后两处字面量。
- 【修正：帮助页两行改由 ELEM_MULT / SHIELD_MULT 拼入】`弱点伤害×' + ELEM_MULT.weak + ' · 抗性伤害×' + ELEM_MULT.resist` 与 `每层使下一次攻击伤害 -' + Math.round((1 - SHIELD_MULT) * 100) + '%'`（后者与 enemyAI 凝甲日志同一推导式）。收益：调克制/石甲平衡只改 data.js 一处、结算/显示/标注多端同步；显示文案逐字不变（`1.35/0.7/40%` 全由常量推导），零 UI 回归。
- 【零回归面】未动机制本身——`elemMult` 判定（先弱后抗、非克制即 1）、`attackMove`/预览/日志的石甲语义、`maxShield`（石心魔像 3 层/洞窟领主 2 层）与凝甲触发线均逐字不变；未动 `ELEM_NAME`、图鉴标注、其余帮助页各行、掉落/经验/金币/难度/成就/存档。只改 data.js 帮助页两行、零新增依赖。
- 验证：`node --check js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v16.7 专项冒烟（/tmp/jrpg_smoke_v167_help.mjs）**13 项断言全过**——常量导出 1.35/0.7/0.6、真实 HELP_PAGES 两行文案与旧字面量逐字一致（`弱点伤害×1.35 · 抗性伤害×0.7` / `-40%` 全由常量推导）、data.js 源码裸字面量 `弱点伤害×1.35`/`抗性伤害×0.7`/`攻击伤害 -40%` 已清除且两行读常量、相邻行（毒蛇中毒/战斗掉落/宝箱掉落）原文逐字不变。

## v16.6 试炼三连战恢复比例单一数据源收口——帮助页「试炼三连战」行同读 RUSH_RECOVER（机制·单一口径，与 CHARGE_MULT / ELEM_MULT / FLEE_SUCCESS 同体系）

- 【battle.winBattle 注释宣称「帮助页标注同读此源」，帮助页却仍裸写 35%/50%】试炼三连战每胜一关自动回血 `35%HP / 50%MP`：这两个比例自 v13.x 起就是 `RUSH_RECOVER = { hp:0.35, mp:0.5 }`（data.js）——`battle.winBattle` 连战换关恢复结算与战斗横幅（`已自动恢复35%HP / 50%MP`）早已同读此源，v12.9 后 `drawBattle` 通关备注 `通关恢复35%HP/50%MP` 也接入了常量——唯独帮助页第 4 页「试炼三连战」行还写着硬编码字面量 `每胜一关回血35%HP/50%MP`，且 `battle.js winBattle` 的注释（第 424 行）明确写着「与战斗横幅/帮助页标注同读此源」：**注释声称帮助页已同源，实际帮助页是该体系最后一处裸奔**。想调试炼恢复强度（如改成 30%/45%）要改结算之外还要记得改帮助页，极易只改结算漏改标注，实际打赢回血变了帮助页却还标旧比例；而写着「35%/50%」，玩家也无从考证这数从哪来。v16.3 收口蓄力倍率时已把帮助页漏网同类病根定为「结算/显示/标注三处同步」，试炼恢复是本体系漏在帮助页的最后一处。
- 【修正：帮助页「试炼三连战」行改由 RUSH_RECOVER 拼入】`每胜一关回血' + Math.round(RUSH_RECOVER.hp × 100) + '%HP/' + Math.round(RUSH_RECOVER.mp × 100) + '%MP'` —— 与结算/横幅/备注同一单一数据源。收益：调试炼恢复平衡只改 data.js 一处、结算/横幅/备注/帮助页四处同步，`battle.winBattle` 注释「帮助页标注同读此源」所言成真；显示文案 `每胜一关回血35%HP/50%MP` 逐字不变（0.35×100=35 / 0.5×100=50 原样推导），零 UI 回归。
- 【零回归面】未动试炼机制本身——连战换关 `hero.rushStage` 递增、判定 `stage >= RUSH_BOSSES.length`、恢复结算 `Math.min` 封顶、换关延迟 1400ms、Boss 序列 `RUSH_BOSSES` 均逐字不变；未动帮助页其余各页各行为逐字不变、未动任何掉落/经验/金币/难度/成就/存档。只改 data.js 帮助页一行、零新增依赖。
- 验证：`node --check js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v16.6 专项冒烟（/tmp/jrpg_smoke_v166_rushhelp.mjs）**12 项断言全过**——常量导出 {hp:0.35, mp:0.5}、`Math.round(RUSH_RECOVER.hp×100)`=35/50、真实 HELP_PAGES「试炼三连战」行文案与旧字面量逐字一致（35/50 由常量推导）、data.js 源码裸 `回血35%HP/50%MP` 已清除且该行读 RUSH_RECOVER、battle.winBattle 结算/横幅与 drawBattle 备注同读 RUSH_RECOVER、相邻帮助行（双徽记条件/重整旗鼓/蘑菇宝箱）原文逐字不变。

## v16.5 精英出没概率单一数据源——雾语林随机遇敌判定与图鉴「稀有精英」标注同读 ELITE_CHANCE（机制·单一口径，与 ELITE_GATE_LV / DROP_* / CHEST_MUSHROOM 同体系）

- 【0.07 只剩一处裸奔，且「稀有精英」只有定性无定量】雾语林随机遇敌有约 7% 概率撞见「石心魔像」精英：这个 0.07 硬编码在 `encounter.js randomEncounter`（判定），而图鉴已讨伐行的出没标注「雾语林·稀有精英」只有定性「稀有」、没有定量——v12.3 收口精英等级门槛 `ELITE_GATE_LV` 时，出没概率仍以裸 0.07 与「稀有」字样并存，是本作机制数值中最后一处未数据化的出没概率：想调精英出现率（如放宽到 10%）要改判定一处，图鉴却永远只能说「稀有」，刷图鉴/刷蘑菇的玩家完全无法预估「撞不撞得到它」。
- 【修正：data.js 新增 `ELITE_CHANCE = 0.07` 为唯一真源】`randomEncounter` 精英判定改读 `Math.random() < ELITE_CHANCE`；图鉴 `whereFind` 对石心魔像（经 `ELITE_GOLEM.name` 判精英，与 monReward/codexStats 同一识别惯例）追加 `· 约N%`（N = `Math.round(ELITE_CHANCE × 100)`）。收益：调精英出现率只改 data.js 一处、判定/标注两处同步；显示文案 `雾语林·稀有精英 · Lv.3起出没 · 约7%`（7% 由常量推导），遇敌行为逐字不变（判定时机 dungeon && level≥ELITE_GATE_LV 原样、0.07 原样）。
- 【零回归面】未动精英判定时机、精英属性（ELITE_GOLEM）、等级门槛（ELITE_GATE_LV）、图鉴其余条目、掉落/经验/金币/难度/成就/存档。只新增一个常量 + 改两个引用点。
- 验证：`node --check js/data.js js/encounter.js js/view/menus.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v16.5 专项冒烟（/tmp/jrpg_smoke_v165_elite.mjs）**24 项断言全过**——常量导出 0.07/3、encounter.js 裸 `Math.random() < 0.07` 已清除且读 ELITE_CHANCE 两处、真实 `randomEncounter` 蒙特卡洛（Lv.3/Lv.6 dungeon 精英率 ≈7% 与 v12.3 基线同界、Lv.1/Lv.2 dungeon 无精英、village/cave 无精英）、eliteEncounter 直达契约（名/isElite/hpMax）、drawCodex 端到端逐字 `（雾语林·稀有精英 · Lv.3起出没 · 约7%）` 且史莱姆/幽冥魔王行原文逐字不变、monReward(石心魔像,5)=xp70/金75（ELITE_GOLEM 式回归）、连续绘制零状态改动（纯显示收敛）。

## v16.4 经验曲线单一数据源——升级结算 / 技能经验预估 / 新档建档同读 XP_GROW / XP_INIT（机制·单一口径，与 CHARGE_MULT / DROP_* / POTION_* 同体系）

- 【1.42 散落 hero.js 三处、初始 20 再两处】升级所需经验逐级 ×1.42 取整（20→28→40→57…）：这个 1.42 硬编码在 `hero.js grantXp`（结算，升级循环 `xpNext = round(xpNext×1.42)`）与 `hero.js skillXpHint`（预估，起档与逐级累加两处 `round(…×1.42)`）——v3.21 起预估就复刻结算的升级链，同一个 1.42 同一文件内互为镜像、却各写各的；而新档首级所需经验 20 又另写在 `core.js newGame`（`xpNext: 20`），`skillXpHint` 旧档兜底再写一句 `|| 20`——同一个 1.42 与 20 五处互不相关：想调经验曲线（如放缓到 1.35、或把首档压缩到 15 经验）要改两个文件三四处，还极易只改结算漏改预估/建档，实际升到某级所需经验变了，「距下一技能还差 N 经验」却还按旧曲线报数对不上；而写着「1.42」「20」，也无从考证这数哪儿来、两处是否早已漂移。
- 【修正：data.js 新增 `XP_GROW = 1.42` 与 `XP_INIT = 20` 为唯一真源】`grantXp` 升级结算改读 `Math.round(hero.xpNext * XP_GROW)`；`skillXpHint` 起档/逐级两处改读同一表达式、旧档兜底改读 `XP_INIT`；`core.newGame` 建档 `xpNext: XP_INIT`。收益：调经验曲线只改 data.js 一处、结算/预估/建档三处同步，绝无第二套口径；行为逐字不变（升级链 20→28→40→57→81→115→163→231→328 与历史公式完全一致，`L1xp5 冰霜击还差 43` 等既有预估数值逐字不变），零 UI 回归。
- 【零回归面】未动升级机制本身——判定 `xp >= xpNext`、xp 结转下一档、`level++`、属性成长（baseStats/WEAPONS/ARMORS）、胜利横幅经验显示、技能领悟时机等 v1.x 起语义逐字不变；未动任何战斗/掉落/金币/难度/成就/存档。只新增两个常量 + 改五个引用点（data 定义/导出、hero 结算与预估三处、core 建档一处）。
- 验证：`node --check js/data.js js/hero.js js/core.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v16.4 专项冒烟（/tmp/jrpg_smoke_v164_xpgrow.mjs）**15 项断言全过**——常量导出 1.42/20、真实 `grantXp` 驱动跨档升级 xpNext 与 `round(20×XP_GROW)=28` 逐字耦合、升级链基准 L1→L8（20/28/40/57/81/115/163/231/328）与历史公式全一致、真实 `skillXpHint` 预估（L1xp5 还差 43 / L2xp10 还差 18 / 旧档缺 xpNext 兜底 XP_INIT 还差 48 / 已学满 null）与既有数值逐字一致、`newGame` 建档 `xpNext = XP_INIT = 20`、hero/core 旧裸字面量（`×1.42` / `xpNext: 20` / `|| 20`）已从源码清除且 import/定义/导出皆在位。

## v16.3 蓄力倍率单一数据源——帮助页「战斗」行标注同读 CHARGE_MULT（机制·单一口径，与 FLEE_SUCCESS / CRIT_MULT / DROP_* 同体系）

- 【`×1.5` 只在帮助页「战斗」行一处裸奔】蓄力倍率 = 下击/技能威力 ×1.5：`CHARGE_MULT = 1.5`（data.js）自 v14.0 蓄力语义收敛起就是结算（battle.js `attackMove` 的 `×CHARGE_MULT`、`doCharge` 日志 `威力 ×${CHARGE_MULT}`）与战斗显示（drawBattle.js 状态栏/横幅 `×${CHARGE_MULT}`）两面的唯一真源——唯独帮助页操作说明「战斗」行还写着硬编码字面量 `6蓄力(下击/技能×1.5)`：同一行「逃跑约60%」早已由 FLEE_SUCCESS 推导，蓄力倍率却是本行漏网的最后一处裸数值。想调蓄力强度（如放宽到 1.6）要改结算/显示之外**还要记得改帮助页**，极易只改结算漏改标注，实际威力变了帮助页却还标旧倍率；而写着「×1.5」，玩家也不知这数从哪来、靠不靠谱。v15.8 收口防御时已把帮助页漏网定为同类病根，蓄力是机制数值数据化后残留在帮助页操作行的最后一处字面量。
- 【修正：帮助页「战斗」行改由 CHARGE_MULT 拼入】`6蓄力(下击/技能×' + CHARGE_MULT + ')` —— 与结算/显示同一单一数据源。收益：调蓄力平衡只改 data.js 一处、结算/显示/帮助页三处同步；显示文案 `6蓄力(下击/技能×1.5)` 逐字不变（CHARGE_MULT=1.5 原样拼入），零 UI 回归。
- 【零回归面】未动蓄力机制本身——蓄力语义（只加成攻击/伤害技能威力、治愈不消耗蓄力）、`attackMove` 结算 `charged ? CHARGE_MULT : 1`、日志文案 `威力 ×1.5 ！`、drawBattle 两处显示均逐字不变。只改 data.js 帮助页一行、零新增依赖。
- 验证：`node --check js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v16.3 专项冒烟（/tmp/jrpg_smoke_v163_charge.mjs）**11 项断言全过**——CHARGE_MULT 导出 1.5、帮助页「战斗」行由常量推导且与旧字面量逐字一致（含同行 FLEE_SUCCESS 推导）、battle 结算/日志与 drawBattle 两处显示同读 CHARGE_MULT、帮助页源码裸字面量 `×1.5` 已清除、import/定义/导出在位、整行文案渲染无异常。

## v16.2 酿造配方/灵药描述单一数据源——brewNow 结算与酿造台配方/可酿判定/差额标注同读 BREW_* 与 ELIXIR_*（机制·单一口径，与 POTION_*/DROP_* 同体系，即 v16.1 收口药水时声明未动的「酿造差额」）

- 【2 株蘑菇 + 10 金散落两处、灵药描述再写一句字面量】酿造高级灵药 = 消耗 `2` 株魔法蘑菇 + `10` 金币产出 1 瓶：这两个数硬编码在 `core.js brewNow`（结算，`mushrooms < 2 || gold < 10` 两判定 + `-= 2 / -= 10` 两扣减）与 `view/menus.js drawBrew`（配方文案「配方：2 株魔法蘑菇 + 10 金币 → 高级灵药 ×1」、可酿判定 `mushrooms>=2&&gold>=10`、差额标注 `max(0,2-…) / max(0,10-…)`）两文件五处互不相关；而酿造台的灵药描述「高级灵药：恢复 80% HP + 40% MP」更是 **v16.1 收口 POTION_*/ELIXIR_* 时唯一漏网的旧字面量**（0.8/0.4 至今未接常量）——同一个 2/10 与 80%/40% 五处互不相关：想调配方（如改 3 株 + 15 金）或调灵药强度要改两三个地方，还极易只改结算漏改文案，酿造成本变了界面却还标旧配方。
- 【修正：data.js 新增 `BREW_MUSHROOMS = 2` 与 `BREW_GOLD = 10` 为唯一真源】`brewNow` 结算改读 `hero.mushrooms < BREW_MUSHROOMS || hero.gold < BREW_GOLD` 与 `-= BREW_MUSHROOMS / -= BREW_GOLD`；`drawBrew` 配方文案、可酿判定、差额标注全改由两常量推导；灵药描述改由 `Math.round(ELIXIR_HP_PCT * 100)` + `Math.round(ELIXIR_MP_PCT * 100)` 推导（补上 v16.1 漏网）。收益：调酿造/灵药平衡只改 data.js 一处、两处同步；显示文案 `配方：2 株魔法蘑菇 + 10 金币 → 高级灵药 ×1`、`高级灵药：恢复 80% HP + 40% MP`、`材料不足（还差 N 株蘑菇、M 金币）` 逐字不变（2/10/80/40 全由常量推导），零 UI 回归。
- 【零回归面】未动酿造机制本身——判定顺序（先任务保护 `mushroomQuestProtects && mushrooms<=3` 拦截、再材料不足早退）、酿造消耗（2 蘑菇 + 10 金 → 灵药 +1）、SFX/renderHUD、任务蘑菇保护仅 ≤3 株时拦截等语义逐字不变。只新增两个常量 + 改两个引用点（core 结算 4 处、menus 展示 5 处 + 1 行注释）。
- 验证：`node --check js/data.js js/core.js js/view/menus.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v16.2 专项冒烟（/tmp/jrpg_smoke_v162_brew.mjs）**25 项断言全过**——常量导出 2/10、真实 `brewNow` 驱动足料酿造（2 蘑菇 + 10 金 → 灵药 0→1，用量与常量逐字耦合）/ 缺蘑菇差 1 不扣 / 缺金币差 1 不扣 / 任务保护（active 且蘑菇≤3）拦截、酿造台三行文案由常量推导且与旧字面量逐字一致、core/menus 的旧裸字面量已从源码清除、import/定义/导出皆在位。

## v16.1 药水恢复量单一数据源——结算、战斗预览、商店文案同读 POTION_*/ELIXIR_*（机制·单一口径，与 DROP_* / POISON_PCT / DEFEND_MULT 同体系）

- 【0.5/+8 与 0.8/+20 散落三处，预览与商店再各写自己的字面量】药水恢复量 = 普通药水 `round(hpMax×0.5)+8`、高级灵药 `round(hpMax×0.8)+20` HP 与 `round(mpMax×0.4)` MP：这三个比例与两个加值硬编码在 `hero.js takePotion`（结算）、`view/drawBattle.js` 战斗指令栏恢复量预览（纯显示）、`shop.js` 药水条目文案 `恢复 50%HP +8`（纯显示）三处互不相关——想调药水强度（如灵药回血放宽到 85%）要改两个文件三处，还极易只改结算漏改预览/商店，结算回 X 血界面却还标旧值；而写着「50%HP +8」，玩家也看不出这数到底从哪来、靠不靠谱。v4.1/v12.2 起药水数值散落至今，是本作机制数值中继 v16.0 战斗掉落后的最后一处结算/展示裸奔。帮助页「高级灵药」行只写「可同时恢复 HP/MP」无数字，不涉字面量。
- 【修正：data.js 新增 `POTION_HP_PCT = 0.5 / POTION_HP_FLAT = 8 / ELIXIR_HP_PCT = 0.8 / ELIXIR_HP_FLAT = 20 / ELIXIR_MP_PCT = 0.4` 为唯一真源】三处同读一份数据：`takePotion` 结算改读 `Math.round(hero.hpMax * ELIXIR_HP_PCT) + ELIXIR_HP_FLAT` / `Math.round(hero.mpMax * ELIXIR_MP_PCT)` / `Math.round(hero.hpMax * POTION_HP_PCT) + POTION_HP_FLAT`；战斗预览改由同一表达式推导（与结算逐字同源）；商店文案改由 `Math.round(POTION_HP_PCT * 100)` + `POTION_HP_FLAT` 推导。收益：调药水平衡只改 data.js 一处、三处同步；显示文案 `🍖+48HP`、`🧪+84HP/+20MP`、商店 `恢复 50%HP +8` 逐字不变（0.5×100=50、8 原样），零 UI 回归。
- 【零回归面】未动药水机制本身——判定顺序（`takePotion` 先耗高级灵药、普通药只在掉血时用）、满状态不浪费、消耗计数、`hp/mp` 封顶 `Math.min`、战斗 `doItem` 与大地图 `usePotion` 均只消费 `result.h/result.m`（不重算数值，改动不扩散）等 v4.1 起语义逐字不变。只新增五个常量 + 改四个引用点（data 定义/导出、hero 结算、drawBattle 预览、shop 文案）。
- 验证：`node --check js/data.js js/hero.js js/view/drawBattle.js js/shop.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v16.1 专项冒烟（/tmp/jrpg_smoke_v161_potion.mjs）**31 项断言全过**——常量导出 0.5/8/0.8/20/0.4、真实 `takePotion` 驱动高级灵药优先（回血/回蓝与常量逐字耦合、封顶正确）与普通药分支（+48、不回蓝）、真实 `buildShopList` 药水文案与旧字面量逐字一致、hero/drawBattle/shop 三处源码旧裸数字 0.5/8/0.8/20/0.4 已清除且 import/定义/导出皆在位。

## v16.0 战斗随机掉落概率/金币数额单一数据源——档位判定与帮助页「战斗掉落」标注同读 DROP_*（机制·单一口径，与 CHEST_* / FLEE_SUCCESS / BURN_PCT 同体系）

- 【0.08/0.2/0.32/0.38 与 +60 散落两处，帮助页再各写自己的字面量】战斗随机掉落 = 胜利后约 38% 触发：`roll < 0.08` 装备升级（木剑/铁剑→秘银剑、布衣/皮甲→锁子甲，已最好则 +60 金）、`< 0.2` 药水、`< 0.32` 魔法蘑菇（仅雾语林/矿脉，否则并入药水档）、`< 0.38` 高级灵药：这四个边界与金币数额 60 硬编码在 `rules.js rollDrop`，`data.js` 帮助页「战斗掉落」行又自写一句字面量「胜利后约 38% 触发随机掉落：8% 装备或+60金 · 12% 药水 · 12% 蘑菇 · 6% 高级灵药」——v12.4 只把概率**讲明**、刻意未动这些数字，两处自此互不相关：想调出货率（如装备档放宽到 10%）要改两个地方，还极易只改结算漏改标注，实际掉率变了帮助页却还标旧值；而写着「8%」「38%」，玩家也看不出这数到底从哪来、靠不靠谱。v15.3 收口宝箱开箱时已把「帮助页又各写一句字面量」定为同款病根，战斗掉落是判定旁剩下的最后一处结算/标注裸奔数值。
- 【修正：data.js 新增 `DROP_EQUIP = 0.08 / DROP_POTION = 0.12 / DROP_MUSHROOM = 0.12 / DROP_ELIXIR = 0.06 / DROP_GOLD = 60` 为唯一真源】两处同读一份数据：`rollDrop` 档位判定改由常量**累进推导**（`edgePotion = DROP_EQUIP + DROP_POTION` → 原 0.2、`edgeMushroom = edgePotion + DROP_MUSHROOM` → 原 0.32、`edgeElixir = edgeMushroom + DROP_ELIXIR` → 原 0.38），金币兜底改读 `hero.gold += DROP_GOLD`、文案改拼 `'✨ 宝箱：金币 +' + DROP_GOLD`；帮助页「战斗掉落」行改由常量推导（合计 `round((DROP_EQUIP+DROP_POTION+DROP_MUSHROOM+DROP_ELIXIR)×100)`、各档 `round(N×100)`、`DROP_GOLD` 原样拼入）。收益：调出货率只改 data.js 一处、两处同步；显示文案 `胜利后约 38% 触发随机掉落：8% 装备或+60金 · 12% 药水 · 12% 蘑菇 · 6% 高级灵药` 逐字不变（0.38/0.08/0.12/0.12/0.06/60 全由常量推导），零 UI 回归。
- 【零回归面】未动掉落机制本身——判定顺序（先装备、再药水、再蘑菇、再灵药）、装备档升级对象与「已最好则 +60 金」分支、蘑菇档仅 dungeon/cave 生效否则并入药水、`hero.drops` 计数、成就「幸运眷顾」等 v5.2/v12.4 语义逐字不变。只新增五个常量 + 改两个引用点（rollDrop 判定/文案、帮助页 1 行）。
- 验证：`node --check js/data.js js/rules.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v16.0 专项冒烟（/tmp/jrpg_smoke_v160_drop.mjs）**22 项断言全过**——常量导出 0.08/0.12/0.12/0.06/60、帮助页文案由常量推导且与旧字面量逐字一致、真实 `rollDrop` 蒙特卡洛 20 万次分布逐字不变（雾语林≈8/12/12/6、合计≈38%、城镇蘑菇并入药水 24% 且绝不落蘑菇、装备档兜底 +60 金文案 `金币 +60` 原样）、rollDrop 存活代码与帮助页行源码中旧裸字面量 0.08/0.2/0.38/60/38% 已清除。

## v15.9 Boss 重击倍率单一数据源——真身/平时两档结算与逐招受击预判同读 HEAVY_MULT / HEAVY_MULT_PHASED（机制·单一口径，与 DEFEND_MULT / CRIT_MULT / SHIELD_MULT 同体系）

- 【`enemy.phased ? 2.3 : 1.9` 硬编码两处，预判与结算互不相关】敌方重击伤害 = `cmdDmg(敌攻, 我防, ×2.3 真身 / ×1.9 平时)`：这两个倍率以三元表达式裸写在 `enemyAI.js enemyAct`（结算，深渊之怒实伤）与 `view/drawBattle.js` Boss 逐招受击预判（`重击 -N血`，防御后预测）两处——同一个 2.3/1.9 两处互不相关：想调重击强度（如真身放宽到 2.5、平时收到 1.7）要改两个文件，还极易只改结算漏改预判，实际掉血变了预判却还报旧值对不上。v15.2 收口石甲时已点名防御「另一机制保持原样」，随后 v15.7/v15.8 连收中毒与防御，本作机制数值至此只剩重击倍率一处仍在结算/预判间裸奔。
- 【修正：data.js 新增 `HEAVY_MULT = 1.9` 与 `HEAVY_MULT_PHASED = 2.3` 为唯一真源】两处同读一份数据：`enemyAct` 重击结算改读 `enemy.phased ? HEAVY_MULT_PHASED : HEAVY_MULT`；`drawBattle` Boss 非真身/真身两分支受击预判改读同一表达式（与结算同式同序）。收益：调重击平衡只改 data.js 一处、两处同步；预判数值逐字不变（真身 2.3 / 平时 1.9 原样），零 UI 回归。
- 【零回归面】未动重击机制本身——判定时机（`pickAct` 按 `type:'heavy'` 权重选招）、`cmdDmg(atk, defMax, mult)` 公式（`mult=1` 时即普攻）、防御中再 ×DEFEND_MULT 取整、`phased` 由变身分支 `enemy.phased = true` 置位、重击震屏 `S.shake`（纯显示）等语义逐字不变。只新增两个常量 + 改两个引用点。
- 验证：`node --check js/data.js js/enemyAI.js js/view/drawBattle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v15.9 专项冒烟（/tmp/jrpg_smoke_v159_heavy.mjs）**5 项断言全过**——常量导出 1.9/2.3、真实 `enemyAct` 驱动非真身重击伤害与 `cmdDmg(…, HEAVY_MULT)` 逐字耦合（且与真身档可区分）、真身重击伤害与 `cmdDmg(…, HEAVY_MULT_PHASED)` 逐字耦合；grep 确认两处可执行代码的裸 `? 2.3 : 1.9` 已清除。

## v15.8 防御架势数值单一数据源——结算、防御中横幅、受击/反击预判同读 DEFEND_MULT / DEFEND_MP / COUNTER_CHANCE / COUNTER_MULT（机制·单一口径，与 SHIELD_MULT / CRIT_MULT / POISON_PCT / FLEE_SUCCESS 同体系）

- 【0.5 / 2 / 0.5 / 0.7 散落三处，横幅与预判再各写自己的字面量】防御架势 = 防御中受到的最终伤害再 ×0.5 取整（保底 1）、防御回合回 2 MP、被命中 50% 几率趁隙反击（反击伤害 = `cmdDmg(英雄攻, 敌防, ×0.7)`，未传浮动参、值精确确定）：这四个数硬编码在 `battle.js doDefend`（回 2 MP）与 `enemyAI.js enemyAct`（减伤、反击判定、反击伤害三处），`view/drawBattle.js` 又各写自己的字面量（横幅「防御中 · 减伤50% · 回2MP/回合 · 50%几率反击」、受击预判再 ×0.5 取整、反击预判 ≈N伤 再 ×0.7）——同一个 0.5/2/0.5/0.7 五处互不相关：想调防御强度（如减伤放宽到 0.45、回蓝加到 3、反击概率提到 60%）要改三个文件四处，还极易只改结算漏改横幅/预判，结算已减 50% 界面却还标旧值，对不上；而写着「减伤50%」，玩家也看不出这数到底从哪来、靠不靠谱。v15.2 收口石甲时已点名此处防御「另一机制保持原样」，本作机制数值至此全部数据化收口。
- 【修正：data.js 新增 `DEFEND_MULT = 0.5 / DEFEND_MP = 2 / COUNTER_CHANCE = 0.5 / COUNTER_MULT = 0.7` 为唯一真源】四处同读一份数据：`doDefend` 回蓝改读 `Math.min(hero.mpMax, hero.mp + DEFEND_MP)`（满蓝不浪费、`gained` 驱动日志文案）；`enemyAct` 减伤改读 `Math.max(1, Math.round(dmg * DEFEND_MULT))`、反击判定改读 `Math.random() < COUNTER_CHANCE`、反击伤害改读 `cmdDmg(hero.atkMax, enemy.def, COUNTER_MULT)`（与预判同式同参、值确定）；横幅改由 `Math.round((1 - DEFEND_MULT) * 100)` + `DEFEND_MP` + `Math.round(COUNTER_CHANCE * 100)` 推导，受击/反击预判（Boss 与非 Boss 两分支）同读 DEFEND_MULT / COUNTER_MULT。收益：调防御平衡只改 data.js 一处、五处同步；显示文案 `防御中 · 减伤50% · 回2MP/回合 · 50%几率反击` 逐字不变（(1−0.5)×100=50、2、0.5×100=50 原样），零 UI 回归。
- 【零回归面】未动防御机制本身——判定时机（玩家行动回合起手 `hero.defending = true`、`playerAction` 经指令表 `defend: doDefend` 分发、战斗结算/下回合起手 `defending = false` 复位）、减伤后保底 1、反击在被命中判定内联（一回合至多一次）、反击值确定不掺随机（预判可放心展示）、设置类行动（药水/蓄力等）照旧耗回合等 v5.3/v6.7 起语义逐字不变。只新增四个常量 + 改四个引用点。
- 验证：`node --check js/data.js js/battle.js js/enemyAI.js js/view/drawBattle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；v15.8 专项冒烟（/tmp/jrpg_smoke_v1580_defend.mjs）**39 项断言全过**——常量导出 0.5/2/0.5/0.7、横幅由常量推导且与旧字面量逐字一致、真实 `playerAction('defend')` 驱动回蓝（48→50 且满蓝不浪费）、真实 `enemyAct` 减伤与 `max(1, round(cmdDmg×DEFEND_MULT))` 逐字耦合、反击 0.4 触发 / 0.99 不触发且伤害与 `cmdDmg(hero.atkMax, enemy.def, COUNTER_MULT)` 逐字耦合、drawBattle Boss/非 Boss 两分支受击/反击预判同读常量、五处旧裸字面量已从源码清除、battle/enemyAI/drawBattle/data 的 import/定义/导出皆在位、防御中绘制不抛异常。

## v15.7 中毒每回合扣血比例/持续时间单一数据源——结算、战斗角标、施毒、帮助页/图鉴标注同读 POISON_PCT / POISON_TURNS（机制·单一口径，与 BURN_PCT / CRIT_MULT / SHIELD_MULT 同体系）

- 【0.05 散落两处、3 硬编码一处、帮助页/图鉴再各写一句字面量】中毒每回合扣血 = `max(2, round(hpMax×0.05))`：这个 0.05 硬编码在 `battle.js applyPoisonTick`（结算）与 `view/drawBattle.js` 战斗角标「每回合 -N血」两处；中毒持续回合 = 3 硬编码在 `enemyAI.js` 施毒分支（`hero.poison = 3`）；`data.js` 帮助页「毒蛇有 35% 概率使你中毒：每回合扣血（约5%最大HP）持续 3 回合」与毒蛇图鉴 `tag:'☠️ 会施毒 · 扣血3回合'` 又各写自己的字面量——同一个 5% 与 3 回合四处互不相关：想调中毒强度（如放宽到 6%）要改四个地方，还极易只改结算漏改角标/帮助页，结算扣 6% 界面却还标「约5%」对不上；而写着「约 5%」，玩家也看不出这数到底从哪来、靠不靠谱。本作其余机制（灼烧/暴击/石甲/蓄力/逃跑/困难倍率/试炼回血/元素克制/宝箱掉落）早已数据化，中毒是 v15.2 收口石甲后剩下的最后一处裸奔数值。
- 【修正：data.js 新增 `POISON_PCT = 0.05` 与 `POISON_TURNS = 3` 为唯一真源】四处同读一份数据：`applyPoisonTick` 结算改读 `Math.max(2, Math.round(hero.hpMax * POISON_PCT))`；战斗角标改由同一表达式推导（与结算逐字同源）；`enemyAct` 施毒改读 `hero.poison = POISON_TURNS`；帮助页改由 `Math.round(MON_BASE.find(m=>m.name==='毒蛇').poison * 100)`（施毒概率读 MON_BASE 真源）+ `Math.round(POISON_PCT * 100)` + `POISON_TURNS` 推导，图鉴毒蛇 tag 改由 `POISON_TURNS` 推导。收益：调中毒平衡只改 data.js 一处、四处同步；显示文案 `毒蛇有 35% 概率使你中毒：每回合扣血（约5%最大HP）持续 3 回合` 与 `☠️ 会施毒 · 扣血3回合` 逐字不变（0.05×100=5、3 原样），零 UI 回归。
- 【零回归面】未动中毒机制本身——结算时机（玩家行动回合起手，毒发先于行动）、伤害下限 `max(2,…)`、层数递减 `poison--`、施毒判定 `Math.random() < enemy.poison`（概率仍读 MON_BASE 毒蛇 `poison:0.35`）、治愈术 `cleanse` 解毒等 v12.x 语义逐字不变；`治愈术`/`启示` 等既有恢复/净化路径未动。只新增两个常量 + 改四个引用点。
- 验证：`node --check js/data.js js/battle.js js/view/drawBattle.js js/enemyAI.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v15.7 专项冒烟（/tmp/jrpg_smoke_v1570_poison.mjs）**30 项断言全过**——常量导出 0.05/3、帮助页与图鉴 tag 由常量推导且与旧字面量逐字一致、真实 `playerAction` 驱动中毒结算（200→-10 与 `max(2, round(hpMax×POISON_PCT))` 逐字耦合、层数 3→2、毒发先于攻击、floor=2 兜底、无中毒不触发）、真实 `enemyAct` 施毒（0.2<0.35 中 3 回合 / 0.9≥0.35 不中）、三处旧裸字面量已从源码清除、battle/drawBattle/enemyAI/data 的 import/定义/导出皆在位。

## v15.6 对话框文字自动换行——超宽长行不再右缘截断（体验·信息透明：对话逐字可读）

- 【对话框不换行，超宽行直接撞右缘被截】`view/menus.js drawTalk` 把每页对话按「数据里写死的一行 = 画面一行」（`drawRichLine` 只做【】金色强调与打字机截字，从不折行）直绘在 `x=136` 起点、无右缘约束——灯长任务 offer 那句「林子里的【魔法蘑菇】带着残灯的荧光——是灯油最后的原料。帮我找回 3 株，好吗？」实测 ~578px，超出可用宽 440px 十几个字，末尾「好吗？」被面板右框切掉一半；对全量对话页扫描（QUESTS.talk 各状态 + STORY + NPC 通用对话共 14 页 44 行）确认还有 `side_mushroom[done]`「谢谢你。井泉的灯油续上了…」~510px 同样超宽。这类问题以后新增长文案还会再犯——修数据只治这两句，不治根。
- 【修正：绘图层像素感知自动换行】`rules.js` 新增纯函数 `wrapTalkLine(line, maxW, measure)`：按 `measure`（注入 CTX.measureText，Node 冒烟可用假量宽）把逻辑行折成 ≤ 可用宽 440px 的视觉片段；【…】金色强调块在折行时整体换行不拆层（除非单块本身超宽才逐字兜底）。`drawTalk` 改用折行片段逐段绘制：打字机预算（`pageShownAt` 的每行字数）沿片段顺序消耗，视觉行数驱动面板高度向下扩（`bh = min(170, 140 + (行数-4)×27)`，`by+bh` 封顶 470 不越画布），单行物理超宽再也不截断。纯显示、零结算变化、不新增任何状态。
- 【验证】`node --check` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v15.6 专项冒烟（/tmp/jrpg_smoke_talk_wrap.mjs）**13 项断言全过**——灯长长行折 2 段全 ≤440px 且拼接还原原文、【魔法蘑菇】块不拆、窄宽兜底、空/null/短行安全、全量 14 页折行后无任何超宽或底部越界、drawTalk 渲染超宽页与打字机中途片段化渲染均不抛错；任务栏常驻冒烟（27 项）复跑仍全绿。

## v15.4 任务栏常驻显示——画布下方 DOM 不再随场景闪隐（体验·信息透明：目标时刻可见）

- 【任务栏只在 world/ending 两个场景出现，进战斗/商店/菜单/对话就消失】`view/hud.js renderHUD` 用 `qshow = S.scene === 'world' || S.scene === 'ending'` 控制 `#quest` 的 `show` class——平时在地图上有任务栏，一开战斗、一进商店/旅店/状态/图鉴/对话等 17 个场景就被 `display:none` 藏掉，「这一栏时显时不显」：玩家想随时扫一眼当前主线/支线目标（右下角信息透明主题的核心诉求）却要在世界图上才能看到，翻菜单时完全断供。
- 【修正：非开局前场景一律常驻】`qshow` 反转为 `S.scene !== 'title' && S.scene !== 'create' && S.scene !== 'story'`——`#quest` 是画布**下方**的 DOM 块（`index.html` 里位于 canvas 与 hud 之间），不遮挡任何画面，常驻零风险；仅在开局前的标题/建角/开场白三屏隐藏（彼时无进行中任务，显示空栏不和谐），其余 19 个场景（world/battle/shop/inn/brew/status/journal/codex/ach/help/travel/pause/talk/dead/win/ending 等）全部常驻显示。纯显示、零结算变化，不新增任何状态。
- 【验证】`node --check` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v15.4 专项冒烟（/tmp/jrpg_smoke_hud_questbar.mjs）**27 项断言全过**——title/create/story 隐藏、16 个进行中场景常驻、world→battle→world 切换不抖动、回 title 无残留、主线文案 🎯 前缀写入、questBannerLines 全新档案不抛错。

## v15.3 宝箱掉落概率/金币公式单一数据源——开箱判定与帮助页「宝箱掉落」标注同读 CHEST_*（机制·单一口径，与 CRIT_MULT/FLEE_SUCCESS/BURN_PCT 同体系，即 v14.9 声明「另一处独立概率，本次不碰」的宝箱开箱裸奔数值）

- 【0.6 / 0.45 / 12、5 散落两处，帮助页再写自己的字面量】开箱掉落 = 雾语林先判 `Math.random()<0.6` 得蘑菇、余下再判 `Math.random()<0.45` 得金币（数额 `12+级×5`）、否则药水；城镇/矿脉跳过蘑菇直接 45% 金币 / 55% 药水——这四个数硬编码在 `world.js onChestStep`，`data.js` 帮助页「宝箱掉落」行又各写一句字面量「60%蘑菇·18%金币(12+级×5)·22%药水；45%金币·55%药水」：想让蘑菇掉率放宽到 70% 要改两个地方，还极易只改判定漏改标注，实际掉率变了帮助页却还标旧值；而写着「60%」「18%」，玩家也看不出这数到底从哪来、靠不靠谱。v14.9 引入 FLEE_SUCCESS 时已点名此处「另一处独立概率，本次不碰」，本作机制数值至此全部数据化收口。
- 【修正：data.js 新增 `CHEST_MUSHROOM = 0.6 / CHEST_GOLD = 0.45 / CHEST_GOLD_BASE = 12 / CHEST_GOLD_PER_LV = 5` 为唯一真源】两处同读一份数据：`onChestStep` 判定改读 `Math.random() < CHEST_MUSHROOM` / `Math.random() < CHEST_GOLD`、金币改读 `CHEST_GOLD_BASE + hero.level * CHEST_GOLD_PER_LV`；帮助页「宝箱掉落」标注改由常量推导（蘑菇 `round(60×100)%`、雾语林金币 `round((1−0.6)×0.45×100)%`、药水补齐、城镇 45/55 同源）。收益：调开箱平衡只改 data.js 一处、两处同步；显示文案 `60%蘑菇·18%金币(12+级×5)·22%药水；45%金币·55%药水` 逐字不变（60/18/22/45/55 全由常量推导），零 UI 回归。
- 【零回归面】未动开箱机制本身——判定顺序（dungeon 先判蘑菇）、金币公式（12+级×5）、任务蘑菇保护/集齐提示（`setSideQuest`）、已开宝箱格等同地面等 v12.10 语义逐字不变。只新增四个常量 + 改两个引用点。
- 验证：`node --check js/data.js js/world.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v15.3 专项冒烟（/tmp/jrpg_smoke_v1530_chest.mjs）**22 项断言全过**——常量导出 0.6/0.45/12/5、帮助页标注由常量拼接推导且与旧字面量逐字一致、两处旧裸字面量已从源码清除、world.js 改读 CHEST_*、`onStep` 真实开箱蒙特卡洛 3 万次分布逐字不变（雾语林≈60/18/22、城镇≈45/55、绝不落蘑菇、金币 Lv.5→37）、drawHelp 第 3 页渲染不抛异常且纯显示收敛。

## v15.2 石甲减伤倍率单一数据源——结算、预览、凝甲提示同读 SHIELD_MULT（机制·单一口径，与 CRIT_MULT/FLEE_SUCCESS/BURN_PCT 同体系，即 v14.9 声明「保持原样」的最后一处裸奔数值）

- 【0.6/40% 散落三处且互不相关】石甲减伤 = 命中最终伤害再 ×0.6 取整（保底 1）并消耗 1 层：这个 0.6 硬编码在 `battle.js attackMove`（结算）与 `rules.js withShield`（伤害预览）两处，`enemyAI.js` 凝甲博客又自己写一个派生值「所受伤害降低 40%」——同一个 0.6 三处互不相关：想调石甲强度（如放宽到 0.5，减伤 50%）要改三个地方，还极易只改结算漏改预览/提示，结算减 40% 界面却还标「×0.6」/「降低 40%」对不上；而写着「40%」，玩家也看不出这数到底从哪来、靠不靠谱。v14.9 引入 FLEE_SUCCESS 时已点名此处「保持原样」，本作机制数值至此全部数据化收口。
- 【修正：data.js 新增 `SHIELD_MULT = 0.6` 为唯一真源】三处同读一份数据：`attackMove` 结算改读 `Math.max(1, Math.round(dmg * SHIELD_MULT))`；`withShield` 预览改读同一表达式；`enemyAI` 凝甲提示改由 `Math.round((1 - SHIELD_MULT) * 100)` 推导。收益：调石甲平衡只改 data.js 一处、三处同步；显示文案 `·所受伤害降低 40%` 逐字不变（(1−0.6)×100=40），零 UI 回归。
- 【零回归面】未动石甲机制本身——命中时判定时机（attackMove 起手）、减伤后消耗 1 层、保底 1、伤害预览 `atkEstimate/skillEstimate` 经 withShield 与结算逐字同源（不虚报 ≈N伤）、技能 `陨石术 breakShield` 碎甲走后门、失败收益等 v12.x 语义逐字不变。只新增一个常量 + 改三个引用点。README 既有「与命中结算的 `×0.6` 逐字同源」一句语义不变。
- 验证：`node --check js/data.js js/rules.js js/battle.js js/enemyAI.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v15.2 专项冒烟（/tmp/jrpg_smoke_v1520_shield.mjs）**24 项断言全过**——常量导出 0.6、提示由常量推导=40、预览有甲=无甲×SHIELD_MULT 取整（含保底 1 与无甲原样）、技能预览同源、真实攻击命中石甲（Math.random 固定 0.99 钉死判定）消耗 1 层且伤害与 `cmdDmg×SHIELD_MULT` 逐字耦合、无甲对照组不受减伤、三处旧裸字面量/自写 40% 已从源码清除、battle/rules/enemyAI 的 import 与同式皆在位、战斗画面/状态页绘制不抛异常。

## v15.1 普攻暴击率/暴击倍率单一数据源——判定、结算、状态页标注同读 CRIT_RATE / CRIT_MULT（机制·单一口径，与 FLEE_SUCCESS/BURN_PCT/CHARGE_MULT 同体系）

- 【暴击 0.12 与 ×1.8 散落三处，且状态页再写自己的字面量】普攻暴击判定 `Math.random() < 0.12` 硬编码在 `battle.js doAttack`，暴击倍率 `? 1.8 : 1` 硬编码在 `battle.js attackMove`（结算），状态页（`menus.js drawStatus`）又另写一句 `· 普攻12%暴击 ×1.8`——同一个 12% 与 ×1.8 三处互不相关：想调暴击平衡（如放宽到 15%、加到 ×2.0）要改三个地方，还极易只改判定漏改标注，结算暴击率变了界面却还标旧值；而写着「12%」「×1.8」，玩家也看不出这数到底从哪来、靠不靠谱。本作其余机制（蓄力 ×1.5 / 逃跑 60% / 灼烧 4% / 困难倍率 / 试炼回血 / 元素克制）早已数据化，暴击是又一处还在裸奔的数值。
- 【修正：data.js 新增 `CRIT_RATE = 0.12` 与 `CRIT_MULT = 1.8` 为唯一真源】三处同读一份数据：`doAttack` 判定改读 `Math.random() < CRIT_RATE`；`attackMove` 结算改读 `(isCrit ? CRIT_MULT : 1)`；状态页标注改由 `'· 普攻' + Math.round(CRIT_RATE * 100) + '%暴击 ×' + CRIT_MULT` 推导。收益：调暴击平衡只改 data.js 一处、三处同步；显示文案 `· 普攻12%暴击 ×1.8` 逐字不变（0.12×100=12、1.8 原样），零 UI 回归。
- 【零回归面】未动暴击机制本身——判定时机（doAttack 起手）、浮动伤害区间 `cmdDmg` 的 0.9~1.1 方差、震屏（暴击 pow=4）、日志 💥/（暴击！）、脆皮怪一击致胜走 `finishPlayer→winBattle` 等语义逐字不变；`攻击Move` 同时服务技能（crit 恒为 false），判暴击只影响普攻，未动技能路径。只新增两个常量 + 改三个引用点。
- 验证：`node --check js/data.js js/battle.js js/view/menus.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v15.1 专项冒烟（/tmp/jrpg_smoke_v1510_crit.mjs）**23 项断言全过**——常量导出 0.12/1.8、标注由常量推导且与旧字面量逐字一致、0.05<0.12 暴击（伤害与 `cmdDmg×CRIT_MULT` 逐字耦合、日志/震屏 pow=4/回合+1）、0.99≥0.12 普通（伤害与 `cmdDmg×1` 一致、无暴击文案）、三处旧字面量已从源码清除、data 常量/导出与 battle/menus 的 import 皆在位。

## v15.0 灼烧每回合扣血比例单一数据源——结算、角标、技能提示同读 BURN_PCT（机制·单一口径，与 FLEE_SUCCESS/CHARGE_MULT/DIFF_SCALE 同体系）

- 【0.04 散落三处，且提示再写一句字面量「约4%」】火焰斩的灼烧每回合扣血 = `max(2, round(hpMax×0.04))`：这个 0.04 硬编码在 `enemyAI.js enemyAct`（结算）与 `view/drawBattle.js`（战斗角标「每回合 -N血」）两处，data.js 火焰斩提示又各自写死「约4%最大HP」——同一个 4% 三处互不相关：想调灼烧强度（如放宽到 5%）要改三个地方，还极易只改结算漏改角标/提示，结算烧 5% 界面却还标「约4%」，对不上；而写着「约 4%」，玩家也看不出这数到底从哪来、靠不靠谱。本作其余机制（蓄力 ×1.5 / 逃跑 60% / 困难倍率 / 试炼回血 / 元素克制）早已数据化，灼烧是又一处还在裸奔的数值。
- 【修正：data.js 新增 `BURN_PCT = 0.04` 为唯一真源】三处同读一份数据：`enemyAct` 灼烧结算改读 `Math.max(2, Math.round(enemy.hpMax * BURN_PCT))`；战斗角标 `每回合 -N血` 改由同一表达式推导（与结算逐字同源，一眼看清烧多少）；火焰斩提示改由 `Math.round(BURN_PCT * 100)` 推导。收益：调灼烧平衡只改 data.js 一处、三处同步；显示文案 `·每回合 -8血` / 提示 `·灼烧2回合·每回合约-4%最大HP` 逐字不变（0.04×100=4），零 UI 回归。
- 【零回归面】未动灼烧机制本身——伤害下限 `max(2,…)`、层数递减 `burn--`、每回合结算时机（enemyAct 开头）、灼烧致死走 `winBattle` 早退等 v12.x 语义逐字不变；火焰斩 `burn:2`（层数）原就在 data.js，保持不动。只新增一个常量 + 改三个引用点。
- 验证：`node --check js/data.js js/enemyAI.js js/view/drawBattle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v15.0 专项冒烟（/tmp/jrpg_smoke_v1500_burnpct.mjs）**19 项断言全过**——常量导出为 0.04、提示由常量推导且与旧字面量逐字一致、结算伤害与 `max(2, round(hpMax×BURN_PCT))` 逐字耦合（200→8 / 20→2 兜底）、灼烧致死走 winBattle 早退不触发攻击分支、角标与结算同常量、两处旧裸 `hpMax * 0.04` 已从源码清除、data 常量/导出/import 皆在位。

## v14.9 逃跑成功率单一数据源——判定与指令栏/帮助页文案同读 FLEE_SUCCESS（机制·单一口径，与 CHARGE_MULT/DIFF_SCALE/RUSH_RECOVER 同体系）

- 【逃跑成功率 0.6 散落三处，且指令栏/帮助页各写自己的字面量】普通怪逃跑判定 `Math.random() < 0.6` 硬编码在 `js/battle.js doFlee`，战斗指令栏（`view/drawBattle.js`）却另写一句「成功率约60%」，帮助页（`data.js HELP_PAGES[0]`）再写一处「约60%」——同一个 60% 三处互不相关：想调逃跑平衡（如放宽到 65%）要改三个地方，还极易只改判定漏改文案，结算是 65% 界面却还标「约60%」，对不上；而写着「约 60%」，玩家也看不出这数到底从哪来、靠不靠谱。本作其余机制（蓄力 ×1.5 / 元素克制 / 困难倍率 / 试炼回血）早已数据化，逃跑率是最后几个还在裸奔的数值之一。
- 【修正：data.js 新增 `FLEE_SUCCESS = 0.6` 为唯一真源】三处同读一份数据：`battle.doFlee` 判定改读 `Math.random() < FLEE_SUCCESS`；指令栏标注改由 `Math.round(FLEE_SUCCESS * 100) + '%'` 推导；帮助页战斗行改由同一常量推导。收益：调平衡只改 data.js 一处、三处同步；显示文案 `·成功率约60%` / `4逃跑(约60%)` 逐字不变（0.6×100=60），零 UI 回归。
- 【零回归面】未动逃跑逻辑本身——Boss 气场压制不可逃、「逃败真实耗回合」等 v14.1 语义逐字不变；石甲的 `×0.6` 减伤是另一机制（rules.attackMove），保持原样；`world.js` 宝箱开出蘑菇的 0.6 是另一处独立概率，本次不碰。只新增一个常量 + 改三个引用点。
- 验证：`node --check js/data.js js/battle.js js/view/drawBattle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v14.9 专项冒烟（/tmp/jrpg_smoke_v1490_flee.mjs）**16 项断言全过**——常量导出为 0.6、帮助页/指令栏文案由常量推导且与旧字面量逐字一致、三种逃跑行为正确（0.5 成功逃脱回 world、0.99 失败留在战场且 battleTurn+1、Boss 气场压制不耗回合）、三处旧字面量与裸 `0.6` 已从源码清除、石甲 ×0.6 保持不动。

## v14.8 无字回廊昼夜标签如实「恒暗」——「被忘掉的地方没有晨昏」（体验打磨·纯显示，承接 v14.6 昼夜标签 / 地图指南同步）

- 【回廊画面恒暗但 HUD 仍报世界时钟阶段】无字回廊是「被忘掉的地方没有晨昏」——画面着色 `drawTimeTint` 对 `gallery` 特判跳过昼夜、恒按 `rgba(10,8,28,.42)` 暗色渲染，但此前 HUD 的昼夜标签（`☀️ 白天 / 🌆 黄昏 / 🌙 夜晚 / 🌅 黎明`）仍无条件读世界时钟 `S.G.time` 的阶段：正午走进回廊，全屏一片昏暗的同时 HUD 却标着「☀️ 白天」，同一处设计在两处互相对着说——新玩家看到「名字 · 无字回廊 · ☀️白天」会以为回廊也分昼夜，与「恒暗无晨昏」的剧情口吻（README「恒暗无晨昏」/ 地图指南）自相矛盾。
- 【修正：回廊改标 🌑 恒暗】`view/hud.js renderHUD` 昼夜标签改为与 `drawTimeTint` **同判 `curMap()==='gallery'`、同读 `S.G.time`**：无字回廊内显示 `🌑 恒暗`（与画面着色、剧情文案三处同口径），其余四图照常显示 90 秒一档的昼夜标签，非回廊零差异。
- 【零回归面】只改 `js/view/hud.js` 一处（`renderHUD` 的 `s-map` 拼装，+1 行变量 + 注释）；未动 `drawTimeTint`（着色判定维持不动）、`data.js`、任何地图/遇敌/战斗/存档逻辑；`periodTag` 仅影响 `s-map` 文本，HUD 其余元素逐字不变。README「昼夜标签」一句同步补注（回廊恒暗）。
- 验证：`node --check js/view/hud.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v14.8 专项冒烟（/tmp/jrpg_smoke_v1480_gallerydark.mjs）**15 项断言全过**——回廊内 `s-map` 含 `🌑 恒暗`、不再含任何昼夜标签，回廊外四图 `S.G.time` 四个阶段（0/95/185/275）标签逐字正确（白天/黄昏/夜晚/黎明）、村镇切回廊再切回标签随地图切换即时联动、非回廊零差异、完整 `renderHUD` 连续 20 次不抛异常、无 document 兜底不抛。

## v14.7 帮助「地图指南」页终焉之神定位同步——第四张地图补入指南（体验打磨·纯数据，承接 v14.5 图鉴 LOC 修正）

- 【地图指南页漏网：终焉之神仍指向旧址】v13.7 剧情重构时终焉之神已随主线从「星井矿脉·终焉水晶」迁至**无字回廊东端祭坛**（矿脉的终焉水晶变为双徽记开门机关），v14.5 已修正记忆图鉴 `whereFind` 的 LOC 表——但 **H 帮助的「地图指南」页（HELP_PAGES[1]）仍在星井矿脉行写着 `中央终焉水晶(隐藏真Boss)`**，同一处过时信息的最后一处漏网：新玩家按 H 翻到地图指南，会被明确告知「去矿脉中央水晶找隐藏真Boss」，实际那里只能开一扇门，终焉之神在回廊尽头——与图鉴/README/实际地图（data.js `MAPS.gallery` 东端祭坛）三方说法都不一致。
- 【无字回廊未入地图指南】第四张地图「无字回廊」（名字石碑·残焰魔像·终焉之神）已存在多时，但地图指南页只列了三张地图 + 通关之路——回廊这张高难终局图在帮助里完全缺席，玩家打到双徽记开门后全靠摸黑。
- 【修正】`js/data.js` `HELP_PAGES[1]` 三行：星井矿脉行由 `中央终焉水晶(隐藏真Boss)` 改为 `中央终焉水晶集齐双徽记后化为门`（与 README「水晶化为门 → 无字回廊」逐字同口径）；新增 `无字回廊` 行 `名字石碑·残焰魔像·东端祭坛藏终焉之神（极高难）`（地图名/内容与 `MAPS.gallery`、`TRAVEL_LIST` 推荐 Lv.10 风格同源）；通关之路行改为四幕链 `讨回灯芯 → 击败洞窟领主 → 双徽记开门 → 回廊尽头面对终焉之神`（呼应 v13.7 四幕结构）。
- 【零回归面】只改 `js/data.js` `HELP_PAGES[1]` 三行（纯显示数据）；未动任何地图/遇敌/Boss/结算/存档逻辑，`drawHelp` 逐字不变（5 行内容 y=80~216 仍在面板 452 界内）；其余三页帮助条目逐字未动。
- 验证：`node --check js/data.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v14.7 专项冒烟（/tmp/jrpg_smoke_v1470_helploc.mjs）**19 项断言全过**——旧文案「隐藏真Boss」与「终焉之神在矿脉」双重清除、星井矿脉行化为门文案、无字回廊行四要素（碑/精英/东端/终焉之神）与 `MAPS.gallery.name` 逐字同源、通关之路四幕链、其余三页帮助逐字在位、drawHelp 第 2 页 5 行绘制不抛异常且全部落在面板内、绘制不改 scene/helpPage（纯显示收敛）。

## v14.6 HUD 常驻攻防读数——大地图一眼可见当前战力（体验打磨·信息透明·纯显示）

- 【攻防是全局最被依赖的一对数字，却只有开 `I` 状态页或进战斗才看得到】本作所有决策都围着这对数转：商店换装比差值、选怪刷练估能不能打、祭坛/试炼碑前犹豫敢不敢开战、战斗里的「我方 攻X 防Y ⚔ 敌方」对比行与防御减伤、敌方攻击预判全读 `atkMax/defMax`——可大地图 HUD 只显示 ⚔️武器名/🛡️防具名，数值本身要按 `I` 或进一仗才能看见。买了把剑到底攻涨到几？面对 ⚠Lv.8 祭坛的召唤能不能接？都要额外开界面去查。
- 【修正：HUD 常驻 `⚔️攻 N · 🛡️防 N` 读数】HUD 第一行统计区（Lv/HP/MP/EXP 之后）新增两个元素：`⚔️攻<b id="s-atk">` 与 `🛡️防<b id="s-def">`，每次 `renderHUD` 从 `hero.atkMax/defMax` 即时写入——与状态页/战斗对比行/商店差价**逐字同源**（都用 `applyStats` 算出并落地的同一对字段），换装、升级、吃药、难度建档后即时刷新，不进战斗也一眼看清当前战力。
- 【零回归面】纯显示层：只改 `js/view/hud.js`（`renderHUD` 加 2 行赋值）与 `index.html`（HUD 加 2 个 span）；未动任何战斗/掉落/结算/存档/平衡数值，`atkMax/defMax` 的生成与消费方（`rules.applyStats`、`drawBattle`、`drawStatus`、`drawShop` 差价）逐字不变。顺带修正壳页帮助条一处过时文案：战斗指令已增到 6 键（攻击/技能/药水/逃跑/防御/蓄力），`战斗中 1-4 指令` → `1-6 指令`（纯文案，与 HELP_PAGES 的 6 键表对齐）。
- 验证：`node --check js/view/hud.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）；新增 v14.6 专项冒烟（/tmp/jrpg_smoke_v1460_hudstats.mjs）**12 项断言全过**——`renderHUD` 对 DOM 桩写入 `s-atk/s-def` 且与 `hero.atkMax/defMax` 逐字一致、换装（木剑→铁剑/布衣→皮甲）后读数即时刷新、`atkMax` 缺省时兜底 0 不写 NaN、既有元素（s-weapon/s-armor/s-snd/s-map）回归在位、完整 `renderHUD` 不抛异常。

## v14.5 图鉴位置标注修正——终焉之神随剧情迁址、残焰魔像补齐定位（体验打磨·纯显示）

- 【终焉之神位置已过时】v13.7 剧情重构时终焉之神已随主线从「星井矿脉·终焉水晶」迁至**无字回廊东端祭坛**（矿脉的终焉水晶变为开门机关），但记忆图鉴 `whereFind` 的 LOC 表仍写旧址「星井矿脉·终焉水晶」——图鉴已讨伐行显示的位置与实际地图（data.js `MAPS.gallery` 东端 `SB` 祭坛）不符。
- 【残焰魔像缺失定位】无字回廊中段精英「残焰魔像」（图鉴第 13 种）此前不在 LOC 表里，图鉴已讨伐行会笼统误显示「草丛随机遇敌」，而它实际是**固定坐标的 Boss 位精英**（MAPS.gallery 中段 `MB`），跟草丛随机遇敌不是一回事。
- 【修正】`js/view/menus.js` 的 `whereFind` LOC 表两处：终焉之神 → 「无字回廊东端」（与主线 `main_true` 地点同源：回廊尽头）、残焰魔像 → 「无字回廊中段」（与地图 `MB` 坐标同源）；其余条目逐字不变（石心魔像·稀有精英 / 幽冥魔王·雾语林祭坛 / 洞窟领主·星井矿脉深处）。Lv.3 出没门槛标注逻辑（`spawnLv`）不受影响。
- 【零回归面】只改 `js/view/menus.js` `whereFind` 一行 LOC 表（含注释），未动 `data.js`（地图/遇敌/图鉴标签）、`rules.js`、任何结算/存档/遇敌逻辑；图鉴绘制（drawCodex）仅读取展示，不涉判定。
- 验证：`node --check js/view/menus.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）。

## v14.4 对话框再进化——标点停顿/逐字音效/【】金色强调/说话人肖像框/面板弹出（纯显示·节奏单一数据源）

- 【标点会「呼吸」】打字机节奏数据化为 `rules.js` 的 `talkCharMs/pageTotalMs/pageShownAt`：每字 28ms，标点（，。！？…—）停 4 拍——`core.talkNext` 的补全判定与 `drawTalk` 的渲染**同读一份节奏表**（替代原先两处各自硬编码 28ms 的隐式双轨），长台词从此有抑扬。
- 【逐字音效】打字过程中每 3 字一声轻响（`SFX.tick`，1150~1300Hz 随机微飘，模块游标随页/锚重置，翻页不炸音）——JRPG 对话的「声音指纹」。
- 【【】金色强调】正文里的【魔法蘑菇】【圣光之剑】等强调词渲染为金色（`drawRichLine` 按 `【…】` 分段、随打字机已显字数截断，分段绘制不断字）。
- 【说话人肖像框】面板左侧 64×64 肖像位：普通 NPC 画 MiniWorld 造型（与大地图同源），名字石碑画碑体——谁在说话/在读什么一眼可辨。
- 【面板弹出】对话开启时 140ms 淡入上浮（`S.talkStartAt` 只在 openTalk 设置，翻页不重放）；打字机正文改由肖像右侧排版。
- 验证：`npm run check` + `npm test` **89/89 + 8/8** 全绿（新增 4 项断言：标点 4 拍节奏、渐进显字、超时全显、石碑/NPC 肖像渲染不抛错）。未动对话内容、任务接交与场景切换。

## v14.3 商店买不起报「精确差价」——从笼统「（不足）」到一眼看差多少金（体验打磨·信息透明，承接 v3.13 旅馆差额 / v3.14 酿造差额 / v14.2 技能「还差 N MP」）

- 【商店是全游戏唯一「买不起不给差额」的界面】杂货商店买不起的装备/药水价格行此前只笼统标 `（不足）`——差 5 金还是差 500 金要自己拿顶部「💰 金币」那行心算；而旅馆（`还差 N 金，无法入住`）、酿造锅（`还差 X 株蘑菇、Y 金币`）、战斗技能菜单（`⛔ 还差 N MP`）全都已按短缺口径直接报出差额——同一套「还差 N」信息透明体系，唯独商店没跟上：新手盯着 `600💰（不足）` 想买勇者之剑，完全不知道还差多少钱、要不要先去刷两趟雾语林。
- 【修正：价格行直接报出确切差价】装备/药水买不起时，价格列由 `600💰（不足）` 改为 `600💰（还差 500 金）`——`N = it.price − hero.gold`，与顶部金币读数同源、零心算；药水因**背包满 99**（而非缺钱）买不起时如实标 `（背包满）`，绝不误报成「还差钱」（买不起的两个原因一眼区分）；买得起时不标任何后缀（`15💰` 保持原样）。纯显示、零结算变化——不碰 `buyPotion/buyWeapon/buyArmor` 的购买判定，只改价格列文案。
- 【零回归面】只改 `js/view/menus.js` 的 `drawShop` 一处（价格列分支，`+7 行`含注释）；未动 `shop.js`（购买/背包上限/蘑菇保护判定）、`data.js`（价格/属性）、任何金币结算或商店键位；价格列右对齐 x=560 反延的差价文本实测最宽约 100px（自 460 起）不与左起 x=86 的商品名行重叠。
- 验证：`node --check js/view/menus.js` 与 `npm run check`（24 模块）全部通过；`npm test` **85/85 + 8/8** 全绿（回归不受影响）；新增 v14.3 专项冒烟（/tmp/jrpg_smoke_v1430_shopgap.mjs）**16 项断言全过**——金币 30 时铁剑/皮甲/秘银剑/龙鳞甲逐项报出精确差额（80/60/220/480−30）、不再出现笼统「（不足）」、买得起的药水无后缀、恰好 80 金买铁剑无差价而更贵的秘银剑（220−80=140）仍报、药水满 99 标「背包满」且不误报差价、0 金币全行报差额且无负数、`drawShop` 重复绘制金币/药水背包零改动（纯显示收敛）。

## v14.2 技能菜单「MP 不足还差 N」口径——红色 MP 成本行下方直读短缺口数（体验打磨·信息透明，承接 v12.5 技能伤害提示 / v14.0 蓄力预览）

- 【只见红字不知道差几点】战斗技能菜单（`2`）右侧的成本列早已会红字标注 `MP N`（红=不足·蓝=够用·灰=封印），但 MP 不足时**只告诉玩家「放不起」，不告诉差多少**——差 1 点还是差 11 点，要自己拿 `当前 MP：x/y` 那一行心算。低级时 MP 紧张、连放两招就空蓝的场景，玩家经常盯着 `MP 8` 的红色干瞪眼：到底还差几点？值不值得先防御回 2 MP / 喝一口药再放？
- 【修正：hint 行右侧常驻短缺口数】MP 不足的技能，在其灰色 hint 行（技能名下方一行）的**右侧**补一条右对齐的红色 `⛔ 还差 N MP`——`N = skill.mp − 当前MP`，一眼看清差几点、回几 MP 够不够放。与 MP 成本行、`doSkill` 的 `mp` 判定同源，纯显示、零结算变化：蓝（够用）与 ⛔封印（灰置）的技能不显示；恰好够用（`mp === skill.mp`）不显示；hint 文本不丢失（短缺口径独占 hint 行右侧空档：hint 左起 x=188 最长约 165px 至 353，短缺口径右对齐 x=474 反延约 70px、自 404 起，逐字无重叠）。
- 【零回归面】只改 `js/view/drawBattle.js` 的 `drawSkillMenu` 一处（`+4` 行含注释）；未动 `data.js`/`rules.js`/任何伤害/MP/技能/封印判定、存档、遇敌或难度曲线。
- 验证：`node --check js/view/drawBattle.js` 与 `npm run check`（24 模块）全部通过；`npm test` **85/85 + 8/8** 全绿（回归不受影响）；新增 v14.2 专项冒烟（/tmp/jrpg_smoke_v1420_skillmp.mjs）**16 项断言全过**——MP=6 时恰 2 条短缺口径（雷鸣差2 + 陨石术差8）、火焰斩/治愈术够用不标、短缺口径右对齐 x=474 且红色 #e14b3f 且落在该技能 hint 行 y=212（不挤技能名行）、与 hint 无重叠、成本行颜色蓝/红回归、恰好够用（MP=14=陨石术）不显示且全蓝、封印治愈术 MP 不足不发短缺口径（仅 3 条且无 ⛔还差2）且成本行灰置、满 MP 短缺口径清零、`当前 MP` 行照常、完整 `drawBattle` 不抛异常。

## v14.1 回合计数如实行——「⚔️ 回合 N」只在行动被实际消耗时推进（机制修正·纯计数）

- 【此前被拒指令 / Boss 逃跑也会虚涨回合】战斗左上角的 `⚔️ 回合 N` 计数在 `playerAction` 起手处**无条件 +1**：MP 不足、无药可喝、技能未学、被气场封印这些「什么都不消耗」的被拒指令，以及 Boss 战按 `4` 逃跑（日志明说「本回合行动保留、不耗回合、不调度敌方行动」），都会白白把计数推高一格——按几下废键，回合数就虚高几回合，与「不耗回合」的文案自相矛盾（长盘 Boss 战按几次 4 试逃跑，计数悄无声息地漂移）。
- 【修正：计数移到 afterPlayer】`playerAction` 起手不再 +1，改在 `afterPlayer`（真正调度敌方回合之前）推进——只有确实消耗了本回合的行动才 +1：攻击/技能/药水/防御/蓄力/逃跑失败照旧「每行动一次进一回合」；Boss 战逃跑、逃跑成功、各类被拒指令不进回合。展示层 `drawBattle` 只读数，行为与既有「结算/标注同源」体系一致。
- 【零回归面】只改 `js/battle.js` 两处（`playerAction` 删一行 +1、`afterPlayer` 加一行 +1 并附注释）；未动任何伤害/掉落/经验/金币/难度/升级曲线、敌方行动、存档结构。`S.battleTurn` 的「开战重置 1 / 左上角绘制」语义逐字不变。
- 验证：`node --check js/battle.js` 与 `npm run check`（24 模块）全部通过；`npm test` **85/85 + 8/8** 全绿（回归不受影响）；新增 v14.1 专项冒烟（/tmp/jrpg_smoke_v1410_turncount.mjs）**17 项断言全过**——startBattle 残留 99→1、普攻消耗行动 +1→2 且调度敌方回合、Boss 战按 4 日志含「行动保留」且 battleBusy 释放且回合不虚涨、MP不足/无药满状态/技能未学三种被拒均不 +1 且 battleBusy 释放可再操作、治愈术/防御/蓄力照常 +1→2、击杀收尾不推进（胜利结算不入敌方回合）、battleTurn=7 时 drawBattle 绘制无异常。

## v14.0 蓄力语义收敛——治疗不再吞蓄力，不再谎称「蓄力加持」（机制修正·小数值不变）

- 【此前治疗会白吃蓄力还谎称加成】蓄力（`6`）的官方口径是「下一次攻击或技能**威力** ×1.5」（`doCharge` 日志与帮助页同文），只加成威力不涉治疗。但 `doSkill` 里**所有**技能都在开头统一 `hero.charge = false`——治疗也不例外：蓄力中按「治愈术」会**消耗掉蓄力**，且战斗日志还标 `（蓄力加持）`，可治疗量却与未蓄力时**逐字相同**（`Math.round(hpMax×0.55)`，预览 `skillEstimate` 同样不加成）——既骗了一次关键的奶量，又把攒好的蓄力白白烧掉，玩家在残血时「蓄力→自愈→下回合爆发」的连招根本不存在。
- 【修正：治疗保留蓄力 + 日志如实】`doSkill` 改为 `if (charged && skill.kind !== 'heal') hero.charge = false`——只有攻击/伤害技能才消费蓄力，**治疗（治愈术）完整保留蓄力**，下一次攻击/伤害技能仍按 ×1.5 结算（与 `doCharge` 文案「攻击或技能威力」一致）；日志尾缀由谎称的 `（蓄力加持）` 改为如实的 `（蓄力保留）`。残血时可以先奶一口维持战线，蓄力留到下一次爆发——连招成立，零数值改动（治疗量公式、伤害公式、蓄力倍率、预览口径全部逐字不变）。
- 【技能菜单同步标注（信息透明·纯显示）】`view/drawBattle.js drawSkillMenu` 治疗行在蓄力中追加 `·蓄力保留`（` +55HP ·解毒 ·蓄力保留`），未蓄力不标——开菜单一眼看清这口奶会不会吃掉蓄力，与结算同源、零第二口径；文本估算宽度约 245px 自 x=170 起不触右缘 MP 列（474 右对齐）。
- 【零回归面】只改 `js/battle.js`（doSkill 一处判定 + 一行日志文案）与 `js/view/drawBattle.js`（治疗预览拼接一行）；未动 `rules.js`（预览仍与结算同源、蓄力只进伤害路径）、`data.js`、遇敌/掉落/经验曲线、Boss 战与存档结构。
- 验证：`node --check js/battle.js js/view/drawBattle.js` 与 `npm run check`（24 模块）全部通过；`npm test` **85/85 + 8/8** 全绿（回归不受影响）；新增 v14.0 专项冒烟（/tmp/jrpg_smoke_v1400_chargeheal.mjs）**15 项断言全过**——治疗生效且回复量 = round(hpMax×0.55) 封顶、蓄力中治疗 `charge` 保留为 true、MP 照扣 5、日志含「蓄力保留」且不含「蓄力加持」、治疗不伤敌、技能菜单蓄力中治疗行带「·蓄力保留」而蓄力外不带、伤害技能照常消费蓄力且命中（×1.5 路径不坏）并标「（蓄力）」、「当前MP」行蓄力标注随状态显隐、`skillEstimate` 治疗预览蓄力前后逐字相同（结算/预览同源）。README 蓄力一行措辞微调同步。

## v13.9 每级成长透明化——创建页常驻「升级成长」标注（体验打磨·信息透明，承接 v6.6 经验读数 / v8.0 困难倍率标注 / v7.2 升级结算）

- 【创建页从不告诉你「练一级得到什么」】角色创建页自 v1.x 起只有 姓名/难度 两项选择：难度一行已常驻困难倍率标注（v8.0，`DIFF_SCALE`），**唯独角色的成长曲线一个数字都不给**——每升一级 HP/MP/攻/防各加多少，从 v1.x 起就藏在 `data.js baseStats`（`38+lv×7 / 12+lv×4 / 7+lv×2 / 3+lv×2`）里，只在胜利后那条一闪而过的升级横幅（`🎉 等级提升到 Lv.N！HP+7 MP+4…`）里才看得到；还没进游戏的新玩家对自己的英雄「会怎么成长」毫无概念，规划吃 Boss 前「再练两级能不能扛住」也没法在选角时评估。现在创建页在困难倍率行正下方**常驻一行**：`每级成长：HP+7 · MP+4 · 攻+2 · 防+2`——选角、换档、重开必经此页，一眼看清这套成长曲线，与「强度/倍率/奖励全透明」同一体系。
- 【与结算逐字同源（单一数据源）】成长数据化为 `data.js` 的 `LEVEL_GROWTH` 常量——由 `baseStats(2) − baseStats(1)` **一阶差分推导**（等级恒为 HP+7 / MP+4 / 攻+2 / 防+2）：升级结算（`hero.grantXp` 的 dh/dm/da/dd）与创建页标注同源于同一个 `baseStats`，改成长曲线只需改 `baseStats` 一处，结算、胜利横幅、创建页绝无第二套口径。只读展示，不参与任何判定。
- 【零行为风险】只改 `js/data.js`（+1 纯常量 `LEVEL_GROWTH`、+1 export）与 `js/view/menus.js`（+import、`drawCreate()` 加 1 行 `fillText`，坐标 (320,408) 为既有空档——上距困难倍率行 26px、下距按键提示行 24px，实测文本估算宽度 224px 不越界、不与任何既有元素重叠）；**未动** `baseStats`、`grantXp`、升级判定/经验曲线、任何战斗/掉落/等级/存档逻辑。`drawCreate` 既有各行（困难倍率 / 姓名 / 难度 / 按键提示）逐字不变。
- 验证：`node --check js/data.js js/view/menus.js` 与 `npm run check`（24 模块）全部通过；`npm test` **85/85 + 8/8** 全绿（回归不受影响）；新增 v13.9 专项冒烟（/tmp/jrpg_smoke_v1313_growth.mjs）**19 项断言全过**——`LEVEL_GROWTH` 数据契约（{7,4,2,2}）、与 `baseStats` 一阶差分逐字同源、真实 `grantXp` 单次升级增长 g{7,4,2,2} 与常量一致（升级后属性实际增量同步核对）、Lv.10 基础属性回归、`drawCreate` 端到端逐字渲染 `每级成长：HP+7 · MP+4 · 攻+2 · 防+2` 且位置 (320,408) 居中不重叠、困难倍率行/按键提示行/姓名行回归在位、成长行估算宽度 ≤560 不越界、连续 2 次绘制文本逐字一致 + level/gold/xp/hp/mp/hpMax 零改动（纯显示收敛）。README 系统清单「职业成长」一处同步。

## v13.8 文档同步——README「快速上手」按键表补齐遗漏快捷键（纯文档·零行为变化）

- 【补齐按键表】`main.js` 里早已绑定的三个快捷键一直没进 README 按键表：世界画面 `T` 快速旅行（Esc 菜单里也有，但直连键无人写）、标题页 `R` 重开新档、阵亡页 `B` 重整旗鼓 / `R` 重开 / `T` 回标题——README 表格补齐 `T` 行、`标题 1/2/3` 行追加 `R`、新增「阵亡 B/R/T」行。逐键与 `main.js` `screens.title / screens.dead / screens.world` 的实际绑定核对（世界 `t/T`→`goto('travel')`；标题 `r/R`→`resetRun()`；阵亡 `b/B`→`retryBoss()`、`r/R`→`resetRun()`、`t/T`→回标题），与游戏内 `HELP_PAGES` 操作页（`快速旅行 T`）与试炼进阶页（`重整旗鼓 B · R 重开 · T 回标题`）保持一致——README 不再是第二套口径。
- 【零行为风险】只改 `README.md` 一行表格（新增 2 行 + 改写 1 行），**未动任何 `js/` 代码**、数值、存档与结构；游戏内帮助页本来就已写全这些键。
- 验证：`npm run check`（24 模块）与 `npm test`（85/85 + 8/8）复跑全绿。

## v13.7 剧情大型重构——四幕链 + 名字石碑 + 两条主题支线（《潮灯记》四幕版）

- 【四幕结构落地】主线任务链加第四幕 `main_gallery`（旗标 `galleryOpen`，双徽记开门时置位）：一幕讨回灯芯 → 二幕星井之守 → 三幕无字回廊 → 四幕初灯的审判；`main_true` 的指向从「矿脉水晶」改为「回廊尽头」，终焉之神战随之迁入回廊东端祭坛（矿脉的终焉水晶变为开门机关）。状态页冒险进度徽记增至五格（灯芯/星井/回廊/初灯/试炼场）。
- 【名字石碑：记忆从掉落物变成世界】回廊北壁 4 块石碑（新瓦片 TY.STELE，MiniWorld Tombstones CC0 贴图 + 程序化回退 + 小地图灰点 + `⏎ 读碑` 面向提示），碑文与 `FRAGMENTS` 逐字同源（同一 data.js 表生成 NPC 台词页，零第二数据源）。
- 【条件式支线机制】`questStatus` 新增 `cond(hero)` 自动转可交付 + `condProg` 实时进度文案（日志/横幅同显），`applyQuestReward` 支持 `potion2` 灵药奖励（领取提示与日志奖励预览同步标注）。
- 【两条主题支线】巡灯人「旧灯卫的名字」（魔王后解锁，集齐 4 枚记忆碎片交付，奖 120 金+灵药，与记忆图鉴/碎片系统咬合）；雾径猎手「雾里的新住客」（讨伐 3 只雾灵交付，奖 60 金+药水，引导记忆图鉴与弱点机制）。
- 【NPC 台词分段推广】巡灯人/雾径猎手改 `linesByStage`（默认 → 魔王后 → 回廊开后）；星砂车夫补真结局彩蛋台词（全镇彩蛋补齐到 6 人）；成就 +2（名字归还/雾中辨形，共 17 项）。
- 验证：`npm run check` + `npm test` **85/85 + 8/8** 全绿（新增 10 项断言：四幕链解锁/转向、碎片条件交付两态、雾灵计数交付、灵药奖励、回廊阶段台词、石碑碑文同源）。旧档兼容：`galleryOpen` 缺省即 falsy，无需迁移。

## v13.6 对话框显示效果——打字机/名牌 chip/翻页指示/聚焦压暗（体验打磨·纯显示）

- 【对话从「整页糊脸」到「逐字道来」】此前 `drawTalk` 一块静态面板 + 全页文字瞬间直出，没有节奏也没有反馈。现在：正文**打字机逐字**（28ms/字，`core.talkNext` 与绘制同节奏——打字中按 Enter 先补全本页、再按才翻页）；说话人**名牌 chip** 压在面板框线上（金字描边）；本页打完且还有后续页时右下角**闪烁 ▼** 翻页指示；对话期间世界**压暗 35%** 聚焦视线；正文加 1px 投影提升可读性。
- 验证：`npm run check` + `npm test` **76/76 + 8/8** 全绿（新增 3 项断言：对话打开在第 0 页、打字中 Enter 只补全不翻页、补全后翻页）。未动对话内容、任务接交逻辑与场景切换。

## v13.5 素材扩充 + 四图重设计——CC0 素材包接入、无字回廊新图（内容扩展·含读档兼容）

- 【素材扩充（全部 CC0，逐包核验）】新增三个素材包（assets/CREDITS.md 汇总来源与许可证）：**MiniWorld Sprites**（Shade，与 Puny 同作者）——6 名 NPC 全部换成差异化造型（灯长/井巫/猎手/巡灯人/镇民/车夫各有其图）+ 石碑/界门/路牌/岩石装饰件；**Trash Mobs**（Emcee Flesher，CC0 双授权）——紫幽灵帧接入新魔物「雾灵」（雾语林 Lv.2 起出没，图鉴第 12 种）；**Fire Golem**（teasloth）——回廊精英「残焰魔像」（图鉴第 13 种）。`atlas.js` 新增 16×16 帧图集支持（SHEET_CELL16），`sprites.js` 新增定制帧位路径（CUSTOM_FRAMES）。Kenney Tiny Dungeon 目检后放弃（散件+色板不搭），0x72 无直链未下——均记录在 CREDITS。
- 【四图重设计】按 RPG Maker 式原则重排：**潮灯镇**广场大灯地标居中、商店/旅馆/酿造环广场、民宅西北、城门东；**雾语林**蛇形主路 + 北环路蘑菇宝箱 + 中段营地安全岛，裂洞移到祭坛南（先魔王后矿脉）；**星井矿脉**轨道引导线贯穿、四区递进；新图**无字回廊**——恒暗线性回廊、北壁 4 块名字石碑（碑文与 FRAGMENTS 逐字同源）、中段残焰魔像（可重复挑战精英）、东端终焉祭坛。所有布局经 BFS 连通性校验（/tmp/mapcheck.mjs）。
- 【机制配套】终焉水晶从「直接开战」改为**双徽记开门 → 无字回廊**（终焉之战挪到回廊尽头祭坛）；新瓦片 TY.STELE（SOLID、MiniWorld 石碑贴图 + 程序化回退、小地图配色、面向提示「⏎ 读碑」）；回廊专属 BGM 轨（更慢更稀的正弦长音）+ 漂浮字符粒子 + 恒暗着色；分图遇敌池（`MAPS[].pool`，回廊只出雾灵/石魔像/骷髅兵）；快速旅行加回廊条目；**读档兜底**——旧档坐标落在重排后的墙里时退回出生点。
- 验证：`npm run check` + `npm test` **73/73 + 8/8** 全绿（坐标类断言已随新图更新）。Boss 数值/遇敌公式/存档结构不变；旧档可读。

## v13.4 剧情重构 B 档——揭示节奏三段化 + 记忆碎片机制化（主题咬合·含旧档迁移）

- 【井巫不再开局剧透】此前井巫第一句台词就把终极反转全部说穿（「魔王只是锁，水晶里才是灯自己——终焉之神」），最终 Boss 揭晓毫无冲击。现在 `NPCS.sage.lines` 拆为 `linesByStage` 三段：默认只点「魔王是一把锁，锁着更深的东西」→ 击败魔王后讲星砂矿史与洞窟领主 → 击败领主后才揭「水晶里睡着的是灯自己」；选段逻辑在 `quests.js npcQuestPages` 回退分支（数据仍纯）。HELP/TRAVEL 攻略页保留不动（玩家主动查阅，不算剧透面）。
- 【记忆碎片：主题机制化】「记忆」从 NPC 嘴里的词变成可收集的机制：石心魔像/幽冥魔王/洞窟领主/终焉之神首胜各掉一枚记忆碎片（`data.js FRAGMENTS` 单一数据源：守门人的脚印/灯卫的誓/最后一车星砂/初灯的名字），`winBattle` 按 `canonicalName` 归一查找、未收集才掉落（精英蘑菇同款先例）；`J` 任务日志新增「记忆碎片」节，已收集显示全文、未收集灰 `？？？` 占位；集齐 4 枚后真结局追加「全记忆」三行页（`ENDING_TRUE_FRAG`，行距收窄不溢面板）；尾声战绩行补 `记忆 N/4`。
- 【图鉴更名「记忆图鉴」+ 支线接主题】已讨伐 = 被记起：面板标题/暂停菜单/标题快捷键/战败提示/帮助页/成就名（图鉴学家→记忆收藏家、图鉴征服→记忆守护者）约 9 处文案同步；蘑菇支线 offer/done 文案接入灯油口径（荧光蘑菇是灯油原料），奖励公式不动。
- 【存档兼容】`newGame` 新增 `fragments: []`；`migrateQuests` 幂等补默认（`if (!hero.fragments) hero.fragments = []`），旧三槽存档直接可读；快照展开式存档使新字段自动入库，无需动 saveGame/slotPreview。
- 验证：`npm run check` + `npm test` **73/73 + 8/8** 全绿（新增 8 项断言：旧档迁移补 fragments、井巫三段按旗标选段、首胜掉碎片、重复击败不重复收集、真结局含碎片加页绘制、日志碎片节绘制）。未动任何数值、任务奖励公式与结局触发条件。

## v13.3 美术优化——装饰层/待机呼吸/震屏/变身闪光/村庄夜灯（纯显示·零结算变化）

- 【怪物贴图补位评估】`NO_SHEET` 里 wolf→orc.png 的既有映射经 ReadMediaFile 肉眼核对为人形兽人，与「野狼」违和，**维持程序化剪影**（snake/tree 同样无合适素材，不硬凑）；程序化 DRAWS 保留作贴图缺失回退。
- 【地图确定性装饰层】`drawTileFx` 补传格坐标，按既有 `(x*19+y*37)` 坐标哈希稀疏点缀：草地小花（白瓣黄心）/草痕、路面石子——零状态、确定性、不进存档，洞窟 GRASS 已被 replaceTiles 换成 CAVE 故天然不触发。
- 【待机呼吸】`drawSheetChar`/`drawSheetStrip` 统一注入 ±1px 相位浮动（`opts.idle` 开关，相位随坐标错开防全场同步），覆盖世界 NPC 与战斗怪物（含程序化回退路径）；主角不动（已有走帧 bob）。
- 【战斗演出】暴击/大额伤害（≥25，与浮字加粗同阈值）与敌方重击触发**震屏**（`S.shake` 220ms 衰减，drawBattle 包 save/translate，技能菜单不随震）；Boss 二段变身触发**全屏白金闪光**（`S.flash` 400ms 衰减）。
- 【村庄夜晚灯火】夜晚时段 TOWN 格叠加暖色窗光相位闪烁，呼应「灯」主题。
- 验证：`npm run check` + `npm test` 65/65 + 8/8 全绿；改动全部位于 view/ 与 `S.shake/S.flash` 两个纯显示字段（battle.js/enemyAI.js 各只加触发行），未碰任何结算公式。README 视觉行同步。

## v13.2 地图系统机制优化——按住连走、传送/遇敌数据化、onStep 查表（机制重构·数值逐字不变）

- 【按住连走 + 预输入】此前移动完全依赖 OS 键重复，且行走动画（180ms）期间的按键直接丢弃——连走卡顿、换向迟钝。现在 main.js 新增 `keyup` 监听维护按住方向集合，`world.holdStep()` 以 40ms 节拍驱动连走（最后按下的方向优先）；行走中的按键记入预输入缓冲，走完立即补走一格；切图/切场景自动清空，杜绝幽灵移动。手感变化仅限「更顺」，步进节奏与逐格逻辑逐字不变。
- 【传送数据化】村庄→森林→矿脉链从 useGate/useExit/portalDest 三处硬编码迁入 `data.js MAPS[].portals`（含村庄 GATE 的 locked/lockedMsg——击败魔王后村门只提示不传送）。world.js 合并为单个 `usePortal(tile)`，对外 useGate/useExit/portalDest 签名不变，视图「踩上通行 → XX」提示与真实传送同读一表。加新地图只需在数据表加一行。
- 【onStep 查表化】宝箱/喷泉/幽冥魔王/洞窟领主/终焉水晶/试炼碑六个踩格分支从 if-else 链迁入 `STEP_HANDLERS` 表（已开宝箱格仍走遇敌槽的既有行为保留并注明）；顺带修掉面对商店格按 Enter 发**空字符串** boxMsg 的异味。
- 【遇敌机制单轨化】`dangerAt` 不再混用 charAt（原始 ASCII）与 at（叠加后瓦片）双轨：'G' 高草坐标在 loadMap 建图时入集，全图危险地形读 `MAPS[].dangerTiles`；增减数值数据化为 `ENCOUNTER = { dangerMin:10, dangerVar:9, fountain:-25, calm:-6 }`，小地图机制标注由静态文案改为模板拼接，显示与结算真正同源。`charAt` 随双轨消除一并删除（无消费方）。
- 【小地图单次遍历】drawMinimap 的双重 24×18 全图循环合并为一趟（颜色填充 + 危险/可走统计 + 危险格收集一次完成），危险标注改为遍历收集结果；每帧省去一轮全图 dangerAt 调用。
- 验证：`npm run check`（24 模块）通过；`npm test` **65/65 + 8/8** 全绿（新增 11 项断言：传送链/村庄 GATE 锁读表、ENCOUNTER 数据契约、三图危险格判定、踩格表覆盖、连走首步即时/预输入缓冲/补走/按住连走）。未动任何地图布局（rows 一字未改）、遇敌数值、WALK_MS、Boss 触发条件与存档结构。README 按键表一处同步。

## v13.1 宝箱开箱内容概率透明化——帮助页补「宝箱掉落」行（体验打磨·信息透明，承接 v12.4 战斗随机掉落）

- 【战斗随机掉落实测概率全透明了，**开箱出货构成仍是黑盒**】v12.4 已把战后随机掉落的四档概率逐字亮出，但同样频繁发生的**踩宝箱**从 v1.x 起就只在小地图上提示「蘑菇宝箱金光脉动」（v3.x 帮助页仅讲找法），**打开后到底出什么、各有几成、金币随等级长多少**从未示人——整天刷雾语林宝箱凑蘑菇的玩家，永远不知道 60% 的蘑菇率是「大概率出」还是「靠脸」；而尘土里开出「🎒 药水/金币」时，也看不出自己撞上的是 18% 还是 22% 的档。现在 H 帮助「魔物状态」页在「战斗掉落」行正下方新增一行**宝箱掉落**说明：`开箱掉落：雾语林 60%蘑菇·18%金币(12+级×5)·22%药水；城镇/矿脉 45%金币·55%药水`——开箱前一眼看清三地出货构成，与 v12.4「随机性全透明」同一体系。
- 【与结算逐字同源（单一数据源·只读展示）】三档比例逐字对应 `world.js onStep` 宝箱分支的唯一裁决链：雾语林（dungeon）`curMap()==='dungeon' && Math.random()<0.6` → 蘑菇 60%；否则 `Math.random()<0.45` → 金币（数额 `12+hero.level*5`，即 12+级×5）；再否则 → 药水 1 瓶——雾语林整体 60/18/22、城镇与矿脉（非 dungeon）45/55，与真实开箱结算绝无第二套口径；金币公式 `12+级×5` 亦逐字同源。纯文字展示，不参与任何判定。
- 【零行为风险】只改 `js/data.js` 的 `HELP_PAGES`（魔物状态页 +1 行）；**未动** `world.onStep` 宝箱分支任何概率/数额/物品、蘑菇支线、宝箱坐标、掉落、经验/金币、难度、成就、存档或地图逻辑；帮助页其它 7 行文案、样式、位置逐字不变；新行估算渲染宽度（560px）≤ 既有最长行「战斗掉落」（570px，v12.4 先例），不新增越界。
- 验证：`node --check js/data.js` 与 `npm run check`（24 模块）全部通过；`npm test` **54/54 + 8/8**；本版本在 v13.0 架构（encounter/enemyAI 拆分、curMap() 访问器、「S.curMap 删除」）落地后复验仍全绿；v13.0 架构、v12.9 计分回归（专项冒烟修 `deep` 改读 rules 后）**26/26**；新增 v13.1 专项冒烟（/tmp/jrpg_smoke_v1210_chestodds.mjs，文件名沿用旧编号）**17 项断言全过**——HELP_PAGES[2] 含「宝箱掉落」行且位于「战斗掉落」之后、文案含完整三档（60/18/22 · 45/55 · 12+级×5）、**走真实 `world.onStep` 宝箱分支蒙特卡洛 3 万次**：雾语林实测 ≈60%/18%/22%（金币数额 Lv.5 恒为 37=12+5×5）、城镇实测 ≈45%/55% 且绝不出蘑菇（与 `curMap()` 前提一致）、drawHelp 第 3 页渲染不抛错、新行估算宽度 ≤「战斗掉落」行（不新增越界）、连续 3 次 drawHelp 零状态改动（纯显示收敛）。README 系统说明一处同步。

## v13.0 架构结构性整改——bind 瘦身、view 只读化、battle 拆分、单一数据源收口（纯重构·零行为变化）

- 【bind.js 腐化修复】v4.0 的晚绑定层已从「view 注册点」腐化成双向总线：world/core 也在注册（applyVictoryWorld/loadMap/openTalk），且 renderHUD/boxMsg/drawBattle/drawDead 等键在 hud/drawBattle/menus 与 view/index 里**重复注册**（删一处不报错）。现在拆成两层：`bind.js` 只留 系统→绘制 钩子（仅 view/ 注册、每键单点，头注如实）；新增 `hooks.js` 承接 system 间晚绑定（applyVictoryWorld/loadMap ← world.js，openTalk ← core.js），world↔battle、world↔core 的逻辑环全部摆到明面。
- 【view 层不再改游戏状态】游戏时钟（冒险时长/昼夜）从 `view/index.js render()` 移到 `main.js` 的 `setInterval(tick, 250)`——渲染停则时间停的隐患消除；`drawWorld.heroDrawPos()` 不再清除 `S.walk`，行走结束判定收口到 `world.walking()`（惰性清除）。grep 验证 view/ 下无任何 `S.G`/`S.walk` 写操作。
- 【battle.js 上帝文件拆分】743 → 462 行：遇敌生成（scaleEnemy/eliteEncounter/randomEncounter/权重）拆出 `encounter.js`；敌方行为表挑选与回合执行（pickAct/enemyAct，灼烧/冻结/变身/石甲/回血/反击/施毒）拆出 `enemyAI.js`（不反向 import battle，编排回调经 deps 传入）；升级循环迁入 `hero.grantXp`；掉落公式迁入 `rules.rollDrop(hero, curMap)`；Boss 重试 retryBoss 迁入 `core.js`（存档/开局语义）；尾部门面再导出删除，world 直读 rules/hero。
- 【单一数据源收口】Boss 报酬/强度不再抄第二份表——`rules` 的 BOSS_REWARD/BOSS_STAT 合并为 BOSS_DEFS，直读 data.js 的 BOSS/CAVE_BOSS/TRUE_BOSS（经 canonicalName 归真身名）；Boss 推荐等级（8/7/12）进 `SPECIES[].lv`，战斗界 enemyLv 与祭坛 ⚠Lv 标签同读；精英石心魔像成长表数据化为 `ELITE_GOLEM`（encounter 与图鉴 codexStats/monReward 同读）；遇敌逐怪权重数据化为 `MON_BASE[].w(lv)`；药水可用性判定抽为 `hero.potionAvailability`（core.usePotion 与 battle.doItem 同源）；core 开局三段重复（initGame/beginAdventure/resetRun）收口为 `startRun`。
- 【双源真相消除】`S.curMap` 删除，当前地图唯一真相为 `G.map`（loadMap 负责同步），全层读取走 `state.js` 的 `curMap()` 访问器（放 state 而非 world，避免 audio→world 成环）；存档/读档的反复调和代码删除，旧档兼容保留（含 legacy `data.curMap` 字段）。
- 【死代码清除】删除 ui.js 桶文件（无人 import）、S.titleFlash、S.innPrice（改 data.js INN_PRICE 常量）、restoreChestGrid 空函数、menus.openShop 空壳、hero.nextSkillHint、rules.skillHintLine/speciesOf、data.CAVE_HOLE/INTERACT。
- 【测试收口】`tests/smoke_v510_freeze.mjs` 接入 `npm test`（不再是孤儿脚本）；smoke 新增 5 项架构断言（goto 清 battleBusy/技能菜单、curMap() 与 G.map 同源、screens 表每项有 onKey、关键场景覆盖、各场景未映射键分发不抛错）。
- 验证：`npm run check`（24 模块）通过；`npm test` **54/54 + 8/8** 全绿。未动任何游戏数值、手感、文案与存档结构（旧三槽存档可读）。README 项目结构一节同步。

## v12.9 试炼三连战「每胜一关自动回血」透明化——横幅/帮助页标注 35%HP/50%MP（体验打磨·信息透明，承接 v3.15 连战横幅 / v11.0 通关金币）

- 【三连战会「补血」却从不告示，玩家可能凭白省药】试炼三连战（试炼碑 ⚔️ 进驻后连战 幽冥魔王→洞窟领主→终焉之神）自 v1.x 起就有一个**从不告示的机制**：每胜一关换关时，自动恢复 `35% 最大 HP / 50% 最大 MP`（`battle.winBattle` 连战胜分支硬编码 `0.35/0.5`）——v3.15 只标「第 N/3 关」进度、v11.0 只标通关金币，**唯独换关回血从未出现在任何界面**：以为「药得留给下一场」的玩家可能在每关收尾前硬灌高级灵药，白白烧掉稀有资源。现在三处同步点破：战斗横幅（左上角 `🧭 试炼三连战 第 N/3 关`）在**前方还有后续关卡时**常驻尾缀 `· 通关恢复35%HP/50%MP`（临场一眼看清该不该省药；第 3 关无后续则不显示，免噪音）；换关瞬间的提示横幅补注 `（已自动恢复35%HP / 50%MP）`（确认这口补给真实到账）；帮助页「试炼进阶」页的试炼三连战行追加 `每胜一关回血35%HP/50%MP`。
- 【与结算逐字同源（单一数据源）】回血比例数据化为 `data.js` 新增 `RUSH_RECOVER={ hp:0.35, mp:0.5 }`：`battle.winBattle` 连战胜分支由硬编码改为读它（`Math.round(hpMax×RUSH_RECOVER.hp)` / `Math.round(mpMax×RUSH_RECOVER.mp)`，**数值结算逐字不变**），战斗横幅注记、换关 boxMsg 注记与帮助页文案同读此源，绝无第二套口径——只读展示，不参与任何判定。
- 【零行为风险】只改 `js/data.js`（+1 常量 RUSH_RECOVER、+1 export、帮助页试炼行一行文案）、`js/battle.js`（import 补 RUSH_RECOVER、连胜分支两行读常量 + 换关横幅补注一行）、`js/view/drawBattle.js`（import 补 RUSH_RECOVER、横幅尾缀注记 4 行）；**未动**任何回血数值（0.35/0.5 本体）、rushStage 推进、连战敌方属性、通关奖励（rushReward）、掉落、经验、难度、成就、存档或任何战斗判定。主线 Boss/普通怪/精英战斗横幅、帮助页其余各行逐字不变。README 系统说明「试炼进阶」页描述一处同步。
- 验证：`node --check js/data.js js/battle.js js/view/drawBattle.js` 与 `npm run check`（23 模块）全部通过；`npm test` **49/49**、`/tmp/jrpg_smoke_esm.mjs` 全量 ESM 冒烟（ALL ESM SMOKE CHECKS PASSED）回归全过；新增 v12.9 专项冒烟（/tmp/jrpg_smoke_v1290_rushrecover.mjs）**26 项断言全过**——`RUSH_RECOVER` 数据契约（hp 0.35 / mp 0.5 且已 export）、帮助页试炼行含 `每胜一关回血35%HP/50%MP` 且数值与常量同源、帮助页试炼行渲染后宽度不超过既有最长行（「技能克制」行 v12.6 先例，右缘不新增越界）、战斗横幅第 1/2 关逐字 `第 1/3 关 · 通关恢复35%HP/50%MP`（注记与 RUSH_RECOVER 同源）而第 3 关不显示、主线 Boss 战不显示试炼横幅、连胜换关结算（第 1/2 关胜利）精确恢复 `round(hpMax×0.35)` / `round(mpMax×0.5)` 且 rushStage 推进到 2/3、下一关换关 boxMsg 含 `已自动恢复35%HP / 50%MP` 与新敌名、恢复后不超 hpMax、第 3 关通关分支回归（rushDone 置位 / rushStage 复位 0 不抛异常）、连续 5 次横幅绘制 hp/mp/gold/xp/level/scene/rushStage/敌方hp/name/blog 长度零改动（纯显示收敛）。

## v12.8 敌方「暗影回血」数额透明化——招数一览补确切回血%（体验打磨·信息透明，承接 v12.7 变身回血比例）

- 【招数一览标「血<40%时」却不标回多少】Boss/精英战右上角的敌方招数一览早已把每招列全：普攻 / 重击（真身后强）/ 石甲（至多N层），唯独「回血」这行只挂触发线——`回血·血<40%时`，**这一口能回满它多少血**从 v1.x 起就藏在 `data.js SPECIES.acts` 的 `pct` 里（幽冥魔王 0.12 / 洞窟领主 0.10 / 终焉之神 0.12，即自己最大 HP 的 12%/10%/12%）：打 Boss 时敌人每次回血，log 只报「恢复 N HP」（随当前 maxHP 浮动），玩家想估算「再压多少输出它才顶不住」「刚才那一下是不是白打了」只能靠体感或翻源码——偏偏「暗影回血」正是三 Boss（含试炼三连战同款）共有的回复手段，几乎每场强战都会触发。现在招数一览的「回血」档同步补上确切回血比例：`回血·血<40%时·恢复12%HP`（洞窟领主 `恢复10%HP`）——开战前一眼看清「它低血时会回满自己一截血、得预留输出」，与 v12.7 变身回血比例（`回15%`）同一「信息透明」体系。
- 【与结算逐字同源（单一数据源）】回血比例直接读招数表 `act.pct`——与 `battle.enemyAct` 回血分支 `Math.round(enemy.hpMax * (act.pct || 0.12))` **同一对象、同一字段、同一 `0.12` 兜底**，绝无第二套口径；百分比 = `Math.round(pct×100)%` 由数据本体换算，绝无第二套口径能改它。仅当敌方确实存在 `forbid:['heal']` 时（终焉之神祸乱形态，`enemy.forbid` 由 `enemyAct` 变身分支 line 483 动态赋值）才尾缀 `·⛔封印`，与既有「⛔ 治愈封印」角标同一判定。
- 【零行为风险】只改 `js/view/drawBattle.js` 的 `drawBattle()` 招数一览「回血」分支一处（+1 行渲染 + 注释）；**未动**任何回血结算（`enemyAct` heal 分支 `pct` 本体）、变身、伤害/掉落/经验、难度、成就、存档或任何判定。普通怪（无 acts）依旧不显示招数一览、绝不剧透；洞窟领主/幽冥魔王/终焉之神其余招数串逐字不变。
- 验证：`node --check js/view/drawBattle.js` 与 `npm run check`（23 模块）全部通过；`npm test` **49/49**；`/tmp/jrpg_smoke_esm.mjs` 全量 ESM 冒烟（ALL ESM SMOKE CHECKS PASSED）、v12.0~v12.7 全部专项冒烟（10/10 · 18/18 · 9/9 · 28/28 · 12/12 · 17/17 · 24/24 · 19/19）回归全过；新增 v12.8 专项冒烟（/tmp/jrpg_smoke_v1280_healpct.mjs）**18 项断言全过**——三 Boss heal 招数据契约（0.12/0.10/0.12 · hpBelow 0.4）、战斗敌人体 withSpecies 携带同一 acts（与 enemyAct 同源）、幽冥魔王招数一览逐字 `回血·血<40%时·恢复12%HP`、洞窟领主 `恢复10%HP`、终焉之神变身前无封印/变身后（镜像 enemyAct 动态赋 forbid）尾缀 `·⛔封印`、普攻/重击回归在位、招数一览右对齐贴 620 左缘不越界、史莱姆无招数一览不剧透、连续 5 次绘制 hp/mp/gold/xp/level/scene/敌方hp/name/phased 零改动（纯显示收敛）、战利品预览与攻防比较行回归在位。README 战斗说明一处同步。

## v12.7 Boss 二段变身「增益数值」透明化——变身线旁列出 攻/防/回血/封印（体验打磨·信息透明，承接 v1.34 变身线）

- 【变身线早已标「何时变身」，却从不标「变身后果」】v1.34 起战斗画面在敌方血条上画出一道金色「二段变身线」，v12.x 又补上精确阈值 `二段变身线 · HP<70`——变身时机明明白白，但**变身瞬间的 攻/防 暴涨、回血比例与「封治愈」从 v1.x 起就只藏在 `data.js SPECIES.phase2` 里**：触发时 blog 只报「力量暴涨，HP 恢复 N」（只给回血数值），攻防加成无从知晓，玩家常在真身那一下被 +7 攻的新数值打崩还不知原因；终焉之神「祸乱形态」会封印治愈，变身前也毫无预告，辛苦攒的 MP 一按治愈术就被 ⛔封印白费。现在变身阈值行的右缘同步常驻列出**变身后果**：幽冥魔王 `变身：攻+7 防+3 回15%`、洞窟领主 `攻+5 防+2 回10%`、终焉之神 `攻+7 防+3 回15% ⛔封治愈`——一眼看清该不该抢在变身线前把输出灌完、或先补状态再硬接真身。
- 【与变身结算逐字同源（单一数据源）】标注逐项直接读 `enemy.phase2`（atk/def 加算、heal 按 maxHP 比例、forbid 封印）——与 `battle.enemyAct` 变身分支**同一份对象、同一套字段**，绝无第二套口径；数值本体 7/3/0.15、5/2/0.10 逐字不变，任何变身判定、回血、攻防结算零变化。
- 【零行为风险】只改 `js/view/drawBattle.js` 的 `drawBattle()` 变身线一处渲染分支（+12 行纯显示）；**未动** data.js phase2、enemyAct 变身逻辑、伤害/掉落/经验、难度、成就、存档或任何结算。变身（phased）后与阈值标签一起消失，行为与 v1.34 完全一致。
- 验证：`npm run check`（23 模块）与 `npm test` **49/49** 全部通过；`/tmp/jrpg_smoke_esm.mjs` 全量 ESM 冒烟（ALL ESM SMOKE CHECKS PASSED）保持通过；新增 v12.7 专项冒烟（/tmp/jrpg_smoke_v1270_phaseboost.mjs）**19 项断言全过**——三大 Boss phase2 数据契约（7/3/0.15 · 5/2/0.10 · 7/3/0.15+封治愈）、战斗敌人体 withSpecies 携带同一 phase2 对象（与 enemyAct 同源）、幽冥魔王绘制出 `二段变身线 · HP<` 与 `变身：攻+7 防+3 回15%`（右对齐·右缘贴 620·估宽起点 >440 不遮阈值/HP 数值）、洞窟领主 `攻+5 防+2 回10%`、终焉之神含 `⛔封治愈` 置前警告、phased 后两行标签同消、史莱姆等无 phase2 怪不出现、连续 5 次绘制 hp/mp/gold/xp/level/scene/敌方hp/name/phased 零改动（纯显示收敛）、战利品预览行与我方攻防比较行回归在位。README 战斗说明一处同步。

## v12.6 元素克制倍率透明化——图鉴/帮助页标注确切数值（体验打磨·信息透明，承接 v7.6 抗性标注 / v6.9 图鉴强度）

- 【图鉴标「弱点·火」却不标数值，「同一招打它到底多打多少」始终是黑盒】v7.6 起战斗预览行已对称标出 `· 弱点火 / · 抗冰`，图鉴已讨伐行也带 `弱点·X / 抗性·X`，但**确切的伤害倍率从 v1.x 起就只藏在 `rules.js elemMult` 的一行硬编码里**（`weak→1.35 / resist→0.7`）：玩家知道「火招打它更痛」「冰招打它更低」，却看不出痛多少、低多少——而 `≈N伤` 预览其实早已默默按该倍率折算，想反推「这招为什么比打普通怪高/低 35%/30%」只能靠体感或翻源码。现在**图鉴已讨伐行**的弱点/抗性标注补上确切倍率：`弱点·火×1.35` / `抗性·冰×0.7`（骷髅兵/树精/石魔像等双标注怪一眼看懂两档），帮助页「魔物状态」页「技能克制」行同步写明 `弱点伤害×1.35 · 抗性伤害×0.7`——「该不该用被克的招 / 能不能靠克制补平」战前一目了然，与既有「强度/倍率/奖励全透明」同一体系。
- 【与结算逐字同源（数据化单一数据源）】倍率数据化为 `data.js` 的 `ELEM_MULT={ weak:1.35, resist:0.7 }` —— `rules.elemMult` 两档返回值改为读取它，图鉴 `codexTag` 标注与帮助页文案同读此源，**结算、预览、图鉴、帮助页四处绝无第二套口径**；数值本体 1.35/0.7 逐字不变，任何伤害/掉落/经验判定零变化。
- 【零行为风险】只改 `js/data.js`（+1 常量 ELEM_MULT、+1 export、codexTag 两行拼接追加倍率、帮助页「技能克制」一行文案）与 `js/rules.js`（elemMult 两行改读常量）；**未动** battle 预览行（v7.6 断言 `弱点火 / 抗冰` 逐字）、SKILL_DATA 提示、遇敌、掉落、伤害公式、难度、成就、存档、地图逻辑。帮助页「魔物状态」页其余 6 行文案逐字不变；未讨伐灰行依旧不剧透（不显示倍率/强度）。
- 验证：`node --check js/data.js js/rules.js` 与 `npm run check`（23 模块）全部通过；`npm test` **49/49**；`/tmp/jrpg_smoke_esm.mjs` 全量 ESM 冒烟（ALL ESM SMOKE CHECKS PASSED）保持通过；v6.9 图鉴（14/14）、v7.6 抗性（ALL）、v12.0（ALL）、v12.2（9/9）、v12.3（28/28）、v12.4（12/12）、v12.5（17/17）回归全过；新增 v12.6 专项冒烟（/tmp/jrpg_smoke_v1260_elemmult.mjs）**24 项断言全过**——`ELEM_MULT` 数据契约（weak 1.35 / resist 0.7）、`elemMult` 返回值与 ELEM_MULT 逐字同源且行为不变（弱火=1.35 / 抗冰=0.7 / 无克制=1）、帮助页「技能克制」行含 `弱点伤害×1.35` 与 `抗性伤害×0.7` 且数值与常量一致、`codexTag` 标注逐字（骷髅兵 `弱点·火×1.35 · 抗性·冰×0.7` / 石魔像 `弱点·雷×1.35 · 抗性·火×0.7` / 史莱姆仅弱火无抗不泄漏 / 幽冥魔王保留 `⚔️ 必掉圣光之剑` 且追加 `抗性·冰×0.7`）、drawHelp 与 drawCodex 端到端渲染含新倍率标注且右缘 < 560 不越画布、连续绘制零状态改动（纯显示收敛）、战斗预览行 `弱点火 · 抗冰` 回归逐字（v7.6 口径不变）。README 图鉴与战斗说明两处同步。

## v12.5 技能提示「火焰斩·灼烧每回合伤害量」明确化——技能 hint 表最后一处黑盒（体验打磨·信息透明，承接 v6.5 灼烧角标）

- 【敌人挂上灼烧后角标有数值，**施放前/状态页却看不到灼烧伤害规则**】v6.5 起战斗画面在敌方**已处于灼烧**时常驻角标 `🔥 灼烧 N · 每回合 -X血`（精确到当前敌人），但技能菜单与状态页的技能提示（`SKILL_DATA.hint`）里，唯独火焰斩这行仍只有 `灼烧2回合`——冰霜击写了 `30%冻结（跳过敌回合）`、雷鸣写了 `穿透一半防御`、陨石术写了 `击碎石甲 · 克真身`，全表五个技能只有火焰斩缺「每回合掉多少」：玩家想算「这两回合烧掉它多少血、值不值得先手挂灼烧」只能靠体感或等挂上再看角标。现在技能提示补齐为 `灼烧2回合·每回合约-4%最大HP`——战斗 `2` 技能菜单与 `I` 状态页「已学技能」行两处同时生效，施放前一眼看清规则。此为技能 hint 表的最后一处黑盒，全表自此逐项带数值。
- 【与结算逐字同源】「约-4%最大HP」直接对应 `battle.enemyAct` 灼烧分支的**唯一裁决式** `max(2, round(hpMax×0.04))`（保底 2）——与 v6.5 角标同一数据源、绝无第二套口径；只说「约」而不断言精确值，恰与 HELP_PAGES「毒蛇中毒（约5%最大HP）」的既有措辞同一约定（保底 2 与 4% 在小血敌人上会取高者，故不写死精确百分比）。只读展示，不参与任何判定。
- 【零行为风险】仅改 `js/data.js` 的 `SKILL_DATA['火焰斩'].hint` 一个字符串（+ 注释）；**未动任何灼烧判定、伤害公式（0.04 本体）、回合递减、掉落、经验/金币、难度、成就或存档逻辑**；`burn:2`、`element:'fire'`、技能伤害多段 `1.8` 逐字不变；v6.5 角标、v12.4 掉落档位、帮助页、图鉴全数不受影响。
- 验证：`node --check js/data.js` 与 `npm run check`（23 模块）全部通过；`npm test` **49/49**；`/tmp/jrpg_smoke_esm.mjs` 全量 ESM 冒烟（ALL ESM SMOKE CHECKS PASSED）保持通过；新增 v12.5 专项冒烟（/tmp/jrpg_smoke_v1250_burnhint.mjs）**10 项断言全过**——`SKILL_DATA['火焰斩'].hint` 含 `灼烧2回合` 与 `约-4%最大HP` 且文本与 `enemyAct` 的 `max(2, round(hpMax×0.04))` 语义同源、战斗技能菜单（drawSkillMenu 覆盖层）火焰斩 hint 行逐字渲染且右缘 < 面板右界 490、状态页（drawStatus）「已学技能」火焰斩行含新 hint 且右缘 < 面板右界 570、冰霜击/雷鸣/陨石/治愈四招 hint 逐字在位（回归）、`skillHintLine('火焰斩')` 返回新文本、连续 3 次重绘 hp/mp/gold/xp/level/enemyHp/burn 零改动（纯显示收敛）。README 战斗说明一处同步。

## v12.4 战斗「随机掉落」概率透明化——帮助页补全掉落档位（体验打磨·信息透明，承接 v5.2/v12.0 奖励透明化）

- 【确定性奖励全透明了，随机掉落的概率却仍是黑盒】v5.2 起已把「精英必掉蘑菇 / 终焉之神+300金」这类**确定性**战利品逐项亮出，v12.0 又补齐了 Boss 必掉圣光之剑，但**战后每场都发生的随机掉落**（`battle.js rollDrop`）从 v1.x 起就是一行硬编码：约 8% 掉装备（木剑/铁剑→秘银剑、布衣/皮甲→锁子甲，已最好则 +60金）、12% 掉药水、12% 掉魔法蘑菇（限雾语林/星井矿脉）、6% 掉高级灵药、其余 62% 不掉落——合计约 38% 有掉落。图鉴底部明明常驻「累计讨伐 · 额外掉落 N」计数、成就栏还有「幸运眷顾（累计 5 次额外掉落）」，玩家却**完全不知道这 N 是哪来的、一场打完到底多大概率出货**。现在 H 帮助「魔物状态」页补上一行常驻说明：`战斗掉落——胜利后约 38% 触发随机掉落：8% 装备或+60金 · 12% 药水 · 12% 蘑菇 · 6% 高级灵药`——一眼看清「刷装备/蘑菇该去雾语林深林」与「概率不算低、别小看额外掉落」。
- 【与结算逐字同源】文案四个档位逐字对应 `battle.js rollDrop` 的各 `roll < 0.08 / 0.2 / 0.32 / 0.38` 边界（8% / 12% / 12% / 6%，合计 38%）；装备档升级对象、蘑菇的地图限制均按真实逻辑表述，绝无第二套口径；与既有「强度/倍率/奖励全透明」同一体系。
- 【零行为风险】仅改 `js/data.js` 的 `HELP_PAGES`（+1 行帮助文案）；**未动 rollDrop 任何概率/结算、掉落物品、成就、计数、地图逻辑**——掉落行为逐字不变，只是把概率讲明；帮助页其它 6 行文案、位置、样式逐字不变。
- 验证：`node --check js/data.js` 与 `npm run check`（23 模块）全部通过；`npm test` **49/49**；`/tmp/jrpg_smoke_esm.mjs` 全量 ESM 冒烟（ALL ESM SMOKE CHECKS PASSED）保持通过；v12.3（28/28）、v12.2（9/9）、v12.1（18/18）回归全过；新增 v12.4 专项冒烟（/tmp/jrpg_smoke_v1240_dropodds.mjs）**12 项断言全过**——HELP_PAGES[2] 含「战斗掉落」行且文案含完整四档（38%/8%/12%/12%/6%）、rollDrop 实测 2 万次分布与标注一致（装备 7.6%≈8% / 药水 12% / 蘑菇 11.8%≈12% / 灵药 6.2%≈6%、合计 37.5%≈38%）、drawHelp 魔物状态页含新行渲染无异常。README 系统清单帮助一行同步。

## v12.3 图鉴「出没等级门槛」透明化——树精/石魔像/石心魔像「Lv.3起出没」标注（体验打磨·信息透明，承接 v6.9 图鉴强度）

- 【收集图鉴的最后一个黑盒：这怪到底几级才出现】图鉴（`B`）已透明了报酬、强度、特性、出没地点，但**「这怪练到几级开始在野外出现」**从 v1.x 起就藏在 `battle.js encounterWeight` 的一行硬编码里：`树精/石魔像` 返回 `level >= 3 ? 2 + floor(level/2) : 0`（Lv.3 才进遇敌池），`石心魔像`（精英）在 `randomEncounter` 里也是 `S.G.level >= 3 && 7%` ——作为「图鉴征服（11 种全收）」的必经怪，玩家在 Lv.1~2 怎么刷都遇不见它们，只会以为脸黑，图鉴里也从不说明。现在已讨伐行的「出没」括号补上**等级门槛**：`树精/石魔像 →（草丛随机遇敌 · Lv.3起出没）`、`石心魔像 →（雾语林·稀有精英 · Lv.3起出没）`——一眼看清「练到 3 级再来刷」，与既有「强度/倍率/奖励全透明」同一体系。
- 【与遇敌分布逐字同源（数据化单一数据源）】门槛数据化为 `data.js` 的 `MON_BASE.minLv`（树精/石魔像 `minLv:3`，其余缺省 1）与 `ELITE_GATE_LV = 3`（精英门槛）——`battle.encounterWeight` 与 `randomEncounter` 精英分支改为逐项读取、`rules.js` 新增纯函数 `spawnLv(name)`、图鉴标注调 `spawnLv`，**数值、遇敌分布、标注三处绝无第二套口径**；未讨伐灰色占位仍不剧透（不出没等级、不出数值）；Boss 为剧情遭遇无门槛、不带标注。
- 【零行为风险】`encounterWeight` 逐档权重与历史公式逐字一致（仅把 `level < minLv` 前置、把 `level>=3?…:0` 改写为缺省档），Lv.1/2 抽不到树精石魔像、Lv.3 起权重 3 逐字不变；精英 7% 门槛 `>=3` 不变；只改 `js/data.js`（+2 个 minLv、+1 常量、+1 export）、`js/battle.js`（encounterWeight 前置门槛 + 精英分支读常量）、`js/rules.js`（+1 纯函数 spawnLv）、`js/view/menus.js`（whereFind 追加等级串）；未动任何掉落、经验/金币、伤害、难度、成就、存档、任务、地图逻辑。
- 验证：`node --check js/data.js js/battle.js js/rules.js js/view/menus.js` 与 `npm run check`（23 模块）全部通过；`npm test` **49/49**；`/tmp/jrpg_smoke_esm.mjs` 全量 ESM 冒烟（ALL ESM SMOKE CHECKS PASSED，含 Lv.1 新手保护、Lv.6+ 后期梯度断言）保持通过；v8.0（21/21）、v7.9（24/24）、v7.8（ALL PASSED）、v7.7（17/17）、v7.6、v7.1（18/18）、v7.0（18/18）、v6.9（14/14）、v12.2（9/9）、v12.1（18/18）回归全过；新增 v12.3 专项冒烟（/tmp/jrpg_smoke_v1230_spawnlv.mjs）**28 项断言全过**——spawnLv 数据契约（5 种 Lv.1 / 树精石魔像=3 / 石心魔像=ELITE_GATE_LV=3 / 三 Boss=1 / 未知与 null 钳制 1）、MON_BASE.minLv 与 ELITE_GATE_LV 单一数据源、遇敌分布行为等价（Lv.1/2 village 2 万次无树精石魔像 → Lv.3 起出现、Lv.9 后期梯度保持、dungeon Lv.2 无精英 / Lv.3 约 7%）、drawCodex 端到端逐字（史莱姆无门槛原文 / 树精与石心魔像 `Lv.3起出没` / 幽冥魔王祭坛原文 / 未讨伐灰行不剧透 / 报酬与强度行保留）、连续绘制零状态改动（纯显示收敛）。README 图鉴一行同步。（注：历史 /tmp/jrpg_smoke_v330_codex.mjs 为 v3.3 时代旧测试，未初始化 v4.0+ 的 `S.G` 直接调 drawCodex 报错，在本次改动前的 git HEAD 上同样失败，属旧测试与现行状态注册表不兼容，与本次改动无关；现行权威图鉴冒烟为 v6.9 14/14。）

## v12.2 战斗指令栏「药水消耗顺序」注明（体验打磨·信息透明，承接 v4.4 药水恢复量预览）

- 【恢复量都标了，喝哪一瓶却是黑盒】v4.4 起战斗画面 `[3]药水` 的恢复量预览行把 🍖/🧪 两种药水能回多少都标出来了，但**按 3 到底先消耗哪一瓶**从 v1.x 起就藏在 `hero.js takePotion` 里：两瓶都在手时 `if (hero.potion2 > 0)` 让**高级灵药必然优先被喝掉**——想把手头稀有灵药省给 Boss 的玩家，可能一次中盘小补（只是差几十 HP、MP 还是满的）就悄悄烧掉一瓶辛苦酿的灵药，等真正需要时已经没了。现在两种药水都在手时，恢复量预览行尾追加常驻注明 `（自动先喝🧪）`——按 [3] 前一眼看清「这一口喝的是舍不得的那瓶」，决定是省着用/改用别的指令，还是就这么喝。
- 【与结算逐字同源】注记判定直接复用 `hero.js/core.js` 的同一条件：`potion2 > 0 && item > 0`（两瓶都在手），文案语义即 `takePotion` 的「高级灵药优先」分支（`potion2--` 先行），绝无第二套说法；药水消耗、恢复量、选择逻辑一行未动。
- 【零行为风险】仅改 `js/view/drawBattle.js` 的 `drawBattle()` 恢复量预览行一处（`+1 行 orderNote 条件串 + 注释`）；**未动任何药水选择/消耗/恢复公式/上限/结算逻辑**——🍖/🧪 谁先谁后的真实行为逐字不变，只有显示层把行为讲明。三种持有态分别验证：仅 🍖、仅 🧪（消耗对象唯一、无歧义）一律不带注记；两者皆有时才出现；`battleBusy`（敌方行动回合）整行不显示，注记随之隐藏。
- 验证：`node --check js/view/drawBattle.js` 与 `npm run check`（23 模块）全部通过；`npm test` **49/49**；`/tmp/jrpg_smoke_esm.mjs` 全量 ESM 冒烟（ALL ESM SMOKE CHECKS PASSED）、v12.1 遇敌机制冒烟（18/18）、v12.0 Boss 掉落冒烟全部保持通过；新增 v12.2 专项冒烟（/tmp/jrpg_smoke_v1220_ordernote.mjs）**9 项断言全过**——源码契约（takePotion 先判 potion2、注记语义一致）、两种在手 `[3]恢复` 行含 `（自动先喝🧪）` 且 🍖/🧪 恢复量逐字在位、仅🍖/仅🧪 不带注记、battleBusy 整行隐藏、连绘 8 次 hp/mp/item/potion2/gold/xp/level/scene/battleTurn/敌方HP 零改动（纯显示收敛）、注记行右缘 < 640 画布宽。README 战斗说明一处同步。

## v12.1 遇敌槽机制标注「每步增减」小字（体验打磨·信息透明，承接 v4.2 遇敌槽读数）

- 【百分比之外，「还差几步遇敌」仍是黑盒】v4.2 起小地图遇敌槽常驻百分比读数与 ≥70% 鸣警，但**槽是怎么涨的、怎么降的**没有任何提示——危险格每步 +10~18、喷泉 -25、其它可行走格 -6 这三条增减规则自 v1.x 起就藏在 `world.js tickEncounter` 里：想算「刷完这趟还要几步才遇敌」「该不该绕去喷泉压槽」，只能靠体感或翻 README。现在遇敌槽 pill **正下方**常驻一行机制小字标注 `危险格+10~18 · 喷泉-25 · 道路-6`（10px 琥珀小字、右对齐小地图右缘、随 pill 一起浮在小地图下方）——与读数同屏，一眼算出「满 100 还差几步」「站喷泉要几回合才能压下去」，与既有「战利品/区域倍率/困难倍率全透明」同一体系。
- 【与结算逐字同源】数值直接对应 `world.js tickEncounter` 唯一裁决处：危险格 `+ 10 + Math.floor(Math.random()*9)`（区间 10~18）、`TY.FOUNTAIN → -25`、其余 `-6`——标注与真实增减逻辑绝无第二套口径；`onStep` 喷泉回血处的同式 `-25` 也一致。只读展示，不参与任何判定。
- 【零行为风险】仅改 `js/view/drawWorld.js` 的 `drawMinimap()` 一处（在原 pill 绘制与 `textAlign` 复位之间 +1 行小字 + 注释）；**未动任何遇敌判定、遭遇槽增减公式（+10~18/-25/-6 本体）、回血、地图、碰撞、掉落或战斗逻辑**；原 pill 文案/颜色/位置逐字不变（v4.2 读数、鸣警、色带回归保持）。
- 验证：`node --check js/view/drawWorld.js` 与 `npm run check`（23 模块）全部通过；`npm test` **49/49**；`/tmp/jrpg_smoke_esm.mjs` 全量 ESM 冒烟（ALL ESM SMOKE CHECKS PASSED）保持通过；新增 v12.1 专项冒烟（/tmp/jrpg_smoke_v1210_encmech.mjs）**18 项断言全过**——tickEncounter 三条增减源码在位、`10+⌊rand×9⌋→10~18` 区间推导、三张地图（village/dungeon/cave）机制标注行逐字绘制且右对齐、0/42/100% 各档标注行恒在、原 pill 读数（`遇敌 0%`/`遇敌 42%`/`遇敌 100% ⚠️ 危险逼近`）逐字回归、连绘 12 次 encGauge/scene/hp/mp/gold/xp/level/curMap/坐标 零改动（纯显示收敛）。README 战斗说明一处同步。（注：历史 /tmp/jrpg_smoke_v420_encgauge.mjs 的 1 项「底色轨道 .8 vs .9」断言为初始提交即存在的旧基线漂移，与本次改动无关，已在 git HEAD 复现核验。）

## v12.0 Boss 战「战利品预览」补必掉圣光之剑标注（体验打磨·信息透明，承接 v5.2/v11.0 确定性奖励透明化）
- 【打赢幽冥魔王必得圣光之剑，预览行却从不写】战斗画面「战利品预览」行自 v5.2 起已列明精英必掉蘑菇（🍄）、终焉之神另 +300 金（💰）、试炼通关奖励（v11.0），唯独 **Boss 幽冥魔王首败必掉的传说剑「圣光之剑」从不示人**——预览行只挂 `经验 +150 金币 +300 · 抗冰`，而 `winBattle` 里 `isBoss` 分支从 v1.x 起就保证首次击败必装备攻+24 的圣光之剑；玩家只有打赢那一刻的 `⚔️ 剑里封着被偷走的黎明——【圣光之剑】！` 才知道，打完才知道「这趟值得打」。现在预览行与图鉴 `codexTag`（`⚔️ 必掉圣光之剑`）逐字对齐补上：`战利品预览：经验 +150 金币 +300 · 抗冰 · ⚔️ 必掉圣光之剑`——进祭坛前一眼看清这把通关关键武器是必得收益。
- 【与结算逐字同源】标注直接判 `enemy.isBoss`（与 `winBattle` 掉落分支读同一旗标），文案与 `data.js` 图鉴 `codexTag('幽冥魔王')` 的 `⚔️ 必掉圣光之剑` 逐字一致，绝无第二套口径；终焉之神（isTrue）与石心魔像（isElite）旗标互斥、绝不误标；试炼 RUSH 首关（isRush 非 isBoss）按设计不掉落、同样不显示。
- 【零行为风险】仅改 `js/view/drawBattle.js` 预览行 `bonus` 拼接一处（`+1 个 isBoss 分支`）；**未动任何掉落/经验/金币/装备/难度/成就/存档/战斗结算逻辑**；`winBattle` 的圣光之剑授予行为逐字不变；普通怪/精英/试炼预览行文本与既有逐字一致（各自旗标为假，零回归）。
- 验证：`node --check js/view/drawBattle.js` 与 `npm run check`（23 模块）全部通过；`npm test` **49/49**；`/tmp/jrpg_smoke_esm.mjs` 全量 ESM 冒烟（ALL ESM SMOKE CHECKS PASSED）保持通过；新增 v12.0 专项冒烟（/tmp/jrpg_smoke_v1200_bossdrop.mjs）**10 项断言全过**——源码 `isBoss` 分支与 `winBattle` 圣光之剑授予同源、图鉴 codexTag 逐字一致、Boss 战 enemy.isBoss 在位、幽冥魔王 xp150/gold300 契约、普通怪/终焉之神/精英/试炼RUSH 四类旗标互斥不误标、连续 3 次重绘 hp/mp/gold/xp/level/scene/enemyHp/turn 零改动（纯显示收敛）；v11.0 试炼奖励冒烟（21/21）与 v10.0 标题快捷键冒烟（10/10）回归全过。README 战斗说明一处同步。

## v11.0 试炼三连战「通关奖励」透明化（体验打磨·信息透明）
- 【试炼值不值得打，奖励是多少从来不说】试炼碑三连战是通关后唯一的高难内容，战利品预览行却只挂一句含糊的 `（试炼通关另有奖励）`——打完三关到底多给多少钱、值不值得为它备药练级，全靠通了才知道；而实际奖励 `150 + 等级×20` 从 v1.x 起就藏在 `battle.js winBattle` 一行硬编码里。现在预览行把**确切数额**写出来：`战利品预览：经验 +90 金币 +0 · 抗火（试炼通关另奖 350 金币，随等级提升）`——进试炼前一眼看清「这趟值多少钱」，与既往「战利品/强度/倍率全透明」体系一致。
- 【与结算逐字同源】奖励公式数据化为 `rules.js` 的纯函数 `rushReward(level)`（`150 + level×20`，等级≤0 钳制为 0），`battle.winBattle` 的结算直接调用它——结算与预览**同一份数据源**，绝无第二套口径；预览数额按当前等级实时显示，注明「随等级提升」，与结算「按最后一关击杀时等级计」语义一致（若试炼途中升级，结算金额随升级同步变大，预览也如实跟随）。非试炼战（普通怪/Boss/精英）完全不显示该串，无泄漏。
- 【零行为风险】仅 `rules.js` +1 个纯函数、`battle.js` 结算一行改为调用它（数值逐字不变 `150 + level*20`）、`view/drawBattle.js` 预览行一处文案由占位改确切数额；未动任何遇敌、掉落、经验/金币、难度、成就、存档、任务、地图逻辑；试炼奖励数值与原公式完全一致，仅显示层变化。
- 验证：`node --check js/rules.js js/battle.js js/view/drawBattle.js` 与 `npm run check`（23 模块）全部通过；`npm test` **49/49**、`/tmp/jrpg_smoke_esm.mjs` 全量 ESM 冒烟（ALL ESM SMOKE CHECKS PASSED）保持通过；v10.0/v9.0/v8.0/v7.9/v7.7/v7.6/v7.5/v7.1/v7.0 专项冒烟全过；新增 v11.0 专项冒烟（/tmp/jrpg_smoke_v1100_rushreward.mjs）**21 项断言全过**——`rushReward` 数据契约（1→170 / 10→350 / 20→550 / 0→150 / 负数与 null/undefined → 150 不 NaN / 随等级递增）、结算同源（level 10 通关最终关 gold 增量恰为 350、rushDone 置位、rushStage 复位、整数无浮点残渣）、试炼战预览行端到端逐字 `试炼通关另奖 350 金币`（level 10）与 `随等级提升`、level 20 联动显示 550、旧占位文案已移除、非试炼战不泄漏、连绘 3 次零状态改动（纯显示收敛）。README 战斗说明一处同步。

## v10.0 标题页快捷键提示补全两行（体验打磨·可发现性，承接 v9.0 暂停菜单）
- 【标题页只提示 6 个键，常驻面板全隐身】标题画面底部唯一一行提示是 `按 1/2/3 选槽 · WASD移动 · Esc菜单 · P存档 · M静音 · H操作说明`——`I 状态 / J 日志 / B 图鉴 / C 成就 / T 旅行 / F 喝药` 这些常驻面板与操作明明都存在（v9.0 起 Esc 菜单也把它们一一列全），却从不在这唯一的新手会盯着的画面出现；不开 H 帮助的新玩家根本不知道还有状态页、任务日志、图鉴、成就、快速旅行、战斗中按 F 喝药这些功能。标题页作为「重置/换档/新开档」的必经界面，理应把全部常驻操作一眼列全。现在标题提示补全为两行：第一行 `按 1 / 2 / 3 选择存档槽 · L 读档 · WASD 移动 · Esc 菜单 · P 存档 · M 静音`，第二行 `I 状态 · J 日志 · B 图鉴 · C 成就 · T 旅行 · F 喝药 · H 帮助`（第二行降暗 11px，主次分明）。
- 【与按键分派逐字同源】两行提示每个快捷键都直接对应 `main.js` 世界画面/标题画面的分派路径（`i→goto('status')`、`j→journal`、`b→codex`、`c→ach`、`t→travel`（含进门前当前地图定位，同 v9.0）、`f→usePotion`、`h→help`、`l→load`、`p→saveGame`、`m→静音`）——先核对该表再落文案，绝不写提示里不存在的快捷键。
- 【零行为风险】仅改 `js/view/menus.js` 的 `drawTitle()` 两行 `fillText` 文案（+ 一条注释）；未动任何按键分派/场景/面板/存档/战斗/数值逻辑；`node --check` 与 `npm run check`（23 模块）全部通过；`npm test` **49/49**；新增 v10.0 专项冒烟（/tmp/jrpg_smoke_v1000_titlekeys.mjs）**10 项断言全过**——两行提示源码逐字在位、drawTitle 渲染不抛错、运行期两行逐字绘制、I/J/B/C/H 逐个按下进入正确界面、T 进入 travel 且定位当前地图、F 喝药不换场景、连续 3 次 drawTitle 不改场景/hp/金币/经验/等级（纯显示收敛）；v9.0/v8.0/v7.9 专项冒烟回归全过。README 快捷键表本就完整、无需改动。

## v9.0 暂停菜单补全「成就 / 快速旅行」入口（体验打磨·可发现性）
- 【两个面板只有快捷键、菜单里找不到】Esc 暂停菜单此前只有 状态/日志/图鉴/存档/帮助 五项面板 + 继续/返回标题——「成就一览（C）」和「快速旅行（T）」明明都是常驻面板，却只能靠 README 里的快捷键找到；新玩家按 Esc 翻菜单根本不知道还有这两个界面可看（成就更是完全隐身）。现在菜单补全为 9 项：`继续冒险 / 状态(I) / 任务日志(J) / 敌人图鉴(B) / 成就(C) / 快速旅行(T) / 存档(P) / 操作说明(H) / 返回标题`——每个可进面板与快捷键一一对齐，可发现性补齐。
- 【接入复用既有逻辑】`PAUSE_ITEMS` 新增 `ach` / `travel` 两项，`main.js` 的 pause 分派直接复用世界画面同样的 goto 路径（`travel` 进入前用与 `T` 键逐字相同的当前地图定位逻辑）；行距 36→32（高亮条 32→28）在 392px 面板内放下 9 行，末行文本 y=380 仍远高于底部提示行 y=412。
- 【零行为风险】未动任何战斗/任务/成就/存档/地图/数值/快捷键本身；`C` / `T` 直开路径逐字不变；Esc 返回、↑↓ 环绕（仍按 `PAUSE_ITEMS.length` 取模）、存档槽提示全部不变；未知 id 依旧静默返回。
- 验证：`node --check js/main.js js/view/menus.js` 与 `npm run check`（23 模块）全部通过；`npm test` **49/49**；新增 v9.0 专项冒烟（/tmp/jrpg_smoke_v900_pausemenu.mjs）**10 项断言全过**——9 项结构与顺序、成就/快速旅行 hint、九选位 drawPause 渲染不抛错、8 个导航项 Enter 接线逐项进入正确场景（含 travel 定位当前地图）、Esc 返回、↑↓ 双向环绕；`/tmp/jrpg_smoke_esm.mjs` 全量 ESM 冒烟（ALL ESM SMOKE CHECKS PASSED）保持通过。README 快捷操作一行同步。

## v8.0 困难模式倍率数据化 + 创建/状态页常驻标注（体验打磨·信息透明，承接 v7.8/v7.9 区域倍率）
- 【困难档的「黑盒」终于拆开】区域倍率（v7.8/v7.9）透明化之后，全游戏只剩一处数字从不示人：**困难档的具体代价**。选档时只有一句「魔物更强、血更高」，进游戏后困难档玩家浑身上下只挂一个 `⚡` 徽标——「这档到底难在哪」从来没被写成明文，README 写着 `敌×1.35血/×1.15攻/×1.12防`，游戏内却从 v1.x 起就藏在 `battle.js startBattle` 的 `if (S.G.diff)` 三行硬编码里。现在困难档的确切倍率**常驻两处**：创建角色页（选档瞬间告知代价）与状态界面（`I` 姓名行）：`[困难 · 魔物HP×1.35 攻×1.15 防×1.12]`——进游戏后随时可核对「我面对的是 35% 血 / 15% 攻 / 12% 防 的魔物」，与 v7.8 区域倍率、v7.7 暴击标注同一「信息透明」体系。
- 【与结算逐字同源】倍率数据化为 `data.js` 的 `DIFF_SCALE`（`{hp:1.35, atk:1.15, def:1.12}`），`battle.startBattle` 的敌方缩放改为逐项读 `DIFF_SCALE`，两处标注**直接取同一份数据**——数值、结算、标注三处绝无第二套口径（`Math.round` 取整次序逐字不变）。
- 【零行为风险】`DIFF_SCALE` 的数值与旧硬编码完全一致、缩放只在 `startBattle` 对 `S.enemy` 生效（boss/精英/普通怪/试炼/RUSH 皆同），困难档战斗数值逐项与改动前完全相同；未动任何遇敌/掉落/经验金币/成就/存档/任务/地图逻辑；普通档（无 `diff`）完全不走该分支、行为零变化。
- 验证：`node --check js/data.js js/battle.js js/view/menus.js` 与 `npm run check`（23 模块）全部通过；`npm test` **49/49**；新增 v8.0 专项冒烟（/tmp/jrpg_smoke_v800_diffscale.mjs）**21 项断言全过**——DIFF_SCALE 数据契约、困难档 `startBattle` 逐项缩放（hp×1.35 / atk×1.15 / def×1.12）且与常量逐字同源、普通档不缩放回归、drawStatus 困难行逐字含完整三倍率（普通档不带）、drawCreate 困难说明含确切倍率、连续 3 次重绘 hp/mp/gold/xp/level/map/scene 零改动（纯显示收敛）。README 角色创建一行同步。

## v7.9 HUD 区域标注「补全全部倍率」攻/防/经验/金币（体验打磨·信息透明，承接 v7.8 区域标注）
- 【只标「魔物×」不看攻防与收益，仍是半个黑盒】v7.8 把区域修正数据化并常驻 HUD，但地图行只写了 `凶险之地·魔物×1.2`——星井矿脉明明还悄悄提升了怪的攻击（×1.15）、防御（×1.1）、经验/金币（各×1.15），玩家却只看见血量倍率；雾语林两处入口同一只史莱姆的难易、矿脉里打怪经验为何偏高，都无从核对。现在地图行**逐项列出全部区域倍率**：`凶险之地·魔物×1.2·攻×1.15·防×1.1·经验×1.15·金币×1.15`、`安宁之地·魔物×0.9·攻×0.9`——「这区危险在哪、收益在哪」一眼看全，与 v7.8 同属「信息透明」体系。
- 【与结算逐项逐字同源】标注的每项倍率直接遍历 `data.js MAPS.zone`（hp→魔物 / atk→攻 / def→防 / xp→经验 / gold→金币），`randomEncounter` 逐项应用同一份数据——village 两项、cave 五项、无 zone 的雾语林（dungeon）完全不显示；只列存在项，绝不虚报不存在的字段。
- 【零行为风险】仅改 `js/view/hud.js` 的 `renderHUD()` 一处字符串构建（`zoneTag`）；**未动任何遇敌/战斗/掉落/经验/金币/难度/成就/存档/地图逻辑**；困难档 `⚡` 徽标与昼夜标签、地图名、角色名拼接顺序逐字不变。
- 验证：`node --check js/view/hud.js` 与 `npm run check`（23 模块）全部通过；`npm test` **49/49**；`/tmp/jrpg_smoke_esm.mjs` 全量 ESM 冒烟（ALL ESM SMOKE CHECKS PASSED，含 `cave 难度倍率生效 ×1.2` / `village 减益倍率生效 ×0.9` 既有断言保持通过）；新增 v7.9 专项冒烟（/tmp/jrpg_smoke_v790_zonetag.mjs）**24 项断言全过**——zone 数据契约（cave 五项 / village 两项 / dungeon 零）、cave 与 village 逐字完整含每项倍率、dungeon 不标注、连续 5 次重绘 hp/mp/gold/xp/level/map/scene 零改动（纯显示收敛）、`randomEncounter` 区域修正（hp×1.2 / atk×1.15 / xp×1.15 / gold×1.15）回归不受影响、标注与 zone 数据逐字同源、困难档 ⚡ 徽标在位。README 视觉一处同步。

## v7.8 HUD 地图行常驻「区域强度」标注 + 区域修正数据化（体验打磨·信息透明，承接 v7.7 暴击标注）
- 【为什么同一个怪，矿脉里比镇里硬一截？】从 v1.x 起,地图倍率就一直硬编码在 `battle.js randomEncounter` 的 `if/else` 里（`cave` 魔物 hp×1.2 / atk×1.15 / def×1.1、经验金币×1.15；`village` hp×0.9 / atk×0.9）——遇敌时怪物属性**默默**被放大/缩小，战斗预览里的血槽也照算，玩家却从不知道「这是地区特性、不是脸黑」；战利品预览、图鉴强度全部透明化的今天，**「为什么这个区更危险/更安逸」仍是黑盒**。现在 HUD 地图行常驻**区域强度标注**：`守灯人 · 星井矿脉 · 白天 · 凶险之地·魔物×1.2`、`潮灯镇 · 安宁之地·魔物×0.9`——进图第一眼就知道该按什么预期练级/补给，与 v7.6 抗性标注、v7.7 暴击标注同一「信息透明」体系。
- 【与结算逐字同源】区域修正**数据化**为 `data.js` 的 `MAPS.zone`（village：`{label:'安宁之地', hp:0.9, atk:0.9}`；cave：`{label:'凶险之地', hp:1.2, atk:1.15, def:1.1, xp:1.15, gold:1.15}`），`randomEncounter` 改为逐项读 `zone`；HUD 标注文本 `zone.label·魔物×zone.hp` **直接取自同一份数据**——数值、标注、结算三处绝无第二套口径。无 `zone` 的地图（雾语林）完全不修正、不显示标注。
- 【零行为风险】**数值逐项与旧公式完全一致**（cave 五项 / village 两项倍率、四舍五入、hpMax 同步）——只是硬编码换成了数据驱动；未动精英公式、掉落、monReward 基准、困难模式（×1.35/×1.15/×1.12）、成就、存档、任务、地图本体；Boss/试炼/RUSH 不走 `randomEncounter`、完全不受影响。
- 验证：`node --check js/data.js js/battle.js js/view/hud.js` 全部通过；`npm run check`（23 模块）全部通过；`npm test` **49/49**；`/tmp/jrpg_smoke_esm.mjs` 全量 ESM 冒烟（ALL ESM SMOKE CHECKS PASSED，含 `cave 难度倍率生效 ×1.2` / `village 减益倍率生效 ×0.9` 两只既有断言保持通过）；新增 v7.8 专项冒烟（/tmp/jrpg_smoke_v780_zone.mjs）**15 项断言全过**——zone 数据契约、`randomEncounter` 确定性逐项应用（cave 五项 / village 两项 / dungeon 零修正）、hpMax 同步、renderHUD 地图行逐字标注（`凶险之地·魔物×1.2` / `安宁之地·魔物×0.9`）、无 zone 地图不显示、标注与 zone 数据逐字同源、连续重绘不破坏地图行结构。README 视觉/区域标注一处同步。

## v7.7 状态界面攻击行常驻「暴击率×倍率」标注（体验打磨·信息透明，承接 v4.6 精确伤害体系）
- 【每个战斗数值都被透明化的今天，暴击仍是最后一个黑盒】v4.6 起敌方「下一击伤害」、v5.4/v6.0 双方精确 HP、v7.1 逐招预判、v7.5 反击伤害——攻击/防御/技能/抗性/石甲/中毒/灼烧全部「看得出数字」，唯独**普攻本身的暴击**（概率 12%、倍率 ×1.8）从 v1.x 起就藏在 `battle.js` 的 `doAttack`：`Math.random() < 0.12` 判定、`attackMove` 里 `×1.8` 结算——玩家只看得到「💥（暴击！）」飘字，永远不知道这一下是几率的哪一面、面板该不该拿暴击概率算期望；README 也只在蓄力一栏顺带提了句「与暴击可叠加」，游戏内零提示。现在状态页（`I`）攻击行右侧常驻**精确标注**：`· 普攻12%暴击 ×1.8`——与结算逐字同源，一眼看清「面板外还有 12% 的无本加成」，练级换装时把暴击期望也一并算进账。
- 【与结算逐字同源】数值直接源于 `battle.js` 唯一裁决处：`doAttack` 的 `const crit = Math.random() < 0.12` 与 `attackMove` 的 `dmg * (isCrit ? 1.8 : 1)`——绝无第二套口径；且只影响**普攻**（技能不触发），标注完整注明「普攻」。辅以位置核对：攻击数值行（x=150 起点、Lv.6 圣剑 `27 +24 = 51` 右缘 ≈198）与暴击标注（x=288 起点、琥珀小字，右缘 ≈360 < 570 面板右界）零重叠，不遮挡任何既有字段。
- 【零行为风险】仅改 `js/view/menus.js` 的 `drawStatus()` 攻击行一处（`+1 行 11px 小字标注 + 注释`）；**未动任何暴击判定、伤害公式（12%/×1.8 本体）、掉落、经验/金币、难度、成就或存档逻辑**；战斗画面指令栏/技能菜单/攻击预览（`≈N伤` 仍为无浮动期望值、不含暴击，口径不变）与其余状态行逐字不动。
- 验证：`node --check js/view/menus.js` 与 `npm run check`（23 模块）全部通过；`npm test` **49/49**；`/tmp/jrpg_smoke_esm.mjs` 全量 ESM 冒烟（ALL ESM SMOKE CHECKS PASSED）、v7.0（18/18）、v7.1（18/18）、v7.3（40/40）、v5.9（21/21）保持通过；新增 v7.7 专项冒烟（/tmp/jrpg_smoke_v770_crit.mjs）**17 项断言全过**——drawStatus 攻击行逐字含「普攻12%暴击 ×1.8」、数值与 battle.js 源码 doAttack(0.12)/attackMove(×1.8) 逐字同源、攻击值右缘 < 288 与标注零重叠、标注右缘 < 570 面板右界、防御/金币/技能/冒险进度各既有行在位（回归）、连续 5 次重绘 hp/mp/gold/xp/level 零改动（纯显示收敛）、drawBattle 渲染与指令栏回归（未被暴击标注污染）。README 战斗说明一处同步。

## v7.6 战斗「战利品预览」行补齐敌方【抗性】标注（体验打磨·信息透明，承接 v6.3/v7.0 伤害预览透明化）
- 【技能伤害明明低一截，却不说为什么】战斗画面「战利品预览」行多年来只标敌方**弱点**（`· 弱点火`），但从 v4.x 起技能期望伤害就已**默默**按 `elemMult` 把抗性 ×0.7 算进去了——打骷髅兵/树精/石魔像/魔王真身时，同一招 `≈N伤` 比打无抗怪物低 30%，玩家只能靠图鉴（`B`）翻数据才能对得上，战斗内「打它为什么这么低」始终是黑盒。现在预览行与弱点对称补上**抗性**：`战利品预览：经验 +50 金币 +39 · 弱点火 · 抗冰`——一眼看清「这怪天生扛冰刀 30%，陨石/火招更划算」。图鉴 `codexTag` 与战斗画面从此同一套口径，绝无第二套。
- 【与结算逐字同源】数值直接读取 `enemy.resist`（与 `elemMult` 抗性 ×0.7、`rules.js` 的 `codexStats`/图鉴 `抗性·X` 同一份 `SPECIES`/`MON_BASE` 数据源）；无抗性敌人（史莱姆/野狼/哥布林/毒蛇/石心魔像）不显示，行文本与 v7.5 逐字一致、零回归；弱+抗并存（骷髅兵/树精/石魔像）与仅抗无弱（幽冥魔王·真身、终焉之神）都正确落位；最宽行（终焉之神：抗火+🍄必掉蘑菇+战胜另+300金）居中右缘 ≈569 < 640 画布宽，不越界。
- 【零行为风险】仅改 `js/view/drawBattle.js` 预览行一处（`+1 个 res 拼接串`，坐标 y=112 居中行不变）；**未动任何伤害公式、`elemMult`、技能期望、掉落、经验/金币、难度、成就或存档逻辑**；Boss 受击预判行（v7.1/v7.5）与其余既有文案逐字不变。
- 验证：`node --check js/view/drawBattle.js` 与 `npm run check`（23 模块）全部通过；`npm test` **49/49**；`/tmp/jrpg_smoke_esm.mjs` 全量 ESM 冒烟（ALL ESM SMOKE CHECKS PASSED）保持通过；新增 v7.6 专项冒烟（/tmp/jrpg_smoke_v760_resist.mjs）**16 项断言全过**——双标注敌人/仅抗敌人/无抗敌人逐字正确、与 v7.5 前无抗行文本完全一致、真实最宽行估算右缘 569<640、连续 3 次重绘 hp/mp/gold/xp/敌方HP/回合/日志 零改动（纯显示收敛）、含抗性怪渲染不抛错；v7.5 反击冒烟（14/14）与 v7.1 Boss 预判冒烟（18/18）保持通过。README 战斗说明一处同步。

## v7.5 防御反击伤害预估「反击≈N伤」补入预判行（体验打磨·信息透明，承接 v6.7 防御态完整效果）
- 【知道会被减半、能回蓝、能反击——却不知道这一刀反击打多少】v6.7 起防御态角标已如实标注「50%几率反击」，但「反击能打掉敌方多少血」仍是黑盒：同样的 `[5]防御`，对石头壳的史莱姆和对软壳的精英效果天差地别，玩家要么按住防御碰运气、要么干脆没把反击算进「该不该防」的账里。现在普通战/Boss 战的**防御中（格挡中）敌袭预判行补上精确反击伤害**：普通怪 `敌方攻击预判：格挡中 · 本回合只受 -10血 · 反击≈18伤`，Boss 战 `格挡中 · 普攻 -12血 · 重击 -23血 · 反击≈18伤`——一眼看清「扛这一刀的同时，白赚敌方多少血」，防御从「挨打少一半」升级成「攻防一体的可计算决策」。
- 【与结算逐字同源】数值直接套 `battle.js` `enemyAct` 防御反击分支的同一式：`max(1, cmdDmg(hero.atkMax, enemy.def, 0.7))` —— 反击倍率 0.7、`cmdDmg` 无浮动（确定性取值），与真实反击结算绝无第二套口诀；普通战与 Boss 战共用同一 `counterDmg` 计算，只有展示位置不同。只在**格挡中（防御态）**显示，未防御时不预览反击值（不提前剧透）。
- 【零行为风险】仅改 `js/view/drawBattle.js` 的 `drawBattle()` 预判分支两处字符串（Boss 分支与普通怪分支各追加 ` · 反击≈N伤`，都在既有预判行内、坐标 y=395 居中行不变，实测文本右缘仍远小于 640 画布宽）；**未动任何防御减伤 / MP 回复 / 反击判定与伤害结算 / 敌方行动 / 掉落 / 难度 / 成就 / 存档逻辑**；非防御态预判行（v5.3）与 Boss 逐招预判行（v7.1）逐字不变。
- 验证：`node --check js/view/drawBattle.js` 与 `npm run check`（23 模块）全部通过；`npm test` **49/49**；新增 v7.5 专项冒烟（/tmp/jrpg_smoke_v750_counter.mjs）**14 项断言全过**——普通怪/Boss 格挡中行逐字完整（受击 + `反击≈N伤`）、反击值与 `max(1, round(max(1, atkMax×2−def)×0.7))` 逐字同源、非防御态原预判行回归且不泄露反击、Boss 逐招 v7.1 回归、致命判定不因反击值变化、连续 2 次重绘 hp/mp/gold/xp/level/enemyHP/defending/turn 零改动（纯显示收敛）；v7.1（18/18）、v7.3（40/40）、v7.4（21/21）专项冒烟保持通过。README 战斗说明一处同步。

## v7.4 状态界面技能区「MP 消耗」显示（体验打磨·信息透明，承接 v4.3 战斗技能菜单）
- 【战斗菜单标了 MP、状态页却一片空白】战斗里按 `2` 的技能菜单每条都实时标注 MP 消耗（v4.3），但常驻的状态界面（`I` 键）「已学技能」列表只写效果说明、不写消耗——出战斗该练哪招、蓝量撑不撑得起连发，只能靠记忆或进战斗才知道。现在状态页技能行补齐 MP：`· 火焰斩（灼烧2回合） · 4 MP`；「下一技能」预估行也同步补上该招消耗：`📖 下一技能：冰霜击（Lv.3 · 还差 48 经验 · 5 MP）`——状态页一眼看清「这一招花多少蓝、下一招要攒多久」。
- 【与战斗技能菜单逐字同源】MP 值直接读取与技能菜单同一份 `SKILL_DATA.mp`，绝无第二套口径；`sd.mp` 缺省（未知技能名）自动不拼接，不显示 NaN、不抛错。
- 【零行为风险】仅改 `js/view/menus.js` 的 `drawStatus()` 两处字符串（已学技能行 + 下一技能行，坐标/字号/行距均不变，文本宽度仍远小于 500px 面板）；**未动任何 MP 结算、技能伤害、领悟曲线、升级判定、战斗或数值**；v6.6 经验行「距升级还差 N 经验」等既有文字逐字不变。
- 验证：`node --check js/view/menus.js` 与 `npm run check`（23 模块）全部通过；`npm test` **49/49**；新增 v7.4 专项冒烟（/tmp/jrpg_smoke_v740_mpstatus.mjs）**21 项断言全过**——火焰斩行 `· 4 MP` 逐字在位且与 `SKILL_DATA.mp` 同源、5 招全学满逐招 MP 正确、全技能后「已习得全部技能」路径、未知技能名不挂 NaN、下一技能行含 v6.6 剩余经验 + MP、连续绘制不改任何游戏状态（纯显示收敛）、v6.6 经验行回归；v6.6 专项冒烟 **10/10** 与 `/tmp/jrpg_smoke_esm.mjs` 全量 ESM 冒烟（ALL ESM SMOKE CHECKS PASSED）保持通过。README 系统清单一处同步。

## v7.3 通关后全镇 NPC 彩蛋对话（新内容·世界完整回应终局，承接 v7.2 井巫彩蛋）
- 【彩蛋只此一家、其余 NPC 依旧旧叙事】v7.2 为井巫补齐了通关彩蛋，但攻下终焉之神、看过真结局之后再回村，镇民还念叨「灯灭之后数着路回家」、巡灯人还在讲「被它装进剑里的黎明」、雾径猎手还在劝「备好药水再走」——**世界只响了一次回声**。现在三位非任务 NPC 也各配通关彩蛋：镇民（「雾散了，镇里的灯一盏盏亮回来…天还是半亮，可够把路看清了」）、巡灯人（「他确是旧灯卫——把黎明封进剑里…那名字，如今归你替他记着」）、雾径猎手（「魔物是雾里长出来的。雾一散，它们便像被领回家的记忆，各归各位」）——同一主题「记得/记忆/灯」的三种和声，兑现 v5.0「灯灭→夺芯→半亮黎明」的结局。
- 【纯数据 + 既有回退分支】仅 `js/data.js` 的 `NPCS`（villager/adventurer/hunter 三条）追加 `after` 台词数组；机制零新增——`js/quests.js` 的 `npcQuestPages` 无待办任务回退时 `hero.trueBoss && NPCS[id].after` 优先返回，v7.2 已搭好，本次只是把既有数据驱动机制填满。
- 【零行为风险】未动任何战斗/任务/成就/存档/地图/数值；任务待办依旧优先于彩蛋（chief/cartman 两条支线不受影响）；未通关玩家台词逐字不变；无 `after` 的 NPC（chief/cartman）即使通关仍走原台词；未知 NPC 仍回退 `[……]` 占位；`npcQuestMark` 顶标判定不受影响（纯闲聊 NPC 依旧无顶标）。
- 验证：`node --check js/data.js` 与 `npm run check`（23 模块）全部通过；`npm test` **49/49**；新增 v7.3 定向冒烟（/tmp/jrpg_smoke_v730_after.mjs）**40 项断言全过**——三个 NPC 数据契约（after 2 页/继续·结束标记/原台词保留）、通关后逐字返回彩蛋、未通关（trueBoss 缺省/false/null）台词逐字不变、cartman/chief 无 after 通关后仍原台词、未知 NPC 占位不变、顶标仍为 null、彩蛋关键句与 v5.0 世界观一致；`node -e import('./js/main.js')` 启动引导零报错。README 系统清单一处同步。

## v7.2 通关后「井巫」彩蛋对话（新内容·世界回应终局，承接 v5.0 记忆主题）
- 【通关后世界毫无反应】击败终焉之神、看完结局后，再回星井矿脉与任何 NPC 对话，台词都还停留在「水晶里睡着终焉之神」的旧叙事里——世界不记得你已经赢了。现在井巫（星井矿脉入口，4,1）在 `trueBoss` 达成后改说**通关彩蛋**：`水晶空了。星砂落回矿脉深处……记得的灯，从此由你掌着。`——两页收尾，兑现全游戏「记得/记忆/灯」的母题。
- 【纯数据 + 一处回退分支】`js/data.js` 的 `NPCS.sage` 新增 `after` 台词数组（仅数据）；`js/quests.js` 的 `npcQuestPages(hero, npcId)` 无待办任务回退时，若 `hero.trueBoss && NPCS[npcId].after` 则优先返回彩蛋。机制天然数据驱动：今后任何 NPC 定义 `after` 即可在通关后换台词，不动其他逻辑。
- 【零行为风险】未动任何战斗/任务/成就/存档/地图/数值；任务待办（接/交任务）依旧优先于彩蛋（chief 支线不受影响）；未通关玩家台词逐字不变；无 `after` 的 NPC（猎人/车夫等）即使通关也仍走原台词；未知 NPC 仍回退 `[……]` 占位。
- 验证：`node --check js/data.js js/quests.js` 与 `npm run check`（23 模块）全部通过；`npm test` **49/49**；`/tmp/jrpg_smoke_esm.mjs` 全量 ESM 冒烟（ALL ESM SMOKE CHECKS PASSED）保持通过；新增 v7.2 定向断言（quests.js 纯函数直测，无 DOM 桩）**8/8 全过**——通关后井巫返回 2 页彩蛋、未通关台词逐字不变、无 after 的 NPC 不受影响、未知 NPC 占位不变、chief 任务待办仍优先。
- 附带：修复技能脚本 `balance_sim.mjs` 适配 v4.0+ `state.js` 导出对象 `S`（原 globalThis 写法已失效），并用其对当前版本复跑蒙特卡洛——普通档生还率 99.97–100%、困难智能喝药 94.72%、凶手均匀分布、新手保护完好，**开局数值无需调整**。

## v7.1 Boss/试炼战「敌方攻击预判」逐招列出（体验打磨·信息透明，承接 v6.3 敌招一览）
- 【最强力的战斗反而没有受击预判】普通怪/精英战在指令栏底部常驻 `敌方攻击预判：-N血（防御后-N血）`，但 Boss 战（幽冥魔王/洞窟领主/终焉之神/试炼三连战）因「多招数、伤害不确定」被整体跳过——y≈395 处一片空白，打 Boss 只能凭体感猜「这刀要不要防」。现在 Boss 战按敌方招数表**逐招列出期望伤害**：`敌方攻击预判：普攻 -24血 · 重击 -46血（防御后-12/23血）`，防御中显示 `格挡中 · 普攻 -12血 · 重击 -23血`；任一招可能致命则橙色 `⚠️致命`（取最重一击为上限）。
- 【与结算逐字同源】直接读取 `battle.pickAct/enemyAct` 同一份 `enemy.acts`：重击倍率同 `enemyAct`（真身 2.3 / 平时 1.9）、受击公式 `cmdDmg(atk, defMax, mult)` 同式同序、防御中 ×0.5 取整同结算；只列会出伤害的招（回血/石甲不算受击，不虚报）；无攻击招的 Boss 自动回退为普攻单招；与 v6.3 敌招一览共用招数数据源，绝无第二套口径。
- 【零行为风险】仅改 `js/view/drawBattle.js` 一处（在既有 `!S.skillMenuOpen` 分支内、`isBossFlee` 时新增绘制，y≈395 为 Boss 战空位，不与血条/角标/命令栏/日志重叠）；**未动任何 Boss 出招判定、变身、伤害公式、掉落、难度、成就、存档逻辑**；精英（石心魔像）与普通怪仍走原单招预判、行为完全不变。
- 验证：`node --check js/view/drawBattle.js` 与 `npm run check`（23 模块）全部通过；`npm test` **43/43**；新增 v7.1 专项冒烟（/tmp/jrpg_smoke_v710_bossprev.mjs）**18 项断言全过**——幽冥魔王/终焉之神/试炼 rush 三线预判逐字同源、非防御态含防御后列、防御态格挡中不叠加括号、真身重击 ×2.3 联动、致命警示（最重一击≥HP）、满血不误报、回血/石甲不计入、精英/普通怪单招预判回归、无 acts 回退不抛错、技能菜单打开不绘制、连续 3 次重绘零状态改动（纯显示收敛）；`/tmp/jrpg_smoke_esm.mjs` 全量 ESM 冒烟（ALL ESM SMOKE CHECKS PASSED）保持通过。README 战斗说明已同步。

## v7.0 石甲减伤计入攻击/技能伤害预览（体验打磨·信息透明，承接 v6.9）
- 【伤害预览在石甲面前虚报约 67%】石心魔像/洞窟领主凝「石甲」后，真实命中会被扣减 40%（`attackMove` 的 `×0.6`，并消耗 1 层），但战斗指令栏 `[1]攻击 ≈N伤` 与技能菜单 `≈N伤` 的**伤害预览完全没算这一层**——盾挂着时玩家看到的期望比实际高出约 67%，常误判「再一刀就能破盾收掉」，实际却差出半刀，还会误选胜率更低的技能。现在预览与结算逐字同源：敌方石甲 >0 时，`[1]攻击` 与技能期望一律按 `Math.max(1, round(N×0.6))` 还原（同 `attackMove`：先按完整期望取整、再扣盾、保底 1），盾一消预览立刻弹回原值。
- 【与结算逐字同源】`rules.js` 新增纯函数 `withShield(dmg, enemy)`，`atkEstimate`/`skillEstimate` 末尾套用——与 `battle.attackMove` 的 `Math.max(1, Math.round(dmg * 0.6))` 同式同序；治疗技能不走 `attackMove`、不受影响（治愈预览仍只算 HP 上限比例）；陨石术「碎甲」同样先扣盾再碎（与真实结算同构：伤害先 ×0.6、层数随后清零）。绝无第二套口径。
- 【零行为风险】仅改 `js/rules.js`（+1 个纯函数、两个返回值套用）；**未动任何真实伤害/命中/石甲层数与结算、掉落、难度、成就、存档逻辑**；`atkEstimate`/`skillEstimate` 只被战斗画面预览（`js/view/drawBattle.js` 指令栏与技能菜单）与测试引用，无盾敌人（普通怪）期望值与既有公式（v4.3/v2.17）完全一致、零回归。
- 验证：`node --check js/rules.js` 与 `npm run check` 全部通过；`npm test` **43/43**；新增 v7.0 专项冒烟（/tmp/jrpg_smoke_v700_shieldprev.mjs）**18 项断言全过**——无盾回归原公式、盾>0 精确 ×0.6 取整保底、shield=0/undefined 不减伤、低伤害保底 1、蓄力×1.5 先乘后扣盾、技能弱电/陨石碎甲同构、治愈不受盾影响、drawBattle 指令栏端到端逐字 `≈7伤`（带盾）、连续重绘 hp/mp/石甲/scene/回合零改动（纯显示收敛）、普通怪预览不回归；`/tmp/jrpg_smoke_esm.mjs` 全量 ESM 冒烟（ALL ESM SMOKE CHECKS PASSED）保持通过。README 战斗说明已同步。

## v6.9 敌人图鉴「魔物强度」数值（体验打磨·信息透明）
- 【战败提示叫人看图鉴强度，图鉴却没有强度数值】阵亡画面一直写着「用图鉴(B)查看魔物强度后再战」，但图鉴「击败可得」行只有经验/金币——这怪有多少血、攻防多高，只能挨打试错才知道。现在**已讨伐行的报酬参考行尾部补齐该魔物在当前玩家等级下的强度参考**：`→ 击败可得：经验 41 · 金币 31 · HP41 攻15 防7`——一眼判断「以我现在的等级，打下它要挨几下、值不值得绕路去刷」，与 v6.3 敌方招数一览、v5.2 战利品预览同一套「信息透明」体系。
- 【与遇敌属性逐字同源】新增纯函数 `codexStats(name, level)`（`js/rules.js`，与 `monReward` 同文件同构）：普通魔物＝`MON_BASE` 线性成长式（`hp[0]+lv×hp[1]` 等，与 `battle.js` 的 `scaleEnemy` 逐字同源）；石心魔像＝精英公式 `58+lv×10 / 12+lv×3 / 15+lv×2`（与 `eliteEncounter` 逐字同式）；三 Boss＝直接读取 `BOSS/CAVE_BOSS/TRUE_BOSS` 对象本身（幽冥魔王 140/15/6、洞窟领主 110/16/10、终焉之神 260/22/13，真身名如「终焉之神·祸乱形态」同样正确映射）。**绝无第二套口径**；与报酬参考同口径取基准值（不含 village×0.9 / cave×1.2 地图倍率与困难模式 ×1.35/×1.15/×1.12 缩放）。
- 【不剧透】只有**已讨伐行**显示强度；未讨伐的灰色 `❓ ？？？ · 未讨伐` 占位依旧不显示任何数值——延续图鉴灰色占位、旅行「未探索」同一原则；未知名称/越界等级（`level=0`、负数）一律钳制安全（等级下限 Lv.1），不闪 NaN。
- 【零行为风险】仅改 `js/rules.js`（+1 个纯函数）与 `js/view/menus.js` 的 `drawCodex()` 报酬行一处（追加显示串，未动行距/坐标/任何奖励与判定）；**未动任何遇敌、伤害公式、掉落、经验/金币、难度缩放、成就或存档逻辑**。
- 验证：`node --check` 与 `npm run check`（23 模块）全部通过；`npm test` **43/43**（新增 8 项 codexStats 数据契约断言）；新增 v6.9 专项冒烟（/tmp/jrpg_smoke_v690_codex.mjs）**14 项断言全过**——drawCodex 端到端（史莱姆/石心魔像/幽冥魔王行强度串逐字在位、未讨伐野狼行不剧透、报酬行保留、强度串行数与讨伐行数一致、文本宽度不越 124~560 画布区）、纯显示收敛（连续 3 次绘制 hp/gold/xp/level/scene 零改动）、边界契约（level=0/负数钳制、未知返 null、三 Boss 固定值）；`/tmp/jrpg_smoke_esm.mjs` 全量 ESM 冒烟（ALL ESM SMOKE CHECKS PASSED）保持通过。README 图鉴行已同步。

## v6.8 世界画面喷泉回血提示「精确恢复量」（体验打磨·信息透明）
- 【知道能回血，却不知道回多少】面向村口喷泉只显示笼统的「踩上回血」——潮灯镇广场喷泉与雾语林泉水都是 **HP/MP 完全恢复**（`world.js` `onStep` 的 FOUNTAIN 分支 `hero.hp=hero.hpMax; hero.mp=hero.mpMax`），但提示一字不提恢复多少，满状态时踩上去更是白白浪费移动。现在提示补齐**精确缺口**：`踩上回血 · HP+15 MP+30`（残血多少一眼看清，决定绕路回村还是继续赶路）；满状态如实提示 `踩上回血 · 状态已满`，不再无谓踩过去。
- 【与恢复逻辑逐字同源】数值即 `hpMax-hp` / `mpMax-mp` 的 `Math.max(0,…)` 钳制，与 `world.js` 喷泉「完全恢复」逐字同源，绝无第二套口径；`S.G` 缺失等边界安全降级为 `0`，不抛错、不显负数。
- 【纯显示零风险】仅改 `js/view/drawWorld.js` 的 `faceHint()` 一处（FOUNTAIN 分支文案，标签宽度按 `measureText` 自动伸缩、位置不变）；**未动任何回血判定 / 交互逻辑 / 地图 / 遇敌 / 数值**；村庄 → 雾语林两处喷泉共用同一来源，行为一致。
- 验证：`node --check js/view/drawWorld.js` 与 `npm run check`（23 模块）全部通过；`npm test` **36/36**；新增 v6.8 专项冒烟（/tmp/jrpg_smoke_v680_fountain.mjs）**12 项断言全过**——满状态提示「状态已满」、缺口 `HP+15 MP+30` 逐字正确（与全恢复口径一致）、HP 满/MP 空显示 `HP+0 MP+满值` 且无负数、背对/普通格无回血提示、连续 3 次绘制 hp/mp/gold/xp/level/scene/curMap 零改动（纯显示收敛）、面向喷泉/普通地面两次渲染不抛错。README 世界交互一节已同步。

## v6.7 防御态指示补全「回MP / 反击」完整效果（体验打磨·信息透明）
- 【只有一半效果看得见，另一半要靠试错猜】`5` 防御的完整设计是「本回合减伤一半 + 回 2 MP + 被命中 50% 几率趁隙反击」，但战斗画面上防御态角标只写 `防御中 · 减伤50%`——回蓝与反击这两条每次防御都在生效、却完全不透明：空蓝防御想靠它回蓝的玩家不知道自己每回合回 2 MP，也不知道被打时有一半机会白赚一下反击。现在角标补全成 `防御中 · 减伤50% · 回2MP/回合 · 50%几率反击`——一眼看清防御「不只是挨打」。
- 【与结算逐字同源】数值直接套用 `battle.js` 自身结算：回蓝 `doDefend` 的 `mp+2`（钳制 MP 上限）、反击 `enemyAct` 攻击分支 `hero.defending && Math.random() < 0.5` 的 50% 判定——绝无第二套说法；纯显示、只读不改任何防御/反击/回蓝判定、伤害公式或难度数值。
- 【零行为风险】仅改 `js/view/drawBattle.js` 一处字符串（坐标 `(308,359)` 不变、不新增行、`bold 12px` 实测宽度约 250px、右缘 308+250<640 不触碰右侧状态角标列 y≥112 与底部战斗记录 y≥448）；**未动任何防御结算 / 反击实现 / MP 回复 / BOSS 或普通怪任何分支**；README 既有「5 防御（…回 2 MP，被命中 50% 几率趁隙反击）」描述即真实行为，无需改动。
- 验证：`node --check js/view/drawBattle.js` 与 `npm run check`（23 模块）全部通过；`npm test` **36/36**；新增 v6.7 专项冒烟（/tmp/jrpg_smoke_v670_defend.mjs）**8 项断言全过**——防御态角标逐字包含 `回2MP/回合` 与 `50%几率反击`、非防御态不绘制该行、回 MP 数值与 `doDefend` 逐字同源（钳制上限）、连续 3 次重绘 hp/mp/gold/xp/level/敌方HP/defending 零改动（纯显示收敛）、普通战与 Boss 战渲染无异常。

## v6.6 状态界面经验行「距升级还差 N 经验」精确读数（体验打磨·信息透明）
- 【知道差得近，却不知道差多少】胜利横幅每次打完都会提示「距 Lv.X 升级还需 N 经验」，但常驻的状态界面（`I` 键）经验行只有 `经验 12/20` 加一根进度条——练级途中反复开状态页时，永远只能靠目测进度条猜还差多少。现在经验行补上**精确剩余读数**：`经验 12 / 20 · 距升级还差 8 经验`——一眼算出再打几只怪能升级，数值与胜利横幅同款口径、绝无第二套说法。
- 【纯显示零风险】仅改 `js/view/menus.js` 的 `drawStatus()` 一处（经验行字符串，坐标 y=98 不变、不新增行）；**未动任何升级判定 / xpNext 曲线 / 经验结算 / 进度条宽度 / 其他任何状态行**；剩余值 `Math.max(0, xpNext-xp)` 钳制，旧档 `xpNext` 缺失等边界只显示 `0`、不抛错、不显负数。
- 验证：`node --check` 与 `npm run check`（23 模块）全部通过；`npm test` **36/36**；新增 v6.6 专项冒烟（/tmp/jrpg_smoke_v660.mjs）**10 项断言全过**——`经验 7/20 → 还差 13` 逐字正确、`xp=0 → 还差 20`、`xpNext=0` 边界不抛错且钳制 `0`、攻击/防御/技能等既有行全部在位（回归）、连续 3 次重绘 xp/xpNext/level/hp/mp/gold 零改动（纯显示收敛）。

## v6.5 中毒/灼烧「每回合扣血数值」角标（体验打磨·信息透明，承接 v5.1/v6.3 状态角标列）
- 【知道会掉血，却不知道掉多少】Battle 里中毒角标只写「☠️ 中毒 3 回合 · 每回合扣血」——毒蛇 35% 施毒 / 火焰斩灼烧 2 回合经常挂身，但每次到底扣多少血全靠体感（低血量还以为能扛，算不明白该不该净化/速战）。灼烧角标更只有「🔥 灼烧 2」连「扣血」字样都没有。现在两处角标补上**精确回合扣血数值**：`☠️ 中毒 3 回合 · 每回合 -6血`、`🔥 灼烧 2 · 每回合 -10血`——一眼算清持续时间内的总失血，决定先净化/防御还是抢输出。
- 【与结算逐字同源】数值直接套用各自结算公式，绝无第二套说法：中毒 = `max(2, round(hpMax×0.05))`（与 `battle.applyPoisonTick` 逐字同式），灼烧 = `max(2, round(hpMax×0.04))`（与 `battle.enemyAct` 灼烧分支逐字同式）；回合计数随 `hero.poison`/`enemy.burn` 实时递减，层数/剩余回合照旧同步。
- 【零行为风险】仅改 `js/view/drawBattle.js` 一处两个字符串（+3 行注释与数值拼接，坐标 `(60,376)` 我方中毒行 / `(620,128)` 敌方灼烧行均不变）；**未动中毒/灼烧判定、伤害公式、回合递减、任何结算逻辑**；数值区右对齐右缘、不与 v5.4 敌方精确 HP / v6.3 敌方招数一览（y162）/ 石甲行（y112）/ 冻结行（y144）重叠；技能菜单覆盖、Boss/试炼各分支不叠加。
- 验证：`node --check js/view/drawBattle.js` 与 `npm run check`（23 模块）全部通过；`npm test` **36/36**；新增 v6.5 专项冒烟（/tmp/jrpg_smoke_v650_dot.mjs）**13 项断言全过**——无中毒/无灼烧不绘制、`hpMax=120→-6血`、`hpMax=260→-10血`（与公式逐字一致）、`hpMax=20/30` 保底钳制 `-2血`、回合与层数显示、四类强敌 中毒+灼烧共存绘制不抛错、连续 2 次重绘 hp/mp/gold/xp/level/敌方HP/burn/poison 零改动（纯显示收敛）；回归 v5.1 冻结角标 **8/8**（含灼烧同列断言）与 v6.3 敌招 **16/16**。README 战斗说明已同步。

## v6.4 HUD 常驻静音状态指示（体验打磨·信息透明）
- 【按了 M 却不知道静没静成】按 `M` 静音/开声只弹一条短暂的 boxMsg 提示（1.2s 后消失），之后画面上没有任何常驻标识能看出当前到底静没静音——常玩的玩家往往要「听一下有没有声」才能确认。现在 HUD 底部状态行新增**常驻静音指示**：开声 `🔊`、静音 `🔇`，与 💰/🍖/🍄/💾 同一行、带 `按 M 静音/开声` 悬浮提示，读档/切换场景后依旧如实反映当前状态。
- 【与真实状态同源】指示直接读取 `S.SND`（音频模块唯一开关，`resumeBgm/stopBgm` 同源），绝对没有第二套说法；按 `M` 切换时立即 `renderHUD()` 刷新——不依赖任何场景切换或下一次 HUD 重绘时机。
- 【零行为风险】仅改 `js/view/hud.js` 的 `renderHUD()`（+1 行）与 `js/main.js` 的 `M` 分支（+1 行 renderHUD 调用）+ `index.html` 壳里加一个 `<b id="s-snd">` 静态节点（纯 DOM）；**未动任何音频逻辑 / 音效 / BGM / 存档 / 场景 / 战斗 / 数值**；`boxMsg` 反馈与现有行为完全不变。`elId` 已在 Node 冒烟中安全降级，纯显示收敛。
- 验证：`node --check js/main.js js/view/hud.js` 与 `npm run check`（23 模块）全部通过；`npm test` **36/36**（新增「静音指示 开=🔊 关=🔇」断言，DOM 桩下 `renderHUD` 随 `S.SND` 切换输出对联）；`node -e import('./js/main.js')` 启动引导零报错。README 控制键表 M 行已同步。

## v6.3 敌方招数一览（体验打磨·信息透明，承接 v5.4/v5.6/v5.7）
- 【知道它会掉多少血、会变身，却不知道它「会怎么打」】战斗画面此前只透明了数值（伤害预判、精确血条、变身线），但 Boss/精英的招数全是黑的：幽冥魔王会回血、石心魔像会凝石甲、变身后重击大幅强化——全靠吃了亏才记住。现在**会使用多招的强敌**（Boss/精英）在战斗画面右上角常驻**敌方招数一览**：`敌方招数：普攻 / 重击 / 回血·血<40%时`；石甲带触发线 `石甲·血<50%时` 与层数上限 `·至多3层`；变身后追加 `·真身后强`；治愈被祸乱气场封印时标注 `·⛔封印`。一眼看出该压血还是防重击。
- 【与敌方行动表逐字同源】直接读取 `battle.pickAct/enemyAct` 用的同一份 `enemy.acts` 数据，`hpBelow`/`maxShield`/`w2`/`forbid` 全部来自行动定义本身，绝无第二套说法；只列招数与触发线，**不剧透出招权重**（守住策略深度）。只会普攻的普通魔物无 `acts`，不显示，避免噪音。
- 【零行为风险】仅改 `js/view/drawBattle.js` 一处（+15 行，在敌方格斗状态角标列下延 y=162 追加，右缘右对齐、不与血条/精灵/预判行/日志重叠）；**未动任何敌方行动判定 / 伤害公式 / 变身 / 掉落 / 数值**；该行只读不改任何游戏状态。
- 验证：`node --check js/view/drawBattle.js` 与 `npm run check`（23 模块）全部通过；`npm test` **35/35**；新增 v6.3 专项冒烟（/tmp/jrpg_smoke_v630.mjs）**16 项断言全过**——普通史莱姆不显示敌招行、幽冥魔王显示 `普攻/重击/回血·血<40%时` 且未变身不加 `真身后强`、石心魔像 `石甲·血<50%时·至多3层`、终焉之神真身 `重击·真身后强` 与封印态 `回血·血<40%时·⛔封印`、未变身/未封印不加标注、洞窟领主石甲同列共存、同一敌人连续 10 次重绘战斗状态零改动（纯显示收敛）。README 战斗说明已同步。

## v6.2 NPC 可交互顶标（体验打磨·信息透明）
- 【知道下一步该做什么，却常忘了该找谁】灯长/星砂车夫是唯二有「动态对话」的角色——可接委托、可交任务都藏在对话里，不搭话永远不知道。现在这两名 NPC 在世界画面上**头顶常驻脉冲标**：灯长「❕ 可接委托」（支线三株荧光可接时）/「❕ 可交任务」（集齐 3 株魔法蘑菇后可交付）；星砂车夫「❕ 可交任务」（击败洞窟领主、矿车支线激活后）。纯闲聊的镇民/巡灯人/井巫/雾径猎手不显示顶标，不喧宾夺主。
- 【与任务日志逐字同源】判定直接复用 `questStatus()`——与 `J` 任务日志 / 世界横幅 / 对话页共用同一状态机。新增纯函数 `npcQuestMark(hero, npcId)`（放 `js/quests.js` 任务模块），顶标出现/消失与日志「可接/可交付」永远同步，绝无第二套说法；顶标自身只与任务状态相关，读档/交任务后即时刷新。
- 【零行为风险】仅改 `js/quests.js`（+1 个纯函数 + 常规 export）与 `js/view/drawWorld.js` 的 NPC 绘制闭包（在 `drawNpcSprite` 后追加两帧脉冲顶标：橙色圆角标 + 白字，脉冲由 `Date.now()/320` 驱动、纯显示）；**未动任何任务状态机 / 对话流程 / 存档 / 数值 / 移动遇敌 / NPC 坐标**；顶标随 NPC 精灵参与纵向遮挡排序，位置在头顶 `y*T-20` 起、不与 `⏎ 对话` 上下文提示冲突。
- 验证：`node --check js/quests.js js/view/drawWorld.js` 与 `npm run check`（23 模块）全部通过；`npm test` **35/35**；新增 v6.2 专项冒烟（/tmp/jrpg_smoke_v620.mjs）**18 项断言全过**——`npcQuestMark` 纯函数 11 项（开局可接 / 进行中无标 / 可交付 / 已完成无标 / 矿车激活可交 / 未解锁无标 / 完成后无标 / null hero 边界逐字断言），`drawWorld` 端到端 7 项（开局村庄灯长「可接委托」上屏、进行中顶标消失、可交付恢复「可交任务」、洞窟车夫顶标上屏、灯长不在本图不显示、连续 3 次绘制 scene/hp/mp/gold/xp/level/encGauge 零改动——纯显示收敛）。README 系统清单已同步。

## v6.1 世界横幅任务「目的地地点提示」（体验打磨·信息透明，承接 v5.5 📍 / v5.9 传送门去向）
- 【知道要做什么，却常忘了在哪做】左上角任务横幅常驻主线 `🎯 讨伐幽冥魔王` / 支线 `📜 找回魔法蘑菇`，但**去哪**只能开 `J` 任务日志看「地点」行（或靠记忆）。现在横幅每一行对**尚未注明地点的目标**直接补齐 `（地点）`：支线进行中 `找回魔法蘑菇 0/3 株（雾语林宝箱 → 回镇找灯长）`、矿车支线 `把星砂矿车的消息告诉星砂车夫（星井矿脉·星砂车夫）`、星井主线 `深入星井矿脉，击败洞窟领主！（星井矿脉深处）`、终焉水晶主线同理——不离开世界画面一眼看到下一步该去哪。
- 【与任务日志逐字同源】地点字符串直接复用 `questLines().journal` 里 `QUESTS.where` 字段（任务日志 J 的同一条数据），绝无第二套说法；横幅宽度按实际最大行重新测算（含地点后自动加宽，仍封顶 500px）。
- 【智能去重】做包含判断前先归一化（剔除「的」与标点）：目标文案**已含**地点名时不再叠加——如主线开头 `前往雾语林深处的祭坛…` 已含「雾语林深处祭坛」就不重复标 `（雾语林深处祭坛）`；全部主线完成后无 active 主线时也不硬凑地点。
- 【零行为风险】仅改 `js/view/drawWorld.js` 的 `drawWorldBanner()` 一处（纯显示，未动任何任务状态机 / 日志 / 存档 / 数值 / 移动遇敌）；`questLines` 内部逻辑与返回结构完全不变。
- 验证：`node --check js/view/drawWorld.js` 与 `npm run check`（23 模块）全部通过；`npm test` **35/35**；新增 v6.1 专项冒烟（/tmp/jrpg_smoke_v610.mjs）**13 项断言全过**——全新开局主线不重复叠加地点、支线 offer/active 行均带地点、星井主线补「深处」、终焉水晶主线带 `（星井矿脉·终焉水晶）`、矿车支线带 `（星井矿脉·星砂车夫）`、双支线同时可见、全部主线完成后不追加、连续 3 次渲染状态零改动（纯显示收敛）、ending 场景渲染无异常。README 任务日志一行已同步。

## v6.0 战斗画面我方精确 HP/MP 数值（体验打磨·信息透明，承接 v5.4）
- 【敌方有了，我方却没有】v5.4 起敌方血条右侧常驻精确数值（`130/260`），但玩家自己的 HP/MP 只有血条内 9px 小字，敌我信息密度不对等；且 Boss/试炼战里「敌方攻击预判」行的 `我方HP x/y` 不显示，Boss 战打到残血时反而最需要一眼看清新血量——现在血条内小字**之外**，玩家侧血条右侧常驻**同款加粗精确数值**：`123/180`（HP 红）与 `45/90`（MP 蓝），与血条同色、`Math.max(0,…)` 钳制（掉血瞬间不显示负数），Boss / 试炼 / 普通怪战一律可见。
- 【纯显示零风险】仅改 `js/view/drawBattle.js` 的 `drawBattle()` 一处（+2 行数值 + 把防御盾标整体右移 50px 让位：盾图标 `translate(238→288,352)`、`防御中 · 减伤50%` 文案 `258→308,359`）；**未动任何伤害/血条/结算逻辑**，与 v5.4 敌方实现逐字同构（同为 `血条同色 + Math.max 钳制 + 右侧常驻`）。坐标核对：数值区 x216~265 与右移后的盾标 x281+ 无重叠、与 `蓄力中`(y336)/`中毒`(y376)/技能菜单覆盖层互不干扰。另给 `js/view/canvas.js` 的 Node 冒烟 stub 补一个 `quadraticCurveTo` noop（防御盾绘制路径用到，纯测试基建，浏览器行为零变化）。
- 验证：`node --check js/view/drawBattle.js js/view/canvas.js` 与 `npm run check`（23 模块）全部通过；`npm test` **35/35**；新增 v6.0 专项冒烟（/tmp/jrpg_smoke_v600.mjs）**10 项断言全过**——普通战/Boss 战我方 HP、MP 数值逐字正确（`123/180`、`45/90`，同款加粗 12px）、半血实时刷新、HP=0 钳制不现负数、防御态盾标右移至 x=308 且数值不被遮挡、数值区与盾标区无重叠（216<288）、15 次重绘 hp/mp/gold/xp/level/敌方HP/日志/phased 零改动（纯显示收敛）。README 战斗说明已同步。

## v5.9 传送门/出口「目的地提示」（体验打磨·信息透明）
- 【提示了却没说去哪】世界地图上被面向可交互对象上下文提示覆盖到的传送门/出口，只显示笼统的「踩上通行」——站在门前根本不知道这扇门通往哪张图，只能踩上去看效果。现在提示升级为**带目的地地图名**：`踩上通行 → 雾语林`（潮灯镇正门）、`踩上通行 → 星井矿脉`（雾语林右下裂洞）、`踩上通行 → 潮灯镇`（雾语林出口）、`踩上通行 → 雾语林`（星井矿脉出口），能踩上去前先看清去向。
- 【与真实传送逐字同源】新增纯函数 `portalDest(curMap, tile)` 放到 `js/world.js`（传送逻辑所在模块），返回值与 `useGate`/`useExit` 的 `transition()` 目标完全一致（村镇门→dungeon、裂洞→cave、EXIT dungeon→village / 其余→dungeon），视图层只认这一个来源，提示与真实行为永不分叉；不存在的传送/非门图块返回 `null` 自动回退为通用「踩上通行」。
- 【零行为风险】仅改 `js/world.js`（+1 个纯函数 + export）与 `js/view/drawWorld.js` 的 `faceHint()` 两处分支（GATE/EXIT 的提示文案）；**未动 `useGate`/`useExit`/`transition`/任何传送判定或数值**。击败魔王后村镇门「雾退了…」的既有锁定行为不变（该场景提示整体隐藏，回归有断言）。
- 验证：`node --check js/world.js js/view/drawWorld.js` 与 `npm run check`（23 模块）全部通过；`npm test` **35/35**；新增 v5.9 专项冒烟（/tmp/jrpg_smoke_v590_portal.mjs）**21 项断言全过**——`portalDest` 四种真实路径 + 边界 null 共 7 项、目的地中文名 3 项、三张地图真实传送点名 4 项、端到端面向门/出口截获绘制文案 4 项（含面向格确为门）、面向普通地形不画「踩上通行」回归、击败魔王后村镇门提示消失且渲染无异常。README 已同步。

## v5.8 Boss 战逃跑「行动保留」行为修复（战斗回归 v2.2/v1.37 既定设计）
- 【回归修复】README 战斗说明与操作说明明确写着 Boss 战「被气场压制，指令置灰 ⛔ 且不耗回合」，v2.2/v1.37 早已文档化「本回合行动保留、battleBusy 立即释放、不调度 enemyAct」——但近期重构后的 `doFlee()` Boss 分支误调 `afterPlayer()`，把「按 4」变成了「白送一回合给 Boss 白打」：被拒后 600ms 敌方仍会抢先手反击扣血，纯陷阱反馈，与指令栏 ⛔「别按 4」的提示自相矛盾（实测：按 4 后 battleTurn+1、battleBusy 锁定、敌方行动被调度）。
- 【行为恢复】现在四类 Boss（幽冥魔王/洞窟领主/终焉之神/试炼连战）按 4：日志「⚠️ XX 的气场压制着你，无法逃脱！（本回合行动保留）」→ `battleBusy` **立即释放**、**不调度敌方行动**、等待后玩家 HP **零变化**，可马上改出 攻击/技能/防御/蓄力 等指令；指令栏 `[4]逃跑⛔` 叠标与「Boss无法逃跑」提示完全不变。
- 【零波及】仅改 `js/battle.js` 的 `doFlee()` Boss 分支一处（删 `afterPlayer()`，改同步释放回合 + 返回 true 跳过重复绘制）；**普通怪/精英「石心魔像」60% 逃跑成功率与失败扣回合逻辑完全不变**，未动任何怪物属性 / 掉落 / 经验金币 / 难度缩放 / 升级曲线。
- 验证：`node --check js/battle.js` 与 `npm run check`（23 模块）全部通过；`npm test` **35/35**；新增 v5.8 专项冒烟（/tmp/jrpg_smoke_v580_fleerelease.mjs）**33 项断言全过**——四类 Boss 各 7 项（开场可行动 / 仍处战斗 / 日志文案 / battleBusy 释放 / battleQ 无调度 / 等待 800ms HP 零变化 / 被压制后立即再行动）、普通怪逃跑 30 次采样 17 次成功（≈60% 判定回归）、逃跑失败仍扣回合、精英不入压制分支、Boss/普通怪两档战斗渲染无异常。README 第 35 行与帮助页「Boss无法逃跑」描述即恢复后的行为，无需改动。

## v5.7 二段变身线精确血量阈值（体验打磨·信息透明）
- 【知道会「变身回血」，却不知道还剩多少血触发 → 一眼看清斩杀/压血时机】三个 Boss（幽冥魔王/洞窟领主/终焉之神·祸乱形态）血量过半会现出真身并回血 10~15%、攻防暴涨——但变身线上只有一句「二段变身线」，玩家只能靠体感「血条过一半」去赌触发点。现在变身线标签补上**精确阈值**：`二段变身线 · HP<70`——一眼知道要压到剩 70 血前抢输出、还是留大招等变身。
- 【与结算逐字同源】数值即 `battle.enemyAct` 的变身判定 `hp < hpMax×(phase.at||0.5)` 取 `Math.ceil`，与困难模式 ×1.35 后的 hpMax 实时联动（困难幽冥魔王显示 `HP<95`）；变身（`phased`）后标签自动消失、只剩暗色细线，与既有行为一致。
- 【纯显示零风险】仅改 `js/view/drawBattle.js` 的 `drawBattle()` 一处（+7 行，替换原「二段变身线」字符串）；**未动变身判定/回血/重命名/攻防加成/任何结算逻辑**，位置仍紧贴血条中线（x≈326~390），不与 v5.4 精确 HP 读数（x≥438）重叠；普通怪/试炼 Boss/技能菜单开合各分支不叠加、不抛错。
- 验证：`node --check` 与 `npm run check`（23 模块）全部通过；`npm test` **35/35**；新增 v5.7 专项冒烟（/tmp/jrpg_smoke_v570_phasehp.mjs）**14 项断言全过**——幽冥魔王 HP<70 / 洞窟领主 HP<55 / 终焉之神 HP<130 逐字正确、困难模式联动 HP<95、变身(phased)后标签消失、普通怪无标签、15 次重绘 hp/hpMax/phased/name/blog 零改动（纯显示收敛）、v5.4 精确 HP 读数共存、HP=129 触发变身判定而 HP=130 不触发（阈界核对）。README 战斗说明已同步。

## v5.6 商店药水持有量标注与购买上限提示（体验打磨·信息透明）
- 【买药总怕重蹈覆辙】商店药水项此前只写价格与恢复量，自己背包里还有几瓶全靠记性；买满 99 上限时被拒也只得到笼统的「金币不足或已满」，分不清是没钱还是没地方装。现在药水行末尾常驻**持有量标注**：`🍖 生命药水 ×1（恢复 50%HP +8）[现有N/99]`——与蘑菇行 `[现N]` 同一套「信息透明」模式，一眼知道还差几瓶到上限、值不值得再囤。
- 【上限反馈分叉】购买被拒时文案拆成两种：背包满 → `🎒 背包已满（药水上限 99 瓶），先去用掉一些吧！`；金币不足 → `金币不足`——不再混为一谈。
- 【零行为风险】仅改 `js/shop.js` 一处（`buildShopList` 药水行字符串 + `buyPotion` 判定拆分），未动药水价格/恢复量/上限数值、未动商店布局与键位；`drawShop` 直接渲染 `it.t`，标注即自动上屏。
- 验证：`node --check js/shop.js` 与 `npm run check`（23 模块）全部通过；`npm test` **35/35**；新增 v5.6 专项冒烟 10 项断言全过（3 瓶/[现有3/99] 标注、恢复量文保留、价格不变、满 99 拒购且不扣金币、背包上限文案、金币不足不消费、正常购买扣 15 金 +1 瓶、0 持有量标注回归）。

## v5.5 快速旅行「当前所在地」标注（体验打磨·信息透明）
- 【一开面板就知道自己站在哪】快速旅行（`T`）此前只显示三个目的地与推荐等级，当前所在地没有任何指示——传送到星井矿脉后想回镇补给，得靠「选中行 ▶ 默认停在当前地图」这一隐性规则猜自己在哪。现在**当前所在地行的地图名右侧常驻绿色 `📍`**（如 `星井矿脉 📍`），一眼确认「我在这、别传错」，传送前不再需要横向对照地图名。
- 【与真实位置全同源】标记读取 `S.curMap` 与 `TRAVEL_LIST` 的 key 逐字比对，随传送/读档/开面板实时刷新，绝无第二套说法；未探索行仍保持「？？？ · 未探索」灰色占位且**不带 📍**（神秘感与「信息透明」并存：只标当前，不剧透未探索地）。
- 【纯显示零风险】仅改 `js/view/menus.js` 的 `drawTravel()` 一处（+2 行，名字串追加 `📍`）；**未动任何传送判定、`visited`/解锁逻辑、`doTravel`、推荐等级提示行、描述行、↑↓/Enter/Esc 键位或数值**；选中 ▶ 高亮、推荐等级（右缘琥珀小字）、描述行全部保持原样。
- 验证：`node --check js/view/menus.js` 与 `npm run check`（23 模块）全部通过；`npm test` **35/35**；新增 v5.5 专项冒烟（/tmp/jrpg_smoke_v550_travelmark.mjs）**19 项断言全过**——潮灯镇/雾语林/星井矿脉三种「当前地」逐一验证仅当前行带 📍、非当前行不带、推荐等级 hint 与描述行在位、仅解锁村庄时未探索行不带 📍、连续 5 次绘制 scene/level/gold/hp/xp/curMap/travelSel 零改动（纯显示收敛）、drawTravel 不改 G 引用；v5.4/v5.1 邻近专项冒烟保持通过（注：/tmp/jrpg_smoke_esm.mjs 为 v2.0 时代基线，断言 `S=globalThis`，与 v4.0 起「导出 S 对象」架构不兼容，属改动前即存在的历史遗留，与本次改动无关；维护中的全量冒烟为 `npm test`）。

## v5.4 战斗画面敌方精确 HP 数值（体验打磨·信息透明）
- 【血条在手心里，数值却在心里】战斗画面上方此前只有一段敌方血条——没人告诉玩家这怪到底还剩多少 HP，Boss 的 260 血与厚血精英全靠体感。现在**血条右侧常驻精确数值** `hp/hpMax`（如 `130/260`），与血条同色同步变化（>50% 绿 / >20% 黄 / 残血红），一眼看清斩杀线。
- 【纯显示零风险】仅改 `js/view/drawBattle.js` 的 `drawBattle()` 一处（+2 行）；**未动任何伤害/血条/结算逻辑**。数值直接取 `enemy.hp/enemy.hpMax`（`Math.max(0,…)` 钳制，击败瞬间不显示负数）；坐标 `ex0+118` 位于血条右侧空隙，不与敌方名/二段变身线/预判行/状态角标列重叠。
- 验证：`node --check` 与 `npm run check`（22 模块）全部通过；`npm test` **35/35**；新增 v5.4 专项冒烟（/tmp/jrpg_smoke_v540_enemyhp.mjs）**7 项断言全过**——普通怪半血/真 Boss 半血/满血/HP=0 钳制/试炼 Boss 各档数值与 `hp/hpMax` 逐字一致、15 次重绘零漂移（纯显示收敛）、真 Boss 绘制不抛错。

## v5.3 防御后伤害预判数值化（体验打磨·信息透明）
- 【「按防能挡多少」终于可以看一眼】战斗画面底部的敌方攻击预判行此前只写`·防御可减半`——知道现在会掉多少血，却不知道按了 `[5]防御` 之后到底受多少。现在预判行补上**防御后的精确伤害**：`敌方攻击预判：-20血（防御后-10血）  我方HP x/y`；已在防御中（敌方行动待发）则直接显示 `格挡中 · 本回合只受 -10血`。
- 【与结算逐字同源】数值与 `battle.js` 的 `enemyAct` 完全同源：受击 = `max(1, cmdDmg(atk, defMax, 1))`，防御中 = 再 `×0.5` 取整（同一公式、同一取整）；⚠️ 致命判断也随防御态切换——非防御看原伤害、防御中看格挡后伤害，不再误报致命。
- 【纯显示零风险】仅改 `js/view/drawBattle.js` 的 `drawBattle()` 一处（+5 行）；**未动 `cmdDmg`/防御减伤/敌方行动/任何结算逻辑**；Boss/试炼战仍不显示该行、技能菜单打开时不叠加、无敌人不绘制。
- 验证：`node --check` 与 `npm run check` 全部通过；`npm test` **35/35**；新增 v5.3 专项冒烟（/tmp/jrpg_smoke_v530_guard.mjs）**12 项断言全过**——非防御/防御中双态文案、数值与公式精确一致、致命随防御态切换、Boss 战不绘制、12 次绘制 hp/mp/gold/xp/等级/敌方HP 零改动（纯显示收敛）。README 战斗说明已同步。

## v5.2 战利品预览补齐确定性奖励（体验打磨·信息透明）
- 【标注的奖励和实际到手对不上】战斗画面「战利品预览：经验 +X 金币 +Y」此前只显示基础经验/金币，但胜利结算里**必然**追加的奖励没有体现：精英（石心魔像 / 洞窟领主 / 终焉之神，`winBattle` 中 `isElite` 分支）胜利后必捡 1 株魔法蘑菇；终焉之神（`isTrue`）在基础金币之外还另有 +300 金币。玩家只能凭图鉴备注或吃一堑长一智，预览行在前误导。
- 【与结算同源补全】预览行现在按敌人旗标追加：精英 → `· 🍄 必掉蘑菇`；终焉之神 → `· 战胜另+300金`。数值不硬编码，全部沿用敌人对象既有字段（isElite / isTrue），与 `winBattle` 判定逐字同源。
- 【纯显示零风险】仅改 `js/view/drawBattle.js` 的 `drawBattle()` 战利品预览一行（+2 行逻辑）；**未动掉落/经验/金币/胜利结算任何逻辑**，普通怪不带注记、试炼战（isRush 无 isElite）不受影响、技能菜单覆盖层不叠加。
- 验证：`node --check` 通过；`npm test` **35/35**；v5.1 冻结专项 **8/8**；v4.4/v4.6 战斗绘制专项 **15/15、13/13**；新增 v5.2 专项冒烟（/tmp/jrpg_smoke_v520_rewards.mjs）**15 项断言全过**——普通怪无注记 / 精英必掉蘑菇 / 洞窟领主必掉蘑菇 / 终焉之神蘑菇+另300金 / 12 次重绘 hp·mp·gold·xp·level·蘑菇·敌方HP·日志零改动（纯显示收敛）/ `startBattle→drawBattle` 真实路径回归。

## v5.1 敌方冻结状态角标（体验打磨·信息透明）
- 【冻没冻住只能等敌方行动看记录 → 一眼即知】冰霜击触发冻结后 `enemy.skipNext=true`，但战斗画面此前没有任何常驻指示，玩家只能等敌方行动时从战斗记录里确认。现在敌方状态角标列（右上方石甲/灼烧同列）新增 `❄️ 冻结 · 下回合无法行动`，紧接灼烧下行（y=144），下回合敌方行动被跳过时自动消失（`skipNext` 复位）。
- 【纯显示零风险】仅改 `js/view/drawBattle.js` 的 `drawBattle()` 一处（新增 2 行）；**未动冻结判定/伤害/敌方行动/任何结算逻辑**（skipNext 仍由敌方行动回合消耗）。无冻结时不绘制、技能菜单覆盖层不叠加、HP/MP/经验/金币/回合各读值不动。
- 验证：`npm run check` 全部通过；`npm test` **35/35**；v5.1 专项冒烟 8 项断言全过（未冻结不绘制 / skipNext 置位后绘制 / 右对齐同列 x=620 / 居于灼烧下行不重叠 / skipNext 复位后消失 / Boss·洞窟领主·终焉之神·精英冻结绘制无异常 / 技能菜单打开叠加无异常 / 冰霜击冻结数据回归）。

## v5.0 潮灯记（剧情重写）
- 【全新世界观】不再是「被选中的勇者去讨伐魔王」。潮灯镇靠记忆之灯活着；灯灭、雾起、昨天开始消失。你是守灯人，要去雾语林讨回被【幽冥魔王】吞走的灯芯——而他其实是把黎明封进圣光之剑的旧灯卫。星井矿脉的洞窟领主守着最后一车星砂；终焉之神是初灯的意志，想熄灭一切记忆以免再失去。击败魔王后是半亮的黎明；击败终焉之神后进入真结局。
- 【文案覆盖】地图更名为潮灯镇 / 雾语林 / 星井矿脉；NPC 为灯长 / 镇民 / 巡灯人 / 井巫 / 雾径猎手 / 星砂车夫；姓名 余烬 / 灯见 / 潮。Boss 内部名、掉落、旗标、存档 schema、地图 ASCII **未改**（幽冥魔王 / 洞窟领主 / 终焉之神 / 圣光之剑）。
- 范围：`data.js` 剧情与任务对白、`world.js`/`battle.js`/`core.js`/`shop.js` 提示、`view/menus.js` 标题与双结局、`index.html`。顺手让尾声画面在渲染循环里常驻（`SCREENS.ending = drawEnding`），真 Boss 胜利直接进入真结局。
- 验证：`npm run check`；`npm test`。

## v4.8 任务日志（系统·信息透明）
- 【定义与显示脱节】`data.js` 里已有 `QUESTS`，但横幅/村长对话/奖励仍各写一套文案。现在日志、左上角横幅、村长/矿车夫对白与发奖**全部从 `QUESTS` 推导**：阶段 `offer/active/turnin/done/locked`，采集进度 `n/3` 与奖励公式同源。
- 【`J` 任务日志】世界地图按 **J** 打开一览：主线（魔王→洞窟领主；双胜后显形终焉之神）与支线（村长委托、矿车之约）分列，标注可接/进行中/可交付/已完成/未解锁，进行中附地点。未接村长委托开局即显示「可接」，不用猜该找谁。横幅可同时列出多条支线（不再只显示第一条）。发奖数值未改（村长 40+Lv×10 金 +2 药水，矿车夫 +80 金）。
- 范围：`quests.js`（journal / `npcQuestPages` / `applyQuestReward`）、`data.js`（QUESTS 补 name/where/talk/reward）、`core.js` 对话走表、`view/menus.js` 的 `drawJournal`、`main.js` 按键、横幅；**未动地图/战斗/掉落/存档 schema**（仍兼容 `G.quest` 迁移）。
- 验证：`npm run check`；`npm test`。

## v4.9 世界 HUD 昼夜时段标签（体验打磨·信息透明）
- 【时间在走却无处可看】游戏内置实时昼夜系统（`render()` 每帧累加 `S.G.time`，`drawWorld` 随昼/昏/夜/黎明四档换色）——但探索时只能靠画面明暗「体感」，没有任何文字读数。现在 HUD 左上角地图名旁常驻**昼夜标签**：`勇者 · 和平村 · ☀️ 白天 / 🌆 黄昏 / 🌙 夜晚 / 🌅 黎明`，每 90 秒一档、实时刷新，与画面着色**同源同判**（直接复用 `drawWorld.js` 导出的 `timeOfDay()`），状态画面里已有的 ⏱️ 时钟可在同一时刻对照验证。
- 【纯显示零风险】仅改 `js/view/hud.js` 的 `renderHUD()` 一处（新增 4 行 + 一个 import；`hud→drawWorld` 为向下单向依赖，不构成环）；**未动时间推进、着色 tint、移动/遇敌/战斗/存档任何逻辑**；困难模式 `⚡` 标记与「角色名 · 地图名」既有格式原样保留，时段段仅追加其后。
- 验证：`npm run check`（22 模块 `node --check`）全部通过；`npm test` **30/30**；新增 v4.9 专项冒烟（/tmp/jrpg_smoke_v490_daytag.mjs）**20 项断言全过**——`timeOfDay` 八组边界精确（0/89/90/150/180/230/358/360）、四时段标签全部写入 `s-map`、名字·地图·时段完整格式与困难 ⚡ 保留、`G=null` 雾化不抛错、`startBattle→renderHUD` 战斗路径回归、drawBattle 绘制无异常。

## v4.7 Puny 像素贴图（美术换装·玩法不动）
- 【程序化色块 → Shade「Puny」CC0 像素图】大地图 16×16 图块 2× 到游戏 32px 格：草地/泥土路/石板/水面/树木来自 Puny World，洞窟岩地/岩壁/宝箱/传送门/祭坛宝石来自 Puny Dungeon。主角用蓝衣战士（龙鳞甲或圣光之剑换红衣），六名 NPC 分用法师/工人/士兵/弓手，战斗史莱姆/兽人/哥布林/骷髅等走角色图集 8 向帧；毒蛇/树精仍走原程序化剪影以免撞脸。贴图加载失败（含 Node 冒烟无 `Image`）自动留在原 Canvas 绘制，**地图 ASCII、碰撞、战斗、存档零改动**。
- 【像素对齐】主画布关闭插值；镜头坐标取整，避免平滑跟随时把 16px 格子拉糊。
- 范围：`assets/puny/`（CC0，见 `ATTRIBUTION.txt`）、新增 `js/view/atlas.js`；`tiles.js` 在图集就绪后替换 `TILE` 缓存；`sprites.js` / `drawWorld.js` 角色与 NPC；`canvas.js` 关平滑。
- 验证：`npm run check`（含 atlas.js）；`npm test` **30/30**（Node 无图走退路，绘制不抛错）。

## v4.6 敌方下一击伤害预判（体验打磨·信息透明）
- 【挨一下疼不疼只能猜 → 一眼预判】战斗画面（玩家拟行动回合）底部常驻一行**敌方攻击预判**：`敌方攻击预判：-N血 · 防御可减半   我方HP x/y`——数值与结算公式（`cmdDmg`）**无浮动同源**，当前防御中则标注「防御减半生效中」；若这口气挨下来会倒地，整行变橙并追加 `⚠️致命`。和 `[1]攻击≈N伤` 互为镜像：看得到自己的输出，也看得到对方的下马威，判断该挥剑、防御还是嗑药不再靠猜。
- 【纯显示零风险】仅改 `js/view/drawBattle.js` 的 `drawBattle()` 一处（新增 9 行，坐标 y≈395 无任何既有元素）；**未动 `cmdDmg`/敌方行动/防御减伤/任何伤害结算逻辑**，普通怪/Boss/试炼逐分支验证；Boss 与试炼战该行不显示（仍走「无法逃脱」警示）、技能菜单打开时不叠加、无敌人防御性跳过。
- 验证：`npm run check`（22 模块 `node --check`）全部通过；`npm test` **30/30**；新增 v4.6 专项冒烟（/tmp/jrpg_smoke_v460_entdmg.mjs）**13 项断言全过**——预判数值与 `cmdDmg` 公式精确一致、致命/防御/Boss/技能菜单各分支绘制不抛错、15 次绘制 hp/mp/gold/xp/level/enemyHp 零改动（纯显示收敛）、攻击行动与敌方行动回合推进正常。README 战斗说明已同步。

## v4.5 成就一览界面（新界面·信息透明）
- 【15 项成果却无一面墙】游戏累计有 15 项成就（初露锋芒→矿车之约），但此前只能靠解锁弹窗被动得知——没解锁的成就长什么样、差多少进度，玩家完全看不到，收藏向玩家无从下手。现在世界地图按 **`C`** 打开「— 成就 —」一览面板：**全部 15 项**常驻列出，✔ 已解锁（绿）/ ✘ 未解锁（灰），每项附描述；**8 项计数成就实时显示进度**（累计讨伐 7/10、额外掉落 3/5、等级 4/5、金币 123/500、图鉴收录 2/5 与 3/11 种），一眼看清距解锁还差多少；每页 10 项，↑↓ 滚动浏览，超出页首/页尾自动钳制。
- 【进度与判定同源】`ACH_LIST` 新增可选 `prog(hero)` 函数返回 `cur/max` 字符串，与既有 `ok(hero)` 判定**逐字同源**（同一批 `hero` 字段——totalWins/drops/level/gold/bestiary），解锁判定 `ok` **完全不动**，成就弹窗/奖励/尾声战绩计数（`ACH_LIST.length`）自动随之显示 15。狂热刷级无副作用：数值只读，纯显示收敛。
- 范围：`data.js`（ACH_LIST 加 `prog` + 帮助页加 `C` 行）、`state.js`（`achScroll`）、`view/menus.js`（新增 `drawAch()`）、`view/index.js`（注册 `ach` 场景）、`main.js`（`ach` 场景按键表 + world 按 `C` 进入 + 教程提示）；**未动任何战斗/掉落/经验/金币/存档/任务逻辑**，图鉴 `B`、状态 `I`、操作说明等邻近界面完整不动（回归验证）。
- 验证：`npm run check`（22 模块 `node --check`）全部通过；`npm test` **30/30**；新增 v4.5 专项冒烟（/tmp/jrpg_smoke_v450_ach.mjs）**21 项断言全过**——15 项成就字段齐全、8 项 prog 数值与 hero 精确一致（3/10、123/500、2/5、全收集 11/11）、`ok` 判定回归、新档/半解锁/超界滚动各档绘制不抛错且 scroll 钳制、20 次绘制 hp/gold/xp/level/ach 零改动（纯显示收敛）、goto('ach') 与 C/Esc/↑↓ 键接线、drawCodex/drawStatus 回归。README 按键表与成就说明同步（成就数 14→15 订正）。

## v4.4 战斗指令栏药水恢复量预览（体验打磨·信息透明）
- 【按 `[3]` 前不知恢复多少】药水是战斗中最常救急的手段，但指令栏只显示数量（`🍖×N 🧪×M`），一次恢复多少只能凭记忆或试按。现在战斗画面指令栏（y=424，指令行上方）在有药水时新增一行**恢复量预览**：`[3]恢复：🍖+68HP  🧪+116HP/+24MP`——数值与 `hero.js` 的 `takePotion` 公式**逐字同源**（🍖=50%HP+8，🧪=80%HP+20 与 40%MP），开战前扫一眼就知道这一口能不能续命、该不该换防御/蓄力。
- 【纯显示零风险】仅改 `js/view/drawBattle.js` 的 `drawBattle()` 命令区一处：`battleBusy`（敌人行动回合）不显示、无药水不显示、只有 🍖 时不带 🧪 段；**未动 `takePotion`/战利品/HP/MP/伤害任何结算逻辑**；Boss 战 `[4]逃跑⛔` 标记与指令栏、普通怪「成功率约60%」标注完全不动（回归验证）。
- 验证：`npm run check`（22 模块 `node --check`）全部通过；`npm test` **30/30**；新增 v4.4 专项冒烟（/tmp/jrpg_smoke_v440_potion.mjs）**15 项断言全过**——双药水/仅 🍖/无药水/battleBusy/普通+Boss 战各场景下预览数值与公式精确一致（68、116+24）、Boss ⛔ 与指令栏回归、多次绘制后 hp/mp/gold/xp/等级/敌方HP 零改动（纯显示收敛）。README 战斗说明同步。

## v4.3 技能菜单标注 MP 消耗（体验打磨·信息透明）
- 【MP 是硬约束，成本却只能靠记】技能菜单（战斗 `[2]`）每行已显示伤害期望与效果，但**唯独不显示各技能的 MP 消耗**——玩家的 MP 上限只有 `12+Lv×4`，火焰斩 4 / 冰霜击 5 / 治愈术 5 / 雷鸣 8 / 陨石术 14，开菜单前能不能放得出想放的技能全凭记忆或试按。现在每行技能右侧**常驻一枚独立 MP 标注**（右对齐、独立列，不挤占技能名/伤害预览/效果提示），与 `SKILL_DATA.mp` 完全同源，正好接在「当前 MP：x/y」下方一目了然。
- 【三色可读】颜色即状态：`MP n` **蓝色=当前 MP 够用**、**红色=不够（该行同时置灰 ⛔）**、**灰色=被终焉之神祸乱形态封印**——开菜单瞬间就知道哪些技能放得出、哪个是差 2 点 MP 临门一脚，不必逐行看清 ⛔ 符号。
- 范围：仅 `js/view/drawBattle.js` 的 `drawSkillMenu()` 一处（加一行 `text()` + 注释）；**未动任何 MP/结算/技能/封印/界面布局逻辑**，`[1]攻击` 预览、`当前 MP` 行、技能行坐标（x=170）与行距（36px）保持原样，MP 列锚定 x=474 右对齐，CJK 撑满也不会与左侧文本重叠（面板右界 490）。
- 验证：`npm run check`（22 模块 `node --check`）全部通过；`npm test` **30/30**；新增 v4.3 专项冒烟（/tmp/jrpg_smoke_v430_mpcost.mjs）**11 项断言全过**——MP 充足时 5 行标注全蓝、数值 4/5/5/8/14 与 `SKILL_DATA` 逐一对上、MP=3 时全红、治愈被封印时该行灰其余蓝、`openSkillMenu` 不抛错、绘制前后 hp/mp/gold/xp 零改动（纯显示收敛）、MP 列右对齐锚定且技能行文本不与 MP 列重叠。

## v4.2 遇敌槽可视化读数（体验打磨·信息透明）
- 【一条细到看不见的红条 → 精确遇敌读数】遭遇槽此前只在右上小地图下方一条 6 像素细红条——什么时候会踩出下一场战斗全凭感觉，刷怪/赶路都缺直观参照。现在小地图遇敌槽**常驻百分比读数**（`遇敌 42%`），数值与 `world.js` 的 `S.encGauge` 完全同源（每踩危险格 +10~18，喷泉/道路 -25/-6，满 100 触发遇敌），一眼知道还差几步遇敌。
- 【≥70% 危险鸣警】遭遇槽达到 **70%** 时读数变橙并追加 `⚠️ 危险逼近`，色带橙/红交替闪烁——延续 威胁预警 / 图鉴标注 / 升级提示 同一套「信息透明」体系，赶路去祭坛前能提前判断该不该补药。
- 范围：仅 `js/view/drawWorld.js` 的 `drawMinimap()` 一处（11 行，纯显示）；**未动任何遇敌判定、遭遇槽增长/衰减公式、地图、碰撞、掉落或战斗逻辑**；`encGauge` 为 0 / undefined / 越界（负数、超 100）均钳制安全渲染。
- 验证：`npm run check`（22 模块 `node --check`）全部通过；`npm test` **30/30**；新增 v4.2 专项冒烟（/tmp/jrpg_smoke_v420_encgauge.mjs）**17 项断言全过**——0/42/69/70/100 各档读数与鸣警阈值精确（69% 不鸣警、70% 鸣警）、色带颜色切换、超界钳制到 100%、`-5/260/undefined` 渲染不抛错、dungeon/cave 渲染无异常、40 次绘制零状态改动（纯显示收敛）。README 遇敌槽一句同步。

## v4.1 可读性（格式、命名、数据坐标、绘制去撞名）
- 【格式】系统层与绘制层补上空格/换行，展开拥挤的 `if`；局部 `G`/`e` 改为 `hero`/`enemy`。
- 【战斗】`playerAction` 拆成动作表（`doAttack` / `doSkill` / `doItem` / `doDefend` / `doCharge` / `doFlee`）；回合队列 `kick`/`clearQ` 改为 `advanceQueue`/`cancelBattleQueue`。
- 【数据】NPC/喷泉/祭坛/酿造坐标与洞窟岩地替换从 `loadMap` 硬编码迁入 `MAPS.*.extras` / `replaceTiles` / `treasure`。
- 【绘制】`view/world.js`、`view/battle.js` 改名为 `drawWorld.js`、`drawBattle.js`，避免与逻辑层同名。
- 【注释】`bind.js` 标明各钩子由谁填充；删掉图块里过时的版本史注释。玩法数值未改。
- 验证：`npm run check`；`npm test` **30/30**。

## v4.0 架构与系统升级（improve-plan 落地）
- 【架构】`globalThis` 注册表改为导出对象 `S`；成就 `ok(g)` 显式传参。系统层通过 `bind.js` 晚绑定绘制，`winBattle` 返回结果并由 `world.applyVictoryWorld` 清祭坛/显形宝藏，拆掉 `battle↔world` 环。
- 【架构】商店/旅馆购买迁出绘制层（`shop.js`）；`goto(scene)` + `main.js` 场景按键表。纯计算进 `rules.js`；绘制拆到 `js/view/`（canvas/tiles/sprites/hud/world/battle/menus）。冒烟收回 `tests/smoke.mjs`。删除单文件备份。
- 【任务】`G.quests` 取代单整数（兼容旧 `G.quest`）。横幅主线/支线分行；任务蘑菇集齐前不可卖/酿。击败洞窟领主后可向矿车夫交任务（+80 金）。
- 【技能】火灼烧 / 冰冻结 / 雷穿防 / 陨石碎甲与克真身 / 治愈解毒。敌人 `weak`/`resist` 进图鉴。蓄力对下一次攻击或技能 ×1.5。
- 【战斗】回合队列；敌人行为表（洞窟领主有二阶段石甲；终焉之神祸乱形态封印治愈）。遭遇槽 0–100 替代危险格 16% 随机。
- 【美术】战斗复用换装 `drawHero`；格子移动 180ms 插值；战斗背景随地图；NPC 外形特征；水/喷泉/洞窟晶尘动画；昼夜绑 `G.time`。
- 验证：`node --check` 全部模块通过；`node tests/smoke.mjs` **28/28 通过**（S 对象、任务迁移、技能字段、洞窟真实胜利流、治疗解毒、绘制不抛错、存档 quests）。

## v3.40 技能伤害预览不再跳变（体验修复·信息透明）
- 【修复】战斗中按 `[2]技能` 打开技能菜单后，攻击技的伤害预览数字**每帧乱跳**（如火焰斩一会 72 一会 65 一会 79）。原因：`drawSkillMenu()` 的预览用 `cmdDmg(G.atkMax, enemy.def, 1, true)` 计算——`alt=true` 分支会掷一次 `Math.random()` 施加 ±10% 浮动，而技能菜单由 30fps 渲染循环逐帧重绘，等于**每帧掷一次骰子画一次数字**。
- 【修复】技能预览改用**期望值口径**（`alt=false`，无随机浮动，与 `[1]攻击` 指令栏的 `atkEstimate` 同一“只显示期望不吃随机”的哲学），逐帧恒定；真实战斗结算路径（`attackMove` 的 `alt=true`）完全不动，出手伤害依旧保留 ±10% 浮动与暴击——预览稳定、打出去照旧带随机，两者互不干扰。
- 【回归保护】治疗技预览（`+N HP`）与 `[1]攻击` 预览本就恒定，未受影响；纯显示改动不碰 MP/HP/伤害结算/蓄力/暴击。
- 改动范围：仅 `ui.js` 的 `drawSkillMenu()` 一处（`alt:true → false` + 注释更新）；未动 `cmdDmg` 函数本体（战斗结算随机路径保留）、`atkEstimate`、任何数值/敌人/技能/结算逻辑。
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）**全部通过**；新增 v3.40 专项冒烟（/tmp/jrpg_smoke_v340_skillprev.mjs）**14 项断言全过**——逐帧抓取 `fillText` 跑 300 帧，火焰斩/冰霜击/雷鸣/陨石术四个攻击技预览**各恒定唯一值**（72/94/115/151 伤，攻23×2-防10×各自倍率，与无浮动期望公式逐一对上）、治愈术预览恒定 48HP、`[1]攻击` 预览恒定 36、300 帧重绘后 hp/mp/gold/敌人 hp 全部不变（纯显示）；v3.1 攻击预览 / v3.39 转向 / v3.29 面前提示 三个邻近专项冒烟全部保持通过。

## v3.39 被挡也能转身（体验修复·交互卡死）
- 【修复】主角朝一个方向按方向键，若目标格是不可走入的 SOLID（NPC、酿造锅、岩石、树、洞窟岩壁…），原 `move()` 会**在更新 `dir` 之前就 `return`**——而 `interact()`（交互）与 `faceHint()`（面前提示）全都按 `dir` 取「面前格」。于是只要迎面被挡住（NPC 本体就是 SOLID 图块），玩家就永远转不过身、无法面向它，**对话/酿造/商店等一切“面对面”交互全部卡死**（典型场景：从侧面走近 NPC 后方向键对着 NPC 按，被挡 → 不转身 → 永远对不上话）。
- 【修复】`move()` 改为**先按按键方向更新 `dir`，再判越界/阻挡**：被挡/贴墙/贴地图边缘时只转身、不位移、不发声、不触发 `onStep`（不会误踩传送门/宝箱/遇敌），转身效果由既有主循环（30fps `render`→`drawWorld`→`drawHero`/`faceHint`）自然呈现。经典 JRPG「贴墙转身」手感 + 信息透明（转身即出「⏎ 对话」提示）。
- 【回归保护】正常行走不受影响；原地转身不改变 `G.x/G.y`、不改任何数值、不进任何场景。
- 改动范围：仅 `world.js` 的 `move()` 一处（4 行语义调整：`dir` 赋值上移，去掉成功后重复赋值）；未动 `SOLID`/`interact`/`faceHint`/`onStep`/任何地图/遇敌/战斗/存档逻辑。
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）**全部通过**；新增 v3.39 专项冒烟（/tmp/jrpg_smoke_v339_turn.mjs）**16 项断言全过**——被村长挡住转身+不位移+纯转身无副作用+转身后 interact 进对话（修复前卡死）、被酿造锅挡住转身+interact 进酿造、贴墙转身、地图边界越界仍转身、正常行走不受影响、面向 NPC 状态 drawWorld 渲染无异常、dir 恒为合法枚举；v3.29 面前提示 / v3.37 矿车夫 / v3.24 隐者 / v3.33 老猎人 四个邻近专项冒烟全部保持通过。

## v3.38 面向 NPC 提示牌并入名字（体验打磨·信息透明）
- 【认出与谁对话】村庄三名 NPC（村长/村民/冒险者）与洞窟/森林的 隐者/老猎人/矿车夫 图块造型完全相同——地图上只有一模一样的脸，谁是村长、谁发支线，新玩家只能挨个凑上去对话才认得出。现在面向任一 NPC 时，v3.29 的上下文提示牌直接标出名字：`⏎ 对话 · 村长 / 村民 / 冒险者 / 隐者 / 老猎人 / 矿车夫`，一眼认出「该找谁领支线、该跟谁打听情报」。
- 【与数据同源·零新机制】名字来自既有 `NPC_SPOTS[坐标]→id` 再查 `NPCS[id].name`（与 `interact()` 打开对话的解析完全同一来源），`NPC_SPOTS` 查不到 id 的孤立 NPC 图块自动回退为原「⏎ 对话」，绝不抛错。复用 v3.29 既有提示牌绘制管线（pill 宽度随文本自适应居中，不遮主角/不遮对象），未新增任何界面/按键/状态。
- 改动范围：仅 `ui.js` 两处——import 增补 `NPC_SPOTS`（data.js 早已导出）+ `faceHint()` NPC 分支一行；未动任何对话内容、NPC 位置、碰撞、交互判定、掉落、经验/金币、数值曲线或剧情。README「面向可交互对象上下文提示」一行同步。
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）**全部通过**；v3.29 提示专项冒烟同步扩展为六名 NPC 名字断言（村长/村民/冒险者/老猎人/隐者/矿车夫）**全部通过**；邻近 NPC 相关专项冒烟 v3.33 老猎人 / v3.37 矿车夫 / v3.36 宝藏矿车 / v3.35 槽位 / v3.24 隐者 / v3.34 洞窟胜利 / v3.31 小地图 **全部保持通过**。

## v3.37 宝藏洞窟「矿车夫」NPC（新 NPC·兑现矿车传说的最后一环）
- 【矿车有主了】v3.36 兑现了击败洞窟领主后矿车宝藏当场显形，但**整座洞窟没有任何人提过这车宝藏**——它是怎么来的、该去哪开，全靠玩家翻胜利台词。现在矿车区正上方（18,11）常驻一位 **矿车夫**：第一页交代设定——「这洞从前是座大铁矿，一车车矿石往洞外拉；后来洞窟领主霸占了矿脉，那年没运走的一车金银财宝，也被碎石埋在了矿车边上」，第二页直接点破玩法——「你若能击败那位洞窟领主，碎石坍落，那车宝藏便会在祭坛左边显形——铁打的买卖。打完别急着走，记得回头把那几箱开了」，与 v3.36 真实机制（MB 祭坛 x20-23 左侧 x18-19 宝藏格）逐字同源——玩家在打洞窟领主之前就知道该去哪收货，矿车传说自此闭环。
- 【角色即图块·零新机制】与 隐者/老猎人 完全同模式：复用既有 NPC 精灵与 SOLID 碰撞（不可走入），复用 `NPC_SPOTS`/`openTalk`/`talkNext` 既有对话管线（2 页，翻完自动回 world），面向时自动获得 v3.29「⏎ 对话」上下文提示——零新增界面/按键/状态。
- 改动范围：`data.js`（`NPCS.cartman` 两页对白 + `NPC_SPOTS['18,11']:'cartman'` 一行）、`world.js`（`loadMap('cave')` 布署矿车夫图块一行）；均在图块布置层，**未动任何地形布局、MB 祭坛、试炼碑、终焉水晶、传送门、宝箱、遇敌判定、掉落、经验/金币、难度缩放或战斗逻辑**——村庄三名 NPC、隐者、老猎人完全不受影响。
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）**全部通过**；新增 v3.37 专项冒烟（/tmp/jrpg_smoke_v337_cartman.mjs）**29 项断言全过**（见该文件头部说明）；邻近 v3.33 老猎人 / v3.24 隐者 / v3.36 宝藏矿车 / v3.31 小地图专项冒烟全部保持通过（注：v3.21 drawStatus 4 例与两个 v1.x 遗留损坏冒烟为改动前即存在的历史遗留，与本次改动无关）。README「三张地图·宝藏洞窟」一行同步。

## v3.36 宝藏矿车兑现（新内容·兑现 v1.x 胜利台词）
- 【兑现自 v1.x 的空头支票】击败洞窟领主时的台词「深处的宝藏矿车向你敞开！」说了很久，但矿车区（洞窟 MB 祭坛 x20-23×y12-13，8 格）被清空成岩地后**什么都没有**——玩家打完迷你 Boss 只得到一句空话。现在矿车区正左方紧邻的 2×2 矿脉格（x18-19×y12-13）在击败洞窟领主的**一瞬间当场显形为可踩踏开启的宝藏宝箱**（普通宝箱奖励：约45%金币 12+Lv×5 / 其余生命药水），胜利台词变为「🏆 洞窟领主被击败！左边的宝藏宝箱缓缓显形了，快去开启！」——名副其实的「宝藏矿车」。
- 【旗标驱动·纯增量】显形完全由 `G.caveBoss` 旗标门控：击败前这 4 格就是普通岩地（踩踏不触发宝箱、正常遇敌判定不受影响）；击败后 `winBattle` 当场调用 `revealCaveTreasure()` 落进 `maze`，此后重进洞窟由 `loadMap` 按旗标再次显形，已开启的照常归入 `G.chests`（绘制回退岩地），不会重复给奖、不会随读档/切图丢失。**未动任何 MB/首领/掉落/经验金币/遇敌/难度/存档逻辑**——洞窟领主打倒后祭坛回退洞窟岩地 TY.CAVE（v3.34 契约）完全不变。
- 【小地图金光扩展】既有「支线蘑菇宝箱未开启金光脉动」的机制从 `G.quest===1` 扩展为 `(G.quest===1||G.caveBoss)`：击败洞窟领主后洞窟里未开启的宝箱（含新显形的 2×2 宝藏）在小地图上金光脉动、一眼找到刚现形的宝藏，开完即熄——同一套信息透明体系。
- 改动范围：`world.js`（新增 `CAVE_TREASURE` 常量 + `revealCaveTreasure()` 并导出、`loadMap` 末尾按旗标调用一行）、`battle.js`（`winBattle` 的 isCaveBoss 分支加一行 `revealCaveTreasure()` + 台词微调）、`ui.js`（`drawMinimap` 金光条件一行）；README「三张地图·宝藏洞窟」一行同步。不新增/改名文件、不改启动方式。
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）**全部通过**；新增 v3.36 专项冒烟（/tmp/jrpg_smoke_v336_treasure.mjs）**28 项断言全过**——击败前 4 格为 CAVE 不显形/踩踏不入 chests/不触发意外战斗、真实胜利流不抛异常且 caveBoss 置位/金币结算照常、击败瞬间 4 格显形为 CHEST、MB 8 格全清除并回退 CAVE（v3.34 契约保持）、踩踏开启入 G.chests 且奖励入账（药水/金币两分支均验）、重复踩踏不重复给奖、重进洞窟再显形且已开保持、caveBoss=0/1 两态 drawWorld 渲染无异常、小地图金光随未开宝箱精确增减（开满 4 箱金光恰减 4 格）、revealCaveTreasure 无旗标时 no-op；v3.34 洞窟胜利 / v3.33 老猎人 / v3.31 小地图 / v3.24 隐者 / v3.20 进度 / v3.35 槽位 全部邻近专项冒烟保持通过，全量 37 份套件中仅 v3.21 的 4 例 drawStatus 与两个 v1.x 遗留损坏冒烟失败（均为改动前即存在的历史遗留，已在原始代码上复现核验，与本次改动无关）。

## v3.35 HUD 常驻存档槽指示（体验打磨·信息透明）
- 【未存先见】Esc 存档前看不到当前槽位——标题页按 1/2/3 选完槽后，游戏中 Esc 会直接覆盖**当前槽**，但界面上没有任何常驻指示，只能靠存档瞬间那条 1.8 秒的「已存档到槽 N」提示确认，误按 Esc 可能把别的档盖掉。现在 HUD 右侧常驻一枚 **💾槽N** 指示（与 `curSaveSlot` 完全同源：标题选槽 2 进游戏即显示槽 2，存完不消失），存档前一眼看清会写进哪个槽；元素带悬停说明「按 Esc 保存到的槽位」。
- 【纯 HUD 显示层】`index.html` 页面壳新增一个 `<b id="s-slot">` 元素 + `js/ui.js` 的 `renderHUD` 写入一行；**未动任何存档/槽位/读档/标题逻辑**，`saveGame`/`load`/标题 1·2·3 行为零变化。
- 改动范围：仅 `index.html`（HUD 第二行一处）+ `js/ui.js`（`renderHUD` 一行）；README「系统」一行同步。
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）**全部通过**；新增 v3.35 专项冒烟（/tmp/jrpg_smoke_v335_slot.mjs）**20 项断言全过**——index.html/ui.js 静态契约在位、默认槽 1 / 切槽 2·3 / 可逆切换均正确写入 s-slot、saveGame 落槽与 HUD 指示同源一致、连续 renderHUD 不改 hp/gold/xp/scene（纯显示收敛）、drawWorld/drawBattle/drawTitle/drawStatus 渲染无异常、其余 HUD 字段（地图/金币/武器/蘑菇）写入无回归；邻近 v3.34 洞窟胜利 / v3.33 老猎人 / v3.32 战败 / v3.31 小地图 四份专项冒烟全部保持通过。

## v3.34 修复：洞窟领主/终焉之神胜利流 ReferenceError（关键 BUG 修复）
- 【Bug】`winBattle` 击败**洞窟领主**/**终焉之神**时引用了 `TY.MB`/`TY.SB`/`TY.GRASS`，但 `battle.js` 的 import 列表在 **v3.0 ESM 拆分时漏掉了 `TY`**（单文件版里 `TY` 在同一作用域没问题），而 `state.js` 全局注册表也只托管可变单例、不注册 `TY` —— 真实游戏流中**击败洞窟领主的一瞬间直接抛 `ReferenceError: TY is not defined`**，`winBattle` 中断、`scene` 卡在 battle、`battleBusy` 永远为 true，玩家只能刷新重来。此前所有冒烟测试（含 v3.11 祭坛清理专项）都是手动置 `G.caveBoss/trueBoss` 旗标后检查绘制，从未走过真实胜利流，所以这个会让两大主线 Boss 战无法通关的崩溃一直潜伏。
- 【修复】`battle.js` 的 `winBattle` 两处：① import 显式补入 `TY`（`from "./data.js"`）；② 祭坛/水晶回退图块由 `TY.GRASS` 改为 **`TY.CAVE`**——洞窟主题化（GRASS→CAVE）只在 `loadMap` 时发生，击败后直接写 GRASS 会在岩地洞窟里留下一片草绿（与 v3.11「回退为洞窟岩地」的记录、`drawWorld`/`drawMinimap` 的旗标覆盖绘制 `TILE[TY.CAVE]` 口径不一致）；改为 CAVE 后真实胜利流与显示层完全对齐。
- 行为收敛验证：击败洞窟领主后 8 格 MB 全部清除、回退为 CAVE；击败终焉之神后 SB 清除回退 CAVE；`caveBoss/trueBoss`/讨伐数/金币/额外+300 等结算全部照常；`winBattle` 战后 1.4s 正常返回 world。**未动任何遇敌、掉落、经验、难度、存档、地图布局或其它战斗逻辑**——纯修复，普通怪/主线魔王胜利流不受影响。
- 改动范围：仅 `js/battle.js` 两处（import 一行 + `winBattle` 两分支的图块常量）；README 已按 v3.11「回退为洞窟岩地」描述，无需改动。
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）**全部通过**；新增 v3.34 专项冒烟（/tmp/jrpg_smoke_v334_cavewin.mjs）**20 项断言全过**——data.TY 导出在位、battle.js import 源码在位、真实击败洞窟领主不抛异常且 caveBoss 置位/讨伐数+1/金币+200、8 格 MB 全清除并回退 CAVE（无 GRASS 残留）、战后洞窟 drawWorld 渲染无异常、真实击败终焉之神不抛异常且 trueBoss 置位/金币含额外+300、SB 清除回退 CAVE、普通怪与主线魔王胜利流不受影响；v3.11 祭坛清理 / v3.10 升级 / v3.18 祭坛标签 / v3.24 隐者 / v3.33 老猎人 / v3.25 精英门槛 / v3.26 开局平衡 / v3.15 试炼进度 八份邻近专项冒烟全部保持通过。

## v3.33 幽暗森林「老猎人」NPC（新 NPC·引导信息透明）
- 【兑现 README 地图指南】幽暗森林此前是**唯一没有 NPC 的地图**——玩家在此遭遇最密集的遇敌（毒蛇施毒 + Lv3 起精英石心魔像），却只能靠试错（帮助第 1 页、图鉴）才摸清。现营地泉水旁常驻一位**老猎人**：第一页指点泉水（「喝一口便精神焕发」）并劝「歇够脚再往深处走，别硬撑着闯」，第二页直接点破森林两大威胁——「草丛里的毒蛇牙上带毒，被咬伤就要一路失血」「森林腹地还有尊石心魔像，皮糙肉厚、还会凝石甲护体。备好药水！」，与 `battle.js` 真实机制（毒蛇 poison 0.35 / 3 回合、精英石心魔像 35% 凝石甲至多 3 层 -40%）完全同源；位置选在营地泉水（14,8）正左一格 (13,8) 的安全空地（非遇敌地块，dangerAt=false），玩家循主线必经之路上顺路可见。
- 【角色即图块】与隐者同模式：复用既有 NPC 精灵与 SOLID 碰撞（不可走入、面对面 Enter 对话），复用 `NPC_SPOTS`/`openTalk`/`talkNext` 既有对话管线，面向时自动获得 v3.29「⏎ 对话」上下文提示——零新增界面/按键/状态。
- 改动范围：`data.js`（`NPCS.hunter` 两页对白 + `NPC_SPOTS['13,8']:'hunter'` 一行）、`world.js`（`loadMap('dungeon')` 布署老猎人图块一行）；均在图块布置层，**未动任何地形布局、传送门、营地泉水、宝箱、祭坛、遇敌判定、掉落、经验/金币、难度缩放或战斗逻辑**——纯增量内容，村庄三名 NPC 与洞窟隐者完全不受影响。
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）**全部通过**；新增 v3.33 专项冒烟（/tmp/jrpg_smoke_v333_hunter.mjs）**23 项断言全过**——数据层 hunter 与两页对白在位（第二页点名毒蛇+石心魔像）、NPC_SPOTS 预设 `13,8→hunter`、村庄/洞窟原预设不受影响、森林 (13,8) 已布置 NPC 图块且泉水(14,8)/出口(1,1)/魔王祭坛(20,13)(20,14)/洞窟入口(18,12) 全部保持、dangerAt(13,8)===false（NPC 不在遇敌地块）、openTalk 进入 talk 与 2 页翻页、翻至末页自动返回 world、猎人图块双向阻挡走位、营地泉水 onStep 照常回满、含猎人帧 drawWorld/drawMinimap 渲染无异常、渲染不改 hp/gold/mp/scene（纯显示收敛）、村庄帧无异常；v3.24 隐者 / v3.29 提示 / v3.31 小地图 / v3.30 图鉴 / v3.32 战败 五份邻近专项冒烟全部保持通过（注：v3.21 drawStatus 4 例与 jrpg_smoke_run 的损坏崩溃均为改动前即存在的历史遗留，已在原始代码上复现核验，与本次改动无关）。README「三张地图」幽暗森林一行同步。

## v3.32 战败复盘（体验打磨·信息透明）
- 【战败不等于白屏】阵亡画面此前只有「R 重开 / T 回标题 / B 重整旗鼓」三个选项——死了一次往往一头雾水：是被谁带走的？当时练到多少级、攒了多少金币？现在**倒地瞬间先复盘**：`败于 {敌人名}`（Boss 二段变身显示当前形态名，如「幽冥魔王·真身」）、`当前 Lv.X · 金币 Y · 累计讨伐 Z 只 · ⏱️游玩时长`，一眼看清这趟冒险的积累与死因。
- 【情境化下一步建议】按死亡性质给出一条针对性提示：**Boss 阵亡**提示「先回旅馆补给并练级，再按 B 重整旗鼓挑战 {Boss 名}」（与 B 重试按钮的 `_bossRetry.bossId!=='rush'` 判定完全同源，试炼连战阵亡不误导）；**普通怪阵亡**提示「回村 旅馆/喷泉 补给，用图鉴(B)查看魔物强度后再战」——把「接下来该干嘛」直接写在死因下面，延续 威胁预警/逃跑成功率/图鉴标注 同一套信息透明体系。
- 【纯显示层】只读 `enemy` 与 `G` 字段（等级/金币/bestiary/time），`enemy` 为空时自动跳过败者行、选项照常显示（容错不闪错）；**未动任何死亡/存档/掉落/结算/判定逻辑**，R/T/B 按键行为零变化。
- 改动范围：仅 `ui.js` 的 `drawDead()` 一处（阵亡画面整体下移 42 像素让出复盘区，全部文本仍在 640×480 画布内）。README「系统清单」一行同步。
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）**全部通过**；新增 v3.32 专项冒烟（/tmp/jrpg_smoke_v332_dead.mjs）**17 项断言全过**——普通怪阵亡（败者行/进度行 Lv5 金币88 讨伐6/时长 01:02:05/回村建议/无 B 按钮）、Boss 阵亡（真身名/带名建议/有 B 按钮）、rush 阵亡（无 B 按钮、普通建议）、连续渲染零状态改动、enemy 为空时容错绘制；邻近 v3.31 小地图 / v3.29 提示牌 / v3.30 图鉴标注 / v3.24 隐者 专项冒烟全部保持通过。

## v3.31 小地图「危险区」红色标注（体验打磨·信息透明）
- 【兑现 README】README「视觉」写有「小地图」，但和平村的**高草丛才是唯一会踩出魔物**的散布式遇敌点（`dangerAt` 仅对 'G' 图块返回 true），小地图却把全村草地图成同一草绿——低血量新手无从分辨哪里安全、哪里会遇敌（遇敌率 16%，残血进草丛就可能白给）。现在小地图上对危险区叠印**红色小点**：和平村的草丛在 minimap 上成片标红、一眼避开；而森林/洞窟几乎全域皆为遇敌区（占比 67%/89%），若同样叠色会淹没整张图、失去信息量，故**危险区占可行走面的少数时才标注**（dn≤walk×50% 才叠涂）。
- 【与真实判定同源】标注读 `dangerAt()`（与 `onStep` 遇敌判定完全同一函数）与 `SOLID`（可行走面统计），村庄 8 格草丛在运行时逐一命中、与地图数据统计一致；红色仅叠加在原有底色之上，不改变任何图块颜色/坐标/比例。**纯显示层**——未动任何遇敌概率、移动、碰撞、掉落、经验/金币、存档或图块逻辑；森林/洞窟小地图渲染照旧（连指针、支线宝箱金光脉动等既有元素完全不受影响）。
- 改动范围：仅 `ui.js` 的 `drawMinimap()` 一处（import 增补 `SOLID` 与 `dangerAt` 两个只读引用，均已在 data.js/world.js 导出）；README「视觉」小地图一行同步。
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）**全部通过**；新增 v3.31 专项冒烟（/tmp/jrpg_smoke_v331_minimap.mjs）**11 项断言全过**——村庄危险区 dn=8/walk=336（2.4%≤50% 应标注）、森林 66.8% / 洞窟 89.2%（>50% 不叠涂）、三张地图 drawMinimap×3 渲染不抛错且 scene/hp/gold/mp/curMap 零变化（纯显示收敛）、drawWorld 含小地图渲染稳定、dangerAt 运行时与地图统计一致、标注分支可正常产生绘图调用；v3.29 上下文提示 / v3.30 图鉴标注 / v3.16 旅行 / v3.13 旅馆 邻近专项冒烟全部保持通过。

## v3.30 敌人图鉴「掉落/特性」标注（体验打磨·信息透明）
- 【兑现 README】README「图鉴 & 成就」早写明「报酬参考（击败可得）」与精英/圣剑掉落，但图鉴行只显示经验/金币——哪些怪有特殊掉落、毒蛇会施毒，全得靠试错。现在**已讨伐行的「击败可得」行尾追加对应标注**：毒蛇 `☠️ 会施毒 · 扣血3回合`、石心魔像 `🍄 必掉魔法蘑菇`、幽冥魔王 `⚔️ 必掉圣光之剑`、洞窟领主 `💠 击败清除祭坛`、终焉之神 `💰 击败+300金`——一眼看清「打它值得/要防什么」。
- 【与真实结算同源】标注来自新增静态表 `CODEX_TAG`（`ui.js`），且逐条与真实逻辑核对：毒蛇 = `MON_BASE` 的 `poison:0.35` 标记（battle.js 命中 35% 中毒 3 回合）；石心魔像 = `winBattle` 的 `isElite` 分支必掉 1 株蘑菇；幽冥魔王 = `isBoss` 分支必掉圣光之剑；洞窟领主 = `isCaveBoss` 分支清除祭坛图块；终焉之神 = `isTrue` 分支额外 +300 金。只读、不参与任何判定，与 `whereFind`/祭坛标签同一套「静态提示」惯例。
- 【不剧透】未讨伐的灰色占位行照旧只显示 `❓ ？？？ · 未讨伐`，五种标注一律不提前显示——延续 图鉴灰色占位 / 旅行「未探索」/ 掉落提示 同一套信息透明原则；普通怪（史莱姆/野狼/树精/石魔像等）无特殊条目，行尾保持原样。
- 改动范围：仅 `ui.js` 的 `drawCodex()` 一处（新增 `CODEX_TAG` 静态表 + 击败可得行追加串）；**未动任何掉落/战斗/结算/经验/金币/存档/遇敌逻辑**——纯显示层增量；README「系统清单」图鉴一行同步。
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）**全部通过**；v3.17 图鉴邻近专项冒烟 **29/29 保持通过**；新增 v3.30 专项冒烟（/tmp/jrpg_smoke_v330_codex.mjs）**12 项断言全过**——毒蛇/石心魔像/幽冥魔王/洞窟领主/第 2 页终焉之神 五种标注逐字在位、普通怪行尾无标注（与原点字符串全等）、未讨伐 9 格灰色占位且不剧透 圣光之剑/+300金/施毒、连续渲染不改 scene/gold/xp/bestiary（纯显示收敛）；v3.29 提示 / v3.16 旅行 / v3.24 隐者 邻近专项冒烟全部保持通过。（注：v3.21 drawStatus 已学满/旧存档两例与本 v3.17 同为历史遗留、与本次改动无关——在原版 ui.js 上同样复现，本次未触碰 drawStatus。）

## v3.29 面向可交互对象·上下文提示（体验打磨·引导信息透明）
- 【引导新手】地图上看到图块却不知道它怎么用，是新手期最大的卡点：商店/旅馆/酿造锅要「面向 + Enter」，宝箱/喷泉/传送门/出口/强敌祭坛/试炼碑则是「踩上去触发」，全靠试错才懂。现在**面向任意可交互对象时，目标图块上方常驻一枚小提示牌**：按 Enter 类显示 `⏎ 对话 / ⏎ 商店 / ⏎ 旅馆 / ⏎ 酿造`，踩踏类显示 `踩上回血 / 踩上开启 / 踩上开战 / 踩上触发 / 踩上挑战 / 踩上通行`——一眼明白该按哪个键、还是直接走上去。
- 【与真实判定同源】提示与 `interact()`/`onStep()` 的真实交互路径一一对应，且按推进旗标自动熄灭：宝箱已开不再提示、魔王已被讨伐时祭坛与村庄传送门都不再显示「踩上开战/踩上通行」（避免误导）、终焉水晶已破不提示、试炼碑仅当双徽记集齐才显示「踩上挑战」——不会出现「提示能触发但实际不能」的说法不一致。
- 【纯显示层】新增 `faceHint()`（`ui.js`）：依 `dir` 取面向格，仅读图块类型与 `G.chests/bossDefeated/caveBoss/trueBoss` 旗标，在 `drawWorld()` 中绘制提示牌；`scene!=='world'` 时（商店/状态等面板把 `drawWorld` 当背景）自动不显示。**未动任何交互、进入判定、开战、宝箱、掉落或数值逻辑**——不含新图块、不占新键位、零外部依赖。
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）**全部通过**；新增 v3.29 专项冒烟（/tmp/jrpg_smoke_v329_hint.mjs）**19 项断言全过**——面向 商店/旅馆/酿造/NPC/宝箱/村庄传送门/喷泉/魔王祭坛/出口/终焉水晶 十类对象分别捕获到对应提示文案（记录型 Canvas 逐字断言）、试炼碑资格前后（无资格不提示 / 有资格提示「踩上挑战」）、魔王讨伐后祭坛与村庄传送门提示熄灭、面向空地与商店背景（非 world）不提示、三张地图带提示渲染无异常、提示绘制不改 hp/gold/mp/scene/curMap/位置（纯显示收敛）；v3.28 逃跑标注 / v3.27 旅行 / v3.24 隐者 / v3.23 日志回看 / v3.22 石甲 / v3.25 精英门槛 六份邻近专项冒烟全部保持通过。

## v3.28 战斗指令栏「逃跑成功率」标注（体验打磨·信息透明）
- 【兑现 README】README「战斗」一栏与帮助页早写明普通怪「约 60% 成功」，但战斗指令栏里只有 Boss 战叠绘红色 ⛔，普通怪/精英的 `[4]逃跑` 右侧从不标注成功率——要不要赌一把逃跑只能靠感觉。现在普通怪/精英的指令栏在 `[4]逃跑` 右侧常驻 `·成功率约60%`（与 `playerAction` 的 flee 分支 `Math.random()<0.6` 完全同源，纯静态展示、不参与判定），与 Boss 战 ⛔ 互补：Boss 看 ⛔、普通怪看成功率，同一套「信息透明」体系一眼分明。
- 【帮助页同步】操作说明第 1 页「战斗」行同步标注 `4逃跑(普通怪·成功率约60%)`（原为省略的成功率），README 战斗一段增补「指令栏实时标注成功率」一句。
- 改动范围：`ui.js` 的 `drawBattle()` 指令栏一行（普通怪分支追加展示串，Boss 分支不加、⛔ 叠绘逻辑不变）、`data.js` 的 `HELP_PAGES` 第 1 页战斗行一处；**未动任何逃跑判定、回合消耗、敌人属性、掉落、经验/金币或难度数值**——逃跑行为与成功率本身零变化，纯显示层。
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）**全部通过**；新增 v3.28 专项冒烟（/tmp/jrpg_smoke_v328_fleerate.mjs）**12 项断言全过**——帮助页战斗行含「成功率约60%」、普通怪（毒蛇/精英石心魔像/史莱姆）战斗指令栏捕获到 `[4]逃跑·成功率约60%` 且无 ⛔、Boss（幽冥魔王/洞窟领主/终焉之神/试炼连战石心魔像）指令栏无成功率标注但叠绘 ⛔、绘制不改 battleTurn/blog/hp/mp/gold（纯显示收敛）、连续渲染 3 次稳定、普通怪栏渲染不抛错；v3.19 逃跑禁标邻近专项冒烟保持通过。

## v3.27 快速旅行目的地推荐等级标注（体验打磨·信息透明）
- 【兑现 README】README「系统清单」早已写明快速旅行「含各目的地推荐等级/特色提示」，但 `TRAVEL_LIST` 一直是 3 元组（key/名称/说明），推荐等级从未真正落地——现在补上：**幽暗森林 `推荐 Lv.3 起 · 当心精英`、宝藏洞窟 `推荐 Lv.6 起 · 高难`、和平村 `安全区 · Lv.1 即可`**，与强敌祭坛等级标签（魔王 Lv.8 / 洞窟领主 Lv.7 / 终焉之神 Lv.12）、敌方等级标定同一套「信息透明」体系（数值均为静态展示建议，不参与任何判定）。
- 【展示层】`TRAVEL_LIST` 每个目的地追加第 4 项推荐等级串（`data.js`）；`drawTravel()` 在**已解锁**目的地的名称行右缘以琥珀色小字常驻显示（`478, …` 右对齐，400 宽面板内不超出、不与说明行重叠），**未解锁**目的地保持「？？？ · 未探索」的神秘感不提前剧透——延续 图鉴灰色占位 / 旅行「未探索」同一原则；选中 ▶ 与说明行照旧。
- 改动范围：`data.js` 的 `TRAVEL_LIST` 一处（3 元组 → 4 元组，纯增量字段）、`ui.js` 的 `drawTravel()` 一行；**未动任何传送判定、visited 解锁逻辑、地图/数值/战斗/掉落/存档**，键位与 `findIndex` 遍历（`x[0]===curMap`）完全不受影响——`main.js`/`core.js` 对 TRAVEL_LIST 的既有用法只取 `[0]` 与 `.length`。
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）**全部通过**；新增 v3.27 专项冒烟（/tmp/jrpg_smoke_v327_travel.mjs）**18 项断言全过**——数据契约（3 目的地/第 4 项非空/Lv.3·Lv.6 落位/键序不变）、doTravel 未探索拦截不改金币不改图、仅村庄解锁时村庄显示推荐等级而两地不提前显示、未探索「？？？」占位与底部提示在位、连续渲染 3 次不改 scene/travelSel/level/gold/hp/mp（纯显示收敛）、全解锁三枚推荐等级齐全、状态/图鉴/帮助/标题邻近界面渲染无异常；邻近 v3.16 旅行冒烟（契约断言 3→≥3 同步更新）与 v3.13 旅馆冒烟（陈旧 HP 常量 41→45、23→27 同步到 v3.26 曲线，历史遗留缺失一并补齐）**全部通过**，23 个冒烟套件全绿。
- 【修复】开局（Lv1-2 进幽暗森林）被随机怪一套连招送命的体验问题。定位到两个元凶：
  - **毒蛇角色倒挂**：它是 7 种普通魔物里攻击基础值最高的（攻 10，比骷髅兵 8 / 石魔像 8 还高），却又是唯一带 35% 施毒的状态怪——Lv1 时一击期望伤害 18（占玩家 41 血 44%）、困难模式 22（两击满血必死），且 v1.21 的 Lv1 新手保护（树精/石魔像 Lv3 门槛）唯独漏了它，是普通/困难两档下开局随机遇怪的首要死因。现基础攻 **10 → 8**（与骷髅兵/石魔像同档），Lv1 期望伤害 18 → 14，困难 22 → 18，施毒特色（0.35 / 3 回合）完整保留——角色回归「低攻高难道」的状态怪定位。
  - **HP 曲线整体偏低**：`baseStats` 血量 **34+lv·7 → 38+lv·7**（Lv1 41→45，全程 +4 平滑），给「连续两场怪 + 毒毒发」的组合拳留出缓冲，后期 Boss 战几乎不受影响（Lv10 仅 104→108）。
- 【模拟验证前置】用真实 `randomEncounter`（真实权重/倍率/精英判定）+ 同源伤害公式跑了 5 组 × 30000 次蒙特卡洛「Lv1 进森林连刷至 Lv5/10 场」对比：普通·保守喝药死亡 0.93%→**0.02%**；普通·无药裸刷 9.2%→**2.0%**；困难·带药 21.9%→**5.3%**；困难·无药 95.2%→91.1%（空血硬刷困难，玩家选择）；凶手分布从「毒蛇一家独大」恢复为各怪均匀。
- 改动范围：仅 `data.js` 两处——`MON_BASE` 毒蛇 `atk:[10,2]→[8,2]`、`baseStats` 血量 `34+lv·7→38+lv·7`；未动任何遇敌权重曲线、地图难度倍率（village×0.9 / cave×1.2）、精英 Lv3 门槛、掉落、经验/金币、技能、Boss 数据或战斗逻辑。
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）**全部通过**；新增 v3.26 专项冒烟（/tmp/jrpg_smoke_v326_opening.mjs）**20 项断言全过**——数据层毒蛇攻 8/施毒 0.35/其余怪未被误改、HP 曲线 45/73/108 落位、applyStats 新档面板 45/11/6/16、Lv1 遇敌实例毒蛇攻 10/血 25/hpMax===hp、对毒蛇期望伤害 14（原 18）、困难缩放 12→18（原 22）不再一击致命、Lv1 无 树精/石魔像/精英（新手保护未破）、Lv3 精英约 7% 与毒蛇照常、Lv6/Lv10 毒蛇威胁占比 ≤25%（后期平滑）、MON_BASE 攻击基础列整体落位；v3.2 中毒 / v3.25 精英门槛 / v2.10 升级 / v3.21 经验预估 邻近专项冒烟全部保持通过。

## v3.25 幽暗森林精英「石心魔像」Lv3 门槛（数值平衡·新手保护）
- 【修复】幽暗森林 7% 稀有精英「石心魔像」原本对玩家等级**无任何门槛**：而 v1.21 的遇敌渐进曲线明确给 树精/石魔像 设了 `Lv3 起才登场` 的新手保护（并写明「Lv1 不会撞上对初始属性极不友好的强敌」），唯独这只看门精英漏了门槛——主线任务从开局就把玩家引向幽暗森林深处，Lv1-2 新手若遇到精英（攻 15+3lv、防 15+2lv，Lv1 时普攻只能打 1 点）会陷入「打不动、逃跑失败就可能送命」的绝境
- 【修复】现在精英与 树精/石魔像 **同一道 Lv3 门槛**：`G.level<3` 时该 7% 分支直接回落到普通权重池（仍按 v1.21 曲线抽取当时合法魔物），Lv3 起精英恢复正常、出现率保持 7% 不变——幽暗森林的最高难度尖峰延后到与其它强敌曲线对齐，新手期进入森林只会遇到可挑战的普通魔物
- 改动范围：仅 `battle.js` 的 `randomEncounter()` 一处（精英判定加 `G.level>=3`）；未动任何精英属性公式、7% 出现率、地图难度倍率、掉落、经验/金币或其它遇敌权重；village/cave 本就不出精英，不受影响
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）**全部通过**（含 Lv6 精英率约 7% 既有断言）；新增 v3.25 专项冒烟（/tmp/jrpg_smoke_v325_elitegate.mjs）**15 项断言全过**——Lv1/Lv2 强制命中 7% 区间也不出精英且回落敌人合法（正属性/hpMax===hp/非精英/属 MON_BASE）、Lv3 精英恢复可用且属性公式正确（hp88/atk21/def21）、Lv3 随机数≥0.07 时仍不出精英（7% 判定未破坏）、village/cave 均不出精英、Lv1/Lv2 各 20000 次抽样精英计数为 0、Lv6 20000 次抽样率约 7%（1377/20000）与基线同界、eliteEncounter 独立调用行为不变；v3.2 中毒 / v3.22 石甲 / v3.23 日志回看 邻近专项冒烟全部保持通过

## v3.24 宝藏洞窟「隐者」NPC（新 NPC·引导信息透明）
- 【新 NPC】宝藏洞窟（第三张地图）此前是唯一一个**没有任何 NPC** 的地图——洞窟领主祭坛、终焉水晶、试炼碑全得靠玩家自己踩上去才知道条件。现洞窟入口处（进门即见）常驻一位白发**隐者**：第一页指点洞窟强敌分布（「洞窟领主镇守着宝藏，中央的终焉水晶里封着终焉之神」），第二页直接点破试炼碑资格（「集齐两枚胜利徽记——击败 幽冥魔王 与 洞窟领主 方能开启最强试炼」），与 `onStep` 的真实资格判定（`bossDefeated && caveBoss`）完全一致，延续 试炼碑状态标签 / 强敌祭坛 Lv 标签 / 帮助第 4 页 同一套「信息透明」体系；也让进门就见矿坑的第三张地图不再冷清
- 【角色即图块】隐者复用既有 NPC 精灵（本就是白发+胡须的老年人造型）与 SOLID 碰撞（不可走入、面对面 Enter 对话），复用 `NPC_SPOTS`/`openTalk`/`talkNext` 既有对话管线，零新增界面/按键/状态
- 改动范围：`data.js`（新增 `NPCS.sage` 两页对白 + `NPC_SPOTS['4,1']:'sage'` 一行）、`world.js`（`loadMap('cave')` 布置隐者图块一行）；均在图块布置层，**未动任何地形布局、传送门、宝箱、祭坛、遇敌判定、掉落、经验/金币、难度缩放或战斗逻辑**——纯增量内容，村庄三名 NPC 完全不受影响
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）**全部通过**；新增 v3.24 专项冒烟（/tmp/jrpg_smoke_v324_sage.mjs）**19 项断言全过**——数据层 sage 与两页对白在位、NPC_SPOTS 预设 `4,1→sage`、村庄三名 NPC 原预设不受影响、洞窟 `(4,1)` 已布置 NPC 图块且入口 E/终焉水晶 SB/试炼碑 TRIAL 保持、岩地主题化仍生效、openTalk 进入 talk 场景与 2 页翻页、翻至末页自动返回 world、隐者图块双向阻挡走位、含隐者帧 drawWorld/drawMinimap 渲染无异常、渲染不改 hp/gold/scene、村庄帧无异常；v3.18 祭坛标签 / v3.11 场地清理 / v3.16 旅行 / v3.5 试炼碑 / v3.15 试炼 / v3.23 日志 全部邻近专项冒烟保持通过

## v3.23 战斗日志回看（体验打磨·信息透明）
- 【回看战斗日志】战斗画面下方的日志区原本永远只显示最后 3 行——长盘 Boss 战（魔王几十回合、试炼三连战近百回合）打到后面，前面的中毒回合数、二阶段变身时机、石甲层数、暴击/反击等历史全部被挤出屏幕，想看前面的回合毫无办法。现在**整场战斗的完整日志可随时回看**：日志超过一屏后，按 `↑/↓`（或 `w/s`，与图鉴滚动的按键习惯一致）在历史里翻动，想看哪一段都行
- 【与图鉴滚动同源】沿用图鉴 `codexScroll` 的滑动偏移模式，新增 `blogView`（0=停在最新、越大越往历史深处）：每次绘制都在 `maxV = 日志行数-3` 处双向钳制（负值/99 一律回合法区间，绝不越界/空屏）；`startBattle` 每场新战斗把偏移归零（Boss 重试、试炼连战、读档后再战都不会带上一场的翻阅位置）；日志 ≤3 行时不出现滚动控件，行为与旧版完全一致
- 【信息透明】右缘常驻一枚小字状态提示：停在最新显示 `↑↓ 回看战斗记录`、翻到最旧显示 `↓ 回到最新`、中段显示 `↑↓ 战斗记录`——与图鉴滚动提示同一体系，一眼明白能翻、翻到哪；纯显示层，未改任何战斗逻辑/数值/日志内容
- 改动范围：`state.js`（注册 `blogView` 一行）、`battle.js` 的 `startBattle`（归零一行）、`ui.js` 的 `drawBattle()` 日志段（窗口切片 + 钳制 + 提示）、`main.js` 战斗键位（↑/↓/w/s 两行）；README「战斗」一行同步。未动任何遇敌/伤害/掉落/经验/金币/难度/存档逻辑——**纯显示增量，零判定风险**
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）**33 项断言全部通过**；新增 v3.23 专项冒烟（/tmp/jrpg_smoke_v323_logscroll.mjs）**25 项断言全过**——blogView 注册初值、0/中段/maxV 三档窗口「画的正是期望 3 行」逐字符断言、负值与 99 越界钳制、日志 2 行不滚动不提示、多次绘制不改写 blog、startBattle 残留偏移(7)归零、新战斗日志重新起算、连续渲染不改 hp/mp/gold/xp/level/battleTurn 纯净性、技能菜单覆盖层共存无异常、单行日志路径正常；v3.22 石甲 / v3.19 逃跑禁标 / v3.15 试炼进度 / v3.21 经验预估 / v3.17 图鉴 五份邻近专项冒烟全部保持通过

## v3.22 精英「石心魔像」专属机制·石甲（新战斗机制·小而自包含）
- 【新机制】幽暗森林的稀有精英「石心魔像」不再只是「血厚防高的杂兵」——它是一尊石心卫士：**血量降至一半以下后，每回合有 35% 概率凝结【石甲】**（至多 3 层），每一层石甲会挡下主角接下来的**一次攻击（普攻/技能）并令伤害降低 40%**。玩家策略随之丰富：石甲层数高时先【6 蓄力】攒一记重击，或【5 防御】稳住回合，一层一层敲开龟壳
- 【信息透明】敌人血条右上角常驻 `🪨 石甲×N（受击-40%）` 层数指示（与玩家「盾/蓄力/中毒」buff 提示同一风格，右对齐不遮挡战斗主体）；战斗日志实时记录「凝结石甲 / 石甲挡下部分伤害（剩余 N 层）」，一眼看清还有几层要拆
- 【与战斗结算同源】石甲只在本场战斗生效，绝不跨场残留（开场 `startBattle` 从数据层深拷贝重构敌人、`eliteEncounter` 不带 shield 字段，重开/Boss 重试/试炼连战等一律干净复刻）；`attackMove` 中先扣层再按 ×0.6 取整（保底 1）结算，飘字/实际扣血/胜利判定全部自动使用减伤后数值；其余敌人（毒蛇/野狼/普通 Boss 二段变身等）完全不受新分支影响
- 改动范围：`battle.js`（`enemyAct` 新增凝甲分支 + `attackMove` 新增石甲减伤两处）、`ui.js` 的 `drawBattle()` 一处（右上角层数指示）、`data.js` 的 `HELP_PAGES` 第 3 页新增「石心魔像·石甲」说明条目；未动任何掉落/经验/金币/升级/存档/地图/难度数值——**纯增量机制，零外部依赖**
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）**全部通过**；新增 v3.22 专项冒烟（/tmp/jrpg_smoke_v322_shield.mjs）**19 项断言全过**——HELP 条目在位、出场精英无 shield、满血不凝甲、半血下 low-rand 必凝 1 层、石甲封顶 3 层、2 层可再凝至 3、攻击消耗一层且伤害 -40%（原 5 → 3）与扣血一致、无石甲伤害保持旧公式不回退、无石甲不产生日志、野狼/Boss 不受分支影响、新开战斗不跨场残留、drawBattle 含/不含石甲渲染均无异常；v3.19/v3.3/v3.2/v3.17/v3.15/v3.12/v3.20/v3.21 邻近专项冒烟全部保持通过


## v3.21 下一技能所需经验预估（体验打磨·信息透明）
- 【信息透明】状态界面（`I`）「下一技能」行升级：原来只显示 `📖 下一技能：陨石术（Lv.7 解锁）`，现在追加**还差多少经验**（`…（Lv.7 解锁 · 还差 156 经验）`），按真实升级曲线逐级预估，一眼看清「还要刷几场、刷什么」才能学到下一个技能；已学满仍显示「已习得全部技能」——延续 威胁预警/战利品预览/图鉴报酬参考/祭坛等级标签 同一套「信息透明」体系
- 【与真实曲线同源】预估来自新增纯函数 `skillXpHint(g)`（`core.js`）：下一个未学技能与 `learnsAt` 同源（已学技能跳过），剩余经验从当前 `g.xpNext` 起按 `winBattle` 的升级链（逐级 ×1.42 取整）逐级累加至解锁等级——与实际升级所需完全一致，不是拍脑袋的粗略值；旧存档缺 `xpNext/xp` 一律兜底（默认 20 / 0），缺字段不闪 0/NaN
- 改动范围：`core.js`（新增并导出一个纯函数）、`ui.js` 的 `drawStatus()` 一处（`nextSkillHint` 调用换成 `skillXpHint`，原 `nextSkillHint` 仍保留导出）；未动任何经验/等级/技能/升级/存档/战斗/掉落逻辑——**纯显示层，零判定风险**；README「职业成长」一行同步
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）**全部通过**；新增 v3.21 专项冒烟（/tmp/jrpg_smoke_v321.mjs）**14 项断言全过**——升级链基准（L1:20→L2:28→L3:40→L4:57→L5:81→L6:115→L7:163→L8:231）与逐级 ×1.42 取整全一致、Lv1xp5 冰霜击还差 43、Lv4 雷鸣还差 57、已学雷鸣后陨石术还差 253、Lv5 已过解锁级跳至陨石术还差 156、Lv2 冰霜击还差 18、已学满返回 null、旧存档缺 xpNext/xp 仍有限数值、null/空参返回 null、drawStatus 渲染无异常且不改 level/xp/hp/gold/ach（纯显示收敛）、连续渲染 3 次稳定、已学满渲染正常、旧存档缺 xpNext 渲染容错

## v3.20 状态界面冒险进度一览（体验打磨·事故功能回归 v2.8）
- 【回归 v2.8】状态界面（`I`）在「下一技能」与「任务目标」之间常驻一行**冒险进度四徽记**：`魔王 / 洞窟领主 / 终焉之神 / 试炼场` 四格进度一次看清——已达成**亮绿 ✓**、未达成**灰色 ✗**（如「✓ 魔王　✓ 洞窟领主　✗ 终焉之神　✗ 试炼场」）。此前状态界面只能看到当前任务目标，隐藏内容（洞窟领主打没打、终焉之神有没有资格、试炼场通没通）毫无线索，只有去踩对应地点/打赢才知道；现在打开角色面板即可一眼判断「还差什么、值不值得去刷」（v3.0 事故回退中连同 v2.8 一起丢失，本次按 v2.8 既定设计回归，补齐 v2.x 事故功能的最后一块）
- 【与推进判定完全同源】徽记数据来自新增纯函数 `adventureProgress(g)`（`core.js`）：只读 `G` 上的四个推进旗标 `bossDefeated / caveBoss / trueBoss / rushDone`，与「百炼成钢」成就、标题存档摘要、试炼碑资格门控共用同一批旗标，永远和真实剧情进度一致，不会出现两套说法；旧存档缺旗标一律 `!!` 容错为未达成
- 【面板净高度不变】原「任务目标」两行（标题+内容）压缩为单行（`任务目标：xxx`），让位给新增的冒险进度行——面板 500×400 尺寸、成就行、关闭提示位置全部保持不变，不超界
- 改动范围：`core.js`（新增并导出一个纯函数）、`ui.js` 的 `drawStatus()` 一处（新增徽记绘制 + 任务目标压单行）；未动任何推进/成就/存档/数值/战斗/掉落逻辑——**纯显示层，零判定风险**
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）**全部通过**；新增 v3.20 专项冒烟（/tmp/jrpg_smoke_v320_progress.mjs）**33 项断言全过**——adventureProgress 四徽记顺序/名称/缺旗标容错契约、四旗标同源（仅魔王/魔王+洞窟领主/含终焉三档分支）、开局全灰 ✗、真通关全绿 ✓、双徽记部分达成、任务目标单行+成就行+关闭提示在位、金币/药水/灵药/冒险时长行原文保持、连续渲染三次不改旗标/hp/金币/成就（纯显示收敛）、旧存档缺 potion2/ach/quest/time/bestiary 字段渲染无 NaN 不抛错；v3.7 状态界面/时长 与 v3.12 标题预览 邻近专项冒烟保持通过

## v3.19 Boss 战逃跑禁标回归（体验打磨·事故功能回归 v2.1）
- 【回归 v2.1】战斗指令栏的 `[4]逃跑` 在四类 Boss 战（主线幽冥魔王 / 洞窟领主 / 终焉之神 / 试炼三连战）中**重新叠绘红色 ⛔**——这些战斗按 4 会被气场压制、不耗回合（`playerAction` 的 flee 分支判定），此前指令栏却只显示普通「[4]逃跑」，与「Boss 战无法逃跑」的实况不一致，README 早已写明「指令置灰 ⛔」但画面一直没画（v3.0 事故回退中连同 v2.1 一起丢失，本次按 v2.1 既定设计回归，让代码兑现文档描述）
- 与技能菜单「MP 不足 ⛔」同风格：普通怪 / 精英「石心魔像」仍可逃跑（约 60% 成功率），指令栏保持原样不叠标；Boss 二段「真身」改名后旗标不变仍叠标
- 改动范围：仅 `js/ui.js` 的 `drawBattle()` 指令栏一处（新增纯显示判定 `isBossFlee` 与一行红色 ⛔ 叠绘，判定与 `playerAction` 的 flee 分支**完全同源** 的 4 个旗标）；**未动任何逃跑判定、回合消耗、敌人属性、掉落、经验/金币或难度数值**，逃跑被压制的既有行为本就在 v2.2 修复中工作正常
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）**全部通过**；新增 v3.19 专项冒烟（/tmp/jrpg_smoke_v319_flee.mjs）**18 项断言全过**——四类 Boss 均叠绘 ⛔ 且保留 `[4]逃跑` 文案、普通怪与精英不叠标、Boss 真身改名后仍叠标、Boss 按 4 逃跑被压制/日志「无法逃脱」/之后仍可行动（无残留锁）、battleBusy 期间不渲染指令栏（无 ⛔）、连续渲染不改动 hp/mp/scene/blog/金币/rushStage（纯显示收敛）、五类 Boss 敌人渲染路径无异常；v3.1/v3.3/v3.15/v3.2 邻近冒烟全部保持通过

## v3.18 强敌祭坛等级标注回归（体验打磨·事故功能回归 v2.9）
- 【回归 v2.9】漫游世界时，**未讨伐**的强敌祭坛上方现在常驻一枚**深红底色金字 `⚠ Lv.X`** 标签：幽暗森林魔王祭坛 `Lv.8`、宝藏洞窟洞窟领主祭坛 `Lv.7`、终焉水晶 `Lv.12`——不必走近踩上、也不用开图鉴/帮助，远远一眼看清强敌所在与建议等级，判断「现在实力够不够、值不值得深入」，与战斗威胁预警、敌方等级标定（v2.3）、试炼碑状态标签的「信息透明」体系同风格（v3.0 事故回退中连同 v2.9 一起丢失，本次按 v2.9 既定设计回归）
- 【同步熄灭】已讨伐的祭坛标签自动熄灭：魔王已倒、洞窟领主已败、终焉之神已破后各自熄灭，与 v3.11 场地清理（祭坛回退岩地）严格同步，不会出现「已打完还挂警告」
- 改动范围：仅 `js/ui.js` 两处——新增模块级纯数据 `ALTAR_TAG`（图块类型 → 已讨伐判定 + 建议等级）＋ `drawWorld()` 一节标签绘制（循环限定当前可见区块，洞窟外/画面外零开销）。**只画每处祭坛的「最顶最左」图块**（`at(x,y-1)/at(x-1,y)` 同为该祭坛类型则跳过）：魔王祭坛竖排 2 格、洞窟领主祭坛 2×2 均只出一枚标签，绝不重复叠标。未动任何敌人属性、触发判定（`onStep` 旗标门控不变）、经验/金币、掉落或难度数值——纯显示层，等级与 `enemyLv()` 标定同源
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）**全部通过**；新增 v3.18 专项冒烟（/tmp/jrpg_smoke_v318_altarlv.mjs）**17 项断言全过**——竖排魔王祭坛只画 1 枚（不叠标）、洞窟 MB(2×2)+SB 各 1 枚共 2 枚、文案含 Lv.8/Lv.7/Lv.12、逐枚击败后各自熄灭直至全灭、村庄无标签、连续渲染不改 bossDefeated/caveBoss/trueBoss/scene（纯显示收敛）；v3.11 祭坛清理 / v3.17 图鉴 / v3.15 试炼 / v3.16 旅行 / v3.5 试炼碑 五份邻近专项冒烟全部保持通过


## v3.17 敌人图鉴「未收集灰色占位」（体验打磨·信息透明）
- 【新显示】敌人图鉴（B 键）不再只列出已讨伐的魔物：固定按 `BESTIARY_TARGET` 全 11 种排列，**未讨伐的格子显示灰色 `❓ ？？？ · 未讨伐` 占位**，已讨伐的照旧显示实况（名称/出没地点/讨伐数/击败可得）——一眼看清图鉴还有几格待点亮、还差哪几种；占位不剧透名称/地点/报酬，保留探索悬念（与快速旅行「？？？ · 未探索」同风格）。此前杀掉 N 只史莱姆后图鉴只有 1 行，玩家完全看不出还有多少物种待发现
- 兑现 README「未收集灰色占位（一眼看清还差哪几种）」的既有描述（v1.x 单文件与 v3.0 重建基线均未真正实现，本次落地）
- 改动范围：仅 `js/ui.js` 的 `drawCodex()` 一处——列表源从「已收集 names」改为「`BESTIARY_TARGET` 全 11 种的 rows（含 got 标记）」，未讨伐分支画灰占位；`codexScroll` 越界钳制同步改为按 11 格计算（上下滚动仍有意义：第 11 格需翻页）；空图鉴仍保持原有「尚未击败任何敌人」引导界面不变。未动任何图鉴数据、`monReward`/`whereFind`、成就判定、掉落或存档结构——纯显示层，零判定风险
- 验证：`node --check js/*.js` 全部 8 个模块通过；新增 v3.17 专项 ESM 冒烟（/tmp/jrpg_smoke_v317.mjs）**29 项断言全过**——空图鉴引导与 0/11、部分收集（实况行 + 9 格占位 + 滚动提示 + 第 11 格翻页补齐 10 处）、越界钳制（999→1 / -7→0）、全收集（无占位、11/11、魔王👿/精英•/石魔像与石心魔像不混淆、末页无滚动提示）、渲染纯净（连续渲染不改 gold/xp/bestiary/scene/curMap）


## v3.16 快速旅行目的地说明（体验打磨·信息透明）
- 【信息透明】快速旅行界面（T 键）每个目的地名称下方新增一行灰色小字，说明到了哪里、那里有什么：和平村「商店·旅馆·村长委托（补给与任务）」、幽暗森林「魔物·宝箱·祭坛·洞窟入口」、宝藏洞窟「强敌·试炼碑·终焉水晶（高难区域）」——此前快速旅行只有三个地名（未探索还只显示「？？？」），想补给/练级/打 Boss 全靠自己记位置；现在一眼看清每个地方的主打内容，与商店药水恢复量、试炼碑状态标签、冒险时长同一套「信息透明」体系；**未探索目的地仍保持「？？？ · 未探索」且不显示说明**（保留探索神秘感，不剧透）
- 改动范围：`js/data.js` 的 `TRAVEL_LIST` 每项追加第三个元素 desc（纯数据）；`js/ui.js` 的 `drawTravel()` 解构改 `[k,nm,desc]` 并新增一行 `if(unlocked&&desc)` 说明绘制——纯显示层，未动任何传送判定、`visited` 逻辑、数值、难度或存档；`doTravel()` 的 `const [k]=TRAVEL_LIST[travelSel]` 与 main.js 的 tab 循环 / `findIndex(x=>x[0]===curMap)` 均只取 key，完全不受 desc 影响
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）**33 项断言全部通过**；新增 v3.16 专项冒烟（/tmp/jrpg_smoke_v316_travel.mjs）**24 项断言全过**——数据结构（条目长 3、desc 非空、key 顺序与名称不变）、导航兼容（tab 循环三圈回 0、findIndex 命中 dungeon）、三态渲染（仅村庄解锁/全部解锁/部分解锁时说明行与「？？？」正确显隐）、渲染纯净性（连续渲染不改 scene/G）、doTravel 契约（未解锁拦截不传送、已解锁正常进 world、三种目的地均可选）

## v3.15 试炼连战关卡进度上线（体验打磨·信息透明）
- 【新显示】试炼三连战（宝藏洞窟试炼碑）**开战期间左上角常驻 `🧭 试炼三连战 第 N/3 关`**：与 `G.rushStage` 完全同源（第 1 关幽冥魔王 / 第 2 关洞窟领主 / 第 3 关终焉之神，全胜即「百炼成钢」），一眼看清三场强敌连战打到了第几场、还剩几场——此前除 Boss 名外毫无进度感，第一关打到胶着时会误以为「就这一场」而放松药水/技能管理；与战斗回合计数、变身线、威胁预警、战利品预览同一套「信息透明」体系
- 纯显示层改动：只改 `js/ui.js` 一处（`drawBattle()` 回合计数下方加 4 行 + import 列表补 `RUSH_BOSSES`）；`Math.min(RUSH_BOSSES.length, Math.max(1, G.rushStage||1))` 钳制，缺字段（旧代码直接进 battle 时 rushStage 为 undefined）/0/越界值一律回退或封顶到合法关卡，绝不出「第 0 关」或 NaN；未动任何敌人属性、连战奖励、掉落、难度缩放、升级曲线或战斗判定
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）**33 项断言全部通过**；新增 v3.15 专项冒烟（/tmp/jrpg_smoke_v315_rushstage.mjs）**26 项断言全过**——三关标签逐关正确且敌名一致、回合计数不干扰、rushStage 缺失/0/99/5 钳制无 NaN、主线魔王/洞窟领主/终焉之神/精英/普通怪五种非 rush 战斗均不显示、渲染不改动 rushStage/battleTurn/敌我血量、startRush 同步置位第 1 关

## v3.14 酿造界面信息透明回归（体验打磨·事故功能回归 v2.16）
- 【回归 v2.16】酿造锅界面（和平村酿造锅按 Enter）补齐两处信息：① 首行常驻 **已酿灵药 N 瓶**（与 HUD/战斗的 🧪 计数同源，一眼看清背包里已存几瓶、还差几瓶才够用，不用再自己记）；② 材料不足时红色提示不再笼统说「材料不足（需 2 株蘑菇 + 10 金币）」，直接报出**具体差额** `还差 X 株蘑菇、Y 金币`（`Math.max(0,…)` 钳制，零材料也不出负数/NaN）——此前想酿灵药必须自己心算配方差多少，与商店「不足」置灰、旅馆差额预警、技能菜单 ⛔ 同一套「信息透明」体系（v3.0 事故回退中连同 v2.16 一起丢失，本次按 v2.16 既定设计回归；v3.13 旅馆记录曾以「酿造差额提示」为参照，本次让该描述真正兑现）
- 改动范围：仅 `js/ui.js` 的 `drawBrew()` 一处（首行加一段 + 不足分支换差额文案）；未动任何酿造配方/价格/判定/数值逻辑，`brewNow()` 的「需 2 株蘑菇 + 10 金币」完全不变；旧存档缺 `potion2` 字段以 `||0` 容错显示 0，不会闪 NaN
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）**33 项断言全部通过**；新增 v3.14 专项冒烟（/tmp/jrpg_smoke_v314_brew.mjs）**21 项断言全过**——材料充足显「已酿灵药 1 瓶」且无「材料不足」、材料不足精确差额（2-1/10-4）、零材料钳制为 2 株蘑菇/10 金币无负数、旧存档缺 potion2 显 0 瓶无 NaN、多次渲染不改动画面上的蘑菇/金币/灵药/药水与 scene、三条渲染路径无异常、drawBrew 不改变 scene、brewNow 酿造契约（足料 / 缺料零消耗）不受影响

## v3.13 旅馆住宿信息透明化（体验打磨·信息透明）
- 【信息透明】旅店住宿界面补齐三行关键信息：① 常驻**当前 HP/MP**（`当前 HP x/max · MP y/max`）；② 受伤时显示本次住宿将确切恢复多少（`今晚将恢复 HP +N MP +M`，与 `hpMax-hp` 同源精确到点数）；③ **金币不足**直接红字预警并报出差额（`金币不足（还差 N 金），无法入住！`）——此前住店只有「价格」一行，钱够不够、一晚能回多少血全靠猜或按了 Enter 才知道（Enter 才弹「金币不足！/精神饱满」）；现在进店一眼看清，与商店「不足」置灰、技能菜单 ⛔、酿造差额提示同一套「信息透明」体系；满状态时仍温和提示「你现在精神饱满，不需要休息。」
- 纯显示层改动：只改 `js/ui.js` 的 `drawInn()` 一处（面板内重排 5 行文案）；未动任何价格、住宿结算、金币判定、数值或逻辑（main.js 的 Enter 住宿判定完全不变），`Math.max(0,…)` 钳制恢复量防止异常状态闪负数
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）**33 项断言全部通过**；新增 v3.13 专项冒烟（/tmp/jrpg_smoke_v313_inn.mjs）**16 项断言全过**——满状态显「精神饱满」且不显恢复行/金币不足、受伤+钱够恢复量精确（41-18=23/16-11=5）、受伤+钱不够差额警告（10-5=5）且恢复行仍在、纯显示不改 hp/mp/gold、仅缺 MP 时 HP +0 MP +6、异常 hp>上限钳制 0 不出负数、三条渲染路径无异常、drawInn 不改变 scene

## v3.12 标题界面存档进度预览（体验打磨·信息透明）
- 【信息透明】标题画面选好存档槽后，槽位行下方常驻一行**当前槽位存档摘要**：`姓名 Lv.X 金币N 所在地图·主线进度`（如「诺亚 Lv.5 金币200 和平村·讨伐魔王中」），按 1/2/3 切换槽位时实时刷新——读档前一眼看清这个槽打到哪里了，不用先进游戏再退出；进度措辞与主线关卡门控完全同源：未击败 → 讨伐魔王中 / 仅洞窟领主 → 已击败洞窟领主 / 仅魔王 → 魔王已讨伐·探索隐藏 / 双徽记 → 讨伐终焉之神中 / 终焉已破 → 已征服终焉
- 改动范围：`js/core.js` 新增纯函数 `slotPreview(s)` 并导出（基于既有 `saveKey`/localStorage，读取解析后只做字符串拼接，**绝不读写或改动存档本身**，无/损坏/缺字段一律容错返回 null 或补 0）；`js/ui.js` 的 `drawTitle()` 加三行展示（有摘要才绘制，无存档整行留空，不挤占「按 L」提示）；未动任何存档结构、数值、战斗、难度或其它界面
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）**全部通过**；新增 v3.12 专项冒烟（/tmp/jrpg_smoke_v312_titlepreview.mjs）**15 项断言全过**——空槽/损坏 JSON/缺 G 字段返回 null、五种主线进度措辞逐一对上、地图名映射（和平村/幽暗森林/宝藏洞窟）、旧存档缺 gold/map 字段容错（金币0 且无 NaN）、saveGame→slotPreview 回路读到最新等级金币、切换槽位实时命中、有无存档两种情况下 drawTitle 渲染均无异常


## v3.11 击败后场地清理回归（体验打磨·信息真实）
- 【打磨】洞窟领主祭坛（`TY.MB`）与终焉水晶（`TY.SB`）被击败后，不再在地图上持续发光：与主线魔王祭坛「被击败后回退为普通地面」的既有模式对齐，两者分别回退为**洞窟岩地**（`TY.CAVE`）；小地图同步转回岩地色。此前只有主线 BOSS 祭坛有这项视觉清理，MB/SB 击败后仍常亮，容易误导玩家「还能再打 / 还有未开要素」——实际 `onStep` 本就按 `caveBoss/trueBoss` 旗标门控、不会重复开战，本次只是让**画面与真实进度一致**
- 改动范围：仅 `js/ui.js` 两处——`drawWorld()` 新增两行覆盖绘制（与既存 `TY.BOSS` 回退相邻）＋ `drawMinimap()` 小地图配色三目运算；未动任何 `onStep`/`winBattle` 判定、数值、掉落、难度或存档结构；击败后进洞重新读图也能正确回退（覆盖绘制按旗标实时判定，不依赖 `winBattle` 临时改迷宫）
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）**33 项断言全部通过**；新增 v3.11 专项冒烟（/tmp/jrpg_smoke_v311_altar.mjs）**15 项断言全过**——未击败时 MB/SB 各自专属发光图块（≠岩地）、caveBoss 置位后祭坛回退岩地而水晶保持发光、trueBoss 置位后水晶也回退、小地图色块同步（紫 #b06ff0 / 金 #ffe94a → 岩地 #39414f）、普通岩地参照全程不变、三帧渲染无异常（坐标从迷宫运行时推导，不硬编码）

## v3.10 商店药水恢复量透明化（体验打磨·信息透明）
- 【信息透明】杂货商店的 **🍖 生命药水** 条目补齐实际恢复量：文案从「生命药水 ×1」升级为「生命药水 ×1（恢复 50%HP +8）」，与 `takePotion()` 的实际结算公式**完全同源**（50% 最大 HP + 固定 8 点），买前一眼看清一瓶能回多少血；与酿造界面已有的「高级灵药：恢复 80% HP + 40% MP」互相印证，普通药水/高级灵药两大补给品的疗效从此全部公开可见，延续攻击预览、战利品预览、敌方等级标定等「信息透明」体系
- 改动范围：仅 `js/ui.js` 的 `openShop()` 一处商品文案；未动任何价格、恢复数值、战斗结算、掉落或酿造逻辑——纯显示层，零判定风险
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟 **33 项断言全部通过**；v3.9 蘑菇 HUD 专项冒烟 **7 项断言全过**

## v3.9 背包蘑菇计数回归 HUD（体验打磨·事故功能回归 v2.15）
- 【回归 v2.15】顶部 HUD 的背包栏重新上线常驻 **🍄 魔法蘑菇** 计数，与 🍖 生命药水、🧪 高级灵药 并列。蘑菇既是村长支线任务物品、又可在杂货铺出售换金、还是酿造高级灵药的核心材料——此前只能靠拾取弹窗/酿造界面/村长对话才知道手里有几株；现在背包里有多少一目了然（v3.0 事故回退中连同 v2.15 一起丢失，本次按 v2.15 既定设计回归）
- 零成本回归：`index.html` 页面壳里的 `<span id="s-mushroom">` 元素在 v3.0 重建时一直保留着（页面壳非事故波及范围），只是 `renderHUD()` 缺了这一行写入——补上后蘑菇计数立即常驻显示，含 `G.mushrooms||0` 容错（旧存档缺该字段时显示 0，不闪 NaN）
- 改动范围：仅 `js/ui.js` 的 `renderHUD()` 一处（新增一行写入）；未动任何任务/掉落/商店/酿造判定、数值与逻辑零改动，页面壳零改动
- 验证：`node --check js/ui.js` 通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）**33 项断言全部通过**；新增 v3.9 专项冒烟（/tmp/jrpg_smoke_v39_mushroom.mjs）**7 项断言全过**——开局 0、拾取 3、酿造后剩 1、旧存档缺字段容错 0、既有金币/药水/灵药字段不受影响、渲染调用无异常、页面壳元素契约在位

## v3.8 宝藏洞窟专属 BGM（体验打磨·事故功能回归 v2.11）
- 【回归 v2.11】进入「宝藏洞窟」后背景音乐不再套用幽暗森林曲目，改为**专属洞窟曲目**：更慢（step 0.32，森林曲为 0.24）、更低沉神秘——A 小调低音律动（A₂=110Hz / G₂=98Hz 副低音衬托）+ 缓速 sine 主旋律（A₃/G₃ 漂浮），贴合「地底深处 + 终焉水晶」的氛围；和平村、幽暗森林、战斗曲目保持原样（v3.0 事故回退中连同 v2.11 一起丢失，本次按 v2.11 既定设计回归）
- 接入点零成本：BGM 分派本就由 `bgmFromScene()` 依据 `curMap` 决定，而进洞/出洞/战后返回/读档/取消静音都走 `resumeBgm()`——新增 `MUSIC.cave` 曲目后只需在 `bgmFromScene()` 加一个 `cave` 分支，全部路径自动切换，无需改任何转场/战斗逻辑
- 改动范围：仅 `audio.js` 一处（新增曲目数据 + `bgmFromScene` 一个分支）；未动任何数值、掉落、难度、剧情、界面或其它曲目
- 验证：`node --check js/*.js` 全部 8 个模块通过；ESM 全量冒烟 **33 项断言全部通过**；新增 v2.11 专项冒烟（/tmp/jrpg_smoke_v211_bgm.mjs）**18 项断言全过**——MUSIC.cave 曲目结构（step 0.32 / sine / seq+bass 合法 / 低音区）、bgmFromScene 四场景分派（battle/village/dungeon/cave + 未知地图回退）、startBgm('cave')+bgmTick 运行、stopBgm 清理定时器、洞窟内 resumeBgm 切到 cave、离开后回 dungeon、静音态安全

## v3.7 冒险时长上线（体验打磨）
- 【新特性】兑现 `newGame()` 里预留但从未使用的 `G.time` 字段——主渲染循环 `render()`（ui.js）按真实流逝时间累加冒险时长，单帧增量封顶 60 秒（切后台/卡顿后恢复不会时间跳变）。**状态界面（I）** 的金币行尾部新增 `⏱️ HH:MM:SS` 实时显示，**尾声战绩页**也追加显示；时长随存档持久化（saveGame 本就整存 G），旧存档缺 `time` 字段也会在首次渲染时自动补 0，读档/重开流程不受影响
- 纯体验打磨：只改 `js/ui.js` 一处（新增模块级 `_timePrev` + 纯函数 `fmtTime` 并导出 + `render()` 走时 + `drawStatus`/`drawEnding` 两行展示）；未动任何数值、掉落、难度、战斗判定、升级曲线或存档结构
- 验证：`node --check js/ui.js` 通过；ESM 全量冒烟 **33 项断言全部通过**；新增 v3.7 专项冒烟（/tmp/jrpg_smoke_v37_time.mjs）**18 项断言全过**——fmtTime 全分支（0/59/60/3661/7325/86399 及负值与 undefined/null 容错）、render() 走时（首帧合法、多帧单调不减、模拟 10 分钟间隔增量被封顶 60s、旧存档缺 time 自动初始化）、drawStatus/drawEnding 渲染无异常、newGame time===0 契约不变

## v3.6 升级攻防同步修复 + 成长明细回归（数值修复·事故功能回归 v2.10）
- 【关键修复·回归 v2.10】**攻防不同步 Bug 根治**：`winBattle()` 的升级循环此前只刷新 HP/MP 上限、从不刷新攻击/防御——`da/dd`（攻防成长量）算了却没生效，`G.atkMax/G.defMax` 永远停留在 Lv.1 的旧值，怪物却随等级渐强（`MON_BASE` 线性成长），导致「妖怪变强、主角原地踏步」，中后期战斗被严重拖难。现每次升级都按 `baseStats(新等级) + 装备` **重算四项上限**（攻击防御一并刷新），`baseStats` 的 攻+2/级、防+2/级 曲线真正生效，成长曲线与怪物难度曲线恢复同步（v3.0 事故回退中连同 v2.1–v2.17 一起丢失，本次按 v2.10 既定设计回归）
- 【打磨】升级提示不再只说「属性上升」——直接列出本次实际获得的成长明细：`🎉 等级提升到 Lv.N！HP+x MP+x 攻+x 防+x`（一次升多级自动汇总，与 `baseStats` 相邻等级差额完全一致；Boss 战保留「你终于可以……」后缀）。顺带清理循环内一行重复的 `hpMax/mpMax` 冗余赋值
- 改动范围：仅 `battle.js` 的 `winBattle()` 升级循环一处；未动任何 `baseStats` 成长公式、怪物属性、经验/金币曲线、掉落、成就判定或难度倍率；Lv.1 开局与读档流程（本就走 `applyStats`）不受影响
- 验证：`node --check` 全部 8 个模块通过；新增 v2.10 专项冒烟（/tmp/jrpg_smoke_v210_upgrade.mjs）**15 项断言全过**——Lv.1 基线攻防正确、多段升级（xp400 → Lv.7）攻 11→23 / 防 6→18 随 `baseStats+装备` 精确重算、升级提示含 HP+/MP+/攻+/防+ 明细且多级汇总一致、边界升级 Lv.1→2 明细精确为 攻+2 防+2、经验不足不升级攻防不变；ESM 全量冒烟 **33 项** + v3.5/v3.3/v3.2/v3.1 专项冒烟（16+25+22+12 项）全部保持通过

## v3.5 试炼碑引导回归（体验打磨·事故功能回归 v2.14）
- 【回归 v2.14】宝藏洞窟中央的**试炼碑**不再靠「踩上去才知道」：试炼碑上方常驻一枚状态标签——未具备资格时显灰黄「试炼·未解锁」，魔王+洞窟领主双倒后转绿色「⚔️ 试炼·可挑战」，与 `onStep` 的真实资格判定（`bossDefeated && caveBoss`）**完全同源**，一眼看清试炼场还差哪枚徽记、现在能不能打，延续 v2.9 强敌祭坛 Lv 标签的「信息透明」体系，同时兑现 README「强敌祭坛与试炼碑状态标签」的既有描述（v3.0 事故回退中一并丢失，本次按 v2.14 既定设计回归）
- 【回归 v2.14】帮助系统 H 新增第 4 页「试炼与进阶」：试炼碑位置/双徽记条件、试炼三连战奖励、被强敌击败按 B 重整旗鼓、快速旅行 T、支线蘑菇宝箱小地图金光提示，全部新特性落进文档；翻页 UI 复用既有 `helpPage` 分页，`HELP_PAGES.length` 自动适配，零新增界面代码
- 标签只在试炼碑进入视野时才绘制（绘制循环限定在当前可见区块内），宝藏洞窟外/画面外零开销；站在碑上时标签位于角色头顶之上不遮挡操作
- 改动范围：`data.js`（HELP_PAGES 新增第 4 页）、`ui.js`（`drawWorld` 内一处标签绘制）；未动任何敌人属性、掉落、经验/金币、难度缩放、Boss/试炼判定或升级曲线
- 验证：`node --check` 两个改动模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）既有 **33 项断言全部通过**；v3.5 专项冒烟（/tmp/jrpg_smoke_v35_trial.mjs）**12 项断言全过**——两档状态标签（未解锁/可挑战）绘制路径无异常、非洞窟地图与画面外不绘制、HELP 第 4 页存在且内容齐、helpPage 越界取模安全、标签文字不抛错等

## v3.4 战斗回合计数回归（体验打磨·事故功能回归 v2.13）
- 【回归 v2.13】战斗界面**左上角常驻 `⚔️ 回合 N` 计数**重新上线：每场战斗从第 1 回合起算、玩家每行动一次 +1——长盘 Boss 战 / 试炼三连战一眼看出胶着程度（已经打到第几回合、还能撑多久），与威胁预警、中毒回合数、二段变身线的「信息透明」体系同风格（v3.0 事故记录中点名回退的功能，本次按 v2.13 既定设计回归；ESM 冒烟基线文件里「battleTurn 断言待回归」的注释随之兑现）
- 生命周期严格限定在单场战斗内：`startBattle` 每场重置为第 1 回合（含 Boss 重试、试炼连战每一关、读档后新开的战斗），玩家任意指令（攻击/技能/药水/防御/蓄力/逃跑）在 `playerAction` 起手处 +1；战斗结束（胜利/死亡/逃跑）后随战斗界面一起消失，不会跨战斗残留；Boss 战气场压制按 4 不耗回合的既有逻辑完全不受影响
- 改动范围：`state.js`（注册全局 `battleTurn`，仅一行声明）、`battle.js`（`startBattle` 置 1 / `playerAction` 起手 +1）、`ui.js`（`drawBattle` 左上角一处绘制）；未动任何怪物属性、掉落、经验/金币、难度缩放、升级曲线或战斗判定
- 验证：`node --check` 全部 3 个改动模块通过；ESM 全量冒烟 **33 项断言全部通过**（新增 战斗回合计数 5 项：残留值 99 开战重置为第 1 回合、drawBattle 含计数绘制无异常、行动后 +1 进第 2 回合、计数不影响普攻伤害结算、再次开战重新起算），连同遇敌分布、难度倍率、Boss 战、图鉴报酬、传送碰撞、技能菜单、v3.0 美术渲染等既有回归全部保持通过；v3.1/v3.2/v3.3 专项冒烟（攻击预览/毒蛇/敌方等级标定）也全部通过

## v3.3 敌方等级标定修复（显示修复·回归 v2.3）
- 【修复】战斗画面的敌方等级长期显示不一致：只有主线幽冥魔王写死 `Lv.8`，而洞窟领主、隐藏真Boss「终焉之神」、试炼连战三名 Boss、「石心魔像」精英，以及 Boss 二段「真身」形态，全部误显示**玩家当前等级**——Lv.1 就撞见终焉之神时它会显示 `Lv.1`，与威胁预警、战力对比、战利品预览的「信息透明」体系冲突，严重误导威胁判断（v3.0 事故回退中一并丢失，本次按 v2.3 既定设计回归）
- 新增纯展示函数 `enemyLv()`（`ui.js`）统一标定：终焉之神 `Lv.12` / 幽冥魔王 `Lv.8` / 洞窟领主 `Lv.7`（试炼连战按对应 Boss 同值），**真身改名后仍正确**（`幽冥魔王·真身`↔8、`洞窟领主 ·真身`↔7、`终焉之神·祸乱形态`↔12）；精英「石心魔像」= 玩家等级 +1；普通随机怪仍随玩家等级渐进，曲线不变
- 顺带 Boss 名**紫色高亮扩展到四类 Boss**（`isBoss`/`isTrue`/`isCaveBoss`/`isRush`），此前只有主线魔王是紫色
- 纯显示层改动：只改 `ui.js`（新增并导出一个纯函数 + `drawBattle()` 敌名一行），未动任何敌人属性、变身机制、经验/金币、掉落或难度缩放数值
- 验证：`node --check js/ui.js` 通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）既有回归**全部通过**；新增 v3.3 专项冒烟（/tmp/jrpg_smoke_v33_enemylv.mjs）**25 项断言全过**——enemyLv 全部分支（三强敌/六种真身改名写法/精英/普通怪/等级不随玩家漂移）、四类 Boss + 精英 + 普通怪 + 两种真身形态共 9 条 drawBattle 渲染路径无异常

## v3.2 毒蛇剧毒机制（新战斗机制·v3.0 事故功能回归）
- 【新机制回归·v2.12】毒蛇终于名副其实：被咬中有 **35% 概率中毒 3 回合**（概率来自 `MON_BASE` 数据条目 `poison:0.35`）。中毒后**每回合起手先毒发扣 5% 最大 HP**（保底 2 点），毒发不夺走本回合行动——中了毒照样能攻击/喝药/防御/蓄力自救，但会持续失血，逼着你在「速战速决」与「喝药续命」间抉择；毒发致死正常进入死亡结算。毒蛇本就是高攻怪，这一刀让「行走幽暗森林/宝藏洞窟时备好药水」的紧张感落到实处（v3.0 事故中被回滚，本次按 v2.12 既定设计回归）
- 状态透明：战斗界面新增**绿色脉动**的 `☠️ 中毒 N 回合 · 每回合扣血` 常驻提示（与蓝色盾、金色蓄力区分；回合计时随毒发实时递减），战斗日志同步记录「☠️ 毒素发作」与「中了【毒】」；帮助系统 H 新增第 3 页「魔物与状态」（毒蛇中毒、中毒自救、高级灵药、魔物强度），翻页 UI 复用既有 `helpPage` 分页
- 生命周期严格限定在单场战斗内：开战/胜利/死亡/成功逃跑均清零，不会跨战斗残留；毒发结算只在 `playerAction()` 回合起手处执行、施毒只在 `enemyAct()` 攻击命中分支触发（仅携带 `poison` 标记的毒蛇），与既有的防御/蓄力/反击/二阶段结构完全兼容
- 改动范围：`data.js`（毒蛇 `poison:0.35` + HELP 第 3 页）、`core.js`（newGame 初始 `poison:0`）、`battle.js`（遇敌深拷贝携带标记 / startBattle 清零 / 回合起手毒发 / 毒蛇攻击施毒 / 胜利·逃跑·死亡清毒）、`ui.js`（战斗界面中毒提示）、`README.md`（战斗说明 + 帮助页数如实同步）；未动任何数值曲线、掉落、经验/金币、难度缩放，非毒蛇魔物属性与行为完全不变
- 验证：`node --check` 全部 4 个改动模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）既有回归**全部通过**；新增 v3.2 专项冒烟（/tmp/jrpg_smoke_v32_poison.mjs）**22 项断言全过**——数据层仅毒蛇带 poison 0.35、HELP 第 3 页、newGame 初始 0、遇敌深拷贝仅毒蛇保留标记（含精英不带）、开战清零、毒发扣 5% 保底 2 + 回合递减 + 不夺行动、毒发日志、毒发致死进死亡结算并清毒、enemyAct 100% 与 0% 双路径施毒、胜利清毒、逃跑清毒、中毒/无中毒两档 drawBattle 渲染无异常

## v3.1 攻击指令伤害预览回归（体验打磨·事故功能补回）
- 【信息透明·回归 v2.17】战斗指令栏的 `[1]攻击` 重新带期望伤害预览 `[1]攻击≈N伤`——v3.0 事故记录中点名回退、待自动完善任务逐步补齐的功能，本次按 v2.17 既定设计重构回归。普攻与技能菜单（`≈N伤`）信息完全对齐，最常用的基础攻击从此也能一眼算出对当前敌人打多少血
- 数值与实战同源：新增纯函数 `atkEstimate(g,e)`（`battle.js`，与 `cmdDmg` 同一公式 `攻×2-防`、保底 1，不含 ±10% 浮动即其均值）；**蓄力中自动 ×2**（与 `attackMove` 的 charged 倍率同源），蓄力态下指令栏数字实时翻倍，配合既有「蓄力中 · 下击×2」提示一目了然；高防/缺字段一律容错保底 1，不闪 0/NaN，`G` 为 null 时返回 1
- 纯显示层：只改 `battle.js`（新增并导出一个纯函数）+ `ui.js` 的 `drawBattle()` 指令栏一处（`enemy` 存在才显示预览，Boss 战/无敌人时退化为原「攻击」字样）；未动任何伤害公式、怪物属性、掉落、经验/金币、难度缩放或战斗判定
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟（/tmp/jrpg_smoke_esm.mjs）既有回归全过；新增 v3.1 专项冒烟（/tmp/jrpg_smoke_v31_atk.mjs）12 项断言全过——atkEstimate 与 cmdDmg 期望一致（30攻/10防→50）、高防保底 1、无防按攻×2、蓄力 ×2（50→100）、G 缺失/缺 atkMax/enemy 缺失三类容错、drawBattle 含 enemy·蓄力·无 enemy 三条渲染路径均无异常

## v3.0 美术资源全面升级（美术）
- **全部 17 种图块重绘**（`ui.js` TILE）：草地加层次/小花/卵石/露珠光点，树木加树影/多层树冠/受光面，岩石加裂缝高光与投影，水波加波光，路径加边缘暗化，宝箱加金属镶边与宝光，喷泉加石槽/水花/喷涌，传送门改漩涡光晕，建筑加门窗/檐线/灯火，终焉水晶与祭坛加光晕
- **宝藏洞窟地下主题化**：新增 `CAVE`（岩地+荧光晶尘）与 `CAVEWALL`（洞窟岩壁）两种图块，`loadMap('cave')` 将草地→岩地、边界树→岩壁，小地图配色与已开宝箱显示同步适配；`dangerAt` 遇敌判定跟随新图块（玩法/掉落/遇敌概率不变）
- **主角精灵重绘**（`drawHero`）：脚下阴影 + 轮廓描边 + 肩部受光 + 腰带/深色裤分层；**随装备换装**——衣甲配色（布衣蓝/皮甲棕/锁子甲钢色/龙鳞甲绿）与剑刃外观（木剑→铁剑→秘银→勇者之剑→**圣剑金色辉光**）随 `G.weapon/G.armor` 实时变化；待机呼吸 + 行走腿部交替小动画
- **怪物按物种差异化造型**（`drawMonster`）：史莱姆黏液高光与底部鼓包、野狼耳鼻尾、骷髅兵深眼眶/牙齿/肋骨、哥布林大耳獠牙、毒蛇竖瞳分叉舌、树精树冠枝干、石魔像裂纹苔点；魔王级红眼+角，终焉之神升级为**金色神环 + 大展双翼**，洞窟领主加肩刺；全部怪物补瞳孔与脚下阴影，受击白闪移至最上层覆盖全细节
- **战斗场地氛围**：敌我身后径向柔光 + 地面光圈，增强空间纵深
- **标题页远景**：双层远山剪影 + 雪顶微光 + 地平线微光地面
- 纯美术改动：只动 `js/ui.js` / `js/data.js`（新增 CAVE/CAVEWALL 图块类型）/ `js/world.js`（洞窟映射与遇敌判定适配），未改任何数值/掉落/难度/剧情；`node --check` 全过 + ESM 全量冒烟全部通过，并新增 @napi-rs/canvas 真实渲染脚本（/tmp/jrpg_viz）逐场景出图人工审查
- 【事故记录】开发过程中一次误将 `js/` 目录删除。已用「构建脚本从 v1.23 快照重建基线 → 逐条重放 import/美术补丁」的方式完整恢复本项目全部内容（v2.0 模块结构 + v3.0 美术），并补修一个此前潜伏的导入缺口（world.js 未导入 BOSS/CAVE_BOSS/TRUE_BOSS，踩魔王祭坛会抛 ReferenceError）。代价：删除时点在档的 cron 增量功能（约 v2.1–v2.17：毒蛇中毒/战斗回合计数/试炼引导/攻击伤害预览等）被回退，将由每小时自动完善任务在后续运行中逐步重新补齐（README/备份文件不受影响）。教训：后续任何涉及本项目的操作先 `git init` 或整目录副本兜底


## v2.17 攻击指令伤害预览（体验打磨）
- 【信息透明补全】战斗指令栏的 `[1]攻击` 现在带期望伤害预览：`[1]攻击≈N伤`——普攻从此不再是全游戏唯一「没有伤害预览」的动作。技能菜单（v1.29）里每个技能都标 `≈N伤`，能一眼算出性价比；唯独最常用的基础攻击要自己心算「攻×2-防」，与技能预览信息不对称。如今二者对齐：普攻同技能一样直接显示对当前敌人的平均伤害
- 数值与实战同源：预览来自新增纯函数 `atkEstimate(g,e)`（`battle.js`，与 `cmdDmg` 同一公式 `攻×2-防`、保底 1，不含 ±10% 浮动与暴击即其均值）；**蓄力中自动 ×2**（与实战 `attackMove` 的 charged 倍率同源），蓄力状态下指令栏数字实时翻倍，配合既有「蓄力中 · 下击×2」提示一目了然；高防/缺字段一律容错保底 1，不闪 0/NaN
- 纯显示层：只改 `battle.js`（新增一个纯函数并导出）+ `ui.js` 的 `drawBattle()` 指令栏一处（`enemy` 存在才显示预览）；未动任何伤害公式、怪物属性、掉落、经验/金币、难度缩放或战斗判定，Boss 战 ⛔ 逃跑压制标记仍按真实 `bar` 宽度定位不受影响
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟 **91 项断言全部通过**——新增 攻击预览 8 项（atkEstimate 与 cmdDmg 均值一致、蓄力 ×2、高防保底 1、无防御按攻×2、G 缺失/缺 atkMax 容错、drawBattle 含预览+Boss+蓄力渲染无异常），连同遇敌分布、难度倍率、Boss 战、中毒、回合计数、试炼碑引导、酿造界面、蘑菇 HUD、图鉴、四场景 BGM、状态界面等既有回归全部保持通过

## v2.16 酿造界面信息补全（体验打磨）
- 【信息透明补全】酿造锅界面（和平村酿造锅按 Enter）此前是全游戏唯一「信息不透明」的补给界面：不显示背包已酿好的高级灵药存量、材料不足时也只笼统说「材料不足」——想酿灵药必须自己记背包里有几瓶、还得心算「2 株蘑菇 + 10 金币」差多少。如今补齐两处：① 配方说明行直接显示 `已酿灵药 N 瓶`（与战斗/HUD 的 🧪 计数同源，一眼看清已存几瓶、还差多少）；② 材料不足时红色提示直接报出具体差额 `还差 X 株蘑菇、Y 金币`（`Math.max(0,…)` 钳制，零材料时也不会出现负数/NaN），不用再心算配方
- 纯显示层改动：只改 `ui.js` 的 `drawBrew()` 一处、未动任何酿造判定/数值/掉落/任务逻辑，`brewNow()` 的「需 2 株蘑菇 + 10 金币」配方完全不变；旧存档缺 `potion2` 字段时以 `||0` 容错显示 0，不会闪 NaN
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟 **83 项断言全部通过**——新增 酿造界面 8 项（材料充足/不足/零材料三档渲染路径、差额正值计算、Math.max 边界钳制、旧存档缺 potion2 容错、纯显示不改数值），连同遇敌分布、难度倍率、Boss 战、中毒、回合计数、试炼碑引导、蘑菇 HUD、图鉴、四场景 BGM、状态界面等既有回归全部保持通过

## v2.15 背包蘑菇计数入驻 HUD（体验打磨）
- 状态栏新增常驻 **🍄 魔法蘑菇** 计数：与 🍖 生命药水、🧪 高级灵药 并列显示。蘑菇既是村长支线任务物品、又可在杂货铺出售换金、还是酿造高级灵药的核心材料——此前只能靠拾取弹窗/酿造界面/村长对话才知道手里有几株，现在背包里有多少一目了然
- 纯显示层：`index.html` 新加一个 `<span>`（只在页面壳里声明元素，未写任何逻辑）、`ui.js` 的 `renderHUD` 补一行写入（含 `G.mushrooms||0` 容错，旧存档缺该字段时显示 0）；未动任何任务/掉落/商店/酿造判定，数值与逻辑零改动
- 验证: `node --check` 全部 8 个模块通过；ESM 全量冒烟 **75 项断言全部通过**——新增 蘑菇计数 5 项（0 株显示 0 不抛错、拾取 3 株正常显示、不影响既有金钱/灵药显示、旧存档缺字段容错显示 0），连同遇敌分布、难度倍率、Boss 战、毒蛇中毒、回合计数、试炼碑引导等既有回归全部保持通过

## v2.14 试炼碑引导（体验打磨）
- 宝藏洞窟中央的**试炼碑**终于不再靠「踩上去才知道」：试炼碑上方常驻一枚状态标签——未具备资格时显灰黄「试炼·未解锁」，魔王+洞窟领主双倒后转绿色「⚔️ 试炼·可挑战」，与 `onStep` 的真实判定（`bossDefeated && caveBoss`）完全同源，一眼看清试炼场到底还差哪枚徽记、现在能不能打，延续 v2.9 强敌祭坛 Lv 标签的「信息透明」体系
- 帮助系统 H 新增第 4 页「试炼与进阶」：试炼碑位置/双徽记条件、试炼三连战奖励、被强敌击败按 B 重整旗鼓、快速旅行 T、支线蘑菇宝箱小地图提示，新特性全部落进文档；翻页 UI 复用既有 `helpPage` 分页，零新增界面代码
- 纯 `ui.js` 一处绘制 + `data.js` 一页文案：未动任何触发判定、敌人属性、掉落、经验/金币、难度缩放或战斗逻辑；单格试炼碑不重复叠标
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟 **71 项断言全部通过**——新增 试炼碑引导 6 项（HELP 第 4 页存在/含重整旗鼓条目、drawWorld 未解锁与可挑战两条渲染路径、资格判定与 onStep 同源），连同遇敌分布、难度倍率、Boss 战、中毒、回合计数、升级结算、传送碰撞、图鉴、四场景 BGM、状态界面等既有回归全部保持通过

## v2.13 战斗回合计数（体验打磨）
- 战斗界面**左上角常驻 `⚔️ 回合 N` 计数**：每场战斗从第 1 回合起算、玩家每行动一次 +1——长盘 Boss 战 / 试炼三连战一眼看出胶着程度（已经打到第几回合、还能撑多久），与威胁预警、中毒回合数、二段变身线等「信息透明」体系同风格
- 生命周期严格限定在单场战斗内：`startBattle` 每场重置为第 1 回合（含 Boss 重试、试炼连战每一关、读档后新开的战斗）；逃跑成功/胜利/死亡后随战斗结束自然不再显示，不会跨战斗残留；Boss 战气场压制按 4 逃跑不耗回合的既有逻辑完全不变
- 纯计数+显示层改动：`state.js` 注册全局 `battleTurn`（仅一行声明，不加任何游戏逻辑）、`battle.js` 的 `startBattle` 置 1 / `playerAction` 起手 +1、`ui.js` 的 `drawBattle` 左上角一处绘制；未动任何怪物属性、掉落、经验/金币、难度缩放、升级曲线或战斗判定
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟 **65 项断言全部通过**——新增 战斗回合计数 5 项（残留值重置为第 1 回合、drawBattle 含计数绘制无异常、行动后 +1 进第 2 回合、再次开战重新起算、击杀结算不受影响），连同遇敌分布、难度倍率、Boss 战、毒蛇中毒、升级结算、传送碰撞、图鉴、四场景 BGM、状态界面等既有回归全部保持通过

## v2.12 毒蛇剧毒机制（新战斗机制）
- 【新机制】毒蛇终于名副其实：被咬中有 **35% 概率中毒 3 回合**（概率随 `MON_BASE` 数据条目配置，`poison:0.35`）。中毒后**每回合开始先毒发扣 5% 最大 HP**（保底 2 点，至多约 15% 血的持续压力），毒发不夺走本回合行动——中了毒照样能攻击/喝药/防御/蓄力自救，但会持续失血，逼着你在「速战速决」与「喝药续命」之间做抉择；毒发致死会正常进入死亡结算。毒蛇本就是高攻怪，这一刀让「行走幽暗森林/宝藏洞窟时备好药水」的紧张感落到了实处
- 状态透明：战斗界面新增绿色脉动的 `☠️ 中毒 N 回合 · 每回合扣血` 常驻提示（区别于蓝色盾、金色蓄力），回合数随每次毒发实时递减；战斗日志同步记录「毒素发作」与「中了【毒】」
- 生命周期严格限定在单场战斗内：新开战斗/胜利/死亡/成功逃跑都会清零，不会跨战斗残留；毒发结算只在 `playerAction()` 回合起手处执行、施毒只在 `enemyAct()` 毒蛇攻击分支触发，两条路径互不干扰，与既有的防御/蓄力/反击回合结构完全兼容
- 信息补全：帮助说明 H 新增第 3 页「魔物与状态」（毒蛇中毒、灵药用法、魔物强度查询），翻页 UI 复用既有 `helpPage` 分页，零新增界面代码
- 改动范围：`data.js`（毒蛇 `poison` 字段 + HELP 新页）、`battle.js`（遇敌携带标记 / startBattle 清零 / 毒发结算 / 施毒分支 / 胜利·逃跑·死亡清毒）、`ui.js`（战斗界面中毒提示）、`core.js`（newGame 初始 `poison:0`）；未动任何数值/掉落/经验/金币/难度曲线，普通魔物（非毒蛇）属性与行为完全不变
- 验证：`node --check` 全部 8 个模块通过；ESM 全量冒烟 **60 项断言全部通过**——新增 毒蛇中毒 11 项（数据层仅毒蛇带 poison、HELP 第 3 页、遇敌深拷贝仅毒蛇保留标记、开战清零、毒发扣血+回合递减、毒发不夺行动、日志记录、enemyAct 100% 施毒路径、非毒蛇不施毒、毒发致死进死亡结算并清毒），连同遇敌分布、难度倍率、Boss 战、升级结算、传送碰撞、图鉴、四场景 BGM、状态界面等既有回归全部保持通过

## v2.11 宝藏洞窟专属 BGM（体验打磨）
- 进入「宝藏洞窟」后，背景音乐从幽暗森林曲目换为**专属洞窟曲目**：更慢（step 0.32）、更低沉神秘（A 小调低音律动 + 缓速 sine 主旋律，A₁/G₁ 副低音衬托），与「地底深处 + 最终水晶」的氛围匹配；幽暗森林（step 0.24 三角波）与和平村原曲保持不变
- 接入点零成本：BGM 分派本就由 `bgmFromScene()` 依据 `curMap` 决定，且所有转场/战后返回/读档/取消静音都走 `resumeBgm()`——新增 `MUSIC.cave` 曲目后在 `bgmFromScene()` 加一个 `cave` 分支即可，进洞、出洞、战后、读档、M 键恢复静音全部自动切换，无需改任何转场/战斗逻辑
- 纯 `audio.js` 一处改动（新增曲目数据 + 一个分支），未动任何数值、掉落、难度、剧情或界面；`startBgm/stopBgm/bgmTick` 定时器行为不变
- 验证：`node --check js/audio.js` 通过；ESM 全量冒烟 **49 项断言全部通过**——新增 8 项（bgmFromScene 四场景分派 battle/village/dungeon/cave、MUSIC.cave 曲目结构、resumeBgm 洞窟内切到 cave 曲目、stopBgm 清理定时器、音频模块加载），连同遇敌分布、难度倍率、Boss 战、升级结算、传送碰撞、图鉴、状态界面、祭坛标签等既有回归全部保持通过

## v2.10 等级提升成长明细 + 攻防不同步修复（数值修复 + 体验打磨）
- 【修复】战斗胜利的升级结算此前**只刷新 HP/MP 上限、从不刷新攻击/防御**：`winBattle()` 的升级循环里 `da/dd` 算了却没生效，`G.atkMax/G.defMax` 一直停留在上一次 `applyStats()` 的旧值（通常就是开局 Lv.1）——怪物随等级变强，主角战力却原地踏步；状态面板还会出现「基础攻+武器 ≠ 总攻」的错位（显示值按新等级算、实际战斗用的是旧值）。现在升级后统一 `applyStats(G)` 重算四项上限，`baseStats` 的攻+2/级、防+2/级曲线真正生效，成长曲线与怪物难度曲线恢复同步
- 【打磨】升级提示不再只说「属性上升」——直接列出本次实际获得的成长明细：`🎉 等级提升到 Lv.N！HP+x MP+x 攻+x 防+x`（一次升多级自动汇总；Boss 战保留「你终于可以……」后缀）。新增纯函数 `levelGainFor(fromLv,toLv)`（`battle.js`）：按 `baseStats` 相邻等级差额汇总成长量，与结算循环真实变化完全一致，零外部依赖，供胜利结算展示
- 纯结算层改动（只改 `battle.js` 的 `winBattle()`/新增一个纯函数）：未动任何 `baseStats` 成长公式、怪物属性、经验/金币、掉落、成就判定或难度倍率；顺带清理了循环里一行重复的 `hpMax/mpMax` 冗余赋值
- 验证：`node --check js/*.js` 全部通过；ESM 全量冒烟 **41 项断言全部通过**——新增 等级提升成长明细 7 项（levelGainFor 同级为 0 / 1→3 与 baseStats 差额一致含攻防 / 下限钳制 Lv.1 / 真实升级结算触发 Lv.1→2 / 升级后 atkMax·defMax·hpMax·mpMax 全部刷新 / 升级提示列出 HP+·攻+·防+ 实际数值），连同遇敌分布、难度倍率、Boss 战、传送碰撞、图鉴、状态界面、祭坛标签等既有回归全部保持通过

## v2.9 强敌祭坛等级标注（体验打磨）
- 漫游世界时，**未讨伐**的强敌祭坛上方现在常驻一枚深红底金字标签 `⚠ Lv.X`：幽暗森林魔王祭坛 `Lv.8`、宝藏洞窟洞窟领主祭坛 `Lv.7`、终焉水晶 `Lv.12`——不必走近踩上、也不用打开图鉴/帮助，远远一眼就能看见强敌所在与建议等级，判断「现在实力够不够、值不值得深入」，与 v1.19 战斗威胁预警 / v1.8 小地图颜色 / v2.3 敌方等级标定的「信息透明」风格同体系
- 已讨伐的祭坛标签自动熄灭（魔王已倒祭坛灰烬、洞窟领主/终焉之神击败后祭坛清除），与真实剧情进度严格同步，不会出现「已打完还挂个警告」
- 推荐等级与 v2.3 `enemyLv` 标定同源（幽冥魔王 Lv.8 / 洞窟领主 Lv.7 / 终焉之神 Lv.12）；标签只画每块祭坛的**左上角**（竖排 2 格 / 2×2 祭坛不重复叠标），随摄像机可视范围绘制，纯显示层改动，只改 `ui.js` 的 `drawWorld()` 一处，未动任何敌人属性、触发判定、经验/金币、掉落或难度数值
- 验证：`node --check js/*.js` 全部通过；ESM 全量冒烟 **34 项断言全部通过**——新增 魔王祭坛未讨伐/已讨伐、洞窟领主+终焉水晶双标签/全熄灭 四条 `drawWorld` 渲染路径，连同遇敌分布、难度倍率、传送碰撞、战斗胜利、状态界面等既有回归

## v2.8 状态界面冒险进度一览（体验打磨）
- 状态界面（`I`）在「下一技能」与「任务目标」之间新增一行「冒险进度」：**魔王 / 洞窟领主 / 终焉之神 / 试炼场** 四枚徽记一次看清——已达成亮绿 `✓`，未达成灰色 `✗`。此前状态界面只能看到当前任务目标，隐藏内容（洞窟领主打没打、终焉之神有没有资格、试炼场通没通）毫无线索，只有去踩对应地点才知道。现在打开角色面板即可判断「还差什么、值得去刷什么」，与「信息透明」风格同体系
- 新增纯函数 `adventureProgress(g)`（`core.js`）：不读任何界面状态、只读 `G` 上的推进标志（`bossDefeated/caveBoss/trueBoss/rushDone`），返回 4 组 `[名称, 是否达成]`；未达成置灰交给 `drawStatus()` 绘制。纯显示层，未动任何推进/判定/成就/数值逻辑；顺手把原本两行的「任务目标」压缩为单行、成就行下移 6px 避让，面板垂直空间不超界
- 数据源与成就/标题摘要完全同源，徽记状态永远和真实剧情进度一致，不会出现两套说法
- 验证：`node --check js/*.js` 全部通过；ESM 全量冒烟 **30 项断言全部通过**——新增 adventureProgress 四项分支（四徽记长度/开局全灰/全达成全绿/仅魔王亮一枚）与状态界面 开局·半程·全达成 三档渲染回归

## v2.7 图鉴未讨伐项报酬参考（体验打磨）
- 【补全】图鉴报酬参考此前只挂在**已讨伐**项下（v1.15），v2.4 加灰色「未讨伐」占位时也一并去掉了参考行——于是「图鉴征服」最需要的恰恰拿不到：未收集的魔物完全查不到「击败它能得多少经验/金币」，值不值得专程去刷只能瞎猜。现在**未讨伐项同样显示参考行**：`→ 击败可得：经验 N · 金币 N（参考）`（灰字跟随占位行走），已讨伐与未讨伐两套信息终于补全
- 复用既有 `monReward()`（本就覆盖全部 11 种：7 基础怪 / 石心魔像 / 三大 Boss），参考值随玩家当前等级实时计算，与真实结算同源——纯显示层改动，只改 `drawCodex()` 一处分支（`rw` 改为无条件计算 + 未讨伐分支补一行绘制），未动任何怪物属性、掉落、经验/金币、成就判定或滚动逻辑
- 验证：`node --check js/*.js` 全部通过；ESM 全量冒烟 **18 项断言全部通过**——新增 未讨伐 7 种/精英/三Boss 报酬参考行绘制、已讨伐参考行回归、空图鉴与全收集绘制、滚动越界钳制、16 场景渲染、遇敌/胜利/存档读档回归

## v2.6 标题画面存档进度摘要（体验打磨）
- 标题画面选中已存档的槽位时，槽位行下方新增一行灰蓝小字进度摘要：`角色名 Lv.N · 💰金币 · 所在地图 · 主线进度 · 成就数`（如 `勇者 Lv.6 ⚡ · 💰500 · 和平村 · ⚔️ 魔王已讨伐 · 成就2/14`）——多存档槽再也不用靠猜「槽2 存的是哪个档」，读档前一目了然；困难模式附 ⚡ 标记
- 进度自动归类：冒险中 / ✅ 支线完成 / ⚔️ 魔王已讨伐 / 🎯 讨伐终焉之神 / ⭐ 真·通关；「按 L 读取当前槽存档」提示相应下移 14px 避让
- 新增纯函数 `slotMeta(s)`（`core.js`）：只读 localStorage + JSON 解析，解析失败/无存档返回 null，不改任何存档/读档逻辑；`ui.js` 的 `drawTitle()` 一处绘制，纯显示层，未动任何数值/玩法
- 验证：`node --check js/*.js` 全部通过；ESM 全量冒烟 **51 项断言全部通过**——新增 slotMeta 九项分支（名称/Lv/金币/地图/进度/成就/难度/空槽/进阶进度腾挪）、标题带/不带存档两种绘制、存档读档往返、全部 14 个场景渲染、遇敌/战斗/技能菜单/逃跑回归

## v2.5 快速旅行目的地提示（体验打磨）
- 快速旅行（`T`）界面上，每张**已解锁**地图下方新增一行灰蓝小字提示：定位 / 推荐等级 / 特色（和平村「新手区·魔物较弱，适合补给与完成支线」、幽暗森林「推荐 Lv.3+ · 强魔物与稀有精英，深处祭坛有魔王」、宝藏洞窟「推荐 Lv.5+ · 魔物更强，中央有终焉水晶」），选目的地时一眼判断当前实力是否合适，避免新开档贸然传进高难地图被秒杀——与既有「信息透明」风格（战斗威胁预警 / 装备对比 / 地图难度曲线）同体系
- 新增纯数据 `MAP_HINT`（`data.js`，文案与 README / H 地图指南一致）；只改 `drawTravel()` 一处绘制，并顺手把面板纵向加大以容纳提示行（400×320 → 400×340，行距 52 → 58）
- 纯显示层改动：未动任何传送判定、解锁条件、怪物属性、掉落、经验/金币或难度缩放；未解锁目的地仍显示「？？？ · 未探索」
- 验证：`node --check js/*.js` 全部通过；ESM 全量冒烟 **14 项断言全部通过**——加载期初始化（G Lv.1 / scene=title）、beginAdventure 开局（金币30/火焰斩/village）、drawWorld / drawTravel（含 MAP_HINT 提示行，半解锁与全解锁两种路径）/ drawStatus / drawCodex / drawHelp / drawBattle 渲染、randomEncounter 生成合法敌人、startBattle 进入战斗、cmdDmg ≥1

## v2.4 图鉴未收集占位（体验打磨）
- 敌人图鉴（`B`）此前只列出**已击败**的魔物——「图鉴征服（收录全部 11 种）」成就缺哪几种完全无迹可寻，只能靠猜。现在固定按 `BESTIARY_TARGET` 全部 **11 种**排列：已讨伐的保持原样（亮色/👿 Boss 紫 + 讨伐数 + 经验金币参考行），未讨伐的以灰色「？名字 + 未讨伐」占位并同样附出没地点指引——一眼看清还差哪几种、该去哪刷
- 顺带修正两处一致性：① 三大 Boss（幽冥魔王/洞窟领主/终焉之神）统一以 👿 前缀 + 紫色高亮（此前洞窟领主/终焉之神因 `isBoss=/魔王/` 判断漏标）；② 累计讨伐统计与滚动提示均改为按全部 11 种计算（防未讨伐项产生 NaN/提示错位）
- 纯显示层改动：只改 `ui.js` 的 `drawCodex()` 一处，未动任何怪物属性、掉落、经验/金币、成就判定或滚动交互；空图鉴（从未击败）仍显示原友好引导页
- 验证：`node --check js/*.js` 全部通过；ESM 全量冒烟 **22 项断言全部通过**——启动初始化、图鉴四态（空/少量/滚动/全收集）、16 场景渲染、遇敌/奖励/真身名归一、存档读档往返

## v2.0 模块化重构（架构）
- **单文件 → ES Modules**：游戏代码从单个 `index.html` 的 `<script>` 拆分为按职责划分的 8 个模块（`js/data|audio|core|world|battle|ui|state|main.js`），数据与逻辑彻底分层，仍零依赖、零构建、离线可玩
- `data.js` 纯数据层（地图/NPC/装备/技能/敌人/剧情/成就/帮助 + `baseStats`/`learnsAt` 等纯函数）；逻辑模块按 成长(`core`)/世界(`world`)/战斗(`battle`)/界面(`ui`) 划分；`main.js` 负责按键分派与启动引导；`audio.js` 封装全部音效/BGM
- **共享状态**：ESM 的 import 绑定只读，无法跨模块给 `let` 重新赋值，故可变单例（`scene`/`G`/`enemy`/`curMap`/`blog`…）统一托管到 `state.js` 的 globalThis 注册表，各模块仍以原始标识符读写，**行为与重构前完全一致**；模块间少量循环依赖均为调用期引用（环安全）
- **启动方式变更**：ESM 在 `file://` 下会被浏览器拦截，新增 `start.command`（macOS 双击）与 `start.sh` 一键启动本地服务器；新增 `package.json`（`type:module`，供命令行工具识别）与 `BACKUP_singlefile_v1.23.html`（重构前单文件回滚保险）
- **验证**：全部 8 个模块通过 `node --check`；新增 ESM 全量冒烟测试（25 项断言：初始化/遇敌分布/地图难度倍率/战斗伤害/击杀结算/传送碰撞/技能菜单绘制）全部通过；服务器 MIME 校验 `.js → text/javascript` 正常
- 纯结构重构：**未改动任何游戏数值、掉落、难度曲线、剧情、成就或交互逻辑**

## v2.1 Boss 战逃跑禁标恢复（显示修复）
- 【修复】v2.0 模块化重构中丢失了 v1.37 的指令栏反馈：Boss 战（主线幽冥魔王 / 洞窟领主 / 终焉之神 / 试炼连战）按 `4` 逃跑会被气场压制、不耗回合，但战斗指令栏仍无条件显示 `[4]逃跑`，与「Boss战无法逃跑」的判定不一致。现在指令栏对 Boss 战在 `[4]逃跑` 右侧叠绘一个**红色 ⛔**（单独红字，与技能菜单「MP 不足 ⛔」反馈风格一致），一眼看清别按 4
- 与 `playerAction()` 的 `flee` 分支判定同源（`isBoss/isTrue/isCaveBoss/isRush` 四类均不可逃），纯显示层，只改 `drawBattle()` 指令栏一处，未动任何怪物属性 / 掉落 / 经验金币 / 难度缩放数值；普通怪与精英「石心魔像」（仍可逃跑）指令栏保持原样
- 验证：`node --check js/*.js` 全部通过 + ESM 全量冒烟 19 项断言（魔王/洞窟领主/终焉之神/试炼连战 指令栏均显 ⛔，普通怪/精英不显；Boss 战按 4 逃跑仍在战斗、日志含「无法逃脱」、回合保留不扣血；存档读档回归）全部通过

## v2.3 敌方等级标定修复（显示修复）
- 【修复】战斗画面敌方名字旁的等级显示不一致：只有主线幽冥魔王写死 `Lv.8`，而洞窟领主（`isCaveBoss`）、隐藏真Boss「终焉之神」（`isTrue`）、试炼连战三名 Boss、「石心魔像」精英，以及 Boss 二段「真身」形态，全部误显示**玩家当前等级**（例如 Lv.1 就撞见终焉之神时它会显示 `Lv.1`，严重误导威胁判断，与 v1.19 威胁预警、v1.29 战力对比的「信息透明」风格相冲突）
- 新增 `enemyLv()` 纯展示函数统一标定：终焉之神 Lv.12 / 幽冥魔王 Lv.8 / 洞窟领主 Lv.7（试炼连战按对应 Boss 同值，真身改名后仍正确）/ 精英石心魔像 Lv=玩家+1 / 普通随机怪随玩家等级渐进；Boss 名颜色判定同步扩展到四类 Boss（紫色），此前只有主线魔王是紫色
- 纯显示层改动：只改 `ui.js` 的 `drawBattle()` 一行 + 新增一个纯函数，未动任何敌人属性、变身机制、经验/金币、掉落或难度缩放数值；随机怪等级仍与玩家等级绑定，曲线不变
- 验证：`node --check js/*.js` 全部通过；ESM 全量冒烟 **27 项断言全部通过**——启动初始化、enemyLv 十项分支（含试炼真身改名后映射）、三地图遇敌生成、Boss/精英/真身/普通四种敌人 `drawBattle()` 渲染、全部 16 场景 `render()` 回归、攻击/技能/防御/蓄力战斗路径、图鉴/成就/存档回归


## v2.2 界面/商店/战斗 三处重构回归修复（修复）
v2.0 模块化重构在“行为与重构前一致”的目标下仍漏了三处，导致多个界面直接崩溃或战斗手感与 v1.37 文档不符，本次一并修复：
- 【修复①】`ui.js` 缺失导入 `ACH_LIST` / `NPCS` / `SFX`：状态界面（`I`）、NPC 对话、通关战绩统计（`drawEnding`）运行时会 `ReferenceError` 崩溃（页面假设能打开，但一按 I / 一对话就白屏）。补上 data.js / audio.js 的导入后三处恢复正常
- 【修复②】商店接线丢失：`openShop()` 只在 `ui.js` 里定义了，入口从未调用——走进杂货铺 `shopList` 一直是空数组，啥都买不了，按 Enter 还会因 `shopList[NaN].act` 崩溃。已在 `world.interact()` 的 SHOP 分支调用 `openShop()`，进店自动填充 8 项商品（实测：玩家面对商店按 Enter → 立即出现 药水/卖蘑菇/4 武器/4 防具/离开）
- 【修复③】Boss 战逃跑误扣回合：v1.37 明确文档化为「按 4 逃跑不扣回合、不掉血、`battleBusy` 直接释放」（其冒烟断言为“不调度 `enemyAct`”），但重构后的 `flee` 分支调用了 `finishEnemy()`（会调度敌方攻击）——按 4 等于站着挨 Boss 白打一回合，可导致直接送死。已恢复：气场压制提示（「本回合行动保留」）后立即释放回合，玩家可马上改出 攻击/技能/防御/蓄力
- 未改动任何怪物属性 / 掉落 / 经验金币 / 难度缩放 / 升级曲线；普通怪逃跑 60% 成功率与失败扣回合逻辑完全保持
- 验证：`node --check js/*.js` 全部通过；ESM 全量冒烟 **36 项断言全部通过**——新增 状态界面(I)/NPC对话/通关战绩渲染、openShop 商品填充与购买路径、进店接线（interact→shop 自动填充 8 项）、Boss 逃跑 battleBusy 立即释放 + 900ms 内不掉血不调度 enemyAct、普通怪逃跑失败仍扣回合被反击（行为保持）、全 16 个场景渲染 + 存档 + main.js 启动引导

## v1.0 基础框架
- 瓦片地图 + 角色移动（WASD/方向键）
- 回合制战斗：攻击/技能/用药/逃跑
- 随机遇敌（史莱姆、野狼、骷髅、哥布林）
- 经验升级、商店/旅馆/喷泉、宝箱
- 通关目标：幽冥魔王

## v1.1 完整化
- 标题画面 + 开场剧情 + 通关/倒下画面 + 尾声
- 双地图（和平村 / 幽暗森林）+ Boss 二阶段「真身」
- 装备系统（4 档武器/防具 + 传说圣光之剑掉落）
- 等级解锁技能（火焰斩→冰霜击→治愈术→雷鸣→陨石术）
- Web Audio 音效（13+ 种）
- 存档/读档（localStorage）

## v1.2 沉浸与收集
- 状态界面（`I`）、敌人图鉴（`B`）、右上角小地图
- NPC + 支线任务（魔法蘑菇）、酿造锅（蘑菇→高级灵药）
- 昼夜循环 + 洞窟雨幕 + 标题星光粒子 + 战斗飘字/技能特效
- 背景音乐（主题乐 / 村庄 / 森林 / 战斗分场景）

## v1.3 深内容
- 第三张地图「宝藏洞窟」+ 洞窟领主打Boss + 隐藏真Boss「终焉之神」
- 精英怪「石心魔像」、成就系统（12+ 项）
- 角色创建（姓名/难度）、困难模式（敌×1.35血等）
- 多存档槽（1/2/3）、H 双页帮助（操作 + 地图指南）

## v1.4 终局与打磨
- 战利品掉落系统（药水/蘑菇/高级灵药/稀有装备）+「幸运眷顾」成就
- 试炼场（Boss Rush）：主线魔王 → 洞窟领主 → 终焉之神 三连战
- 快速旅行（`T`）、商店可售卖蘑菇
- 首次进入世界的新手教程提示
- 大量回归测试 + 语法/运行时校验

## v1.5 战斗手感
- 新增普通攻击「暴击」机制：12% 概率造成 1.8 倍伤害（仅普攻，不动敌人属性/技能平衡）
- 暴击时额外粒子爆发 + 战斗日志标注「（暴击！）」

## v1.6 战斗反馈
- 敌人血条随剩余血量变色（>50% 绿 / 20–50% 黄 / <20% 红），直观感知敌人虚弱程度

## v1.7 状态界面增强
- 状态界面（`I`）预告下一技能的解锁等级（如「📖 下一技能：冰霜击（Lv.3 解锁）」）；全部技能习得后显示「✨ 已习得全部技能」

## v1.8 小地图导航
- 右上角小地图为关键地点补上专属颜色：酿造锅（绿）、NPC（米白）、洞窟领主祭坛（紫）、终焉水晶（金）、试炼碑（青）、村庄入口/洞窟裂缝等传送门（蓝），一眼定位目标

## v1.9 防御指令
- 新增战斗指令 **防御 `[5]`**：本回合受到的伤害减半（并恢复 2 点 MP），战斗日志标注「（被防御格挡！）」；姿态仅在当前回合生效，Boss 重击亦可格挡
- 战斗指令栏 / 帮助页 / README 同步更新按键说明

## v1.10 防御 buff 显示
- 战斗中按下「防御」后，己方 HP/MP 条右侧亮起脉动盾牌图标 + 「防御中 · 减伤50%」文字提示，实时直观反馈本回合的减伤/回 MP 姿态（随动画刷新增强存在感，行动回合自动消失）

## v1.11 技能菜单 MP 提示
- 战斗技能菜单中，MP 不足以施放的技能项直接置灰并标注「⛔」（可用技能保持亮色，治愈术为专属绿色），底部 MP 行追加「灰色 ⛔ = MP 不足」说明，一眼判断当前能放哪个技能，避免猜测施放失败

## v1.12 升级进度可见
- 战斗胜利、未触发升级时，结算提示直接追加「距 Lv.X 升级还需 N 经验」，玩家一眼便能判断还需几场战斗才能升级/解锁技能；升级时仍显示原有等级提升提示（行为不变）

## v1.13 地图难度曲线（数值平衡）
- 随机遇敌的怪物属性不再三张地图一律相同，改为按地图微调：和平村遇敌 HP/攻击降低（约 ×0.9，新手区更友好）、幽暗森林维持标准、宝藏洞窟 HP/攻/防提升（约 ×1.2/×1.15/×1.1）并同步增加经验/金币（×1.15），与 README/地图指南「宝藏洞窟更强魔物」的描述一致
- 只调整 `randomEncounter()` 一处，地图/精英/Boss/掉落概率等行为均不变，符合渐进式难度曲线

## v1.14 世界目标横幅（体验打磨）
- 漫游世界时，画面左上角新增半透明「🎯 任务目标」横幅，实时显示当前主线/支线下一步（找回魔法蘑菇 → 回村领奖 → 讨伐魔王 → 讨伐终焉之神 → 完成），不用打开状态界面也能随时确认该去哪、该做什么
- 将目标文本逻辑抽成 `questObjective()` 函数，状态界面（I）复用同一份逻辑，避免两处文案漂移；横幅仅在 `scene==='world'` 绘制，商店/旅馆/对话框等覆盖界面不受干扰

## v1.15 图鉴报酬可见（体验打磨）
- 敌人图鉴（`B`）中，每个已讨伐魔物下方追加「→ 击败可得：经验 N · 金币 N」参考行，按当前等级实时计算（普通怪/石心魔像/三大 Boss 均覆盖），一眼判断该刷哪只怪更划算
- 将随机遇敌怪物基础模板抽为模块级唯一出处 `MON_BASE`（随机遇敌与图鉴显示共用同一套数值，避免公式漂移）；仅抽模板、不改任何战斗/掉落/平衡数值

## v1.16 防御反击（战斗机制）
- 「防御」指令不再只是一味挨打：防御姿态下若被敌人命中，有一半几率趁隙反击（约 0.7 倍普攻伤害，含飘字与日志「⚔️ 趁隙反击」提示），甚至可能直接把敌人反杀；减伤 50% + 回 2 MP 的原有效果保持不变
- 只改 `enemyAct()` 一处的敌方攻击分支，未动任何怪物属性/掉落/平衡数值；README 防御按键说明同步更新

## v1.17 蓄力指令 + 技能伤害修复（战斗机制 / 平衡修复）
- 新增战斗指令 **蓄力 `[6]`**：消耗一回合凝神蓄力，下一次【攻击】威力 ×2（与暴击 1.8 倍可叠加至 ×3.6），战斗中实时显示「⚡ 蓄力中 · 下击×2」脉动标签（随动画刷新）；蓄力不会因使用技能/药水/防御而被打断，仅在被下一次普攻消耗、成功逃跑或开启新战斗时清除
- 【修复】技能实际伤害未能生效的 bug：`attackMove()` 内部计算伤害时漏乘了技能倍率（原代码只把 `×2.0~×4.2` 写在日志里，实际扣血一直是普攻倍率）。现在把最终伤害值直接传给结算回调，消息与实际扣血一致——火焰斩/冰霜击/雷鸣/陨石术的倍率真正生效
- 只改 `attackMove()` 及其两个调用点（攻击/技能），未动任何敌人属性、掉落或难度缩放；指令栏 / 帮助页 / README 同步更新按键说明

## v1.18 药水反馈打磨（体验打磨）
- 战斗中指令栏实时显示药水余量（`[3]药水🍖×N 🧪×M`），一眼看清还剩几瓶普通药水/高级灵药，不再需要切界面猜
- 满状态（HP/MP 均已满）按药水指令时不再误喝浪费：战斗中提示「✅ 你气满神足，无需用药！」、世界按 F 提示「✅ 状态满满，无需喝药！」，药水/灵药原样保留；残血用药行为不变（仍优先消耗高级灵药）
- 只改 `playerAction()` 用药分支、`usePotion()` 与战斗指令栏绘制三处，未动任何数值/掉落/难度

## v1.19 战斗强度预警（体验打磨）
- 每场战斗开场时评估敌我差距并以战斗日志提示危险程度：遭遇终焉之神等远超实力的敌人提示「⚠️⚠️ 极危」、魔王级提示「⚠️ 强敌」；普通怪按「每击能否打掉过半血 / 是否血厚得打很多回合」实时判定「⚠️ 强敌 / ⚠️ 此敌有些棘手」；新手区史莱姆等练手怪与试炼连战保持不打扰
- 新增 `threatWarn()` 纯展示函数，仅在 `startBattle()` 的战斗开场日志追加提示文本，未动任何怪物属性/掉落/难度数值；低等级乱闯高难区域时一目了然该打还是该跑

## v1.20 技能菜单显示修复（显示修复）
- 【修复】战斗中按 `2` 打开的技能菜单原来只绘制一帧：主循环 `render()` 每帧重绘 `drawBattle()`，下一帧就把菜单面板整块覆盖擦掉，玩家按 `2` 实际上什么面板都看不到（数字键选技能仍暗中生效）。根因：`drawBattle()`/`render()` 都不检查 `skillMenuOpen`。现在在 `drawBattle()` 末尾加 `if(skillMenuOpen) drawSkillMenu()` 覆盖层钩子，菜单随主循环每帧绘制、持续可见
- 顺手修复排版：技能面板加大，使「当前 MP / 灰色 ⛔ = MP 不足 / [数字键]选择 [Esc]取消」说明行收进面板内部（原来画在面板外的半空处漂浮）；`openSkillMenu()` 改为先绘战斗底图
- 只改绘制层三处（`drawBattle()` / `drawSkillMenu()` / `openSkillMenu()`），未动任何技能数值、MP 消耗、解锁等级；低 MP 置灰 ⛔ 提示经浏览器实测正常

## v1.21 遇敌渐进曲线（数值平衡）
- 随机遇敌的怪物不再三张地图一律均等抽取：改为**随等级平滑渐进的权重曲线**——
  - 新手期（Lv1）在和平村只会遇到 史莱姆/哥布林/野狼/骷髅兵/毒蛇，**不会**撞上石魔像/树精这类对初始属性极不友好的强敌（旧版 Lv1 就可能被石魔像两下秒杀）；
  - Lv2 起骷髅兵/毒蛇逐步常见，Lv3 起树精/石魔像登场，Lv6+ 后强敌占比反超弱怪并随等级继续走高（取代旧版「Lv6 后从 0 直接跳到 35% 抽强敌」的生硬跳变，曲线更顺滑）；
  - 洞窟 7% 稀有精英「石心魔像」、村庄 ×0.9 / 洞窟 ×1.2 等地图难度倍率、经验/金币、Boss/掉落全部保持不变。
- 只改 `randomEncounter()` 一处的怪物抽取逻辑，未动任何怪物属性数值、掉落概率、成就或地图倍率

## v1.22 传送门/村民碰撞修复（修复）
- 【修复】传送门（GATE）、出口（EXIT）、村民（NPC）、酿造锅（BREW）原本没有任何碰撞体积：主角可以直接「踩进村民身体里」、直接穿过传送门——找不到幽暗森林入口的困惑，很可能正是因为这个（走了上去而不是面对面按 Enter）。根因：`SOLID` 集合只含 树/岩石/水，与注释「GATE/EXIT 不可穿过」矛盾
- 把 `TY.GATE / TY.EXIT / TY.NPC / TY.BREW` 加入 `SOLID`，传送门与村民现在会挡住去路，走到面前面向它按 Enter 即可交互；商店/旅馆/喷泉/宝箱/Boss祭坛仍保持踩上去触发与原有交互方式不变
- 帮助页操作说明补充「对话/确认（传送门/村民需面对面）」提示；只改 `SOLID` 集合一行 + 帮助一行，未动任何数值/地图/脚本；碰撞回归已加入冒烟测试（GATE/NPC/BREW/EXIT 阻挡 + 草地不误伤）全过

## v1.23 传送门踩踏触发（交互手感优化）
- 传送门（GATE）与出口（EXIT）由「面对面按 Enter」改为**踩上去自动进入**下一张地图：和平村 → 幽暗森林 → 宝藏洞窟的往返全部踩踩即走，不再需要精确站定面向按 Enter
- 具体：`SOLID` 中去掉 GATE/EXIT（保留 村民/酿造锅 碰撞）；`onStep()` 顶部新增 GATE/EXIT 分支（优先于遇敌判定，进门不误触战斗）；抽出 `useGate()`/`useExit()` 供踩踏与按 Enter 两条路径复用，逻辑不漂移
- 村庄入口门的「魔王未讨伐」封印限制保留（击败魔王后踩上门只提示不折返）；冒烟测试改为验证 踩传送门进幽暗森林 / 踩出口回村 / 魔王已倒门失效 / 村民与酿造锅仍阻挡 / 草地可通行，全部通过

## v1.24 低血量危险警示（体验打磨）
- 新增低血量警告：HP 降至 30% 及以下时，顶部 HUD 的 HP 数字与血条**变红并脉冲闪烁**，一眼可辨血量告急，及时喝药/防御/逃跑
- 纯视觉层改动：仅在 `renderHUD()` 内根据 `hp/hpMax≤0.30` 切换 `danger`/`low` 两个 CSS class，配 `dangerPulse` 关键帧动画，不影响任何数值、遇敌、掉落或战斗判定
- 仅改 CSS 三行 + `renderHUD()` 三行，未动战斗/地图/数值；冒烟测试新增低血量开关验证（25/100 → 开，80/100 → 关）通过

## v1.25 终章战绩回顾（体验打磨）
- 通关尾声画面（打倒魔王 → Enter 观看尾声）在剧情诗行下方追加一行「战绩 · 累计讨伐 N 只 · 成就 M/N · 金币 N」总览，通关那一瞬回顾整段冒险的讨伐与收集成果
- 纯显示层改动：仅改 `drawEnding()` 一处（面板略加高、诗句行距微调、新增战绩行），未动任何剧情文本、存档、数值或判定；G 字段均已在游戏中长期存在（bestiary/ach/gold），无新增状态

## v1.26 图鉴真身名规范化（成就/图鉴修复）
- 【修复】Boss 二阶段「现出真身」与试炼连战的改名机制会把真身名（如 `终焉之神·祸乱形态`、`幽冥魔王·真身`）直接写入击败记录：终焉之神通常被杀时已变名，图鉴永远收录不到基础名「终焉之神」，导致「图鉴征服（收录全部 11 种魔物）」成就往往无法达成（只有一回合秒杀 Boss、躲过变身才能解锁）；且图鉴里出现难看的真身名，报酬参考行也不显示（`monReward` 不认识 `终焉之神·祸乱形态`）
- 新增 `canonicalName()` 辅助函数，把二阶段/连战的真身名映射回基础名（覆盖 `·真身` 两种写法与 `·祸乱形态`）；`winBattle()` 计录图鉴改为使用规范化名——图鉴始终收录「幽冥魔王 / 洞窟领主 / 终焉之神」，成就判定随之诚实可达成，报酬行也正常显示
- 只改「新增一个映射函数 + `winBattle()` 一行计录」，未动任何敌人属性、变身机制、掉落、经验/金币数值或成就条件；冒烟测试新增验证（名称映射 5 项 + 真Boss 胜利计录基础名 + 「图鉴征服」成就可解锁 + 渲染路径回归）全过

## v1.27 HUD 经验进度条（体验打磨）
- 顶部 HUD 新增 **EXP 经验进度条**：第 1 行加入 `EXP 当前值/所需值` 文本 + 绿色进度条，随时一眼看到距下次升级还差多少经验（此前经验只在按 `I` 打开的状态界面里才有）
- 纯显示层改动：CSS 加 `.bar.xp` 一条渐变样式 + HTML 加 `s-xp`/`s-xpbar` 两个元素 + `renderHUD()` 加两行（数值与状态界面共用 `G.xp`/`G.xpNext` 同一数据源，`Math.max(1,xpNext)` 防除零），未动任何升级曲线、经验数值、战斗或存档逻辑
- 冒烟测试新增 EXP 栏断言（35/40→`87.5%`、0→`0%`、满→`100%`）与三张地图全路径渲染回归，全部通过

## v1.28 商店可负担性显示（体验打磨）
- 杂货商店中买不起的商品现在直接**置灰并标注「不足」**：金币不够买（武器/防具/药水）或药水已满（99）时，商品名变灰、价格变红并附「（不足）」，底部加一行图例「灰色 = 金币不足/药水已满」；买得起与「离开商店」等项保持原样
- 与技能菜单「灰色 ⛔ = MP 不足」（v1.11）同款模式的即刻反馈：不按下去买错（原回显「金币不足」），看列表一眼就知道哪些买得起
- 纯显示层改动：只改 `drawShop()` 一处的绘制分支，未动任何商品价格、买卖逻辑、库存或数值；冒烟测试覆盖 低金币 / 高金币 / 药水已满 三条绘制路径

---

## v1.29 战斗战力对比（体验打磨）
- 战斗中敌方 HP 条正下方新增**敌我双方 攻/防 一览**（`我方 攻X 防Y　⚔　敌方 攻X 防Y`，居中浅色一行）：以前敌方数值完全不可见只能靠猜，现在一眼判断该强攻、防御减伤还是逃跑走人；困难模式敌方数值已含 ×1.15/×1.12 缩放，与真实结算一致
- 纯显示层改动：只改 `drawBattle()` 内一行绘制，未动任何怪物属性/难度缩放/掉落/平衡数值；冒烟测试新增战力行断言（普通怪显示 + 困难模式缩放值一致）全过

## v1.30 战利品预览（体验打磨）
- 战斗画面新增**战利品预览行**（`战利品预览：经验 +N 金币 +N`，居中亮绿一行，位于战力对比行下方）：开战即见本场击败可得的经验/金币，与 v1.19 威胁预警互补——既要看清危不危险，也要知道值不值得打；试炼连战追加「（试炼通关另有奖励）」说明
- 数值与实际结算同源 `enemy.xp`/`enemy.gold`（含地图难度倍率 ×1.15、困难模式不变），不新增任何状态、不碰任何掉落/平衡逻辑
- 纯显示层改动：只改 `drawBattle()` 内一行绘制；冒烟测试新增战利品预览断言（经验/金币字段有效 + 战斗全流程回归 18 项）全过

## v1.31 商店装备对比（体验打磨）
- 杂货店的武器/防具不再只是「攻+5 / 防+4」的绝对值，而是直接标注与**当前装备**的差值：升级项（如「⚔️ 铁剑 (攻+5) ▲攻+3」）绿色高亮、降级项（如「▼攻-3」）按普通配色显示，一眼判断值不值得买，避免误购降级装备
- 底部图例同步更新为「绿色▲=更强升级 灰色=买不起 · ↑↓选择 Enter购买 Esc离开」
- 纯显示层改动：只改 `openShop()`（商品标签加对比）与 `drawShop()`（颜色分支 + 图例）两处，未动任何商品价格、买卖逻辑、库存或数值；冒烟测试新增装备对比断言（木剑→铁剑 ▲攻+3 / 购剑后反向 ▼ 降级 + 三条绘制路径回归 + 战斗全流程）全部通过

## v1.32 图鉴滚动浏览（修复 + 体验打磨）
- 【修复】敌人图鉴最多只显示前 10 种魔物，而全收集目标有 **11 种**——第 11 种永远藏在「还有 N 种魔物」里看不到，往往正是达成「图鉴征服」成就所缺的最后一种。现在图鉴支持 **↑↓ / W S 滚动浏览**：超过一页时底部实时提示「↑↓ 滚动浏览（还有 N 种）」，11 种全收集也能逐一看完
- 新增图鉴滚动偏移 `codexScroll` 状态 + `drawCodex()` 分页与双向越界钳制（不断按住上滚/列表变短都不会越界显示空白）+ 图鉴按键分支追加 ↑↓ 滚动；未动任何魔物属性、掉落、成就数值
- 冒烟测试新增：全收集 11 种渲染、上滚越界钳制、下滚至末页、列表变短钳制、空图鉴渲染，连同战斗全流程等共 26 项全部通过

## v1.33 技能伤害/恢复预览（体验打磨）
- 战斗技能菜单每条技能后新增**期望效果预览**：攻击技显示 `≈N伤`（对当前敌人按真实结算公式 `cmdDmg` 预估，含 ±10% 浮动）、治疗技显示 `+NHP`（按当前 HP 上限的 0.55 计算）——此前只标「攻x2.0」只能靠心算估伤，现在看一眼就知道值不值得花这 MP；MP 不足仍照常置灰 ⛔
- 与战力对比 / 战利品预览 / 装备对比同款思路：选技能前信息透明，纯显示层改动，只改 `drawSkillMenu()` 一处渲染分支，未动任何技能伤害公式、MP 消耗、技能解锁或平衡数值
- 冒烟测试新增：攻击技 ≈ 伤预览、治疗技 +HP 预览、MP 不足置灰分支，连同战斗全流程等共验证 15 项全部通过

## v1.34 Boss 变身线标记（体验打磨）
- 会二段变身的强敌（主线幽冥魔王 / 隐藏终焉之神 / 试炼连战三Boss）在血量**过半时会现出真身**并回血强攻，此前毫无预告。现在战斗血条 50% 处画一道**金色「二段变身线」记号**（含文字标注），一眼预知变身时机，提前决定补给、防御或爆发输出；Boss 变身（phase2）后记号变淡表示「已过」
- 纯显示层改动：只改 `drawBattle()` 一处绘制分支（新增 5 行），未动任何 Boss 属性、变身机制、经验/金币、掉落或难度数值；普通怪 / 精英怪不会触发该标记
- 冒烟测试新增：Boss 变身线绘制（phase2 前后两路径）+ 普通怪不绘制断言，连同三地图渲染 / 全部覆盖界面 / 战斗胜利全流程 / 存档读档共 9 项全部通过

## v1.35 图鉴出没地点指引（体验打磨）
- 敌人图鉴（`B`）中每个魔物名字后新增**出没地点指引**（灰蓝小字）：7 种基础魔物标注「草丛随机遇敌」，精英「石心魔像」标注「幽暗森林·稀有精英」，三大 Boss 分别标注「幽暗森林祭坛 / 宝藏洞窟深处 / 宝藏洞窟·终焉水晶」——配合 v1.15 奖励参考行，收集 11 种魔物不再靠猜该去哪刷
- 新增 `whereFind()` 纯展示函数（列个官方位置映射表），只改 `drawCodex()` 一处绘制，未动任何魔物属性、掉落、成就或数值

## v1.36 Boss 战不可逃跑（战斗机制 / 平衡）
- 「逃跑」不再对 BOSS 级敌人生效：主线幽冥魔王 / 洞窟领主 / 隐藏终焉之神 / 试炼场三连战的战斗中按 `4` 逃跑，会直接被气场压制（日志提示「⚠️ XX 的气场压制着你，无法逃脱！」），必须战斗到底——消除「打不过就溜回旅馆回满血再来」的无限拉扯；阵亡仍有 R 重开 / 对 Boss 按 B 重整旗鼓兜底
- 普通怪遭遇战逃跑行为完全不变（仍约 60% 概率成功），精英「石心魔像」仍可正常逃跑
- 只改 `playerAction()` 的 `flee` 分支一处条件，未动任何怪物属性 / 掉落 / 经验金币 / 难度缩放数值；战斗说明行与 README 同步标注「Boss战无法逃跑」
- 冒烟测试新增：四类 Boss 战逃跑均被阻止（场景仍为 battle、日志含「无法逃脱」）+ 普通怪逃跑成功率/失败率回归（40 次抽样 25 成功 15 失败）+ 渲染/结算全路径回归共 25 项全部通过

## v1.37 Boss 战逃跑「陷阱反馈」打磨（体验打磨）
- 上版 v1.36 让 Boss 战无法逃跑，但按 `4` 被拒后仍会**白白消耗本回合并挨打**——纯陷阱反馈。现在 Boss（主线魔王/洞窟领主/终焉之神/试炼连战）战按 `4` 逃跑：直接提示「⚠️ XX 的气场压制着你，无法逃脱！（本回合行动保留）」，**不扣回合、不掉血**，玩家可立即换用攻击/技能/防御/蓄力等指令继续战斗
- 同时战斗指令栏对 Boss 战把 `[4]逃跑` 置灰并标 **`[4]逃跑⛔`**（与技能 MP 不足的 ⛔ 风格一致），一眼看出别按 4；普通怪遭遇战仍显示 `[4]逃跑`、保持原 60% 成功率与失败扣回合逻辑完全不变
- 纯反馈层改动：只改 `playerAction()` 的 `flee` 分支 + `drawBattle()` 指令栏一处渲染，未动任何 Boss 属性、变身、经验/金币、掉落或难度数值；README 战斗说明同步更新
- 冒烟测试新增：Boss 战逃跑后 回合保留（不调度 enemyAct、不掉血、battleBusy 释放、仍在战斗场景）+ 日志文案断言 + 命令栏 Boss 显示 ⛔ / 普通怪不显示 ⛔，连同 普通怪逃跑失败仍扣回合、战斗胜利全流程、三地图与全部覆盖界面渲染 共 13 项全部通过

## v19.88 阵亡画面追加剩余补给（体验打磨 / 信息透明）
- 死亡结算屏新增「身上余粮」一行：直接显示当前持有的 🍖 生命药水、🧪 高级灵药、🍄 魔法蘑菇 数量，与金币/等级/讨伐/时间同屏呈现——玩家在「R 重开 / B 重整旗鼓 / T 返回标题」前可一眼判断是否需要先回旅馆/酿造补给，避免回村后才发现药已喝完或蘑菇已卖光。
- 纯显示层改动：只改 `js/view/menus.js` 的 `drawDead()` 一处，新增一行文本并整体下移建议/按键行避免重叠；未动任何战斗结算、死亡惩罚、资源数值或存档逻辑。
- 版本号同步递增：`js/data.js` `GAME_VERSION` 由 `v19.87` → `v19.88`。
- 验证：`npm run check` 25 模块语法校验全过；`npm test` 89/89 + 8/8 全绿；新增专项 review 确认 drawDead 在 hero 存在时新行引用字段（`item/potion2/mushrooms`）均为 hero 快照持久化字段，且 y 坐标从原 264 依次下移到 292/332/362/392，不溢出 480px 画布。

## v19.89 状态页追加当前地图与区域难度（体验打磨 / 信息透明）
- 按 `I` 打开状态面板时，角色名/等级下方新增一行「📍 当前地图名（区域难度标签）· 推荐 Lv.X」，一眼确认自己在哪张图、这张图推荐等级与区域难度标签——此前玩家常忘记当前地图名或不确定是否越级探索，需要再切 `T` 旅行菜单查看。
- 纯显示层改动：只改 `js/view/menus.js` 的 `drawStatus()` 一处，新增一行文本；数据源同读 `data.js` 的 `MAPS[].name`、`recLv`、`zone.label`（与快速旅行、世界 `transition` 难度预警同源），零结算变化；缺 `zone` 的地图（如雾语林）只显示地图名与推荐等级，不抛错。
- 版本号同步递增：`js/data.js` `GAME_VERSION` 由 `v19.88` → `v19.89`。
- 验证：`npm run check` 25 模块语法校验全过；`npm test` 89/89 + 8/8 全绿；新增行在 `village`/`dungeon`/`cave`/`gallery` 状态下渲染均不抛错。

## v19.90 记忆碎片拾取反馈追加收集进度（体验打磨 / 信息透明）
- 【强敌首胜掉落记忆碎片时只报「拾起一段记忆」与回看提示，玩家看不到收集进度】`battle.js` `winBattle()` 在强敌首胜后追加记忆碎片时，`boxMsg` 仅提示 `🕯️ 拾起一段记忆：【X】（按 J 日志回看）`——玩家刚拿到一段关键剧情碎片，想确认「离真结局·全记忆还差几段」仍需按 `J` 翻日志或按 `I` 看状态页；与 v19.85「图鉴完成带剩余金币」、v19.88「阵亡画面带剩余补给」的同一「信息透明」主线不一致。
- 【修正：拾取文案追加结算后收集段数，纯显示零结算】`battle.js` 改为：`🕯️ 拾起一段记忆：【${frag.name}】（按 J 日志回看 · ${fragCount}/${FRAGMENTS.length} 段记忆已集齐）`。`fragCount` 为 `hero.fragments.push(frag.id)` 结算后的实时长度，`FRAGMENTS.length` 与真结局判定、任务进度、名字石碑同读 `data.js` 单一数据源；调记忆碎片总数时只改 `data.js` 一处，提示自动跟随；未改变碎片掉落条件、首胜判定、图鉴/成就结算、`hooks.applyVictoryWorld` 任何逻辑。
- 【零回归面】未动 `FRAGMENTS` 内容与强敌首胜掉落规则；未改 `canonicalName` 归一化；未改任务日志 `J` 记忆碎片节显示；未改真结局加页 `ENDING_TRUE_FRAG`；未改 `winBattle` 其它分支（普通胜利/升级/Boss/试炼/真结局）。只改 2 个文件（`data.js`：版本号 `v19.89→v19.90`；`battle.js`：记忆碎片拾取 `boxMsg` 1 条 + 注释）。
- 验证：`node --check js/data.js js/battle.js` 与 `npm run check`（25 模块）全部通过；`npm test` 89/89 + 8/8 全绿（回归不受影响）。

## v19.92 幽冥魔王胜利反馈追加剩余金币（体验打磨 / 信息透明）

|- 【击败幽冥魔王后只报「圣光之剑」，玩家看不到本次收益与剩余金币】`battle.js` `winBattle()` 在 `enemy.isBoss` 分支只在首次获得圣光之剑时弹出武器提示——玩家刚击败首个主线 Boss，获得一笔金币/经验，想确认「兜里还剩多少」仍需再按 `I` 看状态页；与 v19.80「普通战斗胜利带剩余金币」、v19.81「升级带剩余金币」、v19.82「试炼通关带剩余金币」、v19.91「洞窟领主胜利带剩余金币」的同一「信息透明」主线不一致。
|- 【修正：幽冥魔王胜利文案追加本次获得与结算后剩余金币，纯显示零结算】`battle.js` 在 `enemy.isBoss` 分支开头追加：`🏆 ${enemy.name}倒下！获得 ${enemy.gold} 金币、${enemy.xp} 经验 · 剩余 ${hero.gold} 金`。`hero.gold` 为 `enemy.gold` 结算加入后的实时值；调幽冥魔王金币/经验只改 `data.js` 一处，提示自动跟随；未改变圣光之剑掉落（仍仅在未持有时装备并提示）、Boss 击败后的世界状态、`applyVictoryWorld` 调用时机、任何结算或掉落逻辑。
|- 【零回归面】未动 `BOSS` 属性/掉落/成就判定；未改圣光之剑 `applyStats` 换装与 `bind.renderHUD` 时机；未改 `goto('win')` / `bind.drawWin` / `stopBgm`；未改 `winBattle` 其它分支（普通胜利/升级/洞窟领主/试炼/真结局/记忆碎片）。只改 2 个文件（`data.js`：版本号 `v19.91→v19.92`；`battle.js`：`enemy.isBoss` 分支 1 条 `boxMsg` + 注释）。
|- 验证：`node --check js/data.js js/battle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）。

## v19.91 洞窟领主胜利反馈追加剩余金币（体验打磨 / 信息透明）

- 【击败洞窟领主后只报「宝箱显形」，玩家看不到本次收益与剩余金币】`battle.js` `winBattle()` 在 `enemy.isCaveBoss` 分支只弹出 `🏆 洞窟领主倒下！左边的星砂宝箱显形了，快去开启！`——玩家刚击败首个主线 Boss，获得一笔金币/经验，想确认「兜里还剩多少」仍需再按 `I` 看状态页；与 v19.80「普通战斗胜利带剩余金币」、v19.81「升级带剩余金币」、v19.82「试炼通关带剩余金币」的同一「信息透明」主线不一致。
- 【修正：洞窟领主胜利文案追加本次获得与结算后剩余金币，纯显示零结算】`battle.js` 改为：`🏆 洞窟领主倒下！左边的星砂宝箱显形了，快去开启！获得 ${enemy.gold} 金币、${enemy.xp} 经验 · 剩余 ${hero.gold} 金`。`hero.gold` 为 `enemy.gold` 结算加入后的实时值；调洞窟领主金币/经验只改 `data.js` 一处，提示自动跟随；未改变洞窟领主击败后的世界状态（`hero.caveBoss` 置位、宝藏显形、矿车支线激活）、`applyVictoryWorld` 调用时机、任何结算或掉落逻辑。
- 【零回归面】未动 `CAVE_BOSS` 属性/掉落/成就判定；未改 `applyVictoryWorld`、`world.js` 宝藏显形、任务 `side_cart` 激活；未改 `winBattle` 其它分支（普通胜利/升级/Boss/试炼/真结局/记忆碎片）。只改 2 个文件（`data.js`：版本号 `v19.90→v19.91`；`battle.js`：洞窟领主胜利 `boxMsg` 1 条 + 注释）。
- 验证：`node --check js/data.js js/battle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）。

## v19.93 宝箱蘑菇反馈追加任务进度——雾语林开箱拿到蘑菇后一眼看清灯长支线还差几株（体验打磨·信息透明）

|- 【雾语林宝箱开出魔法蘑菇时只报总株数，接取支线后看不到任务进度】`world.js` `onChestStep()` 在 `curMap() === 'dungeon'` 分支命中蘑菇掉落时，`boxMsg` 仅提示 `🍄 找到魔法蘑菇！（共 ${hero.mushrooms} 株）`——玩家已接取灯长支线后，想确认「离交付还差几株」仍需再按 `I` 看状态页或 `J` 翻日志；与 v19.68「酿造/出售蘑菇保护带进度」、v19.70「酿造成功带剩余材料」的同一「任务进度即时透明」主线不一致。
|- 【修正：宝箱蘑菇文案追加任务进度，纯显示零结算】`world.js` 改为在支线 `side_mushroom === 'active'` 且尚未集齐时追加：`，任务还差 ${MUSHROOM_GOAL - hero.mushrooms} 株`。`MUSHROOM_GOAL` 与酿造保护、商店阻止、任务交付同源读 `data.js`；支线未激活或已集齐时不额外显示，保持文案简洁；未改变开箱掉落概率、蘑菇计数、任务状态流转、`setSideQuest` 触发逻辑。
|- 【零回归面】未动 `CHEST_MUSHROOM` / `CHEST_GOLD` / `CHEST_GOLD_BASE` / `CHEST_GOLD_PER_LV` 任何数值；未改金币/药水宝箱分支；未改 `applyAchievements` 即时判定；未改地图渲染或 NPC 交互。只改 2 个文件（`data.js`：版本号 `v19.92→v19.93`；`world.js`：`onChestStep` 蘑菇分支 1 条 `boxMsg` + 进度推导 + 注释）。
|- 验证：`node --check js/data.js js/world.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）。

## v19.94 喷泉恢复反馈追加具体数值——踩泉时一眼看清回了多少 / 满状态不再静默（体验打磨·信息透明）

||- 【喷泉恢复时只报「HP/MP 完全恢复」，玩家看不到回复量与当前值】`world.js` `onFountainStep()` 在踩到喷泉且未满时仅提示 `⛲ 喷泉清泉涌动，HP/MP 完全恢复！`——玩家刚经历一场战斗，想确认「到底回了多少 / 现在满没满」仍需再按 `I` 看状态页；与 v19.74「喝药带剩余量」、v19.80「战斗胜利带余额」的同一「信息透明」主线不一致。
||- 【修正：喷泉文案追加本次恢复量与最终值，并补全满状态分支】`world.js` 改为：
||  - 非满状态：`⛲ 清泉涌动，HP +${hp}（${hero.hp}/${hero.hpMax}）· MP +${mp}（${hero.mp}/${hero.mpMax}）完全恢复！`，恢复量由恢复前后差值计算，最终值与 `hero.hpMax` / `hero.mpMax` 同源；
||  - 满状态：新增分支，按帮助页/面向提示口径显示 `⛲ 踩上回血 · 状态已满`（此前满状态踩泉无任何反馈）。
||  未改变喷泉恢复结算（仍回满 HP/MP、仍减遭遇槽）、未改音效时机、未改 `ENCOUNTER.fountain` 数值。
||- 【零回归面】未动 `ENCOUNTER` / `POTION_*` / 成长/存档任何逻辑；未改喷泉坐标与地图数据；未改帮助页「喷泉」行文案（本改动与帮助页口径一致）。只改 2 个文件（`data.js`：版本号 `v19.93→v19.94`；`world.js`：`onFountainStep()` 1 条 `boxMsg` + 新增满状态分支 + 注释）。
|||- 验证：`node --check js/data.js js/world.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）。

## v19.95 战斗 MP 不足反馈追加所需与当前 MP——按技能键时一眼看清还差多少蓝（体验打磨·信息透明）

|||- 【战斗中按数字键施法 MP 不足时只报「MP 不足」，看不到技能耗蓝与当前蓝量】`battle.js` `doSkill()` 在 `hero.mp < skill.mp` 时仅返回 `❌ MP 不足！`——玩家刚想放技能，想确认「这招到底要多少 MP、当前差几点」仍需打开技能菜单或按 `I` 看状态页；与 v19.74「用药带剩余量」、v19.94「喷泉恢复带具体数值」的同一「信息透明」主线不一致。
|||- 【修正：MP 不足拦截文案追加技能耗蓝与当前 MP，纯显示零结算】`battle.js` 改为：`❌ MP 不足！【${skillName}】需要 ${skill.mp} MP（当前 ${hero.mp}/${hero.mpMax}）`。`skill.mp`、`hero.mp`、`hero.mpMax` 与技能菜单/状态页同源读数，零数值变化；未改变 MP 判定、技能封印分支、技能结算、战斗回合推进任何逻辑。
|||- 【零回归面】未动 `SKILL_DATA` 各技能 MP 与效果；未改技能领悟、技能菜单绘制、状态页 MP 显示、战斗胜利/失败结算。只改 2 个文件（`data.js`：版本号 `v19.94→v19.95`；`battle.js`：`doSkill()` MP 不足分支 1 条文案 + 注释）。
|||- 验证：`node --check js/data.js js/battle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）。

## v19.96 防御反馈追加当前 MP——摆防御回蓝后一眼看清能否放出下一轮技能（体验打磨·信息透明）

|||- 【摆出防御姿态后只报「恢复 N 点 MP」，玩家看不到当前 MP 能否支撑下一轮技能】`battle.js` `doDefend()` 在结算 `hero.mp += DEFEND_MP` 后，`S.blog.push` 仅提示恢复量——玩家刚靠防御回蓝，往往紧接着就要决定下一轮是继续防御、喝药还是放技能，想确认「当前 MP 够不够」仍需瞄 HUD 或按 `I` 看状态页；与 v19.95「MP 不足拦截带当前/最大 MP」的同一「信息透明」主线不一致。
|||- 【修正：防御文案追加结算后当前/最大 MP，纯显示零结算】`battle.js` 改为：`🛡️ ${hero.name} 摆出防御架势，本回合受到的伤害减半…（MP ${hero.mp}/${hero.mpMax}）`。`hero.mp` / `hero.mpMax` 为防御回蓝结算后的实时值，与技能菜单、状态页、MP 不足拦截文案同源；调 `DEFEND_MP` 只改 `data.js` 一处，提示自动跟随。未改变防御减伤倍率、反击概率、回合推进、敌方行动任何逻辑。
|||- 【零回归面】未动 `DEFEND_MP`/`DEFEND_MULT`/`COUNTER_CHANCE` 数值与防御结算；未改 `doDefend` 的 `renderHUD`、`SFX.block` 时机；未改 `drawBattle` 指令栏「防御中 · 减伤 X% · 回 Y MP/回合」提示；未改 `afterPlayer` 回合推进。只改 2 个文件（`data.js`：版本号 `v19.95→v19.96`；`battle.js`：`doDefend()` 1 条 blog 文案 + 注释）。
|||- 验证：`node --check js/data.js js/battle.js` 与 `npm run check`（25 模块）全部通过；`npm test` **89/89 + 8/8** 全绿（回归不受影响）。

## 技术要点
|- 单文件、零依赖、离线可玩；Canvas 程序化绘制全部图块/角色/敌人
- 音乐/音效用 Web Audio 实时合成（步进音序器）
- 存档存于 `localStorage`（`jrpg_save1/2/3`）
- 每次改动均做全路径功能回归 + `new Function` 语法校验
