# 勇者传说 · 改进计划

> 留档日期：2026-08-19（对应代码约 v3.39）
> **落地状态：v4.0 已执行（2026-08-19）。** 验证：`npm test`（`tests/smoke.mjs` 28/28）。
> 性质：原为设计备忘；下表阶段均已实现。细节以当前 `js/` 为准。
> 约束：**零依赖、无构建、浏览器原生 ESM、程序化 Canvas**。不为架构引入打包器 / React / Phaser / ECS。

---

## 0. 总原则

v3.x 把「信息透明」做满了（威胁预警、伤害预览、面前提示、图鉴标注、逃跑成功率、祭坛等级…）。系统已经**能看懂**，但：

- 架构是全连通图，再加内容成本按「改一处、绕一圈」增长；
- 美术 / 任务 / 技能 / 战斗都偏浅：HUD 文案补的是造型和机制没补的信息。

下一步不该再堆提示文字，而该补**差异**，并先把依赖拧直，避免 v3.34 那种「胜利流 ReferenceError、冒烟没走过真路径」再现。

每一步仍遵守现有纪律：`node --check js/*.js` + ESM 冒烟；能不改存档结构就不改；改存档必须兼容旧字段。

---

## 1. 架构

### 1.1 现状

README 宣称的层次：

```
data（纯） → core / world / battle / ui → main
```

实际是一张全连通图：`core`、`world`、`battle`、`ui` 彼此 import。`state.js` 把约 40 个可变变量挂到 `globalThis`。`ui.js` 约占全部 JS 的一半（~71KB / 1131 行），商店购买、换装也写在绘制层。

已知症状：v3.34 击败洞窟领主时 `TY is not defined`——逻辑以为常量在全局，拆模块后漏 import，冒烟又没走过真实胜利流。

### 1.2 目标依赖（单向）

```
data（常量 + 纯函数；成就 ok(g) 显式传参）
  ↑
rules（伤害、掉落、成长、成就 —— 无副作用）
  ↑
state（导出对象 S + 场景枚举，禁止 globalThis）
  ↑
systems：world / battle / shop / save   ← 禁止 import 绘制
  ↑
view：只读 S，发出「请重绘」
  ↑
main：按键 → 系统，定时器 → view
```

胜利后改地图：`winBattle` **返回结果**（`{ type:'cave-boss', gold, xp, flags }`），由 `world` / `core` 应用。这个体量不需要事件总线。

### 1.3 四件具体事

**A. 用共享对象替换 `globalThis`（收益最高）**

ESM 不能跨模块给 `let scene` 重新赋值。不要继续用全局注册表，导出一个对象，改属性：

```js
export const S = { scene: 'title', G: null, enemy: null, maze: null /* ... */ };
// 任意模块：S.scene = 'world'
```

`ACH_LIST[].ok` 改为 `ok(g)`，所谓纯数据层才真正纯。冒烟也不必污染 `globalThis`。

**B. 拆掉环**

最伤的环：`world ↔ battle`（遇敌 vs 胜利清祭坛 / 显形宝藏），以及 `ui ↔ {battle, core, world}`。`cmdDmg` / `atkEstimate` / `monReward` 应在 `rules`，界面不为画一行字去拉整套战斗副作用。

**C. `ui.js` 上帝模块**

| 留下 view | 挪出系统 |
|---|---|
| 图块 / 精灵 / 粒子 / HUD / 各屏 `draw*` | `openShop` 里改金币、换装备、卖蘑菇 |

文件切分停在这个粒度：`view/tiles.js`、`view/world.js`、`view/battle.js`、`view/menus.js`、`view/hud.js`。再碎过了。

**D. 场景不要再用一根字符串 + 140 行 `keydown`**

```js
const screens = {
  world:  { onKey, draw: drawWorld },
  battle: { onKey, draw: drawBattle },
  shop:   { onKey, draw: drawShop },
};
```

`render()` → `screens[S.scene].draw()`；输入 → `screens[S.scene].onKey(e)`。切换收口 `goto('world')`（顺带切 BGM、清 `battleBusy`）。

### 1.4 明确不要做

- 打包器、React、Phaser、ECS、TypeScript 编译步骤（JSDoc 可渐进加在 `G` / `Scene`）。
- `BACKUP_singlefile_v1.23.html` 在 v3.39 之后是负担；确认稳定后删除。
- 冒烟脚本散落 `/tmp` 无法复现；应收进仓库 `tests/`。

### 1.5 架构落地顺序

1. `state.js` 改为导出 `S`；`ACH_LIST.ok(g)`。行为零变化。
2. 商店 / 旅馆购买逻辑迁出 `ui.js`。
3. 战斗结算改成返回结果，去掉 `battle ↔ world` 环。
4. 按键表 + `goto(scene)` 收口 `main.js`。
5. 按屏幕拆 `ui.js`，抽出 `rules.js`。

做到第 3 步架构已健康；后面拆文件只是可读性。

---

## 2. 美术

### 2.1 现状问题

1. **世界主角 ≠ 战斗主角**。大地图随衣甲 / 武器换色、圣剑发光；战斗里是固定蓝衣木剑（`drawMonster(..., true)`）。
2. **战斗场永远同一块星空渐变**。村 / 林 / 洞 / Boss 背景一样。雨幕、晶尘、昼夜只存在于大地图。
3. **移动是瞬移一格**。腿在摆，人跳 32px。这是「像素风但手感廉价」的主因。
4. NPC 同一张脸，所以才需要「⏎ 对话 · 村长」——提示补的是造型没补的信息。
5. `drawMonster` 按名字 `if/else`；昼夜是墙钟 12 秒一轮，与 `G.time` 无关。README 写洞窟晶尘，代码没有。

### 2.2 做 / 不做

做（保持程序化，不换美术管线）：

| 优先级 | 内容 |
|---|---|
| 1 | 战斗复用 `drawHero`（含换装），废弃战斗简化人 |
| 2 | 格子移动插值 150–200ms（只改绘制，逻辑仍整格） |
| 3 | 战斗背景跟 `curMap`：村 / 林 / 洞三套底 + Boss 径向光 |
| 4 | NPC 用数据里 1 个特征（村长杖、猎人帽、矿车灯） |
| 5 | 水 / 喷泉 / 水晶相位动画；洞穴补晶尘 |

`drawMonster` 改为 `SPECIES[name].draw(ctx)` 注册表。昼夜绑 `G.time` 或弱化成氛围，避免正午进村却是夜滤镜。

不做：精灵表、骨骼、WebGL、独立动画引擎。

---

## 3. 任务系统

### 3.1 现状问题

整条支线是 `G.quest = 0..3`。主线是散落旗标：`bossDefeated` / `caveBoss` / `trueBoss` / `rushDone`。

`questObjective()` 在支线进行中会**盖掉主线横幅**（接蘑菇委托后，左上角不再显示「去讨伐魔王」）。I 键四枚徽记能补主线，但要进状态页才看得到。

蘑菇同时是任务物、商品、酿造材料；任务系统不知道「这株能不能卖」。

村长对话是 `chiefPages()` 硬编码；隐者 / 猎人 / 矿车夫是情报 NPC（保持即可）。

### 3.2 目标模型（小，够用）

```js
QUESTS = {
  main_demon:    { kind:'flag', flag:'bossDefeated', obj:'讨伐幽冥魔王' },
  main_cave:     { kind:'flag', flag:'caveBoss',     unlockOn:'main_demon' },
  main_true:     { kind:'flag', flag:'trueBoss',     unlockOn:['main_demon','main_cave'] },
  side_mushroom: { kind:'collect', item:'mushrooms', n:3, giver:'chief' },
};
G.quests = { side_mushroom: 'active' };  // 取代单个 G.quest
```

存档仍保留旧旗标；journal 由旗标推导，兼容旧档（读到 `G.quest` 时迁移到 `G.quests`）。

- 横幅两行：主线一行 + 当前支线一行（或主线常驻，支线只在相关地图亮）。
- I 键变成任务日志，不再只调一次 `questObjective()`。
- 村长对话改为「任务 id → 阶段 → 文案 / 发奖」。
- 任务物品不足时，出售给出警告（或禁止卖到低于任务需求）。

刻意不做：分支结局、声望、限时、采集任务刷屏。体量上并行「主线链 + 一条村支线」就够质变。若再加一条，优先把「洞窟领主后矿车宝藏」接到矿车夫（内容已在，只是没进状态机）。

---

## 4. 技能系统

### 4.1 现状问题

五招实质是 MP 换倍率的梯子：火 2.0 → 冰 2.6 → 雷 3.2 → 陨石 4.2，全是 `攻×2−防`；治疗回 55% HP **不解毒**。后期只剩陨石和治疗，火 / 冰 / 雷被严格支配。

蓄力只加成下一次「攻击」，与技能互不干涉——空蓝时才有意义。升级自动领悟，没有构筑。

### 4.2 最小改动、最大差异（写进现有 `SKILL_DATA`）

| 技能 | 现在 | 建议 |
|---|---|---|
| 火焰斩 | 2.0 伤 | 伤略低 + 灼烧 2 回合（可叠，克高防 / 石甲） |
| 冰霜击 | 2.6 伤 | 伤中等 + 约 30% 让敌人跳过行动（克回血 Boss） |
| 雷鸣 | 3.2 伤 | 穿透一半防御（克石魔像 / 石甲） |
| 陨石术 | 4.2 伤 | 高伤；对真身 / 祸乱额外倍率，或击碎 1 层石甲 |
| 治愈术 | 回 55% HP | **同时解除中毒**（毒在回合开始跳，当前治疗救不了节奏） |

敌人加 `weak` / `resist`。图鉴已讨伐行显示弱点——仍是信息透明，但这次有机械后果。物种数据应同时带 `poison`、`weak`、`draw`，图鉴标注从这份数据生成，删掉单独维护的 `CODEX_TAG`。

蓄力：下一次攻击 **或** 技能 ×1.5，空蓝和有蓝都有决策。

不做：技能树、职业分流、六人队伍。五个技能做出「对这个对手该按哪一个」即可。领悟仍可按等级；状态页写清「这招克什么」。

---

## 5. 战斗系统

### 5.1 现状问题

玩家行动 → 600ms → `enemyAct`，锁在 `battleBusy` + `setTimeout` 上。Boss 几乎共用脚本：过半变身、40% 血以下 40% 暗影回血、再随机重击。洞窟领主**没有二阶段**（`phase2` 判断不含 `isCaveBoss`），却带着 `isElite`，打完既清祭坛又掉蘑菇。唯一真正独特的是石心魔像石甲和毒蛇施毒。

公式 `max(1, 攻×2−防)` 让高防战变成海绵。遇敌是危险格 16% 随机，容易疲劳。

### 5.2 结构（玩家几乎无感，先做）

把 `battleBusy` + 一串 timeout 收成回合队列：

```
enqueue(player | poison | enemy | phase) → advance()
```

胜负、逃跑、试炼连战走同一个出口。给「技能跳过敌回合 / 蓄力 / 反击」提供干净钩子，也消掉「胜利中途抛错 → 永远 busy」。

建议在架构阶段 3（`winBattle` 返回结果）之后做，否则行为表会继续和清祭坛缠在一起。

### 5.3 内容（玩家有感）

每个敌人一张小行为表，不要再按 `isBoss` 一把抓：

```js
{
  name: '洞窟领主',
  acts: [
    { type: 'attack', w: 50 },
    { type: 'shield', w: 20, max: 2 },
    { type: 'heal',   w: 15, hpBelow: 0.4 },
  ],
  phase2: { at: 0.5, atk: +5, name: '…真身' },
}
```

三场 Boss 应对必须不同，并在指令栏用现有 ⛔ 风格标出来：

- 幽冥魔王：压血量 / 变身线；
- 洞窟领主：磨石甲或肩刺（补上独特机制，不要当缩小版魔王）；
- 终焉之神：祸乱形态禁某指令（禁治疗或禁蓄力）。

遇敌：危险格积攒 0–100 遭遇槽（满了必打；喷泉 / 路可缓降）比再调 16% 更像 JRPG。

不做：ATB、多敌人站位、我方多人。深度来自状态 + 技能克制 + Boss 脚本。

---

## 6. 四块如何咬合

美术的战斗背景、技能粒子、敌人行为特效，共用同一份物种数据：

```js
{ name: '毒蛇', poison: 0.35, weak: 'ice', draw: 'snake', arenaTint: '...' }
```

任务系统给战斗的接口只有旗标，以及「这只怪 / 这个祭坛是否任务目标」。不要把随机遇敌本身做成任务。

---

## 7. 总落地顺序

按风险从低到高。标了「存档」的步骤必须写迁移 / 兼容。

| 阶段 | 内容 | 存档 | 依赖 |
|---|---|---|---|
| A1 | `S` 对象替换 `globalThis`；`ok(g)` | 否 | — |
| A2 | 商店 / 旅馆逻辑迁出 `ui.js` | 否 | A1 |
| A3 | `winBattle` 返回结果，拆 `battle↔world` | 否 | A1 |
| **P1** | 战斗复用换装主角 + 格子移动插值 | 否 | —（可与 A 并行） |
| **P2** | 治疗解毒 + 技能效果分化 + 弱点字段 | 否* | 最好 A3 后 |
| **Q1** | 横幅主线 / 支线分行 + `G.quests` | **是** | A1 |
| **B1** | 敌人行为表 + 回合队列 | 否 | A3 |
| P3 | 战斗分场景背景、NPC 外形、晶尘 / 水动画 | 否 | P1 |
| A4 | `goto(scene)` + 按键表 | 否 | A1 |
| A5 | 拆 `ui.js` / 抽出 `rules.js`；冒烟收回 `tests/` | 否 | A3、A4 |
| B2 | 遭遇槽替代 16% 随机 | 可选 | B1 |
| Q2 | （可选）矿车宝藏交回矿车夫 | 是 | Q1 |

\*技能数据在 `data.js`，旧存档只存技能名数组，一般兼容；若给敌人加字段只影响静态表。

P1 / P2 不改存档，可先出「手感 + 战斗有选择」的小版本。Q1 要兼容 `G.quest`。B1 才是战斗真正的架构升级。

---

## 8. 完成判据（摘）

- 任意模块不再读写未 import 的标识符（无第二起 `TY is not defined`）。
- 进战斗能看出穿了哪件装备。
- 横幅同时看得到主线和支线；卖蘑菇不会在无提示下把任务卡死。
- 打石心魔像 / 毒蛇 / 回血 Boss 时，五招里有明确更优解，而不总是陨石。
- 三场主线 Boss 的应对不同，且指令栏标出禁用 / 机制。
- 旧三槽存档仍能读；`G.quest` 能迁到 `G.quests`。

---

## 9. 不做清单（汇总）

打包器 / 框架 / ECS / 精灵表管线 / 技能树 / 职业分流 / 队伍战 / ATB / 多敌人站位 / 分支结局 / 声望 / 限时任务 / 事件总线。
